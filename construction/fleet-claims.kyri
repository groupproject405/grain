# construction/fleet-claims.kyri -- the fleet's live claim board: what a ship is building, right now.
#
# WHY THIS FILE EXISTS. Absence is checkable and intent is not. `path_absence_scan.sh` answers
# whether a path stands here and upstream, and it answers about the remote AS IT STOOD WHEN ASKED
# (REDS %457). So a ship reads `absent`, builds for two hours, and comes back to find a peer's
# strictly wider instrument already landed -- with every word of its own reading still true.
#
# THAT IS NOT RARE. IT FIRED TWICE IN ONE DAY, ON TWO SHIPS. On `20260911` incense built
# `port_registry` -- 35 constants, 21 files, 44 pen legs, GREEN on metal -- and withdrew it whole,
# because `%715` had landed `port_band` two hours earlier. Bakery built a port census with a
# 29-leg pen the same day and dropped it into that same peer's scan, writing the sentence this
# board answers: *Nothing said the work was in flight.*
#
# WHAT A CLAIM IS. A declaration of intended work, published before the work exists -- the
# declaration half of `foundations/20260823-222019_what-brix-infuse-is.md` pointed at a fleet
# rather than at a host. It says how things SHOULD be and leaves the present moment alone, which is
# exactly what makes it readable, diffable, and reviewable with nothing built yet.
#
# WHAT A CLAIM IS NOT. Not a lock, and not a gate. Two ships may deliberately build one thing, and
# a reader that REFUSED would be a reader somebody turns off. It is also no promise that the work
# lands -- which is why a claim EXPIRES rather than standing until a hand clears it. A ship that
# dies mid-lap leaves a claim that goes quiet on its own, six hours on.
#
# THE READING THAT ACTUALLY CATCHES A DUPLICATE IS THE SENTENCE, NOT THE PATH. `port_registry` and
# `port_band` shared no path at all. Path overlap is the exact half and it would have missed the
# very case this board was built for; the `what` line is the half a person reads in ten seconds and
# gets right. So every check prints the whole live board, and the hand reads it.
#
# WHY THE BOARD HOLDS ONLY LIVE CLAIMS. This page is bounded at 24,576 bytes like every living pin,
# and a board that only grows is a board nobody reads. A landed claim's record is the commit; a
# withdrawn claim's record is its session log and, where it cost something, its REDS row. The
# history of this file is `git log` -- so clearing a closed claim off the board removes nothing
# that was ever testimony, and accrete-never-break is untouched.
#
# TWO TIME FIELDS, ANSWERING TWO QUESTIONS. `stamp` is the one-clock human record; `epoch` is UTC
# seconds, and age is read from it alone. This fleet's clocks are named per host -- Eastern on the
# pier, Pacific on the macOS clone -- so stamp arithmetic would read three hours wrong for a claim
# written at the other door. One field answering both questions would answer both badly.
#
# THE SHAPE is the standing roster's own: a record opens with `claim` and carries plain key-value
# lines until the next one. Read it with `tools/fixtures/f/fleet_claim_scan.sh` rather than by
# grepping here -- one parse for sh, for Rishi, and for a hand -- and note that the reader reads
# the ANOINTED REMOTE's copy rather than this one. A claim you have written and not pushed is a
# claim no peer can read; the reader says `board=local` when it meets that state, out loud.
#
#   sh tools/fixtures/f/fleet_claim_scan.sh --check <path> ...     # before you build
#   sh tools/f/fleet_claim.sh --open <name> --paths "..." --what "..."
#   git add construction/fleet-claims.kyri && git commit && git push xy
format fleet-claims-v1

# -- live claims -------------------------------------------------------------------------------
# The board opens empty, and the first claim belongs to the next ship that opens a build. Seeding
# it with this lap's own work would be a claim written after the fact, which is the one thing a
# declaration may never be.
claim pheromone-one-ident-rule
seat pheromone
stamp 20260917.001152
epoch 1789618312
paths glow/zig_ident.rye tools/fixtures/g/glow_ident_duplication_scan.sh tools/g/glow_ident_witness.rish
what The Glow lowering room writes one rule 29 times. fn zig_safe_ident stands in 29 glow/lower_*.rye files; stripping the max_ constant name leaves 5 distinct bodies and 17 are byte-identical. The character rule -- first byte alpha or underscore, rest alnum or underscore, hyphen mapped to underscore -- is byte-for-byte the same in all 29, and what varies is what a rule written 29 times comes to disagree about: 23 return usize where TAME asks u32, only 5 carry the invariant assert the other 24 omit, lower_shop_nest.rye answers a LENGTH ceiling with error.MissingFace where 28 peers answer error.BadIdent, and lower_alias.rye maps the dot as well. This is also why glow_ceiling_refusal reads 35 of its 40 uncovered sites under the one name error.BadIdent across five ceilings: the refusal record cannot land once while the function it belongs in is copied 29 times. Publish one zig_safe_ident in glow/zig_ident.rye taking the ceiling as a parameter and a refusal.Slot, returning u32 with its invariants asserted; convert a named first cohort to call it; meter the remaining copies as a ratchet that only falls.
claim patchouli-terminator-as-history
seat patchouli
stamp 20260917.002055
epoch 1789618856
paths mantra/src/diff.rye mantra/src/weave.rye mantra/src/main.rye tools/fixtures/m/mantra_document_roundtrip_scan.sh tools/fixtures/m/mantra_document_roundtrip_control.sh tools/m/mantra_document_roundtrip_witness.rish
what REDS %689, whose blocker just closed: split_lines in mantra/src/diff.rye drops the empty token a text ending in a newline produces, so x\ny and x\ny\n split to two identical lines and earn one address in a content-addressed store -- mantra status calls the changed file clean in both directions. Keaton ruled door 2 on 20260916.000449, a weave records the terminator as HISTORY, and the code was built, proven, then WITHDRAWN because keeping the terminator turns every append into a middle insert, which is what %680 broke. %680 is CLOSED as of 20260916.061223: Diff now carries after, and apply anchors an ordinary insert to the line it comes after, citing %689 by number in its own comment. So the withdrawn work is re-landable. Keep the trailing token in split_lines, pair each insert with its anchor so an append lands after the last real line rather than before the terminator, and read the roundtrip scan's gated and reported legs from both sides.
claim copal-sow-classification-batched
seat copal
stamp 20260917.003556
epoch 1789619756
paths tools/fixtures/s/sow_project.sh tools/fixtures/s/sow_project_control.sh tools/s/sow_project_witness.rish
what REDS %642, the card's first agent-doable item, taken under claim-as-override since my own lane's asks want Keaton's word. The booked cure is a content cache; measured on metal first, the cost it is named for is PROCESS SPAWN rather than work. sow_project.sh runs three greps per candidate -- armor, identity, and the ssh-key read on each copy -- which the timing paper prices at 56.69, 103.10 and 55.79 seconds over 9,046 files. Timed here over a 500-file sample: 6,624 ms per file against 186 ms batched, a factor of 35. So the repair is to classify in three batched passes before the loop -- partition the candidate list into armor-bearing, identity-bearing and plain, scrub and copy from those lists, then one batched ssh-key pass over the copies -- keeping every verdict, every withhold and every byte identical. Parameterize MANIFEST, SEED and SCRUB by environment so a pen can drive the real script, write a control that plants every branch on a synthetic field, and prove byte-identical parity against the elder projection on the real tree before claiming anything. No cache, no new state, no staleness window, and custody gate 1 untouched.
claim grass-backticked-path-promise
seat grass
stamp 20260917.015238
epoch 1789624358
paths tools/fixtures/b/backtick_path_scan.sh tools/b/backtick_path_witness.rish tools/fixtures/b/backtick_path_control.sh
what A backticked path in a living page is a promise no guard reads. tracked_link_scan gates every relative Markdown LINK in a living document, and link_text_promise walls a link's anchor text at zero, so both halves of a link are held; a path written in bare backticks with no link around it is held by nothing. The commit-msg hook already carries this tree's own recognition rule for exactly this shape -- a slash, and an extension this tree writes, asked of the filesystem -- and applies it to commit BODIES alone, so the same promise is checked where it can never be edited and unchecked where it can. Measured over 1,289 living tracked .md and .mdc pages, excluding dated basenames, archive/date/yonder shelves, the append-only REDS and CHECKPOINTS ledgers, and any span that is a link's anchor text: 130 broken citations across 45 pages. context/TAME_GUIDANCE.md cites tools/foo.rish and docs/TIGER_STYLE.md; .claude/rules/lindy-first-crux.md cites ../counsel/... where the room sits two levels up. Build the scan, classify placeholder shapes and gitignored build outputs apart from genuine stale citations, report as a ratchet under a ceiling that only falls, and prove it from both sides in a pen.
claim bakery-mute-assert-census
seat bakery
stamp 20260917.020815
epoch 1789625295
paths tools/fixtures/a/assert_evidence_scan.sh tools/a/assert_evidence_witness.rish tools/fixtures/a/assert_evidence_control.sh
what REDS %734's free half. Its cause -- two concurrent rye builds sharing ephemeral .zig shadow names -- is captured and its repair is a compiler-bridge decision wanting Keaton's word. The row names a SECOND fault nobody owns: three of its thirteen captured failures said only 'Lattice build failed', because the assert discards ${build.err}, which is why a fortnight of these left no evidence to read. Every Rishi .ok comes from run -> { out, err, code, ok }, so the evidence always exists and the cure is one interpolation the tree already writes 1,708 times. Measured over 2,571 tracked .rish sources: 10,237 asserts on a run's .ok, of which 1,708 name their own .err or .err_brief, 462 name their own .out or .code, 550 point at a file on disk, and 7,517 are MUTE -- a fixed sentence with the run's own words discarded. On build asserts alone the split is 744 naming the error against 720 mute, so half the tree applies a cure the other half omits. Build the static census reading tracked bytes alone, classify the four answers apart, report mute_asserts as a ratchet under a ceiling that only falls so each lane sweeps its own files, prove it from both sides in a pen, and sweep only the cohort %734 itself names. No sweep of a peer's files, no shared file touched.
claim bakery-lattice-refusals-say-why
seat bakery
stamp 20260917.022248
epoch 1789626168
paths tools/l/
what The sweep half of the census above, bounded to the cohort REDS %734 itself names. Its thirteen captured failures split ten that carried their reason -- unable to load pan.zig -- and three that said only 'Lattice build failed', which is why a fortnight of them left nothing to read. Forty tools/l/lattice_*_witness.rish files each carry two mute asserts, on build and on selftest, for 80 sites; every one binds its record with run, so err_brief is present and the cure is one interpolation. Append the record's own err_brief to each message, leave every other word standing, and lower the assert_evidence ceiling 7,483 to 7,403 in the same commit.
claim diffuser-cold-read-without-charging-the-pier
seat diffuser
stamp 20260917.022840
epoch 1789626520
paths tools/rye/page_evict.rye tools/p/page_residency_witness.rish tools/fixtures/p/page_evict_control.sh active-designing
what The sixfold library-read disagreement my own paper opened and declined to settle: Bakery reads rye/lib/std at 145-154 ms, my native arm at 26, walks agreeing. The named falsifier was echo 3 > /proc/sys/vm/drop_caches, which I refused to run because it charges seven peer ships for my measurement. The premise that refusal rested on is wrong, and one stat settles it: each ship carries its OWN vendor/zig-toolchain, different inodes on one device, so the 16,416,628 bytes in question are my tree's own files and evicting them costs no peer a page. Linux already exposes per-file eviction as posix_fadvise POSIX_FADV_DONTNEED, which needs no root and touches no other inode, and util-linux fincore reads residency back so the eviction is PROVEN rather than assumed. Build page_evict.rye in Rye with bounded path counts and named errors, read residency before and after, then re-time the library arm cold and warm on the native binary and settle whether the page cache is the whole of the six times.
