# construction/fleet-claims.kyri -- the fleet's live claim board: what a ship is building, right now.
#
# WHY THIS FILE EXISTS. Absence is checkable and intent is not. `path_absence_scan.sh` answers
# whether a path stands here and upstream, and it answers about the remote AS IT STOOD WHEN ASKED
# (REDS %457). So a ship reads `absent`, builds for two hours, and comes back to find a peer's
# strictly wider instrument already landed -- with every word of its own reading still true.
#
# THAT IS NOT RARE. IT FIRED TWICE IN ONE DAY, ON TWO SHIPS. On `20260911` incense built
# `port_registry` -- 35 constants, 21 files, 44 pen legs, GREEN on metal -- and withdrew it whole,
# because `%715` had landed `port_band` two hours earlier. Bakery built a port census with a
# 29-leg pen the same day and dropped it into that same peer's scan, writing the sentence this
# board answers: *Nothing said the work was in flight.*
#
# WHAT A CLAIM IS. A declaration of intended work, published before the work exists -- the
# declaration half of `foundations/20260823-222019_what-brix-infuse-is.md` pointed at a fleet
# rather than at a host. It says how things SHOULD be and leaves the present moment alone, which is
# exactly what makes it readable, diffable, and reviewable with nothing built yet.
#
# WHAT A CLAIM IS NOT. Not a lock, and not a gate. Two ships may deliberately build one thing, and
# a reader that REFUSED would be a reader somebody turns off. It is also no promise that the work
# lands -- which is why a claim EXPIRES rather than standing until a hand clears it. A ship that
# dies mid-lap leaves a claim that goes quiet on its own, six hours on.
#
# THE READING THAT ACTUALLY CATCHES A DUPLICATE IS THE SENTENCE, NOT THE PATH. `port_registry` and
# `port_band` shared no path at all. Path overlap is the exact half and it would have missed the
# very case this board was built for; the `what` line is the half a person reads in ten seconds and
# gets right. So every check prints the whole live board, and the hand reads it.
#
# WHY THE BOARD HOLDS ONLY LIVE CLAIMS. This page is bounded at 24,576 bytes like every living pin,
# and a board that only grows is a board nobody reads. A landed claim's record is the commit; a
# withdrawn claim's record is its session log and, where it cost something, its REDS row. The
# history of this file is `git log` -- so clearing a closed claim off the board removes nothing
# that was ever testimony, and accrete-never-break is untouched.
#
# TWO TIME FIELDS, ANSWERING TWO QUESTIONS. `stamp` is the one-clock human record; `epoch` is UTC
# seconds, and age is read from it alone. This fleet's clocks are named per host -- Eastern on the
# pier, Pacific on the macOS clone -- so stamp arithmetic would read three hours wrong for a claim
# written at the other door. One field answering both questions would answer both badly.
#
# THE SHAPE is the standing roster's own: a record opens with `claim` and carries plain key-value
# lines until the next one. Read it with `tools/fixtures/f/fleet_claim_scan.sh` rather than by
# grepping here -- one parse for sh, for Rishi, and for a hand -- and note that the reader reads
# the ANOINTED REMOTE's copy rather than this one. A claim you have written and not pushed is a
# claim no peer can read; the reader says `board=local` when it meets that state, out loud.
#
#   sh tools/fixtures/f/fleet_claim_scan.sh --check <path> ...     # before you build
#   sh tools/f/fleet_claim.sh --open <name> --paths "..." --what "..."
#   git add construction/fleet-claims.kyri && git commit && git push xy
format fleet-claims-v1

# -- live claims -------------------------------------------------------------------------------
# The board opens empty, and the first claim belongs to the next ship that opens a build. Seeding
# it with this lap's own work would be a claim written after the fact, which is the one thing a
# declaration may never be.
claim pheromone-lexer-head-outside-pairs
seat pheromone
stamp 20260915.225111
epoch 1789527071
paths tools/g/glow_rune_alphabet_worker.sh tools/fixtures/g/glow_rune_alphabet_control.sh active-designing/docs/glow/runes.md
what The four rune bindings all derive their head roster from glow/tokens.rye match_rune2's pairs table, and the lexer recognizes two rune-shaped two-byte heads ABOVE that branch -- == (double_equals, the tall-form terminator, consumed by rune_shape.rye at two sites and standing in 105 tracked .glow sources) and :: (the comment head). == is named in none of the three documents the guard binds while its partner -- is named in all three. Derive the outside heads from the lexer too and publish them as their own reading.
claim bakery-rishi-make-pen
seat bakery
stamp 20260915.233209
epoch 1789529529
paths rishi/src/main.rye tools/r/rishi_make_pen_witness.rish tools/fixtures/r/rishi_make_pen_control.sh
what Rishi already owns a CleanupRegistry that releases on normal return, refusal, exit, HUP, INT and TERM -- the card's standing claim that Rishi has no trap at all is false, and the registry's one kind is empty_dir registered only by acquire-lock. Add a make-pen expression builtin: the runtime creates the directory itself under TMPDIR and registers it for bounded recursive release, so it can only ever remove a directory it made in this process and never a caller-supplied path. Turns the 336 unreleased_on_refusal and the 16 never_removed Rishi pens from a language gap into a one-line cure.
claim pheromone-wire-hit-seam
seat pheromone
stamp 20260916.003208
epoch 1789533128
paths comlink/query_wire_retention_cost.rye
what REDS-first: query_wire_retention has read red on every ship since 0d6e6829a landed two hours ago. That commit made mantra/recall_tablecloth_query_wire.rye max_wire_hits derived rather than the literal 8, and it now computes to 2; comlink/query_wire_retention_cost.rye still spells measured_hit_count as the literal 4, so kept_store[0..measured_hit_count] refuses to compile -- end index 4 out of bounds for array of length 2. The harness already carries the right comptime assert and the wire moved under it.
claim bakery-ryekey-toolchain-spelling
seat bakery
stamp 20260916.023136
epoch 1789540296
paths rye/src/main.rye tools/fixtures/r/ryekey_control.sh
what The build receipt keys on the toolchain PATH STRING beside its content, so one pinned compiler wears four live spellings and four different keys -- measured on this tree: RYE_ZIG absolute gives bdd5d31e, the compiler's own default resolution gives c626631a, same 172MB binary, same sources, same flags. The tree spells the pin 980 times relative, 34 absolute, 13 through a root variable, and leaves it unset everywhere rye_build.sh runs. Drop the path and the library path from the key, keep both content hashes, bump the marker to v6, and turn control leg 8 around with its reason written.
claim patchouli-assert-arrival-delta
seat patchouli
stamp 20260916.024535
epoch 1789541135
paths mantra/src/weave.rye mantra/recall_tablecloth_hit_census.rye tools/fixtures/r/rune_assert_sweep_scan.sh tools/r/rune_assert_sweep.rish tools/fixtures/r/rune_assert_control.sh
what REDS %752: the whole-tree unnamed-assert ratchet reads 6492 against 6487 and names no arrival. Bisected on metal across 32 commits -- BOTH arrivals are mantra, my own lane: 0d6e6829a took it 6487 to 6488 (hit_census) and c4260fcd5 6488 to 6492 (src/weave). All five asserts already carry a TRUE written reason the adjacency rule cannot join: four sit under a 20-line postcondition block that never spells the token, and two are separated from their own invariant comment by exactly one line of code. Repair the five, lower the ceiling to what the repair finds, and answer the instrument door -- give the scan a per-file map so the next breach names its arrival instead of costing a bisect.
claim grass-two-rooms-ceiling-narration
seat grass
stamp 20260916.034104
epoch 1789544464
paths context/TWO_ROOMS.md
what The doorway living ratchet closed at zero on 20260911.160504 (42826c45d), and context/TWO_ROOMS.md still narrates the ceiling falling 59 to 39 with a remainder of 33 active-designing and 6 external-research pages. The scan reads living_silent=0 living_ceiling=0 over 1,328 pages today. The page is the Earth-Fixed rota seat and the doorway's own canon, so a rota lap reads a five-day-stale number as a bare fact once per orbit. Amend with the closing paragraph naming the 39 -- 30 ALES fill captures, 6 lineage silos, 3 more -- the wall's structural reason, and what holds each figure still; grade the page at Field.
claim petrichor-recursion-door-tmp-teaching
seat petrichor
stamp 20260916.043356
epoch 1789547636
paths recursion-prompts/README.md tools/fixtures/p/prose_register_scan.sh
what recursion-prompts/README.md is the highest-cited front door over the 20 percent Door ceiling (24 citers, 23 percent of 26 sentences) and it teaches /tmp/claude_lap.jsonl three times in its Watching a run live section. The living fleet loop tools/f/fleet-loop.sh writes session-output/<seat>.jsonl and <seat>.txt INSIDE the tree, which REDS %549 and %620 seated as law; no guard reaches the README because shared_pen reads runners. Repair the stale teaching, sweep the register under 20 percent, and roster the page on DOOR.
claim diffuser-locality-key-vs-avalanche
seat diffuser
stamp 20260916.050045
epoch 1789549245
paths tools/fixtures/t/locality_key_scan.sh tools/t/locality_key_witness.rish tools/fixtures/t/locality_key_control.sh active-designing/20260916-050000_the-key-that-carries-locality.md
what Row 5's second erratum left exactly one door open: every reading in both torus-fold papers rests on a key that avalanches, and a key carrying locality ON PURPOSE is a claim nobody here has proposed. Build a scan that reads the tree's own tracked files under two keys -- SHA3-512 (the tree's own digest, avalanching) and a locality key derived from path structure -- and measures the SAME two properties against each: adjacency (do same-room pairs land near?) and evenness (chi-squared over the fold). The trade is the finding: avalanche buys evenness and destroys adjacency. Whether a locality key can buy adjacency without losing evenness is the falsifier, and it is answerable on this pier today with no board and no new hardware.
