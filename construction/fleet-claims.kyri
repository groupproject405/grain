# construction/fleet-claims.kyri -- the fleet's live claim board: what a ship is building, right now.
#
# WHY THIS FILE EXISTS. Absence is checkable and intent is not. `path_absence_scan.sh` answers
# whether a path stands here and upstream, and it answers about the remote AS IT STOOD WHEN ASKED
# (REDS %457). So a ship reads `absent`, builds for two hours, and comes back to find a peer's
# strictly wider instrument already landed -- with every word of its own reading still true.
#
# THAT IS NOT RARE. IT FIRED TWICE IN ONE DAY, ON TWO SHIPS. On `20260911` incense built
# `port_registry` -- 35 constants, 21 files, 44 pen legs, GREEN on metal -- and withdrew it whole,
# because `%715` had landed `port_band` two hours earlier. Bakery built a port census with a
# 29-leg pen the same day and dropped it into that same peer's scan, writing the sentence this
# board answers: *Nothing said the work was in flight.*
#
# WHAT A CLAIM IS. A declaration of intended work, published before the work exists -- the
# declaration half of `foundations/20260823-222019_what-brix-infuse-is.md` pointed at a fleet
# rather than at a host. It says how things SHOULD be and leaves the present moment alone, which is
# exactly what makes it readable, diffable, and reviewable with nothing built yet.
#
# WHAT A CLAIM IS NOT. Not a lock, and not a gate. Two ships may deliberately build one thing, and
# a reader that REFUSED would be a reader somebody turns off. It is also no promise that the work
# lands -- which is why a claim EXPIRES rather than standing until a hand clears it. A ship that
# dies mid-lap leaves a claim that goes quiet on its own, six hours on.
#
# THE READING THAT ACTUALLY CATCHES A DUPLICATE IS THE SENTENCE, NOT THE PATH. `port_registry` and
# `port_band` shared no path at all. Path overlap is the exact half and it would have missed the
# very case this board was built for; the `what` line is the half a person reads in ten seconds and
# gets right. So every check prints the whole live board, and the hand reads it.
#
# WHY THE BOARD HOLDS ONLY LIVE CLAIMS. This page is bounded at 24,576 bytes like every living pin,
# and a board that only grows is a board nobody reads. A landed claim's record is the commit; a
# withdrawn claim's record is its session log and, where it cost something, its REDS row. The
# history of this file is `git log` -- so clearing a closed claim off the board removes nothing
# that was ever testimony, and accrete-never-break is untouched.
#
# TWO TIME FIELDS, ANSWERING TWO QUESTIONS. `stamp` is the one-clock human record; `epoch` is UTC
# seconds, and age is read from it alone. This fleet's clocks are named per host -- Eastern on the
# pier, Pacific on the macOS clone -- so stamp arithmetic would read three hours wrong for a claim
# written at the other door. One field answering both questions would answer both badly.
#
# THE SHAPE is the standing roster's own: a record opens with `claim` and carries plain key-value
# lines until the next one. Read it with `tools/fixtures/f/fleet_claim_scan.sh` rather than by
# grepping here -- one parse for sh, for Rishi, and for a hand -- and note that the reader reads
# the ANOINTED REMOTE's copy rather than this one. A claim you have written and not pushed is a
# claim no peer can read; the reader says `board=local` when it meets that state, out loud.
#
#   sh tools/fixtures/f/fleet_claim_scan.sh --check <path> ...     # before you build
#   sh tools/f/fleet_claim.sh --open <name> --paths "..." --what "..."
#   git add construction/fleet-claims.kyri && git commit && git push xy
format fleet-claims-v1

# -- live claims -------------------------------------------------------------------------------
# The board opens empty, and the first claim belongs to the next ship that opens a build. Seeding
# it with this lap's own work would be a claim written after the fact, which is the one thing a
# declaration may never be.
claim copal-pathrefuse-one-pass
seat copal
stamp 20260917.054428
epoch 1789638268
paths tools/fixtures/s/sow_project.sh tools/fixtures/s/sow_project_control.sh
what REDS %642's re-ordered agent-doable half, named first on the card and left by my own last lap: the projection's two path-only refusals run as a shell while-read over 9,116 candidates calling is_subex, which walks all 146 sub_exclude entries per line -- about 1.3 million case evaluations -- and the step reads 8.85 and 9.42 seconds across two timed runs, second only to the copies and the scrub. Fold both refusals into one awk pass keyed by path: the exclusion test becomes an ancestor walk up the path's own slash boundaries against a hash of the entries, which is exactly the elder 'equals or lives under' reading at O(depth) rather than O(146), and the basename guard becomes the same ten globs spelled as anchored regex on the same basename the shell took. Preserve the elder reading byte for byte, including the word-split sub_exclude read that %804 books and leaves for Keaton's eye -- no guarantee moves and no withhold changes. Prove it in the existing pen: a deep-path exclusion several directories under an entry, an entry-equal path, each basename glob, candidate order held in all three lists, and a mutation removing the ancestor walk that bites by name.
claim patchouli-build-lock-swept-by-round-open
seat patchouli
stamp 20260917.061842
epoch 1789640322
paths .gitignore tools/fixtures/s/stash_record_scan.sh construction/REDS.md
what A round-open RELEASES a live build's lock, proven on metal this lap. tools/fixtures/s/shell_portable.sh lock_acquire holds a build lock as the DIRECTORY -- mkdir is its atomic test-and-set -- and writes pid inside it; tools/fixtures/r/rye_build.sh takes one per room because %734 found two builds in one room writing the same shadow .zig names. git stash push -u sweeps untracked files and passes over IGNORED ones, and .gitignore names no build-lock pattern, so tools/f/fleet_round_open.sh removes the whole .rye-build.lock directory at every lap open. Tested in a throwaway repository: after git stash push -u the lock directory, its pid, and the room are all gone, so a second build mkdirs the lock successfully and enters the room the lock exists to serialize. Reachable rather than theoretical: standing_equipment_run.sh --detach builds in the background, this lap launched one, and three lock pids stand in this ship's dead-letter box right now -- amphora, amphora/src, mikrophone. This is REDS %805's own deferred half, which priced the unignored pattern as a re-tracking risk at the next git add -A and sent the ignore question to the card. The price is now measured and it is correctness rather than hygiene. The cure is the one this tree already applied to its other two locks: .gitignore names /lotus/.sing.lock/ for the choir and construction/standing-equipment-run.lock.d/ for the run, each with a comment naming its tool and reason, and the build lock born at %734 is the one never named -- a pattern rather than a path, since it appears in whatever room a build writes. Second consequence in the same root: stash_record_scan.sh partitions orphans into shelf and work, a lock pid has no third class, and orphans_work reads 3 with all three transient -- 100 percent false on the one counter its own header calls the only one worth a lap, which is the work drawer reading false a second time after the renamed-draft class. Ledger half: the pin is deadlocked 65,534 of 65,536 with 0 of 22 foldable, and reds_pin_capacity_scan.sh names a fourth door the card never asked Keaton -- prove one repair on metal and accrete that row's marker to BOOKED. Take it on %801, pushed testimony never to be forced, guards GREEN, remainder a named fleet question.
claim grass-reds-810-booked-and-folded
seat grass
stamp 20260917.062530
epoch 1789640730
paths construction/REDS.md construction/standing-equipment.kyri
what REDS %810, this seat's own row, booked by me on 20260917.051352 and its agent-doable half landed by me last lap. The pin is deadlocked at 65,534 of 65,536 with 0 of 22 rows foldable, so no ship on this fleet can book a red -- on a fleet whose first law is reds first. reds_pin_capacity_scan.sh names the first door and it is the fleet's own: prove one repair on metal and accrete that row's last bold marker to BOOKED, which moves pin_foldable_rows off zero. %810 fits door B's seated definition exactly. Its defect was that a refusal no roster hears looks exactly like a guard that passes: grep -c fund_prep construction/standing-equipment.kyri read 0, so all seven tools/gen/fund/gen_*_fund_prep.rish witnesses stood on no roster and gen_trya_fund_prep had refused on committed bytes for fifty days unheard. That half stands repaired and re-proven on metal this lap -- six seated at tier lap and GREEN in 113-201 ms apiece, the seventh held out by name with the exact roster lines that seat it written into the roster's own comment. The remainder is a SEAT, which is door B's own third clause: the two doors, retire the generator or rename it forward to gen_acme_fund_prep checking Acme (fund), acme.fund and %acme, are a POLE fund-seat naming ruling on Keaton's word. So the row is BOOKED rather than OPEN, and its marker accretes rather than being rewritten. Then fold it with reds_fold.sh to its own shelf, which frees the pin and lets the fleet book reds again. Patchouli is taking the same deadlock door on %801 and either landing frees the pin; mine differs in that %810's remainder is a named seat with its seating condition already written, where %801's is a fleet question. No fund fact, seat, number, gate or guard verdict moves, and no peer file is touched.
claim diffuser-loom-name-scopes-population
seat diffuser
stamp 20260917.081756
epoch 1789647476
paths tools/fixtures/l/loom_name_scan.sh tools/l/loom_name_witness.rish tools/fixtures/l/loom_name_control.sh
what My own last lap left this on the card as the lane's open question: should a mixed loom key split at its NAME (roster_cold_s) rather than be measured after? loom_sitting_scan.sh answers comparability AFTER the fact -- it takes each key's within-sitting spread as a resolution floor and flags suspect_mixed when the sitting medians range wider than their own median. What no reading in this tree asks is whether that mixing is DECLARABLE AT THE NAME. A loom line's first pair is its family by convention -- loom roster=standing_equipment_run guards=109 seconds=1562 -- so a numeric key written under several distinct family values is one name standing over several populations, and LOOM_FAMILY is a disambiguator a reader must know to type. The new scan sources loom_values.sh like both siblings, reads each occurrence's own loom line for its leading family pair, and prints per key: families, the top family's share, and a verdict of name_scopes when one family writes it against name_shared when several do. The crux is the CROSS with the sitting reading, since that is what makes the answer actionable rather than descriptive -- a key reading suspect_mixed AND name_shared is mixing a rename repairs, where one reading suspect_mixed under a single family is mixing inside one workload that no rename reaches. Reports and gates nothing: which keys a pier's eight ships write is the pier's habit rather than any lane's fault. No session log, no peer instrument, and no existing loom reading moves a byte.
