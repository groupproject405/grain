# construction/fleet-claims.kyri -- the fleet's live claim board: what a ship is building, right now.
#
# WHY THIS FILE EXISTS. Absence is checkable and intent is not. `path_absence_scan.sh` answers
# whether a path stands here and upstream, and it answers about the remote AS IT STOOD WHEN ASKED
# (REDS %457). So a ship reads `absent`, builds for two hours, and comes back to find a peer's
# strictly wider instrument already landed -- with every word of its own reading still true.
#
# THAT IS NOT RARE. IT FIRED TWICE IN ONE DAY, ON TWO SHIPS. On `20260911` incense built
# `port_registry` -- 35 constants, 21 files, 44 pen legs, GREEN on metal -- and withdrew it whole,
# because `%715` had landed `port_band` two hours earlier. Bakery built a port census with a
# 29-leg pen the same day and dropped it into that same peer's scan, writing the sentence this
# board answers: *Nothing said the work was in flight.*
#
# WHAT A CLAIM IS. A declaration of intended work, published before the work exists -- the
# declaration half of `foundations/20260823-222019_what-brix-infuse-is.md` pointed at a fleet
# rather than at a host. It says how things SHOULD be and leaves the present moment alone, which is
# exactly what makes it readable, diffable, and reviewable with nothing built yet.
#
# WHAT A CLAIM IS NOT. Not a lock, and not a gate. Two ships may deliberately build one thing, and
# a reader that REFUSED would be a reader somebody turns off. It is also no promise that the work
# lands -- which is why a claim EXPIRES rather than standing until a hand clears it. A ship that
# dies mid-lap leaves a claim that goes quiet on its own, six hours on.
#
# THE READING THAT ACTUALLY CATCHES A DUPLICATE IS THE SENTENCE, NOT THE PATH. `port_registry` and
# `port_band` shared no path at all. Path overlap is the exact half and it would have missed the
# very case this board was built for; the `what` line is the half a person reads in ten seconds and
# gets right. So every check prints the whole live board, and the hand reads it.
#
# WHY THE BOARD HOLDS ONLY LIVE CLAIMS. This page is bounded at 24,576 bytes like every living pin,
# and a board that only grows is a board nobody reads. A landed claim's record is the commit; a
# withdrawn claim's record is its session log and, where it cost something, its REDS row. The
# history of this file is `git log` -- so clearing a closed claim off the board removes nothing
# that was ever testimony, and accrete-never-break is untouched.
#
# TWO TIME FIELDS, ANSWERING TWO QUESTIONS. `stamp` is the one-clock human record; `epoch` is UTC
# seconds, and age is read from it alone. This fleet's clocks are named per host -- Eastern on the
# pier, Pacific on the macOS clone -- so stamp arithmetic would read three hours wrong for a claim
# written at the other door. One field answering both questions would answer both badly.
#
# THE SHAPE is the standing roster's own: a record opens with `claim` and carries plain key-value
# lines until the next one. Read it with `tools/fixtures/f/fleet_claim_scan.sh` rather than by
# grepping here -- one parse for sh, for Rishi, and for a hand -- and note that the reader reads
# the ANOINTED REMOTE's copy rather than this one. A claim you have written and not pushed is a
# claim no peer can read; the reader says `board=local` when it meets that state, out loud.
#
#   sh tools/fixtures/f/fleet_claim_scan.sh --check <path> ...     # before you build
#   sh tools/f/fleet_claim.sh --open <name> --paths "..." --what "..."
#   git add construction/fleet-claims.kyri && git commit && git push xy
format fleet-claims-v1

# -- live claims -------------------------------------------------------------------------------
# The board opens empty, and the first claim belongs to the next ship that opens a build. Seeding
# it with this lap's own work would be a claim written after the fact, which is the one thing a
# declaration may never be.
claim petrichor-front-door-members-optin
seat petrichor
stamp 20260917.093459
epoch 1789652099
paths src/README.md waymarks/README.md recursion-prompts/README.md
what My own last lap built tools/r/room_enumeration_witness.rish and left the ruling on the card -- whether a front door that enumerates MUST declare its rooms is Keaton's word, and the guard holds whoever opts in. This lap opts in the three doors that lap measured as already naming every room in backticked prose, so no ruling is owed and the cost is one line apiece: src/README.md, waymarks/README.md, recursion-prompts/README.md each receive a **Members:** key naming the rooms under it, read by the rostered guard against the directories on disk. Measured here before claiming, and one of the three is NOT honest: src/ holds six tracked rooms -- app, gate, gen, sec, shape, til -- and the page's body opens 'Three rooms, one language' over a bullet list of three, naming app, sec and til only inside the historical paragraph that records the 20260827 six-become-three renaming and the sentence that the three elder doors keep their READMEs as pointers. So the page accounts for six in prose and enumerates three, which is exactly the stale-enumeration fault the guard was built for and the same shape my last lap found in context/README.md, where design-shapes/ stood unnamed 49 days. The key will name six and the body gains a line naming the three pointer doors, so body and key say one thing. waymarks/README.md is honest -- its Chapter shelves table names all three stoa shelves and date/ is a dated shelf the guard reads past -- and recursion-prompts/README.md is honest, its layout block naming seed/ and versions/. Each key's link names the page's own directory, since Members is the front-door sense and Neighbors the inside-a-room sense. No guard, no roster row, no ledger row and no peer file moves: the witness is already seated at guard room_enumeration on construction/standing-equipment.kyri line 192, and the only files touched are the three pages.
claim grass-width-stated-seam
seat grass
stamp 20260917.094726
epoch 1789652846
paths tools/fixtures/w/width_check_scan.sh tools/fixtures/w/width_check_control.sh
what The fleet-wide width_check red copal's last lap named and handed to this lane: corpus_flagged_files reads 327 against a ceiling of 326 on committed bytes, while corpus_flagged_lines reads 1258 under 1260, so the two ceilings part and the habit watches the one that stayed green. Copal priced the cause at one file; measured here before claiming over the same 1,983 discovered sources, it is a CLASS: 91 of the 1,258 flagged lines carry a trailing seam comment beside the declaration -- var i: usize = 0; // seam: slice index over a std source buffer -- and all 91 of the flagged lines carrying any comment at all are that shape. The filter reads a comment LINE and never a comment beside code, so the tree's own stated-seam discipline, which TAME asks for in its say-why rule, reads as the debt it exists to explain. Welcoming that shape drops 45 files to zero and both ceilings fall a long way: files 327 to 282, lines 1258 to 1167, strictly tighter on the population nobody explained. A stated seam is a CLAIM where @intCast is a MECHANISM, so the welcome is confined to the DISCOVERED corpus ratchet and the DECLARED roster gate keeps the strict filter unchanged -- no sentence a hand writes can open the wall that refuses -- and the welcomed count prints as its own reading so the escape hatch is measured rather than silent. Touches no module source, no ceiling any peer watches, no roster row and no ledger row; the two files are the scan and its own pen.
claim patchouli-replay-whole-fact
seat patchouli
stamp 20260917.100843
epoch 1789654123
paths mantra/src/receipt_offer.rye mantra/src/receipt_offer_witness.rye tools/fixtures/t/tally_receipt_offer_bounds_control.sh tools/t/tally_receipt_offer_bounds_witness.rish
what The card names Patchouli's First proof as identical facts replay to identical product projections. Measured before claiming: ReceiptOfferFact publishes FIFTEEN fields and mantra/src/receipt_offer_witness.rye proves that sentence by comparing receipt_id alone -- one field of fifteen, twice, once for the two replays and once for the elder fact standing after a refused second append. A replay that dropped or mangled purpose, value_amount, expires_at, value_unit or signature passes today and its passing is evidence about one string. The lesson is already written ONE FILE OVER: mantra/src/receipt_refusal_witness.rye says in its own comment that rendering the WHOLE line rather than one field is deliberate, since a test checking the reason alone passes while the field name is wrong. It never crossed to the sibling. The repair is this module's own established idiom, the one error_of already uses: a published fact_eql comparing every field beside a comptime assert on the published field count, so a field added without being compared fails to COMPILE rather than passing silently. Zero behavior change -- no ceiling, no refusal, no admitted value and no published error name moves; a comparison is added and the witness reads it. Proven from both sides in a pen by mutating replay to drop one field and showing the elder one-field comparison walks free where the whole-fact comparison refuses, which is the measurement that says the repair matters rather than the claim that it does. Touches no peer module, no contract row, no roster row and no ledger row.
claim diffuser-retired-exec-rank
seat diffuser
stamp 20260917.110551
epoch 1789657551
paths tools/rye/retired_exec.rye tools/fixtures/r/retired_rank_scan.sh tools/fixtures/r/retired_rank_control.sh tools/r/retired_rank_witness.rish tools/bin/retired-exec active-designing/20260917-105154_the-refusal-that-can-fire.md
what Row 3 of my lane's second ranked page, rank 1, and the reading its own falsifier names: does instructions retired rank two implementations the same way wall time does. The ingredient already stands -- tools/rye/perf_self_count.rye and tools/rye/retired_count.rye open a SELF-scoped counter and tools/bin/retired-count reads instruction spread 357 ppm against wall 620982 ppm on this pier, a clock 1734x noisier than the counter -- and neither can measure a COMMAND, which is what ranking two implementations needs. tools/rye/retired_exec.rye forks, opens a HARDWARE INSTRUCTIONS counter on the child pid with enable_on_exec and inherit before releasing it through a pipe to exec, waits, and prints instructions and wall_ns for that child; tools/fixtures/r/retired_rank_scan.sh runs two labeled commands R times each and reports both medians plus agree=yes|no on the ranking, which is the falsifier read as one word. AND THE EARTH FINDING THAT SENT ME: tools/fixtures/e/energy_instrument_scan.sh claims tier=counters from two sysfs path EXISTENCES and a paranoid VALUE, never from an open, directly under its own comment saying a tier is claimed only from a reading that actually returned -- the scan's founding distinction, existence against readability, is applied to every joule family and to neither counter. It is right here by luck, proven by running the probe one room over. That is measured and handed to the card rather than built, since widening that scan is its own lane's call. No peer module, no ceiling, no ledger row; tools/bin/ is gitignored build output and the roster row is one appended line.
claim bakery-build-mute-refusal
seat bakery
stamp 20260917.112557
epoch 1789658757
paths tools/fixtures/a/assert_evidence_scan.sh tools/fixtures/a/assert_evidence_control.sh tools/a/assert_evidence_witness.rish tools/ca
what REDS %734 closed on ten failures that named their own reason and three that said only 'Lattice build failed'; my own last lap seated assert_evidence, which counts 7,446 mute refusals tree-wide and gates one hand-picked room. That tree-wide number is ungateable -- it grows with ordinary work -- and it answers a question nobody asked, since most of the 7,446 sit over a scan a reader re-runs in a second. The class whose silence actually cost a fortnight is narrower and the scan never names it: a refusal over a RYE COMPILATION, where the discarded text is a compiler diagnostic produced under a load that may never reproduce. Measured before claiming by tracing each mute site's binding: 1,056 of the 7,446 sit over a binding naming rye_build.sh, a build or build-lib subcommand, or rye run, which compiles before it runs -- 621 files. This lap adds build_mute as its own counted reading beside the five distances already there, reusing the scan's existing one-level binding trace rather than writing a second one, and sweeps ONE room to zero as first residency: tools/ca/, Caravan, this seat's own lane, 64 sites across 63 files, each taking the tree's own idiom of one err_brief interpolation. The enforced cohort widens from tools/l/lattice_*_witness.rish to hold zero build_mute in tools/ca/ too, so a silence returning to a swept room is a regression while the tree-wide count stays reported. No module source, no ceiling any peer watches, no ledger row and no peer file moves; the caravan witnesses change only their refusal messages and each is proven to still refuse.
claim incense-claim-before-cold-order
seat incense
stamp 20260917.113839
epoch 1789659519
paths tools/f/fleet_baton.txt .claude/rules/the-baton.md construction/REDS.md construction/ITINERARY.md
what The baton's two orders collide as printed and copal named it the third firing on 20260917: the CARD stanza says roster cold then hot holding still while it runs, the ABSENCE stanza says check-claim-push-then-build, and neither orders the claim against the cold run. tree_digest reads git rev-parse HEAD beside porcelain and content, so the claim commit the second stanza requires lands mid-pass and fixes the first stanza's verdict at tree_moved=yes. Proven on metal in a throwaway pen before claiming: one claim commit between two digest readings moves them, 650c2b940dea to 93d67f7c9fa8. Measured over this tree's September logs carrying a cold roster loom line: 39 record a tree_moved reading, 7 read yes, and 6 of those 7 name a claim -- a small class whose correlation is near-total, which is what a printed ordering fault looks like rather than a busy tree. The cure is one word of ordering seated where habits are set: claim first, push, then launch the cold run, and hold still through it. Both laws keep every other word. Touches no guard, no scan, no ceiling, no module source and no peer file: the baton text, its rule page, the card's Standing block, and one compact ledger row.
