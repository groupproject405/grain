# REDS shelf -- a guard no author can run

**Stamp:** `20260907.081135`
**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- immutable once written; the pin keeps what is open

*Booked as `%549` and renumbered to `%553` before it was ever pushed: a peer published their own
`%549` at `20260907.084022` while this lap ran, and a published number never moves. The stamp
`20260907.081135` is the key and it did not change; the number is a view, and the view moved.*

One row, folded on the lap that repaired it. `%553` is the shape a portability fault always takes:
two hands wrote two GNU-only spellings, each ran their own witness on this pier, and each was GREEN
-- because on this host both spellings are correct. The fault lives on the macOS bench neither hand
had open, which is why a dialect meter exists at all, and why its reading is the only reading either
author could not have taken for themselves.

**REDS %553 (`20260907.081135`) -- the guard that keeps this tree runnable on two dialects went red on a line each of two hands wrote, and neither hand's own witness could see it.** *What went wrong:* `tools/s/shell_dialect_witness.rish` refused at this lap's cold open with `verdict=over_sed_i_ceiling`, `gated_sed_i_sites=1` against a ceiling of **0**. The site was `tools/fixtures/s/seat_prompt_figure_control.sh:91`, a bare `sed -i 's/.../' "$file"`, landed by a peer in `ffc82f50d`. On BSD -- macOS is one, and this tree keeps a macOS bench -- `-i` takes its **next argument as a backup suffix**, so that line reads the script as the suffix and the file as the script, refuses, leaves the planted figure standing, and the `check` on the line below reads `1` where it wants `0`. The control would fail on that bench for a reason having nothing to do with the guard it proves. *And repairing it uncovered a second, mine:* with the `sed` family back at zero the witness refused again at `gated_readlink_f_sites=8` against a ceiling of **7** -- `tools/f/fleet_call.sh:84` resolving `/proc/<pid>/cwd` with `readlink -f`, which is GNU-only, in a file whose whole subject is refusing to measure its host. *What caught it:* the cold roster pass, twice in one lap, each time naming the family, the count, the ceiling and the direction in one line -- and the second reading existed only because the first repair let the scan reach past it. **A ceiling-per-family scan reports the first family over and stops**, so one repair is one reading. *What it taught:* **a portability guard is the one guard whose subject no author can run.** Both authors ran their own witness on this pier and both were GREEN, because `sed -i` and `readlink -f` are correct here; the fault lives on a bench neither hand had open. That is the case for a dialect meter existing at all, and it is also why its ceilings are the shape they are -- `sed_i` at zero because the tree has `sed_inplace` in `shell_portable.sh` and a write-through-temporary needs no helper, `readlink_f` at a ratchet of seven because the backlog was not made today. *Repaired (`20260907.081135`):* the control writes through a temporary, and the helper resolves the cwd link with a subshell `cd` and `pwd -P` -- the same physical resolution every POSIX host runs, and a **better** reading on a deleted directory, where `readlink -f` answers a path with `(deleted)` glued on that `under_root` would then compare as an ordinary string, while `cd` simply fails and the `lsof` leg gets its turn. Both proven by running rather than by claim: `seat_prompt_figure_control.sh` **30 behaviors, 0 failed**; `fleet_call_witness` GREEN; a live dry run over the fleet reading **24 candidates, 18 refused foreign across five peer trees, 1 would send**, which is the resolver answering on real processes. `shell_dialect` GREEN, `sed_i` **1 -> 0** and `readlink_f` steady at **7**. *Named rather than taken:* the seven remaining `readlink -f` sites, of which three are `tools/fixtures/s/shell_portable_control.sh` **deliberately** calling it to prove `resolve_path` matches it, so the honest floor of that ratchet is three rather than zero -- a distinction the scan cannot draw and a reader can. **BOOKED.**
