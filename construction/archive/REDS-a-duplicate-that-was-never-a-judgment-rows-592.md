# REDS -- a duplicate that was never a judgment

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by a control on real pages
**Folded:** `20260907.215529` from [`../REDS.md`](../REDS.md)

One row, folded to hold the pin under its bound the hour it was written. It teaches that a count
can hold two classes at once: a tool built to end twelve hand repairs refused the thirteenth
because it refused every duplicate stamp on sight, and thirty-eight of the forty-two duplicates in
this room's history were byte-identical rows a rebase had re-applied -- which is arithmetic rather
than judgment. Read it beside [`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %592 (`20260907.211709`) -- the tool built to end `%440`'s hand repairs refused the very next firing, because it refused every duplicate stamp on sight before looking at one.** *What went wrong:* `tools/fixtures/i/index_shelf_repair.sh` landed this morning to end twelve hand repairs, and exited `verdict=duplicate_stamps` whenever `rows_duplicate` was nonzero, ahead of any inspection, reasoning that "which of two rows to lift is a hand's judgment." The thirteenth firing came at my cold open the same evening: `session-logs/date/README-index-20260907.md` carried **three duplicate stamps and two misordered rows**, so the tool refused and a hand sorted 110 rows again. **All three duplicates were byte-identical.** *What caught it:* `index_row_bound` red at the cold open, then reading the refusal's own reasoning against the rows it refused over. *What it taught:* **a duplicate stamp is two classes wearing one count, and only one is a judgment.** Measured over the git history of this room's three newest shelves: 20 revisions carried duplicates, **38 stamps were byte-identical rows against 4 that were not**, and all 4 were one shape -- a REDS number the derived spine renumbered under the row (`%505` against `%507`). An identical duplicate is one log's row re-applied by a rebase, so lifting either copy leaves the same page and the repair is provable rather than trusted. *Repaired:* the tool classifies, lifts the identical class, and refuses the divergent one **while printing both rows**, since a hand asked to choose must see what it is choosing between. The postcondition keys both sides by set when copies are lifted and by multiset when none are, so **one gate** covers both and stays the leg shown to do the stopping. Replayed against the real firing it writes a page **byte-identical to the hand repair**. The scan's duplicate branch fires ahead of the misordered one and named no remedy either, so today's shelf printed a number with the command one branch down -- the `%528` lesson, unlearned one branch over. Control **24 -> 39**, GREEN. **CLOSED**
