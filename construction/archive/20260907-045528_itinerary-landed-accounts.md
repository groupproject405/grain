# Landed accounts folded from the operator card -- `20260907.045528`

**Language:** EN
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded by:** BAKERY, closing `20260907.045528` (the under-named map row)
**Where this sits:** the living card is [`../ITINERARY.md`](../ITINERARY.md); the ledger is
[`../REDS.md`](../REDS.md).

The live front holds what is OPEN and what waits on Keaton's word. An account that has landed folds
here, whole, so the card stays a card. Residues that are still live ride forward on the card rather
than resting here.

## BAKERY -- a guard that counts today's logs reddens the fleet at every midnight

`20260907.003911` CLOSED, folded at
[`REDS-a-guard-shaped-by-the-clock-rows-533.md`](REDS-a-guard-shaped-by-the-clock-rows-533.md).

The first cold pass of `20260907`, at `00:12`, read `guards_red=2` on ONE root -- `rota_declared`
refuses before the day's first log lands, and `standing_equipment` reads its own roster's red. **A
red guard withholds the roster receipt**, so `--scoped` refuses and every ship pays a FULL cold
pass (961s here) for a fact about the clock. Repaired by `capability day_shelf`, the fourth arm,
reading `ROTA_DAY` and `git ls-files` exactly as the guard's own scan does -- **both halves**, since
a shelf whose only log is untracked is what a filesystem-only probe would call present. Ten
behaviors in a real git pen; `%493`'s second half already drops the elder card row.

**A daily red is worse than a standing one** -- it heals by breakfast, so nobody books it.

**Carried forward, still live:** whether any other rostered guard is shaped by the clock this way
is unmeasured.

**Also recorded in that account:** three landed accounts folded to
[`004225`](20260907-004225_itinerary-landed-accounts.md) -- `%516`/`%517`/`%510`,
`%499`/`%503`/`%501`, `%515`; residues ride there. The pin closed 13 bytes under, which the account
named as a judgment call charged to whoever opened next.
