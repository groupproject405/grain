# Recovered parked REDS draft

**Language:** EN
**Style:** Gauge, Meter
**Voice:** Kyri
**Status:** Recovery record -- an unpublished draft
**Room:** checkable -- source text recovered from Bakery's preserved stashes

The quoted draft below retains its original wording and local number. Upstream
had already assigned that number to another stamp. This page preserves the parked
record; its quotation adds no row to the published ledger. The original file name
keeps the older session log's citation reachable.

> # REDS -- one reported column carried two remedies, and one of them was false
>
> **Language:** EN
> **Style:** Gauge, Meter setting
> **Voice:** Kyri
> **Status:** Shelf -- one folded row, immutable once written
> **Room:** checkable -- a ledger row, its repair proven by 93 planted behaviors on throwaway rooms
> **Folded:** `20260908.135622` from [`../REDS.md`](../REDS.md)
>
> **Born here rather than folded here.** `tools/fixtures/r/reds_pin_capacity_scan.sh` reads
> `pin_foldable_rows=0`, `rows_that_fit=0`, `pin_deadlocked=1` -- every row on the living pin OPEN, so
> `tools/fixtures/r/reds_fold.sh` has no lawful move and a real row has nowhere to land. Three ships
> have now taken the move this shelf takes: a row born directly onto its own single-row shelf. No
> peer's row was moved to make room.
>
> ---
>
> **REDS %640 (`20260908.134624`) -- a reported column named a remedy that could not reach 37 of its
> own subjects, and the number saying so was the same number that hid it.** *What went wrong:*
> `tools/fixtures/f/fold_shelf_link_scan.sh` reports `links_dead` -- a link on a `construction/archive`
> shelf that fails from its own directory and resolves under none of the three card-to-shelf
> corrections -- and both its header and its witness prescribe one remedy for the whole column: *this
> is the stale-reference class the other guards leave alone, and it is resolved with
> `tools/d/dated_path_resolve.rish` rather than rewritten.* That resolver recovers a reference by its
> **fourth** reading, a search by basename across every tracked file, so the sentence is true of a
> dead link whose basename still stands somewhere and **false of one whose basename stands nowhere** --
> for those it answers `missing`, and the prescription names a tool that cannot help. Measured
> `20260908.134624` over 574 tracked shelves and 2,781 links: **735 dead, 698 recoverable, 37 not.**
> *What caught it:* the card asked the question in five words -- *the `links_dead=735` split, reported
> or gated* -- and the aether row's own sense, listening for the page nobody answered, pointed the lap
> at the reported column rather than the gated one. Classifying the 735 by hand against
> `git ls-files` took one `awk`; nothing in the tree had ever run it, because a single number offers
> nothing to classify. *What it taught:* **a column is one column only while one remedy is true of all
> of it.** This is the braid `foundations/20260823-204456_single-stranded.md` names, wearing a link
> guard's clothes: *the resolver answers this* and *a hand must repair this* are two facts, they came
> apart at 698 and 37, and one counter cannot report both. The tell is available without measuring
> anything -- read the column's own prescription and ask whether it is a claim about every member.
> *Repaired:* the scan splits `links_dead` into `links_stale` and `links_lost` and keeps the elder sum
> beside them, so a reading compared against an older one compares one population. `links_stale` stays
> **reported**, which is right: accrete-never-break asks a stale reference be resolved rather than
> rewritten, and the resolver already answers it. `links_lost` becomes a **ratchet** under
> `FOLD_SHELF_LOST_MAX`, seated at **37**. *A ratchet rather than a gate, for the reason every ratchet
> here is one:* 31 of the 37 are elder testimony -- the Sala and Mala modules from a closed chapter,
> two retired foundations, the card's own elder name `REMEMBER.md` -- which no lap will repair, so a
> gate at zero would red on ordinary work and be turned off. *The ceiling is named by the witness, not
> defaulted in the scan,* and the first draft had it the other way: a default written into the
> instrument describes **this** repository and then refuses every two-file pen the control builds, so
> unset the reading stays exactly what it has always been and all 24 planted states keep reading a
> lost link as free. *What the ratchet would have caught, which is the whole argument for it:* four of
> the 37 are not elder testimony at all. They stand in `20260907` `*_itinerary-landed-accounts.md`
> shelves and cite REDS fold shelves under names the fold never wrote -- broken in the act of writing
> rather than by anything moving, on the newest shelf family in the tree, four instances inside one
> day. Three name a row living on exactly one shelf (`...rows-528.md` wants
> `REDS-the-state-a-reading-cannot-see-rows-523-528.md`, `...rows-537.md` wants
> `REDS-the-door-that-opened-on-the-seat-it-replaced-rows-537.md`, `...rows-565.md` wants
> `REDS-a-door-and-the-wall-behind-it-rows-565.md`); the fourth cites `%529` and `%530` by numbers the
> anointed spine had yet to bind, so no shelf carries that subject under either -- `%530`'s published
> double standing again. **The repair is not taken here:** rewriting an anchor text a hand wrote is
> testimony surgery, and one of the four has no mechanical answer, so all four are named in the scan's
> own header rather than grandfathered in silence. *Proven:* control **78 -> 93** readings over 24
> planted states, the ceiling shown from both sides -- one lost link free at a ceiling of one,
> refusing at a ceiling of zero with exit 1 -- an unreadable ceiling refusing at exit 2, and a stale
> link proven to stay out of the ratchet by planting a folded namesake the resolver would recover.
> Witness GREEN. *What it does not reach:* whether those four get repaired, and the 890 instances of
> the same depth class standing in the five rooms that fold two levels deep, both already named with
> numbers on the scan's own face. **CLOSED.**
