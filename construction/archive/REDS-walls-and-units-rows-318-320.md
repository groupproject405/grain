# REDS shelf -- rows %318, %319, %320

**Language:** EN
**Folded:** `20260828.160533`, when the pin read 26,629 bytes against its declared 24,576
**Voice:** Kyri
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)

Three closed rows from one afternoon, each about a wall that watched the wrong moment or spoke
the wrong unit. %318 found two living pins over the bound their own headers declare, shipped for
commits on end, because the only guard weighing them runs at a lap's open -- the repair is a
commit-time wall that weighs every touched pin off the index. %319 found one blanket re-include
in the ignore file reopening every ban above it for one room, rescuing nothing the plain form
did not already carry. %320 found the ascii-comment meter counting an em dash as one character
on the Linux benches and three bytes on this Mac, so one tree carried two readings against
ceilings that only ever met one of them -- the repair pins the C locale and counts UTF-8 lead
bytes, which is characters, under every awk.


**REDS %318 (`20260828.160016`) -- two living pins crossed the bound they declare and shipped, because the only guard that weighs them runs at a lap's open and stops at the first one it finds.** *What went wrong:* `construction/ITINERARY.md` stood at **24,579 bytes** against the **24,576** its own header declares, and `construction/REDS.md` at **28,568** against the same bound. The card crossed at `c63c493bc` and rode 3 bytes over for **14 commits**; the ledger crossed at `8b6daf6f5` and **4 commits** shipped across it. Exactly one guard reads that bound -- `tools/equinox/witness/equinox_e123_living_pin_guard_witness.rish` -- and it sits on the cold roster, which a lap opens before touching anything and which has not finished inside a lap on this bench for three consecutive Silence laps (past 40 minutes, then past 50, then still running at 14:31 while this row was written). On the pass that did read it, a second fault hid behind the first: the scan's per-pin loop calls `exit 1` on the first `enforce` row over bound (`tools/fixtures/equinox_e123_living_pin_guard_scan.sh:196`), so it named the card's 3 bytes and never weighed the ledger's 3,992. *What caught it:* `wc -c construction/ITINERARY.md` against the number written in that file's own header, run by hand before appending to it, on a lap whose subject was something else entirely. *What it taught:* **a bound checked at a lap's open is a bound nobody checks at the moment it is crossed.** REDS %293 wrote this reading into its own third field on `20260827` -- *any round whose diff touches a path under `living_pin_max_bytes` runs that guard before it sends*, together with the mechanical form, `git diff --cached --name-only` piped against the pin roster -- and then closed without building it. It fired again the next day on two pins at once, which is a loom rather than a lantern. The other half is %232's shape from a different door: **a guard that exits at its first refusal reports one fault where there are two**, so the louder violation hides the quieter one, exactly as e123 dying under `set -eu` once took e125 and e126 down with it before either could print a reason. *Repaired (`20260828.153737`, `f7da0ece4`):* `tools/fixtures/pin_bound_touch_scan.sh` reads the round's own touched set from `git diff --cached --name-only`, matches it whole-line against `tools/fixtures/living_pin_guard_roster.txt`, takes each touched pin's size off the **index** with `git cat-file -s :<path>` rather than off disk, and asks `tools/fixtures/living_pin_max_bytes.sh` for that path's own bound, so no caller spells the constant and the `session-logs/README.md` exception is honored. It weighs **every** touched pin before it answers. `tools/hooks/pre-commit` gains a RULE THREE that calls it and refuses the commit, printing path, bytes, bound, and overage on stderr; it refuses rather than regenerating, because a pin's overage is prose a person has to choose to spend. This is not a second roof over the elder -- **one roster file, one bound reader, a different moment**: e123 stays whole and slow, and this one is partial, instant, and free on any round that touches no pin. *Proof:* `tools/fixtures/pin_bound_touch_control.sh` proves **36 cases** on real git repositories in a throwaway pen -- every refusal shown from both sides, the boundary met at the bound and again one byte past it, the index preferred over the disk in both directions, an untouched over-bound pin correctly ignored, and **5 cases that arm `tools/hooks/pre-commit` the way a clone arms it and run real commits through it**, since a wall proven only through its own scan is a wall nobody hung. `rishi/bin/rishi run tools/p/pin_bound_touch_witness.rish` GREEN on metal at `20260828.155600`, `cases_failed=0`. Both pins read under bound now -- the card at **24,571** and the ledger at **17,451** after the peer's fold. **CLOSED.** *Standing, and not closed by this row:* the first-exit blindness is repaired only in the new reading, never in e123 itself, so a whole-tree pass still names one pin and stops.

**REDS %319 (`20260828.160342`) -- one blanket re-include reopened every ban above it, for one room, and rescued nothing.** *What went wrong:* `.gitignore:344` read `!nixos-guide/**`. Git lets the **last matching pattern decide**, so a recursive re-include sitting below the macOS-Finder block (line 242) and the `*.zig` ban (line 232) cancelled both inside `nixos-guide/` -- the one room written that way, against 79 admitted by the plain `!/room/` form. The visible cost was one line of `git status`: `?? nixos-guide/.DS_Store` untracked on this Mac bench while `.gitignore` names `.DS_Store` a hundred lines above. The unseen cost was the whole family -- `._*`, `.AppleDouble`, `.Spotlight-V100`, any stray `*.zig` -- one `git add -A` from the tree in that room. *What caught it:* `git status --porcelain` at a lap's open, read rather than skimmed, then `git check-ignore -v nixos-guide/.DS_Store`, which names the pattern that won and its line. The tool answering *which rule decided* found in one call what the tool answering *is this ignored* would never have raised. *What it taught:* **a rule written to let something in reaches everything the rules above it kept out.** The comment beside it -- *the guides home is source, not build output* -- names a real intent, and `**` was reached for as the strong form of it; nothing measured whether the plain form already sufficed. Same shape as %301 (rooms discovered by a property only some rooms have) and %317 (a call graph read through a token position only some calls use): sound in the direction someone tested, blind in the direction nobody did. *Repaired (`20260828.160342`):* the line is deleted, its intent kept in the comment above `!nixos-guide/`. *Proof, both ways, on real git repositories in a throwaway pen:* the room's **12** tracked files plus three planted droppings -- `nixos-guide/.DS_Store`, `nixos-guide/templates/.DS_Store`, `nixos-guide/scratch.zig` -- run under the ignore file with the line and without. **Without: 12 of 12 seen, all three droppings refused. With: the same 12, plus all three droppings.** The blanket rescued not one file, since `!nixos-guide/` alone already carries the room. On the live tree after the cut: `git ls-files -i -c --exclude-standard` reads **0**, so no tracked file anywhere became ignored; `nixos-guide` still lists its **12**; and of **83** `.DS_Store` files on this bench's disk, **0** now reach `git status`, where one did. **CLOSED.** *Named, not taken:* nothing meters a re-include broader than the room it admits, so the next enters the same way. A scan weighing each `!` rule's reach against the paths it actually rescues is its own lap.

**REDS %320 (`20260828.160533`) -- one meter, two units: an em dash counted 1 on the Linux benches and 3 on this Mac, and the ceilings only ever met one of them.** *What went wrong:* `tools/fixtures/rye_comment_ascii_scan.sh` and `tools/fixtures/shell_comment_ascii_scan.sh` counted non-ASCII in comments with a bare `awk` negated class, `[^\001-\177]`. GNU awk in a UTF-8 locale iterates CHARACTERS, so an em dash counted once; the BWK awk both macOS paths ship iterates BYTES, so the same em dash counted three times. The ceilings -- 4,333 for Rye, 505 for shell -- were measured under character semantics, so on this bench the living readings came back **11,405 and 1,358**, 2.6x over walls that were honestly green on the benches that set them, and the four-file control expected `chars=4` where this bench read 12. The shell scan's own header claimed *"THE UNIT IS A CHARACTER, not a byte. This awk reads UTF-8 text"* -- true only of the awk the author happened to have. *What caught it:* the cold roster's `ascii_comment` red on this bench, then the control run alone, whose `files=4 chars=12` named the ratio, and one line -- `echo '—' | awk` reading `len=3` -- named the mechanism. *What it taught:* **a count's unit is part of the claim, and a locale-dependent unit makes one tree carry two readings.** Sibling of %278 (a GNU `grep -P` no BSD grep runs) and of the xargs row %249: the third time a GNU-ism shipped as if it were the tool's name. The deeper habit: a ceiling is only comparable to a reading taken in the same unit, and nothing in the meter said which unit it spoke. *Repaired (`20260828.160533`):* both scans now run `LC_ALL=C awk` -- pinning BOTH dialects to byte iteration -- and count the class `[\300-\377]`, the UTF-8 LEAD bytes. Every non-ASCII character carries exactly one lead byte, so counting lead bytes counts characters, deterministically, under every awk; both unit comments now say so and say why. *Proof:* both controls GREEN with their original expectations untouched -- `total_is_four=yes` and `total_is_seven=yes` are simply true here now -- and the living readings landed EXACTLY on their ceilings, `4333 against 4333` and `505 against 505`, the same numbers the Linux benches measured, which is the fix proven by coincidence-that-is-no-coincidence: one unit, every host. `rishi/bin/rishi run tools/as/ascii_comment_witness.rish` GREEN on metal, twenty planted readings, the shell ceiling proven from both sides. **CLOSED.**
