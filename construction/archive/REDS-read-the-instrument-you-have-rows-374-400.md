# REDS %374, %400 -- read the instrument you already have

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260904.184844` to bring it back under
its bound as `%408` and `%409` landed. One **CLOSED**, one **BOOKED**, and one lesson between them:
**the answer was already being printed beside the hand that guessed it.** `%400`: a repair's own
prose said 107 dated session logs where the scan beneath it printed `prose_skipped=13`, and the
figure was the one part of that repair no witness measured -- Gauge asks every figure to carry its
unit, date and source, and *the instrument beside it* is the strongest source a comment can have.
`%374`: the roster runner counted *this guard broke* and *this guard is parked at a gate the
maintainer named* under one name and gated the receipt on their sum, so a tree whose reds all sat at
custody gates could never earn the cheaper pass -- and the run card it wrote had been saying which
was which the whole time. Granted and built `20260904` on Keaton's word: the roster grows a `gate %N`
qualifier whose vocabulary is read from the living card's own custody section, so a roster can claim
only a gate the card declares. Link depth moved with the text.*

**REDS %374 (`20260830.192524`) -- the receipt is written past the red exit, so a tree whose reds are parked at a custody gate can never earn the cheaper pass, and the refusal told it to run the roster again.** *What went wrong:* `tools/fixtures/s/standing_equipment_run.sh` writes `construction/standing-equipment-receipt.kyri` below the `if [ "$red" -ne 0 ]` branch that already exited 1, so a full pass carrying any red leaves no receipt at all -- and `--scoped` reads that receipt for its basis and refuses without one. On this clone both of the roster's red guards sit at custody gates the living card names, `pond_enclosure_door` at gate %5 and `rule_twin` at gate %7, so every full pass closes `guard_red`, no receipt has ever been written here, and the fusion build's cheaper pass is unreachable by construction rather than by delay. The refusal read `run the full roster once` to a hand who had run it twice that morning, at **2h27m** a pass -- the run card's open stamp `20260830.080600` against its own mtime. The same shape stands one layer up: `tools/s/standing_equipment_witness.rish`, the guard that proves the roster honest, refuses at its scan leg for `runs_red=2` on those same two gated rows, so the instrument that measures the roster is unrunnable on exactly the trees carrying a gate. *What caught it:* this lap's own cold open, which printed `roster_receipt=none` while `construction/standing-equipment-runs.kyri` held a complete 96-guard pass from that morning; both readings cannot be innocent at once. *What it taught:* **one counter carrying two meanings decides for a mechanism that never asked which one it meant.** `red` counts *this guard broke* and *this guard is parked at a gate the maintainer named* identically, and the receipt gate reads their sum -- REDS %338's own class, where OPEN carried both *live defect* and *instances repaired, remainder booked* until the ledger deadlocked on it. *Repaired (`20260830.192524`):* additive readings, no gate moved -- a full pass closing red prints `roster_receipt_write=withheld_guard_red`, and a `--scoped` without a basis prints `scoped_basis_blocked=<guards>` read from the run card, eight names then a count, with a refusal that names them rather than prescribing a pass that cannot help. Six cases in the throwaway pen, three proven to bite under a runner stripped of both lines; the fourth is written down as a companion rather than counted as a refusal it is not. **Whether a receipt may chain from a close whose only reds are custody-gated and card-named is Keaton's word**, since it loosens what a receipt promises -- until then the diagnosis is honest and the cost stands. *Granted `20260904`, Keaton's word, and built the same lap:* the roster grows a fourth qualifier beside `tier`, `host`, and `capability` -- **`gate %N`**, where `tier` is a tier for TIME, `host` for PLACE, `capability` for what a host CAN DO, and this one for **what a maintainer has parked**. A guard carrying it books its red into a separate `gated` counter, so `red` recovers the single meaning it lost: *this guard broke*. A full pass whose reds are all gated closes `run_verdict=ok` and writes the receipt, and the receipt **discloses what it chained past** -- a `gated` count and a `gated_at` line naming each parked guard beside its own gate -- because a receipt reading a bare green after a gated close would promise more than the pass proved, and `--scoped` reads this very file for its basis. Five rows carry the field on this pier: `rule_twin` at %7, three pond guards at %5, `commit_message_guard` at %1, whose red is an untracked-by-design publisher rather than a defect. **The safety is that the vocabulary is not kept in the scan.** This is the one roster field a hand types about its own tree, so the legal gate numbers are read out of `construction/ITINERARY.md`'s own custody section at scan time: a roster can only claim a gate the card declares, retiring a gate on the card retires every row leaning on it in the same edit, and **an unreadable card empties the vocabulary and refuses every gate** rather than welcoming them all. Three refusals hold the field to its meaning and each is proven in a throwaway pen: an ungated red still reds, still refuses, and still costs the receipt; an **absent path is never gated**, since a guard whose file is gone proves nothing whatever its row claims; and a gate the card never declares is refused by name. Fifteen new control legs, every one asked in the direction that would make the field a free pass. The instrument one layer up is freed by the same stroke: `tools/s/standing_equipment_witness.rish` counted the run card's `gated` verdict as red and so was unrunnable on exactly the trees carrying a gate; it reads the word now. **CLOSED** on `tools/fixtures/s/standing_equipment_control.sh`, twenty-one behaviors GREEN on metal.

**REDS %400 (`20260831.095907`) -- a repair's own prose counts the population eight times over, while the instrument beside it prints the right number.** *What went wrong:* the landed repair for `%398` says, in three places -- the scan's header, the control's case 11 comment, and the commit body -- that **107 dated session logs** walked into the citation guard's population when the report card gained `.bron` and `.kyri`. The scan itself prints `prose_skipped=13`. The likely arithmetic is visible in the pre-repair reading it sits beside: `programs_scanned=108` was the whole candidate list, and 108 minus the one file being discussed reads as 107 if the total is taken for the share. The repair is correct and its mechanism is sound; only the figure is wrong. *What caught it:* a second hand arriving at the same repair independently and measuring the population before writing about it. Checked out `ba394dab3b` into a throwaway worktree and ran its own scan: `programs_scanned=95 prose_skipped=13 broken_citations=0`, and `git grep -l -- '](' -- '*.kyri' '*.bron'` reads 13 on that tree. Both readings taken on the repair's own commit rather than on the finder's. *What it taught:* **a number written beside an instrument that prints the same number should be read off the instrument.** The figure is the one part of this repair no witness measures: the control gates the behavior, the scan gates the count at zero broken, and nothing at all reads the population size a comment claims. That is the general shape rather than one slip -- Gauge asks every figure to carry its unit, date and source, and *the instrument beside it* is the strongest source a comment can have. Kin to `%398` in the same file one layer over: that row found a rule copied where it should have been cited, and this finds a reading recited where it could have been run. *And a second instance in the same landing, which is why this is one row rather than a note:* the operator card's own live front reads **94 scanned, 13 prose skipped, 0 broken** where the scan prints `programs_scanned=95`. Same class, same hour, a different number recited beside the same instrument -- and a lantern that fires twice is the shape this ledger exists to catch. *Named, not taken:* all four sites are the landing hand's own prose, so the mechanism and every measurement are written here and the wording is left to its owner. **OPEN.** *Repaired (`20260904.103121`):* the scan header, the in-scan case comment, and the control's case 11 now cite `prose_skipped` rather than reciting a population; this sitting's instrument printed `programs_scanned=95 prose_skipped=15`. The landing commit body stays dated testimony. **BOOKED.**
