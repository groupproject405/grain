# REDS shelf -- row %669, the card advertises and a log quotes

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- one row, folded the hour it closed, its relative links re-anchored one
directory deeper

Folded `20260909.170820` on the send that booked it, the way `%667` was folded the same afternoon.
`construction/REDS.md` had reached 41,958 bytes against the 40,960 its own header declares, and a
row closed the hour it opens has nothing left to do on a pin whose job is to hold what is live.

**One further thing this row learned after it was written, and it belongs here rather than in the
row.** The repair the row records -- dropping ten quoted hex digits from one sentence of a COPAL
account -- was overtaken on the same send: COPAL shelved that whole account in its own next round,
and the sentence went with it. So the card would have come green without this lap. What stands is
the reading rather than the edit: `tools/fixtures/n/nib_honesty_scan.sh` cannot tell a hash a card
OFFERS from one a card QUOTES, eight ships read one card, and the next accurate sentence about a
lost commit will red the fleet again. The row is the record of that, and the guard keeps every bit
of its strictness on purpose.

**REDS %669 (`20260909.170820`) -- the live card spells an advertised hash and a quoted dead one the same way, so a true sentence about a lost commit reads as a broken promise.** *What went wrong:* a COPAL account on `construction/ITINERARY.md` recorded the red it had just closed in its own words -- *the card's nib named `35622f7454` -- a pre-rebase hash no clone holds (%401)*. `tools/fixtures/n/nib_honesty_scan.sh` reads every ten-hex run on the card as a hash the card ADVERTISES, resolves each against the object store, and refuses at `gone=1`. So the sentence naming the dead hash WAS the floating claim, and the account was punished for being accurate. Eight ships read one card, so the red stood on every cold pass rather than on one. *What caught it:* the cold roster pass of `20260909.163434` -- 182 guards, 177 green, `tree_moved=no` -- and then running `sh tools/fixtures/n/nib_honesty_scan.sh` by hand rather than assuming the reading was `%401` recurring. It printed two advertised hashes where the elder red had exactly one, and the surviving one was quoted rather than pinned. *What it taught:* **the live card advertises; testimony quotes.** A hash on the live front is an offer a reader may act on, and the digits of a commit no clone holds are an offer nobody can take. The same digits inside a session log are a fact about a moment, which is what a log is for. The guard stays exactly as strict on purpose: a card that could mark a hash *quoted* would hand every future stale nib that same escape, and the escape would be spelled by the hand whose nib was wrong. *Repaired (`20260909.170820`):* the account keeps its whole finding and drops the ten digits, citing the log that carries them -- `session-logs/date/20260909/20260909-154924_the-record-nobody-carried.kyri`, whose `think` line has held the hash since it was written. **CLOSED**, `nib_honesty` GREEN with `gone=0` on metal.
