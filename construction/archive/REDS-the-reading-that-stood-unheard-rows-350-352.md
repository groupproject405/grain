# REDS shelf -- the reading that stood unheard (rows %350-%352)

**Language:** EN
**Style:** Gauge, Meter setting (see [`../../context/GAUGE_STYLE.md`](../../context/GAUGE_STYLE.md))
**Voice:** Kyri
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Folded:** `20260830.055018` by `sh tools/fixtures/r/reds_fold.sh`

Three rows fold together because each one names the same distance: **the tree had already
produced the reading, and no hand carried it anywhere.** A width gate ran red through a
night with its drift unbooked. A census stood finished and withheld for a month, wearing a
fresh page's clothes. An advisory named its own promotion condition and then grew from five
sites to fourteen while still advisory.

None of the three was a guard that failed to see. Each saw, said so, and was heard by
nobody -- which is a different defect from a blind spot and takes a different repair. A
blind guard wants better eyes; an unheard one wants a shorter path from its reading to a
hand. That is why all three closed by **wiring the reading into something that already
runs** rather than by looking harder.

Rows are reproduced below exactly as the pin carried them, with relative links re-anchored
one directory deeper.


**REDS %350 (`20260830.005741`) -- the width gate ran red for a night while its drift stayed unbooked.** *What went wrong:* the authored-usize corpus crossed its falling ceilings -- 331 files and 1,265 lines against 329 and 1,263 -- so `tools/w/width-check.rish` answered `authored_width_drift` at every roster pass while every seat read the red as another body's booked lap; measurement found no ledger row anywhere, so for a night the fleet's one full-tree width gate refused with nobody allocated to it. *What caught it:* reds-first at a lap open -- the claim "booked to another body" checked against the fetched ledger and found empty. *What it taught:* **a red every seat believes is booked is a red nobody owns** -- the belief substitutes for the row, and the ledger is exactly the instrument that makes ownership checkable rather than assumed. *Repaired (`20260830.005741`):* two one-hit scan indexes on tonight's own touch path -- `brushstroke/tokens.rye` and `brix/infuse.rye`, each a `var sp: usize` wearing a seam comment the markers never granted -- migrated to `u32` with the width law's own said-why, the corpus falling to 328 files and 1,262 lines; the ceilings fell with it to capture the gain, and `tools/w/width-check.rish` answers `verdict=ok` at the new floor with the face witness GREEN over both touched modules. **CLOSED.**

**REDS %351 (`20260830.014444`) -- a census withheld for a month wore a fresh page's clothes.** *What went wrong:* the letter fold of `20260823.144100` filed the fascia health scan's dated control under `tools/fixtures/2/` -- the first character of its stamp -- while `tools/fixtures/f/fascia_health_scan.sh` kept assembling the flat elder path, so the scan answered `detail=control_absent` and `census=withheld` on every run since, and nothing downstream distinguished a withheld census from a passing one. *What caught it:* **Mystery's custody stop on a fascia disagreement** -- its clone read a different grade than the root page and parked rather than guessing, and pulling that one thread here surfaced the withheld census beneath. *What it taught:* **a guard that withholds must be as loud as a guard that refuses** -- `withheld` printed to nobody is `ok` in every reader's eyes, and a fold's repoint sweep must reach paths ASSEMBLED from parts, which no grep for the whole literal can see. *Repaired (`20260830.014444`):* `DATED_DIR` repoints to the folded room with the said-why naming the fold, the census reads live again (`verdict=ok`, 3,307 tools counted), and the root page's fascia 64 is confirmed by generator and direct metric alike. **CLOSED.**

**REDS %352 (`20260830.001115`) -- an advisory that named its own promotion condition grew from five sites to fourteen while its guard read green.** *What went wrong:* `tools/fixtures/s/shell_dialect_scan.sh` counted two GNU-only families -- the in-place edit flag and readlink's resolve flag -- as advisories gated by nothing, and its own witness header set the promotion bar in prose: *a family earns its gate by costing a real reading on a real host; the two still advisory are reported until they do*. Both bars were met and neither family moved. The in-place family's own note in that file recorded five sites measured `20260826.090745`; it read **fourteen** on `20260829`, and all fourteen stood in `pond_enclosure_door_control.sh` and `pond_policy_launcher_control.sh` -- the fixtures of the two guards the macOS bench had been redding on across at least two laps, logged there as *two Pond guards that red on a macOS bench*. Nine new instances of a construct this guard had already named, written into the one lane whose guards break on the bench that construct breaks. Two of the fourteen carried a sharper second fault: a `\n` replacement, which GNU sed reads as a newline and BSD sed reads as the letter n, so on that bench those two plants would have planted the WRONG text rather than refusing -- a case passing vacuously, which is the exact failure this family was booked for. *What caught it:* following the card's one named line -- the GNU-only xargs flag at `pond_enclosure_door_scan.sh:158` -- into its family, and reading the guard's own advisory counters beside it. The guard had been printing both numbers correctly the whole time; nothing was hidden, and nothing was watching. *What it taught:* **a promotion condition written in prose is a condition nobody checks.** It is the sentence this tree keeps relearning one layer up -- a note somebody hopes is read against a thing a program checks (REDS %329, and the `user` mark seated one lap back on the launcher's own unread uid warning). An advisory is honest about what it does not do, and that honesty is precisely what lets a family grow unwatched, so an advisory wants either a ceiling or a named reader, and a ceiling is the cheaper of the two. *Repaired (`20260830.001115`):* both families promoted to gated -- the in-place flag at **zero**, after all fourteen Pond sites took `sed_inplace` from `shell_portable.sh` and the two newline plants took `&` with a literal escaped newline (proven byte-identical on this pier); the resolve flag as a **ratchet at seven**, after the one Pond site took `resolve_path` (it can fall to two and no further today: five sites are the portable helper's own control calling the elder spelling to compare against, each behind a probe that says so when it skips, and a sixth is `tools/ag/agent-jail.sh`, which the Pond quest's accrete-only boundary holds untouched). Both ceilings proven from both sides on planted trees; `tools/s/shell_dialect_witness.rish` GREEN at 25 pass and 0 fail, both Pond controls green, `pond_enclosure_door` and `pond_policy_launcher` GREEN on metal here. **The falsifier is named rather than assumed:** whether the macOS bench's two Pond rows go green is settled by its next run, not by this one. **CLOSED.**
