# REDS -- row %337, folded from the living pin

**Folded:** `20260829.013811` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A repair sized to a meter's reading passed the meter and still died on a clone, because the meter
counts one call and the binary makes three.*

Booked and repaired inside one lap, born on its shelf as %335 and %336 were, since the living pin
stands at 24,571 of its 24,576 bytes and every elder row in it still reads OPEN.

**REDS %337 (`20260829.013811`) -- the meter reads the call a witness makes, and the binary makes two more.** *What went wrong:* `tools/fixtures/w/witness_own_build_scan.sh` counts a witness against the artifacts it names in DIRECT invocation position -- the first quoted element of `run [ ... ]`. Repairing Amphora's eight witnesses to that list gave each one exactly the builds the scan listed, and four of the eight -- `pour_negative`, `carry_negative`, `restore`, `restore_negative` -- still died on an emptied `amphora/bin/`, every one at its first `pour`. They name `amphora` alone in run position, and `amphora/src/main.rye:138` and `:176` resolve `vessel-seal` and `vessel-core` as **sibling subprocesses** at runtime, so the promise a witness makes is wider than the call the scan can see. A second gap rode with it and is the sharper half: `rye build` refuses an absent `-femit-bin` directory with `unable to open output directory`, and the build line already standing in `amphora_first_resident_witness.rish` carried no `mkdir -p amphora/bin` -- so the scan **credited** that pair as self-built while it could not have run on a clone. *What caught it:* running each witness from a wiped directory rather than running them in sequence. The first sweep ran all eight back to back, `pour` built all three binaries, and the seven behind it read GREEN off `pour`'s leavings -- the same borrowed-state green %334 was booked for, reproduced inside its own repair. Emptying the bin before **each** run turned four greens into four honest reds. *What it taught:* **a meter that reads one edge of a promise is a floor on that promise, never the whole of it -- so a repair sized to the meter's list is sized to the wrong number.** The scan says as much in its own `THE HONEST LIMIT` paragraph, from the other side; what was missing is the consequence, which is that satisfying the reading exactly can leave the defect standing. The wider rule underneath: **a credited reading is still a claim, and a claim proven only in the passing direction cannot be told from a bypass** -- the credited `first_resident` build is that sentence wearing a build line. *Repaired (`20260829.013811`):* all eight witnesses carry all three builds and a `mkdir -p amphora/bin` on the first of them; each was proven alone from a wiped `amphora/bin/`, eight for eight, 5 to 6 seconds apiece. The scan's ceiling falls 38 -> 21, its control's planted counts move with it (21 free, 22 refused, both sides shown), and both gaps are written into the scan's own `WHY THIS NUMBER` block so the next reader meets them rather than rediscovering them. `tools/w/witness_own_build_witness.rish` GREEN on metal. **OPEN** on the 21 that remain, and on the reading this row names rather than closes: the scan still counts direct invocation only, so a `run ["sh" "-c" "... path ..."]` form and a runtime sibling are both invisible to it. Widening it is its own lap, and the honest interim is the one this lap used -- **prove a repaired witness from a wiped directory, never from the tree the last one left behind.** **BOOKED (`20260829.160120`)** -- the ratchet this row left open drained to zero the same day: the witness-own-build family closed at ceiling 0 (%334 CLOSED, `20260829.141956`), so the remainder is the one booked lap this row names, widening the scan past direct invocation (door B, `20260829-031804`).
