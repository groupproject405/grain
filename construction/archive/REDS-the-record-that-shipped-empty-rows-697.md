# REDS -- the record that shipped empty, row %697

**Folded:** `20260910.140033` -- **Status:** Archived, complete, never edited
**Rows:** `%697` -- **Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)
**Room:** Checkable -- the row names where it was caught, and the guard that closed it runs on every commit

*The round whose whole reasoning shipped as a zero-byte file, and the reading that moved to the commit that makes the fault rather than the pass that finds it afterward.*

The row stands here exactly as it was written, with its closing clause accreted onto its
own face. A repaired row leaves the living pin so the pin stays the length a reader will
actually read; the lesson travels forward in `tools/hooks/pre-commit` rule eight, and the
row itself stays one click away.

---

**REDS %697 (`20260910.064526`) -- a round's whole record shipped as a zero-byte file, and every session-log guard read green.** *What went wrong:* `f46bc8797` committed `session-logs/date/20260910/20260910-054448_the-two-names-the-geometry-owed.kyri` at **0 bytes**, and the blob is 0 in the only commit that has ever carried it -- no stash, no earlier revision, no other ref. The commit body describes the log's content in ten lines; the log itself holds nothing. The round it recorded is real and landed (`8ffeca01d`, two press pages and a gratitude page, 267 lines), so the WORK survives and its reasoning is gone. *What caught it:* `empty_document` on the cold roster pass -- a guard about **absence** rather than about logs. *What it taught:* **every session-log guard reads the ROW, and none reads the LOG.** `log_has_a_row`, `index_row_bound` and `session_roster_agree` all read the row, and all three were green, because the row was perfect. Sharper: `status_declared`, built to tell a finished lap from a killed one, counted this log in `neither` -- **1 of 32 that day** -- so a record never written is **indistinguishable from a lap cancelled mid-send**. **The same round left a second red sharing one root.** `crushed_index` refused `docs-geode/press/README.md` for **two unlisted members** -- the pages `8ffeca01d` landed. The round shipped its work and skipped BOTH records. *Repaired:* the log carries an honest **reconstruction**, declared `reconstructed` on its own face and sourced from the two commit bodies and three landed files, since the elder bytes never existed to preserve; the index carries both rows and its two spelled counts moved five to seven. Both guards return green. *Not repaired:* nothing reads a log's bytes on the lap that writes it -- `empty_document` reads the INDEX, so it catches this on the NEXT lap's cold pass rather than at the commit that makes it. **OPEN** -- a byte-floor on a newly tracked `.kyri`, at the moment it is staged, wants a lap of its own. *Closed `20260910.140033`:* `tools/fixtures/e/empty_document_scan.sh` grew a second mode. `staged` narrows the same population rule through `git diff --cached --name-only --diff-filter=ACMR` and reads each path's blob out of `git ls-files -s`, so the index that will ship is what answers -- a file written after `git add` is outside this commit, and a file emptied after `git add` still carries its earlier bytes. `tools/hooks/pre-commit` rule eight runs it on every commit and refuses with `detail=RED_staged_empty_document` beside the path. One population rule, two readers, so the wall and the roster can never come to disagree. Landed `71a85f8f7` at `20260910.071421`, **forty-eight minutes after this row was booked**, and the row was never re-marked -- which is the second half of what this red teaches: **the ledger has no way to hear that a repair landed.** `empty_document_control.sh` proves the mode in a pen from four sides -- `staged_refused`, `staged_named`, `staged_written_free`, and `staged_peer_free`, the last holding the wall to the commit's own set, since refusing an author for a file somebody else wrote is how a wall becomes one somebody turns off. **BOOKED.**
