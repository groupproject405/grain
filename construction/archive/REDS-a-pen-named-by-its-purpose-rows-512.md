# REDS shelf -- a pen named by its purpose is a fleet-wide race, row %512 CLOSED

**Language:** EN
**Stamp:** `20260906.213000`
**Status:** Shelf -- immutable once written
**Rows:** `%512` -- booked `20260906.202423`, closed `20260906.213000`
**Voice:** Kyri
**Kin:** [`%511`](REDS-a-guards-reach-is-a-claim-rows-511.md) first named the race; `%291`, whose
reach this row extends past the checkout, stands on [`../REDS.md`](../REDS.md).

**This row has two shelves, and the reason is the ledger's own standing question.** A peer folded
`%512` at `20260906.220000` to
[`REDS-a-pen-one-path-wide-for-eight-ships-rows-512.md`](REDS-a-pen-one-path-wide-for-eight-ships-rows-512.md),
where it reads **BOOKED** with the nine collidable pens *"counted and left to their own lanes."*
This page is the row's **closing** record: the nine were swept two hours earlier on this tree, each
pen given `$$` and each guard run green, and the account below carries the whole reading. Neither
hand knew the other was holding the row, which is the fifth firing of *should an OPEN row carry a
claim, a seat and a stamp, at start?* -- this time on a fold rather than a booking. **Read the peer's
page for what the row taught and this one for how it ended.**

Eight ships share one pier, so they share one `TMPDIR`. A control that names its working directory
after its own purpose gets the same path in every ship, and each run's `rm -rf` reaches the others'
files. What comes back is a fault nobody wrote, arriving differently each time -- which is exactly
what a flake looks like, and why nine of these stood for as long as they did.


**REDS %512 (`20260906.202423`) -- eight ships share one pier and therefore one `TMPDIR`, and a control that names its pen with a constant lets one ship delete another ship's pen mid-build.** *What went wrong:* `tools/fixtures/t/topology_point_metric_control.sh` set `PEN="${TMPDIR:-/tmp}/topology_point_metric_control_pen"`, then `rm -rf "$PEN"` at the top and `trap 'rm -rf "$PEN"' EXIT INT TERM` at the bottom. Every seat on this pier runs it on its own lap at `tier lap`, so two overlapping runs share one directory and each wipes the other's -- the reading that comes back is a fault nobody wrote. *What caught it:* a hot roster pass on a lap that touched no comlink file answered `comlink_topology red`, `bridge_dropped_named 2 wanted=1`, where the same control run alone read `faults=0 verdict=proven`. *Proven on metal, both directions:* two concurrent copies of the elder file both FAILED and **in two different legs** -- `legs_removed_exit 90` and `search_starts_late_named 2` -- which is itself the tell, since a guard that fails differently each time is reading state it does not own; two concurrent copies of the repaired file both read `verdict=proven`. *What it taught:* **`%291` reaches past the checkout.** One writer per tree keeps two ships out of one working directory, and says nothing about the one `/tmp` all eight of them share -- so a pen named by its purpose alone is a fleet-wide race that looks exactly like a flake, and gets recorded as one. The previous lap on this tree wrote *two guards read red cold and green alone... the leading read is contention* and could not tell contention from a race; this is the same observation with its mechanism found. *Repaired:* the pen carries `$$`, the spelling the sibling `topology_attained_control.sh` in the same family already used, with the reason written above the line rather than left for the next reader to re-derive. *The class, measured `20260906.202423` over `git ls-files 'tools/fixtures/*.sh'`:* **15** pen or work assignments under `TMPDIR`, of which **3** already carry `$$` and **12** name a constant. One is repaired here. Of the **11** remaining, **9 are collidable pens** -- `topology_revocation_control_pen`, `topology_floor_control_pen`, `tlb_reach_control_pen`, `tlb_reach_census_pen`, `signal_trap_control_pen`, `signal_trap_scan_work`, `footprint_latency_control_pen`, `footprint_latency_census_pen`, `fleet-lap-verdict-pen` -- one is a snapshot file (`link_witness_round_selfcheck_before.txt`) worth the same look, and one is `grain-amphora-vessel-port-38494.lock`, where a shared name is the whole point of a lock and must stay. They span five lanes, so each wants its own witness run rather than a sweep from here. *Closed `20260906.213000`, and the class named itself before the sweep ran.* The `20260906.210015` cold open on this tree answered **five reds**, and two were `signal_trap` and `tlb_reach` -- both on the nine above, and each GREEN when run alone minutes later. `signal_trap`'s own words were `corrected_exit_code: wanted '143' got '127'` and three `pen_innocent_*` legs reading `wanted '5' got 'absent'`: the pen's files gone by the time the leg looked, which is a deletion rather than a timing wobble. *Swept:* all nine carry `$$` now, each with the reason written above its own line and naming this row, since a reader meets one file at a time. *Proven both directions on metal:* two concurrent copies of the elder `signal_trap_control.sh` both failed **in different legs with different numbers** -- one read `documented_counts got '9'` where five were wanted, having counted the other copy's four files as its own, and the other read `got '0'` beside `cleanup_ran_once got '2'`; two concurrent copies of the repaired file both read `faults 0` and GREEN. *And a control on the control, because one leg was an artifact:* the elder run **alone** from the same pen produces exactly one fault, `scan_absent`, which is a copy running away from its scan rather than a race. Every fault beyond that one is contention, and the artifact fires identically in both copies where the race faults differ -- which is the discriminator itself, since a fault of location repeats and a race does not. *Six guards green over the nine files:* the `fleet_lap_verdict`, `signal_trap`, `tlb_reach` and `topology_floor` witnesses, plus the `footprint_latency` and `topology_revocation` controls run directly, those two families carrying no witness at all -- `%360`'s population, named here rather than repaired here. *One figure of my own corrected:* the census above read **15** `TMPDIR` assignments, 3 carrying `$$` and 12 naming a constant. A fuller sweep over the same `git ls-files` reads **37** -- **5** carrying `$$`, **23** building their name with `mktemp` and so unique by construction, and **9** naming a literal constant. The nine were right; the twelve had lumped `mktemp` templates in with literal names, which is the same reading error one layer down -- a count of what *looks* shared rather than of what *is*. The snapshot file and the port lock stand as the row named them. **CLOSED.**
