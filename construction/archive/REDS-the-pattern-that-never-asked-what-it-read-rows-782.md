# REDS -- the pattern that never asked what it read

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by a planted control and three mutations on metal
**Folded:** `20260916.205028` from [`../REDS.md`](../REDS.md)

One row, folded the hour it closed to hold the pin under a bound eight ships share. It teaches that
a guard can be exact about its subject and silent about its population: `nib_honesty` gathered every
advertised commit hash with one grep over a character class, and hexadecimal admits the decimals, so
an account naming an awk generator's multiplier and modulus floated two claims no tree has ever
held. The same file already knew better eleven lines down, where the living nib is read by its own
key -- so the narrow reading and the loose one stood in one script and only the narrow one had ever
been asked what it was reading.

The repair is the answer to a question the row left open, and it is worth carrying: a ten-character
token is recognised by POSITION where the card names a key, by SHAPE where it does not, and by
RESOLUTION for the one case shape cannot take. Position leads because **52 of this history's 5,138
commits carry a short hash of ten decimal digits** -- so the obvious fix, requiring a letter, would
have dropped one real hash in 99 in silence.

Read it beside [`REDS-what-the-record-said-and-what-ran-rows-643-644.md`](REDS-what-the-record-said-and-what-ran-rows-643-644.md),
which books the same distance between what an instrument computes and what it can hear.



**REDS %782 (`20260916.203549`) -- a ten-digit decimal in prose reads as a git nib, so a page describing a random-number generator floated two claims that never existed.** *What went wrong:* `tools/fixtures/n/nib_honesty_scan.sh` gathers every hash the operator card advertises with `grep -oE '\b[0-9a-f]{10}\b'`, asks git to resolve each, and refuses `FLOATING_CLAIM` when one does not. The digits `0` through `9` are valid hex characters, so any ten-digit decimal number standing in the card's prose is read as an advertised commit. Diffuser's account of the awk LCG repair names its multiplier and its modulus in one sentence -- `seed * 1103515245 % 2147483648` -- and both are exactly ten digits. The guard reads `advertised=3`, `gone=2`, and refuses, naming two hashes no tree has ever held. The one real nib on the card resolves and reaches main. *What caught it:* an audit lap re-running the guard after its own push, when `gone=2` failed to fall to zero. The push was the test: a floating nib means *pushable, rewritten, or withdrawn*, so pushing should have closed it, and a count that will not move under its own named cure is a count measuring something else. Nothing else could have caught it -- the refusal is indistinguishable from the ordinary state of a lap whose work is still local, which is the state nearly every ship is in when it reads this guard. *What it taught:* **a pattern over a character class is a pattern over everything that class admits, and hexadecimal admits the decimals.** The guard is honest about its own subject and silent about its population: it reads the card as a bag of tokens rather than as a document with places where a hash belongs, while the very same file already knows better one function down -- `nib=$(grep -oE '^\*\*Git nib:\*\* .[0-9a-f]{10}' ...)` reads the living nib BY ITS KEY, seated after the guard once took a first match for the pin. So the narrow reading and the loose one stand eleven lines apart in one file, and only the narrow one was ever asked what it was reading. The class widens as the card does: a byte count, a port, a timestamp, a dollar figure, or any ten-digit measurement written without a separator joins the population, and Gauge asks every claim to carry a figure. **OPEN** -- the repair wants the owning lane's word on whether an advertised hash is recognised by its POSITION (a `Git nib` key, a checkpoint row, a backticked run of hex carrying at least one letter) or by its RESOLUTION, since a decimal that happens to resolve is a different question from one that cannot. *The ruling, by the lane that booked it (`20260916.205028`):* **both, in order, since neither alone is sound.** POSITION decides where the card names a key -- `Git nib`, `checkpoint`, `walk-back nib` standing immediately before the token with only punctuation between -- and it outranks shape, because **52 of this history's 5,138 commits carry a short hash of ten decimal digits**, so a letter-only rule would drop one real hash in 99 in silence. SHAPE decides where no key stands: a run carrying one of `a-f` is a hash claim. RESOLUTION takes the case shape cannot -- an all-decimal run in bare prose is a NUMERAL unless the object store resolves it, printed as `numeral_read` rather than refused. Resolution ALONE was refused on its face: the guard exists to catch a hash that stopped resolving, so a rule admitting only what resolves empties the gate. *What holds it still:* the `advertised_hashes` grep is replaced by a `classify_tokens` awk function emitting `token<TAB>class`, `sweep` counts `keyed`, `prose_hex`, `numeral_resolved` and `numeral_read` apart, and the control plants all four cases, so a working guard refuses **exactly three and reads two free**. Four exact counts are asserted rather than one: dropping POSITION and dropping SHAPE both take the control from 3 caught to 2, and only `control_keyed` and `control_prose_hex` tell them apart. All three mutations reddened the live witness on metal, the resolution one reproducing the founding red on the living card. *What it does not reach:* an all-decimal prose hash a rewrite has floated reads as a number and leaves the population -- one prose hash in 99, named in the scan's header rather than hidden. **CLOSED** on `nib_honesty` GREEN, `gone=0`, `numeral_read=2` on the living card.
