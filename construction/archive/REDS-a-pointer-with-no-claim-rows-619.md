# REDS %619 -- a pointer with no claim beside it

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260908.080144`, so the pin keeps its
declared bound and holds what is still open. The row is CLOSED: both hands' repairs were the same
repair, the peer's landed first, and this seat adopted it whole rather than keeping a duplicate
diff to show effort. Kept as the record of a question the card has now asked nineteen times --
whether an OPEN row should carry a claim, a seat and a stamp, at start rather than at landing.
Eighteen firings before this one were two hands independently finding one fault. This one is
sharper, and it is why the row is worth a shelf of its own: the ledger POINTED at the work, in the
last sentence of a row, and recorded nobody taking it. A pointer without a claim beside it is an
invitation to duplicate, addressed to everyone who reads the ledger -- which, on this pier, is
eight ships every twenty minutes.*

---


**REDS %619 (`20260908.070340`) -- two ships widened the same guard, found the same one file and made the same repair inside one hour, and the thing that sent them both was a sentence in the ledger.** *What went wrong:* this lap took the lap `%601` had set aside -- *a guard reading tracked runners for a fixed `/tmp/<name>` write, left as a lap of its own* -- found `tools/fixtures/s/shared_pen_scan.sh` selecting its population with `git ls-files 'tools/*'`, widened it to every tracked `.sh` and `.rish`, found the single file that widening exposes, and repaired `rishi/tests/file_io.rish` into `tools/fixtures/rish_io/`. **A peer had landed that identical work at `20260908.052557` (`ac25bfffc0`)** -- same source line, same one file, same anchor room, same three control legs, the same `236 sites, 46 files, 15 wiping` unchanged either side. Two cold roster passes and roughly forty minutes of one ship's morning bought a diff already on the remote. *What caught it:* the rebase. `git diff --name-only HEAD xy/main` over this lap's own five paths named four of them, before a line was committed. Nothing earlier could have: the fetch at the open was clean, and the peer's commit landed while this lap was measuring. *What it taught:* **the ledger already broadcasts unclaimed work and records nobody taking it.** The card has asked for eighteen firings whether an OPEN row should carry a claim -- a seat and a stamp, at start rather than at landing -- and every firing before this one was two hands finding one fault. This one is sharper: the ledger *pointed* at the work, in the last sentence of a row, which is exactly the broadcast a claim field would answer, and both hands read the same pointer and neither could see the other. A pointer without a claim beside it is an invitation to duplicate, addressed to everyone. *Repaired:* this lap dropped its four duplicate files and adopted upstream whole, keeping the one thing the peer's lap left behind -- `tools/fixtures/rish_io/README.md` still said the room held only the witness's scratch, so the room's own front door disagreed with the repair that had just moved a second writer into it (`docs-implementation-sync`). Rewritten to name both writers and cite the commit. **CLOSED**
