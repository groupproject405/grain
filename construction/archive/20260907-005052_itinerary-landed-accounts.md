# Landed accounts folded from the operator card -- `20260907.005052`

*Folded from [`../ITINERARY.md`](../ITINERARY.md), which holds what is OPEN and what waits on
Keaton's word. Every row named here is CLOSED, and its full three-field record stands in the REDS
ledger or its shelf.*

## GRASS -- the rota's twenty paths were held by nobody, and a day that begins empty read as a fault

Rows [`%529` and `%530`](REDS-the-door-and-the-day-rows-529-530.md), both CLOSED on the lap that
found them.

`rota_declared`, seated the day before, reads the OPEN day's shelf -- and a day gets its shelf when
its first log lands. So it refused between midnight and the day's first log, the witness read that
refusal as an assertion, and `standing_equipment` reddened beneath it for recording the run
(`runs_red=1`, `runs_red_self=0`): one root wearing two red rows, met by every ship once a day. A
red roster withholds the `--scoped` receipt and receipts chain from full greens alone, so each ship
then pays a FULL cold pass -- **896 seconds** measured here at `20260907.001718`, on a pass reading
`guards_run=128 guards_green=124 guards_red=2`. The live day reports `verdict=day_unopened` at zero
now; a day the CALLER names, and an absent `session-logs/date`, both refuse exactly as before.

Then the rota row itself. Earth-Cardinal changed hands on `20260905`, from the one-clock naming spec
to `foundations/20260905-154954_the-clock-and-the-mark.md`, because a rota reads foundations and the
mark law sat on no seat at all. The change reached the grid and not the Earth threshold page -- the
first page a lap opens on that row -- so for two days every earth lap that obeyed the door read the
clock half of the concern and missed the mark half, which was the whole reason the seat moved. **No
link wall could see it, since both paths resolve.** `tools/r/rota_grid_witness.rish` gates
`unresolved`, `sections_missing` and `seat_undeclared` at zero over the grid's twenty paths, refuses
rather than measuring its own parser when the grid's shape moves, and proves sixteen behaviors in a
pen with **every plant compared against the file it replaced** -- one of the 94 controls `%519`
counted, born with the check that row asks for.

## GRASS -- a blind ledger published fascia on the front door

Rows [`%525` and `%526`](REDS-two-instruments-that-could-not-see-their-subject-rows-525-526.md),
both CLOSED. The long-function ledger ended every function at its first `if` block, so it read
**15** functions past seventy lines where **321** stand, and `README.md` published **fascia 68** over
a tree measuring **58**.

**The question it left, still open on the card:** the report card cannot see staleness, in three
instances -- the long-function penalty caps at 25, so a grade cannot tell 26 long functions from 321;
two of 46 pages read `Status: Proposed`, which the TWO_ROOMS gloss omits; and Truth counts whether a
cited path RESOLVES, which is how a page routing every reader to a retired rota seat graded A/95.
