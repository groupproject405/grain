# REDS -- row %559, folded from the living pin

**Folded:** `20260907.095458` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Two careful laps refused a stranded paper as superseded, and both decided it with a shared elder, a
shared subject, and a diff line count -- three readings that read the same from either end. To ask
whether B supersedes A, measure what A holds that B does not.*

The row stands here exactly as it was written. A closed row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the instrument the round
recovered, and the row itself stays one click away.

---

**REDS %559 (`20260907.095458`) -- two laps refused a stranded paper as superseded, and both decided it with evidence that reads the same from either end.** *What went wrong:* `external-research/20260906-175851_the-hop-you-can-compute.md` and its `topology_routed_{witness,scan,control}` family stood in the dead-letter box from `20260906.190715`, on no ref and absent from the tree. Two laps opened the box and refused the set in writing -- `20260906.212539` as *"the SAME paper two hours earlier -- same elder, same subject, 353 diff lines, rewritten at 195719"*, and `20260907.002019` as *"the superseded stamp of what landed as 20260906-195719 ... orphans of a rename, not a loss"*. Both refusals were careful, both were recorded so the next lap would not re-recover it, and both rested on three symmetric readings. A shared elder, a shared subject, and a diff line count all read identically from either text, so not one of them can separate *the successor contains the elder* from *the two overlap and diverge*. *What caught it:* one grep per term across both papers and both scans, `20260907.095458`. **Pancake, bubble-sort, the 5,040-point leg, the arrangement-graph door, and the rule's branch pricing appear in the stranded paper and nowhere in the one that replaced it** -- three of seven shapes and the whole second point on the scaling curve, living in an untracked file inside a stash. The instrument then ran on metal from its real paths: scan `verdict=ok` in 1.8s reproducing every figure in the text, control 26 of 26 behaviors, witness GREEN in 28.5s. *What it taught:* **to ask whether B supersedes A, measure what A holds that B does not.** That reading is asymmetric by construction, which is what the asymmetric question needs; overlap, however carefully counted, answers a different one -- and two independent readers agreeing on a symmetric measurement produced one wrong answer twice, which is agreement without independence. It is `20260907.080356`'s completeness fault moved out of a script and into a reader: a check that reads table to registry and never back passes in silence, and so does a reader who reads successor against elder and never back. *Repaired (`20260907.095458`):* the paper landed at its own stamp carrying a measured comparison table against its sibling and a section recording this refusal; the family landed as **`topology_stretch`**, renamed from the draft's `topology_routed`, which stood three letters from the seated `topology_routing` in a roster of 220 -- a grep that cannot separate two guards is a collision whatever the filesystem thinks. Rostered `tier lap`. **CLOSED.**
