# ITINERARY landed accounts -- shelved `20260916.231943`

**Language:** EN
**Status:** Shelf -- two finished patchouli accounts, kept whole
**Room:** checkable -- every claim names the witness that held it
**Style:** Gauge at Meter
**Voice:** Kyri
**Shelved from:** [`../ITINERARY.md`](../ITINERARY.md)

Two receipts, moved off the card together on the lap that replaced them.

The first stood on the card until this stamp. Its **YOURS** question -- whether a Rishi witness
stating its bounds in `assert ... else` alone should be graded at the door -- **stands open and is
carried forward unanswered**; nothing on this lap touched it.

The second was written by a lap that died mid-send, and describes the Place head-enumeration work
that lands in the same commit as this shelf. It never stood on the living card, and it is kept here
so the work has a receipt rather than only a ledger row.

**PATCHOULI -- A DEAD LAP LEFT A PAPER AND A PROMISE THE CONTROL NEVER KEPT.** Account
[born on its shelf](20260916-200108_itinerary-landed-accounts.md), which is this card's
own open question answered by doing. Two laps died mid-send; `stash@{9}`'s whole set landed --
the three-door paper (**A/93**), `prove_head_insert_lands_last`, and the head passages -- after
the control learned the two plants that head had already promised, **9 legs to 13**, with two
`_lifted` phases naming claim 9 as the leg that reads them. Cold **316 read, 301 green, 13 red**;
hot scoped **311, 294, 15**; `tree_moved=no` both. Four reds were mine and all four are closed.
**YOURS:** the QA card takes its Meter lines from an `invariant:` comment, so a Rishi witness
stating its bounds in `assert ... else` alone is graded at the door -- **19 of 40 sampled
`tools/*_witness.rish` read below B**. A red, a ratchet, or a card that says so?
**MINE:** `stash@{8}` still holds a whole Place head-enumeration lap, so `stash_record` reads
`unlanded=1` and the number is honest. The next lap takes that stash whole.

**PATCHOULI -- A GUARD READS LISTS, AND THE CLAIM THAT WENT STALE WAS A SENTENCE.** Elder account
[shelved whole](20260916-213548_itinerary-landed-accounts.md). `stash@{8}`'s whole set is
landed, so `stash_record` reads `unlanded=0`. The module head of `mantra/src/weave.rye` said a
Place is *run, then site, then position* while `Place.less_than` compares **four** keys since `ord`
landed at `%680`; the lap that repaired four other stale records of this module passed over the
fifth, three paragraphs above the list it was repairing, because `mantra_weave_head_scan.sh`
enumerates the head's **LISTS** and that claim was **PROSE**. The repair is not a sharper reader --
it is to write the claim as a list. The scan's `TYPES` variable is now the whole seam, and a
published value type earns the field reading by standing there and carrying a head list. Control
**17 legs to 30**, every refusal planted and then LIFTED, with two the `Diff` legs could not carry:
`place_declared=4` against a walk that would count `Place`'s own `pub fn` and report five, and
`container_seen=1` beside the ok verdict, because the named-type container rule consumes every
`pub const <Name> = struct {` line it reads -- **that one fired for real** and answered
`weave_container_absent` on a healthy file. The elder lap's fold of `%773` was **dropped**, a peer
having folded that row already. Row `20260916.115437`, by stamp until shared. **PROVEN ON METAL:**
`mantra_weave_annotate`, `mantra_weave_v2`, `mantra_weave_v1_write`, `mantra_weave_v1_lift`,
`reds_ledger_monotone`, `tame_style_check`, `width-check`.
**YOURS, carried forward unanswered:** the QA card takes its Meter lines from an `invariant:`
comment, so a Rishi witness stating its bounds in `assert ... else` alone is graded at the door --
**19 of 40 sampled `tools/*_witness.rish` read below B**. A red, a ratchet, or a card that says so?
**MINE:** `Line` and `Note` are the two published types of this module still described in prose
alone, and each now costs one word in `TYPES` and one list in the head.
