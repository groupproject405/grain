# REDS -- row %547, folded from the living pin

**Folded:** `20260907.072600` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A proof that exists and a proof that runs are two readings, and one word was answering both --
the tally roster reported eleven covered modules while its own runner called five of them.*

The row stands here exactly as it was written. A booked row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the guards the round built,
and the row itself stays one click away.

---

**REDS %547 (`20260906.180651`) -- the roster row said its witnesses run, and it ran five of eleven.** *What went wrong:* `tools/t/tally_roster_witness.rish` carried the sentence *"a covered module has a proof the standing roster actually runs"* directly above five `run [` calls, while `tools/fixtures/t/tally_roster_scan.sh` reported `covered=11` from filenames alone. Six covered modules -- `copy`, `gardens`, `maybe`, `no_padding`, `parse_int`, `region` -- owned a witness on disk that this room's roster runner never invoked. Two of the six were called by `tools/t/tensegral_arc_iv_witness.rish` and `tools/p/parity_ch01.rish`, neither on the standing roster; `tally/region.rye` was called by no runner anywhere, and it is the one memory-carving body in the tree -- `seed` and `gardens` fold into it, and three Caravan rungs reach it through the `caravan/region.rye` symlink. *What caught it:* a hand reading the runner's own claim beside its own call list. No meter took that reading: the scan counted filenames and the sentence claimed a clock. *What it taught:* **`covered` and `heard` are two questions wearing one word** -- a proof exists, and a proof runs -- which is [`single-stranded`](../../foundations/20260823-204456_single-stranded.md) inside a meter, and the air row's touch test finding the hand passing through the fence. **`%540`, booked upstream within the same hour, is the same fault one room over**: one roster answering a page-level question and a prose-level question at once. Two firings of one shape in one hour is a loom rather than a lantern, and the shape has a name now -- *a meter whose reach and whose claim are two readings*. *Repaired:* the scan prints `heard` and `unheard` beside `covered`, gates the gap at zero under `TALLY_ROSTER_UNHEARD_CEILING`, and refuses with `verdict=no_runner` when the runner is absent rather than reporting eleven faults where there is one. An invocation is read as a non-comment line carrying `run [` and the witness path as a **quoted** token, because this witness's own pen writes `${pen}/tools/t/tally_copy.rish` inside a `run [` and a looser match would have read that plant as proof that `copy` runs -- the flag-inside-a-longer-word lesson this room already learned once, one level up. Four refusals are proven from both sides in a pen: an uncalled module, a path named only in a comment, a path sitting inside a longer one, and an absent runner. All six missing calls run GREEN and cost **3s together**, measured `20260907.071500` -- so the claim was not merely false, it was cheaper to keep than to leave. *And it took four round-opens to land:* the work stood complete in `stash@{0}` through `20260906-231128`, `20260907-055442` and `20260907-065155`, re-stashed each time by `tools/f/fleet_round_open.sh` and never committed, which is `%499` happening to a finding about proofs nobody runs. **BOOKED.**
