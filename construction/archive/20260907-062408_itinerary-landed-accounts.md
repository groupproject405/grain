# Landed accounts -- `20260907.062408`, Copal

*Folded off [`../ITINERARY.md`](../ITINERARY.md)'s live front, which holds what is OPEN and what
waits on Keaton's word. Cited by stamp rather than by number where the anointed spine has yet to
bind a view (`.claude/rules/derived-spine.md` rule 4).*

## The elder block this shelf replaces

**COPAL's two accounts, both CLOSED**, folded whole to the
[`000030` shelf](20260907-000030_itinerary-landed-accounts.md), cited by stamp
(`20260907.000030`) -- a flake is when the sentence matters most, and the cellar's catalog named
3 of 17 with nothing reading it. Two questions rode with it, and one is answered below.

## The `chatgpt_mind` control trio needs no caller, because all three are fossils

The elder block asked whether `tools/fixtures/c/chatgpt_mind_{full_clone,loop,real_plan}_control.sh`
carry the reason-loss the day somebody writes them a witness. Read at
`20260907.062408`: **all three open with a `FOSSIL -- Class M` banner, prepped `20260906.114734`
for the mitra shed**, each naming its living mutant -- `tools/f/fleet-loop.sh` reading
`construction/fleet-roster.kyri`, with `tools/f/fleet_watch.sh` above it. A fossil is prepped for a
cut, so nobody will write it a witness, and the loss the question feared has no lap on which to
arrive. The row and its reasons stand in `construction/SHRED_PREP.md`; the cut stays RED until
Keaton circles it.

The question was worth asking and the answer costs one read, which is the whole of the
docs-and-implementation-sync habit: a doc's claim about behavior is checked by reading, never
repeated because it was once true.

## The whole Amphora vessel family took unique pens, and the race was proven both ways first

Seven rostered witnesses -- `amphora_pour`, `amphora_pour_negative`, `amphora_carry`,
`amphora_carry_negative`, `amphora_restore`, `amphora_grand_round`, `amphora_first_resident` --
each named its scratch directory with a constant under `/tmp`. Eight ships run one roster from
eight checkouts on one pier with `TMPDIR` unset, so a constant pen is one directory on all eight.

`tools/am/amphora_restore_negative_witness.rish` had already taken `mktemp -d` when the class was
booked; its seven siblings had not, and nothing in the family carried the shape but that one file.

**Proven on metal, both directions, before the repair shipped.** Four concurrent copies of the
elder `tools/am/amphora_carry_negative_witness.rish` read **two red and two green**, and the two
reds failed at **different assertions** -- line 55 `empty names torn` and line 77
`final heal must pass` -- which is the signature of a race rather than of a defect. Four concurrent
copies of the repaired file read **four green**, and sixteen concurrent runs across the whole
repaired family read **zero red**. Every one of the eight is green standing alone.

`tools/fixtures/s/shared_pen_scan.sh` falls **61 -> 54 files**, **308 -> 265 sites**, and
**7 -> 4 wiping files**, both ceilings lowered to what the tree now reads, with
`tools/s/shared_pen_witness.rish` GREEN at 27 behaviors and 0 faults against the new numbers. The
wiping ceiling is the one that guard's own header says to drive to zero, and this lane held three
of its seven.

## The contention that guard says it cannot prove is measured on this pier

`shared_pen_scan.sh` names its own limit plainly: *"WHAT IS NOT PROVEN. That any pen is ever
actually contended, which depends on which ships run which guard at which second."*

Measured `20260907.060000` from `/proc`, reading every live `standing_equipment_run.sh` by its own
working directory: **all eight ships had a roster pass live at one instant**, their starts spread
over **164 seconds** -- 05:55:10 Petrichor to 05:57:54 Pheromone -- against a pass that ran
**1,422 seconds** here. The passes therefore overlap for the great majority of their length, and a
guard's own window is entered by eight ships inside that same 164-second spread. Contention is the
expected state of every lap on this pier rather than a coincidence, which is why the class kept
firing and each firing looked like a fresh mystery.

*The reading is a fact about this pier and this fleet size. A pier running one ship would read the
opposite, and the guard's sentence would be right there.*

## Reported, and not mine to close: a pass can lose a verdict and mis-seat a red

The same cold pass carried a second flake with a different shape, and it is named here because the
runner belongs to another lane.

`stash_record` read **red at 5s inside the pass** and **green standing alone** on two later runs of
the same tree. It names no `/tmp` pen at all, so the race repaired above cannot explain it. The
runner announced `evidence construction/standing-equipment-reds/stash_record.txt` on the line above
its verdict, and **that file was never written** -- the red room holds a file for each of the other
two reds and all three gates, five of five, and nothing for this one.

In the same pass, **three verdict lines carried more than one guard name fused together**:

```
reds_status_consistencywitness_ownpin_bound_touch comlink_topology green 17s
fleet_round_open borrowed_number green 73s
fleet_roster mantra_weave_annotate green 13s
```

Five guards therefore have no verdict line of their own in this pass's record. The runner prints a
verdict with a single `echo "$name $verdict ${guard_seconds}s"`
(`tools/fixtures/s/standing_equipment_run.sh:890`), so the fusion happens before or around that
line rather than inside it, and naming the exact mechanism wants the runner's own author.

**Why it matters more than a cosmetic blemish:** a fused line means a verdict can be lost, and a
lost verdict is indistinguishable from a guard that was never run. If a `red` can fuse the way
these `green`s did, a pass can also seat a red under the wrong name -- which is precisely the
"a pass names no way to tell a flake from a fault" the live card has been carrying. It is one
candidate mechanism, measured, and it is not proven to be the cause of any particular firing.

The live card already carries *the several-line interleave, wanting an anchor per insert* as an
open item; this is that item with a count and a consequence attached.
