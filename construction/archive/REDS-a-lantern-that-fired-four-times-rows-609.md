# REDS -- a lantern that fired four times had been read four times and understood once

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri
**Folded:** `20260908.021504`
**Row:** `%609` (`20260908.021504`) -- CLOSED

The row folds on the lap it closed, for the reason its neighbours record one shelf over: the living
pin stands near its bound with almost every remaining row OPEN, and a pin whose living parts fill it
cannot accept a new one.

What this row is for, said once here so a reader arriving from a citation knows before they read:
the fault is not that any one guard was written carelessly. Four careful hands met the same buffer
in four rooms, each repaired it correctly where it stood, and each wrote the lesson into the file in
front of them. The thing missing was never a fix. It was the sentence that says *this is one class*,
and the instrument that counts it.

**REDS %609 (`20260908.021504`) -- a guard that reds because it succeeded at reporting, met four times by four hands and named as a class by none.** *What went wrong:* Rishi composes an interpolated string into `StrBuf`, a fixed buffer declared at `rishi/src/main.rye:3101`, and a composition past that width returns `error.StringTooLong` and stops the script. So `say "reading -- ${scan.out}"` makes a silent promise that a scan's output will stay under that width forever, while a scan's output grows with the tree it reads. On `20260907.232343` `tools/r/reds_spine_derive_witness.rish` passed every assert it makes and then died reporting -- `rishi: line 46: StringTooLong` -- once the ledger reached 42 lawful stamp duplicates and its scan printed 4,250 bytes, 154 past the bound, over a tree its own scan called `verdict=ok`. **That firing was the fourth**, and the three before it were each repaired where they landed with the lesson written into a different file's own comments: `tools/fixtures/r/readme_metrics_splice.sh` met it on an 18 KB file, and `tools/fixtures/t/tame_style_long_fn.rish` and its scan met it twice more interpolating a blob into a command. Four accidents in four rooms, one class, no instrument. *What caught it:* a lap that had booked the same repair a peer published first, reading the peer's better fix -- a bare `say scan.out`, which never opens a `StrBuf` at all -- and asking how many other sites make the promise that one had just broken. **502 do.** *What it taught:* **a lantern that fires four times has been read four times and understood once.** The sharper half is the second shape: an `assert ... else "...${x.out}"` composes only on FAILURE, so the one message that cannot be built is the one describing a real disagreement -- the explanation failing exactly when it is owed. **1,741 sites** carry that shape. *Repaired (`20260908.021504`):* `tools/fixtures/s/say_compose_bound_scan.sh` reads every tracked `.rish` for the two shapes, counted apart because they fail at different moments and a refusal that cannot say which is owed leaves a reader a number and no act (%528). `eager` at **502** and `deferred` at **1,741** are ratchets under ceilings that only fall, since the backlog was not made today and a gate that reds on ordinary work is a gate somebody turns off; `safe` reports the **1,015** sites already spelling the repair, so a reader meets the answer beside the count. Ten sites of each kind are named on every pass with `list` printing them all, because a count with no names is the second half of a guard nobody runs (%592). The buffer's width is **read** off Rishi's own declaration rather than carried as a copy. `tools/fixtures/s/say_compose_bound_control.sh` proves **29 behaviors** on planted scripts in real git repositories, every refusal planted then lifted and both ceilings shown from both sides. `tools/s/say_compose_bound_witness.rish` rostered `tier lap` at **5.7s**, and it keeps its own rule: the reading prints through a bare `say`, and the guard contributes zero to both of its own hazard counts. *An error of my own, caught by reading the output rather than trusting it:* the width read **40,968**, because the regex `bytes: \[[0-9]+\]u8` swept the `8` of `u8` into the digits. *And one caught by the control:* the scan's root walk climbs from its own `$0`, so called at its real path from a pen it walked back into this tree and read 502 sites inside a pen holding three files -- each pen carries its own copy now. **CLOSED.**
