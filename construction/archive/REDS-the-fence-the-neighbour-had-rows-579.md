# REDS -- row %579, folded from the living pin

**Folded:** `20260907.155612` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A bound proven on one side of a seam says nothing about the other side, and a stated lens is not a
checked one.*

The row stands here exactly as it was written. A closed row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the instruments the round
built -- `check_bounds` in `mantra/src/diff.rye`, which is a function rather than two inline `if`s
so a witness can press the boundary at the ceiling and one cell past it, and the three plants in
`tools/fixtures/m/mantra_diff_control.sh` that take its control from seven phases to ten -- and the
row itself stays one click away.

---

**REDS %579 (`20260907.111135`) -- the weave names its ceiling and reads it at the edge; the module that feeds the weave allocated a quadratic table with no ceiling at all.** *What went wrong:* `mantra/src/diff.rye` builds an LCS table of `(m + 1) * (n + 1)` cells of `u32` in one `allocator.alloc` call, from two lengths a caller chooses, and it carried **no named max, no named error, and no edge check**. Its two `usize -> u32` seam casts -- `var i: u32 = @intCast(m)` and `@intCast(n)` -- therefore ran with the bound that makes them safe unstated, which is the one thing TAME's width law asks of a seam cast. `mantra/src/weave.rye`, one seam over, names `max_weave_lines` at `1 << 20` and reads it at the edge of both `apply` and `merge` before a line is appended; the module whose output `apply` consumes named nothing. Two documents of 100,000 lines each would have asked for **40 GB** in one call, and the request would have been made rather than refused. *What caught it:* the council rota's **air row**, read through its own sense on lap 4171. The air threshold instructs the lap to test a claimed boundary by pressing on it -- *if the hand passes through, the boundary was a wish* -- and the fixed seat, `single-stranded`, says you check whether a part stands free by trying to pull it out. Pulling `diff.rye` out from under `weave.rye` found the weave's fence exemplary and the fence beside it absent. Sharper still: the module's own guard, `tools/m/mantra_diff_witness.rish`, announces its lens on line three as *"one named max, an invariant on every function, bounds read at the edge"* -- **the guard stated the discipline the module under it did not keep**, and no leg of that guard read its own opening line. *What it taught:* **a bound proven on one side of a seam says nothing about the other side, and a stated lens is not a checked one.** `weave.rye` had a bound, a named error, and edge checks from its first commit, and the proximity made the neighbouring module read as covered. *Repaired:* `max_diff_lines` is `weave.max_weave_lines` **imported** rather than restated, so one quantity has one spelling; `max_diff_table_bytes` is stated in bytes at one gibibyte and `max_diff_table_cells` is **derived** from it by `@sizeOf(u32)`; `DiffError.TooManyLines` and `DiffError.TableTooLarge` name the two ways the fence fails, since two sides that are each ordinary can still ask for a table that is not; and `check_bounds(old_len, new_len)` decides in `u64` before anything allocates -- a function rather than two inline `if`s **so the boundary can be pressed by hand**, which is what lets a witness prove it at the ceiling and one cell past without building a million-line document. `split_lines` refuses on the line that would pass the same ceiling, before the append. *Proven:* three new claims in `mantra/src/diff_witness.rye`, each shown from both directions -- the ceiling admitted at `side` where `(side + 1)^2` **is** `max_diff_table_cells` and refused by name at `side + 1`; the derivation asserted against `weave.max_weave_lines` itself; and `diff` handed a side one line past the ceiling, refusing at its own edge before the table. Three new plants in `tools/fixtures/m/mantra_diff_control.sh` -- `bound_widened`, `ceiling_restated`, `edge_check_removed` -- take the control from seven phases to **ten**, all three exiting 134 against a clean pen at 0. **`edge_check_removed` is the sharpest**: it leaves `check_bounds` standing and correct and deletes only the call, so the decision is right and nobody asks it -- the phase claim 8 cannot see, and the reason claim 10 exists. **A bound nobody calls is a bound.** *Not taken:* the table stays quadratic. A ceiling names what the algorithm costs; an index is what would change it, and that earns its place when a measurement asks rather than before. **CLOSED.**
