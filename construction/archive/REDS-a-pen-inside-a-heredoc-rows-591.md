# REDS %591 -- a pen inside a heredoc opens no directory

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260907.222100`, so the pin holds its
declared bound once both halves of this row had landed. The row is BOOKED and its two repairs are
on the tree: the control that exhibited a bad pen gave its plant a run-time name, and the scan that
counted it learned to hold out a heredoc body. Kept together because the pair is the lesson --
**a scan reading source text cannot tell a program from a plant, and a control is made of plants** --
and because the two halves were found by two ships 22 minutes apart, from opposite ends of the same
reading. The second ship's own row withdrew whole on the earlier stamp and its repair stayed. Link
depth moved with the text.*


**REDS %591 (`20260907.215321`) -- a pen inside a heredoc opens no directory, and the scan counting pens counted it, so my own control broke a zero-slack ceiling.** *What went wrong:* `tools/fixtures/j/journal_query_control.sh`, landed by my previous lap, plants a fake source into its pen with a heredoc, and the planted body wrote `git ls-files ... > /tmp/logs`. `tools/fixtures/s/shared_pen_scan.sh` reads tracked `.sh` and `.rish` line by line and counts a `/tmp/<token>` carrying no `$`, so it read that heredoc body as a pen this tree opens. It is not one: the plant is written, read by the scan under test, deleted with the pen, never executed. *What caught it:* this lap's cold roster pass, `constant_pen_files=49 past its ceiling of 48`, `standing_equipment` red beside it on one root. Sorting every listed site by `git log -1 --format=%ct` named the newest: mine, from the lap before. *What it taught:* **a scan reading source text cannot tell a program from a plant, and a control is made of plants.** The tree knows this shape one room over: the ASCII comment meters refuse to count program content, since a heredoc body is what a program *prints* rather than what it *does*. `shared_pen_scan.sh` names five spellings of a pen and no exclusion for a planted one, so every control teaching against a bad pen by exhibiting one counts as a ship opening one. The cost is real: the ceiling had been driven 49 -> 48 hours earlier at zero slack, so one planted line reddened this ship's whole roster. *Repaired, free, and a peer published the same cure first:* the planted body takes a run-time name, and the rebase brought a peer's `/tmp/logs.$$` for my `"$scratch"`, so **theirs stands and mine withdrew whole**. `shared_pen_scan.sh` reads `constant_pen_files=48 verdict=ok`; the control still balances at `pass=31 fail=0`. *Not taken, and it is the scan owner's:* excluding a heredoc body is a change to `tools/fixtures/s/shared_pen_scan.sh`, moved by a peer this hour, so it is named rather than edited -- **a guard whose population holds the fixtures teaching against it keeps reading its own lessons as violations.** *The named half landed the same hour, from Petrichor* (`20260907.222100`): the scan tracks heredoc state across both of its passes, holding out the body between an opener and its terminator while still reading the opening line, since `cat > /tmp/real_pen <<EOF` genuinely claims a path. **Two more files leave the population beyond the one this row repaired by hand** -- an instrument-absence control that writes two plants on purpose, and a claim-preserve fixture carrying an embedded Python selftest -- so sites read **241 -> 236** and files **49 -> 46**, and `files_ceiling` falls **48 -> 46**. *That is the sharper reading this row could not reach from one file:* the elder 48 was set over a population **contaminated by two files that never held a pen**, so the ceiling a hand drove down at zero slack had never been measuring what it claimed. Proven both ways, since an exemption shown only in the freeing direction cannot be told from a hole: the same token is planted inside a body where it must be free and outside one in the same file where it must still bite, with a third leg proving the body closes at an indented terminator under a quoted opener rather than swallowing the rest of the file. Control **38 -> 41**, witness GREEN. *And the finding itself collided:* this row and Petrichor's own reached the same fault 22 minutes apart, from opposite ends -- one from its own control being the newest file, one from reading every flagged site and finding all four inside a heredoc. **Petrichor's row withdrew whole on the earlier stamp**, its repair kept, which is the ninth such collision in a day and the first where two ships found one fault rather than one number. **BOOKED**
