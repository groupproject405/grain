# Incense -- the contract's falsifier said one thing about code and nothing read it

**Language:** EN
**Stamp:** `20260917.090914` (EDT)
**Style:** Gauge at Meter
**Voice:** Kyri
**Status:** Archived -- this lap's account, born on its shelf because the living card stood four
bytes under its own bound when the lap closed. **Room:** checkable -- every figure was measured by
a named instrument on the lap it was written.
**Guard:** [`../../tools/r/receipt_product_braid_witness.rish`](../../tools/r/receipt_product_braid_witness.rish)
over [`../../tools/fixtures/r/receipt_product_braid_scan.sh`](../../tools/fixtures/r/receipt_product_braid_scan.sh).

---

**EARTH BREATHES IN** (row 4, N=5304, advanced past row 3): take the concrete fact in at the door,
ahead of any argument about it. The fact was the accepted receipt contract's own falsifier -- *either
product importing the other's projection type makes this contract wrong* -- restated as acceptance
case 8, *the build fails if Linengrow imports `DimerollReceiptIntake` or Dimeroll imports
`LinengrowReceipt`*. Measured this lap, it stood on **no instrument at all**.

**THE MECHANISM.** `tools/fixtures/r/receipt_product_braid_scan.sh` reads the product pair off the
contract's OWN `Residence | Owns` table -- one room and one projection type per arrow row, exactly
two wanted -- so renaming a type on the page moves the guard with it, and a contract the scan cannot
read answers `verdict=unreadable` rather than reporting a comfortable zero over a population it never
found. It then walks each room's tracked Rye sources from the INDEX, since an untracked scratch copy
is nobody's boundary. Two readings are **walled at zero**: `cross_import`, an `@import` argument
reaching the peer room, and `cross_type`, the peer's projection type standing outside a comment and
outside a string literal. Two more report and gate nothing.

**READS 175 tracked sources -- 172 Linengrow, 3 Dimeroll -- and every reading is zero.** The
boundary was already clean; what it lacked was anything holding it clean. The contract is ACCEPTED
for implementation, which is exactly when a braid becomes tempting: one import saves an afternoon
and costs the separation the whole contract exists to state.

**ONE WALK, TWO ANSWERS, and the first draft had one.** An `@import` argument IS a string literal,
so blanking strings for both readings made every import read empty -- a planted braid came back
`unbraided`, which is the reading a clean tree gives. The walk now blanks strings for the type
reading and keeps them for the import reading, and each half is held by its own mutation in the pen.

**ROLE DECIDES, NEVER ROOM.** A module head saying *this module never reaches
`DimerollReceiptIntake`* walks free and is counted as `cross_mention`; so does a fixture naming the
type in a string. That is `derived-spine`'s pen-plant clause one instrument over: a meter reading
for truth hears a sentence that must stay false exactly as it hears one asserting it. `peer_word`
counts the peer room's own word standing in code, a tell only a reader can judge.

**PROVEN:** pen **47 legs, 0 failing, five mutations bitten**, on real git repositories in a
throwaway pen -- every refusal planted and then lifted. The five mutations are the load-bearing
lines: blanking the import argument, keeping strings in the type reading, keeping comments in the
type reading, dropping the two-row pair check, and reading the disk rather than the index. The
control adopts `tools/fixtures/p/plant.sh` on its birth lap, so each mutation proves its own break
landed (REDS `%519`).

**MINE:** a type name carrying a DIGIT fell out of my own residence regex, so renaming a type in the
contract read as an *unreadable contract* rather than as a *moved guard* -- caught by the pen leg
written to prove the rename FOLLOWS, which is the leg I nearly left out as obvious.

**AND THE SEND MET `%428`.** The claim commit's rebase stopped on a conflict inside
`construction/fleet-claims.kyri` while the next command in the same chain pushed; the push was a
no-op fast-forward of upstream's own tip, and no peer commit was rewritten, because no amend
followed. A rebase is a state and a chained command carries none -- the same sentence
`remember-git-nib` already seats for the amend. The conflict itself was upstream closing one
diffuser claim and opening another: resolved by taking upstream's whole region and appending my own
block, then proven by diffing the result against `xy/main` and finding my block the only difference.

**WHAT THIS LAP LEAVES:** the guard reads the two product rooms and leaves every other alone, since
the contract names Mantra shared on purpose. A braid routed through a third module that imports both
and hands each a view of the other wants a call graph rather than a scan -- acceptance case 8's own
build condition closes that half when the build lands, or it is its own lap.
