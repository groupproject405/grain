# REDS shelf -- row %703, the false-future gate read 3.4% of the tree

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- one BOOKED row, landed straight onto its own shelf because the pin has no
room for it

Written `20260911`, in the lap that found and repaired the red. `construction/REDS.md` stood at
**40,479 bytes** against the **40,960** its own header declares -- **481** bytes of room, where this
row wants **2,929**. A BOOKED row may fold by the ledger's own law, so a BOOKED row may be born
here -- the move `%693`, `%696` and `%699` each made before it, every one for this same reason.

---

**REDS %703 (`20260911.020039`) -- the false-future gate read 3.4% of the tree, and the 96.6% it missed is where the law puts every log.** *What went wrong:* `tools/fixtures/o/one_clock_mono_scan.sh` built its population from a **flat** glob, `"$d"/[0-9]{8}-*`, over five rooms. A session log is **born on its day shelf** from `20260827.171500`, so `session-logs/*` flat holds **zero** dated files, and the room the gate was built for contributed nothing at all: measured `20260911.020039`, the glob read **247 of 7,344** dated artifacts, with 5,866 session logs, 930 counsel notes, 291 waymarks and 10 foundations folded out of reach. `TRUE_HEAD` read `20260910.193534` off a `foundations/` page while the tree's newest living stamp stood at `20260911.002040`, six hours and eighteen minutes newer. A second blindness rode in the same line: the stamp sed anchored on `_`, so the **237** sprigless dated files this tree carries were invisible even when flat -- `%175`'s exact shape, and `tools/fixtures/d/dated_spelling_scan.sh` gates 19 sites for that spelling and never named this one. *What caught it:* an earth rota lap reading the guard's own printed line at the door -- `HEAD_OK ... stands -22941s from host ... (tolerance 120s)` -- and asking which population produced a head six hours stale. Then a pen, one file, two paths: a log stamped **four hours ahead** of the live clock **on its day shelf** reads `TRUE_HEAD 20260910.193534`, `MONO_OK`, `HEAD_OK`, exit 0; the **same file moved flat** reads `HEAD_BAD ... is 14400s ahead`, `HEAD_FAIL`, exit 1. One byte of path decided whether the tree could see a fabricated stamp, and the path that hid it is the path the law requires. *What it taught:* **the two fixtures proved the predicate and neither proved the reach.** Duty 5 passes its test stamps in through `ONE_CLOCK_HEAD_STAMPS`, which bypasses the population walk entirely, so a green witness said only that the comparison worked. A third reading fell out of the same look: **mono's monotonicity leg cannot fail**, since `true_head` is the maximum OF the population -- plant a year-2099 stamp and it becomes the head -- so `MONO_BAD (unlisted false-future)` is structurally unreachable, and duty 5, weighing the head against the LIVE clock, is the only leg with teeth. *Standing:* the population reads folded shelves and the `[_.]` spelling, by `find` rather than a glob; `TRUE_HEAD` now names `20260911.002040`; and the whole-list rewrite -- one `grep -vxF -f` for the erratum, one `awk` for the above-head pass, replacing a `grep -qx` per stamp -- took the scan **5.475s -> 0.192s while reading 30x the population**. Proven by `tools/fixtures/o/one_clock_population_control.sh` under `tools/o/one_clock_witness.rish` as duty 6: **seven legs** planting real files in real directories, both mutations bitten -- the flat glob restored fails four legs, the `_`-only spelling fails the sprigless leg alone. **BOOKED.**

---

*The pin keeps what is open.*
