# REDS %417, %418 -- what a copy carries

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260905.100435` to bring it back under bound as
`%421` landed. Two **CLOSED** rows about the same act from two sides: **copying is the most
invisible way to be wrong, because every step reports success and the error is in the payload.**
`%417` is the counted half of a reading proving a door exists rather than that the door is sound.
`%418`: a newborn clone inherited four signing values by copy, and `git config user.name ""`
succeeds -- so a field carrying nothing seated nothing while every assert read true, and the one
value that was not empty pointed at a directory a rename had moved. Link depth moved with the text.*

**REDS %417 (`20260905.070821`) -- the counted half of Truth proves a door exists, never that the sign on it is right.** *What went wrong:* `MAP.md` -- the page `read-scope.md` seats as *"the walk that replaces the root `ls`"*, read by every ship every lap and cited by 106 living files -- told its reader that `construction/TASKS.md` and `construction/ROADMAP.md` are the live *"what is next"* instruments. Both stopped being that on `20260823.103804`, when their content fused into `ITINERARY.md` and each was left carrying `**Status:** Pointer` on its own third line. MAP was updated four days after the fusion, on `20260827.155213`, and the sentence rode through that pass untouched. It stood thirteen days on the tree's most-walked orientation page. *What caught it:* the QA report card pushed a molt frame at **C/72**, and the frame was worked by reading the page against the tree rather than by trusting the card's own `truth_counted=100`. *What it taught:* **`truth_counted` proves a cited path resolves; it cannot reach the sentence around the path.** MAP scored a clean 100 on 70 citations while two of those seventy were described falsely -- because `construction/TASKS.md` does exist, and existing is the entire question the counted reading asks. The counted half of Truth is a link checker wearing Truth's name, and every stale *claim* lives in the judged half, which runs only when a person reads. `docs-implementation-sync.md` says exactly this in prose -- *"It does not and cannot check whether a doc's behavioral claim is still honestly true"* -- and then names that check *this rule's job*, which is a job with no meter and no schedule. **This is the lantern's second firing:** `%117` was the same shape, an elder first-hour page telling readers the tools *"came with the copy"* while a fresh clone carried neither, every path in it resolving. *Repaired (`20260905.070821`):* the sentence now names the fusion and its date, so the pointer is a fact the page teaches rather than a trap it sets; and `.claude/rules/` joined Room 2 of the seven, having been absent from the room table while the two-floors paragraph three lines above called it an open shelf. **The loom was measured and declined:** the tree holds six files declaring `**Status:** Pointer`, five of them living, and reading all five closed the set in one lap -- `src/README.md` describes its three correctly, and the sixth hit is a RESTED chapter row in `context/LEXICON.md`, which is testimony. A meter over six files costs more than the reading it replaces, so what generalizes is written here rather than into a scan. *What this does not reach:* MAP reads **C/74** after the repair, and that remainder is not a prose fault -- 67 links across 913 words is 7 per 100 against a Door budget of 1, where the root README carries 53 across 2,005 and reads B+. The meter's index door frees the rate only for a page under 100 words, which no guided map can be. That is a setting question, carried to the card rather than answered by padding a page to suit a rate. **BOOKED.**

**REDS %418 (`20260905.081211`) -- a newborn inherited an empty name and a renamed path, and every assert on the way in said yes.** *What went wrong:* `tools/l/birth_a_clone.rish` seats a newborn's signing identity by copying four values from the field -- `user.signingkey`, `user.name`, `user.email`, `gpg.program` -- and asserts each `git config` call succeeded. **`git config user.name ""` succeeds.** So a field carrying nothing seats nothing, four asserts read ok, and the clone is born unable to name its own author. `grain-pheromone` holds an empty `user.name` and `user.email` today and found out on the first lap that tried to close a round. *And the fourth value was worse, because it was not empty -- it was stale.* `gpg.program` is an **absolute path into the field's own tree**, deliberately: one keeper, custody material behind per-clone config. An absolute path is also the one thing a rename breaks in silence. `grain-harvest` became `grain-petrichor` on `20260904`, and pheromone's config went on naming `/home/keeper/grain-harvest/.gnupg-rye/gpg.sh` -- a directory that no longer exists -- through a fetch, a sign, and a commit, each refusing for its own separate reason. **`tracked_link_scan` cannot reach an untracked `.git/config`**, so no guard in this tree could have said so. *What caught it:* the pheromone seat itself, on its first lap that produced work worth landing. It did the work, could not commit it, diagnosed all four causes correctly, refused to seat a GPG secret key in a sandbox, left eleven files staged with a prepared message, and stopped at `GATES-ONLY`. That is the loop behaving exactly as designed, and the fault surfacing at the only moment it could. *What it taught:* **an inherited value is not a checked value, and a successful call is not a seated one.** Copying is the most invisible way to be wrong, because every step reports success and the error is in the payload. Kin to `%416` one hour over -- an empty answer from a broken instrument reading as a clean tree -- and to `%415`, whose sentence this is in a third material: **a sandbox that mounts one tree cannot reach another tree's files.** Pheromone's config points into petrichor's directory, which its own enclosure cannot see; the *host* shell can, and the jail never will. *Repaired (`20260905.081211`):* the four values are read into named bindings and **refused before they are seated** -- an empty `user.signingkey`, `user.name`, `user.email` or `gpg.program` refuses the birth with the field named, rather than seating an empty string. And `gpg.program` is now tested with `test -x` before it is copied, so a wrapper that a rename has moved out from under refuses at birth instead of at the first commit weeks later. *NOT taken, and it is a gate rather than a difficulty:* `.gnupg-rye/` holds `private-keys-v1.d/`. Making pheromone able to sign means placing **secret key material** into a third directory, which is custody gate **3** -- *holding keys* -- and belongs to Keaton's hand alone. The structural question rides with it and is his too: the one-field design assumes every clone can read one directory, and the enclosure gives each tree only itself, so **per-tree GNUPGHOME is the only shape that works under the jail** and that shape is a custody decision rather than an engineering one. **CLOSED** for the inheritance half; the signing half is a named gate.
