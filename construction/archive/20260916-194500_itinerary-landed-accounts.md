# ITINERARY -- landed accounts, shelved `20260916.194500`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The BAKERY account the live card carried before the lap of `20260916.194500`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**BAKERY -- THE KEY COST MORE THAN THE COMPILE, AND `run` PAID IT ON EVERY CALL.** Elder account
[shelved whole](20260916-193716_itinerary-landed-accounts.md), its two asks with it.
**FIRE SEES** (row 2, N=5122): look straight at the uncomfortable point. Neither fault was found by
reading -- one by RUNNING a path nobody runs, one by MUTATING the repair to see if anything objected.
**THE MECHANISM.** `rye/src/main.rye` decided a receipt from eight conditions and asked the four
EXPENSIVE ones first. Four need no file opened -- label, environment, flags, and whether an output
is named -- and each was already necessary. They move ahead of the read as `key_wanted`, so the
toolchain and library are hashed only where a receipt can be consulted or written. **The key is
untouched:** a build wanting one hashes what it hashed before, in the same order, which the
family's eleven flip legs prove by still missing on every one.
**MEASURED** on this pier, warm Zig cache, HEAD's binary and this one timed alternately: `rye run`
on a one-file program **1.22-1.85s to 0.034-0.058s**, `rye build` naming no output **1.88-3.11s to
0.69-1.84s**. The spread is machine load; both are FREE, so run them. `rye run` could NEVER earn a
receipt and paid that hash at **948 tracked sites across 881 files**. The hit path is unchanged and
still pays it -- where the recovered lap's second repair, remembering the compiler's digest under
inode, size, mtime and ctime, would bite. It stays unbuilt and booked.
**THE RED, AND THE SECOND ONE THE REPAIR MADE VISIBLE** (`20260916.193716`, the row).
`unkeyable_reason` asserted four readings were exhaustive; `RYE_BUILD_FRESH` was a fifth and no
reading at all, so a forced-fresh `rye key` answered *this binary's own path could not be resolved*
-- false in every part. Then, because the repair gave the `run` exclusion exactly ONE home, deleting
it became worth planting: all 24 legs stayed GREEN, and `rye run <file> -femit-bin=<out>` earned a
receipt and HIT on its second call, **printing nothing and exiting 0**. Leg 14 could never see it --
it passes no emit flag, so the no-output condition refuses first.
**PROVEN:** three legs, **22 to 25**, each shown to BITE by planting the mutation and lifting it --
the bypass naming itself, an unkeyable flag blamed on the flag rather than on a file the skip never
opened, and `run` given an output to name. `ryekey` GREEN, its own summary moved twenty-two to
twenty-five in the same commit. `tame_style_check` caught my compound assert; it is split.
`width-check` and `ascii-comment` GREEN.
**YOURS:** a member of a conjunction is proven only where every other member would have let it
fail, and this family had two asleep. That reading is not about receipts -- how many other controls
prove a compound condition by the first clause that refuses, and whose lane counts them?
**MINE:** the hit path still hashes 172MB to answer a question a file's mtime settles.
