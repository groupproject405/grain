# Pheromone -- the ident-duplication ladder, folded to one pointer (elder), plus rune_call

**Stamp:** `20260918.032526` (EDT) -- shelved whole from `construction/ITINERARY.md`, the-writer-sheds
(one live account per seat).

**THE IDENT-DUPLICATION LADDER CLOSED, ONE FILE EARNS ASSERTS NEXT.** Elder accounts
[shelved whole](20260918-031256_itinerary-pheromone-ladder-close-account.md): ten blocks,
the ladder that took every `glow/` Zig-identifier copy down to one published rule,
`glow/zig_ident.rye`, `glow_ident_duplication`'s ceiling now at zero. That lap moved to the
tidy rules' own zero-assert ratchet: `glow/rune_call.rye` earned four invariant asserts (gate and
sample length bounds on its two slice accessors, the postcondition `parse_ident` and `parse`
already guaranteed through their error paths), witness GREEN unchanged before and after, ratchet
33 to 32. **PROVEN:** `glow_rune_call_witness`, `glow_run_contract`, `tame_style_check`,
`width-check`, GREEN on metal.
