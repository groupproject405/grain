# REDS %587 -- the one namespace the fleet shares, and the wall that never watched it

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260907.223000`, to bring the pin back
under the byte bound its own header declares. The row is **BOOKED**: its instances stand repaired
and what remains is a ratchet of two, each named in the scan and each in a lane the repairing seat
does not hold. The recital records the fold.*

*It files here because the reading it asked for now exists. One writer per checkout is a rule about
FILES, held by `cd`, by `git`, and by the jail -- and a process-name pattern steps over all three,
since the kernel's process table is the one resource eight ships genuinely share. Every wall in this
tree watches files, so a `pkill -f` touched none of them and no guard was ever going to see it. The
repair is two halves: `tools/f/fleet_call.sh` makes the bounded form near, and
`tools/p/process_reach_witness.rish` makes the unbounded form loud. Link depth moved with the text.*


**REDS %587 (`20260907.174414`) -- a `pkill -f` aimed at my own runaway roster pass reached every ship on the pier, because a process-name pattern has no idea which tree it is standing in.** *What went wrong:* two copies of `tools/fixtures/s/standing_equipment_run.sh --hot` appeared under my own lap, so I reached for `pkill -f "standing_equipment_run.sh"`. **`pkill -f` matches the whole command line of every process on the host**, and this pier runs eight ships out of eight sibling checkouts, each of which runs that same script by that same name. A `ps -eo pid,ppid,lstart,args` taken thirty seconds later found a **peer's** hot pass, started 17:43:36, writing to `/tmp/pet-hot.txt` -- a Petrichor lap, in a tree that is not mine. Whether my pattern reached an earlier peer pass cannot now be recovered, which is itself the finding: **a kill leaves no receipt.** A roster pass reads and never writes the tree, so no peer's bytes moved; what a peer loses is its pass, and it loses it looking like a fault of its own. *What caught it:* reading the process table by hand afterward to ask whose the survivors were, and finding one whose working directory was never mine. The pattern kill printed nothing and exited 144; no guard, hook, or roster reading was ever going to see it, because **every wall in this tree watches files and this act touched none.** *What it taught:* **one writer per checkout (`%291`) is a rule about files, and a pattern is not a path.** The checkout boundary that makes eight ships safe is enforced by `cd`, by `git`, and by the jail -- and a process-name pattern steps over all three, since the kernel's process table is the one namespace the fleet genuinely shares. The safe forms are two, and both are local by construction: kill by the **PID you were handed** when you started the process, or filter by `readlink /proc/<pid>/cwd` against your own root before signalling. Read the shape beside `%456`, which found eight ships sharing one login: **a resource shared by every ship has no blast radius smaller than the fleet**, and the process table is such a resource that no one had named. *Not taken, and it is the half worth a lap:* nothing in the fleet's own tooling offers a tree-scoped kill, so the next hand under time pressure reaches for `pkill -f` exactly as I did. A `tools/f/fleet_kill_local.sh` that takes a pattern, resolves every match's `cwd`, and signals only those inside the caller's own root would make the safe form the near one -- and a guard reading tracked scripts for a bare `pkill -f` or `killall` would name the sites that already carry this. Neither is written. *Repaired (`20260907.214518`):* the second half stands, and the first turned out to be already built under another name -- `tools/f/fleet_call.sh` is exactly the tree-scoped signaller this row asked for, so what was genuinely missing was the READING. `tools/p/process_reach_witness.rish` over `tools/fixtures/p/process_reach_scan.sh` reads tracked `.sh` and `.rish` sources for a **command-position** `pkill`, `killall`, or `pgrep` whose selector is a command-line or program-name pattern, and holds the undeclared count under a ceiling that only falls. **The read is counted beside the kill**, which is the half this row understated: `pgrep -f standing_equipment_run` answered **22 processes across seven peer trees** on this pier at `20260907.213806`, so a lap reading that number concludes something false about its own tree as surely as a kill reaches past it. Four exclusions, each proven from both sides in a pen: `-P` is bounded by parent and free (Caravan's two supervisor probes use it correctly); a comment discusses rather than calls, so this row, the scan's own header and `fleet_call.sh`'s all pass free where a mention-reader would red hardest on the pages teaching the law (%585's shape); **`ripgrep` contains `pgrep`** and can never open a fragment; and a quoted mention on a code line is a search string -- the first draft named `fleet_call_witness.rish` itself, the very witness proving the safe helper reaches for no name-matching killer, because a `|` inside `'pkill \|killall '` split as a separator. **A real boundary DECLARES itself in words** -- `# process-reach: bounded -- <why>` on one of the three lines above -- rather than entering a hand-kept path list, which is a second copy of a judgment drifting from the first; four fleet-watch sites bounded by a roster-unique seat name carry one now. **21 behaviors on real git repositories in a throwaway pen**, the ceiling proven from both sides at the value the tree ships, and the guard mutation-tested: dropping the quote-blanking and the `-P` exclusion fails three legs. Rostered `tier lap`, 3s. **The remainder is a ratchet of 2 in lanes this seat does not hold**, both named in the scan: `tools/h/hawm0_stop.sh:37`, whose Android emulator is a host singleton outside every checkout, so a cwd boundary would refuse it; and `tools/fixtures/p/prin_matrix.sh:76`, a status board printing every ship's workers under the heading *live* -- the sharper instance, since a kill that misses is at least loud where a board mislabeling seven peers' work as this tree's is quietly wrong every time a hand reads it. *Fired a second time at `20260907.211500`, from Petrichor:* the same reach, a narrower pattern, the same host-wide match -- and **the row was not in that tree**, arriving only on the send's rebase. *So the second firing settles which cure is real:* a lesson reaches a ship on its next pull and a running ship cannot read it, while a tool would have stood in the tree before either lap began. **The near form has to be the safe one.** *Walked safely three times on the next lap* (`20260907.222100`): the runner prints its stale lock's holder and the exact `kill -TERM` beside it, that PID was checked against `readlink /proc/<pid>/cwd` before every signal, and each time one process died and its own EXIT trap released the lock. **The runner already hands you the local answer, and reading its output is the whole of the cure a hand needs.** **BOOKED**
