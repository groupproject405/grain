# REDS %359 -- two roster passes ran in one tree for fifty minutes, and nothing in the runner said so

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260830.194500`, **CLOSED** -- the runner
held no lock, so a second cold pass started beside a first and both ran to completion while one
choir cleared the binaries the other was using. Moved here as rows %372 through %375 carried the
living pin past its 24,576-byte bound. Link depth moved with the text.*

---

**REDS %359 (`20260830.100059`) -- two roster passes ran in one tree for fifty minutes, and nothing in the runner said so.** *What went wrong:* `tools/fixtures/s/standing_equipment_run.sh` holds no lock, no pidfile, and no concurrency check, so a second pass starts beside a first and both run to completion. Two cold passes stood in `~/grain-hush` from `20260830.091545` to `20260830.093000`, and the contention is not merely slow: `tools/ca/caravan_suite_witness.rish` clears `caravan/bin/` before it sings, which is REDS %92's own repair for cold-start self-sufficiency, so one pass deletes the binaries the other pass's rungs are partway through using. Both passes also append to the one run card `construction/standing-equipment-runs.kyri` and write the same receipt, so the record of which pass proved what is interleaved. The first pass reached its last guard in 30 minutes where the roster's own header measures 20m20s, and the second needed 16 minutes to reach guard 34. *What caught it:* a process listing taken for an unrelated reason. The trigger was mine and is worth writing down beside the defect: a check spelled `pgrep -f 'grain-hush.*standing_equipment\|standing_equipment_run.sh$'` used a basic-regex alternation against pgrep's ERE, where `\|` is a literal pipe rather than an OR, so the pattern matched nothing, reported the first pass gone, and I started a second. *What it taught:* **a pattern that silently matches nothing reads exactly like an empty world** -- REDS %240's confident wrong zero, arriving in process space rather than file space. And the tree-level half, which outlives the operator error: **the instrument the fleet trusts to measure the tree has no mutual exclusion, and the fleet is six always-on bodies.** The runner already refuses a dirty index (`lap_unclosed`) and a tree that moved under it (`tree_moved`) -- both are readings about the TREE -- and it has nothing to say about a second reader. *Repair, named and not yet taken:* `lock_acquire` already stands in `tools/fixtures/s/shell_portable.sh`, reaps a dead owner by `kill -0`, and bounds its wait; the runner takes it at zero wait and refuses with a named verdict rather than queueing, since a pass that silently waits half an hour is a pass whose reading nobody can date. It waits for its own lap rather than this one: a run was in flight while this row was written, REDS %221 records that editing the runner mid-run kills the shell, and a runner edited between a cold open and a hot close would measure the commit with different code than it opened with. `tools/fixtures/s/standing_equipment_control.sh` is where both directions get proven. *Repaired (`20260830.124924`):* the runner sources `tools/fixtures/s/shell_portable.sh` and takes `lock_acquire "$lock" 0` on `construction/standing-equipment-run.lock.d` before it measures anything -- ahead of the tree digest and ahead of the unclosed-lap refusal, since that refusal tells a hand to commit and a hand committing while another pass measures moves the tree under it. A pass that finds the lock held prints `run_lock=in_flight pid=N` and `run_verdict=run_in_flight`, runs no guard, and writes no card. **The release is armed only on the side that acquired**, inside the same `trap` that clears the pen: a refusing pass that released would free the holder's lock and walk a third pass straight in. The lock path is relative to the repository root, so the six-body fleet's other trees are lawful concurrency -- measured while this repair was written, `~/grain-dream` ran its own `--hot` pass beside this tree's cold one and neither touched the other. Where the lock's room is absent -- a pen outside `construction/` -- the pass prints `run_lock=skipped_no_room` and runs, the same shape the hit ledger and the receipt already keep, because a silent skip is how the reading would go quietly false the day that room moved. Eleven behaviours proven in `tools/fixtures/s/standing_equipment_control.sh`, and each from both sides on metal: a runner stripped of the acquisition flips five readings to `no`, a runner that releases on the refusing side flips **only** `refusal_keeps_holders_lock`, and one that never releases flips only `lock_released_at_exit` -- so the load-bearing case is proven load-bearing rather than assumed. `tools/s/standing_equipment_witness.rish` carries the eleven asserts and reds by name on the planted fault. The witness's own scan leg still refuses on this pier for `runs_red=1`, which is `rule_twin` at custody gate %7 rather than anything this repair touches; run against a card that gate has not reddened it reads GREEN. **CLOSED.**
