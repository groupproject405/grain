# REDS %692 -- a plant is a subject, never a practice

*Folded from the living pin [`../REDS.md`](../REDS.md) on the lap that booked it. The pin stood at
40,949 of the 40,960 its own header declares -- eleven bytes -- so a row booked and closed inside one
lap folds rather than pushing the pin past its bound, which is the move `%681` and `%548` each made
before it. Booked as `%689` from a local read, renumbered to `%690`, `%691` and `%692` across three rebases --
THREE peers booked ahead of it inside one hour, each carrying an earlier stamp. The derived-spine
law moved it three times and cost one line each time, since every citation spelled the
stamp rather than the number. Rule 4, paid and cheap, exactly as it was written.*

*The clause worth carrying forward. A census reads a source for an idiom, and a MATCHED SPAN CARRIES
NO POSITION -- so a scan cannot tell a line this file executes from a line this file writes into a
pen and hands to another interpreter. `tools/c/convergence_census.sh` met that from one side on
`20260909`, where a write inside a `git filter-branch --tree-filter` string admitted a tool that
writes nothing. `tools/fixtures/e/elf_machine_census_scan.sh` met it from the other side a day later,
where thirteen plants inside a control read as thirteen regressions and refused a 226-guard roster.*

*What made this one different from an ordinary false positive: the offending lines COULD NOT BE
REMOVED. A control proves a guard by carrying the guard's subject, so the plants are the proof. A
census that counts them has asked a control to stop working, and no hand can lower the reading.*

*The general shape, for whoever meets it next: when a reading counts a shape, ask whether anything in
the tree is obliged to CARRY that shape in order to prove it. Test corpora, controls, fixtures, and
documentation examples all are, and each of them will one day arrive inside a census that never
thought about them.*

*And the honest limit, kept because it is the useful half: measured on this tree the position clause
removes nothing outside controls. The name clause carried the repair; the position clause is a wall
for the next plant written where a name list cannot see it.*


**REDS %692 (`20260910.045332`) -- a control landed the shape a census counts, and the census read its plants as thirteen live sites.** *What went wrong:* `tools/fixtures/e/elf_machine_census_scan.sh` counts the tracked runners that still prove an architecture by reading `file`'s prose, under a ceiling of **3** that only falls. It read `sites=3 files=3` from `20260906` until `c496ebba9` landed `tools/fixtures/s/self_matching_assert_control.sh`, whose job is to prove a guard against exactly that shape -- so its plants spell that shape thirteen times -- nine inside heredoc bodies it writes into a pen, four inside `printf` formats and a case argument. The census read every line of every source, and a line carries no position, so a control doing its job read as thirteen regressions. *What caught it:* the cold roster open -- `elf_machine` red, and `standing_equipment` `verdict=roster_broken` behind it, refusing all 226 guards over a reading no hand could lower: the plants cannot be removed without removing the proof. *What it taught:* **a plant is a subject, never a practice**, and this is the second room to learn it. `tools/c/convergence_census.sh` closed the same fault from the other side five days ago -- a WRITE inside a `--tree-filter` string admitting a tool that writes nothing -- with a shell-lexer walk answering whether a line began outside every quoted region. A lantern that fires twice becomes a loom, so that walk moved to `tools/fixtures/l/live_lines.sh` and both censuses read it. *Repaired:* two clauses, each measured alone rather than as a pair, since this family's denominator has been wrong six times -- a site read off a live line takes 16 to 7, and skipping `*_control.sh` (which the convergence census already does by the same name) takes 7 to 3, `verdict=under_ceiling`. **Measured honestly: outside controls the live-line strand removes nothing today** -- both readings land on the same three sites -- so the control clause carries this repair and the position clause catches the next plant written where a name list cannot see it. Nine new legs in `tools/fixtures/e/elf_machine_control.sh` prove the counting rule for the first time -- both refusals shown from the elder side, the ceiling proven at three sites and four: **pass=38 fail=0**. The extraction is proven faithful by its own numbers -- the convergence census reads `candidates=12 candidates_proven=10` before and after, and its control `legs_pass=43 fail=0`. One red arrived inside the repair and is kept here: sourcing the library from `$root` broke every leg of that control, because `CONV_ROOT` points the census at a PEN, and a tool's own home is not the tree it reads. **CLOSED**
