# REDS -- the dock that could not deliver

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven on metal and held by a rostered witness
**Folded:** `20260911.193818` from [`../REDS.md`](../REDS.md)

One row, born on its shelf because the pin had no room for it -- `reds_pin_capacity_scan.sh` read
`rows_that_fit=0` with one foldable row standing in another lane, and the same door `%660` and
`%716` came through. It is the third asking of one question in a single day, and the first to
find that the answer changes scale: `%716` proved that **a wall built for one verb is a wall for
one verb**, and this row proves that **the unit which must land whole is the dock rather than the
file**. A near refusal is invisible at the far end, so a dock left holding a verifying vessel with
no cargo costs more than a dock left holding nothing at all. Read it beside
[`REDS-the-wall-built-for-one-verb-rows-716.md`](REDS-the-wall-built-for-one-verb-rows-716.md) and
[`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %718 (`20260911.193818`) -- a refused carry left a far dock advertising cargo it could not deliver, and every wall said yes.** *What went wrong:* `carry_cross` in `amphora/src/main.rye` assembled the far vessel at the dock's own `vessel.bron` and ferried the resin bodies afterward, so a refusal between those steps left the dock holding a listing whose bodies had yet to arrive. Measured on metal `20260911.183720`, ring-1, the plant a forged near resin body so `ferry_prove_resins` answers `cargo unproven` -- production path, no witness seam: a **first** carry refuses at the near end and leaves **1,565 bytes** at `dock/vessel.bron`, byte-equal to the near vessel, `vessel-core verify` **GREEN** and `vessel-seal open-check` **GREEN**, beside **zero** of the four bodies its manifest names. A far `amphora restore` passes the carry walls, passes the restore walls, reads `files=4 plain_bytes=378`, and only then answers `resin absent`. *What caught it:* my own card row from three hours earlier -- *CARRY has yet to be asked this question* -- run rather than reasoned about, on the earth rota, which takes the concrete fact at the door. *What it taught:* **the unit that must land whole is the dock rather than the file.** A near refusal is invisible at the far end, so a half-made dock costs more than a lost one: an absent vessel says nothing arrived, a verifying one says everything did. *Repaired:* the assembly lands on a `.crossing` scratch inside the dock, the far walls and the far manifest read the scratch, the resins ferry beside it, and `rename(2)` -- one filesystem, so POSIX makes it atomic -- lands `vessel.bron` once every body is across; `refuse_and_clear` takes the suffix it may remove and sweeps the scratch on every refusal. **8 legs** under `tools/am/amphora_carry_atomic_witness.rish`, `tier lap`, the elder shape rebuilt in a pen and watched leaving a verifying bodyless dock over the plant the repaired binary leaves empty. **CLOSED**
