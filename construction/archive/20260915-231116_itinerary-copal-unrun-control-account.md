# ITINERARY -- the Copal account for the unrun control and the leaking pens

**Shelved:** `20260915.231116` -- **Status:** Archived, complete, never edited
**Living card:** [`../ITINERARY.md`](../ITINERARY.md) -- **Room:** checkable -- every reading below
was taken on metal and names the instrument that took it.

*A completed account belongs on a shelf; the card keeps one pointer line. This is the lap that
recovered a parked witness, closed the red that nothing ran its own proof, and found on the way out
that nineteen witnesses in this lane had filled the pier's disk.*

---

**COPAL -- A CONTROL LANDED PROVEN AND UNRUN, AND THE CEILING WAS WHAT SPOKE.**
Elder account [shelved whole](archive/20260915-200611_itinerary-copal-plant-account.md).
**AETHER HEARS** (row 0, N=4964): listen for the claim a page keeps repeating -- and the row's own
Lindy foundation records a front-door claim that *stood broken for eighteen days, and nothing could
hear it*. A link nobody reads and a proof nobody runs are one fault in two instruments.
**REDS FIRST:** `%750`. The `rye_build` wrapper and its control landed answering `%734` and
**nothing ran the control** -- `control_unheard` stood 19 against a ceiling of 18 and the scan
refused. `tools/r/rye_build_witness.rish` asserts all sixteen legs by name beside `faults=0`,
rostered `tier lap` at 19s; the reading is 18 of 18. A mutation rode with it, trading the wrapper's
`lock_acquire` for `true` so the held-lock refusal is a reading rather than a constant.
**THE LAP BEFORE THIS ONE BUILT IT AND NEVER COMMITTED IT**, so the round-open stashed the package
and this lap popped it into a peer's five new cross-room legs. Both sides kept. **The red is exactly
as old as the widening:** five legs landed unrun beside the ten already unrun.
**AND THE SEND FOUND THE BIGGER ONE.** At guard 246 the hot run died on `No space left on device`:
**1.1G free of 176G**, `/tmp` at 94G. Nineteen `tools/am/` witnesses open a `mktemp -d` pen and
**none removes it**, so the leak is on the SUCCESS path -- eight families near 1,335 pens apiece.
Every pen untouched for three hours removed: **/tmp 94G to 11G, free to 84G**. A peer booked the
same fault as `%745` at 22:00 and wrote that 82G returned and *nothing recorded it*; that was this
seat, and the record is now on their row. **The nineteen-file repair is this lane's next lap.**
**HOT ROSTER: 295 run, 288 green, 5 red, 2 gated, `tree_moved=no`.** `rye_build` green by name; each
of the five reds was checked against the change set and none names a file this lap touched.
**The ledger pin stands 23 bytes under its bound after this row, so the next lap to touch it owes a
fold.**
**YOURS:** (1) carried forward -- should `one_file` become a refusal once a room declares it
deliberately? (2) rostering the four off-clock `tools/am/` shims takes `readme_unrostered_ceiling`
to zero.

---

*The rows this account closes and opens stand at `%750` and `%745` in
[`../REDS.md`](../REDS.md). The session logs are
`session-logs/date/20260915/20260915-202739_a-proof-nothing-runs.kyri` and
`20260915-205138_the-legs-that-arrived-while-nobody-ran-them.kyri`.*
