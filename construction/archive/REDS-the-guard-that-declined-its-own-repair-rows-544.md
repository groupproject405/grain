# REDS shelf -- the guard that declined its own repair, row %544

**Language:** EN
**Stamp:** `20260907.062649`
**Status:** Shelf -- immutable once written
**Rows:** `%544` -- folded from [`../REDS.md`](../REDS.md) on `20260907.080000`
**Voice:** Kyri

The shared-pen guard read a wipe off the line holding the path, so a pen assigned to a variable on
one line and `rm -rf`d on another read as a **hold**. The lethal subset -- the files that take a
peer's in-flight state down rather than merely interleaving with it -- counted **four** where the
tree held **six**.

**The part worth keeping is not the miscount.** The scan's own header had already named this shape
and declined the repair, citing a measurement: *zero carry that shape now*, counted across the 54
files it read as holding. That count was taken by the line-scoped reading the paragraph was
defending, so it could not see the shape it was measuring. A limitation written down honestly still
hides its own size, because the sentence naming a blind spot is written from inside it.

**Repaired `20260907.075500`** by two passes over each file: the first collects every variable
assigned a constant pen, matched by the one shape all five spellings share -- a name, an `=`, an
optional quote, the token; the second reads an `rm -rf` line naming one of those variables as a wipe
of that pen, in the three spellings this tree writes (`$pen`, `${pen}`, and the bare word a Rishi
list passes). It named **exactly the two files** the hand-count had found and no third, which is the
corroboration worth having -- a widened meter finding *more* than the hand would have been a new
false-positive class wearing a repair's clothes.

**The ceiling rose 4 to 6, the only raise this ratchet has taken.** Holding four would gate on a
number this row proves was measured wrong. The tree did not worsen; the meter began reading what
already stood there, and it falls to 4 the moment the two remaining files take `mktemp -d` pens.
Those two -- `tools/i/ios_app_shell_witness.rish` and `tools/m/macos_app_bundle_witness.rish` -- are
LOCA-lane surface witnesses wanting a booted simulator to prove GREEN, so a Linux pier names them
for their owner rather than editing them blind.

**Why this row folds on the lap that repaired it.** The repair's own account took
`construction/REDS.md` **2,985 bytes past** its 40,960 ceiling, and this row was the only BOOKED one
standing that belongs to this seat -- so folding it needed no peer's agreement.

---


**REDS %544 (`20260907.062649`) -- the shared-pen guard reads a wipe off the line holding the path, so a wipe one line below it, naming the variable, reads as harmless.** *What went wrong:* `tools/fixtures/s/shared_pen_scan.sh` classifies a site `wipe` or `hold` from the whole line its `/tmp/` token stands on. The commonest shape declares the pen on one line and wipes it on another -- `let pen = "/tmp/grain_ios_shell_pen"`, then `run ["rm" "-rf" pen]` sixteen lines on -- and that second line holds no `/tmp/` token, so the site reads `hold`. The wiping count is thus low, and it is the count the guard's own header says to drive to zero, since a file that merely writes a shared pen may interleave and survive while one that `rm -rf`s it takes a peer's in-flight state down. *What caught it:* the Amphora pen repair of the same hour -- six of that family's seven constant-pen witnesses wipe, and the scan read **three**. Crossing every `hold` file against a plain grep for `rm -r` named **six** candidates tree-wide: reading closed **two as genuine wipers hidden from the count**, `tools/i/ios_app_shell_witness.rish:19,35` and `tools/m/macos_app_bundle_witness.rish:17,30`, both rostered; one as a true hold, `instrument_absence_control.sh`, whose `rm -rf` targets a `mktemp` pen; and three as this lane's own, now repaired. The honest population is **6**; the scan reads **4**. *What it taught:* **the header already named this under-read and declined it -- "a variable tracker for one live instance is a cost with nothing to buy" -- and the premise failed rather than the reasoning.** There were six, half in one lane, invisible for exactly the reason cited as making them rare: a limitation written down honestly still hides its own size, because the sentence naming a blind spot is written from inside it. *Repaired (`20260907.075500`):* the scan takes **two passes** over each file -- the first collects every variable assigned a constant pen, by the one shape all five spellings share (a name, an `=`, an optional quote, the token); the second reads a `rm -rf` line naming one of those variables as a wipe of that pen, in the three spellings this tree writes (`$pen`, `${pen}`, and the bare word a Rishi list passes). It found **exactly the two files the hand-count named** and no others, which is the corroboration worth having: `wiping_files` 4 -> 6, `constant_pen_files` unchanged at 54. Five behaviors prove it in both directions on real repositories in a throwaway pen -- a shell `"$pen"` wipe and a Rishi list wipe each counted, a variable named by `mktemp` freed, and a wipe of some OTHER name inside a file that holds a pen freed while its pen stays counted, so the tracker cannot become a hole either way. Control 27 -> **32** behaviors, witness GREEN. *And the ceiling was re-seated 4 -> 6 rather than held*, which is the only raise this ratchet has taken: the tree did not get worse, the meter started reading what was already there, and holding a number published against a population known to be mis-measured would gate on a reading this row proves wrong. It falls to 4 the moment the two are repaired. *Still not taken, and named for its owner:* `tools/i/ios_app_shell_witness.rish` and `tools/m/macos_app_bundle_witness.rish` both wipe a `let`-held constant pen and both need a booted simulator to prove GREEN, so a Linux pier can name them and must not edit them blind. **BOOKED.**
