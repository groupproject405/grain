# ITINERARY -- landed accounts, shelved `20260916.110747`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer
**Recovered `20260916.195622`, and the living reading is elsewhere.** This account was written by a
pheromone lap that died inside its send, so it never reached a ref and stood only in this tree's
round-open stash. The work it describes landed at stamp `20260916.195622` under
[`20260916-195515_itinerary-landed-accounts.md`](20260916-195515_itinerary-landed-accounts.md),
which carries the standing reading; the design call this page argued -- the record naming the token
KIND, and the kind word travelling from the caller because one scanner serves the `%tag` and ident
paths both -- is what landed, its `%tag` leg proven by a mutation. Read here for what that lap saw;
read there for what stands.

The PHEROMONE account the live card carried before the lap of `20260916.110747`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**PHEROMONE -- FOUR CEILINGS ANSWERED IN ONE WORD, AND EACH GOT ITS OWN LINE.**
**WATER TASTES** (row 3, N=5102, advanced by hand past row 2, read this morning): read up close,
run the actual thing. The row's dual seat -- *How We Grow a Language* -- names the move this lap
made: **diverge by strengthening, never by rewriting**, additive and behavior-preserving.
**REDS FIRST.** The cold endurance run named two in my own tree and one command closed both:
`rye/bin/rye` stood **30,638 seconds** behind `rye/src/main.rye`, so `built_tool_freshness` read
`stale_tool` and `ryekey` read `another spelling of one toolchain changed the key`. `sh
rye/bootstrap.sh` -- the same cure copal published `20260916.073000`, met a third time. Both GREEN
on metal after, `ryekey` at 22 legs.
**THE MECHANISM.** `glow/tokens.rye` returned `error.BadToken` from four different ceiling
comparisons -- `max_name_len` in `scan_ident_span`, `max_cord_lit_bytes` in `lex_cord_lit`,
`max_hex_lit_digits` in `lex_hex_lit`, and `max_src_len` in the entry itself. A `refusal.Slot`
pointer now threads through the six private lexers, `tokenize_reporting` stands beside `tokenize`,
and each of the four fills a `glow/refusal.rye` record naming field, value, ceiling, unit and
reason. `tokenize` keeps its published signature and its bytes, delegating through a scratch slot --
`rune_shape.parse_source`'s own shape, one module over.
**THE FOUR NAME A KIND RATHER THAN A FACE**, which is the one design call here. `rune_shape` names
the face that would not fit; a lexer cannot, because the text that overran is exactly the text that
will not fit inside `max_field_name_bytes`. So they answer `ident`, `tag`, `cord`, `hex` and
`source`, and `scan_ident_span` takes the kind word from its caller rather than guessing it -- the
`%tag` path and the ident path share one scanner and two names.
**THE HEX CEILING COUNTS DIGITS.** Two digits make one byte, so a shared `byte` unit would report
half the number `max_hex_lit_digits` is written in. `Unit` gains `byte` and `digit`; `Reason` gains
four; the 192-byte line derivation was re-read against the widest of them (`too-many-hex-digits` at
19, `digit` at 5) and stands where it stood.
**PROVEN:** `glow_refusal_witness` **9 legs to 16**, GREEN on metal, and three mutations planted and
lifted -- the hex unit swapped to `byte`, the tag kind word replaced by `ident`, the source record
dropped -- each bitten. `glow_tokens_witness` GREEN unchanged, which is the published entry's own
receipt. `glow_ceiling_refusal` reads `recorded` **4 to 8** and `uncovered` **44 to 40**, ceiling
lowered to meet it with no slack, exactly the fall its own arc named last lap.
**THE NEXT FALL, NAMED:** 3 `error.TooManyLines` and 2 `error.TooManyArms` take it to 35. The 35
`error.BadIdent` beneath them are Keaton's open door on this card and no lap opens it.
**MY QA FRAME, CLOSED BY MEASURING** (`.claude/rules/quality-assurance.md`). `glow/refusal.rye`
grades **C+/76**, and `register` is why: 6 of its 8 counted head sentences are counted by the single
word `refusal` -- the module's name, its type's name, and its subject. Struck tree-wide in a pen the
file falls 23 negatives to 12 and still reads 35 percent, so a remainder is real; yet 4 of those 12
are counted on a DECLARATION line -- `pub const Slot = ?Refusal;` is one whole sentence scoring
negative because a type is named `Refusal`. Contorting a refusal record's vocabulary to please a
word counter is being too smart about it, so the frame closes on the reading rather than a sweep.
**YOURS:** should the register reading over a PROGRAM hold out declaration lines the way
`qa_report_card` already holds out declaration docs and reference lines? It reports
`program_decl_lines=24` and scores them anyway.
**MINE, AND HONEST:** the cold endurance run was **stopped at 213 guards of 310** through
`fleet_call.sh --signal TERM`, bounded to this tree and refusing three peers out loud, because six
ships shared the pier and it had spent 43 minutes reaching 213. **163 green, 5 red, 2 gated**; the
two mine are closed above, and `rune_assert_sweep`, `instrument_refusal` and `remember_git_nib` are
a peer's ratchet, a peer's booked site, and this lap's own nib, which the send carries.
