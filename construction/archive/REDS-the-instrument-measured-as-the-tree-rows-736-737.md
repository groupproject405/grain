# REDS %736 and %737 -- the instrument measured as the tree

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260915.213138`, when the pin stood 291
bytes above the 65,536 its own header declares with 14 of 26 rows foldable. Both rows closed within
hours of each other, in one room, on one reading; the pin keeps what is open, so a pair closed
together folds rather than pushing the page past its bound.*

*The clause worth carrying forward. These two are one fault read from two sides: **a guard that
counts its own instrument's material has measured the instrument rather than the tree.** `%736`
compared a file with itself, because a recursive grep follows a symlink and a room's roof was linked
to its own source -- three of six discovered families were one file wearing two names, each reading
`declarations=2 signatures=1 status=agree`, which is agreement proven by counting one thing twice.
`%737` measured twelve plant rooms as a room's drift, because a control's material is built to
differ and the first symlinked plant nominated itself as canon for every other pen's file of the same
basename. The repairs are the same shape from both sides: count distinct files by inode rather than
by name, and read a control's own plants past while COUNTING what was read past, since an exclusion
nobody can see is a door. What neither row can reach is the general case -- nothing in this tree asks,
of an arbitrary reading, whether its population includes the fixtures that prove it.*

---

**REDS %736 (`20260915.174535`) -- a guard counted one file as two roofs and called their agreement proven.** *What went wrong:* `amphora_bounds_agree` reading (1) collects same-name const declarations with a recursive grep, which follows a symlink, and `amphora/src/manifest_entry.rye` links to `amphora/manifest_entry.rye`; three of its six discovered families -- `max_listed_names`, `max_mark_len`, `max_name_len` -- were that one file compared with itself, each reading `declarations=2 signatures=1 status=agree`. The same reading states its law over every same-name const and matches a spelled `u<N>` width alone, so the room's wire words -- `vessel_format`, `shoulder_ring1_wire`, `mark_plain_bytes`, each spelled in two real roofs -- stood outside all four readings. *What caught it:* a fire-row reading of the room's own const census against the guard's output; `grep -c` found no `vessel_format` line in a scan whose header claims every same-name const. *What it taught:* **a comparison is only a proof once you have counted the things compared.** Roofs are distinct files by inode now, a family standing on one file reads `one_file` rather than `agree`, and reading (5) reads word-valued consts by the same law. Both proven by mutation. **CLOSED** on `tools/am/amphora_bounds_agree.rish` GREEN with sixteen planted pens.

**REDS %737 (`20260915.200351`) -- a planted control's own files were measured as a room's drift.** *What went wrong:* `tools/fixtures/c/copy_lag_scan.sh` reads every tracked `.rye` basename held both as a symlink and as a regular file, resolves the symlinks to one canon, and compares each other regular file against it. Twelve plant rooms under `tools/fixtures/amphora_bounds_plants/` name their files `a.rye` and `b.rye`, so every pen shares both basenames with every other, and the first SYMLINKED plant to land -- `roof_symlink/b.rye`, a real link onto its own `a.rye`, planted to prove a roof is a file rather than a path -- nominated `roof_symlink/a.rye` as canon for every `b.rye` in the tree. Two unrelated pen files then read `behind` it, `basenames_both_ways` rose 108 to 110 over a header naming 108, and sixteen of the reading's eighteen `sibling` lines were plants. *What caught it:* this seat's own hot roster pass, `copy_lag red`, on the lap that staged the symlink plant and before it was committed. *What it taught:* **a plant is built to differ, so measuring one as drift measures the plant.** A control's material is the one population whose disagreement IS the proof, so a reading that admits it is reading its own instrument. The 64 fixture `.rye` paths are read past now and COUNTED as `fixtures_read_past`, since an exclusion nobody can see is a door -- the same exclusion `tools/fixtures/a/ascii_document_scan.sh` makes, for the same stated reason. Proven from both sides in the pen: the identical lagging file reads `behind=0` under `tools/fixtures/` and `behind=1` in an ordinary room, so the filter reads a PATH rather than a shape, and removing it reds three legs. Pen 31 to 36. **CLOSED** on `tools/co/copy_lag_witness.rish` GREEN.
