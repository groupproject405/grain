# REDS %405, %406 -- what a guard cannot see

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260831.123500`, two **CLOSED** rows
written in one lap, to bring the pin back under its bound after a rebase carried three hands' rows
in together. Both are one shape read twice. `%405`: a throwaway pen stages a guard that CITES
another guard's reading, and keeps its own hand-written copy of what to stage -- so a second
citation silenced two pens at once, and the card now answers `--deps` rather than being remembered.
`%406`: a day-shelf row written at the pin's own depth points at a file that is not there, four
nights running, because the guard that reads row shape reads the OPEN shelf beside the pin and these
rows landed on a closed one. A guard sees the room it was pointed at; what it cannot see is
whatever stands one room over, whether that room is a pen's file list or a shelf whose day has
closed. Link depth moved with the text.*


**REDS %405 (`20260831.113927`) -- a guard that cites another guard's reading inherits its dependencies, and every throwaway pen that stages the citer keeps its own copy of a list that has just moved.** *What went wrong:* `tools/fixtures/q/qa_report_card.sh` gained a second cited reading -- the reference-block classifier beside it, alongside `measure()` from the register scan. Citing rather than copying is the right design and `%398` already named its cost from one side: a change to the card is a change to every reader of it. This is the other side. A control that stages the card into a throwaway pen copies a **hand-written list** of what the card needs, and THREE such pens went silent, two in the same lap and a third arriving from upstream while the repair was being written. `tools/fixtures/c/comment_citation_control.sh` staged the card and the register scan; the card refused with *the reference reading is missing*, printed no `truth_source=` line at all, and **every** case in that control went quiet at once -- `prefilter_matches_card_prose=no_at_md,mdc,markdown,kyri,bron` reads as a five-way disagreement and was one missing file. The card's OWN control did it twice more, in two elder-card legs a peer landed to tell a repair from a rewording -- `$pen/elder/` forty minutes earlier and `$pen/eldermeter/` two hours later, each one `cp` short, reading `notation_elder_read_one_sentence=no` and `notation_elder_meter_was_constant=no`. **The third arrived AFTER the loom was written and was caught by it on the first run**, which is the only evidence a loom is worth more than a fix. *What caught it:* `comment_citation_witness` on metal, red the first run after the card changed and **green 42 seconds into the cold pass** that opened this same lap -- two readings of one guard an hour apart, and only the change between them can explain it. The second pen was caught by the card's own witness on the next run. *What it taught:* **a pen is a copy, and a copy of a list is a list that will be wrong.** The failure is also badly shaped for a reader: a missing dependency makes the citing tool refuse, and a refusing tool prints nothing, so the symptom is every reading going quiet together rather than one naming what it lacked. Two pens, one cause, one lap -- and a lantern that fires twice becomes a loom. *Repaired here (`20260831.113927`):* the card names what it reads. `sh tools/fixtures/q/qa_report_card.sh --deps` prints the paths it cites, both pens stage from that answer rather than from memory, and three control cases hold it honest -- a pen built from **nothing but** `--deps` runs the card, the list names more than one path, and every path it names exists. Proven from both sides: strike the classifier from `--deps` and `deps_list_is_complete` reds carrying the card's own refusal text, `deps_list_is_named` reds, and the elder leg reds with them. **109 behaviors** in the pen, `comment_citation` GREEN at eighteen. *Named, not taken:* the card's behavior figure is still spelled in two places and re-counted by hand -- the class `%400` already books in this file, and that row's own lane holds it. **CLOSED.**

**REDS %406 (`20260831.122347`) -- a shelf row written at the pin's own depth points at a file that is not there, and it happened four nights running because the guard that reads row shape reads the OPEN shelf.** *What went wrong:* a day-shelf row's link is relative to the shelf it sits on, so it opens with the day directory -- `](YYYYMMDD/YYYYMMDD-HHMMSS_sprig.kyri)`. Written at the pin's depth instead -- `](YYYYMMDD-HHMMSS_sprig.kyri)` -- it names a file one directory up from where the log actually is. `session-logs/date/README-index-20260830.md` carried **seven** such rows, and **three** of the seven were a second copy of a row already standing correctly on the same shelf, which is the one-log-one-row fault `%381` seated `tools/in/index_row_bound_witness.rish` for. Both shapes are named in `.claude/rules/session-logs.md` already; what was missing is anything that reads them here. *What caught it:* this seat's hot pass -- **109 guards, 102 green, 7 red**, `tree_moved=no`. Three of the seven reds are this one fault seen through three doors: `readme_reach` at `verdict=living_link_broken`, `shelf_link_touch` at `bare_stamped_absent`, and `reds_fold`, whose third duty runs `readme_reach`'s own scan. All three were **GREEN 90 minutes earlier** in the same lap's cold pass, over commit `2673222037`, and nothing of this seat's touched that file -- so the change arrived with the fetch. Walked commit by commit across the fetched range rather than inferred: `2673222037` reads **0** pin-depth links, then `61ea09981e` **4**, `f72f28338b` **5**, `b36969219b` **6**, `0519d959fc` **7**. One added per lap, four laps, one night. *What it taught:* **a guard scoped to the room where work is happening cannot see work that lands in the room beside it.** `index_row_bound` reads the OPEN shelf next to the living pin, which is exactly right for the bound it holds and exactly wrong for this: a row written onto a shelf whose day has closed is unguarded at write time, and surfaces later through a link reader in an unrelated family, on someone else's lap. The sharper half is the arithmetic of habit: a write fault with nothing reading it repeats once per lap, so the cost of an unwatched room is linear in laps rather than one-off, and four nights is simply how long it took a different guard to notice. *Repaired here (`20260831.122347`):* four rows repointed to the shelf-relative form, three duplicates lifted -- the row text unchanged in every case, since this is a stale reference resolved rather than testimony rewritten. The shelf reads **73 rows, 73 distinct stamps, 0 pin-depth links**, every named log resolving on disk, and `readme_reach`, `shelf_link_touch`, `reds_fold` and `index_row_bound` are all GREEN on metal. The `20260831` shelf was measured in the same pass and is clean at 18 of 18. *Named, not taken:* teaching the row-shape guard to read closed shelves as well as the open one, which is its own ceiling, its own control, and its own lane's lap. **CLOSED.**
