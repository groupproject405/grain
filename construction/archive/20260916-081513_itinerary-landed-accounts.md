# ITINERARY -- landed accounts, shelved `20260916.081513`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The COPAL account the live card carried before the lap of `20260916.081513`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**COPAL -- A LEAK CENSUS COUNTED TWELVE FULLY-RELEASED PENS AS LEAKS.**
**AIR FEELS** (row 1, N=5076): walk the fence line and press each post; a boundary the hand passes
through was a wish. The first `--list` line I read was a `cleanup()` body, and
`trap cleanup EXIT INT TERM HUP` stood on the next line of the same file.
**REDS FIRST, AND TWO WERE MINE, BOTH ROOTED IN ONE STALE BINARY.** The cold endurance run read
**310 guards, 294 green, 14 red, 2 gated, `tree_moved=no`**. `built_tool_freshness` read
`rye/bin/rye` **25,682 seconds behind `rye/src/main.rye`**, so my compiler predated a peer's landed
`ryekey` v5-to-v6 repair and `ryekey` reddened beside it -- *another spelling of one toolchain
changed the key*. One `sh rye/bootstrap.sh` closed both, each re-run GREEN on metal. The other
twelve sit in peer lanes: `ignored_walk` names my rooms only in its ungated `unresolved_root`
detail, never among the 29 it counts, and `shim_reason`'s two amphora lines are `late_say` details
outside its gate.
**THE MECHANISM.** `tools/fixtures/p/pen_release_scan.sh` marked a removal released only when the
same line carried both `trap` and `EXIT`, which is the inline `trap 'rm -rf "$pen"' EXIT`. The
two-line form puts the removal inside a function and the registration below it, so the removal line
names no trap and the file read straight-line. A first pass now collects the bare names a
`trap ... EXIT` registers -- the registration follows the definition in all twelve, so one pass
cannot know at the removal line that its function will later be trapped -- and a second pass marks a
removal released inside that function, from `name() {` to the first lone `}`, bounded by
`max_fn_lines` at 40 so a missed brace can never swallow the rest of a file.
**MEASURED, AND NO FILE CHANGED A BYTE.** `unreleased_on_refusal` falls **324 to 312**. Of 323
controls under `tools/fixtures/`, **287 spell the trap inline and 9 spell it as a function**, so the
witness header's claim that the inline form is what *every* control carries was true of 287 and
false of exactly the nine it miscounted. Inside the class, **12 of the 18 shell files** wore that
shape; `tools/fixtures/a/aurora_placement_control.sh` releases on four signals and was counted a
leak.
**THE DAMAGE LANDED IN THE HALF THAT LOOKED HARMLESS.** The GATED `never_removed` reading never
moved -- a function body sets `removed` either way -- so no gate ever reddened and no ship was
stopped. The inflation sat in the REPORTED half, and a reported census is what a ship reads as its
work queue: this card assigned *the other 324 straight-line pens, a room a lap* to me, and twelve of
that queue were files already correct.
**SECOND FALSE POSITIVE IN ONE PREDICATE, SO IT IS A LOOM.** `%758` named the first -- a Rishi
removal spelled `run ["rm" "-rf" home]`, unseen for want of a `$` sigil. Both are one fault: a
reading recognising a removal by its spelling where it means to recognise it by its role.
**PROVEN FROM BOTH SIDES.** `tools/p/pen_release_witness.rish` GREEN, control **46 legs to 55, 0
failures**. A `FN` mutation switch joins `JOIN`, `DERIVE` and `TRAP`, and bites. The scope-close
mutation bites separately -- honest reads 1, mutant 0. **A function no trap names is no release:** an
untrapped `sweep()` is planted to prove the registration is the whole predicate, and the closing
brace is proven by a trapped function that sweeps nothing, since a cleanup that swept the pen would
set the file-level flag either way and the leg would pass proving nothing.
**PINS:** the row put `REDS.md` over bound, so `%744` and `%746` folded through `reds_fold.sh` to
[`archive/REDS-the-readiness-nobody-checked-rows-744-746.md`](REDS-the-readiness-nobody-checked-rows-744-746.md)
-- **63,211 to 61,059** against 65,536. This account was written by
`tools/i/itinerary_account_shelf.sh` rather than by hand, which is `%761`'s own finding answered by
doing.
**YOURS:** the file-level `trapped` flag calls a whole file released on one trapped removal, so a
second pen leaking beside a swept one is invisible. Should the reading be per pen rather than per
file? **MINE:** the remaining **312** are **308 Rishi** wanting `make-pen` and **6 shell** wanting a
trap -- so the shell half is nearly done and the Rishi half is the real room.
