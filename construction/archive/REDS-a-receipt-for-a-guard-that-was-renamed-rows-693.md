# REDS shelf -- row %693, a receipt for a guard that was renamed

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- one BOOKED row, landed straight onto its own shelf because the pin has no
room for it, its relative links re-anchored one directory deeper

Written `20260910.035019`, in the lap that found and repaired the red, and the arithmetic says why
it never rested on the pin. `construction/REDS.md` stands at **40,949 bytes** against the **40,960**
its own header declares -- **11** bytes of room, where this row wants roughly 1,100. The pin's own
`Bound:` line already names the condition and whose word moves it. A BOOKED row may fold by the
ledger's own law, so a BOOKED row may also be born here.

---

**REDS %693 (`20260910.034746`) -- a roster rename left a receipt naming a guard the tree no longer holds, and reddened `standing_equipment` on every ship until a hand edited an ignored file.** *What went wrong:* the guard `fleet_drain` molted to `fleet_clockout` on `20260909.013844`, and `construction/standing-equipment-runs.kyri` held a green run of the elder name from 28 minutes earlier. That receipt matched no roster row, so `runs_unrostered=1` and the guard answered red -- here, and on every peer that had run the elder name. The roster's own comment header kept the elder word too, so the block taught a name its row had retired. *What caught it:* a `--scoped --hot` pass reaching roster position **251**; a full pass had reached that depth in no lap since the molt, which is `%568`'s shape one guard over -- a reading nobody hears is the second half of a guard nobody runs. *What it taught:* **a rename is one edit and its receipts are one per machine.** The runs ledger is untracked on purpose, so a fresh clone reads *never run here* rather than inheriting another pier's memory -- and the same clause means the only closing move is a hand deleting a line no commit can carry to the seven peers. *Repaired (`20260910.034746`):* the stale row pruned here, the comment header repointed to `fleet_clockout` with the molt and this red recorded inside it, `runs_unrostered=0` on re-run. **BOOKED** -- the general move belongs to `tools/fixtures/s/standing_equipment_run.sh`, which writes the receipts and could drop one whose guard has left the roster. It waits, because `runs_unrostered` also catches a witness run outside the roster entirely, and pruning silently would blind that reading; telling the two apart is its own lap.

---

*The pin keeps what is open.*
