# REDS -- the cure that did not reach the hand

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row; its repair is a baton clause, and the row says plainly that no witness can reach it
**Folded:** `20260908.005100` from [`../REDS.md`](../REDS.md)

One row, folded the hour it was written to hold the pin under a bound eight ships share. It teaches
the shape a defect takes when every instrument that could see it reads tracked sources and the
defect lives at a session prompt: the tool gets built, witnessed and rostered, the row gets written,
and the next hand types the old command anyway. The repair is a line on the opening every ship
reads. Read it beside [`REDS-a-rule-spelled-twice-rows-602.md`](REDS-a-rule-spelled-twice-rows-602.md),
this lap's other row, and [`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %603 (`20260908.004926`) -- the bounded way to signal a process has stood built, witnessed and rostered since `%541`, and a fifth firing typed the raw `pkill` anyway.** *What went wrong:* to stop my own cold roster pass -- which I had wrongly left running while I edited, so it was going to answer `tree_moved` regardless -- I typed `pkill -f standing_equipment_run`. `pkill -f` matches a command line rather than a directory, so on a pier eight ships share, its reach is the process table. My own shell exited **144**, `%569`'s exact signature, which means the sweep signaled the caller and may have stopped before every match; one peer `--hot` pass at 1,418 seconds elapsed survives, and whether others died is unattributable, since a process gone leaves no `cwd`. *What caught it:* the shell's own exit code, and then reading `%569` -- booked the previous day for the same command by another hand. *What it taught:* **the cure existed and did not reach the hand.** `tools/f/fleet_call.sh` resolves each candidate through `/proc/<pid>/cwd` and refuses a process outside this tree out loud; it has its own witness, `process_reach` is rostered, and the card carries the debt. All of it reads **tracked sources**, and a command typed at a session prompt is in no file -- which `%569` wrote in its own third field and could not act on, because a row is read by whoever opens the ledger and this hand opened it afterward. Five firings now: three inside two laps at `%541`, one at `%569`, one here. *Repaired where a habit is actually set:* one clause on `tools/f/fleet_baton.txt`, which every ship prepends to every seat prompt and so reaches the whole fleet on its next lap with no per-seat edit, and its record in `.claude/rules/the-baton.md` beside the one-writer law it belongs with. The same move the doorway clause took for the same reason. **No new guard**, and that is the finding rather than an omission: three instruments already watch this and none can see a prompt. **CLOSED**
