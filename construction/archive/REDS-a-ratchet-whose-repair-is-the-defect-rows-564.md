# REDS -- row %564, folded from the living pin

**Folded:** `20260907.110022` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A meter that names a repair only the defect can perform has forecast its own idleness -- and the
price of the honest repair was the finding: the desk runner paid for the Glow compiler once per
desk, so a batch runs three hundred for less than the elder witness spends on two hundred.*

The row stands here exactly as it was written. A booked row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the guard the round built,
and the row itself stays one click away.

---

**REDS %564 (`20260907.110022`) -- the corpus had a meter for how far one hand had reached and no runner for the room, and the ratchet it left named a repair that reproduces the defect.** *What went wrong:* `tools/g/glow_run_desk_witness.rish` names 218 desks in 1,133 hand-written lines and stands on no roster, exactly because rostering it would publish a 218-desk green as coverage of a 352-desk corpus (`%532`). That row's repair was `glow_desk_reach_witness`, which READS the room and holds `uncovered_bare` as a ratchet at **83** -- an honest meter, and one whose only repair as written was 83 more hand-written blocks in the file whose shape was the fault. **A ratchet whose named repair reproduces the defect is a ratchet nobody is going to work.** *What caught it:* the roster row's own closing sentence -- *it is what makes rostering that witness honest later* -- read back on the next lap of this seat, and the question of what "later" would actually cost. *And the cost was the finding.* `tools/g/glow_run_worker.sh` rebuilds `glow/bin/glow_run` from `glow/glow_run.rye` on EVERY invocation, which is right for a hand running one desk and is most of the price 301 times over. Measured on this pier `20260907`: **3.80s** per desk through the worker, **2.42s** of it that rebuild, **1.38s** the desk's own lower, build and run -- so the elder witness's own shape spends roughly **527s re-linking one unchanged binary** across its 218 blocks, and a batch that builds the compiler once runs **301** desks for less than the elder spends on 218. *Repaired (`20260907.110022`):* `tools/fixtures/g/glow_desk_run_scan.sh` derives the bare-runnable set from the room on every pass -- every `.glow` under `glow/gen/` minus those declaring they must not run by EITHER marker, minus those the worker will only take with a sample -- then lowers, builds and runs all 301 behind the same directory lock the worker takes. Measured whole: **325s** under a load average of 14, `selected=301 ran=301 failed=3`. `--list` prints that selection and runs nothing, so a meter asking *which desks are spoken for* can read the instrument that does the running rather than restating its rule. `tools/fixtures/g/glow_desk_run_control.sh` proves **29 behaviors** on miniature desk rooms in a pen -- both declaration markers separately, the head band's own edge, `--list` proven to hand the runner nothing, and the ceiling planted then lifted; `tools/g/glow_desk_run_witness.rish` rostered `tier cadence`. **The union is exclusion in the safe direction:** the reach scan gates the two markers' DISAGREEMENT at zero and excludes by their intersection, and a runner excludes by their union, because a desk half-declared is one to decline rather than one to hand the compiler on a technicality. *A correction to `%532`, from metal:* the failure ceiling stands at **3**, the three unmarked data fixtures that row named -- and they refuse in **two** distinct ways, `sample-demo-fact-line-lits` and `sample-demo-fixture-lits` at `TooManyLines` and `sample-digraph-table` at `unsupported Glow head`, where `%532` recorded one refusal for all three. Giving them a marker stays a language custody ruling. *And the guard's own first version proved its refusal with a stub that could not run.* The two ceiling legs swap the per-desk runner -- one that always refuses must break the ceiling, one that always passes must read zero -- and they were written `/bin/false` and `/bin/true`. **This pier's `/bin` holds exactly one entry, `sh`.** So the refusing leg passed because the stub failed to EXECUTE, which is indistinguishable from a refusal, and the passing leg is the only thing that could tell: it read `failed=301` against a ceiling of 3 and refused. **A welcome asserted as hard as a refusal is what caught it**, which is the reason the tree asks for both. Repaired to the bare words `false` and `true`, which every shell resolves. *A second repair rode with it:* the live pass ran FIRST in that version, so a fault in a seconds-long leg was found after minutes of Zig builds. The cheap legs run first now, and the ordering is written into the file's own head as load-bearing rather than tidy. *And a standing guard caught the third fault in this same file.* `signal_trap` read `lock_releasing 1` against a ceiling of zero: both of this scan's traps were written `EXIT INT TERM`, and a handler that frees a lock or a pen WITHOUT exiting does not stop the script -- POSIX runs it and RESUMES where the signal landed, so the pass carries on against the temporary directory its own handler just removed. That is `%487`'s mechanism exactly, one file over, and the repair is the runner's own spelling: `trap ... EXIT` for the cleanup, `trap 'exit 130' INT` and `trap 'exit 143' TERM` beside it. **Three faults in one new instrument, and a standing guard found each one** -- a portability fault the welcome leg caught, an ordering fault the cost exposed, and this. *What this does not reach, and it is the next lap rather than a question:* `glow_desk_reach_scan.sh` still reads `covered` from the elder witness alone, so it says 83 uncovered bare desks while 301 are run every cadence pass -- **two instruments, two answers, one population.** The union is one `if` over `--list`, and it lands with its own pen behaviors rather than at the end of this lap. **BOOKED.**

**A note on the number.** This row booked `%561` from the anointed spine and renumbered to `%564` on the rebase that brought it current: `%561` was already bound upstream to `20260907.104723`, an earlier stamp, so the published number stands and the unshared row moves ([`derived-spine`](../../.claude/rules/derived-spine.md)). **It cost one file.** Every living citation of this round spelled the STAMP rather than the number, which is rule 4 doing exactly the work it was written for.
