# REDS -- row %562, folded from the living pin

**Folded:** `20260907.105303` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A census asked one question of every dated name it found -- does a file of that name exist? -- and
so it could not tell a promise from a page saying the file is gone. Half of what it called breakage
was the tree telling the truth about a gap.*

The row stands here exactly as it was written. A closed row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the instrument the round
repaired, and the row itself stays one click away.

---


**REDS %562 (`20260907.105303`) -- the dated-path census counted a shelf's honest declaration of a missing log as a broken reference, 88 times, and stood the gate at 173 against a ceiling only a debride may raise.** *What went wrong:* `tools/fixtures/d/dated_path_scan.sh` classified every dated basename it found by asking one question -- does a file of that name exist? -- so a row reading ``- `20260722.133103` --- ITINERARY git nib after STOA229 `20260722-133103_remember-git-nib-stoa229.bron` *(log never landed)*`` landed in `broken_gone` beside a genuinely stale link. That row is the **repair**: the session-log shelves' own header states it word for word -- *the row keeps its stamp and its meaning, and carries no link, since a link promises a file a reader can open* -- and the census charged the shelves for performing it. Measured `20260907.104201`: **88 of the 173** lost references were declared absences, 87 wearing `*(log never landed)*` across three shelves and one wearing `-- never landed in any commit`, and **zero** of the 88 also linked what they called absent. *What caught it:* the gate itself, RED at 173 against 165, plus refusing to answer it from the box. The elder reading in `construction/ITINERARY.md` had the split at **139 mentions / 15 bare / 15 links** with a repairable count of zero, and reading the citing LINE of all 173 rather than sampling gave `declared_absent=88 / promise=13 / mention=72`. Opening one of the 88 in its shelf is what named the class: the row was not a promise at all, and no reading that counts basenames could have seen the difference. *What it taught:* **a reference and a citing line are two things, and a census that reads only the first cannot tell a promise from a page saying the file is gone.** The tree had already met this shape twice and paid it by hand both times -- `REDS %246` taught that a dated name on a full-line comment is a page a round *wrote about*, and the ceiling comment above records `%245` costing one raise because *a row naming a lost reference is itself one -- the cost of saying so, paid once and visible*. Paid once, that is honest; at 88 it inverts the gate, since over half of what the guard called breakage was the tree telling the truth about a gap. *Repaired (`20260907.105303`):* a fourth verdict, `declared`, printed as `broken_declared` beside the gate rather than folded silently into it. **Two bounds keep the marker from becoming an escape hatch, and both are planted in `tools/fixtures/d/dated_path_control.sh` case four rather than trusted:** a line that also **links** the basename stays counted however it is worded, since no adjacent prose withdraws a promise a link is still making; and the declaration must stand on the **same line**, so a header sentence silences nothing beneath it. Four ghosts in one pen -- plain, declared, declared-and-linked, declared-in-a-header -- read `refs_total=5, broken_gone=3, broken_declared=1`, the answer known before the walker ran. **The narrowing proves itself honest by what did not move:** `refs_total` 23,961 and `refs_home` 13,998 are unchanged, so nothing left the set the census walks and 88 references merely changed bucket. `refs_lost` **173 -> 85**, and the ceiling **falls 165 -> 85** in the same commit, which is the direction a ceiling is always free to go. *Not taken, and why:* the elder box asked whether a **gate may move to the class it was built for**. It need not, and that is the finding -- the gate was already on the right class, and the set of references reaching it was wrong. The question of gating a `lost_promised_living` cell stays Keaton's, now costing 13 promises rather than 173 references. *Found twice, 25 minutes apart, and that is the second finding.* A prior lap of this same seat (`20260907.103237`) reached the same root, anchored its pass on the italic `*(log never landed)*` marker, and its whole change -- scan, control, witness, REDS row and session log -- sat in `stash@{0}`, parked there by THIS round's own open at `20260907-104153` and never read. `stash_record` is `tier lap` and stood **RED at `records_unlanded=1`** the whole time, naming the drawer exactly as its header promises: *one recording a lap whose CODE had landed, so the work shipped and its reasoning did not, and the next lap paid to rediscover it.* The guard worked; the lap that needed it **skipped the cold open**, so the warning arrived in the hot pass, after the duplicate was built. **A guard that runs at the open and a lap that opens without it are two different systems**, and only the second one was ever mine to fix. *What the elder lap holds that this one does not,* left on the card rather than merged: five printed readings splitting the remainder into promise and mention against living and testimony, including the `lost_promised_living` cell the open gate question actually turns on. **CLOSED.**
