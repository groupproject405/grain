# REDS -- row %580, folded from the living pin

**Folded:** `20260907.155612` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*The scan's own prescribed repair was the repair.*

The row stands here exactly as it was written. A closed row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the three scans it corrected,
each now spelling the separator as the character class its own header had already named, and the
row itself stays one click away.

---


**REDS %580 (`20260907.151232`) -- a guard reading one line at a time called two correct scans wrong, and the fix it prescribes was the fix.** *What went wrong:* `tools/fixtures/d/dated_spelling_scan.sh` greps for a one-clock stamp shape followed by a bare `_`, line by line. A shell `case` may spell the option as two arms -- `...-......_*)` on one line, `...-........*)` on the next -- and answer both spellings of a dated name correctly. `ascii_document_scan.sh:205` and `reds_citation_scan.sh:133` each stood that way, each already right, and each was charged with requiring the sprig. A red guard withholds the roster receipt, so `--scoped` refuses and **eight ships pay a full cold pass** for a defect that was not there. One of the two was narrow after all, in the other direction: `reds_citation`'s sprigless arm named `.md` alone, so a sprigless `.kyri` log read as living where the law calls it testimony. *What caught it:* the water rota row, taking `dated_spelling` off `%568`'s list of five red cadence guards and reading the two sites rather than the count. *What it taught:* **the scan's own prescribed repair was the repair.** Its header already names `[_.]` -- the separator written as a class -- and one arm spelled that way matches exactly what the two arms matched, cannot drift from a sibling it no longer has, and is visible to a meter reading a line at a time. So neither the two scans nor the meter needed widening; the tree needed the spelling it had already written down. `narrow_dated_patterns` **2 -> 0**; `reds_citation` unchanged field for field, the widening costing nothing today and holding for the first sprigless non-`.md` citer. Three witnesses GREEN. The blindness is now named in the meter's own *WHAT THIS DOES NOT REACH*, pointing both ways. **CLOSED**
