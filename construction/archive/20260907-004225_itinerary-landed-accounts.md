# ITINERARY -- landed accounts, folded `20260907.004225`

**Language:** EN
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded from:** [`../ITINERARY.md`](../ITINERARY.md), the live front

Three BAKERY accounts, each closed and each already pointing at its own shelf. They fold together
because the pin they stood on sat 48 bytes under its ceiling on `20260907`, and a live front is for
what is open.

## `%516`, `%517`, `%510` -- closed and folded

Shelves: [`222720`](20260906-222720_itinerary-landed-accounts.md) and
[`210016`](20260906-210016_itinerary-landed-accounts.md); rows to
[the name that asked one question](REDS-the-name-asked-one-question-rows-516-517.md) and
[a guard that reads a proxy](REDS-a-guard-that-reads-a-proxy-rows-510.md).

**Residue, carried here rather than lost:** a third-reason enclosure skip stands; two refusal
wordings differ on this bench; a deadlocked pin reads `ok`.

## `%499` / `%503` and `%501` -- closed with DIFFUSER

Shelf: [`173444`](20260906-173444_itinerary-landed-accounts.md). `%499`/`%503` was a discriminator
that turned out to be a tautology; `%501` took two answers -- the Moore bound asked in the direction
that decides, and then the abelian floor beneath it.

## `%515` -- the wall and the path it never saw

Shelf: [a wall on a path the work does not take](REDS-a-wall-on-a-path-the-work-does-not-take-rows-515.md).

`tools/hooks/post-commit` writes `living-pins-owed` beside the page debt, and `pre-commit` rule
three answers it with an `index` mode that weighs every rostered pin off the index rather than only
the staged ones. `cases_run` went **36 to 54**, the pen planting nothing: two lawful commits, and
`git rebase` auto-merging them **119 bytes over a bound of 100**. It fired on its own send, where
the rebase merged the operator card to **41,206** of 40,960 with neither side over alone.

**Residue:** a rebase pushed with nothing between it and the push is still the gap that ships an
over-bound pin once.

## Why these three fold together

Each is a wall built where a wall was missing, and each was found by a cold pass reading its own
tree rather than by anyone predicting it. That is the pattern worth carrying forward: the roster is
the instrument that finds these, and the cost of one running is a few hundred seconds against a
fleet-wide fault standing for days.
