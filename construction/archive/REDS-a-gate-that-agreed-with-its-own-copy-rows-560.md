# REDS -- row %560, folded from the living pin

**Folded:** `20260907.094747` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A gate that decides a number about a module, compared only against a copy of that number spelled
beside it, is green in exactly the state it exists to refuse.*

The row stands here exactly as it was written. A closed row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the reader the round built --
`tools/fixtures/m/mantra_gate_constant_scan.sh`, which takes the decided constant off the desk and
the field count out of the Rye, so no instrument in the family spells either number -- and the row
itself stays one click away.

---


**REDS %560 (`20260907.094747`) -- two Glow gates decided the field counts of `Line` and `Diff`, both structs grew a field, and both gates went on answering 0 for the number their own subject carries -- green under a witness, a control, and the cadence roster.** *What went wrong:* `src/gate/gate-mantra-line-fields-eq-u32.glow` read `?:  (eq sample 3)  1  0` while `mantra/src/weave.rye` declares `Line` with four fields (`text gen pos site`), and `src/gate/gate-mantra-diff-fields-eq-u32.glow` read `(eq sample 2)` while `Diff` declares three (`site inserts deletes`). `Line` grew `site` when identity became a pair, and `Diff` grew it on `20260906.212206`; the sur shape `src/shape/shape-mantra-diff-field-count.glow` moved two to three in that lap and the gate one directory over did not, so two Glow desks named one fact and disagreed. A gate whose whole subject is deciding *exactly* was answering `0` on the truth and `1` on a count no struct carries. *What caught it:* reading `tools/m/mantra_a1_equality_witness.rish`'s own header against `tools/fixtures/r/rye_struct_fields_scan.sh --count` -- the header declares *a Line has 3 fields, a Weave has 2, a Diff has 2*, the reader answers `4 2 3`. Nothing in the tree had asked: the guard asserts `line fields=3 decide exactly`, the Rye witness passes `"3"` to the desk, the desk decides 3, and `mantra_a1_equality_control.sh` proves the family refuses when that one number is *moved* -- three copies of a number agreeing with each other. `%482` rostered these four proofs a day earlier and made them heard; being heard is not the same as being right. *What it taught:* **a proof nobody runs and a proof that runs against its own copy of the number fail identically from the outside, and the second is the harder one to see, because it is green.** The sur limbs one directory over had already learned this -- `mantra_glow_tend_limb1` and `limb3` read the placard's number and then count the struct, with the comment *a guard that carries its own copy of the number can only ever agree with itself* (`%500`, `20260906`) -- and the lesson stopped at `src/shape/`, one directory short of `src/gate/`, where the same four numbers stand. **A lesson that repairs one instrument and not its twin is a lantern.** *Repaired (`20260907.094747`):* both desks decide the count their struct carries, both Rye witnesses ask the three questions around it, and the numbers are now READ from both sides -- `tools/fixtures/m/mantra_gate_constant_scan.sh` takes the decided constant off the `eq` rune and the field count out of the module by the same reader the sur limbs use, answering `ok`, `constant_disagrees`, `desk_absent`, `desk_decides_nothing`, or `struct_unreadable`. The guard asserts `verdict=ok` for all four desks and no number is spelled in it; the control plants a disagreement in a pen and watches that same scan refuse, so the comparison is written once and both read it -- two phases, both sides, `constant_agrees_verdict=ok` and `constant_disagrees_verdict=constant_disagrees`. GREEN on metal. *Not taken:* the Aurora three and the Caravan one named beside these in `src/gate/README.md` are unrostered and unbound alike, and each pins a number in another lane's room. **CLOSED.**
