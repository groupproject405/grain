# REDS shelf -- the notation reads whole, row %402

**Folded:** `20260831.113534` -- one CLOSED row off the living pin when the morning's booking
carried it past its declared byte bound.

What it taught: the report card classified Kyri notation as prose, so every comment-borne
document was discarded and every record block fused into one pseudo-sentence -- nineteen files
answering exactly C+ 75, the signature of a reading that measures nothing. The repair seated a
third artifact kind, notation, that prepares its own body; the fault class is one classification
answering two questions that wanted different answers.

Rows fold here in ascending order, links re-anchored one directory deeper by the fold tool.

---

**REDS %402 (`20260831.095637`) -- the card that grades a notation file threw away its whole document and scored its records, and all nineteen files carrying real comment prose read exactly one sentence and exactly C+ 75.** *What went wrong:* `tools/fixtures/q/qa_report_card.sh` classifies an artifact once and hands `measure()` -- lifted from `tools/fixtures/p/prose_register_scan.sh`, cited rather than copied -- whatever text that classification chooses. A `.md` goes in raw, which is right, since `#` opens a heading there and both readings drop it. A program goes through an extractor that strips `//!`, `#`, and `::` first, which is `%276`'s repair and `%358`'s. `.bron` and `.kyri` classify as **prose**, so they go in raw -- and in Kyri notation `#` opens a **comment**, which is where the file's entire document lives. Register and Reach therefore dropped every line of it. What remained was the record block, `guard <name>` / `path <p>` / `tier lap`, which carries no terminal punctuation, so consecutive records fused into ONE pseudo-sentence rather than meeting the reading's own under-four-words floor one at a time -- the floor that handles a short line correctly the moment something separates them. On `construction/standing-equipment.kyri`: **498 comment lines and 7,271 words discarded, 457 records read as a single 915-word sentence**, a Flesch-Kincaid grade of **327** against a Door ceiling of 9, Reach floored at 0, composite **C+ 75**. All **nineteen** tracked `.bron`/`.kyri` files carrying 200 words or more of comment prose read **exactly one sentence** and **exactly 75** -- `template-manifest.bron` at 1,786 words of that one sentence, seven Caravan system descriptors among them. Forty-two tracked notation files carry comment prose at all. *What caught it:* the living card naming `standing-equipment.kyri` at C+ 75 as this seat's next door, and reading the number instead of acting on it. At all three settings it answered 75, 75, 100 -- a composite that moves only when Meter switches the reading OFF is a composite measuring nothing -- and `reach=0 (grade 327 against 9)` beside `1 sentences` said which half had been read. Nineteen files answering one number is `%358`'s own signature, and 75 is that fault's arithmetic: Reach floored at zero with the other three perfect is 300/4. *What it taught:* **a repair aimed at a comment mark lands in the branch that owns an extractor, and the fault walks in through the branch that has none.** `%276` taught the program extractor `#` and `//`; `%358` taught it Glow's `::`; both correctly left the prose branch alone, because a Markdown heading really is a heading. `%392` then added `.bron` and `.kyri` to that same classifier so `truth_source` would read them as prose, correctly again -- and the one line that settles `truth_source` settles `prose_path` beside it. Three correct edits, and the third family arrived on the wrong side of a door the first two were built to hold. **One classification answered two questions that wanted different answers**, which is `%398`/`%399`'s shape one instrument over. *Repaired (`20260831.095637`):* a third `artifact_kind`. `.bron` and `.kyri` classify as **notation** and prepare their own body -- each comment line gives up its `#[ ]?` sigil, each record line closes with a period of its own -- so the document is read and a short record meets the four-word floor alone, exactly as a table row already does, while a long field value like a session log's `obs` line stays one unit and is read. `truth_source` is untouched, so `%398`'s prose skip and `tools/fixtures/c/comment_citation_scan.sh` read what they read; Meter frees register and reach for every non-program artifact rather than for `prose` by name, so a log read at Meter is unmoved. After: `standing-equipment.kyri` reads **278 sentences, grade 11**, composite **A 91** at Field, and its true weakness is legible for the first time -- **38% negative over 278 sentences**, refusal-led prose, for which Gauge's own answer is Meter. The nineteen spread **87 to 99**. `README.md`, `tools/fixtures/r/reds_fold.sh` and `glow/rune_shape.rye` read byte-identical before and after. Eight cases join the pen, **six biting** against a card carrying the elder classifier, including the pair of plants it could not tell apart and the leg reading the elder card's own answer of one sentence. `comment_citation` and `prose_register` GREEN beside it. The witness recited two counts of itself, 72 in its header and 79 in its close; both read the control's counted **83** now. **CLOSED.**
