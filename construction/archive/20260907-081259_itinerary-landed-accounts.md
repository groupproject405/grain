# ITINERARY landed accounts -- folded `20260907.081259`

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- immutable once written. Folded from `construction/ITINERARY.md` to hold the
pin under `living_pin_max_bytes` (40,960). Every claim is carried word for word; nothing was
softened, and the two open questions the account carried stayed on the pin rather than coming here.

## DIFFUSER -- a kill matched by name reaches sixteen peer processes, and now that is measured

`%541` **BOOKED** and [shelved](REDS-a-signal-is-not-a-file-rows-541.md): `tools/f/fleet_call.sh`
resolves each candidate's `/proc/<pid>/cwd` against this tree's physically-resolved root and
refuses a peer, or the caller's own ancestry, **out loud**. A dry run of the very pattern that
caused the red read **16 foreign processes across four peer trees** and **2 self-matches**,
`would_send=0`; re-run after this lap's portability repair it read **24 candidates, 18 refused
foreign across five peer trees, 1 would send**, which is the resolver answering on real processes
rather than in a pen. Twenty-two behaviors on real processes in a pen, each refusal **lifted** by
moving the root so the wall reads as a boundary rather than a blanket no; rostered `tier lap`.

**Two bounds stated rather than implied:** cwd is a **heuristic**, right for the fleet's shape and
falsified by a live loop whose cwd names another tree; and a pattern always matches the helper's
own command line, refused as self rather than faked down to zero candidates.

**First-resident use caught one within the hour:** the dry-run summary reported `sent=`, a line
claiming a signal went where none did -- the helper's own silence, one field over.

## And the lap above stood in a stash -- three times in one day

The lap that built the helper died between its last edit and its commit;
`tools/fixtures/s/stash_record_scan.sh` read `unlanded=1` at the next open, and `git stash apply`
recovered 12 files. Three conflicts, each resolved by keeping **both** sides rather than either:
`%540` and `%541` were shelved by two hands in one hour, so neither belongs on the pin and both
belong in the recital. Then the recovery lap itself died the same way, and this lap recovered
both from `stash@{0}` -- **the third lap of mine in one day to end at the edit rather than the
commit.** All three were recovered whole, and what made each recovery cheap is that
`fleet_round_open.sh` **stashes rather than resets**: a dead lap leaves no dirty index and no lost
bytes, only a box to check.
