# REDS -- the guard that named its own gap

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- one ledger row, closed by witnesses on metal
**Folded:** `20260909.204315` from [`../REDS.md`](../REDS.md)

One row, and the thing it found was written down already. The compass rose is this tree's return
habit, and the one guard watching it declares in its own header, under WHAT IS NOT PROVEN, that it
cannot tell whether the rose names the right stations. It could not, and the rose had been missing
one since read scope seated: the map that says which rooms a lap may walk at all.

The row folds on arrival because it closed on arrival. What it leaves behind is a question for a
lane rather than a repair: every scan in this tree carries a WHAT IS NOT PROVEN block, each written
by the hand that knew the gap best, and nothing walks that queue.

**REDS %674 (`20260909.204315`) -- the return habit walked six stations past the law that bounds the walk, and the guard built for that page names the omission it cannot see.** *What went wrong:* `foundations/20260826-024943_follow-our-compass.md` is the compass rose, this tree's return habit and the aether row's cardinal seat: when direction frays, a lap walks its numbered stations to recover. Read scope seated `20260827.155213`, naming `MAP.md` as *the walk that replaces the `ls`* and dividing every room into an open shelf or a closed stack. The rose carried six stations after that day and named neither. So did `docs/COMPASS.md`, the cold-start rose, at ten steps; so did `.claude/rules/align.md` and its Cursor twin, which restate the walk for the reconcile pass; so did `ORGANIZING.md`, which the map's own Status line sends keepers to. Measured `20260909.204315` with `git grep -E '(^|[^A-Z/])MAP\.md'` -- the character class matters, since a bare `MAP\.md` matches `ROADMAP.md` and inflates every count -- **of the tree's orientation pages only `README.md` and `llms.txt` named the map at all**. The reader most likely to over-read is the one who has lost direction and is opening doors to find it, and that reader met no floor plan on the way back. *What caught it:* the aether rota row read as its own sense. The row asks a lap to listen for *the silence where a claim used to be and a witness now stands*, and `tools/fixtures/c/compass_station_scan.sh` says the quiet part in its own header: under **WHAT IS NOT PROVEN** it writes *that the rose names the right six stations*. The guard gates `stations_missing` and `stations_pointer` at zero and declares the completeness question outside its reach, so the omission stood green for thirteen days in the one instrument watching that page. *What it taught:* **a guard that names its unproven half is telling a reader where to look, and nobody was reading it.** The header is not an apology; it is a survey of the gap, written by the hand that knew the gap best, and it costs one read to act on. The same sentence generalizes past this page: every scan in this tree carries a WHAT IS NOT PROVEN block, and that block is an unworked queue nothing walks. *Repaired (`20260909.204315`):* the rose opens at **Where you are** -- `MAP.md`, the seven rooms and the two floors -- and the six elder stations keep their order and their words at 2 through 7. `docs/COMPASS.md` takes the map as **step 0**, which leaves every existing step number true and its own two `rose step 7` citations intact. `align.md` and `align.mdc` name the map in their restatement, and the twin's stale instruction to check `TASKS.md` / `ROADMAP.md` -- pointer stubs since `20260823.103804` -- now names the card that absorbed them. `ORGANIZING.md` links the map back and drops the same stale claim. **And two sentences cited stations by ordinal**, one in the rose and one in `foundations/README.md`, so prepending a station would have falsified both: each now names its station rather than counting to it, which is `.claude/rules/stamp-and-name.md` applied to a read order. `compass_rose.rish` GREEN at 7 stations, `compass_station_control.sh` 18 pass 0 fail, `foundations_link` GREEN, five QA cards at A. *Booked `%673` from a spine that had yet to publish the peer's own; a published number never moves, so this unshared row derives above it (`derived-spine` 3). The key is the stamp.* **CLOSED.**
