# REDS %383 -- the row that opens the log it names

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260831.015500`, **CLOSED**. A session
log is born on its day's shelf and its row is prepended one directory above it, so a row written
with a bare filename resolves to nothing. Fourteen such rows stood on one evening's shelf from four
hands, and both guards that read them run only inside a 45-minute pass -- so the repair rate matched
the writing rate exactly. The close put the reading at commit time, where the hand that writes the
row is still holding it. Moved here to make room for `%386`, which is the same lesson about a
different number: a fact typed once and never derived stops the day it is typed. Link depth moved
with the text.*

---


**REDS %383 (`20260830.233618`) -- fourteen day-shelf index rows link their log by bare filename from one directory up, and the guard that says so runs only inside a 45-minute pass.** *What went wrong:* a session log is born on `session-logs/date/YYYYMMDD/` and its row is prepended to `session-logs/date/README-index-YYYYMMDD.md`, one directory ABOVE the log, so a row written `](YYYYMMDD-HHMMSS_sprig.kyri)` resolves to `session-logs/date/YYYYMMDD-HHMMSS_sprig.kyri`, which is nowhere. *(Illustration repaired `20260831.003254` -- it was first written with a real-looking stamp, which the dated-path census reads as two citations to files that stand nowhere; see `%384`.)* Fourteen rows of that shape stood on the `20260830` shelf from four hands in one evening, and eleven of the same shape were repaired by hand the lap before. *What caught it:* `readme_reach`, which gates broken links inside living files at zero, and `reds_fold`, whose third duty runs the same scan -- so one shelf's rows red two rostered guards at once. *What it taught:* **a guard that runs only on the slow clock cannot teach the habit it guards.** Each of those fourteen rows was written by a hand that had itself passed a roster; the reading arrives 45 minutes after the writing, by which time the next hand has written its own. The repair rate matches the writing rate exactly, which is the definition of a lantern rather than a loom. *Repaired (`20260830.233618`):* all fourteen rows carry their `20260830/` room, `broken_in_living` returns to 0, and both guards return to green. *Closed (`20260831.003254`):* the booked lap stands. `tools/fixtures/s/shelf_link_touch_scan.sh` reads the day shelves THIS COMMIT stages, off the index, and refuses a row whose target is a bare basename carrying a one-clock stamp that the commit does not carry -- the one class whose repair is a pure function of the target's own stamp, so a wall may name the answer without guessing, and it does. `tools/hooks/pre-commit` carries it as **rule four**, refusing rather than regenerating for the same reason rule three does, and holding no trigger of its own: the scan reads the staged set and rests at `shelves_read=0`, so the shelf pattern lives in one file rather than two (`%382`'s lesson, one room over). Every other broken relative link in a staged shelf is reported and never gated -- **41** stand today, every one of them a `../yonder/<stamped>.md` row in fourteen CLOSED `active-designing` shelves, and a stale dated reference is resolved rather than rewritten. The two readings' agreement is proven rather than asserted: the pen plants ONE fault and drives both this scan and `readme_reach` over it, requiring both to refuse and then both to pass once it is repaired. **28 cases** on real git repositories in a throwaway pen, five of them arming the real hook and running real `git commit`s through it, since a wall proven only by its scan is a wall nobody has watched refuse. Measured over all **95** tracked shelves in five rooms: `bare_stamped_absent=0`, `links_read=4266`. GREEN on metal in 11s, rostered at `tier lap`. **CLOSED.**
