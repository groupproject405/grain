# Landed accounts -- folded from the live front `20260906.180832`

*Held word for word from [`../ITINERARY.md`](../ITINERARY.md), so the card carries what is OPEN and
what waits on Keaton's word. Two Patchouli accounts: `%507`, the guard that read a park as a
landing, and `%506`, the parked `Weave.annotate` work that came home. Both landed, both already
pointing at the REDS shelf that holds their detail. Relative links were rewritten one level deeper,
since a link reading `archive/x.md` from the card reads `x.md` from inside `archive/`.*

*Folded to make room. The card stood at 40,912 of its 40,960 bound with 48 bytes of slack, and this
round's account needed a block of its own. The fold was the first move rather than a trim, which is
what the `20260906.113014` cairn asks for -- every word below is the card's own, unedited.*

**PATCHOULI -- A PARK IS NOT A LANDING, AND THE GUARD BUILT FOR THE FIRST DEAD-LETTER BOX ACCEPTED
THE SECOND AS PROOF THE FIRST WAS EMPTY.** `%507` CLOSED,
[folded](REDS-a-park-is-not-a-landing-rows-507.md). `stash_record_scan.sh` read `refs/heads`
and `refs/remotes` **whole** -- where the round open parks -- so a record on
`pier/diverged-20260906-131810` alone read `landed:refs/heads/pier/...`, **`unlanded=0 verdict=ok`**,
while `stash@{0}` held a **complete unlanded feature**: `annotate`, its witness, a scan, a control,
a runner, a roster line, **1,107 insertions already GREEN**, on no reader ref at all. **Repaired
here:** `unlanded=1 parked=1 verdict=records_unlanded`; **23** park refs stand. `reader_refs` excludes
the `pier/` namespace; `parked` reports **beside** the gate, so `fleet_round_open.sh` is untouched. Control **22 -> 34**, **8 new legs bitten** against the elder scan.

**AND THE PARKED WORK LANDED: `%506` CLOSED**,
[folded](REDS-the-property-nobody-named-rows-506.md). `Weave.annotate` -- one `Note` per
position, both counts, five `Reading` words -- and `mantra_weave_head_scan.sh`, holding the head to
its own `pub fn` both ways. **Ten claims GREEN**, the fourth holding annotate to merge count for count. Roster `runs_unrostered` **1 -> 0**; a four-line comptime walker took `rye_witness_walker`
back under its ceiling rather than raising it. **Yours:** the identity gap stays unblocked, and
closing it wants a fourth field on `Line`.
**Yours, no longer rhetorical:** mine went `%485` -> `%501` -> `%503` -> `%504` -> `%506` in one day,
beaten FOUR times while the work sat parked -- should an OPEN row carry a claim, a seat and a stamp,
at START?

*Folded further `20260906.203000`: a peer condensed both accounts above into one card line, and that line folds here too as the card met its bound.*

**PATCHOULI -- a park is not a landing.** `%505`'s sibling, `%507` CLOSED and whole on its [shelf](REDS-a-park-is-not-a-landing-rows-507.md): `reader_refs` excludes the `pier/` namespace, `parked` reports beside the gate, control **22 -> 34**. `%510` above is the drawer beneath it.
