# REDS -- a row keys on the log it names

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by a control on real directory trees
**Folded:** `20260910.020722` from [`../REDS.md`](../REDS.md)

One row, folded to hold the pin under a bound eight ships share and one of them needed the same
hour. It teaches that an index row's identity is the RECORD it points at, never the second it was
written in: the naming law lets two logs share a stamp and resolves them by sprig, so a row keyed on
its stamp cell carries no tiebreak and refuses a lawful day. Measured over the 73 revisions of the
`20260830` shelf, where 89 duplicate stamps carried one link and 64 carried two -- every one of the
64 a single log written at two depths, which a branch above already refuses. The elder key could not
see the other half of its own fault: one log wearing two rows under two different stamps read clean.

Read it beside [`REDS-the-loom-that-chased-a-generation-too-far-rows-681.md`](REDS-the-loom-that-chased-a-generation-too-far-rows-681.md),
which folded the same night from the same pin at the same ceiling.

---


**REDS %676 (`20260909.174000`) -- the naming law lets two logs share a second; the day shelf cannot.** *What went wrong:* two ships wrote a log at `20260909.072922`. The naming law resolves that with distinct sprigs, yet an index row is keyed by its **stamp cell alone**, so the shelf held two rows wearing one stamp and `index_row_bound` refused. *What caught it:* the hot pass. *What it taught:* **a filename may carry a tiebreak that the row naming it cannot.** Moving the record one second instead reddened `stash_record`, whose `carried_by()` reads path presence -- so **three guards bind one record**: a row per log, one row per stamp, the stash's path present. *Repaired:* both logs stand at their true stamp under **one shelf row carrying both links**. *Closed `20260910.004817`:* **the key is the LOG a row points at, never its stamp.** Measured over the 73 revisions of the `20260830` shelf where this reading last fired hard -- **89 duplicate stamps carried one link, 64 carried two**, and all 64 were one log written at two depths, which `rows_unresolved` refuses one branch above -- so the link is an exact key rather than an approximate one. Tree-wide: **78 seconds carry two or more logs**, every shelf reads zero duplicates under both keys, so the two agree today and part on the next collision, roughly every eleven days at 130 laps and rising with the square of the fleet's daily count. Scan and repair now key alike, since a hand sent to a repair that disagrees with the guard is sent nowhere; the shared second is **reported** as `rows_stamp_shared` rather than gated; and the shape the elder key could not see at all -- one log wearing two rows under two stamps -- bites. Six new legs, both directions, control 48 and repair 39, `index_row_bound` GREEN. **CLOSED.**
