# REDS -- a row that says a class is empty closes it harder than a guard does

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri
**Folded:** `20260907.232118`
**Row:** `%596` (`20260907.232029`) -- BOOKED

The row folds on the lap it was booked, for the reason its predecessor recorded one shelf over. The
living pin stood at **40,946 bytes against a bound of 40,960** when this lap opened, and twelve of
its fourteen rows read OPEN, so `reds_fold.sh` may move almost none of them. A pin whose living
parts fill it exactly cannot accept a new row, so a finished row goes where finished rows belong
and the pin keeps a pointer.


**REDS %596 (`20260907.232029`) -- my own row called four lost references unrepairable, and three of them were a fabricated stamp any lap could correct.** *What went wrong:* `%594`, booked by this seat three hours earlier, wrote that `dated_path`'s `lost_living` references "sit in vendored `gratitude/reaper/`, an immutable `waymarks/date/README-index-` shelf, and a REDS fold archive" -- a sentence saying the repairable class was already empty, so a reader would not open one. Opened one at a time this lap, three of them were living text carrying a stamp that has never existed on any branch. `gratitude/reaper/docs/user-guide-index.md` closes with **our own** annotation naming ``active-designing/20260630-020012_lotus-the-daw.md``; the file is `active-designing/yonder/date/20260630/20260630-014012_lotus-the-daw.md`, a different second, and `git log --all --name-only` finds the cited name in no commit ever made. `tools/fixtures/d/dated_path_scan.sh` taught its own boundary rule with `99991_20260619-090912.md`, and `tools/fixtures/j/journal_query_census.sh` taught its classifier with `session-logs/20260710-000045_x.md` -- two illustrations built from real-looking stamps, which the mark law names outright: *build one from a real-looking stamp and a sprig naming no file, and it reads as a real citation to every reader and every tool.* The census was counting its own teaching as breakage, the shape `%591` closed one guard over. *What caught it:* the earth rota lap re-reading `dated_path` red at the cold open and listing its lost set by file rather than counting it -- the diagnosis `%562` printed the five readings for, used as intended for the first time. *What it taught:* **a row that says a class is empty closes it harder than a guard does.** A guard reds every lap and invites a reading; a sentence in the ledger saying "nobody can climb this" is read once and believed, and the three sites it covered stood for as long as the sentence did. `%594`'s wider finding holds -- 91 of the 93 are testimony no lap may repair -- and the clause about the repairable class was the part written from a glance rather than from opening the files. *Repaired (`20260907.232029`):* the gratitude annotation points at the real path; both illustrations take letter placeholders, `99991_YYYYMMDD-HHMMSS.md` and `session-logs/YYYYMMDD-HHMMSS_sprig.kyri`, which carry the same teaching and cite nothing. `refs_lost` **95 -> 93**, `lost_living` **4 -> 2**. *What the two remaining are, and why they are honest:* a `waymarks/date/README-index-` row and a `construction/archive/REDS-...` fold shelf, both immutable by their own laws -- so the split's `living` predicate, which reads a basename for a stamp, calls two shelves living because a shelf's name carries none. `lost_promised_living` reads **0** and is the number that stays true. *Not taken:* the gate still stands at 93 against 85 and no lap can close it, since testimony alone is 91 and grew **17 in three days, 11 on `20260907`** -- eight of those eleven the shelf-stamp shape `%594` walled at `22:18`, which makes tomorrow's reading a falsifier a later lap can run. Moving the gate to `lost_promised_living` is the scan's own named proposal and stays Keaton's word. **BOOKED**
