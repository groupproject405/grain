# REDS %584 -- a floor that refuses every rise

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260907.192800`, so the pin holds its
declared bound as `%585` lands on it. The row is CLOSED and its repair is on the tree: a plant
adoption count asserted as an equality refused the exact work it existed to encourage, and a hand
had raised the literal seven times in one day rather than reading the comparison beneath it. Kept
as the elder of the family `%578` opened, where the measuring half of one instrument was repaired
and the gate that reads it was left backwards. This lap recovered the row and its repair whole out
of `stash@{0}`, where a round-open had parked them at `20260907.183139`. Link depth moved with the
text.*


**REDS %584 (`20260907.180000`) -- a floor written as an equality refuses every rise, so the fleet stopped for the one thing the guard exists to encourage.** *What went wrong:* `tools/p/plant_witness.rish` asserted the literal `sourcing=13` -- an equality -- beneath its own comment calling it *"a floor that only rises"* and a header calling adoption *"reported rather than gated."* Neither sentence described the code: an equality refuses a rise exactly as hard as a fall, so a lane ADOPTING the plant law red `plant` and, through it, `standing_equipment`, for every ship until a hand edited one number here -- **seven times in one day**, 4 up to 13, each rise a lane doing the right thing and paying a fleet-wide refusal. *What caught it:* this lap's cold roster, `guards_red=2` on one root: `plant red 1s` with `control_verdict=ok` above it, so the helper was perfect and only the census had moved -- **`%578`'s own reading, eleven hours earlier, on this line, for the rise before this one.** *What it taught:* **a ratchet's direction lives in its comparison, never in its comment.** `%578` repaired what the scan MEASURED and left the equality that reads it: the census became exact and the gate stayed backwards, two halves of one instrument repaired a day apart, because only the measuring half was where anybody looked. *Repaired:* `FLOOR=13` is a constant in `tools/fixtures/p/plant_adoption_scan.sh`, compared as `sourcing >= FLOOR`, printed as `adoption_floor_held=`, refused by name as `verdict=adoption_regressed` -- so a control that LOST its import reds and one that gains it walks free. The witness reads that word rather than a number, which is what made the number two files' business. *Proven both ways on metal:* `plant_control.sh` grows **30 behaviors to 39** on a real git repository of two adopters and one abstainer, the floor moved either side of that count **by `plant_apply` itself**, so a stale plant reads `nothing`; the load-bearing leg strips the refusal out of a copy and watches the same below-floor pen walk free at `verdict=ok`. Live reading: **179 controls, 14 sourcing, floor 13, held.** *Not taken:* `remainder=165` counts controls that plant nothing at all, so it can never reach zero -- `%578`'s open question, still Keaton's. **CLOSED.**
