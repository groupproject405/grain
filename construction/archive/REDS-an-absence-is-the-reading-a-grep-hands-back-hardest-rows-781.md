# REDS -- row %781, folded from the living pin

**Folded:** `20260916.202456` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A front door counted a rostered gate among the silent, because the reading that would have
found it was a grep for four names at once, and the one line that answered carried a hundred
and twenty paths. An absence is the reading a grep hands back hardest: its evidence is what
the reader skimmed past.*

The row stands here exactly as it was written. A closed row leaves the living pin so the
pin stays the length a reader will actually read; the lesson travels forward in the page and
the witness header the round repaired, and the row itself stays one click away.

---

**REDS %781 (`20260916.110857`) -- a front door counted a rostered gate among the silent, and the word it counted with covered two different facts.** *What went wrong:* `src/gate/README.md` has said since `20260906` that **four of the eight equality gates are heard and four await a runner**, naming *the Aurora three and the Caravan one* as standing on no roster, on the evidence that *a grep across `tools/` and `construction/` returns their names only here*. `tools/ca/caravan_suite_witness.rish` has named `tools/ca/caravan_c4_exit_meanings_eq_witness.rish` in its `let witnesses = [ ... ]` array since `20260823` (`008725232`), and `caravan_suite` is a rostered guard. That grep answers with one line -- the roster array is **120 witness paths on a single line**, so the matching name arrives inside a wall of its siblings. `tools/m/mantra_a1_equality_witness.rish:31-33` carries the same sentence in its own header, *all four also unrostered and uncalled*. *What caught it:* the aether rota's instruction to listen for the claim a page keeps repeating, run against the four names one at a time -- `git grep -l <name> -- tools construction` for each, which is four readings where the elder lap took one. *What it taught:* **an absence is the reading a grep hands back hardest, because its evidence is what the reader skimmed past.** A present name in a long line and an absent name in no line produce nearly the same impression at a glance, and only the second is what the sentence claimed. This is the same wound `%457` booked one room over -- a seat concluding absence from a reading that was honest and incomplete -- so the pattern has now fired twice and wants naming rather than a third repair. *And the second half is sharper than the first:* **the word `heard` was doing two jobs.** The page used it for *rostered and called*; a reader takes it for *run*. Read off this pier's own run card, `construction/standing-equipment-runs.kyri`: **70 of the 79 guards at `tier cadence` have never run here**, and both `mantra_a1_equality` and `caravan_suite` are among the 70 -- so **zero of the eight gates have ever actually been sung by the roster on this checkout**, five of them being rostered notwithstanding. The cadence clock turned here for the first time on this lap, under `--cadence-slice 1`, which named `spoken_ascii last=never`. *The repair:* the paragraph reads **five heard, three awaiting**, names the Caravan suite and the commit that rostered it, and separates membership from sound with the run card's number beside it; an erratum block below names the elder reading, its stamp, and the grep that produced it; the witness header's own sentence moves with it, since a claim written twice is a claim two files may come to disagree about. The page also joins the `DOOR` roster of `tools/fixtures/p/prose_register_scan.sh` at **29% to 10%**, and declares its Door setting on its own face, which is the wall that roster carries. *What this does not reach:* whether the Aurora three should be rostered, and under what suite -- that is Aurora's lane and a short lap in its own room; and whether 70 never-run cadence guards is a fault or a clock still winding up, which is the card's own open reading. **CLOSED.** *Booked `%773` on a lap whose send was lost; the row stood in this ship's round-open stash box alone until `20260916.201445`, and the number it had booked was spent upstream in the meantime -- so the stamp held and the view moved, which is the derived spine doing its ordinary work.**
