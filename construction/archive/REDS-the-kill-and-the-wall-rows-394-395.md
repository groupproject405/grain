# REDS %394, %395 -- a signal and a wall, both scoped narrower than the fault

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260831.073652`, both **BOOKED** -- one lap's
two findings about a scope chosen for the ordinary case and met by the other one. The first is a
signal: `pkill -f` matches by pattern across the whole host, and this pier runs one tree per star
with three of them invoking the same script name, so a lap stopping its own runner pressed a
fleet-wide button and needed forensics afterward to say whether it had. The second is a wall: a pin
bound weighed only the pins a commit STAGES, which is the right scope for the round that moves one
and blind to the rebase that carries two more in beside it -- so an enforced pin shipped 1,040 bytes
over with every guard green. Together they say one thing twice: a filter narrow enough to be cheap
is narrow enough to miss, and the miss looks exactly like a pass. Both rows name their repair and
both landed it. Moved here to bring the pin back under its bound. Link depth moved with the text.*


**REDS %394 (`20260831.045858`) -- a lap stopped its own roster with a host-wide pattern kill, on a pier where three bodies run the same script name.** *What went wrong:* the hot pass reddened on a placeholder this lap had left in the card's Git nib field, so the lap stopped the line to fix the root -- and stopped it with `pkill -TERM -f standing_equipment_run.sh`. `pkill -f` matches every process on the HOST. This pier carries one tree per star and three of them run `tools/fixtures/s/standing_equipment_run.sh` by that exact name, so the command is a fleet-wide stop button any lap can press by accident. The charter's *a claim never touches a peer's in-flight work* was written one layer up, about files. It also killed the lap's own background waiter, which is how it announced itself: exit 144. *What caught it:* reading the process table afterward rather than trusting the kill -- `pgrep -fa` still showed `standing_equipment_run.sh --hot`, and `readlink /proc/<pid>/cwd` named a peer's tree. *What it taught:* **a process filter is a path filter's blind twin -- read scope bounds which files a lap walks, and nothing bounds which processes it signals.** The evidence says this instance reached only the lap's own runner: the peer's run card was written at 05:08, before the kill, and its next `--hot` pass began at 05:13:04, after -- which is what an uninterrupted pass looks like. That is evidence rather than proof, and a kill needing forensics afterward is the wrong kill. *Repaired (`20260831.045858`):* the lap re-read its runner's pid from its own `nohup`, confirmed `ppid=1` -- an orphaned local runner, where the peer's carries its supervisor's pid -- and signalled that one pid. *Named, not taken:* a stop helper that resolves a runner by `/proc/<pid>/cwd` against the caller's own tree, so the safe form is the easy one. **BOOKED.**

**REDS %395 (`20260831.062211`) -- an enforced pin shipped 1,040 bytes over its bound with every guard green, because the guard weighs only the pins a commit STAGES.** *What went wrong:* `construction/REDS.md` stands on `tools/fixtures/l/living_pin_guard_roster.txt` at `enforce`, bound 24,576. This round booked a 1,853-byte row against a pin at 23,878, which fit. Then the send collided, and the rebase carried upstream's `%392` and `%393` in beside it, taking the pin to **25,616**. A rebase skips `tools/hooks/pre-commit` by git's own design (`%339`), and the single amend that followed staged `construction/ITINERARY.md` alone -- so `tools/fixtures/p/pin_bound_touch_scan.sh` read `mode=staged touched_pins=0 verdict=ok` over a pin 1,040 over its wall, and it was pushed to both remotes that way. *What caught it:* a `wc -c` typed by hand at the close of the round, beside the bound `living_pin_max_bytes.sh` printed one line above -- 25,616 against 24,576, read by eye. No rostered guard could have: the hot pass ran before the rebase, over a pin still under. *What it taught:* **a guard scoped to what a commit TOUCHES cannot see what a MERGE brings, and both arrive through the same door.** `touched_pins` is the right scope for the fault it was drawn for -- *the round weighs the pin it moved* is its own printed story -- and a rebase moves a pin nobody staged. This is `%388`'s class one room over: a guard measuring an empty set is greener than a guard that is right, and `touched_pins=0` is that emptiness printing itself with nothing reading the count. *Repaired (`20260831.062211`):* `%390` and `%391`, both **BOOKED** and both this tree's own, folded to `archive/REDS-the-cost-and-the-door-rows-390-391.md` through `reds_fold.sh`; the pin reads 21,836. *Named, not taken:* a reading that weighs every ENFORCE pin unconditionally -- at the runner's first line beside `staged_uncommitted`, where the whole cost is one `wc -c` per roster row -- so the scope that made this invisible is not the only scope anyone runs. **BOOKED.**
