# ITINERARY landed accounts -- folded `20260908.080144`

**Language:** EN
**Status:** Shelf -- landed accounts folded whole off the living card, kept word for word
**Voice:** Kyri

*Folded from [`../ITINERARY.md`](../ITINERARY.md) so the card keeps its declared bound of 40,960
and holds what is still OPEN. Nothing here was condensed: the PATCHOULI block below stands exactly
as it was written, with its one relative link re-anchored a directory deeper. Every claim in it
landed -- the widening was adopted from COPAL rather than kept as a duplicate, the `rish_io`
front-door repair stands in the tree, and the row it names is CLOSED and folded to
[`REDS-a-pointer-with-no-claim-rows-619.md`](REDS-a-pointer-with-no-claim-rows-619.md). The one
question it carried for Keaton -- whether an OPEN row should carry a claim at start rather than at
landing -- stays live on the card rather than resting here, because an open question is not a
landed account.*

---

**PATCHOULI -- TWO SHIPS MADE THE SAME REPAIR IN ONE HOUR, AND THE LEDGER SENT BOTH.**
Elder [shelved](20260908-070340_itinerary-landed-accounts.md). Cold open **187 guards,
1,445s, 182 green, 2 red, 3 gated**: `stash_record` at `orphans=34` still `%592`'s seat, and
`standing_equipment` behind it. A first pass read `index_row_bound` red over **the tree before my
own round-open reset** -- `tree_moved=yes` at its close said so, and the settled tree reads green.
A reading taken across a reset is not a reading.
**I TOOK `%601`'s CLOSING SENTENCE** -- *a guard reading tracked runners for a fixed `/tmp/<name>`
write is left as a lap of its own* -- and found that guard already built: `shared_pen_scan.sh`
selected its population with `git ls-files 'tools/*'`, blind to **68 tracked runners** elsewhere.
Widened it names one file, `rishi/tests/file_io.rish`, whose `list-dir "/tmp"` assertions could
pass on a **peer's** files with `write-file` broken -- a false green rather than a clobber.
**COPAL LANDED THAT WHOLE DIFF AT `052557` (`ac25bfffc0`)** while I measured: same source line,
same one file, same anchor room, same three control legs, the same `236/46/15` either side. The
rebase caught it -- four of my five paths had already moved upstream. I dropped mine, took theirs.
**THE ROW (`20260908.070340`) BOOKS THE EIGHTEENTH FIRING**, and it is sharper than the seventeen before: every earlier
one was two hands finding one fault, where here **the ledger pointed at the work in a row's own
closing sentence** and recorded nobody taking it. A pointer with no claim beside it is an
invitation to duplicate, addressed to everyone. **Yours** -- it is your own standing question.
**Kept:** `tools/fixtures/rish_io/README.md` said the room held only the witness's scratch while a
second writer had just moved in; A/93.
**Next:** the port is mechanical and stays **Keaton's word** (`%589`).


---

## This round's account, folded on landing (`20260908.084500`)

*The card carried this whole while it was being written and folded it the moment two peer commits
rebased over it, leaving the pin 274 bytes past its bound. Nothing is condensed: every sentence
stands here as it stood on the card, with relative links re-anchored a directory deeper. The open
question stays on the pin, because an open question is not a landed account.*

**PATCHOULI -- A RATCHET EXISTS TO REACH ZERO, AND ITS CONTROL COULD NOT SURVIVE ZERO.**
Elder [shelved](20260908-080144_itinerary-landed-accounts.md).
**THE WATER ROTA SENT THIS LAP AND ITS OWN INSTRUCTION SAVED IT** -- run the thing rather than
read about it. `tally_caller_map` stands unheard, checking **`-e` existence** on a hand-listed 19
while its canon says callers reach marks *by symlink or import, not copies*, and **69** stand in
the tree. Existence is a weaker question than the sentence it guards -- `%614`'s shape.
**I NEARLY BUILT THE FOURTH DUPLICATE OF THE DAY, AND RUNNING THE TREE STOPPED IT.** `copy_lag`
(`20260906.070240`) carries that rule to **109 basenames**, deriving each canon from its own
symlinks -- and had already told `lotus/stack.rye` apart as a **sibling**, the false positive my
basename read produced an hour earlier. It read `behind=1`: `caravan/parse_int.rye`, a real file
among **eighteen sibling marks its own room links**, drifted by **two comment lines, one each
way**. The canon took the copy's comment first, so the link dropped no writing; then the file
became a symlink, as `mand/capabilities.rye` did `20260907.191247`. Ceiling **1 -> 0**.
**AND THE CONTROL REDDENED ON THE LAP THAT CLOSED IT** (`20260908.080144`, **CLOSED**): leg 3
planted one lag and asserted `verdict=ok`, true only above zero. Its right pattern stood **one leg
away** -- leg 8 asks the scan for its ceiling and proves both sides; leg 3 asks now too. Of **194
controls, 3** read a ceiling and the other two were already right, so the class is one file.
Proven three ways, the last inverted so the marker says **no**. 31 behaviors GREEN.
**Kept:** `copy_sameness` spoke a `20260729` split as now while it reads **23, 0, 0**. A/91, B+/89
**Molted:** `tally/parse_int.rye`, **149 citers**, **C+ 75 -> A+ 98** in place under a cairn --
second page this week where a Door ceiling bit a passage whose subject is a refusal.
**Yours:** the caller map names **18 of 69** homes and `copy_lag` covers the drift half -- is its
remaining seat the **prose binding** alone, and should it say so?
**Next:** the port is mechanical and stays **Keaton's word** (`%589`).
