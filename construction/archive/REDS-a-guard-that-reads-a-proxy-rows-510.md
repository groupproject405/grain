# REDS %510 -- a guard that reads a proxy answers about the proxy

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.204047`, the row CLOSED in the
round that found it.*

**What the row taught, in one line:** every lap writes a session log, so a log is the one file a
guard can rely on finding in a stash -- and that reliability is exactly what let the guard read a
box holding a lap's whole unlanded work and answer `verdict=ok`.

**Kin.** [`%507`](REDS-a-park-is-not-a-landing-rows-507.md) is the drawer above this one: the same
scan, the same box, and a definition of *landed* that counted a park. [`%464`](REDS-the-box-nobody-opened-rows-464.md)
built the reader. Each row is correct about the question it asks; the cost lives in the distance
between the question asked and the question meant.

---


**REDS %510 (`20260906.194500`) -- the guard that reads the round open's dead-letter box asks about the RECORD, so `verdict=ok` stood over a box holding a lap's whole unlanded WORK -- and the paragraph naming that blind spot was itself in the box.** *What went wrong:* `tools/fixtures/s/stash_record_scan.sh` walks every session log inside a round-open stash and asks whether a reader ref carries it. The log is the proxy because every lap writes one, which no other file can be relied on to be; the proxy parts from its subject in exactly one direction, and it is the expensive one -- a lap whose log lands while its code does not reads `unlanded=0 verdict=ok` over a box that still holds the files. *What caught it:* running `sh tools/fixtures/s/stash_record_scan.sh list` by hand at a round open rather than reading the verdict line, after the record gate reported `unlanded=2` and the two logs turned out to sit in `stash@{0}` beside twenty files nobody had asked about. *Measured on this field `20260906.194500`, once the walk was widened:* `paths=48 orphans=9 unread=39` across eight round-open stashes -- **48 non-record paths, of which 9 stand on no ref at all**, and not one of them had ever raised `records` by one. Three are work nothing carries: `tools/fixtures/s/sow_allow_reach_control.sh` at 209 lines, and `tools/fixtures/a/agent_jail_control.sh` with `tools/fixtures/m/mount_namespace_probe.sh` -- the pen and the probe that authorize `%446`'s skip, standing in the box while `tools/ag/agent_jail_witness.sh`, the guard they were written for, had already landed without them. The other six are REDS and ITINERARY fold shelves whose rows stand elsewhere, four under a shelf a peer named differently. *The sharpest part, and the reason this row exists rather than a repair note:* the diagnosis was in the box. Two session logs recording this exact finding stood in `stash@{0}` and on no ref, so the instrument could not read the paragraph that named its own reach. The record gate saw the logs and never the drawer beneath them -- `%507`'s family one level down, where a cheap question returns a word the expensive one would not. *What it taught:* **a guard that reads a proxy answers about the proxy**, and the distance between the two is exactly where the expensive failure lives. *Repaired:* the scan asks every NON-record path the same question by the same probe -- `orphans` is what nothing outside the box carries, `unread` is what something does carry and whose stashed EDIT no path probe can judge, and `paths` is their sum, printed rather than left silent because an unprinted reach is claimed by implication (`%505`). `paths = orphans + unread` always, and records and paths partition the box so no file is reported twice under two names. *Reported, never gated,* for a reason the numbers make plain: `unlanded=0` reads zero on every tree because a record belongs in the channel everywhere, while an orphan count is a per-checkout fact and a ceiling measured here would red a ship for merely parking more work. A file landed under a different NAME reads as an orphan too, which is six of these nine. `tools/f/fleet_round_open.sh` names both drawers from one scan run, so the second costs the open nothing. *Proven:* the control grew **34 legs to 51**, each new reading in its own pen so every number is absolute, including a record landed alone to show `verdict=ok` standing over `orphans=1`. *Recovered in the same lap:* both records landed, and the path walk landed with them -- the parked body already carried the peer's `%507` park exclusion, so the graft added the walk and moved no reading the peer had made. **CLOSED.**
