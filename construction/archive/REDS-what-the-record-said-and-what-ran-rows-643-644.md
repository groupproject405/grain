# REDS -- what the record said, and what actually ran

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- two folded rows, immutable once written
**Room:** checkable -- two ledger rows, each repair proven by a control on metal
**Folded:** `20260908.183900` from [`../REDS.md`](../REDS.md)

Two rows folded the hour the second was written, to give the pin back the headroom eight ships
share: thirteen OPEN rows had left it 232 free bytes, so no lap could book a red at all, and the
raise is Keaton's word by the pin's own header.

They teach one thing from two rooms. **A stated intention and the mechanism that carries it are two
artifacts, and in both cases nothing was reading them together.** In `%643` a hand's request to stop
a loop and the loop's own round-open clearing step were each correct alone, and the second erased
the first. In `%644` a merge's postcondition named four properties in prose and asserted three in
code, and the clause that passed was the field document order reads first. Neither is a mistake in
reasoning; both are a promise written in one place and kept in another, with no instrument spanning
the gap. Read them beside [`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %643 (`20260908.165143`) -- a hand asking a loop to stop in the twenty seconds between laps was answered by the loop erasing the request.** *What went wrong:* `tools/f/fleet-loop.sh` opened each iteration with an unconditional `rm -f .loop-gates-only` and read the sentinel only at the foot, so the file served the AGENT (set during its own lap, read below it) and served a HAND poorly: a `touch` landing during the 20s sleep was deleted by the next iteration before anything read it, and the loop worked on. It failed silently, toward *keep running*, which is the one direction a stop must never fail. The only reliable hand-stop left was killing the process, which cuts a lap mid-send -- the very fault that made `status` a written-before-the-send field. *What caught it:* Keaton, asking whether the watcher should learn to pause a seat so a fresh interactive session could take its tree. *What it taught:* **two stops with two owners want two files.** `.loop-gates-only` is the agent's and is right to clear at the top, since a stale one would block tonight's first lap; `.loop-drain` is the hand's, is read before a lap opens AND after one finishes, and is removed by the hand that set it. `fleet_watch.sh` refuses to re-arm a drained seat, since a watcher undoing the one request the drain exists to make is worse than no drain. Proven by `tools/fixtures/f/fleet_drain_control.sh` -- 9 behaviors, every sentinel planted and lifted, the two sentinels proven distinguishable. **CLOSED** on that control.

**REDS %644 (`20260908.182735`) -- `Weave.merge`'s postcondition named four properties and asserted three, and the one it let pass is the field document order reads first.** *What went wrong:* `mantra/src/weave.rye`'s merge doc promises "places strictly increase, every line keeps a real generation, and next_pos and next_run stand above every position and run in the result." Its postcondition loop asserts `gen >= 1`, `pos < merged.next_pos`, and strictly increasing places. **It never asserted `line.run < merged.next_run`** -- the third of the three counter invariants the `Weave` type declares at line 422, and the field `Place.less_than` compares FIRST. Two things rest on it: the next `apply` stamps its whole insert block with `self.next_run` and appends, and `current` reads the backing list without sorting. Both arguments are written out in `current`'s own header, and both cite **`apply`'s** postcondition, which does assert all three -- so the reasoning stood on a post that exists on one path into a weave and not on the merge path. `from_v1` carried the same gap, asserting `pos < next_pos` and `gen >= 1` and not the run. *What caught it:* the air rota row read as its own sense -- walking the fence line and pressing each post rather than reading what the posts claim. Three constructors produce a `Weave` and the type declares three counter invariants; only `apply` asserted all three, which a comparison across constructors shows and a reading of any one of them hides. *What it taught:* **a postcondition written in prose and a postcondition written in asserts are two artifacts, and nothing was comparing them.** This is `%463`'s family in a module header: the doc's fourth clause read exactly like the three that were checked. *Repaired (`20260908.182735`):* the assert added to `merge` and to `from_v1`, each with its `// invariant:` reason. The merge one is **isolating and proven from both sides on metal** -- `tools/fixtures/m/mantra_weave_merge_control.sh` gains a `run_counter` phase rewriting the counter's `@max` to `@min`, which exits **134** with the assert and **0** without it while every other witness claim passes, so the plant reaches this line rather than an assert already standing. The witness names the phase and reads nine breaks where it read eight. The `from_v1` one is **a restatement and says so in its own comment**: the lift control's existing `next_run` plant is caught by the witness's `lifted.next_run == 1` whatever that assert does, measured the same way, and it is kept for uniformity across the three constructors rather than for a break it catches alone. *Not taken:* `mantra/src/weave.rye` grades **C+** on the report card, reach 60 against a Door budget of 9 -- a property of 618 words of dense module header rather than of this lap's ten lines, measured before and after and unmoved. A reach molt of the whole header is its own round. **BOOKED.**
