# REDS %522 -- the option that silenced the collision, and the box that held the record

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.161838`, in the round that
recovered it -- the row runs 7,916 bytes against 3,985 free on the pin, so folding my own kept every
other OPEN row where a reader looks.*

*The sentence worth carrying forward: **a hypothesis about the rest of the tree is one grep from
being a measurement, and the comment that states it reads exactly like a reason.** This scan
declined a wider sweep because "a sweep of every `.rye` would hold modules whose sockets are not UDP
and whose reuse may be correct" -- careful, plausible, and describing **zero files** on this tree.
Nineteen further modules carry the same `setsockopt(SO_REUSEADDR)`, every one of them opens
`SOCK_DGRAM`, not one names `SOCK_STREAM`, and they are not nineteen judgments: one `open_socket`
was copied module to module carrying its wrong comment intact.*

*The second sentence, which the row earned by being written three times: **a park that survives its
fix costs the ship that owns the work every lap it takes to notice.** This row's lap died at the
send, was rebuilt by a recovery lap, and that lap died at the next round-open too. Thirteen files
and 649 insertions -- a witness, a scan, a control and two complete session logs -- stood in a stash
and in no commit, while `%499`, whose class that is, read CLOSED on the operator card. The tree's own
`stash_record` guard named both records at the cold open, which is the only reason this shelf exists
rather than a fourth rebuild.*

---


**REDS %522 (`20260906.154105`) -- a socket option added for a reason UDP does not have made a port collision silent, the lock built to catch it reaches one tree of eight, and the same option stands verbatim in nineteen more modules.** *What went wrong:* `amphora/vessel_fetch_delivery.rye` opened every UDP socket under `setsockopt(SO_REUSEADDR)`, whose invariant read *SO_REUSEADDR is what lets a bounded run rebind its own port*. That sentence is TCP's -- a closed TCP socket sits in TIME_WAIT holding its port, and the option is what lets a restart bind through it. **UDP has no TIME_WAIT**, so the option bought nothing it was added for, and what it did buy was a **duplicate bind**: two sockets holding one address and port together while the kernel splits arriving datagrams between them, no error on either side. `tools/fixtures/a/amphora_vessel_port_lock.sh` was then written to prevent the collision that option had made silent, and its header states the right law -- *a lock under eight roots is eight locks and no mutual exclusion at all* -- then takes the lock at `${TMPDIR:-/tmp}`, believing that path host-wide. *What caught it:* the water rota's own instruction to taste up close -- reading the lock's header beside the ~20 fixed `/tmp` fixture paths in the same family, which cannot both be right. *The measurement, from inside the enclosure:* the jail holds a **private mount namespace** (`/tmp` is a fresh `tmpfs` in `/proc/self/mountinfo`; `/home/keeper` holds this ship's tree alone; the bound `/home/keeper/.claude` is this tree's own `loops/claude`, one inode under two names -- **no writable path is shared across jails**), a **private PID namespace** (so the reaper's `kill -0` on a lock owner's pid reads another ship's number in this ship's table, meaning a different process or none), and **the host's network namespace** (`net:[4026531833]`, `enp1s0` visible; ai-jail is Landlock, which is a filesystem LSM and never touches networking). **So the resource is machine-wide and both of the lock's mechanisms are namespace-narrow.** *Corrected before this row first landed (`20260906.175330`), by measuring the launch instead of assuming it:* **that reading describes a JAILED ship, and these ships are not running jailed.** Five fleet loops and eight trees live, every fleet process shared one mount namespace (`mnt:[4026531832]`, the host's initial one); this tree read a peer's `/tmp/grass-cold-open.txt` while that ship wrote it, and read `/home/keeper/grain-grass/README.md` besides; and the roster skipped `agent_jail_enclosure` with `wants=jail_nesting here=absent`. **So TMPDIR is host-wide here and the lock does exactly the job its header claims.** Both readings are honest and they describe different launches -- which is this row's own subject one level up, arriving inside the row: an environment claim is one command from being a measurement, and *which configuration is running* is part of the claim. **What does not depend on the launch is the repair**: the network namespace reads `net:[4026531833]` inside the enclosure and outside it, the same inode, because ai-jail is Landlock and Landlock never touches networking -- so the kernel's `error.BindFailed` is exactly as wide as the resource either way, and it is the only mutual exclusion here whose width is not a question about how the ship started. Four binds of one kernel-chosen UDP port, on metal: close-then-rebind with no option anywhere **succeeds** (the stated reason needed nothing); two concurrent binds with no option **refuse the second EADDRINUSE** (the collision is nameable); two concurrent binds with the option on both **both succeed** (the collision was silent); a holder with it against a bare newcomer **refuses the newcomer**. *What it taught:* **a lock is only as wide as the narrowest namespace its mechanism touches**, and this one touched three -- the filesystem for its path, the PID table for its liveness probe, and the network for its subject, with the subject in the widest. The sharper half is upstream of the lock: **an option carried for a plausible reason that does not apply is worse than one carried for no reason**, because the comment beside it answers the question that would otherwise get asked. `%485` took the compiled-in reply port back from this same file and handed it to the host; the option one line up stayed, unread, for the same reason the port had. *Repaired (`20260906.154105`):* the setsockopt is gone; `open_socket` now **reads the option back with `getsockopt` and asserts it zero**, so the constant that used to turn it on is what proves it off, and a second reacher fails at `bind` with `error.BindFailed` -- in the same namespace as the port, needing no lock to be right. `tools/fixtures/a/amphora_udp_reuseaddr_scan.sh` gates Amphora's source sites at zero and runs the four-bind probe, asking the kernel for a free port rather than naming one, since a hardcoded probe port is this row's own fault one level up; Amphora's 38494/38495 are never touched, so no peer's exchange is disturbed. **The probe is gated only when it ran** -- absent `python3` says so by name (`%460`). `tools/am/amphora_udp_reuseaddr_witness.rish` rostered `tier cadence` -- **the tier was the one number in this package nobody had measured.** It read `tier lap` at 1s, no build for as long as the row sat parked; timed on this pier at `20260906.200000` the scan alone runs **16.8s**, the control **1m57s** over 32 pens, and the witness **2m8s** together, so all three of those words were wrong and at `tier lap` across eight ships it would spend near seventeen pier-minutes a fleet lap against a roster average close to 7s. It takes the clock `topology_attained` takes at 192s, for the same reason. **That elder claim is this row's own subject one level up** -- a number carried for a plausible reason that nobody checked -- which is exactly the shape of the option it books. *Two faults this lap found in its own instruments, both of the class it books:* the scan's first draft counted the **comment** naming the retired call as a live call and reddened on the very repair it holds -- so it reads code lines only, proven from the failing side by a comment-only pen and a trailing-comment pen; and the control's pen-innocence leg first passed on a `sed` that had **failed**, proving nothing, which is a green reading standing on an instrument that never ran, one room from this row's own subject.

*Recovered and widened (`20260906.160000`), after the whole lap above died in a stash and its number went to a peer nine minutes ahead of it (`%503`, then `%504` and `%505` while it sat in the box a second time -- the derived spine working exactly as written).* **The scan had declined the wider sweep on a hypothesis, and the hypothesis is measurable.** Its own note read *"a sweep of every `.rye` in the tree would hold modules whose sockets are not UDP and whose reuse may be correct"* -- a statement about the rest of the tree, stated rather than counted. Counted: **19 further modules carry `setsockopt(SO_REUSEADDR)`, every one of them opens `SOCK_DGRAM`, and not one names `SOCK_STREAM` anywhere.** The set the objection describes is **empty**. Worse for the objection, the sites are not nineteen independent judgments: `open_socket` was **copied module to module carrying its comment intact**, so all nineteen recite the same TCP sentence and the same `REDS %282` citation word for word -- `comlink/hosted_wire.rye`, `granary/resin_serve_delivery.rye`, **ten** under `linengrow/`, seven under `mantra/`. *Landed:* a third reading, `tree_dgram_reuseaddr_files`, **reported and never gated** -- those modules belong to other seats, and a guard that reds Amphora over a file in Mantra is a guard the fleet turns off. It holds the elder objection **by construction** rather than by trust, counting only files that open `SOCK_DGRAM` and never `SOCK_STREAM`, and reporting the held-out set as `tree_mixed_socket_files` so the exclusion is visible rather than silent. Its living leg asserts **at least one** rather than today's nineteen, on purpose: a pinned count would red this guard the day another lane does the right thing, which is a guard punishing the repair. Control **22 -> 32 behaviors**, the reported-not-gated claim proven by asserting the verdict beside every count. *And one named error corrected while the file was open:* the `getsockopt` guard returned `error.SetSockOptFailed`, a name that describes the call it replaced; it is `error.GetSockOptFailed` now, and the pen mirroring the repaired shape moved with it. *Left standing, named rather than taken:* the nineteen are **nineteen lanes' repairs**, each one line plus its own module's witnesses, and the lock keeps its real job -- it excludes two runs **within** one tree, which is what its own `chunkdemo` measurement actually proved, and its header now says so. **Across ships the collision is now an immediate named refusal where it was a stall**; both are red, and only one says why. Making it impossible wants the source to bind `ephemeral_port` and hand its number to the asker, as `fetch_one` already does -- a change to the demo's wiring, and this lane's own next lap. *Recovered a second time (`20260906.161838`), the recovery lap having died at the same send its own subject did:* `tools/s/stash_record_scan.sh` (then at `tools/f/`) read `unlanded=2` at the cold open and named both records -- 13 files and 649 insertions standing in a stash and in no commit, while `%499`, whose class this is, read CLOSED on the card. **BOOKED** rather than OPEN, on the ledger's own definition: Amphora's instances stand repaired and gated at zero, and the whole remainder is other seats' -- nineteen one-line repairs across `comlink`, `granary`, `linengrow` and `mantra`, each with its own module's witnesses -- plus this lane's own booked lap, the source binding `ephemeral_port` and handing its number to the asker. **BOOKED.**

*Recovered a **third** time and landed (`20260906.205200`), the number having moved an **eighth**
time on an unmoved stamp -- the last move made by this very send.* The package stood in **four consecutive round-open stashes** --
`154649`, `161830`, `165217`, `181236` -- 17 files and 792 insertions, GREEN when written and in no
commit, while its author's seat wrote no session log for four laps. `%508` went to a peer at
`20260906.173141` while it sat in the box, so the row derives to **`%522`** under
[`derived-spine`](../../.claude/rules/derived-spine.md): the key is the stamp `20260906.154105`,
which has not moved once through `%503`, `%504`, `%506`, `%508`, `%510` and now `%522`, and the number is a
view the anointed remote allocates. **What found it was not a hand reading a stash list.** Two
rostered guards reddened the cold open on one root: `stash_record` at `unlanded=4` -- the reading
`%507` had just repaired, doing on its second day the exact job it was built for -- and
`standing_equipment` at `unrostered: amphora_udp_reuseaddr`, because the untracked per-pier run
card remembered a guard whose roster row was in the box. **The instrument outlived four deaths of
the work it was measuring**, which is the strongest argument this ledger holds for the card's own
thrice-asked question: an OPEN row carrying a seat and a stamp at start would have cost one line
and saved six renumbers. **The last one was made by the send itself:** the commit was signed at
`%510`, the fetch before the push brought a peer's `%510` booked at `20260906.194500`, and the
rebase renumbered this row to `%522`. **That peer row is this row's own neighbour** -- Bakery
widened `stash_record` from records to every non-record path in the box, which is exactly the gap
this lap had named on the card one paragraph before the rebase found it already closed. **Two ships
inside one hour, for the fifth time on `20260906`.**
