# REDS -- row %699, born on its shelf

**Born:** `20260910.082230` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`../../.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Door B landed as the one door that reaches the cause, and twelve days later the pin stands where
it stood -- because the population that refilled it is a different population.*

Born on its shelf as `%335` through `%341` were, since the living pin holds **40,771 of its 40,960
bytes with all 16 rows OPEN and none foldable**, so no lawful fold exists and the row has nowhere
in the pin to go. That is this row's own subject, so it is written where its subject put it. **The
birth is recorded practice rather than law:** `%338`'s door C -- sanction the single-row shelf birth
in `reds_fold.sh`'s own contract -- was costed and declined, and it is Keaton's word still. The
recital records the shape it stands on: *a row closed on the lap it is booked can be born on a
shelf.* Its cost is the one `reds_fold.sh` guards against, and
[`REDS-fold-recital.md`](REDS-fold-recital.md) carries the trail.

**REDS %699 (`20260910.082230`) -- the ledger deadlocked again, and the meter that detects the wall named no door.** *What went wrong:* `construction/REDS.md` stood at **40,771 of the 40,960 bytes** its own header declares -- **189 bytes** against a median row of **2,422** -- with **all 16 rows OPEN** and **none foldable**, so `tools/fixtures/r/reds_pin_capacity_scan.sh` read `pin_deadlocked=1` and `verdict=ok`. Five ships met that wall in one morning and each re-derived the same options from scratch: three folded a peer's fresh row to make room, one folded its own `%688`, one declined the move by name and called it a workaround. **At least one red went unbooked** -- `20260910.064044`, the amphora framed-name finding, stands cited in `construction/ITINERARY.md` and in `construction/standing-equipment.kyri` and in no ledger row, and the card block carrying it shelves. *What caught it:* running the capacity scan up close after reading five ships' cards ask Keaton for a bound raise in one morning, then reading **who holds** each of the 16 rows rather than only whether each was open. *What it taught:* **door B split a live defect from a booked remainder, and never split who can CLOSE a live one.** `%338` measured its nine open rows and found not one open on an unrepaired defect -- ratchets, seats, booked laps -- so door B drained them and the pin fell 24,828 to 5,388 bytes. The population that refilled it answers differently: **9 of the 16 name Keaton, a custody gate, or a numbered gate**, and 7 of those name `Keaton's word` outright. Eight ships find reds at fleet rate; a row waiting on one person leaves at one person's rate. **That is a refill mechanism rather than a busy week**, and no status word in the ledger can see it, because OPEN answers *is the defect live* and never *who may close it*. The second half is smaller and sharper: **a meter that detects a wall and goes quiet about the doors is half a meter.** The foldable cell has printed the exact command to type since `%517` -- *write a shelf head and run `sh tools/fixtures/r/reds_fold.sh ...`* -- and the deadlocked cell printed the diagnosis and stopped, which is why five careful hands spent five laps' attention arriving separately at three doors already costed in an essay. *Repaired (`20260910.082230`):* `reds_pin_capacity_rows.awk` gains a third reading, `held()`, and the scan prints **`pin_held_rows`** beside `pin_open_rows` with **one `detail: pin_held %N` line per row**, so the proxy is checked rather than trusted; the deadlocked cell gains **`detail: pin_deadlock_doors`**, naming `%338`'s three doors, whose word each is, and the born-on-a-shelf practice with its cost and its recital. **Reported, never gated** -- a gate on rows only one person may close would red hardest on the laps that found them. **Nine new control legs** in `reds_pin_capacity_control.sh` prove the reading from both sides on planted rows -- a Keaton row counts, a custody row counts, an ordinary open row does not, a CLOSED row naming Keaton counts for nothing, an empty reading prints zero rather than nothing, and the doors line rides with the deadlock and stays away from a healthy pin -- **50 cases, 0 failed**, and both mutations bite: blinding `held()` fails two, deleting the doors line fails two. `tools/r/reds_pin_capacity_witness.rish` GREEN on metal. *Not repaired:* the deadlock itself. `%338`'s three doors stand exactly as costed, and only one of them removes the cause; **which, and when, is Keaton's word.** **BOOKED** -- the blindness is measured and named at the wall; the remainder is a seat.
