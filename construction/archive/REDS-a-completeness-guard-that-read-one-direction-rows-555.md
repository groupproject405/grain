# REDS shelf -- a completeness guard that read one direction (`%555`)

**Language:** EN
**Stamp:** `20260907.080356`
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded from:** [`../REDS.md`](../REDS.md), on the lap that booked it, because the pin stood at
40,333 bytes of a 40,960 bound with ten OPEN rows and nothing closed to move.

---

**What the row teaches.** A completeness check compares two rosters, and an absence is well-formed
on **both** sides. Reading one direction proves the safer half and leaves the likelier one silent:
here the sealed registry is re-sealed by the draw itself, where the readable face is edited by a
hand -- so the face is the side that drifts, and the face was the side nobody read. The operator
card, which is neither roster and enumerated anyway, was the instance the rule's own lantern found.

---

**REDS %555 (`20260907.080356`) -- the operator card named 13 of 30 seated ladders, because the one guard that compares the two rosters read only the direction already safe.** *What went wrong:* `construction/ITINERARY.md` enumerated 13 ladders while `construction/waymark-registry.bron` held **28 living** and the rule table **30** -- and every ship reads that card whole each lap. *What caught it:* the Earth-Cardinal rota seat, *count, never number*, read against the card. *What it taught:* **an absence is well-formed on BOTH sides of a completeness check.** `waymark_table_registry_completeness.sh` proved table -> registry since `%298` and never the reverse, which is the likelier half: a draw re-seals the registry as its own act, where the face is a hand's edit. *Repaired:* the scan reads both ways, `absent_from_table` gated at zero, `abandoned` draws and hand-seated NAMES excluded by design and reported by name, 17 behaviors planted in throwaway pens; the card names no roster. **CLOSED.**
