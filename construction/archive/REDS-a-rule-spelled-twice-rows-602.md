# REDS -- a rule spelled twice

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by three legs each bitten on a plant
**Folded:** `20260908.010000` from [`../REDS.md`](../REDS.md)

One row, folded the hour it was written to hold the pin under a bound eight ships share. It teaches
where a centralized rule leaks: not at the callers that can route through it, which is where the
comment was looking, but at the one reader whose shape forbids routing -- and that reader is
reliably the one no guard reaches, because the same shape that forbids routing forbids a plant.
Read it beside [`REDS-whose-module-is-this-rows-593.md`](REDS-whose-module-is-this-rows-593.md),
this lane's other count taken by hand and found short, and
[`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %602 (`20260908.004856`) -- the wreck rule for marks is spelled twice in Amphora, and the second spelling is narrower.** *What went wrong:* `amphora/manifest_entry.rye` welcomes exactly two roots, `plain-bytes` and `manifest`, and its own comment says the check sits in the parser *because a rule written five times is a rule four places can forget*. `amphora/src/main.rye:append_cargo_line` spelled it a sixth time: its own tokenizer over the same `mark digest name` grammar, its own copy of the `plain-bytes` literal, and `if (!std.mem.eql(u8, mark, mark_plain_bytes)) return error.CargoUnproven` -- one root where the contract holds two. A vessel carrying the `manifest` root passes six readers and dies there. The same paragraph's census was short by two: `take_listed_line` at `vessel_core.rye:234` is a third parser site in that roof, named in neither the comment nor the roster note. *What caught it:* the earth rota row, read through its own sense -- `foundations/20260703-202312_the-marked-value.md` at the dual seat is Amphora's own page, so the lap took the shoulder-line vow to the module and counted the sites rather than trusting the comment that counted them. `amphora_mark_wreck`, seated the day before for exactly this vow, reads vessel-core's refusal frame and never opens `src/`. *What it taught:* **a rule is spelled twice wherever one reader cannot route through the other, and that reader is exactly the one no guard reaches.** This door writes what it reads -- `pour` builds the cargo plain, seals it, `restore` opens the same plain -- so no vessel edit reaches the divergence without breaking the AEAD seal first, which is why it stood since the module was written. Unreachable is not the same as absent. The reader borrows `mark_verdict` now and bounds the mark field ahead of it, so an over-long mark refuses rather than tripping that function's precondition assert; the written root is held against the contract's at comptime, so a rename there stops this build. Three legs added to `amphora_mark_wreck`, each proven to bite: the elder narrower rule restored in a source pen, the bound line deleted, and a divergent literal planted whose build refusal is asserted to name the comptime tie rather than merely to be non-zero. GREEN at 7.4s. **CLOSED**
