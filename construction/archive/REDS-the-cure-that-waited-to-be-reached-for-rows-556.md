# REDS %556 -- a portability cure stood in the tree for eight days, and the room that most needed it never reached for it

*Folded off the living pin [`../REDS.md`](../REDS.md) on the round that booked it, by its own
author, because landing the row put the pin 1,731 bytes above its 40,960 bound with exactly one
foldable row on it -- this one. It reads **CLOSED**: the site is repaired, both witnesses are GREEN
on metal, and the guard that found it reads `sed_i_sites=0`. No peer's row was moved to make this
room.*

*Its number moved THREE times in ninety minutes and its stamp never moved once.* Booked `%548`;
renumbered to `%549` when a peer published their own `%548`; to `%553` when upstream published
`%549` through `%552` during this lap's hot pass; and to `%556` when the very next fetch brought a
peer's `%553`, `%554` and `%555`. Eight ships were landing rows faster than one lap could verify and
push, which is what a shared counter looks like from inside.

*The second move is the interesting one, because the two rules met and only one of them yields:
upstream's `%549` carries `20260907.084022`, which is LATER than this row's `20260907.080201`, so
*the earlier stamp keeps the number* would hand it here -- yet *a published number never moves* holds
theirs, and renumbering is confined to **unshared** rows, which is this one. **The unshared row
moves, every time**, and that is what keeps the pair from deadlocking the way `%530` did
([`derived-spine`](../../.claude/rules/derived-spine.md)).*

*Across all three moves, **not one living citation needed an edit**, because every one already
spelled the stamp. The whole cost was three shelf renames and three lines -- the law's fourth rule
doing exactly the work it was written for, three times in one lap.*

*The same rebase found a peer had repaired this row's own site an hour earlier, with `sed_inplace`
from `tools/fixtures/s/shell_portable.sh`. Two hands, one red, one hour -- the fleet friction the
card keeps naming. The resolution takes `plant_apply`, which SUPERSEDES rather than competes: it is
the same temporary-through-the-original-inode write, so it is equally portable and equally
mode-preserving, and it also proves the rewrite landed. The peer's reasoning is kept in the file's
own comment, credited.*

*The sentence worth carrying forward: **a cure that lands as a helper waits to be reached for, and
the room that most needs it is the last room anyone looks in.** `sed_inplace` had stood in
`tools/fixtures/s/shell_portable.sh` since `20260830`. The file that broke the ceiling was a
**control** -- and planting a line and then lifting it back out is precisely the work that wants
in-place editing, so the one room with the strongest reason to reach for the cure is the room
nobody thought to check.*

*Its second sentence is about how the fault printed. One site reddened **two guards**:
`shell_dialect` on its own reading, and `standing_equipment` on the run card where it read the
first. A roster pass that reports `guards_red=2` for one defect is honest and still costs a reader
a minute deciding whether two things are wrong -- the run card is a mirror, and a mirror doubles
what it shows.*

*Its third: **the better answer was one room over, and it did more.** `tools/fixtures/p/plant.sh`'s
`plant_apply` is portable and mode-preserving like `sed_inplace`, and it also proves the plant
landed (%519). Nine hand-rolled lines checking emptiness and byte-identity came out, and the tenth
reading they had all missed came in -- a `sed` program the interpreter refuses leaves a partial
file that differs from its source, which a bare byte comparison reads as a landed plant.*


**REDS %556 (`20260907.080201`) -- a control's plant reached for the one in-place-edit spelling BSD reads as a filename, and the guard that holds that family at zero had never seen it until the cold pass.** *What went wrong:* `tools/fixtures/s/seat_prompt_figure_control.sh:91` wrote `sed -i 's/It holds 10 Rye modules\.//' "$(prompt_of "$p" alpha)"` to lift a planted figure back out of a pen prompt. GNU `sed` reads `-i` as edit-in-place with no argument; BSD and macOS `sed` read the NEXT word as the backup extension, so on the second bench that line renames the prompt and the check below it measures a file that is no longer there. `tools/fixtures/s/shell_dialect_scan.sh` holds `sed_in_place_flag` at a ceiling of **zero**, and the site put it at **one**. *What caught it:* this lap's cold `--scoped` roster open, `guards_red=2` -- `shell_dialect red` with `verdict=over_sed_i_ceiling`, and `standing_equipment red` reading the first off the run card, so **one fault printed as two guards**. *What it taught:* **a portability cure lands as a helper and then waits to be reached for, and a control is the last room anyone reaches for one in.** `sed_inplace` has stood in `tools/fixtures/s/shell_portable.sh` since `20260830`; the file that broke the ceiling is a *control*, whose whole job is planting and lifting, which is precisely the work that wants in-place editing. The tree already held the better answer one room over: `tools/fixtures/p/plant.sh`'s `plant_apply` edits through the original inode -- portable, mode-preserving, and it **proves the plant landed** rather than trusting `sed`'s exit code (%519). *Repaired (`20260907.080201`):* the control now sources `plant.sh` and calls `plant_apply` for the removal and `plant_write` for the innocence copy, retiring nine hand-rolled lines that checked emptiness and byte-identity and missing the tenth reading -- a `sed` program the interpreter refuses leaves a partial file that differs and reads as landed. Behaviors **30 -> 31**, the new one asserting the removal is a landed plant; `tools/s/seat_prompt_figure_witness.rish` pins the count and was moved with it in the same commit. `shell_dialect` GREEN, `pass=47 skip=0 fail=0`; `sed_i_sites=0`. **Adoption of `plant.sh` rises by one, and this is the first site the rising floor was reached by a guard rather than by a hand.** **CLOSED.**
