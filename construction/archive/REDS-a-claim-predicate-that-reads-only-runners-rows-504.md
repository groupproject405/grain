# REDS shelf -- a claim predicate that reads only runners, row %504

**Language:** EN
**Stamp:** `20260906.150954`
**Status:** Shelf -- immutable once written
**Rows:** `%504` -- folded from [`../REDS.md`](../REDS.md) on `20260906.223012`
**Voice:** Kyri
**Kin:** [`%498`'s reach-and-claim boundary](REDS-reach-and-claim-are-two-boundaries-rows-497-498.md),
which this row carries one room further, from executable callers into the pages a reader consults.

A file is *claimed* by whatever a reader would follow to it. This row is what happens when an
instrument reads only half of that: `rye_compile_reach_scan.sh` built its claim set from
`git ls-files -- '*.rish' '*.sh'` -- the runners -- so a witness named as proof in two living
Markdown pages sat in `never` while the scan answered `asserted=0`, correctly by its own rule and
wrongly about the tree.

**The instrument that found it could not have found it four hours earlier.** The census's own
`unreached` leg needed walkers to read, and `walked` stood at 2 that morning against 65 by the
afternoon. A census's blind spot shrinks as its subject grows, so the reading that surfaces a
defect is often the one taken after unrelated work widened the input -- which is an argument for
running a meter again after a lap that had nothing to do with it.

**Folded here because the pin met its bound**, oldest foldable row first, with every byte kept.
The row stands below exactly as it was written, its relative links re-anchored one directory
deeper by `tools/fixtures/r/reds_fold.sh`.

---


**REDS %504 (`20260906.150954`) -- a witness two living pages cite as proof was compiled by nothing, and the census built to find exactly that let it pass, because its claim predicate reads runners and a documentation page claims just as loudly.** *What went wrong:* `glow/nock/nock_glow_mirror_witness.rye` is named by `glow/nock/README.md` as *"the seam's first witness-backed pin"* and stands in the **Witness** column of the seam table in `active-designing/docs/glow/nock-interop-seam.md`. **No `.rish` runner built it and no import reached it**, so nothing in the tree ever handed the file to a compiler; its own header line 4 still spelled `rye run nock/nock_glow_mirror_witness.rye`, a path with no `glow/` in it and no file behind it since the move. `tools/fixtures/r/rye_compile_reach_scan.sh` had it in `never` and still answered `asserted=0`, correctly by its own rule: `asserted` is `never` intersected with `named_reading_real`, which is built from `git ls-files -- '*.rish' '*.sh'` and reads **runners alone**. A Markdown page naming a file as its evidence is invisible to it. *What caught it:* `tools/fixtures/r/rye_witness_walker_scan.sh`'s `unreached` leg, on the first lap it had walkers to read. Sixty-three Glow witnesses took the comptime declaration walker in one pass; the census answered `walked=65 unwalked=56 unreached=1` and named the file, because a walker only forces analysis when the compiler is handed the file holding it. *What it taught:* **a claim predicate that reads only executable callers is blind to the pages a reader actually consults**, which is `%498`'s reach-and-claim boundary reaching documentation. The instrument that found it was itself unable to find it four hours earlier, when `walked` stood at 2: **a census's blind spot shrinks as its subject grows**, so the reading that surfaces a defect is often the one taken after unrelated work widened the input. *The measurement:* of **17** never-reached non-specimen tracked `.rye` files, **4** are named by a living tracked Markdown page; two of those four are session-log day shelves and one is a dated construction seed, leaving `mycelium/rehearsal.rye` under `mycelium/README.md`, where the citation describes what the file does rather than claiming it proves something. So the class is real and its live population of proof-claims is now zero. *Repaired:* `tools/n/nock_glow_mirror_witness.rish` builds and runs the witness and asserts four of its printed claims, GREEN on metal; the stale self-path in the witness header is corrected; `rye_compile_reach_scan.sh` and `rye_witness_walker_scan.sh` both read `unreached=0` with the runner staged. *Not taken, and sized:* widening `named_reading` to read living Markdown would re-classify the whole `never` set in one step and needs its own control -- the same reason `%496` held its link density back. **BOOKED** *(the instance is repaired; the predicate gap is a booked lap, not a live defect).*
