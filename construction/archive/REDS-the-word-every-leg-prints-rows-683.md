# REDS shelf -- row %683, the word every leg prints

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- one CLOSED row, landed straight onto its own shelf because the pin has no
room for it, its relative links re-anchored one directory deeper

Written `20260910.020512`, in the lap that recovered the work it records, and the arithmetic says
why it never rested on the pin. `construction/REDS.md` stands at **39,994 bytes** against the
**40,960** its own header declares -- **966** of room, where a row of this length wants half again
that -- and fourteen of its sixteen rows read **OPEN**, so the two that are lawful to move belong
to peers who left them unclaimed. A closed row lands on a shelf directly under those conditions;
that is the ceiling speaking, and it is recorded here rather than felt as friction. Second lap in a
row from this seat to do it, which makes it a pattern rather than an accident.

The row was booked `%673` from a local read on `20260909.211344` and never reached a commit -- the
round open stashed the lap. The anointed spine has since published `%673` for a different row, so
this unshared row derives above the fetched head at `%683`. A published number never moves and an
unshared one does, so the stamp is the identity and the number is a view of it (`derived-spine` 1
and 3).

**REDS %683 (`20260909.211344`) -- a guard's summary leg matched a word every one of its legs prints, so the summary could be deleted whole and the guard still read green.** *What went wrong:* `tools/g/glow_rune_shop_core_witness.rish` watches `looks_like_shop_core`, the predicate deciding whether a `|%` source lowers down the welcome road or the shop's. It runs a Rye selftest and asserts on the captured text, and its summary leg read `assert selftest.out contains "GREEN"`. Every one of the delegate's seventeen leg lines ends in `-- GREEN`, so that assert matched the **first** leg and could not tell a finished run from one abandoned at leg two. *What caught it:* a control fixture written for that guard, at its fifth plant. Three plants moved the module so the selftest exited non-zero, which proves only that the delegate can sound; a fourth reworded one claim line and the runner's own text leg bit at line 37; the fifth renamed the closing summary and **refused nothing**. A leg that cannot fail is a leg that was never there. *What it taught:* **a substring test over an output whose every line carries the same word asserts on the word's presence rather than on the claim's.** The class is named rather than claimed: re-measured `20260910.020512` by `git grep -oh 'contains "GREEN"' -- '*.rish'`, **1,379** sites across **823** of 2,454 tracked `.rish` files, with **36** rostered guards carrying one (the parked lap read 1,375 / 819 / 35 the evening before, so the figure is FREE and rising -- run the grep rather than reading either). A site is vacuous only where the delegate prints GREEN beside its own summary, and that discriminator wants each delegate RUN rather than grepped, so the figure is a population and not a defect count. The same blindness stands written down one file over: `glow_vane_pair_mirrors_witness.rish` records in its own `20260828` header that `contains` is a substring test and the worker's `EXIT:0` tail contains a `0`. Two firings in one family and no reading ever made of it. *Repaired:* the leg reads the summary's own words -- `contains "GREEN: looks_like_shop_core separates"` -- and `tools/fixtures/g/glow_rune_shop_core_control.sh` proves it able to sound: a `git worktree` pen with the three build outputs linked back, the runner, module and selftest copied in **from disk** so a repair is proven in its current form, five plants each lifted and read back, and the pen's runner cut at a `# CONTROL LEG` marker so a pen does not make a pen. **18 checks, 0 failures, 22s on metal.** *And the same shape stood at the file's own top level, found by running the recovered thing:* the seventeen-leg `GREEN` was printed **before** the control leg ran, so a run abandoned during the control's twenty-two seconds left a transcript ending on the announcement, identical to a finished one. The guard closes on a claim naming the control now, which is the form the sibling `tools/g/glow_desk_reach_witness.rish` already keeps. *Recovered `20260910.020512` by `git checkout stash@{1} -- <paths>`, a tree read rather than a blob read, so the control's `100755` survived (`exec-bit`), and re-proven on metal rather than read off the parked log: control 18/0, witness exit 0 at 17.7s, `tame_style_check` GREEN, `shell_dialect` `verdict=ok`, `standing_equipment_redleg` `ceiling_ok=yes`.* **CLOSED.**

## What the record caught that the work could not

`stash_record` gates `unlanded` on the session log rather than on the code, and this recovery is a
measurement of why. The parked lap's own log carries one `obs` line the parked *files* do not: it
left `standing_equipment_redleg`'s ceiling at **53** with the reading at **52**, deliberately,
because lowering with no slack re-arms the fleet-wide refusal `%670` measured the day before, on a
question Keaton has open.

This lap lowered it to 52 before reading that line -- measuring the population both ways on metal
first, 53 with HEAD's witness and 52 with the recovered pair, which is the reading the parked lap
had also taken and then declined to act on. The record refuted the recovering lap, and the ceiling
was put back.

**A record is not a receipt for the work; it is the half of the work that carries the reasons a
diff cannot.** That is `%636`'s thesis stated from the recovering side, and it is why a record
landed is worth a lap of its own.
