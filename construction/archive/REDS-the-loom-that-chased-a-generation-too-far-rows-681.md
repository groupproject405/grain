# REDS %681 -- the loom that chased a generation too far

*Folded from the living pin [`../REDS.md`](../REDS.md) on the lap that booked it. The pin stood at
39,451 of the 40,960 its own header declares, with 1,509 bytes of headroom against a row of roughly
three thousand; a row booked and closed in one lap folds rather than pushing the pin past its bound,
which is the move `%548` made from the same family one shelf over.*

*The clause worth carrying forward. This is the FOURTH firing of the liveness family -- `%387` read
the holder's parent, `%528` added the process group leader, `%548` added the leader's own parent --
and the first in which the error changed direction. Three firings under-reported, calling a dead
lap's pass alive, and each was repaired by adding one more generation to look up. `%548` closed by
stating plainly that the reading `cannot call a live pass gone`. That was true on `20260907.065808`
and false on `20260908.113404`, when a `--detach` flag was seated that severs the link at birth by
design -- leaving no further generation to add, and turning three sound readings into an accusation
against every pass the fleet now launches.*

*What that costs a family of repairs: the shape itself was wrong, rather than incomplete. A reading
built on ancestry cannot survive a launcher built to have none, and no fifth generation would have
helped. The cure is to stop asking who started a pass and start asking whether its verdict can still
be spent -- a question the runner already writes into every detached transcript, and had never read
back.*

---


**REDS %681 (`20260909.234718`) -- the fourth firing of the liveness loom, and the first where it accuses a live lap.** *What went wrong:* `tools/fixtures/s/standing_equipment_run.sh` reads three ancestry questions -- the holder's parent, its process group leader, and the leader's own parent -- and any one answering `gone` makes `lap=gone`, printing *its output reaches nobody* and advising `kill -TERM`. The `--detach` flag seated on the baton `20260908.113404` launches `nohup sh "$0" "$@" &` from a parent that exits at once, so a detached pass is reparented to init **within milliseconds of launch, by construction**, while the lap that launched it sits waiting on the transcript. All three readings then describe the FLAG rather than the lap. *What caught it:* deciding whether to TERM a pass that held my lock. The bounded reading was right about that one -- its `launch_head` `513d283` was no longer HEAD -- so I stopped it, opened my own with `--detach`, and two minutes later the same refusal told me, the live lap waiting on that very transcript, that `parent=gone group_leader=gone lap=gone`, *its output reaches nobody*, and to kill pid 3237632. Across the pier the same moment, and this is the figure that matters: **every one of the 7 trees holding a run lock held it with a pid whose `ppid` is 1 and whose lock carries a `transcript`** -- 7 of 7, each a `--detach` launch, each read as abandoned. The lock is only ever held by a pass and every pass is now launched detached, so the reading is wrong for effectively every holder a lap can meet. *What it taught:* **a reading does not have to change to go wrong -- the world it reads can.** `%387` read the parent; `%528` added the group leader when a detached pass kept a live parent; `%548` added the leader's parent when the harness gave the pass its own session; each asked *is my immediate answer alive* and the fleet kept moving which process that was. This is the fourth, and the direction inverted: three firings under-reported, and `%548` closed by stating *it cannot call a live pass gone* -- true on `20260907.065808`, falsified one day later by a launch form designed to have no waiting ancestor at all. **A loom chasing generations cannot survive a launcher that severs the link at birth**, because there is no further generation to add. *Repaired (`20260909.234718`):* the refusal stops asking ancestry when ancestry cannot answer. The owner writes `$lock/transcript` only when `STANDING_TRANSCRIPT` is set, and one line in the file sets it, so the file's **presence is the launch form** and no new marker is needed: a detached pass reads `lap=detached` and is told the answerable question instead -- its own `launch_head` against HEAD, which named 3653764 void this lap where ancestry called a live pass abandoned. A foreground pass keeps every elder reading unchanged, since its parent genuinely is the lap's shell. And the refusal is asked **once** for both sites: it had been written twice, and only the copy the baton does NOT name had ever learned any of this, so a lap refused by `--detach` was told to read a transcript with no word on whether a reader was left. *Repaired further:* the refusal is asked **once** for both sites, `owner_lap_read` and `owner_lap_detail` beside `owner_transcript`. **CLOSED** by `tools/fixtures/s/standing_equipment_control.sh`, readings **325 -> 345**: the plant is what `--detach` actually makes -- a live process reparented to init whose lock also carries a transcript -- and the same plant runs against a copy with the detection disabled, which must read `lap=gone` and name `kill`, or a repair whose absence changes nothing was never load-bearing. The elder `parent=gone` is asserted to still fire underneath, since a check that had merely stopped reading the process table would pass every other leg. Shown on metal at `20260910.011500`: a real detached pass on this tree reads `parent=gone ... lap=detached` and *its launch_head 4a1d75aaf5 is still HEAD, so it is measuring this tree -- wait*.
