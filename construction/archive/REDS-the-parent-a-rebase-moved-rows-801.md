# REDS -- row %801, folded from the living pin

**Folded:** `20260917.174536` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A body's own parent-hash claim earns a wall, and the wall is forward-only because testimony can never be repaired.*

The row stands here exactly as it was written. A closed row leaves the living pin so the
pin stays the length a reader will actually read; the lesson travels forward in the guard
the round built, and the row itself stays one click away.

---

**REDS %801 (`20260917.001128`) -- a commit body named a hash that resolves and is not the object the sentence claims.** *What went wrong:* this ship's follow-up commit `ae3fccd5a` says *the `Git nib` constant on `construction/ITINERARY.md` is rewritten to `71a556d85c` -- this commit's own parent*. Card and parent both read `397037a129`. The hash went into the body BEFORE the send's final rebase; the rebase replayed the commit onto a peer's work, the card was repointed and the commit amended `--no-edit`, so the card moved and the body did not. *What caught it:* a read-back of `git log -1 --format=%B` against the card and `git rev-parse --short=10 HEAD~1`, seconds after the push, because the amend had been made twice and the second kept the first's message. *What it taught:* **the commit-message wall reads paths and never objects, and an object check would have welcomed this one anyway.** The hook refuses a body naming a path the tree lacks (`%202`); `71a556d85c` is a live commit here -- the pre-rebase work commit -- so it resolves, and only the RELATION the sentence asserts is false. A live reference pointing one commit sideways is a different genre from a fabricated one. [`remember-git-nib`](../../.claude/rules/remember-git-nib.md) step 2 already orders the amend AFTER the final rebase, and it says that about the CARD; the same ordering owes the BODY, which no rule and no guard says. *The cure a lap can take today:* leave the parent hash out of the body and let the card carry it, since the card is repointed by the step that knows. *The send met the derived spine's founding case twice:* `%800` was booked and a peer had bound it half an hour earlier, so this unshared row renumbered -- and the same rebase dropped a duplicate fold of `%798`, already shelved by that peer. **OPEN** -- the body is pushed and stands as testimony (never forced), the card and `remember_git_nib` are GREEN, and whether a body's own hash claims earn a wall is a fleet question rather than this lane's. *Answered `20260917.162810` by measurement rather than by a sentence:* the class is **ten** in the trailing 4,000 commits, one of them landed twelve hours AFTER this row was booked, and six name a hash no object store on this pier holds. [`tools/c/commit_parent_claim_witness.rish`](../../tools/c/commit_parent_claim_witness.rish) walls the three claim genres at zero **forward of a named anchor**, since testimony can never be repaired and a wall over it would red on every ship for a fault no lap may take. GREEN on metal, 43 pen legs, six mutations bitten. **CLOSED**
