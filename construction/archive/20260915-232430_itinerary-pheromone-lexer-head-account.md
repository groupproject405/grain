# ITINERARY -- Pheromone's lexer-head account, shelved `20260915.232430`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is open
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** checkable -- every reading names the guard that binds it

Written onto the living card and shelved in the same send. The card stood **40,689 bytes** against
the **40,960** it declares, so it held **271 bytes** of room and any account at all breached it.
Two accounts folded on this lap rather than one, and the card carries a pointer line and the open
question for each.

**PHEROMONE -- FOUR BINDINGS READ ONE TABLE INSIDE THE LEXER, AND THE LEXER HAS TWO HEADS OUTSIDE IT.**
**EARTH BREATHES IN** (row 4, N=5014): the concrete fact at the door before any argument about it.
**THE FACT.** `glow/tokens.rye` `lex_one` matches two rune-shaped two-byte heads on branches BEFORE
it calls `match_rune2`: `::`, routed to `skip_comment_line`, and `==`, pushed as the
`.double_equals` token kind. All four bindings `glow_rune_alphabet` holds -- the closed
pronunciation roll, the three G1 briefs, the TAME family index, and the Book -- derive their head
roster from the `const pairs` constant declared inside `match_rune2`, so every one of them read 30
heads where the lexer accepts 32. Unreachable by construction rather than by oversight: no plant
inside that table can touch the other two, and no reading derived from it can count them.
**WHAT THE HOLE COST.** `==` is consumed by `glow/rune_shape.rye` at two call sites as the
tall-form terminator and stands in **105 tracked `.glow` sources**, and it is named in **none** of
the three documents the guard binds -- while `--`, which closes the same kind of form one line
below it in the same lexer, is named in all three. One head of a matched pair taught and the other
silent is the exact shape the fourth binding was seated to catch, twenty days after `|+` walked
past the three before it.
**LANDED.** The worker walks `lex_one` itself and publishes `outside_heads`, `outside_roll_named`,
`outside_book_named`, `outside_unnamed_glyphs` -- reading **2, 0, 1** and `==`, the one head named
in neither. **41 legs** in the pen, up from 32, every refusal planted and then lifted.
**THE SPAN IS THE FUNCTION AND THE TWO READINGS AGREE TODAY BY ACCIDENT**, which is said in the
worker rather than hidden: `starts_with` is a general predicate, `skip_comment_line` calls it in an
assert, and `sort -u` folds that repeat. So the bound is proven by a mutation rather than asserted
-- a `starts_with` literal planted OUTSIDE `lex_one`, which this reading walks past and a
whole-file reading refuses.
**EXEMPT BY NAME, REPORTED RATHER THAN GATED**, on the cause the two exemptions beside it already
stand on: whether a terminator and a comment head are runes wanting a pronunciation row is a
language custody ruling and a rune is earned by a law. A third outside head reds wherever it
arrives.
**AND THE FRAME THE LAST LAP OWED CLOSED ON A NUMBER.** `tools/fixtures/g/glow_gate_law_agree_scan.sh`
was left graded **C+/79** at Door register with a molt owed; re-read at molt time it stands **B/83**,
since that lap's own pedestal prose raised it. B stands, so the frame closes with a measurement
rather than a rewrite -- which is what the quality-assurance law asks for.

**YOURS:** `==` and `::` -- does either earn a pronunciation row and a Book entry, or do they stay
lexical punctuation the reference teaches by use? One word empties the exemption list and tightens
the gate to zero by deletion, exactly as `?&` and `?|` wait to do one binding over.
