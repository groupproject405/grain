# REDS -- row %586, folded from the living pin

**Folded:** `20260907.164850` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A ratchet's control plants a little damage into its own pen so the counting readings have something
to count. That works at every ceiling but one. At zero, the pen's own plants are the thing being
measured, and a control built to prove a meter starts reporting a clean tree as dirty -- so the lap
that finally finishes a language is the lap that breaks the instrument proving it.*

The row stands here exactly as it was written. A closed row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the instrument the round
repaired, and the row itself stays one click away.

---


**REDS %586 (`20260907.164850`) -- a pen that plants into itself cannot ask a ceiling of zero.** *What went wrong:* `tools/fixtures/g/glow_comment_ascii_control.sh` plants three non-ASCII characters into its pen to give the counting readings a subject, then asks that same pen whether it sits under the LIVING tree's ceiling. Two jobs on one reading, and they part company at **zero**. *What caught it:* the control, run straight after `CEILING` fell **942 -> 0** for the Glow sweep -- `clean_pen_under_ceiling=no` for a tree that was perfectly clean, predicted from reading the pen and then confirmed on metal rather than trusted either way. *What it taught:* **the first sweep that FINISHES a language is the one that breaks its control**, because a ratchet's pen and the tree's ceiling are the same number only while the ratchet is above zero. *Repaired (`20260907.164850`):* the counted plants are rewritten to ASCII before the ceiling is asked, so the ceiling legs are proven from both sides against a pen holding no non-ASCII at all -- the only pen whose reading is honest at every ceiling, zero included. Pen **14 -> 16**, adding `cleared_pen_reads_zero`. *Named rather than taken:* the Rye and shell meters still carry residue and will meet this on the lap that finishes them; the repair is written into their sibling's head so the next hand meets it before the wall does. **CLOSED.**
