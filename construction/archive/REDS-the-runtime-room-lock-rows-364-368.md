# REDS fold -- the runtime room lock

Rows %364-%368 moved here on `20260830.043900`. Together they carry the Caravan suite's fixed-runtime collision, its bounded lock repair, and the four operator mistakes caught while proving and signing that seam. The machine distinction is the point: shared paths were a tree defect; nested quoting, interpreter choice, a live yielded roster, and commit vocabulary were bounded hand errors caught before the send.

**REDS %364 (`20260829.215517`) -- the Caravan choir shares runtime stores across child witnesses, so suite order can red a child that passes alone.** *What went wrong:* `tools/ca/caravan_suite_witness.rish` runs 121 registered child witnesses in one process order while those children use fixed paths under `caravan/` for runtime notes and binaries. Two accidentally concurrent rosters failed on different children, and a later single hot roster failed at `caravan_taper_witness.rish` after one isolated whole-suite GREEN. *What caught it:* the seam read from both sides on the Linux pier -- `dwell`, `abandon`, and `taper` each passed alone; one isolated suite passed all 121; cold and hot roster suites failed at shifting children. *What it taught:* **a choir that proves every singer must also give each singer a fresh room** -- serial process order alone does not isolate fixed runtime paths, and an isolated child GREEN cannot settle the aggregate seam. *Repaired (`20260830.000637`):* the suite takes one bounded runtime-room lock before clearing or building any fixed path, writes the live Rishi pid so a stopped holder can be reaped, and releases after all 121 children pass. Two suites started two seconds apart both ran all 121 witnesses GREEN in turn; the second crossed only after the first released. **CLOSED.**

**REDS %365 (`20260829.234423`) -- I edited the tree while a cold roster still ran after its command yielded.** *What went wrong:* the full `standing_equipment_run.sh` call returned its captured output at 30 seconds while its shell and Caravan choir remained alive, and I treated that tool return as process completion. I then rebased and edited under the still-running measurement, so its opening digest could no longer testify about one still tree. *What caught it:* the process table during the two-suite probe showed the elder Rishi process and its standing-equipment parent still alive, nine minutes after the command had returned. *What it taught:* **a yielded command is a live process until its session or process table says otherwise** -- before a file moves after any long witness, read the returned session identity or confirm the process is gone; a digest can detect movement only after the invalid overlap has already happened. *Repaired (`20260829.234423`):* both the invalid cold roster and the dependent two-suite probe were stopped, their empty lock was removed, and the lap restarts its cold witness from a still tree. **CLOSED at booking.**

**REDS %366 (`20260830.000124`) -- I escaped shell quotes inside a Rishi command string, so both lock probes waited on a path carrying literal quote marks.** *What went wrong:* the new suite lock used `\"$lock\"` inside the Rishi string. Rishi passed the backslashes through to `sh -c`, making the quotes data rather than syntax; both suites looped for the full bound while the intended lock directory did not exist. *What caught it:* the process table printed each live `sh -c` command with the backslashes intact, while a scoped filesystem reading showed no lock directory and both output files stayed at their three opening lines. *What it taught:* **inspect the argv a nested interpreter receives** -- one language's string escape may become another language's literal character, and a timeout proves only waiting until the waited-on name is checked. *Repaired (`20260830.000124`):* the controlled paths contain no spaces, so the shell command uses plain expansions and the next probe must show the intended directory plus its parent pid before the full choir claim. **CLOSED at booking.**

**REDS %367 (`20260830.001702`) -- I ran a Rishi width witness through `sh`.** *What went wrong:* the follow-up command spelled `sh tools/w/width-check.rish`, so the POSIX shell parsed Rishi's `let`, `say`, and `assert` forms as shell commands and emitted a screen of syntax errors. *What caught it:* the shell refused on line 28 before producing any width verdict. *What it taught:* **the extension names the interpreter** -- `.rish` runs through `rishi/bin/rishi run`; `.sh` runs through `sh`; a familiar witness basename does not erase its language. *Repaired (`20260830.001702`):* the failed invocation changed no tree state and the witness is rerun under Rishi before any GREEN claim. **CLOSED at booking.**

**REDS %368 (`20260830.015241`) -- my first commit body described the lock yet named none of the wall's mechanism words.** *What went wrong:* the body said the suite "takes a bounded lock" and "records the live Rishi pid," yet it never said file, function, parameter, command, call, module, or another counted mechanism word. The prose was reconstructable to me and invisible to the wall's declared meter. *What caught it:* `tools/hooks/commit-msg` refused before signing and left the message intact. *What it taught:* **open a commit body with the changed file and operation in ordinary engineering words** -- a clear idea still needs the vocabulary the executable contract measures. *Repaired (`20260830.015241`):* the retry begins with `tools/ca/caravan_suite_witness.rish` and names its shell command before the wider meaning; the same six-path limit stays on the retry. **CLOSED at booking.**
