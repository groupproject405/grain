# REDS shelf -- one number, two rows: the %364 collision and its census

**Language:** EN
**Status:** Fold shelf -- immutable once written; rows moved from the living pin
**Voice:** Kyri

The pair this shelf holds is one story told from both ends: the detached-head row that
booked `%364` honestly and lost a race it could not see, and the census row that measured
the double booking, seated the derive scan's fourth reading, and was itself renumbered
twice while closing -- the second time because a fresh `%370` landed upstream during the
very repair, proving the new reading's worth on its first day.


**REDS %364 (`20260830.161637`) -- the lap opened on a detached head one commit behind `main`, so the card's named next door was a door already walked, and the same pedestal was built twice.** *What went wrong:* this bench's checkout sat detached at `2844d1d485` while `main` carried `b9a90e7ae`, the `20260830.080427` lap's `max_name` pedestal -- desk, three readings, control, session log and card edits -- whose `git rebase` onto `e59f5ebcc` was interrupted at 10:51 and left a `.git/rebase-merge` directory behind. The next lap opened at 11:28 on the detached head, where `src/shape/shape-tablecloth-name-bound.glow` does not exist and `construction/ITINERARY.md` still names `max_name` as this seat's next door. The card was read correctly and the door was genuinely open in the only tree the lap could see, so a second desk, three readings, and fourteen control cases were built beside work already standing one commit away. *What caught it:* the send. `git rebase xy/main` refused with *there is already a rebase-merge directory*, and that directory's `done` file named the commit it had been replaying -- `shape: a length, and the alphabet beside it`. Nothing earlier in the lap could have: the cold roster reads the tree's CONTENTS, and every reading it takes was honest. *What it taught:* **a lap's first question is which commit it stands on, and a detached head answers it wrongly in silence.** REDS %321 taught that a dead lap leaves no dirty index because its leavings are stashed; this is that lesson one layer over -- a dead lap can leave a clean index AND an interrupted rebase, with the checkout detached behind `main`, and both are invisible to every guard that reads files, because neither is a file. The duplicate was not waste in the end, which is the honest second half: each build found something the other missed. *Repaired (`20260830.161637`):* the two are reconciled rather than one discarded. The earlier commit is the base, since it carries a correction the rebuild lacked -- the witness pins the nine `ClothError` names in its own asserts, so it DOES carry a roster of its own, and an honest tenth path would red a file whose header promised otherwise. The rebuild's two findings ride on top: every bound desk spells its number twice, in the `shape` line and under `example`, and only the second was ever read (three desks, three readings, three plants); and `src/shape/README.md` had drifted from its own room -- six residents with no roster row at all, and two rows whose numbers their desks had moved past, `errors = 10` against a desk reading 13 and `fields = 4` against a desk reading 5. Both repaired, and the roster now agrees with all 35 residents. The abandoned `rebase-merge` was read before it was aborted; `orig-head` proved nothing was orphaned. `tools/t/tablecloth_glow_tend_witness.rish` GREEN on metal over 44 pen cases -- 6 welcomes, 38 refusals, 0 wrong. *Remainder:* a reading that refuses to open a lap on a detached head behind `main`, or over a `.git/rebase-merge` directory, belongs on the standing runner's first line beside `staged_uncommitted`. Named here as its own lap. **BOOKED.**

**REDS %371 (`20260830.170230`) -- one ledger number carries two rows, and both stand on the anointed spine.** *What went wrong:* `%364` is bound to two different stamps -- `20260829.215517`, the Caravan choir's shared-runtime row, folded to [`archive/REDS-the-runtime-room-lock-rows-365-369.md`](REDS-the-runtime-room-lock-rows-365-369.md) (the shelf's name after this row's own repair) on `20260830.043900`, and `20260830.161637`, the detached-head row standing on this pin. Both are published on `xy/main`, so by the derived spine's own third line -- a published number never moves -- neither can be renumbered without a word. Measured across every file `tools/fixtures/r/reds_spine_files.sh` names: **275 distinct (number, stamp) pairs for 274 distinct numbers**, so this is the only instance in the whole spine. It is the exact shape `.claude/rules/derived-spine.md` was written to prevent, and it happened anyway: the `20260830.161637` lap read `next_free=364` from the fetched anointed spine and booked correctly, while the peer's fold stood local and unlanded. That lap's commit reached `xy` at `16:26:46` and the peer's at `16:40:35`, fourteen minutes later. Fetch-before-book is honest and still loses a race it cannot see, because the allocator can only answer for what has landed. *What caught it:* a rebase. `xy/main` had moved two commits, and reading what the peer's rows renumbered TO -- `%364`-`%368` -- against the number this seat's own desk, log, and card already cite. **Neither standing guard can see it**, which is the sharper half. `tools/gen/chapter/reds_ledger_monotone_witness.rish` holds `1..N` with no gaps, and a duplicate makes no gap -- it read GREEN at `measured=368` with the pair standing. `tools/fixtures/r/reds_spine_derive_scan.sh` compares this tree's `(number, stamp)` bindings against the anointed spine's and read `rebindings=0`, because both copies are upstream, so nothing was *re*bound here. Each guard is correct on its own question, and the pair leaves one shape uncovered. *What it taught:* **a spine guarded for gaps and for divergence is not yet guarded for a number used twice.** The derived-spine law names the key (the stamp), names the allocator (the anointed remote), and proves renumbering confined to unshared rows -- and nowhere asks whether one number carries one stamp, which is the single property all 2,519 `%N` citations in this tree rest on. The missing reading is one line beside those already taken: distinct pairs counted against distinct numbers, held equal, which is how this row was measured before it was written. *Repair, named and not taken:* by the allocator's own rule the earlier-landing row holds, so the Caravan fold's `%364`-`%368` would derive above the spine's head; those are another lane's published rows, so which side moves is Keaton's word rather than an agent's. *Repaired (`20260830.184416`, the word granted):* the later landers moved gaplessly -- the carried fold shifted one seat to `%365`-`%369` with shelf, recital, logs, and index rows swept together, and this row itself, the latest lander of all, took `%371` -- moved twice, because a second `%370` landed upstream during this very repair, proving the fourth reading's worth on its first day. The missing reading is seated: the derive scan counts distinct (number, stamp) pairs against distinct numbers as its fourth gated reading -- and seating it found the deeper fault, the scan's own `sort -n -u` spine build, which compared lines by their leading number alone and silently dropped the second stamp of any doubled pair, so the guard written for this fault was blind to it until the sort went bytewise. Proven both ways on this very collision: the scan named `double_booked %364` before the shift and answers clean after. **CLOSED.**
