# REDS -- the page moved and the claim did not (rows %264, %265)

**Language:** EN
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded:** `20260826.115303` from [`../REDS.md`](../REDS.md), both **CLOSED**.

Two rows from one round, and both are a claim left standing after the page beneath it moved.
%264 is a generator refreshing a headline's three numbers and leaving the date saying when they
were counted -- a figure carries a unit, a date, and a source, and nobody reads the one nobody
types. %265 is a witness asserting LEXICON's five bounded work units still end in `season` after
the panchanga replaced that word with `chapter`; the molt swept living prose correctly and could
not see an assertion in another language holding the elder string. A guard or a generator quoting
a living page owns the repair on the lap that page moves.

**REDS %264 (`20260826.093035`) -- the headline refreshed two of the three things a figure carries.** *What went wrong:* `tools/fixtures/reds_ledger_headline_write.sh` rewrites this ledger's headline from the measured spine, and the headline's own sentence reads *counted from the ledger and its archives on `<stamp>` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`*. The writer rewrote the total, the remainder, and the span, and left the stamp exactly where a hand last typed it. This round moved the numbers 259 -> 263 and the sentence still claimed they had been counted at `20260826.070513`, two hours and four rows earlier. *What caught it:* reading the diff of the writer's own output rather than its verdict -- `changed=yes` and `verdict=ok` are both true of a rewrite that leaves a date behind. *What it taught:* **a figure carries a unit, a date, and a source, and a generator that refreshes the number owns the date beside it.** Gauge asks for all three at once, and a generated claim is exactly where the three can drift apart in silence, because nobody types it and so nobody reads it. The near-cost is a reader trusting a two-hour-old provenance on a number measured this minute -- small here, and the same shape as any stale claim wearing a fresh figure's clothes. *Repaired:* one more `-e` on the writer's `sed`, reading the one clock the way every other writer in that room reads it, so the date moves on exactly the path the numbers move. Proven on metal in `tools/fixtures/reds_ledger_headline_control.sh` as an eighth behavior, gated in the control's own verdict and shown from both sides: the living writer leaves today's stamp and the elder writer leaves the plant's `20260101.000000` and reds the control. CLOSED.

**REDS %265 (`20260826.114500`) -- a molt moved a seated word and stranded the guard quoting it.** *What went wrong:* `tools/gen/chapter/equinox_e50_geode_expedition_witness.rish` asserts that `context/LEXICON.md`'s **geode** row still names the five bounded work units in order. The panchanga seated on `20260826` replaced the outermost sense, `season`, with `chapter`, and rewrote that row. The witness kept asserting the elder word and refused, taking `season_leaf_choir` red with it -- 32 of 33 leaves green, one refusal, `choir=failed`. *What caught it:* the **cadence tier**, on the first lap that closed a rota cycle after the molt. `season_leaf_choir` runs on the fifth round rather than every lap, so the red waited exactly as long as its clock says it may -- which is the tier working, rather than the tier failing. *What it taught:* **a guard that quotes a living page owns the repair on the lap that page moves.** This is `docs-implementation-sync` stated as a cost rather than a rule: the molt swept living prose correctly and could not see an assertion in another language holding the elder string. It is the second time this one line has broken on a sweep -- REDS %236 was the ascii-first pass turning its middots into hyphens -- and a lantern that fires twice is worth naming: this assertion pins prose that living law rewrites, so it will move again, and the comment beside it now says so. *Repaired:* the assertion reads `chapter`, the comment records both sweeps and why the line follows the tree rather than the day it was typed, and a tree-wide sweep for the elder phrase in living code found no other site -- dated testimony keeps every word it wrote. `e50` GREEN, `season_leaf_choir` GREEN. CLOSED.

