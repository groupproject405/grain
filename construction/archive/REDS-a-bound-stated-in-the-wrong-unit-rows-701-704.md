# REDS -- rows %701 and %704, folded from the living pin

**Folded:** `20260911.004500` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Two rows booked within four hours of each other, and both are one shape seen twice -- a bound
stated in a unit that does not bound the thing. One was a cadence promised to a round nobody
counts, so a quarter of the roster stood rostered and unheard. The other was an answer bounded by
a substring, so a guard proving the identity is 1 passed on a desk answering 105.*

Both rows read **BOOKED** when they folded: their instances stand repaired and what remains is a
ratchet. They left the living pin on the lap that seated the second, because the pin stood at its
40,960-byte ceiling to the byte and a BOOKED row is the only kind the law lets move. That is the
fold working as written, and it is also the reason the ceiling itself is on the card as a question
for Keaton -- a pin that can seat a new row only by shelving one is a pin at the end of its rope.

---

**REDS %701 (`20260910.233112`) -- the slower clock had never turned, so a quarter of the roster was rostered and unheard.** *What went wrong:* `tier cadence` was written so a costly guard could answer on a slower clock rather than stand off the roster (the fault `%219` booked), and the runner's own header named that clock **"the fifth round"**. Nothing in this tree counts rounds, so no pass was ever the fifth: measured `20260910.221225`, `cadence_never_run_here` read **74 of 74** -- every guard on the slower clock had never run on this pier even once. The vocabulary forbade an exemption and the mechanism was one, which is why no reading looked wrong: `--all` and `--tier cadence` both work, and neither is on any lap's path. *What caught it:* the scan's own `cadence_never_run_here`, which had stood at 58 when `%568` booked it and at 74 here -- a number that had been printed on every cold pass for days and read as a backlog rather than as a clock that had never started. Singing six of the 74 by hand took **582 seconds** and turned up **five green and one red**: `dated_path` reads **107 lost against a ceiling of 85**, and **100 of the 107 stand in dated testimony** no lap may edit -- a ceiling its own lane cannot reach, `%626`'s class, unheard since the tier was seated. *What it taught:* **a cadence needs a mechanism, never a round number** -- the run card already records when each guard last answered, so the guards that have waited longest ARE the ones whose turn it is, and a never-run guard sorts first because it has waited longest of all. `--cadence-slice N` rides an ordinary lap pass and takes N turns off that card; running them writes the stamps that put a different set at the front next lap, so the clock is turned by the thing it measures and there is nothing to remember. **BOOKED** -- the rotation stands proven (23 legs, two mutations bitten); whether the fleet's default rises above zero is Keaton's word, since a cadence red costs every ship its receipt.

**REDS %704 (`20260911.003518`) -- a guard proving the product identity is 1 passes on a desk answering 105.** *What went wrong:* `tools/t/tally_a2_list_reducer_witness.rish` reads its desk's answer with `assert p2.out contains "1"`, a SUBSTRING read, so every wider number carrying that digit satisfies it -- and the same file's own sibling leg expects **105**. A prod desk ignoring its samples and answering 105 for everything passes both legs. Two more collisions are demonstrable inside a single file rather than argued: needle `1` inside sibling needle `15` in that same witness, and needle `6` inside sibling needle `36` in `tools/t/tally_glow_tend_a2_gate_fold_witness.rish`. **58 legs tree-wide, 21 of them mantra and tally.** *What caught it:* a fire rota lap reading the lane's own guards for what they cannot refuse. The sibling family was gated the day before -- `%700`'s neighbour, a needle the worker's own `EXIT:0` trailer supplies -- and that scan **could not see this half by construction**, since it counts only needles the trailer can supply and `1` is not one of them. *What it taught:* **a bound stated as a substring bounds nothing**, the third firing of that shape in this room (`%500` presence-rather-than-a-count, `%678` hits-rather-than-bytes), so the lantern became a loom. *Standing:* `tools/fixtures/s/silent_leg_scan.sh` grows a second reading on the same corpus -- `wide`, gated at **zero** across mantra and tally and ratcheting at **37** elsewhere -- and the two families are **disjoint by construction**, so one loose leg pays exactly once. All 21 lane legs carry `(lines x.out) contains "N"` now and all nine witnesses ran GREEN. Control **43 legs, 0 failing**, three mutations each biting their own leg; claim three is proven through this tree's own Rishi over the stream a desk answering 105 actually produces. **BOOKED.**
