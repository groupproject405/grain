# REDS shelf -- rows %353, %354, %355

**Language:** EN
**Folded:** `20260830.100059`, when the pin read 28,257 bytes against its declared 24,576
**Voice:** Kyri
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)

Three closed rows from one morning, each about a spelling that meant one thing to a reader and
another to the machine. %353 found a literal `/bin/bash` in a witness family on a pier whose
`/bin` is empty, so two rostered guards refused before reading anything -- the repair belongs to
the family rather than to the file that was blamed. %354 found a bound on a name's LENGTH doing
silent duty as a bound on its ALPHABET, so a name carrying a space stored cleanly and rendered a
manifest the module's own parser refused. %355 found %353's own repair incomplete nineteen
minutes later: two more absolute interpreters stood nested inside `bash -c` strings, where a
sweep reading command position cannot see them, and the refusing-side proof beside them had been
passing vacuously on a shell error rather than on the defect it names.

---


**REDS %353 (`20260830.021542`) -- a fixed interpreter path in a witness family reddened two guards on the pier whose `/bin` is empty.** *What went wrong:* `tools/p/pin_bound_touch_witness.rish`, `tools/w/waymark_rung_drift_witness.rish`, and the two controls beneath them invoked their scans as a literal `/bin/bash` -- twelve live sites across four files. This NixOS pier holds an empty `/bin`, with bash at `/run/current-system/sw/bin/bash`, so both guards refused with `CommandFailed` before reading a single pin or label: two rostered readings measuring nothing on the bench that had just been handed them. The tree's own convention was already overwhelming and already right -- **8,425** `run ["sh"` sites and **30** `run ["bash"` against **14** absolute ones, and the third file carrying those, `tools/s/skate_native_core_witness.rish`, is `host macos`-gated, which is exactly why the shape survived unseen. *What caught it:* the roster re-verify after a rebase -- both guards read green before the fast-forward and red after, so the arrival was legible in one reading rather than a hunt; a peer's card had caught the sibling half in the same hour and named it there. *What it taught:* **a lesson applied to one file of a family is a lesson the family has not learned.** REDS %348, closed nineteen hours earlier, taught that an interpreter is an environment fact and made `tools/hooks/pre-commit` probe for one; `tools/fixtures/p/pin_bound_touch_scan.sh` learned it too, and probes four candidates while validating each. Its own control, its own witness, and its sibling family did not -- so the next lap wrote the fault back in beside the repair, one file over. The repair belongs to the family, never to the file that was blamed. *Repaired (`20260830.021542`):* twelve sites take the PATH-resolved spelling the tree already uses, while the scan's probe list keeps its absolute first candidate, since a probe IS the repair rather than the fault; both witnesses GREEN on metal here. **The loom is named rather than built:** `tools/fixtures/s/shell_dialect_scan.sh` gained two families this same round and is the right home for a seventh reading over an absolute interpreter inside a Rishi `run` call -- narrow enough that it cannot swallow a probe list. It waits for its own lap rather than shipping half-proven at the tail of this one. **CLOSED.**

**REDS %354 (`20260830.030609`) -- a bound on a name's LENGTH was read as a bound on its ALPHABET, and the manifest round-trip the module promised was false for every name carrying a space.** *What went wrong:* `pond/apps/tablecloth.rye`'s `store_artifact` checked a name's length only -- `NameEmpty` under one byte, `NameTooLong` over `max_name` -- and never its bytes, while `render_manifest` writes that same name into `artifact <name> <digest> <len>\n` and `parse_manifest` reads it back with `splitScalar(u8, text, '\n')` then `tokenizeScalar(u8, line, ' ')`. So a name the store lawfully accepted rendered a manifest this module's own parser refused: `store_artifact(cat, store, "my notes", doc)` succeeds, renders `artifact my notes 21341ca6...fc0dd6 77` -- five tokens where the grammar allows four -- and `parse_manifest` returns `BadManifest` at the no-slack-past-the-fields check; a newline name refuses one clause earlier, at the missing address token. The module's own header claimed *Render and parse round-trip it name for name and address for address*, and that claim was false for every such name. The path reaches the signed surface: `tablecloth_keyed.rye`'s `store_owned` verifies a deed and a keeper signature and then hands the caller's name straight through. *What caught it:* **the air rota lap's own fence walk** -- pressing `max_name` before building its pedestal, with Single-Stranded's sentence about the seam where a value is flattened to text and parsed back naming the shape one row of the grid earlier. *What it taught:* **a bound on a value's length is not a bound on its alphabet, and where a module flattens a value to text and parses it back, the alphabet is the invariant the grammar actually needs.** `max_name = 48` was silently doing a second job nobody had written down: the shortest valid `artifact` line is 77 bytes, so at 48 no name could carry a forged one, and manifest injection was out of reach by arithmetic rather than by a wall. Raise the bound for longer human names -- a change whose said-why offers no warning -- and the forgery becomes reachable. *Repaired (`20260830.030609`):* `name_is_one_field` reads the two bytes the grammar reserves and `store_artifact` refuses them by name (`NameHasSeparator`) at the edge a name enters; `max_name`'s comment now says it bounds length alone. Proven on metal both ways -- `tools/b/buhr_tablecloth_witness.rish` GREEN over two store refusals and the parser-side proof of the line they prevent, and a throwaway pen with the wall removed showing the space name stored, rendered, and refused by the module's own parser. *Remainder closed (`20260830.055018`):* `pond/apps/spool_cloth.rye` takes the same wall in the same shape -- `name_is_one_field` reads the two bytes the `large <name> <hex> <len> <resins>` grammar reserves, `store_large` refuses them by name (`NameHasSeparator`) at the edge a name enters, and `max_name`'s comment now says it bounds length alone. The signed surface is covered with it: `pond/apps/spool_keyed.rye`'s `store_owned_large` verifies a deed and a keeper signature and then hands the caller's name straight to `store_large`, so a verified owner buys a catalog seat rather than a pass through the wall, and its doc comment says so now. Proven on metal both ways -- `tools/g/gism_spool_cloth_witness.rish` GREEN over two store refusals and the parser-side proof of the six-token line they prevent, and a throwaway worktree with the wall removed printing `the store accepted "my notes" with no wall` and then `this module's own parser refused the manifest it just rendered: error.BadManifest`. **CLOSED.**

**REDS %355 (`20260830.031001`) -- the interpreter repair swept command position and left two spellings nested inside a string in the file it repaired.** *What went wrong:* REDS %353, closed nineteen minutes earlier, moved twelve absolute `/bin/bash` invocations in the rung-mark guard family to the PATH-resolved spelling and claimed both witnesses GREEN on metal. Two more stood in `tools/w/waymark_rung_drift_witness.rish` itself, lines 48 and 52, **inside** the `bash -c` strings its pen legs hand to `run`, where a reading that looks for an interpreter in command position cannot see them. This pier's `/bin` holds `sh` alone, so the elder spelling exited **127** with `No such file or directory` -- which satisfied `assert (bitten.ok == false)` for a reason having nothing to do with rung drift. The refusing-side proof was passing **vacuously**, the one failure such a proof exists to prevent. *What caught it:* the roster's cold open one lap later, on the assert BESIDE the vacuous one -- `bitten.out contains "verdict=rung_drift"` read a shell's error message where a verdict belonged. A leg checking only that the scan failed would have read green while proving nothing. *What it taught:* **a refusal must refuse, and it must refuse for its own reason** -- the companion to the sentence this tree already keeps, since a check that a command failed cannot tell a caught defect from a missing interpreter. And the mechanical half: **a sweep sees the shape it greps for, so a shape one level down survives the sweep that names it.** %353's repair was right everywhere it looked, and its GREEN claim was true of the run that made it. *Repaired (`20260830.031001`):* both sites take the PATH-resolved `bash`, the said-why above the pen block names the vacuous pass, and the witness answers GREEN on metal here -- 588 labels mapped, both boundary sides proven, `rung_marks_living=17247` at its ceiling. Measured this lap: **four** absolute-interpreter sites remain inside `run` strings tree-wide, all in `host macos`-gated `tools/s/skate_native_core_witness.rish` where `/bin/bash` stands, so the population outside that gate is zero. The loom %353 named now has its true width -- it must read **nested strings**, or it would pass this row's own defect. **CLOSED.**
