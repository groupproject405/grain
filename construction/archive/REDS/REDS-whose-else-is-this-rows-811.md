# REDS -- whose else is this

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its reading proven by a control on real git repositories
**Folded:** `20260917.055209` from [`../REDS.md`](../../REDS.md)

One row, folded the hour it was written to hold the pin under a bound eight ships share -- the
second time that sentence has been written on a shelf in this room, and the pin stood at **exactly**
its 65,536 bytes when this row arrived, so the fold was the only lawful way to land it at all.

It teaches that a red priced in the branch that revealed it is a red scoped from one reading. A hot
roster pass found a dangling `else` by its refusal, so the row asked who else takes the refusing
branch and answered *almost nobody* -- true, and not the question. The other branch loses the
author's message in silence, on every pier, leaving nothing for any pass to read.

**REDS %811 (`20260917.035352`) -- a dangling `else` was booked as a refusal one pier sees, and it is a message every pier loses in silence.** *What went wrong:* `%806` read `if COND then assert X else "MSG"` right and priced it one way -- *fires only on a pier in the unlucky state.* Run in a pen, the TRUE branch drops the author's sentence and reports the bare condition, on every pier, leaving no refusal to read. *What caught it:* a pen script written to reproduce the refusal, run with its condition true by accident. *What it taught:* **a red priced in the branch that revealed it is scoped from one reading.** Meter and full account: `tools/r/rish_dangling_else_witness.rish`. **BOOKED** -- 6 sites left; the doors stay with their lanes.
