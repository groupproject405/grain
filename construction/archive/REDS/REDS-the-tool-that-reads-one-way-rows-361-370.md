# REDS shelf -- the tool that reads one way, rows %361-%363 and %370

**Folded:** `20260830.212616` -- four CLOSED rows off the living pin when the day's bookings
carried it past its declared byte bound.

What they taught together: each row is a tool reading its input differently than its author
pictured -- a delegate filter keying a body before the body was whole (%361), Git reading
options placed after the pathspec separator as pathspecs (%362), the mechanism wall counting
words a body did not carry (%363), and the report card reading a 0-100 service figure where
hands wrote 0-4 (%370). The instrument's reading is the reading; the author's picture is not.

Rows fold here in ascending order, links re-anchored one directory deeper by the fold tool.

---

**REDS %361 (`20260830.145851`) -- the first delegate filter keyed a body before the body was whole, so every delegate stayed actionable.** *What went wrong:* `caravan_ladder_carry_scan.sh` marked `delegates[body]` while its awk loop was still appending lines to `body`; the associative key held a partial string, and the completed body never found it. The first living scan therefore printed all 699 copied families as actionable and left `mend_the_plan` at the head, exactly the stale answer the change meant to retire. *What caught it:* the witness's passing-side assertion that an already lifted delegate must leave `CARRY_TOP`; the scan itself stayed green because the total remained honest. *What it taught:* **state collected over a record belongs to the completed record, not to its changing key.** Keep a scalar while parsing, then attach it at the closing boundary. *Repaired (`20260830.145851`):* the parser now resets `is_delegate` at the function head, sets it while reading, and assigns the marker only when the closing brace fixes the full body text. The living queue reads 576 actionable families, and a planted pair proves both sides: an ordinary copy stays in the queue while a delegate copy remains in the total and leaves the queue. **CLOSED.**

**REDS %362 (`20260830.152752`) -- I put commit-message options after `--`, so Git read them as pathspecs.** *What went wrong:* the first path-limited commit command placed both `-m` options after the option terminator and the seven paths. Git therefore searched for files named `-m` and the message sentences rather than creating a commit. *What caught it:* Git's pathspec refusal before the index or HEAD moved. *What it taught:* **every option belongs before the option terminator; only explicit paths belong after it.** *Repaired (`20260830.152752`):* the retry places signing, scope, and message options first, then `--`, then the seven named paths; the unchanged HEAD and staged path list prove the refused command moved nothing. **CLOSED.**

**REDS %363 (`20260830.152844`) -- the commit body described the change without one counted mechanism word, so the wall refused it.** *What went wrong:* the message said "scan" and "witness," while the guard's engineering vocabulary counts words such as `file`, `script`, `function`, and `call`; its 32-word body therefore found zero mechanisms where one was required. *What caught it:* `tools/hooks/commit-msg`, which left the message unchanged and HEAD unmoved. *What it taught:* **a mechanism sentence names the ordinary artifact explicitly, rather than relying on a tree-local tool name to imply it.** *Repaired (`20260830.152844`):* the retry opens by naming the shell script and witness file before describing the selector behavior. **CLOSED.**

**REDS %370 (`20260830.154940`) -- the report card's one judged reading carries its scale in exactly one place, and it is the only place a lap does not read.** *What went wrong:* `qa_report_card.sh` grades as `(register + reach + truth + service + 2) / 4`, so `--service` sits on the same 0-100 scale as the counted readings. The loop's seat prompt says so exactly -- *"four questions worth 25 each"* -- and **every surface that teaches the reading drops that clause**: this card's Standing block, `.claude/rules/quality-assurance.md`, `context/GAUGE_STYLE.md`, and the usage line `[--service N]`. A hand reading any of the four answers *three of four*. Grading `construction/standing-equipment.kyri` I did exactly that: the tool read 3 of 100 and returned **C+ 76** on a file that reads **A+ 99** once scaled. A reading below B **pushes a molt frame**, so the instrument the fleet grades itself with can turn an honest judgment into invented work. *What caught it:* the number disagreed with the artifact -- a roster with 25 living citers, named by the live card, does not read C+. *What it taught:* **a judged input carries its scale where the judgment is made, not where the arithmetic is.** It is %341/%357's borrowed-number class turned inward: one number meaning four to the reader and one hundred to the program. *Repaired:* the card prints `service_scale` beside `service` on every run, the usage line names `0-100`, the Standing block restores *worth 25 each*, and rostered `qa_report_card_witness.rish` asserts it. Additive on purpose -- a witness, two launchers, and three scans parse this card. **CLOSED.**
