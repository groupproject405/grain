# REDS -- a guard's own words, and the path that reached the input

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- three folded rows, immutable once written
**Room:** checkable -- three ledger rows, each repair proven by a control on metal
**Folded:** `20260916.084019` from [`../REDS.md`](../../REDS.md)

Three rows folded the hour the third closed, to give the pin back the headroom eight ships share:
the close took `REDS.md` to 67,171 bytes against a bound of 65,536, so no lap could book a red.

They share a shape worth keeping together. Each found a claim that nothing in the tree was holding
still, and in each the claim was the instrument's own account of itself. `%764` -- a guard printed
an instruction its own scan refused, and recited a leg count two short of what it ran. `%763` -- a
build receipt keyed on the path that reached its input rather than on the input, so one pinned
compiler wore four spellings and bought four rebuilds. `%762` -- a resolver's header declined a
bound on the ground that no depth could be justified from measurement, and the measurement had
never been taken.

A guard measures the tree. What measures the guard is a control, and where a control is silent a
sentence stands unchecked for as long as nobody rereads it.

**REDS %762 (`20260916.055648`) -- one new `readlink -f` site reds `shell_dialect` on all eight ships, and three separate cures each fail for a different structural reason.** *What went wrong:* commit `d65db16c0` (`20260916` 04:45, diffuser) added `target=$(readlink -f "$path" 2>/dev/null)` at `tools/fixtures/a/aurora_placement_scan.sh:131`, taking `gated_readlink_f_sites` from **7 to 8** against a ceiling of 7. The resolve flag is GNU-only, so the gate exists to keep the guards running on both piers. *What caught it:* this seat's cold endurance run, `shell_dialect red 1s`, fifty minutes after the commit landed -- and it reds every ship's pass until it is closed. *What it taught, by failing three ways:* **(1)** the obvious swap -- source `resolve_path` from `tools/fixtures/s/shell_portable.sh` through the scan's own `ROOT` -- reds **12 of 54** legs of `aurora_placement_control.sh`, because that control `cd`s into a pen and `ROOT` comes from `git rev-parse`, so it resolves into the pen where no helper stands. **(2)** the tree's correct answer to that, the git-free walk up from `$0` that `rye_compiled_reach_scan.sh:95` carries under the comment *TWO ROOTS, on purpose*, reds `m4_bites` and -- worse -- lets `m3_bites` **pass for the wrong reason**. That control plants its four mutations by COPYING the scan into the pen and running the copy, so `$0` resolves inside the pen, the walk finds no `rishi/bin`, and the mutant exits 2 before measuring; a leg asking whether a reading fell below one is satisfied by a script that never read anything. **A control that mutates by copying forbids its subject from sourcing by a walked path**, which is a constraint on the mutation style rather than on either cure. **(3)** spelling one hop inline, which passes **54 of 54** legs and both witnesses GREEN -- and **changes the scan's readings**: `room_pairs` **75 to 74**, `room_edges` **225 to 224**, and `top_pair pond -> tally weight` **15 to 10**. `readlink -f` follows a symlink chain to a fixed point; one hop does not, and this scan resolves an import symlink to *the room it lands in*, so a link pointing at another link names the wrong room. A green control and a green witness both passed a silent behavior change to a peer's measurements, which is why the readings were diffed against HEAD rather than trusted. *What this lap did:* reverted the peer's file to HEAD byte for byte, its readings and all 54 legs confirmed unchanged, and booked this row rather than shipping any of the three. *What it needs, named for the owning lane:* a resolver that keeps multi-hop resolution without the GNU flag -- a bounded loop, whose maximum depth `shell_portable.sh`'s own header declines to name from measurement, so seating one is a decision about the shared helper rather than about this file. The alternative is a `+1` on the ceiling, which a ceiling that only falls forbids. *What closed it (`20260916.084019`), and the measurement the elder header asked for:* `resolve_path` declined a loop on one written ground -- *bounding it means naming a maximum depth nobody here can justify from measurement* -- and that premise was untested. Three readings name it. **The tree's own population:** of 834 working symlinks, 793 resolve in one hop, 40 in two, and exactly one in three, so the deepest chain ever filed here is 3. **The host's own limit:** kernel path resolution refuses at the 41st hop, POSIX `SYMLOOP_MAX` at 40, so a longer chain names a path nothing here could open. **And the elder tool ignores it:** coreutils walks in userspace, resolving a 200-hop chain that then refuses to open -- so `readlink -f` was never the conservative spelling. The bound is **40**, borrowed from the kernel rather than invented, thirteen times the deepest real chain; a cycle spends the budget and refuses instead of hanging. *And a fourth reading arrived uninvited:* a multi-hop walk produces a relative target like `a/link/../../b`, and a bare `cd` is LOGICAL -- it collapses `..` before resolving symlinks. bash accepts that path and dash, which is `sh` here, REFUSES it. Every guard runs under `sh` and every interactive check of one runs under `bash`, so the fault reads healthy in the one place a person looks; `cd -P` in both files is the cure. *What this lap did:* the helper walks to a fixed point under the named bound, `aurora_placement_scan.sh` spells the same bounded walk inline -- forced, since its control plants mutations by copying the scan into a pen -- and every reading held byte for byte against HEAD, `room_pairs` 75 and `top_pair pond -> tally weight=15` included. Six control legs prove the bound from both sides; without it the control does not fail but **HANGS**, which is the elder objection demonstrated. *What it does not reach:* the seven remaining sites, five of them the helper's own control calling the elder spelling on purpose. **CLOSED** on `gated_readlink_f_sites=7` at a ceiling of 7, `shell_portable_control.sh` 53 legs and `aurora_placement_control.sh` 54 legs at zero failures, and `shell_dialect_witness` and `aurora_placement_witness` GREEN on metal.**

**REDS %763 (`20260916.023136`) -- one pinned compiler wears four spellings and buys four rebuilds, because the receipt keys on the path that reached it.** *What went wrong:* `rye/src/main.rye` fed the receipt key `hasher.update(zig)` and `hasher.update(rye_lib)` -- the toolchain's path STRING and the standard library's -- beside the content hashes of both. A path names how an input was found rather than what it compiled, so `vendor/zig-toolchain/zig`, `$PWD/vendor/zig-toolchain/zig`, a symlink, and the default `<exe_dir>/../../vendor/zig-toolchain/zig` resolution are four keys over one binary. Measured in a throwaway pen on this tree: `22d31612`, `cd1673e8`, `08b4401c`, `b0da7b36` -- four full rebuilds of a source nothing had touched. The tree spells that one pin **29 distinct ways** across tracked sources, `vendor/zig-toolchain/zig` 905 times and `$PWD/vendor/zig-toolchain/zig` 25, and leaves it unset everywhere `rye_build.sh` runs. *What caught it:* the module's own header, which had never claimed otherwise. It promises a key over *"the whole import closure's bytes, the forwarded flags, the toolchain's bytes, the sorted standard-library tree, and this binary's own bytes"* -- five contents and no path. The doc was right and the code carried two extra inputs it never announced; `ryekey_control.sh` leg 8 then asserted the undocumented behavior as law. *What it taught:* **a cache key must hold what decides the output, and nothing that merely decides how the input was reached.** The distinction is measurable rather than a matter of taste, and the measurement is what turned the leg around: at `-OReleaseSmall` both path spellings emitted **byte-identical** binaries. *And the measurement needed its own control, which is the sharper half.* The first reading said both paths DIFFER -- until the same build was run twice under one unchanged spelling, which also differed. **At `-ODebug` this toolchain is not reproducible at all:** two builds of one source parted in **45,027 of 10,239,778 bytes**, entirely in anonymous struct serial numbers (`__struct_15431` against `__struct_15413`). A comparison taken in that mode measures the compiler's scheduler, so the noise floor swallowed the question and answered it wrongly in the safe-looking direction. It also settles the safety argument: a Debug receipt was already serving one of many equally valid outputs, so dropping a path that reaches none of them takes nothing further away. *What this does not reach:* why the Debug type-name counter varies, which is the vendored toolchain's own business and is named here rather than chased. **CLOSED** on `ryekey-v6`, both path updates dropped, and `tools/r/ryekey_witness.rish` GREEN on metal at twenty legs -- leg 8 turned around with its reason written, and a library-spelling leg beside it.

**REDS %764 (`20260916.050900`) -- a guard told a reader to pass a flag it refuses, and recited a leg count two short of what it runs.** *What went wrong:* `tools/f/fold_shelf_link_repoint_witness.rish` prints one sentence on its red path -- *run the repointer with `--apply`* -- and `tools/fixtures/f/fold_shelf_link_repoint.sh` answered `refused: unknown argument --apply` at exit 2, since it took a bare `apply` or nothing. The instruction a ship receives at the one moment it needs one did not work, and it had already propagated: `construction/ITINERARY.md` carries a peer's account naming the same spelling. Beside it, `tools/f/fold_shelf_link_witness.rish` recited *78 readings* in a literal while its control emitted **76**. *What caught it:* this lap's own repair. Shelving an account left one link at the card's depth, the hot endurance run read `fold_depth_lost=1`, and following the refusal's instruction produced an exit-2 refusal rather than a repair. The count came out of the same touch: two legs added for the flag made the recited 78 accidentally true, and the committed control re-run on its own answered 76. *What it taught:* **a guard's own words are a claim about the guard, and this tree reads every claim except those.** Both halves were self-description -- what to run, and how much was run -- and neither was held by anything. The cure for the second already stands one room over: `tools/r/rye_build_witness.rish` counts its legs off the control rather than reciting them, in its own words *the shape of overcount a recited total wears permanently*. *What this lap did:* `--apply` is a synonym for the default, so the printed instruction lands and a reader guessing the dashed form from `--dry-run` beside it is right; a genuine typo still refuses by name, proven on its own leg. The reading count is derived from the control's output and proven live -- a planted reading takes it to 79, and removing it returns 78. *What this does not reach:* every other witness reciting a literal total, which is a census nobody has taken. **CLOSED** on `tools/f/fold_shelf_link_witness.rish` GREEN at 78 derived readings with both new legs asserted, and `tools/f/fold_shelf_link_repoint_witness.rish` GREEN.
