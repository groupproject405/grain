# REDS -- page-control paths

**Language:** EN
**Style:** Gauge, Meter
**Status:** Shelf -- closed row
**Room:** checkable

**REDS %828 (`20260924.233357`) -- shared page binaries and controls that followed their copied paths.** *What went wrong:* two witness scripts compiled into one fixed output. The eviction control called Bakery its own peer. The residency control copied its sampler to a different depth, passed relative fixture paths, and accepted any changed verdict as a successful mutation. *What caught it:* the build-target scan, source review, and the new unchanged-copy baseline. *What it taught:* a mutation must reach the intended input and name its expected failure. *Repaired:* private build directories preserve executable depth, controls receive an absolute binary path, and mutation assertions name their actual failure. **CLOSED** -- 12 eviction cases and 20 residency cases pass; both witnesses also pass concurrently, compiler refusal releases both private directories, and the build-target scan reads `verdict=ok`. Full roster status stays separate.
