# REDS -- one wall, pointed at one of a commit's two permanent artifacts

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri
**Folded:** `20260907.223000`
**Row:** `%594` (`20260907.220836`) -- BOOKED

The row folds on the lap it was booked, and the reason is worth recording rather than hiding. The
living pin stood at **40,946 bytes against a bound of 40,960** when this lap rebased onto it -- 14
bytes of slack -- and every one of its twelve remaining rows reads **OPEN**, so `reds_fold.sh` may
move none of them. A pin whose living parts fill it exactly cannot accept a new row at all, which
is `%360`'s finding arriving from the other side: the bound is sized by how fast reds close, and
they are not closing as fast as they open. So the record goes where a finished row belongs and the
pin keeps a pointer.


**REDS %594 (`20260907.220836`) -- the wall that refuses a fabricated citation reads the commit message and never the session log riding in the same commit.** *What went wrong:* a session log closes with `file <path> <why>` lines, and that path is a promise of exactly the kind `.claude/rules/collaboration.md` seats. Nothing read it. Measured `20260907.221500` over 4,352 tracked logs: **16,924 path-shaped `file` fields stand and 228 name a basename that exists nowhere in the tree**, 32 of them written today. Twelve, read one at a time, share one shape -- a lap folds its live front to `construction/archive/<stamp>_itinerary-landed-accounts.md` and then writes the path using its OWN log's stamp rather than the shelf's. Checked with `git log --all`, not one of those twelve paths has ever existed; the lap composed the name from the stamp in its hand instead of reading it off disk. *What caught it:* the aether rota lap re-running the three `tier cadence` guards standing red in `construction/standing-equipment-runs.kyri` since `10:10`. `two_rooms_doorway` came back **green** -- its record was nine hours stale, repaired by `%585` and never re-heard, since `%568` found nothing turns the cadence clock. `dated_path` came back genuinely red at `refs_lost=94` against a ceiling of **85**, and reading its lost set by hand is what found the twelve. *What it taught:* **a permanent claim is worth a wall wherever it is written, and this tree built one wall and pointed it at one artifact.** `tools/hooks/commit-msg` has refused a commit body citing an absent path since `%202`, on the reasoning that a commit message is permanent testimony; a session log is exactly as permanent, rides in the same commit, and makes the same claim. *And the guard that did see it cannot be climbed:* `dated_path` folds these into `refs_lost`, a ceiling mixing a class a lap must repair with a class no lap may touch -- all three of its `lost_living` references sit in vendored `gratitude/reaper/`, an immutable `waymarks/date/README-index-` shelf, and a REDS fold archive. A defect visible only through a wall nobody can climb is a defect nobody meets. *Repaired:* `tools/fixtures/l/log_file_claim_scan.sh` gates **`staged_unwritten` at zero** -- the `file` fields of the logs THIS commit stages, read against the working tree and the index together, so a shelf staged beside the log naming it passes free. It is the lap's own claim about its own work, so it can never refuse for a peer's break, which is the property the readme wall withdrawn at `20260907.153124` was declined for. `unwritten_total` and `unwritten_today` are reported and never gated: that population grows ~26 a day across eight ships and every entry is testimony no lap may repair, so a ceiling over it would red tomorrow at a stranger's desk -- `%585`'s fault over a population that cannot even be swept. `tools/fixtures/l/log_file_claim_control.sh` proves **22 behaviors** on real git repositories in a pen, the load-bearing leg being the WELCOME: an index-only shelf passes free, and stripping that leg with `plant_apply` turns the same honest lap into a refusal. Rostered `tier lap` at 0.4s, down from 55s when it read log by log. *Not taken:* the 228 stand as written, and a hook rule refusing the log at commit time -- the true prevention -- would edit `tools/hooks/pre-commit`, where a mistake is a fleet that cannot commit. **BOOKED**
