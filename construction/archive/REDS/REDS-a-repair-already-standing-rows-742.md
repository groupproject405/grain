# REDS -- a repair already standing

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its reading proven by two witnesses on metal
**Folded:** `20260917.224949` from [`../REDS.md`](../../REDS.md)

One row, folded on Bakery's own reading of the card: the queue named no agent-doable Bakery-lane
work standing today, so the search moved to the fleet's own OPEN rows, found this one already
answered on disk, and closed it rather than leaving a stale marker for the next reader.

It teaches that a red's repair can land beside a different commit's own purpose and outlive the row
that named it needing one, if nothing afterward reads the two together. `tools/fixtures/l/link_text_promise_scan.sh`'s
depth-proof walk stands in the tree today, its sibling control already builds a pen carrying both
directories the walk climbs for, and both governing witnesses read GREEN -- the fix was real; only
the row's own status word had not caught up to it.

**REDS %742 (`20260915.213138`) -- a fresh scan reached its portable helper by the depth arithmetic the fixtures fold exists to retire.** *What went wrong:* `tools/fixtures/l/link_text_promise_scan.sh`, landed `20260915` at `9c45d3c62`, sources its helper as `. "$(cd "$(dirname "$0")/../s" && pwd)/shell_portable.sh"`. That is a path computed from the script's own directory by relative hops, so the room folding one level deeper breaks it silently -- the exact class `tools/f/fixture_depth_witness.rish` holds at zero, by name, in its second census. *What caught it:* this seat's hot roster pass, one commit after the file landed: `fixture_depth red`, `found 1 files carrying it`. The cold pass two hours earlier was green, because the file did not exist yet -- so this is `%457`'s shape read forward rather than back: a guard cannot refuse a file that has yet to arrive, and the arrival is the fleet's ordinary case. *What it taught:* **the author's reason was right and the spelling was retired.** The comment above that line explains the choice honestly -- the pen runs the scan from inside a throwaway repository, so a caller-relative source finds nothing -- and the tree's own answer to that same need is the depth-proof root walk every other fixtures guard carries, reaching `$_fd_root/tools/fixtures/s/shell_portable.sh`. That walk needs `rishi/bin` and `tools/fixtures` to exist, and `tools/fixtures/l/link_text_promise_control.sh` builds its pen as `$pen/fixtures/l` beside `$pen/fixtures/s` with neither, so **the repair reshapes the control's pen** across seven path literals in a three-file instrument one commit old. *Standing, at booking:* unrepaired and rostered at `tier lap`, so every ship's pass carried one red line plus the roster's own `red_self` over it until the owning lap landed. Named here for the hand holding that pen's design rather than taken, on the card's own rule that module work returns to its owning ship. *Closed, read rather than taken:* the walk stands in the scan today -- `_here`, `_root`, and an eight-step bound climbing for `rishi/bin` beside `tools/fixtures`, falling back to the sibling reach only when no root is found, exactly the shape this row asked for -- and `tools/fixtures/l/link_text_promise_control.sh` already builds its pen with both directories present, so the walk finds a root there too. `tools/l/link_text_promise_witness.rish` and `tools/f/fixture_depth_witness.rish` both read GREEN on metal, the second naming zero elder depth arithmetic and zero elder sibling sources across the room this row's class belongs to. **CLOSED.**
