# REDS -- a line is not a call

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri
**Folded:** `20260908.115028`
**Rows:** `%640` (`20260908.115028`) -- BOOKED

The row folds on the lap it was booked, for the reason the shelves beside it record: the living pin
stands at its bound and every other row on it reads OPEN to another ship, so a finished row goes
where finished rows belong and the pin keeps a pointer.

What it holds is a measurement problem with an instruction on the end of it. Two of TAME's advise
ratchets counted a pattern with `grep | wc -l` and then told a lap to migrate what they counted.
A line is not a call, and this tree writes a compiler -- so `glow/`'s source carries the text of
the Rye it emits, and a line grep read a compiler doing its job as debt to be paid.


**REDS %640 (`20260908.115028`) -- two ratchets asked a lap to migrate what they counted, and what they counted was a line.** *What went wrong:* TAME's advise half printed `parseInt( application sites = 125` and `Ed25519 application sites = 26`, each read by one `grep -hF <pattern> | wc -l` over the twenty rooms. A line is not a call. `glow/` is this tree's own language implementation, so its source carries the TEXT of the Rye it emits -- `append_print(out, &used, "    const amount = std.fmt.parseInt(u32, argv[2], 10) catch return 2;\n", .{})` is a compiler writing a call, and the witnesses beside it assert `std.mem.indexOf(u8, rye_argv, "parseInt(u32") != null` on that emitted text. **All 79 of the parseInt overcount sit there.** Ed25519's overcount is a different shape at the same seam: 29 of its counted lines are `//` prose. Both ratchets say *migrate on touch*, so every counted line was an instruction to make an edit with no call behind it -- rewriting a Glow literal would change what the compiler generates. *What caught it:* the air rota read at its own sense. Air feels for the boundary, and the fixed seat's test is to pull a part out and see what moves, so this lap asked whether each room the ratchets name can actually IMPORT the destination they name. It cannot: **36 of the 46 real `parseInt(` sites sit in rooms carrying no `parse_int.rye` symlink** -- glow 3, pond 20, lantern 11, rye/src 1, ember 1 -- while caravan, linengrow, rishi/src and mycelium each carry one. Reading those sites to check the reachability claim is what showed most of them were not calls at all. *What it taught:* **a count is an instruction when the line beside it says migrate, and an instruction has to be true per site rather than in aggregate.** The elder subtraction compounded it -- `parseint_canonical` counted `tally/parse_int.rye`'s comment mentions, so a comment count was subtracted from a line count and the two errors partly cancelled, which is why the number looked plausible. And reading the remainder rather than counting it settled the Ed25519 question outright: the single surviving site is `glow/nock/nock_glow_mirror_witness.rye`'s `const exact: u64 = 32; // ... Ed25519 seed length`, a **trailing comment**, so the genuine debt outside the kumara seam is **zero**. *Repaired (`20260908.115028`):* `tools/fixtures/t/tame_style_app_sites.sh` counts program position -- dropping a `//` comment line, a `\\` multiline-string continuation, and an occurrence inside a string literal earlier on its own line, with `\"` and `'"'` neutralized first so neither flips quote parity -- and carries the elder `fromEd25519` exemption as an explicit `--exclude` flag rather than losing it in the rewrite. Written **once** and called by both halves: `parseInt(` reads **46**, `Ed25519` reads **1**. `@memcpy(` is deliberately left on its own two independent readings, since it measures identically under both rules (0 emitted, 0 comment) and is the one number the parity selftest compares. Control **16** legs in a throwaway pen, both parity traps shown and the one documented overcount asserted so it cannot move in silence; `tame_style_app_sites` rostered `tier lap`. *Against myself:* the first draft dropped the `fromEd25519` exemption entirely and read 11 -- caught by reading the eighteen sites rather than trusting the number, which is the same move that found the fault. **BOOKED** -- the two counts stand corrected and gated; the remainder is the reachability half, **35 real `parseInt(` sites in four rooms that cannot import `tally/parse_int.rye`**, which is a symlink per room across three other ships' lanes and theirs to seat.
