# REDS -- the wall that decides its own memory

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- two folded rows, immutable once written
**Room:** checkable -- two ledger rows, each repair proven from both sides on metal and in a pen
**Folded:** `20260915.192527` from [`../REDS.md`](../../REDS.md)

Two rows of one family, folded the evening they were written to hold the pin under a bound eight
ships share. Together they teach what a guard over a memorized number can and cannot say. The first
found that a Glow gate desk carries its module's law as a literal, and that every witness over such
a desk proves it decides on both sides of its own memory rather than on both sides of what Rye
declares. The cure is a `law` line in the desk's own head, from which the constant's value, the
head's restatement, and the body literal are all derived.

The second is the arithmetic of that cure read honestly. Its own header priced the remainder from
memory beside a meter already answering, and the word `unlinked` was carrying two questions at once
-- *this scan does not hold it* and *nothing holds it* -- which four desks another guard already
holds pulled apart. Read desk by desk, the remainder is three kinds rather than a backlog, and the
ratchet reached a floor that is not zero.


**REDS %733 (`20260915.185309`) -- a Glow gate memorizes a Rye constant, and every guard over it proves only that the gate decides on both sides of its own memory.** *What went wrong:* [`src/gate/gate-tally-name-len-bound-u32.glow`](../../../src/gate/gate-tally-name-len-bound-u32.glow) evaluates Tally's name wall at the door as `?:  (gth sample 31)  0  1`, while `tally/gardens.rye` holds the declaration `pub const max_name_len: u32 = 32`. The desk states the law a second time in the `invariant` line of its own head. That is **three copies of one number in two languages**, and twelve witnesses of the family -- [`tools/t/tally_name_len_a1_gate_bound_witness.rish`](../../../tools/t/tally_name_len_a1_gate_bound_witness.rish) and its siblings -- each run the desk twice, 12 answering 1 and 40 answering 0. **Every one of them stays green after the Rye constant moves**, because a wall at 31 still decides on both sides of itself; the Glow door would answer for a law the Rye module no longer holds. *What caught it:* the Pheromone seat's own ladder rung -- express receipt facts and Tally bounds in the smallest Glow form already owned -- read as a question about the forms already standing. Measured **25 tracked `.glow` desks memorizing a non-zero literal**, and the seven whose law resolves to a named Rye constant agreed **exactly**. *What it taught:* **`%532`'s lesson one room over, in the other direction.** There the three enumerations were of a *population*; here they are of a *law*. Both agreed perfectly, and both agreements lived in a person's memory, which is the one place a check cannot stand. The sharper half: a guard can be green, correct, and load-bearing while proving the wrong half of its subject -- *this gate decides* rather than *this gate decides what Rye decides*. *The reason a scan could not simply resolve the name:* `max_name_len` is declared in **31 tracked Rye modules at four values** -- 200, 48, 64, 32 -- and the only PUBLIC `wire_capacity` reads **528** in `comlink/wire_format.rye` while the Aurora-named gate correctly mirrors the private `const wire_capacity: usize = 512` in `aurora/src/posted.rye`. A by-name resolver would have called that correct gate a disagreement. So the desk declares its own law once, in its head -- `::  law  <file> <const> below|atmost|exact` -- and the constant's value, the head's restatement, and the body literal are all derived from it. Three relations, each present in the corpus today: `below` (a capacity, wall at const-1), `atmost` (a width the const itself satisfies), `exact` (the `eq` family). Two `gth` conventions live side by side, so the relation is declared rather than read off the operator. *Repaired (`20260915.185309`):* [`tools/fixtures/g/glow_gate_law_agree_scan.sh`](../../../tools/fixtures/g/glow_gate_law_agree_scan.sh) reads the population off the BODY rather than the filename -- which is what finds `gate-comlink-addr-width-u32`, memorizing sixteen under neither naming pattern -- and gates `law_malformed`, `law_unresolved`, `head_disagree`, and `body_disagree` at zero. `unlinked` is a **ratchet at 18** rather than a gate, because several unlinked desks mirror a struct field count or an enum variant count rather than a named constant, and linking those is each owning lane's own lap. Seven desks linked on this lap. [`tools/fixtures/g/glow_gate_law_agree_control.sh`](../../../tools/fixtures/g/glow_gate_law_agree_control.sh) proves **40 behaviors** on planted desk rooms in a throwaway pen, every refusal planted then lifted and the ratchet shown both ways; **three mutations proven to bite**. *Proven on metal rather than in the pen alone:* moving `max_gardens` from 8 to 16 in `tally/gardens.rye` leaves `tools/g/glow_tally_pair_bound_witness.rish` **GREEN** and reds the new witness by name, naming file, constant, new value, and the literal the wall should carry. *An error of my own, caught by the control before it shipped:* the first draft called the scan out of the tree with the pen as the working directory, and the scan's own upward root walk resolved to **this repository** -- so an empty pen read `walls=25` and every planted case measured the live tree. The scan is copied into the pen now. **A control that does not enter its pen measures the wrong tree**, which is `%729`'s sentence from two hours earlier arriving by a second road. *Standing:* rostered `tier lap`; the 18 unlinked desks fall as each lane links its own. **BOOKED.**

**REDS %738 (`20260915.192527`) -- a ratchet's own header priced its backlog from memory, and the word `unlinked` counted desks another guard already holds.** *What went wrong:* [`tools/fixtures/g/glow_gate_law_agree_scan.sh`](../../../tools/fixtures/g/glow_gate_law_agree_scan.sh), seated two hours earlier by this same seat, opened its ratchet paragraph *"The twenty-seven unlinked desks are not a fault made today"* while the meter thirty lines below it answered **18**. A number written from memory, standing beside an instrument already answering the same question -- `%532`'s shape and Gauge's fifth thing, in a file whose whole subject is a number stored where a check cannot stand. *The sharper half:* the same paragraph called all eighteen *a backlog*, and a reader taking that at its word would read `unlinked` as a count of desks **nothing** holds. Read one desk at a time on `20260915.191259`, that is wrong for four of them: [`src/gate/gate-mantra-line-fields-eq-u32.glow`](../../../src/gate/gate-mantra-line-fields-eq-u32.glow) and its three siblings are each compared against their Rye struct by [`tools/fixtures/m/mantra_gate_constant_scan.sh`](../../../tools/fixtures/m/mantra_gate_constant_scan.sh) over [`tools/fixtures/r/rye_struct_fields_scan.sh`](../../../tools/fixtures/r/rye_struct_fields_scan.sh), under [`tools/m/mantra_a1_equality_witness.rish`](../../../tools/m/mantra_a1_equality_witness.rish) -- read on metal this lap at `decided=5 counted=5`, `3/3`, `3/3`, `3/3`, all `verdict=ok`. So `unlinked` means *this scan does not hold it*, never *nothing holds it*, and the word invites a reader to miss the difference. *What caught it:* taking the ratchet's own instruction literally and reading each of the eighteen rather than the total. *What it taught:* **a ratchet's remainder is a population, and a population deserves the reading a claim deserves.** Sorted, the fourteen now standing are three kinds and **not one of them can carry a `law` line as the grammar stands**: five are not gate desks at all -- three generated language examples under `glow/gen/i/` and two planted fixtures another control reads; four are the Mantra desks already held above; five name a law with no plain-integer constant to point at -- `gate-comlink-addr-width-u32` mirrors a seated shape and no Rye module declares an ipv6 address length, the two Aurora length desks mirror `tally/kumara.rye`'s `seed_length` and `signature_length`, which are Ed25519 **expressions** `const_value` returns empty for on purpose, and two more mirror enum variant counts. **The ratchet is at its floor, and the floor is not zero.** *Repaired (`20260915.192527`):* the header carries that anatomy in place of the backlog sentence, names the three shapes a future law grammar would have to learn, and says why narrowing the population to `src/gate/` would buy a lower floor by blinding the scan to a gate desk written anywhere else. Four desks linked in the same lap -- [`gate-skate-kind-ceiling-u32`](../../../src/gate/gate-skate-kind-ceiling-u32.glow) to `brushstroke/skate_input_event.rye max_kind atmost`, [`gate-skate-ring-admit-u32`](../../../src/gate/gate-skate-ring-admit-u32.glow) to `brushstroke/skate_event_ring.rye event_capacity below`, and both Lantern faces to `pond/apps/lantern_face.rye max_face_text atmost` -- taking `linked` 7 to **11** and the ceiling **18 to 14**. *Proven on metal rather than in the pen alone:* each of the three relations was made to bite from the Rye side and then restored -- `max_kind` 8 to 12 reds `body_disagree` and `head_disagree` by name; `event_capacity` 128 to 256 reds with *wants (gth face 255)*, which is the `below` arithmetic proving itself; `max_face_text` 38 to 40 reds **both** Lantern desks at once. `glow_gate_law_agree` GREEN, its control **40 legs, 0 failed**, and `glow_skate_gates`, `lantern_face`, and `mantra_a1_equality` each GREEN after the move. *And the hot roster pass found the elder lap's own arrears, one lap late.* `ignored_walk` read `unfiltered=28` against a ceiling of 27, naming `glow_gate_law_agree_scan.sh:164` -- the `find . -name '*.glow'` pen fallback, landed by the seating lap, whose hot pass was TERMed at `tree_moved` and so never read it. A walk reads every path standing on the disk, so a desk under `.lap/` or a parked worktree would be counted where no clone holds one (`%722`'s class). The walk asks `git check-ignore --stdin` and drops what git disowns; where no repository stands the call answers nothing and the walk stands whole, which is the only state that branch runs in. `ignored_walk` reads `verdict=ok`. *And that repair carried its own trap, which the pen caught in one run:* the first spelling filtered with `awk 'NR==FNR {...}'`, and an EMPTY first file leaves `NR==FNR` true on the SECOND file's first record -- so the whole walk landed in the drop set and the pen read a tree holding no desks at all, **23 of 40 legs failing at once**. The disowned set is read in `BEGIN` through `getline` now, which has no such edge, and the control returns 40 legs, 0 failed. *What is not repaired:* the floor. Lowering it needs the `law` grammar to learn a second kind of home -- a struct's field count, an enum's variant count, or a const whose value is an expression -- which is a lap with a design behind it rather than a sweep. **BOOKED.**
