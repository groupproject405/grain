# REDS -- one room list in five files, and a parity selftest that could not see it

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri
**Folded:** `20260908.101259`
**Rows:** `%630` (`20260908.101259`) -- CLOSED

The row folds on the lap it was booked, for the reason the shelves beside it record: the living
pin stood over its bound and almost every row still on it reads OPEN, so `reds_fold.sh` may move
nothing else. A finished row goes where finished rows belong and the pin keeps a pointer.

What it leaves behind is one sentence worth carrying: equal counts over unequal populations still
compare equal, so a parity selftest proves arithmetic and never proves reach.

**REDS %630 (`20260908.101259`) -- one room list stood in five files, and a green parity selftest compared two different populations.** *What went wrong:* the TAME style pair reads authored `.rye` from a hand-listed set of rooms, and that list was spelled inline five times -- `tools/t/tame_style_scan_bans.rish`, `tools/t/tame_style_scan_advise.rish`, `tools/fixtures/t/tame_style_scan_advise_legacy.sh`, `tools/fixtures/t/tame_style_scan_bans_legacy.sh`, and `tools/fixtures/t/tame_style_long_fn_roster.sh`. The bans half widened on `20260827` to seven more rooms -- `glow`, `mycelium`, `constel`, `lattice`, `ember`, `lantern`, `scribble` -- and its own comment argued the general case in the same breath: *two rosters that must agree are one roster plus a bug waiting for the day they stop agreeing.* Only that half moved. Twelve days later the advise half still named fifteen rooms and two `glow` FILES, so one script pair read **857 files against 1,126**, and `tame_style_scan_advise_selftest.rish` called their counts equal every lap. *What caught it:* the fire rota, reading the ledger before anything new, then pressing the reflex list in `context/TAME_CORE.md` against what actually measures it -- the same move that walled the chapter allocator at `20260908.083050` one lap earlier. *What it taught:* **equal counts over unequal populations still compare equal**, so a parity selftest cannot see a roster gap; it can only see an arithmetic one. Parity here held by luck -- `image/`, `mikrophone/` and `amphora/` stood in one list and not the other and happened to carry none of the four counted patterns, so the day one arrives the selftest would have reddened naming drift that was really a population. *What the gap hid:* the `@memcpy` ratchet printed **0 application sites** while **137** stood in authored Rye, 129 of them in `glow/`, this tree's own language implementation. `parseInt(` read 29 against **125**; `Ed25519` 20 against **26**; zero-assert files 11 against **60**. *Repaired (`20260908.101259`):* the rooms live once, in `tools/fixtures/t/tame_style_rooms.txt`, and all five readers strip its comments and read it. The advise half's three per-file `map` + `join` + shell-arithmetic ratchets became one `grep | wc -l` each through the run seam the camelCase line beside them always used -- **because the elder technique cannot widen**: it builds one shell expression holding a term per file, and the wider roster refused with `StringTooLong` on line 31, measured, after a first guess that `max_list_items` would be what bit. Its zero-assert list is now drawn by `grep -L` and filtered in process, so 127 answers materialize where 1,126 questions would have crossed the 1,024-item bound. GREEN on metal: `tame_style_scan_bans`, `tame_style_scan_advise`, the advise parity selftest (137 both sides), `tame_style_check`, `tame_style_long_fn_witness`. The `>70`-line ledger reads **681 functions over 1,126 files** where it read 322 over 612, and carries no ceiling. *Two findings named rather than taken:* `tame_style_scan_bans_legacy.sh` is reachable only by `tame_style_scan.rish bans-legacy`, which nothing rostered calls, and it exits 1 at HEAD on five `///` comment lines the native half's `tame_style_ban_noncomment_files.sh` correctly reads past -- proven by running the committed bytes, so the red predates this lap. Whether a dormant parity copy whose predicate the native half superseded has outlived its seat is a **removal**, and removals are Keaton's word. **CLOSED** -- the roster is one file, and every reader of it is green.
