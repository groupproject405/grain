# REDS %747 -- a guard that could not answer wore the word for one that found something

**Status:** Shelved testimony -- **Room:** checkable

Folded from `construction/REDS.md` on `20260917.164918` to seat the newest row under the pin's
bound. The row is reproduced whole and unedited; the ledger's first law is that a row is never
edited or removed.

**REDS %747 (`20260915.220137`) -- a full disk reads as four broken guards, and the evidence that would have said otherwise is the file the disk refused to write.** *What went wrong:* a hot endurance run launched at `20260915.215400` died at guard **27 of 376** with `standing_equipment_run.sh: line 1480: times: write error: No space left on device`, reporting **four red** on the way down -- `equinox_e123_living_pin_guard`, `exec_bit`, `living_card_ascii` and `prose_register`. Two of those four had run **GREEN on metal in this same tree minutes earlier**, and ran GREEN again three minutes later: `exec_bit` and `living_card_ascii`. *What caught it:* the write error itself, and then the evidence trail. The transcript names `construction/standing-equipment-reds/exec_bit.txt` and `living_card_ascii.txt` beside those two verdicts, and **neither file exists** -- the room holds five files and not one of them is either name. So the runner announced an evidence path, the write was refused, and the red stood with a citation to nothing. *What it taught:* **a guard that cannot RUN reports the same word as a guard that FOUND something**, and the disk is the one condition under which that mistake is fleet-wide and simultaneous. This is the gate-versus-fault distinction one level up from where `%744` found it: there a page called a gated build a green view, here a runner calls an unwritable pen a red tree. **The transience is what makes it dangerous.** Measured three minutes apart on this pier: `df` read `176G 166G 1.1G 100 percent` at the death and `176G 84G 84G 50 percent` at `220137`, so roughly 83G was released by a peer between the two readings and the evidence of the cause was gone before a hand could read it. A reader meeting that transcript tomorrow sees four reds and a healthy disk. *What holds this figure still:* nothing. Both `df` readings are FREE and the pier is shared by eight ships -- run `df -h /` rather than trusting either. *And a peer booked the likely cause 44 seconds ahead of this row* (`20260915.220053`): a witness leaking a 24MB pen into `/tmp` on every run for nine days. Two ships met one disk from two directions in the same minute, one reading the leak and one reading what the leak did to a roster. *Not repaired, and not reached:* the runner is `tools/` lane, the eight trees on this pier are eight other writers, and a disk a peer filled is emphatically not this seat's to clear (one writer per checkout, `%291`). **BOOKED** (`20260917.150000`) -- the door is built and the word is **`unrun`**. `standing_equipment_run.sh` reads the evidence write's own exit status, which it discarded; a refusal it could not record is counted apart from `red`, named, refused under `run_verdict=guard_unrun`, and withholds the receipt, and the path is announced exactly when the file stands. It spans two files because `standing_equipment_scan.sh` counted every verdict that was neither green nor gated as red, so a fifth word alone would have re-made the fault one file over. Proven in a pen with a shimmed `tail` carrying the recorded ENOSPC line, both mutations bitten; `/dev/full` holds the plant faithful. *What remains:* an `unrun` row still reads as recorded, so the staleness readings age it out for nobody.
