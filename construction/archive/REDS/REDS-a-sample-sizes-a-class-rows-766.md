# REDS -- row %766, folded from the living pin

**Folded:** `20260916.112149` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../../.claude/rules/reds-first.md)

*One row that closed a class `%646` had sized from a five-of-forty sample, and found three shapes
where the sample had shown one.* Counted rather than sampled, the submodule half was already swept;
the live half was nine witnesses hard-asserting `test -x` on a gitignored build output, none of
them rostered; and a **third** shape stood behind both, unnamed by the elder row at all -- six
witnesses refusing on an unset `WAYLAND_DISPLAY`, on a fleet that runs headless.

What the row leaves behind is a rule about measurement rather than about witnesses. **A sample sizes
a class; only a count names its shapes.** A sample drawn from a population tells you roughly how big
the population is, and it tells you nothing about how many KINDS live in it -- so a class booked off
a sample is a class whose repair is aimed at whichever kind the sample happened to show. The cost
here was a third of the subject standing unnamed in the row that existed to name it.

*Folded here because the pin stood 926 bytes over its 65,536 bound after a peer's row and this
lap's own both landed on one rebase. No open row was touched to seat it.*


**REDS %766 (`20260916.082153`) -- the precondition class was booked off a 5-in-40 sample, and counted it came out as three shapes, one of them a stale grep nobody had read.** *What went wrong:* `%646` named the class -- a witness hard-asserting on something a correct clone may lack is unrostable, so its red is one nobody hears -- and sized it by sampling, since nothing could count it. Counted, the submodule half was already swept (40 of 43 `gratitude/tigerbeetle` dependents skip honestly; the three that do not are a `say` line, a correct presence report, and a witness retired from the living suite by name on `20260711`). The live half was a different shape: nine witnesses under `tools/gen/chapter/` bind `brushstroke/bin/brushstroke-wayland-seed`, run `test -x` on it, and assert that call's exit -- on a gitignored build output, with none of the nine rostered. A **third** shape stood behind them, unnamed by `%646` at all: six witnesses refuse on an unset `WAYLAND_DISPLAY`, and every ship in this fleet runs headless. *What caught it:* running the nine after the sweep. Five reached their skip and four still refused, which is the only way the display shape could have surfaced -- a structural grep for the artifact could not see it, and a fourth refusal behind it was not a precondition at all. `tools/gen/chapter/one_graph_dag_paper_witness.rish` greps `construction/SHRED_PREP.md` for two shed elders whose rows **folded** to `construction/archive/SHRED_PREP-second-mitra-shed-record.md`, so the grep had been reading an absent record since the fold; the witness is unrostered, so the red went unheard exactly as `%646` describes. *What it taught:* **a sample sizes a class; only a count names its shapes.** Two of the three shapes here were invisible to the sample, and the sample's own arithmetic was right. The second lesson is narrower and sharper: **a skip that always fires proves nothing**, so the control plants the artifact and sets the display, and asserts the swept witness walks past the skip and refuses on its own first real claim. *Repaired:* all nine sweep to the worked example's idiom -- `say SKIP`, `say GREEN`, `exit 0` -- and each is rostable; the folded grep is repointed to the archive shelf; 95 non-ASCII characters in the touched nine convert on touch, proven by re-deriving each file from its committed bytes. `tools/fixtures/p/precondition_skip_scan.sh` reads the class by structure and holds `hard` under a ceiling that only falls, seated at the measured **7 across 5 files**, none of them in this sweep. *What it cannot see, said out loud:* a precondition reached through a nested witness, a shell function, or a runtime path -- **18 unresolved sites** are reported rather than guessed at. **CLOSED** on `tools/p/precondition_skip_witness.rish` GREEN, 11 control legs on real repositories in a throwaway pen, both mutations bitten.
