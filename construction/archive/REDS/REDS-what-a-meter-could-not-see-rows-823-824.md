# REDS -- what a meter could not see

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- two folded rows, immutable once written
**Room:** checkable -- two ledger rows, each measured on the lap that booked it
**Folded:** `20260917.222209` from [`../REDS.md`](../../REDS.md)

Two rows, folded within the hour they were booked, to hold the pin under a bound eight ships share
-- the fourth time that sentence has been written on a shelf in this room today. A peer's row landed
while this seat worked and took the pin to **65,890 of 65,536 bytes**, which refuses every commit on
every ship until someone sheds. These two are this seat's own, so nobody else's work was touched.

**What the pair shares, and it is why they travel together.** Each is a reading that could not see
what it was asked about. `%823` counts orphan pages against an inbound set that never opens `MAP.md`
-- the page this tree calls its walk -- so it reported 24 while a reader reached every one. `%824` is
a wait loop asking `pgrep -f` whether its own work had finished, matching its own command line and
waiting forever. **One meter was blind to a page; one was blind to itself.** Neither was broken, and
neither could say so.

**The second is the sharper, because its silence is the fault.** A self-matching pattern that SIGNALS
kills a peer and is loud; a self-matching pattern that WAITS is indistinguishable from patience.

---

**REDS %824 (`20260917.222209`) -- a wait loop asked `pgrep -f` whether its own work had finished, matched itself, and waited forever.** *What went wrong:* this seat armed two background shells reading `until ! pgrep -f "process_fanout_scan.sh --sample" >/dev/null; do sleep 5; done` to wait for a census it had launched. The pattern sits in the waiting shell's OWN command line, so `pgrep -f` matched the loop itself on every pass. The census finished; both loops ran on, holding two shells open until a hand asked what was still running. *What caught it:* Keaton, saying two shells were still running. No guard, no timeout, and no output -- the task files stood at zero bytes, which reads exactly like a job still working. *What it taught:* **this is `%541`, `%569` and `%587` in a spelling none of them named, and the failure mode is inverted.** Those firings had a command-line pattern reach a PEER and kill it -- loud, and repaired by `tools/f/fleet_call.sh`, which resolves each candidate's working directory and refuses what sits outside this tree. Asked the same question here it answers `would_send=0`, `refused_self=1`, `refused_prose=1`: the bounded tool was right where the raw one had trapped itself. **A self-match that SIGNALS is visible; a self-match that WAITS is silent**, since a loop waiting on itself is indistinguishable from a loop waiting on real work. *The sharper half:* the clause on `tools/f/fleet_baton.txt` names the deed as *resolving a pid from a command-line pattern, by any tool*, and lists `pkill`, `pgrep`, `ps | grep | awk` and a `/proc` walk as examples. This seat had read that clause aloud in the same session and still typed it, because the idiom arrived as a WAIT rather than as a kill -- and because the harness's own guidance offers exactly this `until` form for waiting on background work. The wound lives at the seam between a host idiom and this tree's law, which is a place no guard in this tree can read. *Repaired:* both shells stopped through the harness's own task control rather than by signal; `fleet_watch.sh`'s own polling sleep was identified and left untouched. *What this does not reach:* a guard, for the reason the clause itself gives -- a command typed at a session prompt sits in no file. What a lap can do instead is wait on a **content predicate**, which `standing_equipment_run.sh` already models: a pass is finished when its transcript carries `run_verdict=`. **BOOKED** -- the instances stand stopped; the habit is where habits are set.

**REDS %823 (`20260917.214010`) -- a reachability meter read 24 orphans while a reader could reach every one, because its inbound set never held the page this tree calls its walk.** *What went wrong:* duty 3 of `tools/l/living_docs_lint.rish` names a roster page an orphan when no link reaches it from the docs roster or `docs/README.md`. `MAP.md` sits in neither, and `.claude/rules/read-scope.md` calls that page *the walk that replaces the root `ls`* -- the one page a lap is told to navigate by. *What caught it:* a fascia pass asked to follow the connections home to root and down to leaves, which split the 24 by whether `MAP.md` reached each: **seven were already reachable and false**, seventeen were reachable from nowhere -- `construction/`, `session-logs/`, `kumara/`, `lotus/`, `image/`, `linengrow/` and eleven more. `linengrow/` was the sharpest, named in the module-ring table in bare backticks where every sibling row carried a link. *What it taught:* **a reachability reading measures its own inbound roster rather than the tree's fascia, and the two part silently the moment an entry point is added anywhere else.** The count never moves when the fault is repaired, so nothing tells a hand the repair worked -- which this lap proved by predicting the count would fall by at most seven and watching it fall by **zero**, since which rooms were linked was irrelevant to a meter that cannot open the page they were linked from. *Repaired:* all seventeen now stand in `MAP.md`, every one of its 73 cited paths resolves, and the unreachable count reads zero. *What this does not reach:* the meter. Widening duty 3's inbound set to include `MAP.md` belongs to the lane that owns the lint, and a reader who trusts the 24 today is reading a number about a roster. **BOOKED** -- the instances stand repaired; the reading stays as it was. *The meter followed* (`20260917.215600`, bakery): `tools/fixtures/l/living_docs_lint_scan.sh` duty 3 now reads `MAP.md` as a third inbound source beside the docs roster and `docs/README.md`; the orphan count falls **24 to 6**, all six genuine (`amphora/README.md`, `dimeroll/README.md`, `granary/README.md`, `mand/README.md`, `mandi/README.md`, `mycelium/README.md`), none of them a page `MAP.md` already reaches. `tools/l/living_docs_lint.rish` GREEN on metal (advisory duty, never fails); the roster itself is unchanged. **CLOSED** -- the reading now matches the tree.
