# REDS -- a printed line is not a read one

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its reading held by `stash_record` at `unlanded=0`
**Folded:** `20260917.001938` from [`../REDS.md`](../../REDS.md)

One row, folded to hold the pin under a bound eight ships share. It teaches that the absence family
has a fourth place, and it is this tree's own stash: a lap rebuilt from scratch work that stood
finished in its own checkout, proven and logged, because both standing absence instruments answer
about a remote and a live board rather than about a stash.

The sharper half is the second one. `fleet_round_open.sh` printed *1 session log(s) stand in the
dead-letter box and nowhere else* as the second line of that lap's first command, an hour before a
byte was written. A seat cannot judge absence from local bytes it declined to open, and a third
instrument saying the same thing a third time is the shape this ledger keeps refusing.

**REDS %799 (`20260916.195622`) -- a lap rebuilt, from scratch, work that stood finished in its own tree's round-open stash, and the round-open had printed the fact in its second line.** *What went wrong:* a pheromone lap at `20260916.110834` threaded a `refusal.Slot` through `glow/tokens.rye`'s private lexers, seated `tokenize_reporting` beside `tokenize`, took the witness from 9 legs to 16 and ran three mutations against it -- then died inside its send. Nothing reached a ref. Six hours later the next lap of the same ship read its own expired claim, treated the work as unbuilt, and built it again: the same six functions, the same four records, the same ceiling fall from 44 to 40. *What caught it:* the hot scoped endurance run, `stash_record red` with `unlanded=1`, naming the parked log by path. And earlier, unread: `tools/f/fleet_round_open.sh` printed **`1 session log(s) stand in the dead-letter box and nowhere else`** as the second line of this lap's first command, an hour before a byte was written. *What it taught:* **the absence family has a fourth place, and it is this tree's own stash.** `path_absence_scan.sh` fetches the anointed remote and asks `here=` and `upstream=`; a stash is neither, so it answers `absent` about work sitting in this checkout. `fleet_claim_scan.sh` reads the live board, and a dead lap's claim has expired, so it answers `clear`. Both instruments were right and both were blind, exactly as the baton's ABSENCE clause says of work living in an agent's head -- except this work lived on this disk, complete and proven, with its own session log beside it. And the second half is sharper than the first: **the instrument fired, at the top of the lap, and a printed line is not a read one.** `%457` taught that a seat cannot judge absence from local bytes; this teaches that a seat cannot judge absence from local bytes it declined to open. *The repair:* both parked artifacts land as testimony -- the elder log whole and unedited, the elder account shelf wearing a banner naming the living reading -- and the design call that lap made and this one had not, the kind word travelling from the caller because one scanner serves the `%tag` and ident paths both, was ADOPTED rather than discarded, its `field=tag` leg proven by a mutation that reds the witness. *What this does not reach:* a guard. The round-open already prints the reading and `stash_record` already gates it; what failed is a hand reading past two true lines, and a third instrument saying the same thing a third time is the shape this ledger keeps refusing. What a lap can do instead costs one command at its open -- `sh tools/fixtures/s/stash_record_scan.sh list` -- before it believes any claim about what stands unbuilt. **BOOKED** -- `stash_record` GREEN at `unlanded=0`, the duplicated work landed once, and the remainder is a habit rather than a repair.
