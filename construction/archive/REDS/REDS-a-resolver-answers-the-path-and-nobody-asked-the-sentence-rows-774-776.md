# REDS -- rows %774 and %776, folded from the living pin

**Folded:** `20260916.205600` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../../.claude/rules/reds-first.md)

*Two rows on one theme: an instrument that resolves a PATH cannot answer for the SENTENCE beside
it, and this tree had built the first kind and read it as though it were the second.*

`%774` found a refolded REDS shelf whose departed name every elder citation still carried, with no
instrument able to say where those citations had gone -- a name that resolves nowhere and a reader
with nothing to ask. `%776` found every page on a shelf grading B+ or better on a Truth reading
that resolves each cited link and never reads the claim, so the page teaching newcomers to read
this tree had stood false since its birth commit and scored full marks for it. In both, the
measurement was honest about the half it could reach and silent about the half that mattered, and
silence at the verdict line reads exactly like a pass.

The rows stand here exactly as they were written. A closed row leaves the living pin so the pin
stays the length a reader will actually read; the lesson travels forward in the guards the rounds
built, and the rows themselves stay one click away.

---

**REDS %774 (`20260916.103855`) -- a refold renames a REDS shelf, and every elder citation of the departed name resolved nowhere, with no instrument able to answer.** *What went wrong:* a fold shelf is named `REDS-<title>-rows-<N>.md`, and a later refold RENAMES it -- absorbing the row into a wider range, or retitling the same rows. Living citers repoint in that pass; **dated testimony keeps every word it wrote**, so the elder basename stands in the record forever and opens nothing. Measured this lap over tracked files: **563 distinct shelf basenames are cited, 88 of them exist on no path, and 80 of those 88 are cited ONLY by dated testimony** that accrete-never-break forbids rewriting. *What caught it:* a fire rota lap reading the row's own dual seat, `foundations/20260818-081438_the-three-depths-of-removal.md`, which names this exactly -- a refold is a **breach**, *a rename the present forgets and the past remembers* -- and then asking what recovers the past's half. Nothing did. *What it taught:* **a breach is only safe where a resolver stands behind it.** The tree already knew this for day shelves and built `tools/d/dated_path_resolve.rish` for them; the REDS shelves took the same breach with no such instrument, because the two resolvers it does own cannot reach this shape -- **checked by running them, not assumed**: `dated_path_resolve` computes a day directory from a one-clock stamp and a shelf basename carries no stamp, answering `verdict=missing` on the very citation now recovered; `fold_shelf_link_scan` reads link DEPTH from inside a shelf rather than a renamed target. This is `%626`'s family one room over -- there a lost-reference ceiling stood unreachable because its lane could not edit testimony; here the references were not merely ungated, they were **unanswerable**. *Standing:* the key is the derived spine's own -- the shelf's TITLE is a view and its ROW NUMBERS are the identity, since `.claude/rules/derived-spine.md` rule 3 freezes a published number forever. `tools/r/reds_shelf_resolve.sh` reads the row headlines standing on disk today and answers six verdicts: `home`, `recovered-by-rows`, `living-pin`, `split`, `ambiguous`, `missing`. **82 of the 88 recover.** The remaining **6 are gated at a ceiling that only falls**, and their composition is printed rather than asserted: **4 `ambiguous`**, every one naming a **published double** (`%512`, `%530`, `%675`) that the derived-spine law states no lap may repair, so the resolver refusing there is the law working; and **2 `missing`**, both **pen plants written by a living witness rather than a fixture** -- `tools/c/convergence_tree_prove_witness.rish` and `tools/r/reds_pin_capacity_witness.rish` -- which is this lap's own second finding: **the census reads past `tools/fixtures/` as a proxy for *planted*, and the proxy is incomplete.** They are counted rather than excluded, because widening an exclusion to make a number smaller is how a census stops measuring its subject. **The gated reading is `unrecoverable` rather than `absent`**, since a refold RAISES `absent` every time it runs and a gate that reds on the tree's own tending is a gate somebody turns off. **32 legs** on real shelves in a throwaway pen, every verdict planted and then lifted, with two mutations proven to bite their own leg: dropping the `^` line anchor makes a shelf that merely QUOTES another row's headline claim to hold it, and trading the exact row match for a substring lets `%20` be read out of `%200`. The witness asserts the pen's leg tally beside its verdict, so a leg written tomorrow is heard. *And the instrument was built TWICE, which is its own finding:* an earlier lap of this same ship built it at `20260916.100409`, reached the same key and the same blind-resolver reading by its own road, and **its work was swept into a round-open stash by the very next lap of the same ship** -- recovered here from `stash@{0}` alongside two parked session logs. The claim board could not prevent it, because the claim was the sweeper's OWN (`incense-reds-shelf-resolver`, opened `09:52`), so every check read *claimed by me* and answered correctly. That is `%457`'s second half one room in: **a build in flight is invisible to a scan, and a build in a stash is invisible to a claim.** `stash_record` is the instrument that names it, and it did -- it read RED at this lap's cold open and was read as two parked LOGS rather than as a parked BUILD, because its `unlanded` reading counts records and its `orphan:work` reading counts files, and nobody had asked the two to be read together. **BOOKED** -- the instrument stands and the 6 are each named; whether the two pen plants earn a widened read-past is one lane's call, and the 4 doubles wait on `%530`'s own standing word.

**REDS %776 (`20260916.183325`) -- the shelf's every page graded B+ or better on a Truth reading that resolves the link and never the sentence, and the page teaching newcomers to read this tree had been false since its birth commit.** *What went wrong:* `docs-geode/study/README.md` -- the shelf's own *how to read this tree*, the second of the three doors its front page hands a newcomer -- told them `gratitude/` holds *more than two hundred pages of study*. Measured three ways this lap: **124** tracked Markdown pages, 124 on disk, 134 tracked files in the room entire. It was false on the day it shipped, in commit `af7adb74d` of `2026-08-21`, when the room held **110**, and it stood **26 days**. *What caught it:* the report card's own confession, read as one. Grading every living page on the shelf returned B+ or better -- **30 pages, none below 86** -- and every composite carried `truth_mode=counted`, the path-resolution half standing in for the judged half that went unread. `.claude/rules/quality-assurance.md` already records that blind reading shipping a false `A+/98` **twice, both times on this shelf**, so the grades were distrusted rather than banked, and the judged half was done by hand: each countable claim on the lowest-graded page measured against the tree. The first pressing found nothing -- `tools/f/fetch-toolchain.sh` prints none of the four lines the first-hour tutorial promises, until you read the scan it `exec`s, which prints all four. The second found this. *What it taught:* **a counted Truth reading asks whether a link opens, never whether the sentence around it is true.** `gratitude/README.md` resolves, so `truth_counted=100` and the composite banked it; what that sentence claims about the room behind the link is a question no path check is shaped to ask, and the two failures the law records are both exactly this shape. And plainly: **a page can be wrong the day it ships.** The elder two of this class were caught by readers walking the tree; this one was caught only because a grade was disbelieved. *The class, and its bound:* this is the **third firing** on one shelf of a typed number standing beside the thing it counts, and the shelf's own front page already tells the story of the other two -- *ten rooms* against a table listing twelve, false from the commit that added the twelfth; and *1,004 pages* the eight `lessons/` pages outgrew in two days. Both were freed rather than corrected. A sweep of every living page here for the same shape -- a quantity claim about a countable room -- found **exactly one living instance**, which is this one, so the repair is the whole class rather than a sample of it. *The repair:* the count leaves the sentence, the reading stands beside it (`git ls-files 'gratitude/*.md' | wc -l`), and the page keeps the story of its own error in three lines -- because the habit printed directly above it is *read the witness rather than the claim*, and a page teaching that should be seen practicing it. The page grades **B+/88**, up from B+/86, with Reach 80 to 90 and its register at 17 percent, inside the Door ceiling of 20. *What this does not reach:* the judged half of the other **29** pages on this shelf, unread this lap and unreadable by any instrument the tree owns -- the counted half will go on returning 100 for each of them. Whether the card should refuse on an unread judged half, as it already refuses on an unscored Service, is named in the QA law as Keaton's word and stays there. **BOOKED** -- the false sentence is gone and the shelf-wide sweep bounds the class at one; the standing remainder is a reading rather than a repair.
