# REDS -- rows %760 and %761, folded from the living pin

**Folded:** `20260916.084104` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../../.claude/rules/reds-first.md)

*Two rows about the last step of a send, where a correct tool sits one reach away and nobody
reaches for it.* One amended a commit that had already published and force-pushed the rewrite, so
the two mirrors disagreed for ninety seconds over one tree. The other found the repointer that ends
a whole fault class standing correct and reachable while 285 of 474 account shelves had been written
by hand past it.

Neither was a broken instrument. Both were a habit that ran out just before the instrument did, and
that is the shape worth carrying off this shelf: a send's final minute is the place where a rule is
read, a cure is remembered, and neither is run.


**REDS %760 (`20260916.051311`) -- a follow-up commit was amended and force-pushed after it had already published, and the two mirrors disagreed for ninety seconds.** *What went wrong:* this seat pushed `construction: close the assert arrival claim` to both remotes, then saw the card's Git nib still naming the work commit's parent rather than the follow-up's, applied [`remember-git-nib`](../../../.claude/rules/remember-git-nib.md) rule 5, and reached for `git commit --amend` on a commit **already published**. The amend rewrote a published hash, so the push needed `--force-with-lease`, which `xy` accepted while `gp405` kept the elder `3030b6078` -- one tree, two mirrors, two answers. *What caught it:* the seat's own read of its push output. Nothing in the tree could: *never forced* lives in `.claude/rules/send-word.md` and on the baton, and a typed command sits in no file -- the wall `%457`, `%549` and `%569` each met. *What it taught:* **rule 5 and the send order pull against each other, and this is the rule's SECOND firing inside one day.** `%759`, booked eight hours earlier, is the same clause read one step too early from the other direction -- a nib written before a rebase rewrote the commit it named. That row closed with *the law's own sentence still open*, and this one is what an open sentence costs: the rule says so in its own text without saying what to do when the follow-up has already left. **A lantern that fires twice is a loom**, and both firings point at one missing clause rather than two habits. Rule 2 spends its one amend BEFORE the push, on purpose. Rule 5 asks a follow-up to carry the nib forward, and a lap that pushes first has no amend left to spend. The lawful order is plain once written: **read the nib the follow-up will need, write it, commit -- then push, once.** A follow-up's nib is its own parent, known before the commit rather than after, which is what rule 5 already says. This lap read it after. *The repair, and its honest limit:* `gp405` was brought to `xy`'s state under a lease naming the elder hash, so both mirrors agree; the work commit `4f73eca00` was never touched and stands byte-identical on both. The window was about ninety seconds and no peer fetched inside it, which is **luck rather than discipline** and is why this row stands. **BOOKED** -- the instances stand repaired, both mirrors agree, and the remainder is a habit: a follow-up reads its nib before it commits, never after it pushes.**

**REDS %761 (`20260916.053252`) -- the writer that ends this fault class is correct, reachable, and bypassed on 285 of 474 account shelves.** *What went wrong:* `fold_shelf_link` refused this tree for the sixth time in one class, naming `construction/archive/20260916-042700_itinerary-diffuser-unturned-rows-account.md` for a link reading `archive/20260916-030500_...-account.md` -- the spelling the live card carries, which from inside `construction/archive/` resolves to `construction/archive/archive/...` and opens nothing. *What caught it:* this seat's cold endurance run, `fold_shelf_link_repoint red`, and `tools/f/fold_shelf_link_repoint.sh --apply` wrote the one correction the scan had already computed. *What it taught, after a wrong root was tested and discarded:* the first hypothesis was that `tools/i/itinerary_account_shelf.sh` hands back `card_link=archive/<basename>`, so a ship pastes the spelling that becomes the fault when its own block folds one lap later. **That is false, and proving it false is the finding.** Fed a link whose target reads `archive/YYYYMMDD-HHMMSS_sprig.md` -- placeholders rather than a stamp, since an illustration spelled like a citation is read as one by every reader and every link guard ([`stamp-and-name`](../../../.claude/rules/stamp-and-name.md), and `%759` one row down) -- `tools/fixtures/r/reds_fold_reanchor.sh` answers with the `archive/` segment gone and the basename alone. It COLLAPSES a link landing in the shelf's own room and GAINS a level on one pointing out of it, both correct, in one pass. The writer is right. *So the root is that nothing reaches for it:* the shelf that reddened carries a hand-written head (`# Diffuser account --`, `**Shelved from:**`) rather than the writer's own (`# ITINERARY -- landed accounts, shelved`, and the sentence naming the tool). Measured this stamp over `construction/archive/*itinerary*account*.md`: **474 account shelves, 189 carrying the writer's provenance line, 285 written by hand** -- so 60 percent of shelves bypass a correct instrument seated `20260909` for exactly this class, and its own header had already measured the same gap the other way round (*208 shelves, all written by hand, and NOTHING in `tools/` writes one*). **This is `%620`'s ruling one room over, unheard:** a habit that must be typed is a habit that will be typed differently, and the cure there was the tool handing the hand its name. Here the tool hands over the whole shelf and the header line proving it did, and no guard, no law page and no baton clause asks a ship to reach for it. *What holds this still:* nothing. `fold_shelf_link` and `fold_shelf_link_repoint` are both GREEN on metal, and they catch the CONSEQUENCE one shelf at a time, five laps running. *The door beside this row, named rather than built:* the writer's provenance line is a checkable token in a dated file, so a scan could read whether an account shelf declares it -- a ratchet over 285, falling as ships adopt the tool, walled at nothing, since dated testimony keeps every word it wrote and no shelf already standing may be rewritten to satisfy a meter. Whether that guard is wanted, and whether the baton gains the one line that sends a ship to the writer, is Keaton's word. **BOOKED** -- the instance stands repaired and both guards GREEN; the bypass is a habit with a measurement and no instrument.**
