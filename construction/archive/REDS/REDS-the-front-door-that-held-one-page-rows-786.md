# REDS -- the front door that held one page

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven on metal with four mutations bitten
**Folded:** `20260916.215746` from [`../REDS.md`](../../REDS.md)

One row, folded BOOKED to hold the living pin under a bound eight ships share. Its instances stand
repaired: all 27 front-door links were rewritten the way the rung prescribes, `FRONT_DOOR` widened
from four names to eight, the ceiling lowered 848 to 820, and the control taught to read the
ceiling out of the scan's own `ceiling=` line. What remains is a ratchet a lane can now read.

It teaches that a roster drawn around filenames is a roster drawn around a convention, while a
reader walks a graph -- so a gate naming the four files GitHub recognises held the front page whole
and left every second page broken, on the exact walk the front page exists to open.

---

**REDS %786 (`20260916.210332`) -- the front-door gate held one page, and the walk that page is built to start left the seed at step two.** *What went wrong:* `tools/fixtures/s/seed_link_scan.sh` gates every relative link in a FRONT-DOOR document at zero and ratchets every other shipped page under a ceiling. Its `FRONT_DOOR` variable named the four root files GitHub itself recognises -- `README.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, `CHANGELOG.md` -- which is an honest answer to *what does a visitor open first* and a poor one to *what do they open second*. `README.md` stood gated and reading zero while linking directly into `MAP.md`, `ORGANIZING.md`, `SOURCE.md` and `docs-geode/README.md`, which carried **15, 6, 3 and 3** links into rooms the public seed leaves behind. `MAP.md` was the sharpest: it names the private rooms in one sentence, says outright that `template-manifest.kyri` withholds them, and then hands the seed reader a door to each. So a stranger arriving at the seed found the front page whole and every second page broken, on the exact walk the front page exists to open. *What caught it:* an air-row rota reading of this tree's own shipping shelf -- press a claimed boundary and feel whether the hand passes through. The boundary claim is the witness header's own: *every relative link in a front-door document lands on a path the seed also ships*. Pressed one page further out, the hand went straight through. **No guard could have caught it**, because the roster is deliberately named rather than discovered, and a hand-named roster goes stale silently by construction. *What it taught:* **a roster drawn around filenames is a roster drawn around a convention; a reader walks a graph.** The four elder names share nothing but GitHub's recognition of them, and the set a first-time visitor actually opens is the transitive one the front page links. **And a second finding rode in on the measurement.** The scan printed a ratchet site only while its counter was at or under five -- `if (ratchet <= 5)` -- so **847 sites showed five**, while the scan's own header prescribes a repair that *falls on touch instead of in one sweep*. A per-page repair asks a lane to learn on touch that its page is in the set, and no lane could find its own share; the ceiling had fallen 867 to 848 in three weeks, which is what a debt nobody can locate looks like from outside. *What this lap did:* repaired all **27** links the way this rung itself prescribes -- a withheld room is NAMED IN PROSE as a backticked path rather than linked, which is the idiom `docs-geode/README.md` and `.claude/rules/read-scope.md` already write -- added a sentence to `MAP.md` telling a reader that a room in plain backticks is a field room the seed leaves behind, widened `FRONT_DOOR` to eight, lowered the ceiling **848 to 820**, and gave the ratchet a `--list` reader. **And the ceiling stopped being spelled twice:** `tools/fixtures/s/seed_link_control.sh` planted `849` against a literal `848`, so a lowering in the scan would have reddened the control -- the drift the ASCII-document family booked one instrument over. The control reads the ceiling out of the scan's own `ceiling=` line now, proven by lowering the scan to 12 and watching the plant follow with no hand. *What holds it still:* `tools/s/seed_link_witness.rish` GREEN on metal at `front_door_guarded=8`, `front_door_links_outside_seed=0`, `other_living_links_outside_seed=820`; control **22 legs from 16, 0 failing**, with `control_legs` and `control_failed` asserted so a leg added tomorrow is heard; four mutations bitten -- the roster narrowed, `--list` ignored, an unknown argument swallowed, and the ceiling lowered. `tracked_link`, `link_text_promise`, `crushed_index`, `ascii_document` and `prose_register` GREEN beside it; the four repaired doors grade **A/93, A/91, A/96, A/96**, every one scored rather than reported. *What this does not reach:* the **820** that remain, **527** of them in `external-research/` and **162** in `context/` -- rooms whose repair is a rewrite per page and stays a ratchet. And `bibliography.md`, whose 16 sites all name `gratitude/`: a page whose whole subject is a withheld room wants a ruling rather than a sweep. **BOOKED** -- the instances repaired, the roster widened, the remainder a ratchet a lane can now read.
