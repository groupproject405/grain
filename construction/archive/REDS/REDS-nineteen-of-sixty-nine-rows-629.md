# REDS -- nineteen of sixty-nine

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri -- **Room:** checkable
**Folded:** `20260908.091217`
**Row:** `%629` (`20260908.091217`) -- BOOKED, booked `%627` and renumbered **twice**, once on each of two rebases inside forty minutes: peers published `%627` and then `%628` while this row stood unshared, so it derives above each in turn (`derived-spine` rule 3). Both moves cost one line, because every living citation of this row spells the stamp rather than the number (rule 4). The key never moved -- it is the stamp.

The row folds on the lap it was booked, for the reason its neighbours record one shelf over: the
living pin stands at its bound with thirteen of its fifteen rows OPEN, and a pin whose living parts
fill it cannot accept a new one. The repair stands in the tree and the account is on the card, so
the row moves here whole rather than being cut short.

What the row is for, said once so a reader arriving from a citation knows before they read. A
**mark** in this tree is a shared implementation a room reaches without copying it -- Zig admits an
import only from the root file's own directory, so `caravan/tally_copy.rye` is a symlink onto
`tally/copy.rye` rather than a second copy of it. The canon sentence is one line in
`tally/README.md`: *callers reach marks through their own symlinks or imports, not copies.*

Three faults stood in the guard written to hold that sentence, and the third is why the first two
could stand for months. Its population was a **hand-written list**, so it spoke for 19 of the 69
marks the tree actually holds. Its predicate was `-e`, which follows a symlink and therefore also
passes, in silence, on the regular-file copy the canon sentence forbids -- **the one state it names
was the one state it could not read.** And it sat on **no roster row**, so nothing ran it on any
lap, which is what gives an unheard guard's defects no arrival date.

The lesson worth more than the file: **a hand list and a derivation are blind in opposite
directions.** A list cannot grow with the tree. A derivation cannot see a member LEAVE it -- a link
retargeted out of `tally/` simply stops matching, and the count falls without a word. So the honest
population is the union of both, which costs one loop.

---


**REDS %629 (`20260908.091217`) -- a guard read 19 of 69 with a predicate its own canon forbids, and nothing on any clock ran it.** *What went wrong:* `tools/fixtures/t/tally_caller_map_scan.sh` carried a hand-written list of nineteen paths and tested each with `-e`, under a canon -- `tally/README.md` section *Who calls Tally* -- reading *callers reach marks through their own symlinks or imports, not copies*. Three faults stood in one file. **Reach:** deriving the population from the index reads **69** tracked symlinks resolving under `tally/`, so the list saw 30% of the marks it speaks for, and **six** of the missing fifty reach the canon through another room's link (`granary/parse_int.rye -> ../linengrow/parse_int.rye -> tally/parse_int.rye`), a shape no one-hop reading of the link text can see at all. **Predicate:** `-e` follows a symlink, so it proves a link resolves -- and it passes, in silence, on the regular-file COPY the same sentence forbids. The one state the guard names is the one state it could not read. **Clock:** the witness sat on no roster row, and its only caller `tools/t/tensegral_arc_iv_witness.rish` is unrostered too, so nothing ran it on any lap. *What caught it:* the question standing on `construction/ITINERARY.md` -- *is that guard's remaining seat the prose binding alone?* -- read under the fire rota, whose instruction is to look straight at the point most laps would route around. Measuring to answer it found the reach and the predicate; `grep tally_caller_map construction/standing-equipment.kyri` found the silence. *What it taught:* **a hand-written population and a derived one are blind in opposite directions, so the honest reading is the union of both.** A list cannot grow with the tree; a derivation cannot see a member LEAVE it, since a link retargeted out of `tally/` simply stops matching and the count falls without a word. Keeping the nineteen by name beside the derived sixty-nine costs one loop and closes both. And the third fault is the one that makes the other two expensive: an unheard guard's defects have no arrival date, so both had stood since Arc IV without a single red. *Repaired (`20260908.091217`):* the scan derives its population from `git ls-files -s`, checks the named nineteen under the canon's own rule (a symlink resolving to a real file under `tally/`), counts chains as lawful, and refuses under five named verdicts -- `dangling_mark`, `named_missing`, `named_is_copy`, `named_escaped_canon`, `prose_binding_lost` -- with the specific ones ordered ahead of the generic `no_marks_found`, which the pen taught: each named fault empties the derived population as a side effect, so the generic verdict answered every specific refusal first. The symlink walk is spelled with bare `readlink` plus `cd`/`pwd -P` rather than `readlink -f`, bounded at eight hops, since a GNU-only resolver returning empty on the Mac door would read as a tree holding no marks at all (`%240`). `tools/fixtures/t/tally_caller_map_control.sh` proves **thirty-two behaviours** on real repositories in a throwaway pen, every refusal planted and then lifted, including one leg asserting that the elder `-e` predicate DOES pass the copy -- so the repair is shown rather than claimed. The guard is rostered `tier lap`, and `unheard` falls 1088 -> 1087. *What stays open:* whether `tools/t/tensegral_arc_iv_witness.rish` and its three Arc IV siblings want roster rows of their own is that family's lane, not this one. **BOOKED**
