# REDS -- a guard whose rooms were a hand-written list

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri -- **Room:** checkable
**Folded:** `20260908.101759`
**Row:** `%635` (`20260908.101759`) -- BOOKED, booked once against the anointed spine after a fetch. The key is the stamp.

The row folds on the lap it books, for the reason its neighbours record one shelf over, and this
lap can put a number on it. `tools/fixtures/r/reds_pin_capacity_scan.sh` read the living pin at
**40,949 bytes against a declared bound of 40,960** before this row was written -- eleven bytes of
headroom, 13 of 15 rows OPEN and therefore unfoldable, and `rows_that_fit=0`. So the pin cannot
accept a red of any length from any ship, and a row that books itself has to shelve itself in the
same breath. That reading is carried to `construction/ITINERARY.md` as a gate rather than absorbed
here, because the repair -- closing open rows, or sizing the bound for eight ships rather than for
the rate reds close -- belongs to a hand rather than to the lap that happened to trip over it.

What the row is for, said once here so a reader arriving from a citation knows before they read.
The two-rooms doorway law asks every forward-facing page to name which register it speaks from at
its door, and `tools/t/two_rooms_doorway.rish` counts the pages that do not. Its population came
from three room names typed into one `git ls-files` pathspec. The tree ships more forward-facing
prose rooms than that list holds, and the one missing here was `docs-geode/` -- the shipping shelf,
whose whole job is the page a stranger meets first.

The lesson is the one the row's third field states: a guard whose population is a hand-written list
is bounded by what the hand remembered, and its greenness reports the list rather than the subject.
This family had already learned that on the depth axis, three times, each time under a fold that
narrowed a glob. Nothing folded here. The list was short from the day it was written, and the guard
was green for every one of those days.


**REDS %635 (`20260908.101759`) -- the doorway guard's rooms are a hand-written list, and the shipping shelf was never on it.** *What went wrong:* `tools/fixtures/t/two_rooms_doorway_roster.sh` draws its whole population from three rooms named literally in one `git ls-files` pathspec -- `external-research/`, `active-designing/`, `docs/`. `docs-geode/` is the shipping shelf, the room whose entire job is the page a stranger meets first, and it stood outside the doorway law for the whole of its life. It is not the only one. Read with this family's own per-file verdict the same lap: `manual/` holds 29 population pages of which 7 name no room, `foundations/` 83 of which 35 do not, `context/` 105 of which 38 do not -- **80 post-seating pages carrying no register token, none of them reachable by the guard that exists to count exactly that**. The tree states its own room count twice and the two disagree in the open: `docs-geode/README.md` opens by naming "three prose rooms" -- itself, `manual/`, and `docs/` -- which overlaps the roster's three in exactly one room. *What caught it:* the earth rota row read at its own sense. Earth breathes in, meaning take the concrete fact at the door ahead of any argument about it, and earth's fixed seat is `context/TWO_ROOMS.md` -- so this lap opened `docs-geode/templates/README.md` to grade it and read the door line before the body. The token was absent, and whether any guard would ever have said so answered itself in one grep of the roster. *What it taught:* **a guard whose population is a hand-written list is bounded by what the hand remembered, and its greenness reports the list rather than the subject.** This family already knows the shape on the depth axis -- `%381`'s flat-room `index_row_bound`, `log_has_a_row` reading `flat_logs=0` for nine days, and this scan's own 607-page fold blindness -- and every one of those was a *glob* narrowing under a fold. This is the same failure on the *room* axis, where no fold and no rename happened at all: the list was simply shorter than the tree, from the first day it was written. *Repaired (`20260908.101759`):* `docs-geode/*.md` joins the pathspec, and the join costs nothing today -- 10 pages enter the population, 0 name no room, and `doorway fails=3` against a ceiling of 3 stands unchanged, because the one genre on this shelf that stamps its basenames is the blog and its first piece already reads `mixed`. The scan gains a fourth reach leg, `geode=`, refusing at zero the way the other three do; `docs/` and `docs-geode/` part at the fourth character, so `starts-with "docs/"` counts them apart -- checked rather than assumed, since a reach leg satisfied by a sibling room's pages is this family's own silent-narrowing shape wearing a new coat. Control **19 -> 23** legs: the elder three-room roster planted and proven to refuse, its verdict proven to refuse with it, and the sibling prefix proven distinct by planting a `docs-geode/` page with no `docs/` page beside it. `docs-geode/README.md` gains a *The room at the door* section, the write-time reach that has held `active-designing/` free of silent pages since `20260907.015907`. *Not taken, and named rather than routed around:* the other three rooms. Bringing them in means either 80 repairs in one lap or a raised ceiling, and a ratchet's ceiling only falls, so that is **Keaton's word** rather than a lap's; it is carried on the card with those numbers attached. **BOOKED** -- the instance is repaired and gated, and the remainder is three measured rooms waiting on that word. *Against myself, twice in one lap:* I read two doors by eye and asserted what the per-file verdict then contradicted -- the blog piece I wrote last lap, which my own grep called silent and which names `mixed`, and `foundations/20260703-202312_the-marked-value.md`, which I wrote into a roster comment as silent and which is stamped before the seating. Earth's own instruction is that the concrete fact outranks the plausible story; the verdict script is the fact, and my eyes were the story.
