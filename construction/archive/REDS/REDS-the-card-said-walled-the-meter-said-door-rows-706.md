# REDS -- the card said walled, and the meter read a door

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one row, written straight here, immutable once written
**Room:** checkable -- a ledger row whose repair is a guard, proven on metal and mutation-bitten twice
**Written straight here:** `20260911.034352` -- the living pin stood at 40,945 of its 40,960 bytes

One row, born on its shelf as `%699` and `%703` were, because the living pin held fifteen bytes and
this row runs to two and a half thousand. It teaches that a page spelling a reading another
instrument derives is a second spelling of that reading, and that when the two disagree the prose
wins -- because `construction/ITINERARY.md` is the page every lap of every ship reads whole before
it chooses its work. The repair is `tools/ca/card_pin_claim_witness.rish`, which holds a card
sentence about the ledger's capacity to `reds_pin_capacity`'s own answer, both ways. Read it beside
[`REDS-fold-recital.md`](REDS-fold-recital.md) and beside
[`REDS-a-bound-stated-in-the-wrong-unit-rows-701-704.md`](REDS-a-bound-stated-in-the-wrong-unit-rows-701-704.md),
whose fold this row's own lap took a second time from a checkout three commits behind -- which is
the limit its last clause names.

---

**REDS %706 (`20260911.034352`) -- the card told eight ships the ledger was walled while the ledger's own instrument read a door.** *What went wrong:* clause (2) of the INCENSE row on `construction/ITINERARY.md` read *"the REDS pin holds ~8 bytes with every row OPEN, so no new red can be booked until the bound rises on your word"*, true when a hand wrote it at `20260910.070937`. `%701` accreted to **BOOKED** sixteen hours later, so `tools/fixtures/r/reds_pin_capacity_scan.sh` read `pin_deadlocked=0`, `pin_foldable_rows=1`, `pin_headroom=0` -- its own *one fold away from healthy* cell, repairable by whichever lap meets it, in one command, with no word from anyone. Nothing carried that to the card, and the card is the page every lap of every ship reads WHOLE to choose its work, so reds-first was told it had no landing place by the page it reads first. **A peer had already taken the fold at `20260911.004500`** -- `%701` and `%704` onto one shelf, that shelf's own header naming the same ceiling -- and the claim survived the repair that disproved it, which is the fault at its plainest. *What caught it:* the air rota lap, whose instruction is *a claim of a boundary is tested by pressing on it*; the hand went through. *What it taught:* **a page spelling a derived reading is a second spelling, and when the two disagree the prose wins, because the card is read first and read whole.** SECOND FIRING: on `20260910.140033` that scan's own doors line read "no lawful fold exists here" from a state that had passed, and four ships carried "yours, Keaton" onto this same card in one morning. A lantern that fires twice becomes a loom. *Repaired (`20260911.034352`):* the clause struck and pointed at the instrument, and the **Reds first** directive widened to say capacity comes from the scan. The loom is `tools/ca/card_pin_claim_witness.rish` over `tools/fixtures/c/card_pin_claim_scan.sh`, which **quotes** `pin_deadlocked` rather than recomputing it -- a third spelling would be this fault in the guard's clothes -- and gates `claim_disagreements` at zero both ways: a claim over a door refuses, the same words over a walled pin walk free. A line naming either instrument reads as a citation. **28 pen cases**, refusals planted then lifted, two mutations bitten. *And one limit named by this lap's own duplicate fold:* the capacity scan reads LOCAL bytes, so on a fleet `pin_foldable_rows=1` can name a row a peer has already shelved -- this lap folded `%701` a second time from a checkout three commits behind and dropped its shelf on the rebase. The baton's ABSENCE block is the answer, and the reading is not yet wired to it. **CLOSED.**
