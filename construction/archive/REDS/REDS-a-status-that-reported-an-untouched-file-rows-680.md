# REDS -- a status that reported an untouched file, row 680

**Language:** EN
**Style:** Gauge at Meter
**Voice:** Kyri
**Status:** Fold shelf -- immutable once written
**Room:** Checkable -- the row names where it was caught
**Folded:** `20260916.101500` from [`../REDS.md`](../../REDS.md), the oldest CLOSED row on the pin,
to make room for two rows booked the same morning.

**What it taught.** `mantra status` answered that a file had changed where nothing had touched it,
which is the shape a reader trusts least once they have met it: a report that cries out is a report
that gets read past. The row stands closed; this shelf keeps its three fields where a reader can
still reach them.

---


**REDS %680 (`20260910.001500`) -- `mantra status` reports a change on a file nothing has touched.** *What went wrong:* store `a\nb\nc`, edit the middle line to `a\nB\nc`, run `mantra add`, then `mantra status` on the untouched file. It answers **1 added, 1 removed, the same text `B` on both lines** -- a diff describing a MOVE. The replacement lands in **run 1**, and `Place.less_than` reads the run ahead of the position, so it sorts after every run-0 line and leaves the document. A pure append reads clean, which is why it stood: the shape needs a replacement or insert. *What caught it:* the CLI record round pressing on the boundary its own change sits behind -- the air rota's touch test, run against the new binary and one built from the commit before it. **Both answer identically**, so the wide-record writer is parity and this red is older than it. *What it taught:* **every guard in the weave family reads a function, and none read the document the CLI hands back.** Six witnesses stood GREEN over a store that cannot return what it was given. `mantra_cli_record` reads bytes off disk now, and names this refusal in its own header. *Not repaired:* `v1_run` sorts a later hand's block whole **on purpose**, so merge keeps a branch together rather than interleaving it at a site tie. Document order and merge order want different readings of the same triple, which is a design seam rather than a mechanical fix. *Read `20260915.224705`, and a second comparator is the wrong repair -- the module says so itself.* `Place.less_than` sorts run, then site, then pos, which keeps a hand's block together for merge. A document view wants pos first, so the obvious fix is a second ordering beside it. Its own docstring forbids exactly that: *an order spelled at each call site is an order those call sites may come to disagree about, which for a merge would mean two sides computing two documents from one history* -- and two orderings would make the document a merge COMPUTES differ from the one a view SHOWS, which is that fault wearing a feature's clothes. **So the question moves one step back.** The replacement leaves the document because `apply` gives it a NEW RUN, and run sorts first; the ordering is doing what it was built to do. Whether a replacement should take a new run at all, or inherit the place of the line it replaces, is a question about how places are ASSIGNED rather than how they sort. *Keaton's word taken (`20260916.000929`): a replacement inherits the place of the line it replaces.* Measured on metal rather than reasoned, and the ruling turns out to need three things, two of which are now proven and one of which is the remaining work. **First, a replacement cannot inherit the place WHOLE.** `LineId` is `(pos, site)` and `Place` is `(run, site, pos)`, so the identity is a SUBSET of the place -- probed directly, an inherited place answers `eq` **true** against the line it replaced, which is the collision the weave forbids. One component must stay fresh. **Second, a fresh SITE is the only candidate and it does not order correctly today**, because site sorts ahead of pos: replacing the middle of `a b c` puts the new line after `c` rather than after `b` -- probed, `B.less_than(c)` reads **false**. **Third, swapping the order to `(run, pos, site)` fixes it and changes nothing that exists.** Probed both ways: the replacement sorts before `c` under the swap and not under the current order, and across 200 pairs drawn from one-site-per-run data the two orders **disagree on zero**. That premise is structural rather than lucky -- `apply` reads `self.next_run` once and `diff.site` once per call, so every run the weave holds carries exactly one site and the site tie is never exercised. *The remaining work, named exactly:* **`Diff` says WHAT was inserted and WHICH identities were deleted, and nothing links a text to the identity it replaces.** `inserts` is a list of text and `deletes` a list of `LineId`, so `apply` cannot tell a replacement from an insert beside a delete. Pairing them is a change to the diff's own type, and building the ordering half without it would be dead code. *CLOSED (`20260916.061223`), by building the v3 record on Keaton's word.* The probes above said a line needs an order key apart from its identity, and it does: **`Line` and `Place` carry `ord`**, the comparison reads `run`, `site`, `ord`, then the position as a final tiebreak, and every line written before this field carries `ord == pos` so both elder readers lift unchanged. **`Diff` carries `replaces`** beside `inserts`, one entry each, computed by gap: within a stretch between two kept lines, the nth departure is answered by the nth arrival, which is how a diff tool decides a hunk is a replacement. **`apply` inherits the run AND the order key** from the line a replacement names, keeps a fresh position so the identity stays unique, and **inserts the line at its place** rather than appending -- the backing list is held in place order and an inherited place lands in the middle. **`to_v3`/`from_v3` and `read_v3_record` carry the order key to disk**, because a v2 row has none and a replacement written through v2 would read back at the end of its document. *Proven on metal:* store `a b c`, edit the middle line, re-add -- the untouched file reads **clean**, where it reported `1 added, 1 removed, the same text on both lines`. All **29** mantra witnesses green, from a green baseline. *A design error of mine that a witness caught:* the first draft read `ord` ahead of `site`, and the merge witness refused it inside the hour -- two branches from one base draw the same positions and so the same `ord`, and ordering by it first alternates their paragraphs. My probe had compared one-site-per-run data, which is the one shape a merge never makes. *Five mirrors moved with the change, each refusing on the lap it arrived:* two gate desks, two shape placards, and the module head. *Three plants were re-aimed* at the reader the CLI now writes for, one of them caught by `plant_matched_nothing` -- `%519`'s guard working. **CLOSED.**
