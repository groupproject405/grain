# REDS -- the explanation that could not be built was the one describing the failure

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri
**Folded:** `20260907.234825`
**Row:** `%597` (`20260907.234825`) -- CLOSED

The row folds on the lap it was booked, for the reason its two predecessors record: the living pin
stood at its bound with twelve of its rows reading OPEN, so `reds_fold.sh` may move almost none of
them and a finished row goes where finished rows belong.


**REDS %597 (`20260907.234825`) -- a guard reddened on a tree its own scan called green, because the reading outgrew the buffer that composes a sentence.** *What went wrong:* `tools/r/reds_spine_derive_witness.rish` printed its scan with `say "reds-spine-derive: the reading on this tree --\n${scan.out}"`. Rishi composes an interpolated string in a `StrBuf` bounded at **4,096 bytes** (`rishi/src/main.rye`, `bytes: [4096]u8`), and returns `StringTooLong` past it. Measured `20260907.232343`: `tools/fixtures/r/reds_spine_derive_scan.sh` printed **4,250 bytes** -- 154 past the bound -- once this tree's ledger reached **42 stamp duplicates and 2 published doubles**, each printed as its own `detail:` line. The scan read `verdict=ok`. The witness read RED. *What caught it:* this lap's hot roster pass, where `reds_spine_derive red 12s` stood beside two REDS guards genuinely red on a peer's double-bound `%592`, so the evidence file had to be opened one at a time rather than read as one cause. Its first three lines were the witness's own header, and the fourth was `rishi: line 46: StringTooLong`. *What it taught:* **a guard that reds because it succeeded at reporting is a guard ships learn to read past**, and this one is on the roster of eight. The sharper half is one line above: the same composition sat in the `else` clause of the `verdict=ok` assert, so **the one sentence that could not be built was the one describing a real disagreement** -- an explanation that fails exactly when it is needed, and which no green run would ever exercise. The bound itself is correct and TAME asks for it by name; what was missing is that a witness printing an unbounded reading through a bounded composer has named no maximum at all. *Repaired (`20260907.234825`):* the reading is said **before** it is judged, as `say "reds-spine-derive: the reading on this tree --"` then a bare `say scan.out`, which writes the value straight out and opens no `StrBuf`; the assert then carries a short, always-composable sentence pointing at the reading above it. The witness is **GREEN** on metal with the same scan output that reddened it, `next_free=596` and `published_doubles=2` printed whole. *Not taken, and named rather than swept:* every other witness interpolating a scan's whole output carries the same latent bound, and finding them is a census rather than a repair -- a lap's worth of work, and the meter is the interesting half. `reds_ledger_monotone` and `reds_row_present` stay RED on `%592` bound to two stamps and `%512` standing on two shelves, both arriving from peers in this lap's rebase and both **published**, which `derived-spine` rule 3 leaves to Keaton -- so two guards gate the class a third one deliberately does not. **CLOSED** on `tools/r/reds_spine_derive_witness.rish` GREEN.
