# REDS -- a lantern that fired twice

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its reading proven by a control on real git repositories
**Folded:** `20260925.130653` from [`../REDS.md`](../../REDS.md)

One row, folded to make room on a pin at its bound (10 bytes of headroom) the moment a fresh
disk-headroom repair needed to land.

It teaches that a finding recorded only in prose reaches only the reader who happens to read that
prose, and that a lantern firing twice is the measurement a first firing's "declined to build"
lacked. The loom it names is built and proven; the row closes carrying no open half.

**REDS %795 (`20260916.220730`) -- the lantern fired twice in forty minutes, from a second hand,
while its own row stood OPEN.** *What went wrong:* commit `00493b3be` on `20260916.214715`, whose
subject and body describe a perturbation probe resolving a guard's instrument by role, replaced
item **7** of the INNER LOOP in `construction/ITINERARY.md` -- **Prove on touch**, the directive
telling every lap to run the cold endurance run before work and the hot one after staging -- with a
`**Git nib:**` line. That is `%789` exactly: same page, same list, same item, same replacement,
forty minutes after `db4ae9f0b` restored it and while `%789` stood OPEN on the pin naming the class
in plain words. The card again carried the nib twice and the list again read 6, nib, 8. *What
caught it:* a hand setting this lap's nib with
`sed -i 's/^\*\*Git nib:\*\* \`[0-9a-f]*\`/.../'`, which reported **two** substitutions where the
card's own third mention says the fact is *named once above ... one fact in one place*.
`git show ca5f356a8:construction/ITINERARY.md | grep -n '^7\.'` against the same read at HEAD named
the missing item, and `git log -S 'Prove on touch'` named the commit. Every standing guard was
green through both firings, as `%789` had already predicted: `nib_honesty` reads the nib BY ITS KEY
and finds it whichever line carries it, `ascii_document`, `tracked_link` and the pin bound read
bytes rather than meaning, and the roster's whole 316-guard pass says nothing about a list. *What
it taught:* **a red named and left unbuilt is a red that fires again, and the second firing is the
measurement the first one lacked.** `%789` closed by naming the cure -- a guard holding every
numbered list in a living pin to `1..N` with no gaps -- and declined to build it on the stated
ground that the lantern had fired once. The interval between firings was **forty minutes**, across
two seats, on the one page an eight-ship fleet reads at every lap open, and the second hand had
every reason to have read the row: it stood OPEN, on that same page, two screens above the list it
then ate. So the rule this tree already carries -- *a lantern that fires twice becomes a loom* -- is
not a counting rule about lanterns but a rule about REACH: a finding that lives only in prose reaches
the reader who happens to read that prose, and the class recurs at whatever rate the fleet's hands
turn over. The loom is claimed as `bakery-living-pin-list-spine` and **built on this lap**:
[`../../../tools/i/itinerary_list_witness.rish`](../../../tools/i/itinerary_list_witness.rish) over
[`../../../tools/fixtures/i/itinerary_list_scan.sh`](../../../tools/fixtures/i/itinerary_list_scan.sh)
holds every ordered run in the seven pins of `tools/fixtures/l/living_pin_guard_roster.txt` to
`1..N` -- no gap, no repeat, no foreign line between two items -- at **zero, a wall**, since the
population was measured at zero on the repairing lap. A run is keyed by indent and held on a
stack, so a nested list is its own run and its parent RESUMES after it; the first reading closed
the parent on every indent change and read an ordinary nested list as broken, which three control
legs caught. **28 legs** stand in
[`../../../tools/fixtures/i/itinerary_list_control.sh`](../../../tools/fixtures/i/itinerary_list_control.sh),
every refusal planted and then lifted, the control's own leg tally asserted beside its verdict --
and the two strongest are not plants: **both firings replay out of this repository's own history**,
`5f96df5e1` and `00493b3be`, each with its own parent commit welcomed beside it. Two mutations bite,
and the first one written did NOT: with the foreign-line unwind deleted, `1,2,3,STOLEN,5` still
fails the ascending check, so the unwind's real job is the other direction -- it keeps two separate
`1..N` lists from reading as one run of `1,2,1,2`, a false refusal on ordinary writing. A plant aimed
at the wrong clause proves nothing, and only running it says which. **CLOSED.**
