# REDS -- rows %636, %678, %702, %740, %741, folded from the living pin

**Folded:** `20260916.001000` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../../.claude/rules/reds-first.md)

*Five rows about two readings that had to agree.* Two gates that could not both be green over a
parked lap; a ceiling declared in hits and enforced in bytes; a whole-index commit that reverted a
peer's repair; a refusal whose reason vanished at four thousand bytes; and a scan that reached its
helper by the wrong depth.

Each closed or booked by making the two readings one. The four parked records turned out to claim
each other, so landing them as a set satisfied both gates at once. The hit ceiling is derived from
the payload now, so the count and the bytes cannot disagree. The bounded capture fields let a
refusal say its reason and mark where it cut.

The rows stand here exactly as they were written. A resolved row leaves the living pin so the pin
stays the length a reader will actually read; the lesson travels forward in the guards the rounds
built, and the row itself stays one click away.

---


**REDS %636 (`20260908.105623`) -- two gates disagree about a parked lap, so `unlanded` has no lawful closing move for a lap the tree has already healed around.** *What went wrong:* the cold roster answered `stash_record red`, `unlanded=3` -- three round-open stashes holding a session log nothing else carried. Landing those three logs took `unlanded` to `0`, and the hot pass then answered `log_file_claim red`, `verdict=staged_claim_unwritten`, `staged_unwritten=8`, naming the eight paths those logs claim. **A parked lap's `file` fields name the files parked beside it, so the two gates can never both be green over a parked lap while its work stays in the stash.** *What caught it:* the fire rota row read as its own sense -- looking at the parked CODE before restoring it, rather than trusting a stash to be recoverable. All three are superseded, each differently. `stash@{4}` (`20260907.224502`) made `Weave.apply` binary search `self.lines.items`, calling identity order a `Weave` invariant; `8fb075741` (`%599`) repaired the same red eight hours later from a DIFFERENT parked lap, sorting the caller's `diff.deletes` into a scratch slice and walking the weave once -- because `merge` returns its union in **place** order (`bdb20178b`), which is not identity order. Applying the parked version now would binary search a list a merged weave leaves unsorted, and nothing here would catch it: `git merge-file` against `stash@{4}^` returned four conflicts whose first two carry the opposite claims in their own comments, where a clean apply prints nothing. `stash@{0}`'s `rish_report_bound` is superseded by `say_compose_bound` (rostered `20260908.014200`, `%592`), reading the same 4,096-byte `StrBuf` hazard off Rishi's own declaration across **2,422 scripts and 3,281 shaped sites**, gating **shares in per mille** where the parked guard narrows to **38 sites across 26 files** under a ceiling on the count -- the shape its successor's own comment rejects, since a count ceiling reds on the most ordinary act on this pier. It was recovered and proven on today's tree first (scan `under_ceiling`, control `pass=15 fail=0`, witness GREEN), so the withdrawal judges duplication rather than health. *What it taught:* **a record and the work it records are one unit, and a gate counting only one of them has no closing move for the case where the other is gone.** `stash_record` gates `unlanded`, which counts logs, and REPORTS `orphans_work` -- 6 paths, ungated -- which counts the code. Where the work still wants landing the gate is right and the move is to land both; where the tree has healed around it there is no move, since `log_file_claim` refuses the log on a rule that is also right and the only remaining act is to **drop the stash**, a cut that stays Keaton's word (`.claude/rules/debride.md`). This is `%626`'s shape read from the other side -- a ceiling no lap can reach -- with `%463` beside it, since `orphans_work` says nothing about the half that decides. *Repaired (`20260908.105623`):* nothing landed and nothing dropped. Three logs staged, refused, withdrawn; the recovered guard family proven green and withdrawn; the parked `Weave.apply` code deliberately not applied, with this row where a future recovery reads why first. *Closed (`20260915.223454`), and neither door was the one that opened.* Nothing was dropped and no guard was rewritten. **The four parked records claimed EACH OTHER.** Reading their own `file` fields against the tree, 13 claims stood and 6 were missing -- and every one of the six was one of these four logs naming another, because each lap in the chain was itself a lap that tried to land the parked records before it. Landed one at a time, every one reds; **landed as a SET, every claim is satisfied**, since each claimed log exists on disk once all four are written. *What made that visible:* `log_file_claim`'s `exists()` accepts a claimed path present on disk OR tracked, and never required it to be staged in the same commit -- so the deadlock this row recorded was an artifact of landing them SINGLY rather than a disagreement between the two gates at all. *Measured on metal:* the four restored from `refs/stash^3`, their four rows added to the `20260914` shelf, and then **`stash_record` reads `unlanded=0 verdict=ok` while `log_file_claim` stands GREEN beside it** -- the first reading where both are green together. `index_row_bound` GREEN. **CLOSED.**

**REDS %678 (`20260909.213236`) -- the query wire declares a ceiling in hits and enforces one in bytes.** *What went wrong:* `mantra/recall_tablecloth_query_wire.rye` publishes `max_wire_hits = 8` beside `max_wire_payload = 340`, with nothing tying the two. At `recall_lap1.rye`'s name ceilings one hit encodes to 119 bytes, so `build_response` returns **3 hits** inside the declared 8 and `encode_response` refuses them with `error.Overflow` on the next call. *What caught it:* a census of all 31 query shapes, built to ask how large an answer can be. *What it taught:* **a count that bounds a variable-length thing bounds nothing.** *Closed (`20260915.224250`), by the door the module's own comment named.* `max_wire_hits` is **derived** rather than declared: `max_peer`, `max_bolt` and `max_path` are named constants, `max_hit_bytes` is their sum with the length bytes and the revision, and the ceiling is `(max_wire_payload - response_head_bytes) / max_hit_bytes` -- **2**, which is what the encoder always kept. Two comptime asserts tie it: every hit the ceiling admits fits the payload, and the ceiling admits at least one. The two literal length checks now read the constants. *The census flipped from proving the defect to proving the agreement:* it filled a catalog to **3** and watched `encode_response` refuse; it fills to the ceiling now and asserts the whole response encodes, and its printed claim moved from *the declared hit ceiling is not the wire's bound* to *IS the wire's bound, by arithmetic*. *And a missing invariant surfaced while re-aiming the control's mutation:* the census claimed to measure a WORST case using its own literal name lengths, and nothing checked those equalled the format's ceilings -- a census measuring a comfortable case and calling it the hard one. `worst_peer_len == qw.max_peer` and `worst_bolt_len == qw.max_bolt` now assert it, and the names plant bites on that. Control **21 legs, 0 fail**; census, query-wire and delivery witnesses GREEN. *Not reached:* whether a query matching MORE than the ceiling should truncate and say so rather than refuse. That widens the record format, which is doors 2 and 3, and it is a different question from the one this row booked. **CLOSED.**

**REDS %702 (`20260911.000651`) -- a whole-index commit reverted a peer's repair.** *What went wrong:* `84a0ebc26` rewrote `README.md`'s Style line back to its pre-`570526cad9` text from a stale tree. *What caught it:* `prose_register red`, `door_setting_undeclared=1` -- the guard `570526cad9` built beside that clause. *What it taught:* **a revert and an edit are one shape in a diff.** Read `active-designing/date/20260911/20260911-000651_the-front-door-that-named-no-ceiling.md`. *Read `20260915.175200`:* the instance stands repaired -- `README.md`'s Style line names the Door setting and `prose_register` answers `door_setting_undeclared=0`, `verdict=ok`. The remainder is the CLASS -- a revert and an edit are one shape in a diff -- which no instrument in this tree reads today. **BOOKED.**

**REDS %740 (`20260915.211125`) -- a refusal's reason is dropped in silence at 4,096 bytes, and the placeholder is printed instead.** *What went wrong:* `assert x.ok else "... ${x.out}"` composes the capture into the message, and past a bound the composition **silently emits the literal text `${x.out}`** rather than the value, a truncation, or a named refusal. Bisected on metal: 4,000 interpolates, 4,090 interpolates, **4,096 interpolates, 4,100 does not** -- Rishi's 4,096-byte `StrBuf`, the same bound `%592` found for `say`. *What caught it:* capturing a caravan subscribe-poll failure. Two rounds reported the reason as the literal `${test.out}`, and a probe against output size found the cliff. *What it taught:* **the reason is lost exactly when there is most of it.** A short compiler error arrives whole; a selftest that printed pages arrives as a placeholder, so the diagnostic-rich case is the one that fails, and it fails looking like an assert that never interpolated at all. *The tension this sits in, named rather than resolved:* `%700`'s own repair made interpolating a capture into an assert message law -- *the whole difference between a flaky guard a hand can diagnose and one that can only be inferred* -- while `tools/fixtures/s/say_compose_bound_scan.sh` counts every such site as **deferred** risk for this exact reason, and reads **551 per mille against a ceiling of 489**, already breached before this lap touched anything. Both are right. *Withdrawn this lap:* 164 asserts across 106 files in `tools/ca/` were given their reason and then reverted, because they took that breached ratchet 551 to 566, and a rostered ratchet only falls. *Doors:* a bounded slice of the capture rather than the whole of it, a composition that refuses by name past its bound, or a larger bound. Each is a Rishi decision. *Door taken (`20260915.212102`), the bounded slice, on Keaton's word.* `rishi/src/main.rye` gains two fields beside the whole captures rather than replacing them -- **`out_brief`** and **`err_brief`**, each bounded by a named `max_brief_bytes` of **512** and **marked where they cut**: `... [N more bytes]` and `[N earlier bytes] ...`, so a reader can tell a short reason from a cut one. **512 because the composed message must fit 4,096 WITH its prose and a second capture beside it**; eight briefs still fit. *Head for `err`, tail for `out`, and the asymmetry is the point:* a compiler names its error first and elaborates after, while a program's last words are its reason -- both real cases from the captures that opened this row. *Proven:* 13 behaviors in a throwaway pen under `tools/r/rishi_brief_witness.rish`, every leg from BOTH sides, since a brief proven only on long input cannot be told from a field that always truncates -- **and the cliff itself is a leg**, so a tree that forgot it would red rather than drift back to composing whole captures. The interpreter was built to a pen, run against seven standing witnesses GREEN, and only then installed. *What this does not reach:* the standing `say_compose_bound` breach at **551 per mille against 489**, which predates this lap and reads exactly as it did before it -- the briefs give that ratchet a way DOWN, since a site composing a brief is bounded by construction, yet no site was swept on this lap. *Swept (`20260915.213251`), and the breach is closed.* **2,773 sites across 979 files** moved from composing a whole capture in an `assert ... else` message to composing its brief, taking `deferred` **2,576 to 0** and `deferred_per_mille` **550 to 0** against a ceiling now lowered **489 to 0** -- a wall, so a new whole-capture refusal reds on the lap it lands. *The meter was taught BEFORE the sweep, and the order mattered:* a brief matched none of the three classes, so every converted site would have fallen out of `shaped`, the denominator, and shrunk numerator and denominator together -- the exact fault this scan already records finding in 155 safe sites. A fourth class, **`bounded`**, counts them and keeps them inside `shaped`; seven control cases hold it, taking that control to **48 with none red**, including one proving a whole capture BESIDE a brief still reads deferred, so the widening is no blanket amnesty. *The eager class is deliberately untouched at 524:* a `say` line composing a capture is often a REPORT rather than a refusal, and other guards assert on those words, so truncating one changes behavior rather than register. *A stale interpreter is the one hazard, and it is loud rather than silent:* `rishi/bin/` is gitignored, so a ship that pulls these sources without rebuilding gets the literal placeholder -- the same benign fallback an absent field always gives, no crash -- and the rostered `rishi_brief` guard reds there and names the rebuild. *The eager class swept too (`20260915.214513`), on Keaton's word.* **527 sites across 467 files** moved from composing a whole capture in a `say` report to composing its brief, taking `eager` **524 to 0** and its ceiling **145 to 0**. Both classes now stand walled at zero, `bounded` reads **3,102**, and the whole composition surface of this tree is bounded by construction. *The worry that held it back was measured rather than argued:* a report shortened where another guard asserts on its words changes BEHAVIOR. Read in context, the pattern is a selftest written to a FILE, the file said for a human, and the assertion grepping the FILE -- so the say carries nothing structural, and the tail a brief keeps is where GREEN lives anyway. *Proven by running rather than reading:* 55 witnesses spanning the eager sites AND the guards that run other guards, **0 red before and 0 after**; then 50 more chosen at random from the swept set, **2 red, both proven pre-existing** by running the committed version of each -- an absent emulator and an absent Android toolchain, which is `%646`'s class rather than this one. *And the condition case was checked, since truncation there would change LOGIC rather than wording:* zero conditions were swept, matching the scan's own note that the case reads zero. **BOOKED**.

**REDS %741 (`20260915.200210`) -- the page whose whole thesis is that a typed number goes stale typed three of them, and a generator re-typed them on every run.** *What went wrong:* `docs-geode/libraries/README.md` opens by saying an index keeps its truth for exactly as long as it is counted off the tree rather than typed. Three paragraphs down, its own prose spelled **463** modules for `image` and `lotus` together, **1,770** witnesses for `rishi` against **98 percent** of the tree, and **1,313** for `rye` -- three free figures carrying no stamp, no source, and nothing holding them still. Measured on metal this lap by the scan's own method: the path test answers **1,948** for `rishi` and **1,356** for `rye` against **1,985** witnesses. Two of the three had drifted; only the percentage held. *What caught it:* the QA report card, run over every front door on this shelf as an earth-row reading of the concrete fact at the door. This page graded **B/83** -- the lowest door in the room, Reach **70** at grade **12** against a Door budget of **9** -- and the 90-word sentence that cost it the grade was the same sentence carrying all three figures. *What it taught:* **a free figure inside a generator is worse than a free figure on a page**, and that is the sharper half of `%735`'s class. A typed number on a page goes stale once and sits there; a typed number inside `tools/fixtures/g/geode_libraries_scan.sh` is re-asserted by every regeneration, arriving in the reader's hands with a generated page's authority behind it. The freshness witness cannot see it either: `tools/g/geode_libraries_witness.rish` holds the page equal to a fresh render, and a stale figure baked into the renderer is identical in both. So the guard was GREEN the whole time, correctly. *Repaired (`20260915.200210`):* the heredoc prose in `tools/fixtures/g/geode_libraries_scan.sh` describes both elder readings rather than spelling them, names them **free**, and prints the `find`-and-`grep` pipeline that re-derives one; `image` and `lotus` are pointed at the generated table below rather than summed into prose. The page was regenerated from the scan, `geode_libraries_witness` is GREEN on metal, and the door read **B/83 to A/91** -- register **86 to 95**, Reach **70 to 100**, grade **12 to 9**. The dated `20260905` measurement inside the scan's own code comment keeps every word: it names its stamp, so it is testimony rather than a claim. **BOOKED** -- the instance is repaired and the remainder is a class: no instrument in this tree reads a free figure out of a generator's own prose, and building one is a lap this row does not open.
