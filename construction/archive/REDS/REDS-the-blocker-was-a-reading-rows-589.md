# REDS -- the blocker was a reading

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri
**Folded:** `20260908.151057`
**Rows:** `%589` (`20260907.210124`) -- BOOKED

The row folds on the lap that booked it, for the reason every shelf beside it records: the living
pin stands at its bound, every other row on it reads OPEN to another ship, and a finished row goes
where finished rows belong while the pin keeps a pointer.

What it holds is a guard that arrived after its own predicted hazard, and a door that was never
locked. `mantra/src/main.rye` declared its own copy of Mantra's weave model, the module widened
past it, and the sentence that kept the two apart for a day was *reconciling the CLI rewrites a
shipped on-disk record*. That sentence was a claim about reading wearing the clothes of a claim
about the record, and the way it fell was not an argument. Two binaries were run through the same
six edits and their content-addressed digests were compared.


**REDS %589 (`20260907.210124`) -- the module's model widened, two copies did not move, and the guard written for exactly that sat asleep in a stash.** *What went wrong:* `bc37657e8` widened Mantra's weave model in `mantra/src/weave.rye` -- `Line` gained a `site` field and `Diff` gained `site`, so a line is named by the pair `Line.id()` returns. Neither of the two other tracked files declaring the same triple moved. `mantra/src/main.rye`, the CLI that actually writes `.mantra/`, still declares `Line` as `text gen pos` and `Diff` as `inserts deletes`; `rye/tests/mantra_weave_test.rye` inlines that elder copy. `tools/m/mantra_glow_tend_limb1_witness.rish` -- the guard `construction/ITINERARY.md` names as THE LOCK on widening `Line` -- reads one file, so the widening reddened nothing anywhere. *What caught it:* `tools/m/mantra_weave_model_witness.rish`, written on `20260906` measuring all three copies in agreement and then parked in a round-open stash rather than landed (`%499`). Recovered with `git checkout stash@{7}^3 -- <paths>` and run on metal, its first live pass answered `disagreements=1 unreadable=1`. *What it taught:* **a guard parked is a guard whose own predicted hazard can arrive unheard** -- and that hazard was written in its header, in words, the day before it came true. *Landed (`20260907.210124`):* the rung stands rostered with `disagreements` under a ceiling that only falls rather than a zero no lap may reach, since reconciling `main.rye` rewrites a shipped on-disk record and widening the model stays a seam at Keaton's word; the test's single-line declaration is spread so the reader can check it, `unreadable` gated at zero, and the ceiling proven from both sides on one break in the pen. *Half repaired (`20260908.151057`), and the blocker above was false:* `mantra/src/main.rye` imports `mantra/src/weave.rye` now rather than declaring its own `Line`, `Diff` and `Weave` -- `compute_diff` names deletes by `Line.id()`, `serialize_weave` writes `Weave.to_v1`'s rows, `deserialize_weave` parses rows and hands them to `Weave.from_v1`, and `cmd_status` matches a delete by identity. The clause *reconciling `main.rye` rewrites a shipped on-disk record* was the reading that held this shut, and it is **wrong**: the elder binary and the ported one were each run through the same six edits -- insert, delete, append, prepend, empty the file, refill it -- and every content-addressed digest matched, twelve commit and weave names and all thirteen store files. Under a content-addressed store a matching digest IS a byte-identical record, which is the strongest answer available to *does the port move the record*. `rw1_history_contract` and `slc1_accept` both build and exercise the ported CLI and both read GREEN. The copy count fell 2 to 1 and `DISAGREE_CEILING` fell with it, which the scan's own comment names as the cure it welcomes -- deleting a copy rather than reconciling one. *What the metal added past the two v1 guards:* they proved the round trip over a weave built in a test; this proved it over the records the shipped CLI actually writes, through the store, across a binary boundary. **BOOKED** -- the instance that shipped is repaired, and the remainder is one ratcheted copy: `rye/tests/mantra_weave_test.rye` still inlines the elder declaration, standing under a ceiling that only falls, and porting it is its own lap.
