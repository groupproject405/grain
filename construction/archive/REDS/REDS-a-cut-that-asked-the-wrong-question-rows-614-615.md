# REDS -- a cut that asked the wrong question, and a guard that froze a moving room

**Language:** EN
**Status:** Shelf -- two folded rows from `construction/REDS.md`, kept whole
**Voice:** Kyri
**Folded:** `20260908.053400`
**Rows:** `%614` (`20260908.052314`) - `%615` (`20260908.052600`) -- both CLOSED

Both rows fold on the lap they were booked, for the reason the shelf beside this one records: the
living pin stood at **39,316 bytes against a bound of 40,960** when this lap opened, and the two
rows below take it past. Twelve of its fourteen rows read OPEN, so `reds_fold.sh` may move almost
none of them; a finished row goes where finished rows belong and the pin keeps a pointer.

**What the two taught together.** One cadence choir surfaced both, and each is a guard that answered
a question narrower than the one it was trusted for. A shred verified *zero operational citers* and
never asked who merely needed the file to **exist** -- while its own prep row had already written
the three names down. A dialect guard gated an exact split, `10` bare dates against `1` full stamp,
over a room its own counsel had described as **migrating** -- so the first page to migrate reddened
the guard that recorded the migration. In both, the tree already held the sentence that would have
prevented the fault, one artifact away from the check that failed.


**REDS %614 (`20260908.052314`) -- the cut asked what CALLS a file, and its own prep row had already named three things that only needed it to EXIST.** *What went wrong:* `tools/fixtures/d/dated_classify.py` was **CUT `20260830.190407`** on the circled word, and `construction/SHRED_PREP.md` records the cut as *zero operational citers re-verified*. Three tracked scans named it anyway, each as an existence check rather than a call: `tools/fixtures/e/equinox_e116_dated_one_definition_scan.sh` in its `CLASSIFY` constant, in two `rg` patterns asserting both roofs source the shared classifier, and in its own `shared_classify=` output line; `equinox_e117_fork_extend_breach_close_scan.sh` and `equinox_e118_metal_corrections_scan.sh` in `CLASSIFY`, read by `git ls-files --error-unmatch`. All three answered `instrument=failed detail=control_absent` for eight days. **The prep row authorizing that cut had named all three, by name:** *py kept only for dated equinox e116/e117/e118 existence checks*. *What caught it:* `season_leaf_choir` at `tier cadence` -- so it was audible only because `%568`'s first cadence pass ran, and then only on a lap that read the run ledger rather than a lap-tier pass. e117 and e118 reached the choir as four refusing leaves; **e116 is red by the same root, sits in no leaf room, and nothing names it at all**. *What it taught:* **a file that is only required to exist has citers no call-graph sweep will find** -- and the sentence that would have caught it was already written, in the ledger that authorized the cut. *Repaired (`20260908.052314`):* each constant repoints to the living mutant `tools/fixtures/d/dated_classify.rish`, seated `20260809` with the same `census`/`classify`/`health`/`shed` interface, which both roofs already source -- so e116's leg is true again and measures what it always meant. GREEN on metal: e116, e117, e118 scans exit 0, and `season_leaf_choir` sings **33 of 33, red=0**. **CLOSED**

**REDS %615 (`20260908.052600`) -- a guard froze the split of a room its own counsel had called a migration.** *What went wrong:* `tools/fixtures/e/equinox_e133_fuse_verify_dialect_scan.sh` gated `bare_date=10` and `full_stamp=1` as exact equalities over the eleven `context/*.md` law pages carrying a `**Last updated:**` header. e133's own finding was that `context/LEXICON.md` is the **first of eleven** to take a full stamp, and that the compact roof reads a bare date and a full stamp under one pattern -- a migration, named as one. `context/TWO_ROOMS.md` then took a full stamp, the split read 9 + 2, and the leaf refused inside `season_leaf_choir`. **The population never moved: eleven pages before, eleven after.** *What caught it:* the same cadence choir, on the same lap as `%614`. *What it taught:* **a guard that gates a snapshot of a moving population reds on the movement it recorded** -- the ratchet-at-its-ceiling shape, one artifact over, where the ceiling is an equality and honest work is what breaks it. *Repaired (`20260908.052600`):* the leg gates what the counsel actually claimed and what honest work cannot reverse -- **at least one law page carries a full stamp, and the Lexicon is among them** -- and prints `bare_date`, `full_stamp`, `law_pages` and `census_gate=direction_not_split` as diagnosis. Every other leg is untouched, so the guard still bites the e132 widen pattern in both dialect scans, e111 green, the counsel's own language, the shred held RED, and seat 128 reserved. The witness asserts the direction rather than the split, and its `story=` line no longer recites `10_bare_1_stamp`. **CLOSED**
