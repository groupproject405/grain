# REDS -- row %768, folded from the living pin

**Folded:** `20260916.092827` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../../.claude/rules/reds-first.md)

*One row that booked a ratchet's three-hundred-step jump as growth in the tree, and closed on the
finding that the tree had only changed its spelling.* The reading's awk pattern was anchored at a
closing brace; one commit swept 986 tracked sources onto a longer field name that reports just as
truly, and the pattern could not follow.

What the row leaves behind is a test worth keeping. Asked whether the instrument had moved, the
first reading ran the elder scan against today's tree and got the same answer -- which proves the
instrument's BYTES unchanged and says nothing at all about its REACH. A ratchet's number is a
finding only once the predicate is proven to still reach the thing it names.


**REDS %768 (`20260916.074727`) -- a ratchet gained three hundred in a day, and the instrument that reads it never moved.** *What went wrong:* `shim_reason` holds two ratchets over bindings asserted on without reporting their capture, and both stand breached -- `unsaid_rostered` **1235** against a ceiling of **922**, and `unsaid_unrostered` **7768** against **6157**. Both ceilings were set on `20260914` by `95d1e5ae7`. *What caught it:* the cold endurance run of the lap that found it. It is red on every ship and it stood in no ledger row. *What it taught:* **a ratchet's number is only a finding once the instrument is proven still.** The obvious reading of 1235 against 922 is that three hundred swallowing asserts arrived, and the obvious alternative is that the scan widened and is reporting its own new reach as a regression -- which this tree has booked before. So the reading was taken twice: `git show 95d1e5ae7:tools/fixtures/s/shim_reason_scan.sh` run against that tree answers **1235 / 7768**, character for character what the living scan answered. **The instrument is unchanged and the growth is the tree's.** *And it is fast:* the session log of `20260915.225740` records this family at `unsaid_rostered=923` against 922 -- one over, from a peer's newest guard -- so **312 arrived in under a day**, against a family that had crept by ones. *What holds these figures still:* the two ceilings are **walled** and reading red; the counts are **free**, and free in the rising direction -- re-read on metal at `20260916.083946` they answer **1239** and **7755**, so the rostered half gained four more in the two hours this row spent parked. Run `sh tools/fixtures/s/shim_reason_scan.sh | grep '^unsaid'` rather than trusting either pair. *Not repaired:* giving an assert its reason raises the `say_compose_bound` ratchet in the same move, which `%740` already names, so the price belongs to the lane that owns that tension rather than to a lap passing through. **OPEN** -- the door is a reading that names which commits carried the 312, since a ratchet breached by one arrival and a ratchet breached by three hundred want different cures. *Erratum and closure (`20260916.092253`) -- the growth was never the tree's, and the test that said so could not have seen otherwise.* The reported-capture reading in `tools/fixtures/s/shim_reason_scan.sh` opens on `${v.out}` and `${v.err}` **anchored at the closing brace**. `6b2965fe7` (`20260915.2121`) added `out_brief` and `err_brief` to the record `run` returns, at `rishi/src/main.rye:1571-1572` -- a bounded tail of stdout and a bounded head of stderr, added so a refusal could carry its reason without blowing `say_compose_bound` (`%740`). Twelve minutes later `106d182b4` (`20260915.2133`) swept **986 tracked `.rish` sources** from the whole captures to the bounded ones. Every swept binding still reports; the pattern stops at the brace and cannot read the longer field name. *Measured with one ruler* -- this checkout's scan and roster over each tree in turn, so only the tree varies: **922** at `9c45d3c62` (1857), 922 at `46afb975b`, 922 at `6334bcb45`, **923** at `80369f61a` (2059), **924** at `6b2965fe7` (2121), **1,240 at `106d182b4`** (2133) with `unsaid_unrostered` **6,157 -> 7,697** in that one commit, 1,247 at `57b6b904f`, 1,252 at `339be29a3`. *Proven from both sides:* the widened pattern reads **922/6,157 before the sweep and 923/6,157 AT the sweep commit**, where the narrow one reads 1,240/7,697 -- so the sweep moves the widened reading by nothing at all. *Why this row's own test could not see it:* running `95d1e5ae7`'s scan against today's tree answers character for character **because both copies carry the same blind pattern**, so that test proves the instrument's BYTES unchanged and says nothing about its REACH. *And the roster was not the cause either:* swapping `95d1e5ae7`'s roster for today's over one tree moves the rostered reading by **8**. *What it taught:* **a meter reads a spelling, and a tree may change its spelling in one commit.** A ratchet's number is a finding only once the predicate is proven to still reach the thing it names, which is one step past *the instrument is unchanged*. *Repaired:* `(out|err)(_brief)?` at three patterns -- the two `${...}` readings and the anchored `^say v.out$` in the late-say pass; the unanchored `say` prefix rule needed nothing, and the late-say widening moves no number today. The reading falls **1,239 -> 903** and **7,755 -> 6,144**, and both ceilings fall to meet them without slack, as every entry in that arc does. Crediting a bounded capture is right on the reading's own terms, since the subject is a binding reported NOWHERE; what a bounded capture cannot promise is that it carries as much as a whole one, and that is the bound's whole purpose. **CLOSED** on `tools/s/shim_reason_witness.rish` GREEN on metal, 132 control legs over 29 repositories in a throwaway pen, the widening proven load-bearing by running the same pen under the pre-repair pattern and watching it count the binding.
