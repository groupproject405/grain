# REDS -- a law read under one of its two names

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its reading proven by a control on real git repositories
**Folded:** `20260917.164713` from [`../REDS.md`](../../REDS.md)

One row, folded the hour it was written to hold the pin under the bound eight ships share. The pin
stood **195 bytes** below its 65,536 when this row arrived and 20 of its 22 rows read OPEN, so the
fold was the only lawful way to land it: a page whose living parts outgrew its number earns a raise,
and a raise is Keaton's word.

It teaches that a law read under one of its two names covers one of its two cases. Rule 5 of
`.claude/rules/remember-git-nib.md` is named *follow-up* and its arithmetic already fitted a commit
landing before the work commit; eleven days passed with nobody reading it that way, because the name
pointed at one end of the lap.

**REDS %815 (`20260917.164713`) -- the claim writer makes two commits a lap and carried the operator card on neither, so a rostered guard read stale through the whole build window on every lap that claimed.** *What went wrong:* `tools/f/fleet_claim.sh` wrote `construction/fleet-claims.kyri` and no other file. The baton's ORDER clause asks a lap opening a new instrument or taking a booked red to check, claim, **push**, then build, so the `--open` lands as its own commit BEFORE the work exists; `--close` lands another after it. Both left `construction/ITINERARY.md` naming HEAD~2, which is none of the three states `tools/r/remember_git_nib_witness.rish` accepts, so the guard read `stale` until a later commit repinned. *What caught it:* two ships, on two cold endurance runs, one day apart -- copal on `20260917.061322` and diffuser on `20260917.164302`, each recording it as a session-log row. **Neither closed it and it stood on NO ledger**: copal handed it to a peer whose live claim was about hash claims in commit bodies, a different subject, and the row was never booked. A finding handed on is a finding nobody holds. *Measured before repairing rather than reasoned,* over this tree's **204 claim-opening commits** put to the guard's own state predicate copied verbatim: **185 stale**, and **184 of those commits whose only file is the board**. Over the last 55 openings, **55 of 55**, standing a mean of **12.7 minutes** and a maximum of **62** across a mean of 1.49 commits. That window is exactly where the ORDER clause puts the cold run, so the fleet's most expensive reading -- 355 guards, held still, bought to say *these answers describe THIS tree* -- carried a red it manufactured itself. *What it taught:* **a law read under one of its two names covers one of its two cases.** Rule 5 of [`.claude/rules/remember-git-nib.md`](../../../.claude/rules/remember-git-nib.md) is named *follow-up* and describes a commit landing AFTER the work commit; its arithmetic -- `HEAD` read before the commit, since that HEAD becomes its parent -- was already exactly right for one landing BEFORE it, and nobody read it that way for eleven days because the name pointed at one end of the lap. So the repair is a widening rather than a new shape, and `lead-in` joins `follow-up` as a **peer name** under [`alias-sameness`](../../../.claude/rules/alias-sameness.md) rather than a third: one implementation, one refusal, two lawful words. **CLOSED** on a witness (`20260917`): `tools/f/fleet_claim.sh` carries the card on `--open` and `--close` through `tools/fixtures/r/remember_git_nib_write.sh`, **only when the board's bytes moved**, so the idempotent no-op open leaves BOTH files byte-identical and the water row's law holds over the pair; it **degrades rather than refuses** when the card, the repository, or the writer is absent, since a claim the fleet cannot read is worse than a card one commit behind. `tools/fixtures/f/fleet_claim_control.sh` stands at **62 legs, 0 failing**, sixteen of them this carry's, its centrepiece landing a board-only commit and reading `stale` from the guard's own predicate before reading `parent` from the carried one -- the red run rather than argued -- with two mutations bitten: a carry above the idempotence exit, and the amend's `HEAD^` in the carry's place. `tools/fixtures/r/remember_git_nib_shape_control.sh` stands at **41 legs**, seven of them proving the peer name cannot disagree with the name it aliases, with a mutation giving it the amend derivation bitten. *What no instrument reaches:* staging. The writer moves two files and never commits, by its own seated law, so a lap that stages the board alone still ships a stale card -- set on the baton, where habits are set.
