# REDS -- a plant that borrowed a contract

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri -- **Room:** checkable
**Folded:** `20260908.053644`
**Row:** `%613` (`20260908.053644`) -- CLOSED, booked `%612` and renumbered once: the anointed spine had spent that number on `20260908.052546` while this row stood unshared, so it derives above it (`derived-spine` rule 3). The key never moved -- it is the stamp.

The row folds on the lap it closed, for the reason its neighbours record one shelf over: the living
pin stands near its bound with almost every remaining row OPEN, and a pin whose living parts fill it
cannot accept a new one.

What the row is for, said once here so a reader arriving from a citation knows before they read.
A test fixture in this tree is usually a file that stands alone -- broken on purpose, run by a
guard, proving a wall refuses it. This one did not stand alone. It shared a filename stem with a
real desk of the language's own room, and the desk runner reads the stem to decide whether a file
may be handed an argument at all. So the plant's ability to be run came from its neighbour, and
the two witnesses asserting on its refusal were resting, without saying so, on a shell `case`
entry written for somebody else.

The reading that would have caught it had been printing since `%539` and gating nothing, because
the collision looked like a question about names -- which of the two files keeps it? -- and that is
a language custody ruling nobody had made. Reading what the collision *cost* is what showed the
pair was never symmetric: one file is a generated desk of the corpus and the other is a hand-written
plant under `tools/fixtures/`. The plant took a clearer name, the desk never moved, and the ruling
turned out to have been standing in front of a repair rather than behind one.


**REDS %613 (`20260908.053644`) -- a malformed plant proved the grammar wall on a run permission granted to a different program in a different room.** *What went wrong:* `tools/fixtures/g/gate-count-u32.glow` is a deliberately broken desk -- a `|=` bartis whose `?:` is missing its else arm -- run by `tools/g/glow_skate_gates_witness.rish` and `tools/t/tally_glow_tend_a1_gate_bound_witness.rish`, each asserting the lowering answers `MalformedBody`. It shared its stem with `glow/gen/g/gate-count-u32.glow`, a valid desk of the language's own room, and `tools/g/glow_run_worker.sh` dispatches on the **stem**: its `case` at line 46 demands exactly one `@u32` sample decimal for that name. So both witnesses passed `"3"`, and had to -- run **bare**, the plant answered `FAIL: gate-count-u32.glow needs exactly one @u32 sample decimal` rather than the refusal it exists to prove. The plant's whole run contract was owned by a file it has nothing to do with. *What caught it:* the air rota row, whose test is to pull one part out and see what moves with it. `stem_collision=2` had been printed by `tools/fixtures/g/glow_desk_reach_scan.sh` since `%539` and gated nothing, on the ground that which file keeps a shared name is a language custody ruling. Reading what the collision actually **cost** is what found the coupling; the count alone had been true and inert for a day. *Proven from both sides on metal, before any repair:* the same bytes copied to a stem the `case` does not name answered `FAIL: only sample-u32 / gate-*-u32 ... take a sample`, and the elder spelling run against a worker with that one `case` entry removed answered the same -- **no `MalformedBody` anywhere**, so both witnesses would red with a message about a grammar wall for a reason living in a shell `case` in another room. *What it taught:* **a fixture that borrows a name borrows a contract.** `%539` read the collision as symmetric and therefore as a ruling -- which file keeps the name? -- and it was never symmetric: one file is a generated desk of the corpus, the other a hand-written plant under `tools/fixtures/`. The cure was subtraction rather than a ruling, and the ruling had been standing in front of it. *Repaired (`20260908.053644`):* the fixture renamed to `tools/fixtures/g/gate-count-malformed.glow`, a stem no `case` names, so it refuses **bare** with `bartis lower failed (MalformedBody)` and depends on nothing -- no argument, no permission, no stranger. Both witnesses drop the borrowed `"3"` and run GREEN; the desk in `glow/gen/` never moved, so no custody question was answered here. `stem_collision` becomes a **ratchet at 1** rather than a bare report: a third collision reds on the lap it arrives, and the ceiling stands at the one pair a lap may not repair -- `sample-demo-fact-line-lits` under `glow/gen/s/` and `linengrow/gen/`, one of the three fourth-kind files `%532` named, whose marker genuinely is a language custody ruling. Ceiling proven from both sides in `tools/fixtures/g/glow_desk_reach_control.sh`: **86 behaviors** (was 81), one shared stem walking at the ceiling, a second refusing by name with a non-zero exit, and the plant lifted back to green. *Not taken:* the 46 sample-taking desks nothing runs still want a value somebody must choose, and where those values live -- a fifth hand-written enumeration, or a marker inside the desk -- is a language custody ruling this lap declines. **CLOSED** on `glow_desk_reach`, `glow_skate_gates` and `tally_glow_tend_a1_gate_bound` GREEN.
