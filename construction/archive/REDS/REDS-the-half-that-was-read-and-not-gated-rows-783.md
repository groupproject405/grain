# REDS -- the half that was read and not gated

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by a control on real files
**Folded:** `20260916.213205` from [`../REDS.md`](../../REDS.md)

One row, folded the day it closed to hold the pin under a bound eight ships share. It teaches that a
guard which READS two values and GATES one has published a fact it does not hold: `declared_model_scan.sh`
printed both the model id and the effort value out of `.claude/settings.json`, gated the id alone, and
four of six living pages quoting that file drifted to a word the file has never read. A printed reading
looks exactly like a held one from the outside, which is why the repair was a second gate rather than a
sharper eye. It stands beside `%782`, folded the same day, which is the same distance measured the other
way -- a guard whose population admitted more than its author meant. Both faults live in the gap between
what an instrument reads and what its author believed it was reading.


**REDS %783 (`20260916.192147`) -- the guard read both halves of one file and gated only the half that never moved.** *What went wrong:* `tools/fixtures/d/declared_model_scan.sh` has read `.claude/settings.json` for BOTH `model` and `effortLevel` since it was written, through `tools/fixtures/d/declared_model.sh`, and printed `declared_model=` and `declared_effort=` on every run. It compared the model against a named roster of three declaring pages and passed the effort value to **no comparison at all**. So the one field it measured and never gated is the one that drifted: `.claude/rules/session-logs.md` said that file configures `claude-opus-5` at **`max`** while the file read **`medium`**, its own sibling `.claude/rules/session-log-provenance.md` said `medium` correctly the whole time, and the seed's section 0 and this family's own USAGE comment one file over said `max` beside them. Six living sites quote that file; **four of the six were wrong**, counting the Cursor twin of each law page. *What caught it:* the card's own handoff from petrichor, which had read the effort sites while re-running a tutorial's quoted output and named the disagreement without owning the lane. Nothing mechanical could hear it: the guard whose whole subject is this file computed the value and dropped it on the floor. *What it taught:* **a reading nobody acts on is the second half of a guard nobody runs** -- `%568`'s finding, one instrument over and in a sharper form, because here the instrument ran on every lap of every ship and still could not hear the fault it was standing on. **A meter that prints a number it never compares is teaching a reader that the number is watched.** *And the second half is why the repair took care:* the effort is legitimately **not one value**. `tools/f/fleet_lap.sh` execs `--effort max` inside the enclosure while `tools/f/fleet-loop.sh` execs `--effort medium` bare, which this pier runs, so `GLOW_PROFILE.template.kyri` reading `effort max` and `docs-geode/tutorials/running-the-fleet.md` tabulating both are each right. A single-value gate would have been false to the tree and would have reddened honest pages. *What this lap did:* repaired all four false sites, then widened the scan with a **second named declaring roster** over the effort field -- six pages that state what `.claude/settings.json` ITSELF carries -- gated by **presence of the driver's value**, the same presence-over-absence discipline the model half already keeps and for the same reason: a page teaching the launch-path split stays free to name both efforts in one sentence. `GLOW_PROFILE.template.kyri` is deliberately **off** that roster and named as such in the scan's header, since it records how a clone's work is produced rather than what the settings file carries. *And the control learned to answer for its own legs:* every reading now prints through one `leg` function that tallies it, and `control_verdict` reads `leg_failed` when any leg does. Until this stamp a `no` stood beneath a green `control_verdict=ok` unless the witness happened to name that key -- which fired immediately, when the new roster reddened leg 4 and the control still printed `ok`. *What holds it still:* `tools/d/declared_model_witness.rish` GREEN on metal, **26 control behaviors up from 16, 0 failing**, two mutations each caught at the verdict rather than at a named key -- dropping `effort_over` from the gate, and reading a baked `max` in place of the driver's value. *What this does not reach:* whether the effort a page names is the effort a lap actually ran at. This reads what the tree says about itself, exactly as the model half does. **CLOSED** on `declared_model` GREEN, `declaring_effort_over=0` over six pages.
