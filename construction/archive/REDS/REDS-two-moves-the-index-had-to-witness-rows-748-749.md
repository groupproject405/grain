# REDS -- rows %748, %749, folded from the living pin

**Folded:** `20260916.053252` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../../.claude/rules/reds-first.md)

*Two rows about a move the index had to witness, rather than the working tree.* A front door
declared an exhaustive list of the pieces beside it and the list had gone short, which no roster
meter could see, since a room index proving it reaches a page says nothing about a door promising
to reach them all. And three files kept their exec bits through a rename only because the guard
that reads them reads the staged tree.

Both closed the same way: by asking the index what a hand had already asked the filesystem. A mode
lives in the tree object rather than the blob, so a rename repointed in the working tree and left
unstaged is a mode change nobody committed -- and that guard reds on it, once, mid-repair, which is
the whole reason it reads the index at all. The door's own page now carries the measurement that
refuses a naive meter and names the honest population.

The rows stand here exactly as they were written. A booked row leaves the living pin so the pin
stays the length a reader will actually read; the lesson travels forward in the guards the rounds
built, and the row itself stays one click away.

---

**REDS %748 (`20260915.222900`) -- a front door's census stood two short for five days, and the forward direction is unread.** *What went wrong:* `press/README.md` declared *four pieces stand* while six stood; two pieces from `20260910.054448` reached no table. *What caught it:* a Door-register sweep. *What it taught:* `front_door_claim` proves a page's claim ON a door and `foundations_reach` proves a room index reaches a page; neither asks whether a door's own list reaches every living piece beside it. `press/README.md` carries the measurement refusing a naive meter and names the honest population: the door that DECLARES an exhaustive list. *Status:* BOOKED; page repaired, rostered on DOOR.

**REDS %749 (`20260915.224426`) -- three tool files sat in a room their own names never said, and the path sweep that moved them could not see the directory named without a file.** *What went wrong:* `tools/m/bearing_quorum_witness.rish`, `tools/fixtures/m/bearing_quorum_scan.sh` and `tools/fixtures/m/bearing_quorum_control.sh` landed in the `m` room at `e5e6f1729` (`20260915.212457`) while the letter rule computes `b` from their own basenames. The tools fold's whole promise is that a path is a pure function of the basename, so three entries the resolver cannot compute are three references it can never recover. *What caught it:* `tools/t/tool_letter_room_witness.rish` on this seat's cold endurance run, at guard 57 of the roster. *What it taught:* **a path sweep matches paths, and a pen builds a room.** `git grep tools/fixtures/m/bearing_quorum` found every reference across twelve files and repointing them left `tools/w/whitepaper_definitions_witness.rish` RED at 35 of 55 legs, because `whitepaper_definitions_control.sh` names the room ALONE on its `mkdir -p` line -- `"$_d/tools/fixtures/m"` -- and then writes a stub into the repointed file path inside a directory that no longer exists. The write failed silently and thirty-five legs read `wanted=changed got=unchanged`, which looks like a mutation that stopped biting rather than a pen that was never built. **A directory reference carries no filename, so a sweep keyed on `<room>/<file>` is blind to it by construction**, and the guard that caught it was the one downstream rather than the one doing the move. *What holds this still:* `tool_letter_room` gates `misfiled` at zero and reads the INDEX, so a move left unstaged still reds -- which it did, once, mid-repair. Repaired and proven: `bearing_quorum`, `whitepaper_definitions`, `tool_letter_room` and `exec_bit` all GREEN on metal, the three files carrying modes `100644`, `100755`, `100755` through the move. *Status:* BOOKED.
