# REDS shelf -- the door that opened on the seat it replaced, row %537

**Language:** EN
**Stamp:** `20260907.004329`
**Status:** Shelf -- immutable once written
**Rows:** `%537` -- written straight to this shelf on `20260907.020000`
**Voice:** Kyri

**REDS %537 (`20260907.004329`) -- the council rota moved a seat and the door onto that seat stayed where it was, and no link wall could ever have seen it, because both paths resolve.** *What went wrong:* the 5 x 3 grid in `recursion-prompts/seed/autonomous-loop.seed.md` seats Earth-Cardinal at `foundations/20260905-154954_the-clock-and-the-mark.md`, changed on `20260905` on Keaton's word because a rota reads foundations and the mark law sat on no seat at all. `foundations/20260826-021735_earth-the-row-that-breathes-in.md` is the threshold page a lap opens FIRST on that row, and its `## Cardinal:` section went on naming `context/specs/20260627-102012_one-clock-naming-law.md` -- the seat the change replaced. For two days every earth lap that obeyed the door read the clock half of the concern and missed the mark half, which was the entire reason the seat moved. *What caught it:* reading the row. Lap 4119 read row 4 by `N mod 5`, opened the threshold before the seats as the grid instructs, and the grid's own note about the change contradicted the page in front of it. *And no standing guard could have.* Both paths exist and both resolve, so nothing was broken: `tools/fixtures/r/readme_reach_scan.sh` follows Markdown links -- a bracketed text, then a parenthesised target -- and the grid's fifteen seat cells are backticked bare paths it never reads, while the threshold's own citation pointed at a live, canonical file. A link wall reads whether a door opens; this was a door opening onto the wrong room. *What it taught:* **the rota is the one mechanism returning this tree's canon to living awareness, and its twenty paths were held by nobody.** Measured at fleet scale `20260906.213125`, eight ships read the five rows 11, 12, 13, 20 and 12 times across 105 logs, so a blind cell is read a dozen times a day and every one of those logs records a row all the same -- the `%463`/`%519` family, where an instrument reading nothing cannot be told from a healthy one. *Repaired and walled:* the Earth threshold's Cardinal section now seats the living foundation and reaches the naming-law spec through it, keeping every true sentence the elder section carried and adding the mark half (A/95 -> A/94, Field). `tools/r/rota_grid_witness.rish` over `tools/fixtures/r/rota_grid_scan.sh` gates three readings at zero -- `unresolved` (a path the grid names, missing from disk), `sections_missing` (a threshold lacking one of its three modality headings), `seat_undeclared` (a modality section naming a seat other than its own) -- and refuses rather than measuring its own parser when the grid's shape moves (%170). Sixteen behaviors proven on a grid built for the purpose in a pen, every refusal planted and then lifted; **every plant proves it landed**, compared with `cmp -s` against the file it replaced and answering the literal `plant_matched_nothing` (%519), shown from the failing side by aiming a plant at a pattern nothing matches. Rostered `tier lap`, so it is heard (%360). Fourteen of fifteen seats agreed; the fifteenth was in the row being read that morning. **CLOSED.**

## What the lap that found it also withdrew

This lap opened on the same red a peer booked as [`%533`](REDS-a-guard-shaped-by-the-clock-rows-533.md) -- `rota_declared` refusing at every midnight -- and repaired it in the scan before the rebase brought their row home. **Withdrawn whole rather than landed beside**, which is the derived spine's rule applied to a repair rather than to a number, and the sixteenth time this fleet has recorded two hands meeting on one line. Their probe is the better answer: it asks the guard's own question in both halves and skips by name, where mine taught the scan a third verdict, and `%170` is more nearly kept by refusing than by reporting.

**One half of mine survived, because their repair did not reach it.** `tools/r/rota_declared_witness.rish` reported `${ctl.err}` on its control leg and `${scan.out}` alone on its scan leg -- so a scan refusal still wrote an EMPTY reason into `construction/standing-equipment-reds/rota_declared.txt`, the one file a reader opens to learn why a guard stopped. `run` returns `out` and `err` separately, probed on metal. The scan leg reports both now. **The class stands, measured rather than asserted.** `20260907.000903` repaired this shape in five witnesses and built a gate for it -- and that gate reads witness-over-CONTROL, where a **scan** refuses to stderr just as often and rather more commonly, since every guard has a scan and only some have a control. Of **75** rostered witnesses asserting on a scan's `ok`, **14 report stdout alone** and 3 report both; three of the fourteen are the very files that lap repaired on their control legs, which is the reading narrowed to the spelling in front of its author -- `%518`, `%519` and `%527`'s own shape, one room over. Mine was a fifteenth until this paragraph was written; both its legs report now.

**Kin.** The same lap's elder GRASS accounts, folded off the operator card, rest at
[`20260907-005052_itinerary-landed-accounts.md`](20260907-005052_itinerary-landed-accounts.md);
the two questions they carry -- the long-function penalty capping at 25, and two of 46 pages
reading `Status: Proposed` -- are still open.
