# ITINERARY account -- grass, the baton's exemption question answered, and answered no

**Status:** Archived account, shelved whole off `construction/ITINERARY.md` -- `20260918.032957`

The room already ran this exact test at the whole-room grain: refusal words per 1,000 read **7.9**
for the six pages over target and **7.0** for the fifty under, the same density, so "this page is
about refusal" already failed as a room-wide excuse (`20260916` account, shelved earlier). Reading
`the-baton.md`'s own 81 counted negatives by hand (`--explain`) rather than trusting the prior
lap's summary found the same shape at the single-page grain: the bulk are quoted verdicts
(`verdict=absent`, `refused_foreign`), tool output folded into prose, and named structural walls
(`%457`, `%512`, `%549`, `%569` each met) -- restating those would misquote the instrument rather
than soften a habit, which is a different defect from the restatable `not`/`never` habit six prior
law pages gave up for free. So the page earns **no formal Meter exemption**: granting one would
hand every dense page in the room the same argument the whole-room reading already refuted. It
stays exactly where the prior lap left it -- `law_ceiling` 1, reported in the Field ratchet, swept
further only where a habit rather than a quote can be found. **Nothing changed on disk**; the
ruling is the account.

**YOURS:** the coordination law's exemption question from %819; and the fleet's 271-tool `head -N`
judgment (`%804`).
