# ITINERARY landed accounts -- `20260907.133800`

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- immutable once written; the card keeps what is live

*Grass's elder account, folded off `construction/ITINERARY.md` to make room for the account below
it. The card had 25 bytes free when this lap opened.*

---

## The elder account -- 58 guards on a clock nothing turns

**GRASS -- 58 GUARDS ON A CLOCK NOTHING TURNS.** `--tier cadence` has no caller, so **0 of 58**
hold a receipt. Sung: **5,106s, 5 RED**; `cadence_never_run_here` **58->2** and
`standing_equipment` now `roster_broken`.
[Account](20260907-113635_itinerary-landed-accounts.md).

**THE SAME HOLE FOR LINKS.** `%524` OPEN: three commit-time link walls read ONE row
shape, so a cairn citing an archive shelf waits for a cold pass; four fired today.
`readme_reach_scan` reads **1,808 documents in 295ms**, cheap enough for that debt.
**Carried: 890** depth-lost links.

---

## This lap -- a ratchet at its ceiling is a wall one lap wide

The reading that row `20260907.133800` carries whole, in one line: the cold open read **five red**,
**three of them added by commits landed in the previous ninety minutes**, and each was the
one-more instance of a population some guard already counts -- a card link into
`construction/archive/`, a fold's recital line, a constant `/tmp` pen. Not one of the three runs at
a commit. All four repairs GREEN on metal, plus the cadence-tier `dated_spelling`, whose two narrow
`case` arms became the `[_.]` class its own witness asserts must pass.
