# ITINERARY -- landed accounts, shelved `20260911.014147`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The BAKERY account the live card carried before the lap of `20260911.014147`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**BAKERY -- A CENSUS NAMED 38 GUARDS FOR A BEHAVIOR NOBODY HAD RUN.**
**EARTH BREATHES IN** (row 4, N=4614): take the concrete fact at the door, ahead of the argument.
**THE FACT:** `gitlink_dependent_scan.sh` counts a runner naming an optional gitlink on a working
line with no roster row, calls it `optional_unrostable`, and prints of each *reds when an optional
submodule is absent*. I ran two before reading further. Both exit 0. So I ran all of them:
**38 of 38 exit 0**, each printing an honest SKIP, the whole population in **768 ms**. Not one reds.
The count counted a SHAPE and the sentence claimed a BEHAVIOR.
**AND A PEN COULD NEVER HAVE CAUGHT IT.** The control's CASE 3 plants `assert clone.ok else
"...ABSENT"` -- written from the header's own sentence rather than from a file on disk -- so the
only hard-asserting specimen anywhere was the one the claim built for itself.
**MECHANISM:** the reading is `optional_unrostered`, which is what it counts. A `probe` mode RUNS
each optional dependent whose gitlinks stand empty and classifies `probe_green` / `probe_red` /
`probe_unread` -- unread rather than guessed, since observing absence by deleting a clone is the one
move a meter may never make. `probe_red` is gated at **zero**, which turns `%646`'s 5-in-40 sample
into a wall. An absent runner refuses `no_runner` rather than calling every dependent red.
**PROVEN:** **37 control legs, fail=0**, three mutations bitten -- dropping the verdict clause,
the absent check, the runner refusal. Probe costs 3s of the guard's 14. Scan B+ 86, control B+ 86,
witness B 83. The witness also SAYS the card it told a reader to read, which it never printed.
**PIN ROOM MADE, NOT ASKED FOR:** the pin stood at **481 bytes free** with every row OPEN, so no
ship could book. Folding the one BOOKED row (`%701`) to its shelf freed 2,475; my row took most of
it back. **301 free now, 9 OPEN rows, none foldable by law.**
**YOURS, KEATON:** (1) the 38 are measured clean and therefore **rostable today** -- 38 rows behind
`capability tigerbeetle_clone`, or leave them unheard? (2) The pin's bound is the fleet's real
choke: it has been at its ceiling for three days and the model behind 40960 assumed 8 open rows.
Row `20260911.014147`.
