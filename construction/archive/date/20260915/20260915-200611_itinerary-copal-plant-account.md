# ITINERARY -- Copal's plant-and-roof account, shelved `20260915.200611`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is open
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** checkable -- every reading here names the witness that binds it

This lap's Copal account, written onto the living card and shelved in the same send, because
[`20260915-180554_itinerary-landed-accounts.md`](20260915-180554_itinerary-landed-accounts.md)
moved the per-ship accounts off the card an hour before this one landed. A finished account
belongs on a shelf; the card carries one pointer line.

---

**COPAL -- A GUARD COUNTED ONE FILE AS TWO ROOFS AND CALLED THEIR AGREEMENT PROVEN.**
**FIRE SEES** (row 2, N=4917): look straight at the bright point where the work is joining.
Elder Copal account [shelved whole](20260915-175358_itinerary-landed-accounts.md).
`amphora_bounds_agree` states its law over every same-name const and reads
`const <name>: u<N> = <digits>;` alone. Two consequences, both measured on the living room.
A recursive grep follows a symlink, and `amphora/src/manifest_entry.rye` links to
`amphora/manifest_entry.rye` -- so **three of its six discovered families were one file compared
with itself**, each printing `declarations=2 signatures=1 status=agree` about an agreement no
drift could break, while `digest_hex_len` reported five declarations across four real files.
And a const whose value is a quoted WORD spells no width, so the room's **wire words stood
outside all four readings**: `vessel_format` (`amphora-v1`) in the parser's roof and again in the
writer's, `shoulder_ring1_wire` in two, `mark_plain_bytes` in two. Zig forbids `src/` importing its
parent, so the second spelling is structural, and the version refusal landed `20260913` stood on a
word held still by nothing.
**LANDED (`175358`):** `roofs` counts distinct files by inode, a one-file family reads `one_file`
rather than `agree`, and reading (5) reads word consts by the same law. The living room answers
`same_name_one_file=3`, `words_read=5 words_agree=3 words_one_file=2`, `verdict=ok` -- so the
widening lowers nothing today and holds the door. **Both repairs proven by mutation**: counting paths reds
`plant_roof_symlink`, emptying the word roster reds `plant_word_positive`. Sixteen planted pens
GREEN, `amphora_roster` GREEN over 103 behaviors, TAME bans clean. No vessel byte changed.
**YOURS:** should `one_file` become a refusal once a room declares it deliberately, or stay the
honest name it is now?
**AND THE PLANT MEASURED AS DRIFT.** The hot pass reddened `copy_lag` on the very symlink this
lap planted. That reading resolves each both-ways basename to one canon, and twelve plant rooms
name their files `a.rye` and `b.rye`, so `roof_symlink/b.rye` nominated its own `a.rye` as canon
for every `b.rye` in the tree: two unrelated pen files read `behind` it and `basenames_both_ways`
rose 108 to 110 over a header naming 108. **A plant is built to differ, so measuring one as drift
measures the plant.** The 64 fixture paths are read past and COUNTED as `fixtures_read_past`, the
same exclusion `ascii_document_scan.sh` makes. Proven from both sides -- the identical lagging file
reads `behind=0` under `tools/fixtures/` and `behind=1` in an ordinary room -- so the filter reads
a PATH rather than a shape. Pen 31 to 36; row keyed by stamp `20260915.200351`.
**AND THE PLANT MEASURED AS DRIFT.** The hot pass reddened `copy_lag` on the very symlink this
lap planted. That reading resolves each both-ways `.rye` basename to one canon, and twelve plant
rooms name their files `a.rye` and `b.rye`, so
`tools/fixtures/amphora_bounds_plants/roof_symlink/b.rye` nominated its own `a.rye` as canon for
every `b.rye` in the tree: two unrelated pen files read `behind` it and `basenames_both_ways` rose
108 to 110 over a header naming 108. **A plant is built to differ, so measuring one as drift
measures the plant.** The 64 fixture paths are read past and COUNTED as `fixtures_read_past`, the
same exclusion [`ascii_document_scan.sh`](../../../../tools/fixtures/a/ascii_document_scan.sh) makes.
Proven from both sides -- the identical lagging file reads `behind=0` under `tools/fixtures/` and
`behind=1` in an ordinary room -- so the filter reads a PATH rather than a shape. Pen 31 to 36;
row keyed by stamp `20260915.200351`.
