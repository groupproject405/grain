# ITINERARY account -- the largest singleton, and the one body that was genuinely different

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one landed account, immutable once written
**Room:** checkable -- proven on metal, witnesses named
**Folded:** `20260917.213500` from [`../ITINERARY.md`](../../../ITINERARY.md), to bring the card back
under its byte bound after the landing.

**WATER TASTES** (row 3, N=5619): weigh the whole before the part. The sibling account named eight
singletons and left the largest untouched, because it was the one body `glow/zig_ident.rye`'s own
header calls out by name as genuinely different -- this lap weighs that difference before touching
the code.

**THE MECHANISM.** `glow/lower_shop_gate.rye` declared a hand-rolled `fn zig_safe_ident` at 12 call
sites, the largest singleton this room still carried. Two things set it apart from the 24 peers
that already delegate: it answered a length-ceiling refusal with `error.MissingFace` where 28
peers answered `error.BadIdent`, and it mapped no dot at all (no `.` case in its character switch).
Both are published behavior this module's own `LowerError = rb.ParseError || error{Overflow}`
already carries, and `glow/lower_shop_gate_witness.rye:901` switches on `rb.ParseError.MissingFace`
by name.

**THE DELEGATION KEEPS BOTH.** The wrapper calls `zig_ident.safe_ident` with `.refuse` as the dot
mode -- exact behavior parity, since the elder body already refused a dot by falling through its
own character check -- and catches the shared rule's `error.BadIdent`, answering
`error.MissingFace` in its place. `LowerError` gains no new member; `rb.ParseError` is unmoved; the
witness leg that reads it stays green without a touch. This is the same discipline the ceiling
comment's own three depths of removal names: a record set beside a published name adds, where a
rename would breach.

**THE DUPLICATION METER FELL WITH IT.** `glow_ident_duplication_scan.sh`'s `copies` reads **7**,
down from 8; `stubs` reads **22**, up from 21. `CEILING` is lowered to `7` in the same commit, and
its own comment now names the shop-gate delegation and the seven singletons that remain.

**THE WIDTH BILL DID NOT MOVE.** `lower_shop_gate.rye`'s own `zig_safe_ident` already answered
`u32`, unlike the three `max_arm_len` siblings the prior lap touched -- so `width-check`'s
`corpus_flagged_lines` and `corpus_files_ceiling` both stand exactly where they stood.

**PROVEN.** `lower_shop_gate_witness` (1035-line `run_selftest`, every case GREEN, the
`BodyGateNotYetLowered` and `MalformedBody` refusal legs unmoved), `glow_ident_witness`,
`glow_run_contract_witness`, `width-check` (unmoved by design), `tame_style_check`,
`glow_ident_duplication_scan` beside its own `glow_ident_duplication_control` (17 legs,
`control_failed=0`), GREEN on metal, in that order, after the round-open settled the tree.

**YOURS:** the seven singletons that remain -- `lower_core` (4), `lower_call3` (4), `lower_call2`
(3), `lower_alias` (2), `lower_face_lit` (2), `lower_shop_nest` (2), `lower_trap` (1) -- and
`lower_alias` still wants `Dot.to_underscore` rather than `.refuse`, the one body that stays a
genuinely different rule after this lap's fall.
