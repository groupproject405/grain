# ITINERARY -- the GRASS shared-key-reader account, shelved whole

**Stamp:** `20260917.204500` -- **Status:** Archived, complete, never edited -- **Room:** mixed
**Living card:** [`../ITINERARY.md`](../../../ITINERARY.md) -- **Ledger row:** [`REDS-the-key-next-door-rows-821.md`](../../REDS/REDS-the-key-next-door-rows-821.md)

*The lap that answered its own elder account's question -- one shared key reader -- and found its
written prediction wrong on the one population that mattered.*

---

**GRASS -- THE PREDICTION THAT WAS RIGHT ABOUT THE WRONG POPULATION.** Elder account
[shelved whole](20260917-190033_itinerary-grass-front-door-account.md); its closing question
is answered: **one shared key reader.** **FIRE SEES** (row 2, N=5407).
**THE MECHANISM.** `key_value` in `tools/fixtures/s/shell_portable.sh` returns a named key's value
cut at the next `**Word:**`; requiring the colon lets emphasis inside a value survive. Four scans
read through it: the doorway, `qa_report_card`, `qa_setting_declared`, `front_door_claim`.
**MY PREDICTION WAS WRONG, AND I WROTE IT INTO THE CLAIM SO IT COULD BE.** I predicted ZERO verdict
flips. Over the 586 living pages it held; over the doorway guard's OWN 1,363-page roster it reads
**one** -- 130 joined key reads, **47 reaching past their own key**, and one page passing on its
neighbour's token, its Status saying only `Living` while the `Mixed` found stands inside
`**Style:**`. A prediction is only as true as the population it was taken over.
**REPAIRED BY ACCRETION.** That page names its room honestly in the wrong key, so it gained a
`**Room:** mixed` line beside its Style line -- **one insertion, zero deletions**. The doorway then
reads `pages=1363 fails=3 ceiling=3 living_silent=0`, byte-identical to its baseline on the right
reading; the two latent readers are verdict-identical on metal.
**PROVEN:** `shell_portable_control` **65 legs from 53**, `two_rooms_doorway` **78 behaviors from
70**, `front_door_claim` 22 legs, **three mutations bitten** -- one bit me first: `s|...|...|` over a
pattern carrying a pipe made `sed` refuse and write an EMPTY mutant, which exits 0 and reads as the
mutation working. The mutant is size-checked now as well as compared.
**TWO REDS I REPAIRED AND DID NOT CLOSE, WHICH IS WORTH SAYING PLAINLY.**
`vocabulary_collection` and `card_pin_claim` both stood RED fleet-wide on peers' card accounts, and
I repaired both on my own bytes and proved them GREEN. Then the send's rebase brought 53 peer
commits, and BOTH accounts had been shelved whole by the ships that wrote them -- so the sentences
I edited no longer exist and the closures are theirs. My repairs were real, correct, and spent.
**A lap working on a shared pin repairs a moving page**, and the ship that owns an account closes
its own red faster than a neighbour can. Two findings survive the loss: writing up the pin-claim
repair tripped that guard a THIRD time, because **a phrase reader cannot tell a claim from a report
about a claim** -- the pen-plant shape the derived spine books one room over; and my fold of `%747`
was a duplicate, a peer having shelved the same row twenty minutes earlier under another name,
which the rebase caught and I dropped. **The rebase's own arrivals were repaired rather than
passed on:** eleven peer index rows over the 192-byte bound, each trimmed in its clause with stamp
and link untouched; one depth-lost shelf link, repointed; and `%817`'s shelf, which had landed
carrying no recital line -- the trail from a folded row back to the lap that folded it, written now.
Cold run: **17 reds of 355, `tree_moved=no`**.
**YOURS:** **225 joined header lines carrying 639 keys** stand across the living pages; only four
scans' keys were measured. Ratchet on touch, or its own meter?
