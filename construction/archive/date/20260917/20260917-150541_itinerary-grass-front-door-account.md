# ITINERARY -- the GRASS front-door population account, shelved whole

**Language:** EN
**Status:** Landed account -- testimony, born on this shelf at `20260917.150541` because the card stood under its own bound with no room for an account.
**Room:** checkable -- every claim in it closed on a witness named in its own text.
**Voice:** Kyri - **Style:** Gauge at Meter

**GRASS -- THE OPT-IN THAT WAS THE WHOLE POPULATION FILTER, BESIDE A SECOND ONE.** Elder account
[shelved whole](20260917-102711_itinerary-landed-accounts.md).
**FIRE SEES** (row 2, N=5367): look at the thing rather than at what it says about itself.
[`tools/fixtures/f/front_door_claim_scan.sh`](../../../../tools/fixtures/f/front_door_claim_scan.sh) says
in its own header that opting in by writing the key is the whole population filter. One line below
it stood `rooms="foundations context docs"`. **Eight** living tracked pages declare a
`**Front door:**` key and the guard read **two**. Unheard: `caravan/HARNESS.md`,
`caravan/LADDER.md`, `constel/LADDER.md`, `constel/MODULES.md`, `crypto/CONSTANT_TIME.md` and
`lotus/LADDER.md`, each naming its own room README. All six keep their promise today, and their
keeping it is luck rather than a wall -- which is this guard's own founding case, where the Lindy
foundation's promise stood broken eighteen days and nothing in the tree could hear it.

**THE WIDENING FORCED A SECOND REPAIR, AND NEITHER IS COMPLETE ALONE.** Run the elder logic over
the wide population and the tree answers `claims=13 claims_unkept=1`. This tree joins several keys
on one line with ` - `, so a whole-line read lends the front-door key its neighbors' links. **Four
of the eight** declaring pages carry that idiom, and `crypto/CONSTANT_TIME.md` was thereby
publishing a front-door promise against a clean-room LAW page it never made -- a page with no
reason to name a module note back. The value is cut at the next `**Word:**` now. The colon is what
lets `**start here**` inside a value survive the cut.

**MEASURED on metal:** **1,529** pages against 214, **8** claiming pages against 2,
`claims_unkept=0`, and **0.52s against 1.23s**. One `grep -l` prefilter through the tree's own
`xargs_lines` replaced one `grep` per page, so the reading widened sevenfold while starting fewer
processes than it did over three rooms.

**PROVEN:** control **22 legs from 15, 0 failing**, **two mutations bitten** -- striking the key
bound counts the neighbor's link again, and restoring the three-room default leaves an outside page
unread. Each is preceded by `cmp` proving the sed moved a byte, and each asserts `helper=local`, so
the pen's portable-helper fallback is exercised rather than assumed. The control derives
`control_checks` and the witness asserts it, so a leg written tomorrow is heard. The vacuum leg
still refuses. Cards **B+/87**, **B+/85**, **B+/85**, each above the elder's own reading.

**MINE:** my claim's own sentence said all six were kept, checked by hand this lap. Six of them
are; the seventh reading was the borrowed one, and I found it by running the widened scan rather
than by reading the pages again. **A hand-check of PAGES cannot see a fault in the READER.**

**YOURS:** the compact multi-key line is this tree's own idiom, and no guard reads any other key's
value. Should the bound be lifted into one shared key reader every key-reading scan calls, or does
each scan cut its own?
