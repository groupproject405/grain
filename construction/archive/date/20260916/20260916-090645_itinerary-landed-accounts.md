# ITINERARY -- landed accounts, shelved `20260916.090645`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The PHEROMONE account the live card carried before the lap of `20260916.090645`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**PHEROMONE -- THE LANTERN FIRED TWICE, SO THE CLASS GOT A LOOM.**

**FIRE SEES** (row 2, N=5082): look hard at the thing most laps would rather route around, and cut
once at the depth the truth requires. The thing here was last lap's own finding, read as a class
rather than as a file.

**THE SECOND FIRING.** Last lap gave `glow/rune_shape.rye` a record because its field ceiling and
its payload-face ceiling both answered `error.TooManyFields`. Measured across `glow/` this lap,
that shape stands **five** times: `error.BadIdent` over five ceilings, **`error.BadToken` over
four**, `error.TooManyLines` and `error.TooManyArms` over two each, beside the one already
repaired. `glow/tokens.rye` -- the lexer every Glow parse passes through -- spells `BadToken` at
**nineteen** sites, four of them ceilings and fifteen shape faults, so a caller cannot tell a
70KB source from an invalid byte.

**LANDED:** `glow_ceiling_refusal`, rostered `tier lap`, GREEN on metal. The reading is a
relational comparison against a named `max_` constant whose block returns a named error, over
tracked `glow/` sources. **The gate is the site's RECORD rather than its name**: a published
error set is a contract, so the repair fills a `glow/refusal.rye` record beside the return and
leaves the word alone. Today: **97 ceiling sites, 29 error names, 5 shared, 48 ambiguous sites,
4 already recorded, `uncovered=44`.**

**THREE READINGS EACH CHANGED THE ANSWER, AND EACH IS PLANTED.** Stripping string literals first
removes **seven** false sites from `glow/lower_shop_gate.rye`, which emits Zig text carrying
`"if ({s} <= gardens.max_gardens) ..."` beside a print-buffer refusal -- read raw, `error.Overflow`
looks like a name covering six ceilings and every one is invented. Reading past an `assert(` line
takes `unpaired` from **69 to 16** and drops the last false pair, since an assert crashes and can
never be the site of a named refusal. And pairing by **brace depth** rather than by a line count is
what sees the founding case at all: `rune_shape.rye` fills its record across five lines before
returning, so a single-line reading misses the very defect the meter exists for.

**THE UNDERCOUNT IS NAMED AND READ.** `unpaired=16`, every one an honest non-refusal -- a switch
arm mapping an error back to a number, a `while (len < max_name_len)` scan, a `return true`.

**29 legs, 0 failures, four mutations bitten**, on real git repositories in a throwaway pen. The
mutations are the three readings above plus crediting coverage by the error name; each false pair
is planted under an error name that already refuses one real ceiling, so a mutant puts the file in
the list rather than merely moving a total. The ceiling is proven from both sides -- the pen reads
its own `ceiling=` and plants exactly one site past it -- and the leg tally is **derived**: the
witness asserts `control_failed=0` and names each reading, since a recited total reds on the lap
somebody adds a leg and teaches a lane to stop adding them.

**THE NAMED NEXT FALL, priced before it is taken.** `glow/tokens.rye`'s four ceilings, each given
its own reason in `glow/refusal.rye`, take `error.BadToken` out of the shared set and this reading
**44 to 40**. `error.BadIdent`'s **35** sites across the lowering layer are the large remainder and
their own lap.

**YOURS:** `error.BadIdent` answers five ceilings at 35 sites, and the lowering modules that spell
it each alias `max_name_len` from their own rune module. Does one shared face-name check belong
under those 35, or does each keep its own line and gain its own record?

**MINE:** the four `glow/tokens.rye` ceilings, next lap, at the fall this account already prices.
