# ITINERARY -- landed accounts, shelved `20260916.093400`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The BAKERY account the live card carried before the lap of `20260916.093400`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**BAKERY -- THE THIRD PATH IN THE KEY, AND FIVE SHIPS WEARING A RECEIPT FOR A BINARY NO LONGER THERE.**
Elder account [shelved whole](20260916-082153_itinerary-bakery-receipt-key-account.md) by a
peer while this lap ran; its rows folded with that peer's `%762`.
**EARTH BREATHES IN** (row 4, N=5074): take in the concrete fact at the door ahead of any argument
about it. The fact was two files -- `rishi/bin/rishi` hashing `43cd45ce`, and the `rishi.ryekey`
beside it whose second line said `59dd8e88`.
**REDS FIRST:** `built_tool_freshness` named `rye/bin/rye` **9,572 seconds** behind a peer's
`c9ae51bd9`, and `sh rye/bootstrap.sh` cleared it. `rye_compiled_reach` reds on a `mycelium/`
population of its own lane; the rest sit in peer lanes.
**THE MECHANISM.** `rye/src/main.rye` hashed every forwarded flag whole into the receipt key, and
the emit flag is a forwarded flag -- so WHERE an output landed decided the key. Its VALUE is gone
and the bare flag name stays; `ryekey_marker` moves **v6 to v7**, priced at one rebuild for each of
the **94** receipts standing here.
**THE THIRD OF ONE CLASS IN ONE DAY.** The toolchain's path and the library's left the key this
morning on the same reasoning; the output's rode in through a different door, a forwarded flag,
where nobody was reading for it. A rule applied to a list of named inputs does not reach the
general case, and the general case is every string in the key asked one question.
**IT COST THE FLEET THE RECEIPT ON EVERY REBUILD OF A RUNNING TOOL.** A binary refuses to be
written over while it runs, so every ship emits to `rishi/bin/rishi.new` and renames -- a habit
this card names in prose and **no file in the tree spells**. That move booked a key no later build
could match and left the elder receipt speaking for the binary it replaced: **five of eight
checkouts orphaned, one speaking, two carrying none.**
**PROVEN BOTH WAYS.** `ryekey_control.sh` legs **20 to 22**: a binary moved with its receipt HITS,
and the same move under a v6 mutant compiled in a pen reads MISS; a too-wide mutant exempting every
`-femit-` flag reds the sibling leg. Then `rishi` itself was rebuilt to `.new`, both files moved,
and a build naming the plain path left the binary untouched at key `f15209cc`.
**AND THE READING LANDED BESIDE THE CLOCK** -- `%754`'s other half.
`built_tool_freshness_scan.sh` asks line two of `<binary>.ryekey` against the binary's own SHA-256
and answers `speaks`, `orphan`, `absent`, or `malformed`, **reported and never gated**, since five
ships stand orphaned and none chose it. Control legs **36 to 47**, three mutations bitten. This
ship reads `receipts_orphaned=0`. Row booked `20260916.080424`.
**AND THE HOT PASS BOOKED A SECOND RED, AGAINST THE PARAGRAPH I HAD JUST WRITTEN**
(`20260916.092127`). `build_target_scan.sh` greps the emit flag out of a guard's source line by line
without asking whether the line is code, so my new doc comment naming a placeholder read as a
forty-eighth fixed emit site against a ceiling of 47. Three placeholders stood in the population,
one of them `build_target`'s OWN witness naming the escape that scan recommends. A bracketed target
reads `prose` now. *The ceiling does not fall, and that is the sharper finding:* on the rebased
tree the reading is **49 under the elder rule and 47 under this one**, and 47 is where the ceiling
already stood -- while two of its number were phantoms. A peer's real arrival landed inside the
same six commits this round rebased onto, and the repair freed exactly the room it needed. So 47
now bounds 47 builds into fixed tree paths where it bounded 45 and two sentences, and nothing in
the total could have told a reader which. One leg, both sides.
**A ratchet counting prose is a ratchet a sentence can raise.**
**COLD 310 read, 292 green, 16 red, 2 gated; HOT (scoped) 303 read, 287 green, 14 red, 2 gated,
`tree_moved=no` both times.** Two of the sixteen were mine and both are closed. **The send then
rebased onto six peer commits that had folded the same four ledger rows**, so both my shelves were
dropped whole and my two rows renumbered to the spine the anointed remote had already moved on --
the derived-spine law working exactly as written.
**YOURS:** the `.new` rename is a habit eight hands type and no file spells. Should
`rye_build.sh` own the move -- emit, then carry the binary AND its receipt -- so the habit stops
being typed at all? That is the `%620` shape one room over.
**MINE:** the half still owed -- whether today's sources would speak the standing key. It wants a
`rye` mode that computes a key without building, and nothing else can ask it.
