# ITINERARY -- landed accounts, shelved `20260916.213318`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The INCENSE account the live card carried before the lap of `20260916.213318`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**INCENSE -- A PIN REWRITE ATE A DIRECTIVE, AND THE LIST IT LEFT READ 6, NIB, 8.**
**AIR FEELS** (row 1, N=5138 -- rows 0, 2, 3 and 4 were each read earlier today, so I advanced by
hand to the oldest): a claim of a boundary is tested by pressing on it. I pressed on the word
**advertised** and found a second, larger thing behind it.

**THE RED THAT LANDED.** Commit `5f96df5e14`, on `20260916.111857`, whose subject and body describe
a new REDS shelf resolver, replaced item 7 of the INNER LOOP in `construction/ITINERARY.md` with a
`**Git nib:**` line. Item 7 is **Prove on touch** -- run the cold endurance run before work and the
hot one after staging, grade touched prose B or better, keep Truth at 60 or higher. The card already
carries its nib under *Product direction* and says so in its own words, *named once above ... one
fact in one place*, so the same write left the pin standing twice. The directive was absent for ten
hours and roughly 120 commits from the page every ship of an eight-ship fleet reads at each lap open.

**NOTHING STANDING COULD HAVE CAUGHT IT.** `nib_honesty` reads the nib BY ITS KEY, so it finds one
whichever line carries it. `ascii_document`, `tracked_link` and the pin bound read bytes rather than
meaning. The numbered list's jump from 6 to 8 is the one tell, and it is visible to a reader and to
no instrument in this tree. A guard holding every numbered list in a living pin to `1..N` with no
gaps would catch the next one on the lap it lands; the lantern has fired once, so it is named rather
than built. Item 7 stands restored, the duplicate pin removed to pay for its bytes, and the row is
booked and CLOSED.

**AND THE LAP'S FIRST HALF WAS BUILT TWICE, WHICH IS THE LESSON.** I opened on `%782` -- Grass's
close-of-lap red, OPEN, its repair explicitly wanting the owning lane's word: is an advertised hash
recognised by POSITION or by RESOLUTION? The card and the living pins are my lane, so I measured
both sides before ruling. Over the card's last 200 revisions: **290 advertised runs, 201 keyed to
the card's own `**Git nib:**` field and 89 standing in prose, and all six that ever failed to resolve
were keyed** -- so the loose prose reading has refused nothing that reading by position would have
missed. Against that, the cost of narrowing, read from the object store rather than assumed: **138
of this tree's 15,741 commits, 0.88 percent, carry an all-decimal ten-character short hash**, against
the 0.93 percent `(10/16)^10` predicts. I built the three-class sweep, planted one hash of each class
in the control -- whose own second plant was `0123456789`, so the narrowing would have dropped
`RED_floating_nib_caught` from 2 to 1 while every other leg stayed green -- and bit four mutations.

**A peer landed the same ruling forty minutes ahead of me**, in `8030d809a`, with a `classify_tokens`
awk function and four classes rather than three, telling a decimal that happens to resolve from one
that cannot. Theirs is the better instrument and it was first, so mine is discarded whole and theirs
stands untouched.

**WHAT THAT COST, AND WHAT IT TEACHES.** I ran the claim check at my open and the board read clear;
I never OPENED a claim, because the baton's clause asks for one when a lap opens a NEW INSTRUMENT
and this was a repair to a file that already stood. That reading of the clause is exactly the gap:
`%782` was a named, OPEN, unclaimed red on the shared pin, which is the loudest possible invitation
for two ships to take it in the same hour, and nothing in the tree says so. The absence scan cannot
help here -- both files existed, and what was missing was a sentence saying somebody was inside one.
**A booked red is a claimable thing**, and the cheapest repair is to say so where habits are set.

**PROVEN.** `e123-living-pin-guard`, `reds-monotone`, `reds-pin-capacity`, `reds-spine-derive`,
`reds-citation`, `unshared-citation`, `tracked-link`, `link-text-promise`, `index-row-bound`,
`exec-bit`, `ascii-document`, `ascii-comment`, `prose-register`, `qa-report-card`, `nib-honesty` and
`remember-git-nib` GREEN on metal. `%783` folded to its own shelf to make pin room, and the ledger's
census headline moved with both the fold and the new row.

**YOURS -- THE TWO ROOMS ARE TWO MECHANISMS, NOT TWINS.** My own meter named this on `20260911` and
nothing has acted since: `rule_twin_scan.sh` reads `cursor_only=2`, and both are live law --
`arbor-voice`, which governs every `.arbor` voice tile and the three tracked files in `arbor/`, and
`fuse-resin-cleanup`. No Claude ship can read either, and every live seat in
`construction/fleet-roster.kyri` runs `claude`. Copying is no free repair, and the reason is
measurable: `.cursor/rules/` scopes law by frontmatter -- **27 of its 58 pages carry
`alwaysApply: false` and 11 carry globs** -- while `.claude/rules/` has no scoping at all, so all
**295,276 bytes** of its 56 pages load on every lap of every ship, and **104,501 of those bytes are
law Cursor itself keeps silent until a matching file opens**. `arbor-voice` is exactly such a page:
`alwaysApply: false`, `globs: **/*.arbor`, three files governed. Copy it always-on and a module of
three files is read on all 5,138 laps; leave it and a ship editing an `.arbor` tile has no law at
all. A third door -- Claude's own on-demand mechanism, a skills room -- is a mechanism this tree has
never seated and no law here names, so it waits for your word rather than a lap's.
