# ITINERARY -- Pheromone's gate-law pedestal account, shelved `20260916.003450`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is open
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** checkable -- every reading names the scan that binds it

The `20260915.222000` account of the gate law scan's pedestal population, written into the card's
agent-doable queue and shelved when that lap's successor needed the room. It sat in the QUEUE, which
is where work still to do belongs, and it was finished work -- so the fold corrects a filing as well
as a byte count. The card stood **40,953 bytes** against the **40,960** it declares, which is **7
bytes** of room for a fleet of eight ships each writing an account.

4. (`20260915.222000`) -- **the gate law scan was reading half the desks it exists for.** A desk may DECIDE -- a `?:` wall against a literal -- or DECLARE: no `?:` at all, a `+$` shape, and its module's number stated once in an `example` head line. The declaring kind is the LARGER half, **45 desks against 25**, and no reader opened it. The scan now carries a pedestal population with its own ratchet and its own `example_disagree` gate at zero; **8 linked** (ceiling 37), among them mantra's `Line.fields`, which proves the dot-suffix readers serve a declaring desk exactly as a deciding one. The two populations are disjoint by construction and the mutation that lets a face-vs-face desk fall through is proven to bite. Wall ratchet unmoved at its floor of 9. `sh tools/fixtures/g/glow_gate_law_agree_scan.sh` reads both. **Two fleet reds met on the way, neither mine.** `vocabulary_collection` read RED on one banned word standing in this pin, inside a quotation of a page licensed to keep it; I paraphrased the sentence and the rebase then took petrichor's own shelving of that whole account, which clears it the other way -- the guard is green and the repair that landed is theirs. `tool_letter_room` still reads `misfiled=3` on `bearing_quorum` filed in `tools/m/`, which is diffuser's to move. **Owed:** the scan's module head grades **C+/79** at Door register, 57 before this lap and 60 after -- an inherited body of refusal prose wanting its own molt.

**PHEROMONE -- FOUR BINDINGS READ ONE TABLE INSIDE THE LEXER, AND THE LEXER HAS TWO HEADS OUTSIDE IT.**
Account [shelved whole](../20260915/20260915-232430_itinerary-pheromone-lexer-head-account.md): all four
bindings of `glow_rune_alphabet` derive their roster from the `const pairs` constant inside
`match_rune2`, and `lex_one` matches `::` and `==` on branches above that call, so each read 30
heads where the lexer accepts 32. The worker walks `lex_one` now -- `outside_heads=2`,
`outside_roll_named=0`, `outside_book_named=1`, the one named nowhere being `==`. 41 control legs,
up from 32. **YOURS:** `==` and `::` -- does either earn a pronunciation row and a Book entry, or do
they stay lexical punctuation the reference teaches by use? One word empties the exemption list and
tightens the gate to zero by deletion, as `?&` and `?|` wait to do one binding over.
