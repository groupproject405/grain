# ITINERARY -- the Pheromone account, born on its shelf

**Stamp:** `20260916.033517`
**Room:** checkable -- an account written straight to its shelf, the card keeping the pointer
**Status:** Archived -- historical continuity outside Mitra and shred-prep
**Voice:** Kyri
**Style:** Gauge at Meter

The card stood at its 40,960-byte bound when this lap closed, with eight ships writing into it. So
this account is born here rather than folded here later, which is the very door the card's own
*Open doors* section asks about. The card carries the finding and the pointer; the whole account is
below.

---

**PHEROMONE -- GLOW CANNOT WRITE TWO OF THE PRODUCT'S FOUR PUBLIC TYPES, AND THE NUMBER THAT STOPS
IT WAS FROZEN BEFORE THE PRODUCT EXISTED.** Elder
[shelved whole](20260916-033517_itinerary-pheromone-lexer-head-summary.md).
**FIRE SEES** (row 2, advanced by hand past row 1, read by the three laps before me).
**THE MECHANISM.** My rung reads *express the receipt facts in the smallest Glow form already
owned*. That form is the multi-field `$:` shape, and `glow/rune_shape.rye` declares
`max_fields: u32 = 9` -- with no comment, alone among the bounds around it. The contract declares
`ReceiptOfferFact` at **15** fields and `LinengrowReceipt` at **11**, so two of its four public
types cannot be written in Glow at all. All fifteen map onto auras Glow admits -- `value_amount`
needs `@u64`, the 9,000,000,000 ceiling standing above `@u32`; `product_digest` fits `@ux` at
exactly 32 bytes -- so the capacity is the only thing in the way.
**THE NINE IS A SEATED FREEZE, AND I WIDENED NOTHING.** `20260720`'s ruling stopped the ladder at
nona because *another face name teaches no new nest law* -- true while the rungs were copy-paste
Greek numerals and nothing asked for a tenth. Its own escape clause reserves a widening for your
word. Something asks now, with a requirement rather than a preference.
**LANDED:** `glow_shape_capacity`, deriving the ceiling from the Glow source and the type widths
from the contract's own `## Public types` block. **44 pen legs, 0 failures**, five refusals planted
and lifted, three mutations bitten, one live leg. **The gate is the instrument, never the gap:** a
reader that silently parses zero types prints a flawless `types_over_capacity=0` about a page it
never opened. My own pen caught my own scan -- the first draft split a field line on commas before
trimming the colon, so `offer: map<a, b>` read as two.
**A REBASE RESURRECTED TWO CLOSED CLAIMS.** `85a8684c1` replayed a stale `fleet-claims.kyri` over
`24cd44150`, so four landed claims -- my two, bakery's, diffuser's -- read `status=building` on the
board. Mine are closed again here.
**MINE:** the aura half is a hand's, since the contract names field names rather than types.