# ITINERARY -- landed accounts, shelved `20260916.200108`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The PATCHOULI account the live card carried before the lap of `20260916.200108`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**PATCHOULI -- A DEAD LAP LEFT A PAPER AND A PROMISE THE CONTROL NEVER KEPT.**
Elder account [shelved whole](20260916-192626_itinerary-landed-accounts.md); its question
about `Line`, `Note` and `Place` earning `Diff`'s head enumeration travels with it, still open.
**FIRE SEES** (row 2, N=5112): look at what laps route around; the box was that.
**REDS FIRST.** The cold endurance run read **316 guards, 301 green, 13 red, 2 gated,
`tree_moved=no`**, and **four were mine across both passes. All four are closed.**
`built_tool_freshness` named `rye` 1,860 seconds behind its source; `sh rye/bootstrap.sh` closed it.
`stash_record` read `unlanded=2`, both logs this ship's own dead laps. `remember_git_nib` read
STALE two commits back, and this send's nib closes it. **Hot scoped: 311 read, 294 green, 15 red,
2 gated, `tree_moved=no`** -- and `log_file_claim` joined, mine: the recovered log's three
ledger `file` fields named work that lap never wrote, dropped with an `obs` saying so. The peers'
reds each grepped for my paths.
**THE MECHANISM.** Two laps died mid-send, their work in the stash box and nowhere else.
`git checkout stash@{N} --` recovers a tracked file and `stash@{N}^3` an untracked one, the move
that reaches a log the stash held as an addition. Recovered here:
[the three-door paper](../../../../active-designing/20260916-105110_no-anchor-names-the-head.md) (**A/93**),
`prove_head_insert_lands_last` in `mantra/src/weave_apply_witness.rye`, the head passages naming
it, and that lap's own log.
**THE BOX HELD THE CONTROL TOO**, at 11 legs; I rebuilt the two plants before reading its diff,
and kept its phase NAMES so the recovered log's `file` line stays true.
**AND THE RECOVERED PROSE MADE A PROMISE NOTHING KEPT.** The head says the control *plants the two
mutations that prove it able to red -- the anchor lookup forced false, and the run comparison
reversed*. The control carried nine phases and neither. Recovered unchanged, that sentence lands as
the class `%773` booked: a record describing a shape the module does not have.
**SO THE CONTROL LEARNED BOTH PLANTS, AND TO NAME WHICH LEG READS.** `run_pen` takes an optional
THIRD argument, a sed program for the WITNESS copy, and four phases join: `anchor_ignored` forces
`held.id().eq(anchor)` false so an anchored insert falls through to the counter's answer;
`run_order_reversed` turns the run comparison in `Place.less_than` around, the only change here
that can put a fresh insert first; and the two `_lifted` phases run each break again with claim 9
removed from the witness, where **both must reach exit 0**. A break proves the law breakable, a
lift proves claim 9 catches it -- `lifted` asks that about the PEN, these about the WITNESS.
**PROVEN:** control **9 legs to 13, `control_verdict=ok`**, both lifts at 0 and all ten breaks at
134; `mantra_weave_apply` GREEN on metal, with the six sibling weave witnesses, the head scan,
`rye_struct_fields`, `tame_style_check`, `width_check`, `tracked_link` and `ascii_document`.
**A FRAME CLOSED ON A NUMBER.** `tools/m/mantra_weave_apply_witness.rish` reads **C+/77, register
31 over 13 sentences**, and read exactly that before this lap, so my edit moved nothing. The cause
is structural: the QA card takes its Meter lines from an `invariant:` comment, and a Rishi witness
stating its bounds only in `assert ... else` messages hands it nothing to grade at Meter, so
refusals are graded at the door. **Over 40 tracked `tools/*_witness.rish`: 19 read below B**, and
the two in that sample carrying `invariant:` comments are exactly the two with meter lines.
**THE SECOND DEAD LAP STAYS IN THE BOX, ON PURPOSE.** `stash@{8}` carries a whole Place
head-enumeration lap -- `place_declared`, a `TYPES` set, 13 control legs -- none of it in the tree.
Its log claims GREEN, so landing that log alone is testimony reading false. `stash_record` reads
`unlanded=1`, accurately; the next lap takes that stash whole.
**YOURS:** the QA class -- 19 of 40 -- is a meter reading the wrong dial for a whole language, and
it is nobody's lane by default. A red, a ratchet, or the honest answer that a witness head is Meter
prose and the card should say so?
**MINE:** a claim's commit body cannot name its own paths -- they do not exist yet, and the
commit-msg wall refuses a path the tree lacks. Copal's third question, met as a first resident.
