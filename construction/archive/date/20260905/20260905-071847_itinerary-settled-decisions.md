# ITINERARY settled decisions -- the fifth shelf, `20260905.071847`

**Language:** EN
**Style:** Gauge, Meter setting
**Status:** Shelf -- immutable once written; the record for rows the living card released
**Room:** checkable
**Released from:** [`../ITINERARY.md`](../../../ITINERARY.md)
**Cairn:** [`../CHECKPOINTS.md`](../../../CHECKPOINTS.md), row `20260905.071847`

One row released because a newer measurement of the same thing stands beside it. `%412` recorded
the roster pass falling from 1,510 seconds to 990 seconds; `%413` measured it again at 856 seconds
and named the three guards that took it there. The elder row keeps every number it measured, which
is why it rests here rather than disappearing.

---

**`%412` CLOSED -- a third of the roster's clock went to forking, not reading.** The pass ran 111
guards in 1,510 seconds, with a 2-second median and 13-second mean; five guards held 46 percent of
the time. Two were slow by accident. `living_card_ascii` forked `mktemp`, `iconv`, and `rm` for each
of 14,709 files, about 44,000 processes, and moved from 137 seconds to 25 seconds through one awk
pass. `reds_row_present` invoked its scan once per row, rereading the spine 411 times, and moved
from 177 seconds to 12 seconds through one `--all` read. `sow` remained expensive on purpose and
moved to cadence because its act is custody gate `%1`.

The measured pass moved from 1,510 seconds to 990 seconds, with 1,003 seconds projected. Before an
expensive thing becomes rare, measure whether its cost serves the proof. A cadence tier on the two
accidental cases would have hidden 277 seconds of waste behind a slower clock.
