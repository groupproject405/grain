# REDS -- a guard that could not say what it knew

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- two ledger rows, each repair proven by a witness on this tree and by planted cases in a throwaway pen
**Folded:** `20260907.232316` from [`../REDS.md`](../REDS.md)

One row, folded the hour it was written to hold the pin under a bound eight ships share. An
instrument held a reading it could not deliver: it gated one number standing over two stamps without
any way to tell a doubling this tree created from one the anointed spine already carried, so a
ceiling of one refused eight ships over a deadlock only Keaton may settle.

**A second row stood here and withdrew whole.** The same cold pass found
`tools/r/reds_spine_derive_witness.rish` dying `StringTooLong` on a tree its own scan called
`verdict=ok`, booked at `20260907.230500` and repaired the same way -- the reading said before it is
judged, never interpolated. A peer reached the identical fault and the identical cure from the other
end and published first, as
[`REDS-the-explanation-that-cannot-be-built-rows-597.md`](REDS-the-explanation-that-cannot-be-built-rows-597.md)
(`20260907.234825`). Two rows for one incident is worse than one row under somebody else's number,
so this lap's row is withdrawn rather than renumbered, and their page carries the finding. That is
the tenth such collision in a day, and the second where two ships found one fault rather than one
number.

Read it beside
[`REDS-a-duplicate-that-was-never-a-judgment-rows-592.md`](REDS-a-duplicate-that-was-never-a-judgment-rows-592.md),
whose row is one of the two published doubles that fired the ceiling, and beside
[`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %598 (`20260907.230000`) -- one law, two derivations, and the blind one held the gate: a ceiling refused eight ships over a doubling no lap may repair.** *What went wrong:* `tools/fixtures/r/reds_ledger_monotone_scan.sh` counted `numbers_double_bound` -- one row number standing over two stamps -- under a ceiling of 1, on the argument that holding the line makes the next doubling red inside the tree that made it. That argument wants one fact the scan cannot reach: whether the doubling is one a lap may repair. It reads FILES, so a pair this tree created and a pair the anointed spine already carries look identical to it. On `20260907` the second published double landed -- `%592`, bound to `20260907.211709` on a shelf and to `20260907.215114` on the living pin, both already on `xy/main` -- and `.claude/rules/derived-spine.md` rule 3 holds for both rows, so neither number may move and the deadlock is Keaton's word (booked `20260907.014654`). The ceiling reddened `reds_ledger_monotone` and `reds_row_present` on every ship that fetched it. *What caught it:* this lap's cold roster open, four guards red at once, and reading `reds_spine_derive` beside them: the same tree, the same hour, `published_doubles=2` and `verdict=ok`. That guard reads the anointed remote and split this exact class in two on `20260907.024141` -- `double_booked` gated at zero for a pair a lap repairs in one line by renumbering its own unshared row, `published_doubles` reported and gated by nothing -- **and the sibling scan never learned it**. *What it taught:* **two derivations of one law that disagree are one law written twice, and the one that cannot see holds the gate until somebody looks.** The tree has the sentence already, in the scan that learned first: *a gate that reds on what nobody may touch is a gate somebody turns off.* *Repaired:* the teeth move to the instrument holding the remote. `numbers_double_bound` is reported here with both stamps named, `numbers_double_bound_gated_by=reds_spine_derive` prints beside it so the class never reads unguarded, and the ceiling is gone rather than raised -- proven by a leg that plants a SECOND double and watches it pass, since a reading shown only at one instance cannot be told from a ceiling quietly bumped to two. A further leg runs `reds_spine_derive_scan.sh` on a planted local double in a throwaway git repository and asserts `double_booked=1 verdict=double_booked`, so the pointer is checked by running rather than by citing. `rows_double_shelved` keeps its own ceiling and its own word. Four guards GREEN. **CLOSED**
