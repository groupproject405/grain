# REDS %573 -- a page that calls itself living is not testimony

**Folded:** `20260907.145113` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A test drawn to decide whether a path reference may be rewritten was borrowed to decide whether a
page's prose obeys the word bans, and the two questions disagree on 147 pages that had already
answered in their own hand. Folded on its own lap, because a rebase brought the pin 2,106 bytes
above its bound and this was the newest row that a fold may lawfully move.*

The row stands here exactly as it was written. A BOOKED row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the instrument the round
repaired, and the row itself stays one click away.

**REDS %573 (`20260907.145113`) -- the meter that rules living prose dropped every page whose basename carries a stamp, including 147 that declare themselves Living in their own hand.** *What went wrong:* `tools/fixtures/l/living_prose_roster.sh` excluded any path matching a stamped basename on the mark law's test -- such a name IS testimony. That test decides whether a **path reference** may be rewritten, and is right there. Borrowed to ask whether a page's **prose** obeys the word bans it answers a different question: **555** tracked pages fall to the stamp rule alone, and **147** carry a `**Status:**` value of `Living` -- 40 `foundations/` pages on the council rota, 25 `context/specs/`, 18 `active-designing/`. The roster read 379 pages of a tree whose own rows call 767 living. *What caught it:* the water rota row, whose sense works only up close -- so the lap read the actual threshold page rather than a count. `foundations/20260826-021734_water-the-row-that-tastes.md`, opened by every ship on this row, summarised its dual seat as *dogfood and gate* while `20260618-184912_growing-a-language.md` had already been swept to *live in it as first resident, and gate*. **A paraphrase quoting a word its source had dropped, on a page eight ships read, and no meter could see it.** *What it taught:* **a page that calls itself living is not testimony, whatever its filename says.** This is `%540`'s own next layer: that row moved duty 1 from a 60-path page roster to a 379-path prose roster and named the remainder as a decision, right for testimony and wrong for pages that had already answered. *Repaired (`20260907.145113`):* a stamped page re-enters when a `**Status:**` line's **value** is `Living` -- the page's own word, so the meter judges nothing and no list is kept. Roster **379 -> 520**, duty 1 prints **0 -> 3**, all three living-declared `context/specs/` pages actionable on touch. *And the control caught the first draft*: matching `Living` anywhere on the line named a plant reading *Status: Retired -- a word that is not Living*, so the predicate reads the **value** -- `%570`'s exact-against-loose lesson, met the same day one instrument over. `retired_word_control.sh` proves **10** behaviors on real git history, the new admission bitten and both stamped shapes shown apart. `context/specs/20260707-053212_radiant-vocabulary-pass.md` joins the vocabulary-record exemption, being the law that seats three of the banned words. *Not reached:* whether those three lines get swept is on touch, Tier 2 style rather than a red. Duty 1 refuses nothing, so no gate moved and no ceiling rose. **BOOKED**
