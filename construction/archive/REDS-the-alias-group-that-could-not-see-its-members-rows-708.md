# REDS shelf -- row %708, the alias group that could not see its own members

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- one BOOKED row, born on its own shelf because the pin has no room for it

Written `20260911`, in the lap that found and repaired the red. `construction/REDS.md` stood at
**40,945 bytes** against the **40,960** its own header declares -- **15** bytes of room, where this
row wants roughly three thousand. A BOOKED row may fold by the ledger's own law, so a BOOKED row may
be born here -- the move `%699`, `%703`, `%706` and `%707` each made before it, every one for this
same reason. This is the sixth this week and the third this morning. The number moved once before it was
shared: this row booked `%707`, the anointed spine spent that number on `20260911.053330` while
this lap ran, and the derived-spine law keeps the earlier stamp -- so the key is the stamp
`20260911.055500` and `%708` is the view.

---

**REDS %708 (`20260911.055500`) -- an alias group is a claim about members, and the reading could not see its own.** *What went wrong:* reading (2) of `tools/fixtures/a/amphora_bounds_agree_scan.sh` proves that differently spelled ceilings meaning one quantity carry one number -- `cargo_ceiling` names `max_resin_bytes`, `max_seal_plain` and `max_cargo_bytes`. It counted const LINES across the whole group into one `FOUND`, skipped any name the room did not declare, and compared the numbers it happened to find. So **one member declared twice at one value answered `alias_cargo_ceiling_declarations=2`, `signatures=1`, `status=agree`, `verdict=ok`, exit 0** -- a three-name claim proven by reading one name twice, with the other two standing nowhere in the room. Its only floor, `FOUND < 2`, counts lines rather than names, and two lines of one name is reading (1)'s own subject: the group reading proved nothing of its own and said `agree`. *What caught it:* a water rota lap taking the row's instruction literally -- run the actual thing up close -- and running the scan against a two-file pen where `max_resin_bytes` stood alone, twice. Both readers in turn against the same landed plant: elder `status=agree` exit 0, repaired `membership=incomplete` exit 1. *What it taught:* **a typed roster fails in two directions, and this family met both in two laps.** `%706`'s sibling row one lap earlier widened reading (1), whose roster was typed and could not GROW -- a name the room declares that nobody typed in. This is the same fault reflected: a roster declared in the instrument cannot SHRINK, so a member leaving the room takes the claim with it and the reading keeps answering about what remains. A declared relation must therefore be read for its membership before it is read for its agreement, since the members are the half the room can change without telling the instrument. *Repaired:* the group prints `members`, `present`, `declarations` and `missing` before any comparison; a group no member of which stands under this root reads `membership=absent` and is skipped; a group missing a member reads `incomplete` and fails, naming who is gone; a group with fewer than two distinct names present reads `thin` rather than `agree`; and the elder `thin` branch, which fired on one LINE and only under the living root, is subsumed by a refusal that names the missing members instead of counting them. The living room reads `membership=whole` for both groups, so the widening lowers nothing today. `alias_incomplete` lands as the thirteenth planted pair, and `alias_disagree` gained its third member in the same commit so that its only fault stays the divergence it was planted for. Witness GREEN; the mutation bites at the first membership leg. **BOOKED.**

---

*One shelf, one row. The pin holds what is open.*
