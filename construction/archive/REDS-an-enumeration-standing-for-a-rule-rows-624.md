# REDS -- an enumeration standing in for a rule

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Style:** Gauge (see [`../../context/GAUGE_STYLE.md`](../../context/GAUGE_STYLE.md))
**Voice:** Kyri
**Room:** Checkable -- the row names where it was caught
**Folded:** `20260908.073157`

One row, closed on a witness the lap it was found. It carries a lesson about instruments rather
than about links: a guard's greenness is evidence only about the cases its own predicate can
reach, and a `case` that lists instances instead of stating the rule behind them is complete only
by luck. The nine links it found had stood since `20260724`, under a gate rostered `tier lap` that
read zero over them on every lap of every ship in the fleet.

---


**REDS %624 (`20260908.072125`) -- a gate held at zero read green on every lap of every ship while nine of its own subjects stood in the column it reports rather than gates.** *What went wrong:* `fold_shelf_link_scan.sh` decides whether a shelf link is depth-lost by a `case` with two arms -- a target beginning `archive/` strips to the bare name, one beginning `../` gains a level. Those two are spellings of one rule: **text folded down a directory needs one more level of climb.** The rule was never written; only the two instances a hand had repaired by hand the day before. A bare sibling -- `](TASKS.md)`, correct in `construction/` and one climb short from a shelf -- is the third spelling, matched no arm, and fell through to `links_dead`, where the scan's own header describes that column as stale references for `dated_path_resolve.rish` to answer. Nothing was ever going to run the resolver on them. So `fold_depth_lost=0` stood beside **nine repairable links**: eight naming `TASKS.md` and `ROADMAP.md` from the two season ledgers folded `20260724`, one naming `20260620-212126_usize-width-baseline.md` -- each a page that stayed in `construction/` while the text citing it descended. The guard is `tier lap`, so it ran on every lap of every ship in the fleet, green, over links it was built to bite. *What caught it:* measuring a claim rather than repeating it. The previous lap closed by naming *a fifth spelling the loom misses* and placing it among the 744 reported dead links, so this lap classified all 744 by which correction would resolve each -- and nine resolved under one climb, all nine inside this guard's own room. The class was named right and the room was one door over: the miss was not another guard's blindness, it was this guard's own `case`. *What it taught:* **an enumeration standing in for a rule is complete only by luck, and it reads green over exactly the cases nobody thought to list.** The tell is one question a reader can ask of any source line: does this line draw the boundary its own WHY claims? COPAL booked the same shape the same day one room over, as a population picked by directory. A guard's greenness is evidence only about the cases its own predicate can reach. *Repaired (`20260908.073157`):* the correction block now carries the rule above the `case` that spells it, and a third arm takes a bare relative target; an absolute path takes no arm, since without its own case the bare arm would test a doubled slash and mean something else. The header's ambiguity argument holds unchanged and is now stronger: `archive/`, `../`, and neither-of-those **partition** every relative target, so a target matches at most one and no repair ever guesses. The arm widens what is LOOKED at and never what is claimed -- each correction tests its candidate on disk, and the scan resolves from the shelf's own directory first, so a bare name already answering inside the room is unreachable by it. That last property is the only way widening could steal a correct link, so it is proven by planting one namesake in **both** directories at once rather than reasoned. The repointer needed no change: it types what the scan approves. All nine repaired by that program across three files, scan back to `fold_depth_lost=0`, and **proven to have moved no prose**: with every `](target)` masked, all three shelves hash identically against their committed bytes. Control **51 -> 66 behaviors** over 23 pens, six new phases, each refusal planted and then lifted; seven asserts in the witness; the roster WHY and the witness header both moved from two corrections to three. *Named, not taken, with a number:* `links_dead=735` still merges two populations -- **605** name a stamped target, which `dated_path_scan` already gates as `refs_lost`, and **130** name an unstamped one that nothing reads at all, since `tracked_link_scan` reads past any stamped-basename file and every shelf in this room is stamped. Whether that split is reported or gated is a judgment about testimony and stands on the card. *Erratum:* booked `%619` from a fetched spine, and the lap was cut off before its send; the round-open of `20260908.080430` stashed it whole, and by the time it was recovered three peers had published `%619` through `%623`. A published number never moves and an unshared row does, so this row landed at `%624` -- the stamp `20260908.072125` is the key that made the move cost one line (`derived-spine` 1 and 3). **CLOSED**
