# Landed accounts -- folded from the live front `20260907.002052`

*Held word for word from [`../ITINERARY.md`](../ITINERARY.md), so the card carries what is OPEN and
what waits on Keaton's word. PHEROMONE's two landed halves, folded in the round that withdrew a
duplicated landing and kept the one repair inside it. Both blocks' relative links are re-anchored
one level deeper here, which is the correction a fold owes every link it moves.*

---

**PHEROMONE -- A TWO-CHARACTER TEST CLAIMED EVERY `|%` SOURCE, AND FOURTEEN DESKS STOPPED
LOWERING.** Row `20260906.195208` CLOSED out of an hour-old park, whole on its
[shelf](20260906-214931_itinerary-landed-accounts.md): the Glow step of
`manual/grain-os/get-started.md` had refused **seven days**, `stash_record` reads **1 -> 0** and
`declared_ceiling` **1 -> 0**. **`%504`
[shelved](20260906-195208_itinerary-landed-accounts.md), remainder standing.** **Two peer
shelf links repaired on the rebase** -- one a depth kept through a fold, one an anchor naming `%510`
over the `rows-511` path; `fold_shelf_link` and `reds_citation` were red on the anointed order for
every ship. **Yours, sized not taken:** rostering `glow_run_desk_witness.rish`.

**PHEROMONE -- my own `41 orphans` is WITHDRAWN**, `harness_roster` landed gating both halves at zero, and the three-firing lantern's spec stands -- whole on the [`20260906.090312` shelf](20260906-090312_itinerary-landed-accounts.md).

*Live residue kept on the card: rostering `glow_run_desk_witness.rish` is still yours.*

---

*Three elder recital pointers rode down with these blocks, so the way in stays one hop rather than
gone -- each shelf names its own rows:
[`134234`](20260906-134234_itinerary-landed-accounts.md),
[`140206`](20260906-140206_itinerary-landed-accounts.md),
[`092312`](20260906-092312_itinerary-landed-accounts.md). The first anchor read `145647` on the
card, a stamp no shelf carries, opening the `134234` shelf: `%502`'s class, repaired here.*
