# REDS -- a reflex three pages stated, and no guard read

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri
**Folded:** `20260908.083214`
**Rows:** `%627` (`20260908.083050`) -- CLOSED

The row folds on the lap it was booked, for the reason the shelves beside it record: the living
pin stood near its bound and almost every row still on it reads OPEN, so `reds_fold.sh` may move
nothing else. A finished row goes where finished rows belong and the pin keeps a pointer.

**The shelf was written for two rows and holds one.** The second, booked `20260908.080200`,
recorded a renumber sweep of mine that was scoped to a file and rewrote a peer's published `%613`
on the neighbouring recital line. On this send's rebase that defect was already on the anointed
spine as `%621`, booked at `20260908.071909` by a ship that met it an hour earlier -- and the
one-character repair had itself already landed twice from two other hands. **So my row withdrew
whole**, which is the derived spine working rather than failing: the stamp is the key, an earlier
stamp holds the number, and a duplicate row teaches nothing a reader could not read upstream. What
it cost is one line, because every citation of it spelled the stamp. **The surviving row renumbered
in the same breath**, booked `%622`, re-seated `%623`, and re-seated `%626` as two more peers spent each in turn -- the first reported when the spine said a peer had taken
`%622` at `20260908.080144` -- the same law, the same one-line cost.

**What the surviving row found, and how.** The council rota's Air row asks a lap to run a hand
along an edge and press, because touch only works at a surface -- you meet a thing where it ends.
Pressed that way, `context/TAME_CORE.md` stated the chapter-allocator reflex as an absolute, two
more law pages said it again, and no guard anywhere read the word. The hand went straight through
a fence three pages had described. What made it invisible is that every reflex beside it on the
same list was held, so a list of guards read as covering a list of reflexes.

---

**REDS %627 (`20260908.083050`) -- a reflex stated three times as an absolute, and checked by nothing.** *What went wrong:* `context/TAME_CORE.md` line 28 carried the chapter-allocator reflex in the imperative -- *never construct `ArenaAllocator` in authored `.rye`* -- `context/TAME_GUIDANCE.md` said it again under its own heading, and `context/specs/inherited-names.md` gave it the layered form a reader acts on. Three law pages, one absolute, and `git grep -il arenaallocator -- tools/` answered **zero**. `tame_style_scan_bans.rish` carries eleven bans and this was not among them; `tame_style_scan_advise.rish` carries nine ratchets including the two siblings the SAME reflex list names -- `parseInt(` and `Ed25519` -- and not this one; `tame-check.rish` reads three textual rules and this was none. The rule stood on habit alone across **1,958** tracked Rye sources. *What caught it:* the council rota's Air row, whose threshold page states the test outright -- *a claim of a boundary is tested by pressing on it; if the hand passes through, the boundary was a wish* -- and whose cardinal seat IS `TAME_CORE.md`, read as *a reflex that has drifted from its witness is a fence post rotted at the base, firm to the eye and loose to the hand*. Pressed one at a time, every other Rye reflex on that page answered with a guard by name. This one answered with nothing. *What it taught:* **a checkable-surface list proves what it names, and says nothing about what it omits** -- so a page carrying both a list of guards and a list of reflexes reads as though the first covers the second, and the two lists on `TAME_CORE.md` had been drifting apart in plain sight. The omission was invisible precisely because the neighbouring reflexes were all held. *Repaired (`20260908.083050`):* `tools/c/chapter_allocator_witness.rish` over `tools/fixtures/c/chapter_allocator_scan.sh` walls two counts at zero -- `arena_direct` for a line naming `ArenaAllocator`, `garden_alias` for one naming `GardenAllocator`, gated separately because reaching for the inherited type and stealing Tally's reserved name are two faults with two cures. **A wall rather than a ratchet, because the tree stood at zero when it was seated**: both counts read 0 over 1,958 sources, so the ceiling and the wall are one number and no lap inherits a backlog. The affirmative half is reported and never gated -- **737 of 1,958** sources reach `init.arena.allocator()`, and a file reaching no arena may be entirely correct under the freestanding clause. Thirty-five behaviors on real git repositories in a throwaway pen, every refusal planted and then LIFTED, every free rule shown from both sides so none becomes a hole: a comment teaches while a code line in the same file counts, dated testimony keeps every word while a living file does not, inherited std keeps the name while `rye/lib/stdish` one character over does not. Rostered `tier lap`; all three law pages now name the guard beside the claim, and `TAME_CORE.md`'s own *walks all four* became *all five* in the same pass. **CLOSED** on `chapter_allocator` GREEN.
