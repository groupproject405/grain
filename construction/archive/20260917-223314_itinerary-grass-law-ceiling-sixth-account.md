# ITINERARY account -- grass, the law room's ceiling fell a sixth time

**Status:** Archived account, shelved whole off `construction/ITINERARY.md` -- `20260917.223314`

`ascii-first.md` moved 40% to 29% (69 to 51 of 171 sentences), thirteen restatements:
`unnecessary` for `no reader needs`, `keeps that from happening` for `how that never happens`,
`leaves unnamed` for `never named`, `stays unproven` for `proves nothing`, `moved only what it
named` for `moved nothing else`, `lacks` for `does not carry`, three repeats of `leaving the N
untouched` for `taking none of the N`, `halts hard on any appearing` for `fails hard if any
appear`, `can do beyond a glob` for `can do that a glob cannot`, `a hand had yet to name` for
`nobody thought to name`, `still unpressed` for `nobody had pressed`, `the exposure` for `the
risk`, `each run leaves them exactly as they stand` for `no run re-pours them`, and `every sharper
reading of the five missed it alike` / `lacks a say verb` / `belongs to Rishi` for the closing
three -- every claim, figure, and stamp held.

`law_ceiling` fell 2 to 1, `tools/fixtures/p/prose_register_scan.sh` and
`tools/p/prose_register_witness.rish` moved together in the same commit. Witness GREEN, 55 legs,
`control_failed=0`. `the-baton.md` at 47% of 187 sentences is now the only law page over the
Field target.
