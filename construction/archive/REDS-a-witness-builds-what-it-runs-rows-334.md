# REDS -- row %334, folded from the living pin

**Folded:** `20260829.141956` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A witness that names its build in a comment and then runs the binary keeps a promise no clone can
keep. The row opened at forty-six such promises and closes at none.*

Folded rather than born here: the row stood in the pin from `20260828.213503`, and the lap that
closed it proved the last four repairs from DELETED artifacts on a macOS clone where all four were
already absent. Folding it returns the first byte of headroom the pin has had since the capacity
meter measured `pin_deadlocked=1` -- nine rows, none foldable, five bytes free.


**REDS %334 (`20260828.213503`) -- a witness named its build in a comment, then ran the binary.** *What went wrong:* `comlink/bin/` is gitignored (`.gitignore:185`), and `tools/co/comlink_handshake_turn_witness.rish` and `comlink_turn_route_witness.rish` carried `# Build first: rye build ...` then `run ["comlink/bin/handshake-turn" "selftest"]`. A comment builds nothing, so a clone died on a missing file rather than a handshake fact. *What caught it:* Comlink's four witnesses, each named by no runner. *What it taught:* **a repair without a meter fixes one of three** -- `comlink_topology_witness.rish:19` already had it. *Repaired:* both take it, GREEN with binaries deleted; `tools/w/witness_own_build_witness.rish` ratchets `unbuilt_pairs` at **46** over 35 witnesses (was 48). Stood open on the 46. *Closed (`20260829.141956`):* the 46 fell to 4 over `20260829` -- Mandate's seven, Amphora's eight, Scribe, Vault, Basin, Sundial and Kumara's bind each took their own build, and the scan learned to follow ONE HOP, which freed ten Pond and Glow pairs that had been building through a tracked script they run all along. The last four took the build their Comlink sibling already carried: `tools/k/kumara_contact_witness.rish` -> `kumara/bin/contact`, `tools/s/settlement_constellation_witness.rish` and `tools/s/settlement_names_witness.rish` -> `settlement/bin/constellation` and `settlement/bin/names`, and `classical-vedic-astrology/seat_nakshatra_witness.rish` -> `classical-vedic-astrology/bin/seat_nakshatra`. Each takes the six-line shape `comlink_topology_witness.rish` seated: read the vendored toolchain into `zig`, `mkdir -p` the bin room because `rye build` refuses an absent `-femit-bin` directory, build, and assert the build BEFORE the invocation. Proven the way the row asks rather than the way a pier allows -- all four artifacts were ABSENT on this macOS clone, all four witnesses died with a bare `CommandFailed` naming no cause before the repair, and all four read GREEN after, each run again from a DELETED artifact. `CEILING` falls 4 -> 0, so the ratchet becomes a wall with no population left to grandfather: a witness invoking a path the repository declines to carry reds on the lap it arrives. The control's two ratchet plants move to 0 and 1, and one case beside them was repaired in the same pass -- `absent_never_gates` asserted a literal `verdict=ok`, which conflated the new wall with the one-machine presence fact that case exists to isolate, so it compares the two readings now and proves that changing PRESENCE ALONE moves nothing. `tools/w/witness_own_build_witness.rish` GREEN on metal at `unbuilt_pairs=0 ceiling=0` and `self_built=58`, over 35 control behaviors. **CLOSED** -- and the repair's first act was to surface a defect the missing build had hidden for six days, booked as REDS %343 on its own shelf.
