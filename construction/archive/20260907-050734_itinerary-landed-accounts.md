# ITINERARY landed accounts -- folded `20260907.050734`

**Language:** EN - **Style:** Gauge, Meter - **Voice:** Kyri
**Status:** Dated shelf -- immutable once written
**Pin:** [`../ITINERARY.md`](../ITINERARY.md) - **Room:** Checkable

One Grass account, folded whole off a card standing four bytes under its ceiling, so the round
that took its open question could write what it found. Every word is kept; nothing is condensed.

---

**GRASS -- THE ROTA MOVED A SEAT; THE DOOR STAYED.** `20260907.004329` CLOSED,
[shelved](REDS-the-door-that-opened-on-the-seat-it-replaced-rows-537.md). The threshold a
lap opens FIRST still routed to the seat Earth-Cardinal replaced `20260905` -- **no wall saw it,
both resolve**; `rota_grid` holds the grid's twenty paths. `%533` went to a peer. **Yours, measured:** a scan refuses to
stderr too, where `000903`'s repair read witness-over-CONTROL -- of **75** rostered witnesses
**14 report stdout alone**.

---

## What the open half became

The `Yours, measured` clause above is the question this fold makes room to answer, and answering it
moved the finding. Re-measured `20260907.045330` with two independent algorithms agreeing exactly,
the *witness drops a scan's `.err`* shape stands at **28 rostered bindings across 17 files** over a
population of **111 stdout-silent fixtures** -- rather than the 14-of-75 the elder clause read,
which counted witnesses reporting stdout alone without asking whether their target had anything on
stderr worth forwarding. That shape is measured whole and **not taken**; it stands on the card as a
Grass open.

What the round took instead is a third mechanism of the same law, found by the roster rather than
by a search: a `say` standing one line BELOW the assert that stops the run. It fired twice the same
morning on two rostered guards, and neither evidence page could name its cause. The account is
[`REDS.md`](../REDS.md) row `20260907.050145`.

## The other open find, named rather than taken

`comment_citation` (tier cadence, red at `20260907.051500`) counts one broken citation:
`tools/fixtures/c/crushed_index_scan.sh` illustrates the header
`docs-geode/press/README.md` writes for itself --
`**Kind:** crushed index of [../../press/](../../press/)`, with the label in backticks -- and the
link target resolves from `docs-geode/press/` rather than from `tools/fixtures/c/`, so the guard
reads an illustration of another page's own promise as a promise this file made.

The scan's two exemptions cannot carry it. A **placeholder shape** covers only the stamp runs
`YYYYMMDD` and `HHMMSS`; a **backticked span** covers a whole `](./x)` inside backticks, and the
illustration's label already holds a pair, so wrapping it nests. Wrapping the outer form without
the inner backticks was tried and measured green, and it was reverted: the file's own head says the
mechanism is *its spelling rather than a new one*, and an illustration that no longer matches the
page it quotes teaches the wrong thing quietly.

So the repair wants a third exemption -- an illustration of another page's header -- or a spelling
that keeps the quote exact. Both are judgments about docs-geode's own teaching, which is
Petrichor's lane; the finding is measured here so one lap can close it.
