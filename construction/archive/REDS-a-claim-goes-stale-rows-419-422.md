# REDS %419-%422 -- a claim goes stale quietly

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260905.124043` to bring it back under bound as
`%423` landed, four rows written across one long day by three ships. What runs through them is a
claim that stayed on the page after it stopped being true: a credential newer by mtime and dead by
contents, a guard reading a room it was no longer pointed at, a machine named by a check that could
not see it, and a stash holding a lap that had already finished. Each read correct on its own face,
and each needed something outside itself to notice. Link depth moved with the text.*

**REDS %419 (`20260905.083758`) -- the guard that watched an emptied room, and read the emptiness as health.** *What went wrong:* `tools/l/log_has_a_row_witness.rish` gates one promise -- every session log is named by a row in the index that holds it -- and it read the FLAT room, `session-logs/` at its top level. Its control proved a folded log free *"because its row is on the day's shelf,"* an exemption that was correct and whose destination nothing checked. Then `20260827.171500` seated BORN ON ITS DAY'S SHELF, so the flat room is empty by design and the exemption grew to cover every log in the tree. For **nine days** the scan measured `flat_logs=0` and reported `verdict=ok` on every roster pass while four logs stood unreachable from any front door -- `20260828.114450`, `20260829.095622`, `20260903.234946`, `20260904.095307`. Two more faces: the pin's shelves table named `20260904` **open** while seven laps stood on today's shelf, so the page whose own sentence is *"the newest day's shelf is the one to open"* pointed at yesterday; and its per-day counts had drifted on four days, one by six. *What caught it:* the Earth rota row at lap 3939 -- *the concrete fact at the door, before any argument about it*. The pin's table was read first, and its top row said `20260904`. *What it taught:* **a guard whose subject is moved by a law must move with it, and only the population count tells a repointed guard from a sleeping one.** This is %381's shape in this rung's own sibling -- `index_row_bound` read the pin alone, measured `rows=0`, and passed for four days while the open shelf carried 87 rows for 64 logs. One law moved both guards' subject; one was repointed and one was not. The sharper half is %416's: **a gate held at zero cannot tell a clean collection from a dead instrument, because both answer zero.** *Repaired (`20260905.083758`):* the scan reads the shelves AND the pin in one `find` and one `awk` over 4,056 logs across 44 shelves in **0.17s**, five readings gated at zero and 216 pre-law gaps on a falling ceiling; the four rows were written and the pin re-derived. **36 behaviors**, both sides of every refusal, including one elder case the law inverted: an empty room now REFUSES. No closed-shelf exemption, since a missing row is repaired by ADDING one. The instrument floor fired on its own first run. *Verified and corrected on the landing lap (`20260905.092929`):* the pre-law block was written as one *"no index in this tree reaches"*, and the control three files away already disproved it -- its nineteenth behavior reads a gathering index for its links and asserts they are counted. Measured against the guard's own population, that gathering carries **162 of the block's 373** logs and 211 stand unwritten. True of DAY-named shelves alone; the mechanism was right and only the prose outran it. Corrected in three living sites before publication, the elder log left as written. **The lantern fired a third time in one day:** `session-logs/README.md` graded **A/93** at `truth_counted=100` while its top row named yesterday -- `%117`'s and `%417`'s shape, where every path resolves and the sentence is false. This firing's claim is **derivable**, so it is gated; theirs are not, and stay Truth's judged half. **Booked `%418` on a fetched tree and renumbered on the rebase** -- `20260905.081211` reached the spine first and keeps the number; the stamp is the key and never moved. **CLOSED** on `tools/l/log_has_a_row_witness.rish` GREEN.

**REDS %420 (`20260905.092929`) -- the ledger's own census line stood twice, and the guard that reads it checked its numbers rather than its number.** *What went wrong:* `construction/REDS.md` carried its census headline -- *"Rows: 418 - in the tree before the ledger: 6 ..."* -- as **two byte-identical lines**, 38 and 39, confirmed by `md5sum` on each. It entered with `60776bcad` and rode through `e8e62349f` unremarked, so the page that states how many rows the ledger holds stated it twice. *What caught it:* a rebase. This lap's own row collided on `%418`, so the ledger had to be read line by line to place the renumbered row, and a `grep -n` for the headline returned two hits where one was wanted. Nothing was looking for it. *What it taught:* **a guard can check what a line says and never how many times the page says it.** `tools/gen/chapter/reds_ledger_monotone_witness.rish` reads GREEN against the duplicate, measured on metal before this row was written, and it is not blind by accident -- it holds a `duplicate_headlines` reading under a ceiling, seated by `%287`. That reading counts **row** headlines sharing one number, which is a different question from how many **census** headlines a page carries, and the second question had never been asked. The headline check that %127 seated reads the first match and validates its three numbers against the spine, so a second identical copy is invisible to it by construction. This is the family `%417` and `%419` both sit in, in a third material: a reading that is exactly right about the thing it looks at, and silent about the thing beside it. *Repaired (`20260905.092929`):* the duplicate line is removed and the census re-derived to **420** rows with a remainder of **400**, both read from the spine rather than incremented by hand. *What this does not reach, and it is the honest half:* the guard is unchanged, so the next duplicate arrives as quietly as this one did. A cardinality reading over the census headline is one assert and belongs on the lap that next opens that witness; it is named here rather than built, because a lantern earns its loom on the second firing and this is the first. **BOOKED.**

**REDS %421 (`20260905.100421`) -- a newer file is not a better credential, and one logged-out checkout was one launch away from being three.** *What went wrong:* `%415`'s repair seeds a tree's Claude login from the host and **refreshes it when the host's copy is strictly newer**, reading `-nt` and nothing else. Signing out -- or a session expiring past refresh -- does not delete that file. It **rewrites** it with all seven fields intact, valid JSON, correctly shaped, and **empty token strings**: `accessToken` and `refreshToken` at length **zero**, `expiresAt` at the Unix epoch. Such a file is the newest thing on disk, so an mtime comparison ranks it **above** a working credential. The clause was one launch from copying emptiness out of the tree that held it and into every other tree on the pier. *What caught it:* Keaton pasting petrichor's terminal. Its loop answered `Failed to authenticate: OAuth session expired and could not be refreshed` **five laps running**, and `%414`'s brake stopped it at three fast failures with the words *the agent was never reached*. The brake worked exactly as written; the thing it stopped was a fault of mine from four hours earlier. *Measured on metal:* the host carried **509 bytes** with 108-character tokens and a valid expiry; `grain-petrichor/loops/claude` carried **281 bytes** with **zero-length** tokens -- and was **twelve minutes newer**. The elder clause therefore refused to repair it, correctly by its own rule and wrongly by every other measure. *What it taught:* **freshness is not fitness, and a file's timestamp says nothing about whether its contents still work.** The sharper half is the direction of the failure: this is a **replication** clause, so a wrong reading does not stay local -- it propagates. Kin to `%416` one file over, where a ratchet read a dead instrument's silence as the healthiest tree it had measured; both are an instrument answering a question next to the one anyone asked. *Repaired (`20260905.100421`):* **usability is read before freshness.** A `claude_login_live` probe greps for a non-empty `accessToken` of sixteen characters or more -- the cheapest honest test, phoning nowhere, and the one thing that separates the two states an mtime cannot tell apart. The host is copied only when its own credential is live; a tree holding a live credential is never overwritten by an empty one; and a logged-out host **says so once** rather than replicating its emptiness, since a `claude login` at the pier shell then repairs every tree on its next lap. Proven both directions on metal: a planted empty credential is refused, and petrichor's room went from 281 bytes and dead to **509 bytes and live** on the first run of the repaired launcher. **CLOSED.**

**REDS %422 (`20260905.111120`) -- the guard that proves a seat is a chair, not a computer, was itself written for one computer -- and on every other tree it reported the law working as a violation.** *What went wrong:* `tools/fixtures/f/fleet_roster_control.sh` proves the fleet's one-writer-per-checkout law (`%291`) by asking `tools/l/fleet-loop.sh` two questions in `FLEET_DRY` mode: the seat at home walks free and prints its engine banner, and any other seat is refused by basename. Both seat names were **spelled as literals** -- `incense` for the free direction, `petrichor` for the bitten one -- under a comment that stated the assumption out loud: *"this tree is grain-incense."* It is exactly right in `grain-incense` and **inverted** in every other tree of the fleet. Run on the petrichor pier the loop answered **both questions correctly** -- `petrichor` walked free with `seat=petrichor engine=claude`, `incense` was refused with *"belongs in grain-incense; this tree is grain-petrichor -- refusing"* -- and the control read both right answers as **three** failures: `dry_run_names_the_engine=no`, `banner_names_the_engine=no`, `wrong_tree_refused=no`. The guard did not merely fail away from home; it **reported the law working as a breach**. *What caught it:* the cold roster open at the head of this lap, `guards_red=2`, `fleet_roster red 3s`, and its own evidence file naming three legs -- then reading the control beside the loop rather than trusting either. The loop was never wrong; on metal, in both directions, it was right the whole time. *What it taught:* **a control that names a machine cannot be run on the fleet it guards.** The tell was in the file's own header, which promises *"every refusal is shown from both sides"* -- and the one leg that could not keep that promise portably was the one whose two sides were hard-coded to two different computers, so exactly one checkout in three could see both. It is also the day's own family in a fourth material -- `%416`, `%419`, `%420`, `%421` each read an instrument that answered the question next to the one anyone asked -- with the sharpest cost yet: this reading was not merely silent, it was **backwards**, and a guard that reds when the law holds is one a hand learns to expect red and stops reading. The cost compounded quietly: a red guard withholds the roster receipt (`roster_receipt_write=withheld_guard_red`), and `--scoped` refuses without a full green receipt, so this tree could never write one and every lap here paid a full ~20-minute cold pass. *Repaired (`20260905.111120`):* the control **reads which seat is at home from the tree it stands in** rather than spelling it -- `here=$(basename "$ROOT")`, then one `awk` over the roster's own rows for the live seat whose tree matches, and another for the first live seat whose tree does not. The engine word is read from the table too (`--engine "$home_seat"`), so the leg now proves the loop's banner **agrees with the roster** rather than matching a literal this file would also have to keep in step. A checkout that is no live seat's tree prints `control_verdict=not_a_fleet_tree` and exits rather than reading zero, since an empty answer here would be byte-identical to a passing one. Proven on **two** tree names on metal: 37 checks and **0** failures in `grain-petrichor`, and in a pen directory named `grain-incense` the home seat walks free with `engine=claude` while the away seat is refused -- so the repair generalizes the reading rather than moving the assumption to a new machine. The derivation was checked against all three live seats, each deriving its own seat as home and a real peer as away. `tools/f/fleet_roster_witness.rish` GREEN. *What this does not reach:* the pier's other lane red, `agent_jail`, is unrelated and is an **environment fact rather than a tree red** -- `bwrap: Failed to make / slave: Operation not permitted`, because the roster was run inside the very sandbox agent-jail exists to create. It is named here rather than papered over with a `capability` line, which would mark the guard skippable on hosts where it genuinely works. **CLOSED** on `tools/f/fleet_roster_witness.rish` GREEN.
