# REDS %396 -- a borrowed scope

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260831.083000`, **BOOKED** -- a lap
borrowed the shape of a peer's unlanded repair through an environment variable, and `TMPDIR` has no
scope. Every control the pass ran built its throwaway pen inside `.git`, which is inside the tree the
pen exists to stand outside of, so three guards lost the one case that asserts the pen looks like
nowhere. The pin keeps what is open; this row's repair landed on the lap that found it, and it moved
here the same lap because the pin stood 381 bytes from its wall with a live regression still to
record. Link depth moved with the text.*


**REDS %396 (`20260831.073652`) -- a throwaway pen placed inside the tree stops being a pen, and three guards lost the refusal leg that tells them from a bypass.** *What went wrong:* this lap opened its cold roster with `TMPDIR` pointed at a directory inside `.git`, borrowing the shape of a peer's unlanded repair -- `xy/pier/diverged-20260831-064342` puts the RUNNER's own private pen under `git rev-parse --absolute-git-dir` after a host reclaimed a `mktemp` directory while a 35-minute choir still held it. That fix is narrow by construction and says so in its own comment: *"Controls that drive the runner outside Git keep the ordinary system temporary directory."* An environment variable has no such narrowness. `TMPDIR` is inherited by every process a pass starts, so every control that builds a throwaway pen built it at `.git/hush-pen/tmp.XXXX` -- **inside the very tree the pen exists to stand outside of.** Three controls carry a case that asserts the pen looks like nowhere, and all three broke: `fixture_depth`'s markerless-pen walk climbed out through `.git`, found the real root four steps up inside its 8-step bound, and returned `0 /home/keeper/grain-hush` where the case wants exit 2; `dated_spelling` read `no_tree_told_apart RED -- a directory with no git tree did not read distinctly`; `pond_seal_gate` read `no: gitless_unreadable -- want exit 1 with 'verdict=unreadable', got exit 0`. *What caught it:* the pass's own count, read against the previous one. The cold close read **108 guards, 104 green, 4 red** where the same roster read 107 green and 1 red the lap before, over a tree that had moved by three upstream commits and no work of mine. Three new reds beside no new work is the wrong shape, so each was re-run alone -- **green on the ordinary `TMPDIR`, red under the override, both directions shown**, which is what tells an environment fact from a tree defect. *What it taught:* **a fix's scope is part of the fix, and an environment variable has no scope.** The peer's repair reaches one `mktemp` call in one script; the borrowing reached every process in a 39-minute pass, and it broke exactly the leg a guard cannot afford to lose -- a refusal proven only in the passing direction cannot be told from a bypass, so three guards spent this pass unable to prove they still refuse. The cost ran past the false reds: `run_verdict=guard_red` withholds the roster receipt, which is the receipt `%374` is about, so a borrowed mitigation spent the thing it was reaching to protect. Kin to `%394` in shape rather than in subject -- `pkill -f` reaches every process whose NAME matches, `TMPDIR` reaches every process that RUNS. *Repaired (`20260831.073652`):* the override is gone and this lap's remaining passes use the ordinary system temporary directory; all three guards stand GREEN on metal, each proven from both sides. The `/tmp` reclaim the override was reaching for is real and stays the peer's to land, in the narrow shape their own file already carries. *Named, not taken:* a control asserting its own pen is outside the tree before it plants anything, since the three that broke each discovered it through a different symptom. **BOOKED.**
