# REDS -- the third answer a witness did not know

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven on metal from both sides
**Folded:** `20260908.104016` from [`../REDS.md`](../REDS.md)

One row, folded the hour it was written so the pin can accept the next red: eight ships share this
ledger, and `tools/fixtures/r/reds_pin_capacity_scan.sh` read `rows_that_fit=0` with the row
standing. It teaches that a control may answer three ways where the witness over it asserts one,
and that the honest repair removes the CAUSE of the abstention rather than teaching the witness to
accept it -- here, a background lock holder that borrowed its launcher's process group, given a
group of its own by `set -m`. Read it beside
[`REDS-a-seat-named-in-four-places-rows-623.md`](REDS-a-seat-named-in-four-places-rows-623.md),
the fold that made room for it, and [`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %634 (`20260908.093729`) -- the witness over the tree's most-run instrument knows two answers where its own control gives three, so a pass reddened on how it was launched.** *What went wrong:* `tools/fixtures/s/standing_equipment_control.sh` proves the runner's orphan-lock reading by planting a lock holder whose process-group leader is certainly alive, written `sleep 45 & live_holder=$!`. A shell WITHOUT job control starts no new process group for `&`, so that holder borrowed the group of whatever launched the control -- and a lap that launches its roster pass detached exits while the twenty-five-minute pass runs on. The leader was then gone, the plant answered `live_group_plant=unavailable`, and `tools/s/standing_equipment_witness.rish:415` asserts `live_group_plant=ok` and knows no third answer, so the control's honest abstention read as a failure. The instrument reddened on **how it was started** rather than on what it measured: red on every detached launch, green on every hand-run one. *What caught it:* a hot pass on `20260908.093729` carried one red its own lap had not caused, reproduced from both sides that morning -- the same plant re-run from a shell that stays alive read `ok`, and from a shell that then exits read `unavailable`. Its row waited three hours because `construction/REDS.md` stood at 40,949 bytes of 40,960 and `tools/fixtures/r/reds_pin_capacity_scan.sh` read `rows_that_fit=0`. *What it taught:* **a control's honest third answer is a fault only to a witness that knows two** -- and the abstention is worth keeping, since a reading proven only where it fires cannot be told from one stuck on. The repair therefore removes the *cause* of the abstention rather than teaching the witness to accept it. *Repaired (`20260908.104016`):* `set -m` wraps that one command, and POSIX job control puts each background job in a process group of its own, so the holder leads its own group and depends on no launcher; job control is turned off again immediately, because it also changes how background children take SIGINT and SIGQUIT. Proven on metal both ways under a launcher that dies -- `setsid sh -c 'nohup sh <control> ... & exit'` reads `live_group_plant=unavailable` at `f16c05934` and `ok` with the repair, and the whole witness, which reddened under that launch, now closes GREEN under it. **CLOSED**
