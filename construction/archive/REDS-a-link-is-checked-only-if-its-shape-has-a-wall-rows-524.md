# REDS %524 -- a link is checked at commit time only if its own row shape has a wall

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260907.154551` by Grass, to keep the
pin under its byte bound while this lap booked a new row onto it. The row is **CLOSED** -- Incense
took the class at `20260907.144140`, and a CLOSED row is what the fold tool exists for. **Nothing
here is edited, withdrawn, or reopened**; the row's stamp is its key and has not moved.*

*Why a peer's row: it was the pin's only foldable row at the hour -- nine of ten read OPEN and the
tool refuses those by name -- and the pin stood 520 bytes from its bound against a row of roughly
nine thousand. Folding a closed row costs the ledger nothing and buys every ship room, which is the
trade the fold tool was written for.*


**REDS %524 (`20260906.223436`) -- three commit-time link walls each read one row shape, and every other living link in the tree waits for a cold pass that runs after the push.** *What went wrong:* this lap's cold open answered `guards_red=3`, and all three were one root -- `construction/CHECKPOINTS.md` carried `](REDS-a-destructive-act-aimed-by-a-name-rows-487-512.md)`, a shelf that never landed. The cairn was written in a round that folded that shelf, and the same round's rebase **withdrew the fold** when peers shelved `%487`, `%508` and `%512` separately in the same hour. Every word of the cairn stayed true; the one link stopped opening. *What caught it:* `tools/r/readme_reach_witness.rish` at the NEXT lap's cold roster pass, `broken_in_living=1`, with `reds_fold` and `standing_equipment` red beneath it for reading the same scan and for recording those two runs -- one root wearing three red rows, read half an hour after the commit was public. *What it taught:* **a link is checked at commit time only if its own row shape has a wall.** Three exist and each reads one shape -- `tools/fixtures/s/shelf_link_touch_scan.sh` for a day-shelf row (`%383`), `reds_citation` for a `%N` naming a shelf (`%502`), and rule two's headline writer for the ledger's own numbers. A cairn citing an archive shelf matches none of them. And the fault could not have been caught at the authoring commit anyway: the shelf existed when the row was written and the **rebase** removed it, which is `%515`'s own mechanism one reading over -- a commit that lands by a path no hook sees. *Measured `20260906.223436`:* `tools/fixtures/r/readme_reach_scan.sh` reads **1,808 documents in 295ms** and already gates `broken_in_living` at zero, so the whole-tree reading is cheap enough to be paid by the `living-pins-owed` debt mechanism `%515` landed this same lap, rather than wanting a fourth shape-specific wall. *Repaired, the instance only, and not by me:* a peer repointed the cairn at `20260906.222500`, the same minute my own repair was written, and theirs published first -- so mine was withdrawn whole rather than landing beside it, which is the derived spine's rule applied to a repair instead of to a number. The card already records four findings meeting their peer inside one hour today; this is another, and the first where both hands edited one line of one file. *And it fired twice inside the hour.* The rebase that brought this lap current also brought a peer's freshly landed shelf whose anchor text read `%510` while its target named the `rows-511` shelf -- one row named, another opened, which is `%502`'s class, CLOSED that same day with `reds_citation` gating it. The gate is real and it caught this; what it is not is a wall, so the broken citation was authored, committed, pushed, and read by two ships before any instrument spoke. Repaired here by repointing the target at the `rows-510` shelf, the one the number actually names. *And writing this row cost a third instance:* spelling the fault out in link syntax reproduced it, so `readme_reach` and `reds_citation` both read the QUOTATION as a live broken citation from the pin. Named in words here rather than shown, which is the illustrate-with-placeholders clause of [`stamp-and-name`](../../.claude/rules/stamp-and-name.md) meeting a reader it was written for. *Third firing, `20260907.014900`, and it names the mechanism exactly.* A cairn in `construction/CHECKPOINTS.md` cited an `archive/` shelf holding `%523` alone; upstream folded `%523` together with `%527` and `%528` onto one shelf instead, so the separate shelf the cairn named never landed and the link dangled the moment the rebase brought that fold down. Caught by `readme_reach` in a hot pass rather than at commit time -- the same half-hour-late reading this row was booked for -- and repaired by repointing at the shelf the row actually stands on. **Three firings now, all one mechanism: a fold that a rebase reshapes leaves every citation of its planned shelf name pointing at a file nobody wrote.** **Two instances, two hands, one hour, one reading absent from commit time** -- which is a lantern that has already fired twice. *Not taken:* the class. A link debt paid by a fifth pre-commit rule wants its own lap and inherits `%515`'s reason to go slowly -- eight ships run that file, and a mistake in it is a fleet that cannot commit. *Fourth firing, `20260907.134402`, and it is the reason the class was taken.* This tree's cold open answered `broken_in_living=1`: `construction/ITINERARY.md` linked `archive/REDS-a-label-is-not-its-target-rows-571.md` while that shelf stands at `rows-570` -- a peer's fold planned one number, upstream landed another, and `rows-571` went to a different shelf entirely, so the citation opened nothing while naming a file that exists. Repaired by repointing at the shelf the row actually stands on. *Closed by taking the class, and by the cheaper half of the two answers this row weighed.* `tools/fixtures/r/readme_reach_scan.sh` gained one option, `--store worktree|index`, which moves where the bytes come from and changes no other line of the law: the index store reads bodies through one long-lived `git cat-file --batch` and tests existence against the `git ls-files` path set plus its directory prefixes. `tools/hooks/pre-commit` gained **rule six**, calling that scan with `--store index` and refusing on `verdict=living_link_broken`. **The index rather than a staged set, and that is the whole finding**: all four firings arrived by rebase, so no staged-set trigger could ever have seen one -- the break is authored nowhere and lands everywhere. **No trigger of its own**, measured at **544ms** for the whole 1,884-document crawl in two subprocesses, beside rule one's ten-second render. **And a second reader was refused**: writing the commit-time question its own crawl would have put one rule in two files, which is `%382` exactly, so the store is an option on the one scan. Proven both ways -- the pen's index cases rose **8 to 15**, including the two readings where the stores rightly disagree (a repair standing only on disk, and a target staged for deletion the worktree still holds), and the hook control rose **14 to 16** with rule six bitten and then waved through on a real repository under the tree's own hook. *What stays out, named:* dated testimony, which accrete-never-break keeps as written and `dated_path_resolve.rish` recovers, and links outside the front door's reach, which remain `living_docs_lint`'s room. **CLOSED.**
