# REDS shelf -- a wall on a path the work does not take, row %515

**Language:** EN
**Stamp:** `20260906.223436`
**Status:** Shelf -- immutable once written
**Rows:** `%515` -- folded from [`../REDS.md`](../REDS.md) on `20260906.223436`
**Voice:** Kyri

A pre-commit hook refuses a commit that stages a living pin over the byte bound its own header
declares. Git runs that hook for `git commit` and `git commit --amend` and for no other path, and
the fleet's twice-pulled send lands its work by `git rebase` whenever the anointed order moved --
which, on eight ships, is most sends. So the wall stood on a path the work does not take, and six
of the last 120 commits touching `construction/REDS.md` shipped it over its declared bound.

**Why this row folds here, and why now.** It closed on the lap that landed the class repair it had
named and left, so it is the largest CLOSED row standing and the pin's own headroom is what folding
it buys. Booking the row beside it brought `construction/REDS.md` to **40,350** bytes against
**40,960** -- 610 of headroom, which is a judgment call charged to whichever lap opens next. This is
`%517`'s ruling applied by the lap that met it: one fold away from healthy is not healthy, and it is
repairable by whichever lap meets it with no word from anyone.

**What it taught, in one line.** A fast wall and a slow backstop are not two chances at the same
question when the fast one sits on a path the work does not take -- and the sibling that already
noticed those landings, `tools/hooks/post-commit`, was recording a debt for one rule while the other
had no second chance at all.


**REDS %515 (`20260906.211529`) -- the pin wall fires at `git commit` and the fleet lands its work by `git rebase`, so a pin crosses its bound, ships to both remotes, and is caught only by the next lap's cold pass -- after it is public.** *What went wrong:* `tools/hooks/pre-commit` RULE THREE refuses a commit staging a living pin that is over the byte bound its own header declares, reading the size off the index with `git cat-file -s :<path>`. It runs at `git commit` and `git commit --amend` and on no other path. `tools/hooks/post-commit` repairs that skip for RULE ONE by recording a debt in `$(git rev-parse --git-dir)/derived-pages-owed` which the next pre-commit pays -- and records nothing at all for rule three, which therefore has no second chance. On an eight-ship fleet the twice-pulled send rebases whenever the anointed order moved, which today was most sends; one lap took five rebases in one round. *What caught it:* opening this lap and finding `construction/REDS.md` at **41,153** bytes against the **40,960** its own header declares -- 193 over, before this lap changed a byte -- with two witnesses refusing on it, `tools/gen/chapter/reds_ledger_witness.rish` at `verdict=past_bound` and the rostered `tools/equinox/witness/equinox_e123_living_pin_guard_witness.rish` beside it. *Proven on metal:* in a throwaway repository with a pre-commit hook that only records that it ran, four commits -- base, plain, side, amend -- produced exactly four entries, and the `git rebase` replaying the side commit produced none. The hook's own header states the same thing in words; what nothing stated is that rule one's repair was never extended to rule three. *Measured, so the class is not one bad night:* of the last **120** commits touching `construction/REDS.md`, **six shipped it over the bound its own header declared at that commit** -- `ffeec497e` (41,153 over 40,960), `c43ba6b09` (44,003 over 40,960), and `19fc3395b`, `978ab7e78`, `60776bcad`, `5625f857e` against the elder 24,576. Both of today's carry a committer time later than their author time, the signature of a replay. *What it taught:* **a fast wall and a slow backstop are not two chances at the same question when the fast one sits on a path the work does not take.** The roster does catch it -- at the next cold open, which is after the push, so the guard reports history rather than preventing it. *Repaired here, the instance only:* `%487`, the oldest foldable row, folded to its own shelf oldest-first as the ledger scan's own detail line prescribes; the pin reads **37,317** with 3,643 of headroom and both witnesses are GREEN. *A second fold of this lap's was withdrawn on the rebase:* it shelved `%489` alone while a peer was shelving `%489`, `%508` and `%509` together, and theirs published first -- so mine was dropped whole rather than landing beside it, which is the derived spine's own rule applied to a fold instead of a number. **This row's own number moved on that same rebase**, from the `%514` a local read allocated to the `%515` the spine had free once a peer's `%513` landed at `20260906.210117` -- the stamp `20260906.211529` never moved, which is the law working. *The class repair, sized and NOT taken:* post-commit records a pin debt exactly as it records a page debt, and the next pre-commit pays it by weighing the rostered pins rather than only the staged ones -- symmetrical with rule one, and refusing the NEXT commit for a pin the previous one landed, which is how rule one's debt already behaves. It is left for its own lap because eight ships run that file and a mistake in it is a fleet that cannot commit; the same reason `%499` and `%512` name rather than reach. *The class repair landed `20260906.223436`, in the shape this row named:* `tools/hooks/post-commit` writes `living-pins-owed` in the git directory beside `derived-pages-owed` whenever a commit lands without `pre-commit-ran`, and rule three reads that marker to choose its mode -- `staged` while no debt stands, and a new `index` mode of `tools/fixtures/p/pin_bound_touch_scan.sh` while one does, weighing EVERY rostered pin off the index rather than only the ones this commit's diff names. `head` was refused as too narrow: a rebase replays many commits and only the last one's tree is readable that way. *Proven by doing, and the pen plants nothing:* a third throwaway repository arms both hooks and makes two commits each welcomed by its own wall -- a peer appending to the end of a pin, our own line prepending to the top -- then lets `git rebase` auto-merge them into **119 bytes against a bound of 100**, a blob neither side committed and no wall ever weighed. Against HEAD's hooks the next ordinary commit passed free; against the repaired pair it is refused, naming `detail_path` and `detail_over_by=19`. The pen's `cases_run` went **36 -> 54**, thirteen of them this reading, each asserted by name in the witness rather than counted -- and the control now prints its own total, since the two headers and the witness's own GREEN line each carried a number that had stopped being true. *Measured:* the whole-roster reading costs **437ms** against the staged reading's **106ms**, paid once per unseen landing rather than per commit, so the cost argument the scan's own header makes survives intact. *Residue, named:* the debt refuses the NEXT commit, so a rebase followed straight by a push with nothing between them ships an over-bound pin once. The tree's own send puts a Git-nib amend there (`remember-git-nib` rule 2) and git DOES run pre-commit for an amend, so the ordinary send path is closed; a send that rebases and pushes with no amend between is the gap, and a `pre-push` wall is a wider blast radius than a pre-commit one and wants its own lap. **CLOSED.**
