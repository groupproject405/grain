# ITINERARY -- landed accounts, folded from the live front

**Folded:** `20260907.191757` -- **Status:** Archived, complete, never edited
**Living card:** [`../ITINERARY.md`](../ITINERARY.md)

*The Patchouli account of the lap that closed the weave's interleave, folded off the live front on
the third recovery of that same lap, so the card could hold the recovery beside the work it
recovered. The full record is the session log
[`20260907-173200_two-paragraphs-come-back-whole.kyri`](../../session-logs/date/20260907/20260907-173200_two-paragraphs-come-back-whole.kyri)
and its successor at `20260907-175632`.*

---

**TWO BRANCHES APPEND A PARAGRAPH EACH; BOTH COME BACK WHOLE.** The interleave -- the last gap
`mantra/src/weave.rye`'s head named -- **is closed.** `LineId` answered identity AND order, so two
sides appending three lines each shared three positions and the site settled the tie **three
times**. Order is its own type now, **`Place`**: run, site, position, where a **run** names one
`apply` and is read once before its stamping loop. **The identity pair is untouched**, so the order
was repaired with every line's name left as written. Claim 10 counts **one** site change over six
added lines where a shuffle gives five, proven from the failing side.

**AND A PLANT MATCHED AND STILL TESTED NOTHING** -- `%519`'s sibling, which `cmp -s` cannot see, on
[its own shelf](20260907-175554_itinerary-landed-accounts.md) with the two closures beside it:
`%441`'s erratum reached the foundation, `width_check_th3` is rostered.

**AND THE WHOLE LAP WAS PARKED AT THE NEXT ROUND OPEN, PROVEN AND UNSENT** -- thirteen files,
`%499` again. `git stash apply` returned every one against a head two commits on, one conflict in
the fold recital where **both** folds belong. **The park cost the recital line, the row's number and
that paragraph; the work cost nothing**, because it was parked whole rather than half sent. That was
the second park of three; the third is on the live card.

**THEN IT WAS PARKED A THIRD TIME.** `stash@{0}` at `20260907.183234`, twenty files, all returned
by `git stash apply` with three conflicts where two hands wrote at once. **A published fold does not
move**: a peer's `%576` shelf banner and recital line stand, mine dropped. **The row renumbered a
third time**, `%582` to `%583` to `%584` to `%586` and on to **`%587`** off `--next` after a fetch -- one `sed` on one site,
because `%513` means every living citation spells the stamp. The ledger came back 307 bytes over,
closed by folding BOOKED `%578` to its [shelf](REDS-a-grep-for-a-path-rows-578.md).
**Six witnesses GREEN on metal**, since a clean apply proves a patch and never a program.
**`%499` costs one lap per park**, and the park's argument -- whole rather than half sent -- holds.


**AND `%519` FIRED A THIRD TIME, ON THE LAP THAT LANDED ITS SECOND.**
`mantra_a1_equality_control.sh` spelled `(eq sample 2)` in three plants; this lap's weave desk moved
to **3**, so all three matched nothing and two phases gave **exit 0** where 134 is owed, against a
correct desk. **The literal is gone:** `decided` reads the desk's own `(eq sample N)` line and the
three plants derive from it. Control **8 phases GREEN**.

