# REDS -- row %821, folded from the living pin

**Folded:** `20260917.204500` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A doorway guard passed a page on a token belonging to the key next door, and the measurement that
found it was run to disprove the lap's own written prediction.*

The row stands here exactly as it was written. A booked row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the reader the round
published, and the row itself stays one click away.

---

**REDS %821 (`20260917.182000`) -- a doorway guard passed a page on a token belonging to the key next door.** *What went wrong:* `tools/fixtures/t/two_rooms_doorway_scan_one.sh` read a door key as `**Status:**` and everything after it on the line. This tree folds several keys onto one header row joined by ` - `, so the read ran through its own value into the neighbour's, and a room token standing in a NEIGHBOUR key answered for this one. `foundations/20260823-111029_the-seed-that-ships-every-fifth-round.md` passed the two-rooms gate that way: its `**Status:**` says only `Living`, and the word `Mixed` the guard found stood inside the `**Style:**` key beside it. *What caught it:* a measurement run to DISPROVE this lap's own claim. The claim predicted **zero** verdict flips under a cut reader and said so in writing before the build. Over the 586 living tracked pages the prediction held. Over the guard's OWN 1,363-page roster it read **one** -- 130 joined key reads, 47 of them reaching past their own key. *What it taught:* **a prediction is only as true as the population it was taken over, and a guard's population is its own roster rather than the tree's.** The same lesson `20260915.223327` booked one lane over -- a claim prices its class from the files it can already see -- arriving here as a prediction that was right about the wrong corpus, made by the hand that wrote the cut. The second lesson is the cure's shape: the fault lived in a reader written **three times**, once per scan, so it was repaired once. `key_value` in `tools/fixtures/s/shell_portable.sh` cuts a bolded key's value at the next bolded key, and `two_rooms_doorway_scan_one.sh`, `qa_report_card.sh`, `qa_setting_declared_scan.sh` and `front_door_claim_scan.sh` all read through it now. *Repaired:* the exposed page ACCRETED a `**Room:** mixed` line beside what its Style line already said -- one insertion, zero deletions, no word of dated testimony rewritten -- and the doorway reads `pages=1363 fails=3 ceiling=3 living_silent=0` exactly as before, now on the right reading. The two latent readers are verdict-identical on metal: 272 Style lines, `setting_named=79` before and after; `claiming_pages=8 claims=8 claims_unkept=0` before and after. *What this does not reach:* the other whole-line key reads this tree writes. 225 joined header lines carrying 639 keys stand across the living pages, and only the keys four scans classify on were measured. **BOOKED** -- every instance found is repaired, the reader is published, and the remaining surface is a ratchet that turns on touch.
