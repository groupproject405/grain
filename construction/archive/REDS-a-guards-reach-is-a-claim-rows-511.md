# REDS shelf -- a guard's reach is a claim, row %511

**Language:** EN
**Stamp:** `20260906.204500`
**Status:** Shelf -- immutable once written
**Rows:** `%511` -- folded from [`../REDS.md`](../REDS.md) on `20260906.204500`
**Voice:** Kyri
**Kin:** the card account this row folded out of rests on the
[`195215` landed-accounts shelf](20260906-195215_itinerary-landed-accounts.md).

One guard read its subject through a filter narrower than the subject itself, printed a green
verdict, and the numbers that would have shown the gap were already on the page with nothing
naming them. A filter is a claim about a subject, and a guard whose filter is narrower than its
box reports on the box. The repair is not a wider gate: print the remainder, name it by line, and
let a reader judge it.

**This row was recovered from a park, and it travelled with a twin that did not survive.** The lap
that found both died between its last witness and its send, so `fleet_round_open.sh` stashed the
whole package and the next lap on this tree opened on `stash_record` answering red about its own
author's work. The rows were booked `%509` and `%510` there. **The second is WITHDRAWN**: it named
the dead-letter reading's blindness to a stash carrying no session log at all, and a peer published
[`%510`](../REDS.md) at `20260906.194500` naming the adjacent half of
the same reading -- a record that landed while its work did not -- with an instrument that walks
**every non-record path in every stash** and so closes this blindness too. Measured on this tree
after taking theirs: `paths=42 orphans=3 unread=39`, and the recordless stash is named by line.
`%484`'s rule decides it -- one defect belongs to one record, and theirs published first. This is
the second withdrawal this tree has made in one day, and the second where the fetch before the send
is the only reason it is a duplicated hour rather than a duplicated roster seat.

---

**REDS %511 (`20260906.164437`) -- a guard built to read `Row N folded to` citations read the spelling one page writes and missed the one the ledger's own law requires, so it saw 47 of 192.** *What went wrong:* the FOLD form in `tools/fixtures/r/reds_citation_scan.sh` required a BARE number -- `Rows?[ ]+[0-9]+(-[0-9]+)?[ ]+folded to` -- while the fold recital writes `Row %184 folded to`, carrying the `%` sigil [`git-signing.md`](../../.claude/rules/git-signing.md) seats for every number this tree assigns itself. The form was drawn from the fold INDEX, which spells it bare, so **145 citations of the very shape the form was built for stood outside it**. Two further spellings were missed the same way, both NUMBERED: five anchors reading `REDS %108` rather than `%108`, and one reading a sigilless `488`. Measured on one tree, the elder reading covered **66 of 284** living shelf links. *And a second fault beneath it, opposite in sign:* `holds()` split a path on `-` and read the two numbers as a SET, so a citation naming a row INSIDE a span counted as a disagreement. Read on metal, `REDS-the-integration-and-its-wake-rows-242-254.md` carries rows 242, 245, 246, 247, 248, 251 and 254, and `REDS-the-booked-remainders-rows-291-330.md` carries eight of its own -- so the set reading calls **18 honest citations wrong**, which is the false-positive class that already cost `%505`'s round its first form. *What caught it:* `%505` printed `unread_links` instead of only what it gates, and its own recommend named reading that remainder as the next lap. The fault was inside the remainder: of 215 links called unreadable, **151 wore a shape the guard already claimed to read**. *What it taught:* **a form drawn from the page in front of you is fitted to a sample, and the tree's own law is the wider sample.** The spelling table is now measured with the pattern itself and printed in the header -- 79 `Row %N`, 34 `Rows %N-%N`, 25 `Rows N-N`, 23 `Rows %N and %N`, down to four lists running to eight members -- and `through` is deliberately ABSENT, since the recital writes `Rows %1 through %172 are folded` and never `folded to`; a form reading a spelling nobody writes is this same fault inverted, so pen case 32 holds that absence rather than trusting a reader with it. *Landed:* reach **66 -> 219 of 284**, zero disagreements, and the 65 still unread named by shape -- 61 repeat their own path in the anchor, four read `folded` or `their own`. Pen **33 -> 52 cases**, every new refusal planted and then lifted. *Erratum inside the lap:* a reworded comment carried an apostrophe into the single-quoted `awk_prog` string, ending it early and turning 49 pen cases into empty reads rather than one loud syntax error; `sh -n` named it in one command, and a shell quote that fails quietly is the worse half of that pair. **CLOSED.**
