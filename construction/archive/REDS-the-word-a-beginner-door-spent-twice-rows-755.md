# REDS -- the word a beginner door spent twice

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, closed on a grading and a census run on metal
**Folded:** `20260916.002424` from [`../REDS.md`](../REDS.md)

One row, born onto its shelf because the pin stood at 65,513 bytes of its 65,536 bound and
`reds_pin_capacity_scan.sh` read `rows_that_fit=0`. It teaches that a repair sweeping a page and
its declared mirror sweeps exactly what the mirror law reaches, and that a second page carrying
the same sentence is reached by nothing at all -- so the question a repair owes is *who else wrote
this*, asked as a grep rather than remembered.

**REDS %755 (`20260916.002306`) -- a formula on a beginner door named an operation the tree does not declare, six days after the same formula was repaired one room over.** *What went wrong:* `foundations/20260825-211056_what-mantra-is.md`, the page written for a complete beginner and cited by the root `README.md` and by `docs-geode/tutorials/the-first-hour.md`, read `**The weave** is what happens when many bindings are read together` above the block `weave(name_1 ... name_n) -> bytes_1 ... bytes_n (order preserved, n bounded)`. Three faults in one paragraph. The tree declares no `weave` that reads many bindings -- the operation is `build_batch` in `mantra/resin_batch.rye`, and the module's word is **batch**. The bound is real and was left unnamed: `n bounded` promised a ceiling where `max_batch_entries` is 16, walled by `error.TooManyEntries` returned before `build_batch` writes its first byte. And `weave` had already been given, on `20260905`, to the living line history at `mantra/src/weave.rye` -- which this same page meets forty lines further down, introduced as Mantra's **second** promise, so one page spent one word on two referents and told the reader they were different things. *What caught it:* the air rota row -- law and boundary, read by touch -- pressing the page's own claim that *its bound is named before the read*. The hand went through: no operation, no `n`. A grep for the notation then found the same formula in `context/CHEMICAL_FORMULAS.md` carrying a repair already made, commit `a65642872` on `20260909`, whose body reads *"no module in this tree declares a function, constant, or type by that name doing a bulk read."* *What it taught:* **a declared mirror is a guarantee about two files and a claim about none of the others.** That repair swept the formula page and `biochemistry/README.md`, its mirror, proven byte-identical by `tools/fixtures/d/document_mirror_scan.sh` -- and the foundation carrying the same sentence sat outside the mirror, outside the commit, and outside every guard, because `qa_report_card.sh` scores Truth by whether cited paths RESOLVE and a formula cites no path at all. A second firing, six days apart, of a defect whose cure was already written down. *Repaired (`20260916.002306`):* the paragraph now reads `batch(bolt, revision) + held -> manifest + bytes`, the same line its sibling page carries, with one beginner sentence naming bolt, revision and `held`, and with `max_batch_entries` 16 and `TooManyEntries` said out loud. An erratum at the foot quotes the elder paragraph whole, so accretion keeps every word the body no longer serves the reader with. The page grades **A/90** at Door setting -- register 15% of 58 sentences against the 20% ceiling, reach 100, 0 of 9 cited paths unresolved, truth judged. *Closed on a census rather than on a guard:* `grep -rnE "^[a-z_]+\([^)]*\)[^-]*->" --include=*.md` over living rooms reads **7 formula lines across 4 pages**, naming three operations -- `batch`, `infusion` and `weave` -- and every one now resolves to a declaration on metal: `mantra/resin_batch.rye:132`, `brix/infuse.rye:96`, and `mantra/src/weave.rye`, where the third survives only inside the erratum's quotation of what it replaced. *What this does not reach:* no instrument reads a formula block and asks whether its operation is declared. The census above is a command rather than a guard, and the next page to write a formula is held by habit. **BOOKED.**
