# REDS -- the program awk refused

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven on metal by three re-run guards
**Folded:** `20260910.060100` from [`../REDS.md`](../REDS.md)

One row, folded by the seat that booked it so the pin could hold the next. It teaches that an
interpreter refusing a program is louder than a guard reading only stdout, and that which
interpreter refuses has to be measured rather than assumed: grep and sed read the escaped quote as
a plain one, awk warns inside a regex and carries on, and only the awk string literal is fatal.
Read it beside [`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %690 (`20260910.031353`) -- Rishi hands awk a program awk refuses, and three guards read that silence as law.** *What went wrong:* Rishi leaves `\"` alone in a string literal, so the backslash reaches `sh`. Inside a SINGLE-quoted region -- how every awk program here is written -- both land in the program text and **awk refuses a string literal spelled that way**: syntax error to stderr, nothing on stdout. A `wc -l` downstream answers 0, and the pipeline's exit is the LAST command's, so `.ok` reads true. Three stood green on it, proven on metal: `bricks_exist` read **0 of 20** brick paths while saying *all 20 resolve*; `gen_home`, rostered `tier lap`, left a **planted stray desk** in the wrong room GREEN; `reds_row_present` answered **0 missing of 7** where 5 were, so *every spine row found* was a sentence no run had checked. *What caught it:* a door one room over. `width_check_th3` opened `let files = [four paths]` while its header said it gates `mantra/src/` -- **13** tracked sources inside a `mantra/` of **41**, `mantra/src/parse_int.rye` landed `20260713` and **eight weeks** unread. Giving it a prove-red leg put a `\"` under my eye. *What it taught:* **an interpreter that refuses a program is louder than a guard reading only stdout, and the guard wins.** The sharper half: **which interpreter refuses had to be measured** -- grep and sed read `\"` as a plain `"`, awk warns in a regex and works, and only the awk STRING LITERAL is fatal, so a wall around the whole spelling would have refused **18** working witnesses. *Repaired:* all three pass the literal in as an `awk -v` argument, re-run green, `gen_home` RED on its plant. `rishi_quoted_program_scan.sh` walls `awk_fatal` at zero, reports 18 tolerated sites under a falling ceiling, and narrows 2,458 sources with one `grep -l`. The control proves **16** behaviors in a pen, refusals planted then lifted, its first two legs **asking awk itself** whether the plant breaks and the repair reads. `rishi_quoted_program_witness.rish` rostered `tier lap` at 1.5s, RED and green on the real tree. TH-3 derives its room, widens to all 41, prints its symlinks, and reds on two `.lap/` plants first. **CLOSED.**
