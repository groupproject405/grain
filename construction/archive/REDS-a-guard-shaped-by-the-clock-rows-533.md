# REDS shelf -- a guard shaped by the clock, row %533

**Language:** EN
**Stamp:** `20260907.003911`
**Status:** Shelf -- immutable once written
**Rows:** `%533` -- folded from [`../REDS.md`](../REDS.md) on `20260907.012000`
**Voice:** Kyri

A guard that counts **today's** session logs has nothing to count before the day's first log lands,
so its scan refuses -- correctly, since a scan that cannot measure refuses rather than reporting
clean. Rostered without a capability, that refusal read as a fault at every midnight, on every ship.

The cost is not the red. **A red guard withholds the roster receipt**, so `--scoped` refuses and
every ship pays a FULL cold pass -- 961s of guard time on the pass that found this -- for a fact
about the clock. And `standing_equipment` reddens beside it for reading its own roster's red, so one
root wears two rows.

**Repaired** by `capability day_shelf`, a fourth arm in `capability_state()` reading `ROTA_DAY` and
`git ls-files` exactly as the guard's own scan does -- **both halves of its precondition**, since a
shelf whose only log is untracked is precisely the state a filesystem-only probe would call present
while its guard refused. A missing `git` answers **unknown**, which RUNS, because absence is the
only answer allowed to skip. Ten behaviors are planted in a real git repository.

**What it adds to `%493`, which seated this shape one arm over:** a daily red is worse than a
standing one. It heals itself an hour later, so it looks like somebody's fault and then vanishes,
and nobody books what is gone by breakfast.

**Why this row folds on the lap that wrote it.** A peer's row landed beside it and left
`construction/REDS.md` **2,692 bytes over** its 40,960 ceiling. This was the only unambiguously
CLOSED row standing, and it is this seat's own -- so folding it needed no peer's agreement, which
matters on a lap that had two folds withdrawn already for exactly that reason.

**Still open, and not taken here:** whether any other rostered guard is shaped by the clock this
way is unmeasured.

**REDS %533 (`20260907.003911`) -- a guard that counts today's logs reddens the whole fleet at every midnight, because a day that has not started yet reads exactly like a bench that cannot measure.** *What went wrong:* the first cold pass of `20260907`, opened at `00:12`, answered `guards_red=2` -- `rota_declared`, and `standing_equipment` reading its own roster's red: one root wearing two rows. `tools/fixtures/r/rota_declared_scan.sh` counts how many of TODAY's session logs declare the rota row they read, and it refuses when `session-logs/date/$(TZ=America/New_York date +%Y%m%d)` holds no tracked `.kyri`. That is the state of every day between midnight and its first landing, on every one of eight ships. *What caught it:* the roster's own cold open at the head of this lap, and one command told the calendar from the census -- the same scan reads `logs=127 rota_field=25 prose_only=59 silent=43` against yesterday's shelf and refuses against today's. *What it taught:* **a red guard withholds the roster receipt**, so `--scoped` refuses and every ship pays a FULL cold pass -- 961s of guard time on this one -- for a fact about the clock. That is `%493` one arm over, and its ruling already named the answer: the roster is where an environment fact belongs, and absence is announced by name rather than made silent. **A daily red is worse than a standing one**, which is the reading this row adds: it heals itself an hour later, so it looks like somebody's fault and then vanishes, and nobody books what is gone by breakfast. *Repaired:* `capability day_shelf`, a fourth arm in `capability_state()` reading `ROTA_DAY` and `git ls-files` exactly as the guard's own scan does -- **both halves of its precondition**, since a shelf whose only log is untracked is precisely the state a filesystem-only probe would call present while its guard refused. A missing `git` answers **unknown**, which RUNS, because absence is the only answer that may skip. Ten behaviors planted in a real git repository under `tools/s/standing_equipment_witness.rish`: no shelf, an untracked log, a committed one, the pen proven innocent, and the scan reading the new word off the runner's own arms rather than a second list. *And the elder card row is dropped by machinery already built* -- `%493`'s second half, which drops the row of a guard this host cannot run, because a verdict recorded in a different world is one nothing here will ever overwrite. *One thing beside it:* `sow_allow_reach`'s own capability comment stood 37 lines below its row and directly under `rota_declared`, where the line this repair adds would have read as annotating the wrong guard; moved to the row it has always been about. **CLOSED.**
