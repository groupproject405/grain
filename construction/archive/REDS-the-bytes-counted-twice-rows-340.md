# REDS -- row %340, folded from the living pin

**Folded:** `20260829.033150` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A character census followed symlinks, so it counted one set of bytes twice and set its ceiling on
the doubled reading.*

Booked and repaired inside one lap, born on its own shelf as %336 through %339 were, since the
pin stands five bytes under its bound and every elder row still reads OPEN. The lesson it carries:
a meter that asks the filesystem to resolve a path counts the destination once per road that
reaches it.

**REDS %340 (`20260829.033150`) -- the bytes counted twice: a census followed 56 symlinks and set its own ceiling on the doubled reading.** *What went wrong:* `tools/fixtures/r/rye_comment_ascii_scan.sh` enumerates with `git ls-files "*.rye"`, which lists a symlink **and** its target as two separate paths, then opens each one -- so every linked module's comment characters were counted once for the link and once for the file. On `20260829` commit `edf0dd3754` added `tools/rye/sha3.rye` and `tools/rye/keccak256.rye` as symlinks into `crypto/`, whose 32 non-ASCII characters had stood unchanged since `20260826`; the meter read **4,342 against a ceiling of 4,333** and reported a rise that never happened. Not one character had been added to any comment in the tree. *What caught it:* the roster's re-verify pass on a rebased tree, and then arithmetic rather than a sweep -- `4,342 - 32 = 4,310`, twenty-three UNDER the ceiling, which said the content was innocent and the count was not. *What it taught:* **a meter that resolves a path counts the destination once per road that reaches it**, and the tree already knew this one meter over: `tools/fixtures/w/witness_reach_scan.sh` hit the same root on `20260828` -- 270 tracked symlinks, 23 added under `brushstroke/` in one evening, one mark read twice. **A lantern that fires twice becomes a loom**, so the reading is written into the scan rather than remembered. The idiom and its sentence are the tree's own, from `tools/fixtures/c/caravan_wrap_class_scan.sh`: *a symlink is skipped -- its laws belong to the room its target lives in.* `tools/fixtures/c/caravan_ladder_roster_scan.sh` deliberately reads the other way with `-type f -o -type l`, and **both are right**: that meter asks which modules a room HAS, where a shared body reached by a link is genuinely one of them, and this one asks how many characters EXIST, where a byte counted twice is a byte miscounted. The distinction is what the meter is asking, and it belongs in the comment beside each choice. *Repaired (`20260829.033150`):* `[ -L "$f" ] && continue` in both ASCII censuses -- the Rye meter and its shell sibling, which carries the same enumeration and would take the same wound the first time a `.rish` or `.sh` symlink lands. **The ceiling fell with the fix, 4,333 to 3,794**, and that gap is the finding rather than a bonus: the two new links were 32 characters, and skipping symlinks removed **548 across 56 of them**, so this meter had double-counted every linked `.rye` since it was written and its ceiling carried **539 characters of phantom headroom**. A ceiling only falls, and this is the fall the correction owes. `tools/fixtures/r/rye_comment_ascii_control.sh` grows from seven proven behaviors to nine, planting a link beside its target in a throwaway git repository -- the link uncounted, **and the target still counted on its own row**, so the skip is proven to drop a duplicate rather than a file. Proven from both sides on metal: with the skip removed the same control reads `files=5 chars=5`, `symlink_skipped=no`, `total_is_four=no`, and the scan was restored byte-identical afterwards, checked by SHA-256. `tools/as/ascii_comment_witness.rish` GREEN, with both readings asserted by name. **CLOSED.** *What this does not reach:* every other meter in this tree that enumerates with `git ls-files` and then opens what it finds. Two are now known to have taken this wound and both are repaired; a census of the rest is named here as its own lap rather than swept blind, since the right answer differs per meter and only the meter's own question decides it.
