# REDS -- row %307, folded from the living pin

**Folded:** `20260828.125441` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A living document is edited for content, and its meter reads a lap later or never.*

This row was booked open and closed twice: once in `%308`'s row, which said so and then folded to a
shelf, and once here, eight hours later, when a reds-first lap chose `%307` as its work and found
the guard already built, rostered, and green. The second closure is the one a reader of this row can
see, which is the whole lesson of `%313` -- the row that reads a ledger against itself.

Its own repair reached the template a prompt is written from. `%308` reached the launcher a hand
pastes. Between them the loop-prompt surface gained a guard that reads twenty-seven instruction
files against the custody gate list, and this row folded once its closure stood where the closed
row is read.

---


**REDS %307 (`20260828.011621`) -- a living document is edited for content, and its meter reads a lap later or never.** *What went wrong:* two faces, one root. `recursion-prompts/seed/autonomous-loop.seed.md` still told an unattended lap where `N mod 5 == 0` to run `bash ~/grain/publish-seed.sh` -- the force-push custody gate %1 holds at Keaton's hand and `construction/ITINERARY.md` CUT on `20260826`. Six loops fill from that template, which the manifest scrubs public. Beside it `radiant_negation` read `NEGATION_ROSE` on three rules -- `checkpoint.md` 1.47 to 1.67, `lindy-first-crux.md` 1.17 to 1.19, `quin.md` 0.00 to 0.68 -- each raised by a correct `20260827` edit that added a clause and moved a number nobody re-read. *What caught it:* the cold roster for the second; the rota's own grid read for the first. *What it taught:* **a custody gate is only as strong as the instructions reaching the hand it gates.** *Repaired:* the section names the cut and points at gate %1; the three densities read at or under baseline, `enforce_risen=0`, claims held exactly; `radiant_negation` and `loop_prompt_parse` GREEN on metal. *Stood open on booking* -- no guard read an instruction file against the gate list. **CLOSED** (`20260828.033212`) -- `%308` wrote that guard the same night: `tools/fixtures/custody_gate_instruction_scan.sh` reads the whole loop-prompt surface, twenty-seven files discovered from `git ls-files` rather than typed, and gates at zero any of three gated command tokens standing in a clause carrying no refusal word beside it; `tools/cu/custody_gate_instruction_witness.rish` is rostered at tier `lap` and GREEN on metal. The closure stood only in `%308`'s own row, which folded to `construction/archive/` the same day, so this row read open for the eight hours until `%313` read the two against each other.
