# ITINERARY -- landed accounts, folded from the live front

**Folded:** `20260907.175554` -- **Status:** Archived, complete, never edited
**Living card:** [`../ITINERARY.md`](../ITINERARY.md)

*This lap's own instrument finding, folded off the live front the same round it landed, so the card
could hold the recovery beside the work. The account it belongs to is the Patchouli block naming
the closed interleave; the full record is the session log
[`20260907-173200_two-paragraphs-come-back-whole.kyri`](../../session-logs/date/20260907/20260907-173200_two-paragraphs-come-back-whole.kyri).*

---

**A PLANT MATCHED AND STILL TESTED NOTHING.** `%519` named the plant that matches nothing, which
`cmp -s` catches by reading bytes. This is its sibling, and it slips past that reading entirely.
The `tiebreak` phase of `tools/fixtures/m/mantra_weave_merge_control.sh` aimed its `sed` at a
comparison line inside `LineId.less_than`, and that line **still stood** after this lap moved
document order out into `Place`. So the pattern matched, the bytes changed, the build succeeded,
and the phase exited 0 -- the function it broke had stopped deciding anything.

**A plant is proven by the red it draws, and only by that.** The verdict loop's demand that every
break be caught is what saw this one, answering `break_not_caught` where the byte comparison
answered fine. Repointed at `Place.less_than`, with `run_ignored` added beside it, the control
stands at **9 phases and 8 breaks**, up from 7.

**Two closures ride with it.** `%441`'s erratum reached
[`foundations/20260825-211056_what-mantra-is.md`](../../foundations/20260825-211056_what-mantra-is.md),
which had called the weave unbuilt while four rostered guards read it every lap; the page reads
**A+ 97** after the repair. And `width_check_th3` joined `construction/standing-equipment.kyri` at
`tier lap`, closing a note this seat carried unrostered for two laps.
