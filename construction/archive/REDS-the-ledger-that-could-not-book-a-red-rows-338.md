# REDS -- row %338, folded from the living pin

**Folded:** `20260829.031804` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`../../.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Four guards read the ledger every lap and all four were green, because each of them asks whether
what is written is consistent and none of them asks whether anything more can be written.*

Born on its shelf as %335, %336 and %337 were, since the living pin stands at 24,571 of its 24,576
bytes and every row in it reads OPEN. That is this row's own subject, so it is written where its
subject put it.

**REDS %338 (`20260829.031804`) -- the ledger of what we got wrong could not accept another row, and no meter was asking.** *What went wrong:* `construction/REDS.md` stood at **24,571 bytes of the 24,576** its own header declares -- **five bytes** of headroom against a **median row of 1,983** -- with **all nine** of its rows marked OPEN. `tools/fixtures/r/reds_fold.sh` refuses `row_open` by design and by a right reason ("the pin keeps what is open; folding one would hide live work on a shelf nobody reads for live work"), so with nine of nine open there was **no lawful fold**, no way to make room, and nowhere for a new red to go. The card recorded the consequence plainly: **four reds went unbooked on `20260828` for that reason alone**, and three more -- %335, %336, %337 -- were born directly onto single-row shelves instead. One of those, **%337, is still live**, which is precisely the state `reds_fold.sh` refuses a fold to prevent: a lap reading the pin under reds-first sees nine of the ten open reds. *What caught it:* measuring the pin rather than trusting four green guards. The card's Now block named "one structural" in a sentence; the numbers behind it had never been taken. `reds_ledger_monotone_scan.sh`, `reds_status_consistency_scan.sh`, `reds_spine_derive_scan.sh` and `reds_ledger_headline_scan.sh` all read this ledger and all four were green throughout, because **every one of them asks whether what is written is CONSISTENT and not one asks whether anything more can be WRITTEN.** *What it taught:* two rules, and the first is the sharper. **A status field that answers two questions will eventually be read for the wrong one, and the reader that suffers is whichever tool was written against the narrower meaning.** The pin's single OPEN flag carries *the defect is live* and *a remainder is booked* at once, and the fold tool can only read the flag. Measured this lap: **not one of the nine was open on an unrepaired defect** -- two stand on ratchets (%311, %334), four on seats awaiting Keaton's word (%306, %326, %327, %328), three on booked future laps (%291, %301, %330) -- and reds-first is explicit that *a ratchet turns on touch and books nothing*. The second: **consistency and capacity are different questions**, so a ledger can fill up in full view of four green guards. That is REDS %301's own sentence one instrument over -- there a meter discovered rooms by a property only some rooms have; here four meters asked a question that was never the one that mattered. *Repaired (`20260829.031804`), the blindness rather than the deadlock:* `tools/fixtures/r/reds_pin_capacity_scan.sh` over the shared row reader `tools/fixtures/r/reds_pin_capacity_rows.awk`, proven by `tools/fixtures/r/reds_pin_capacity_control.sh` on real files in a throwaway pen and gated by `tools/r/reds_pin_capacity_witness.rish`, GREEN on metal. It **gates `phantom_recital_shelves` at zero** (a recital line naming a shelf not on disk is the trail ending mid-sentence), **ratchets `unrecorded_shelves` at 62 and `shelf_open_rows` at 2** under ceilings that only fall, and **reports, never gates**, the capacity table and `pin_deadlocked` -- reported because raising a page's bound is Keaton's word, seated that way once already for `session-logs/README.md`, and a gate that reds on ordinary work is a gate someone turns off. **31 control cases**, both ceilings shown from both sides, every refusal planted and then removed, the absent-pin misuse refused at exit 2. The row reader is spelled **once** and called twice, for the pin and for every shelf, and the witness **proves** this scan's count against `reds_status_consistency_scan.sh`'s own rather than promising they agree (`pin=9 shelf=1 spine=10`). *Two reds found inside this repair, both worth the space:* the control's own harness swallowed every expected refusal, because `( cmd ); echo $?` under `set -e` aborts the subshell before the echo runs -- three cases read `no` while the scan was correct. And the witness's pen rows were eaten by `printf` reading `%1` as a conversion specifier, so its **deadlock case passed vacuously on zero rows** before the green path beside it failed and exposed both. That is REDS %311's lesson -- *the framing a harness prints is not evidence* -- reproduced inside the lap that cites it, which is the honest reason it is written here rather than smoothed over. **OPEN** on the seat it names, and on nothing an agent may close: **the deadlock itself stands.** Three doors are costed in [`../../active-designing/20260829-031804_the-ledger-that-could-not-book-a-red.md`](../../active-designing/20260829-031804_the-ledger-that-could-not-book-a-red.md) -- raise this page's bound as a per-page exception (the arithmetic is `n x 1,983 + 5,044`), split the OPEN flag into the two things it is saying so `reds_fold.sh` can fold a booked remainder while still refusing a live defect, or sanction the single-row shelf birth in the fold tool's own contract. Only the second removes the cause. Which one, and when, is Keaton's word. **`shelf_open_rows` rose 1 -> 2 on this row's own account**, since it had nowhere else to go, and it falls to zero the day the pin can hold both. **CLOSED (`20260829.160120`)** -- door B landed on Keaton's word `20260829`: BOOKED split from OPEN in reds-first, `reds_fold.sh` and both status scans read the last marker one way, eight booked rows drained to their shelf, and the pin fell 24,828 to 5,388 bytes with every ledger witness GREEN (`344e349b2a`). The deadlock this row names is over, and `shelf_open_rows` falls to zero as this sentence's elder predicted -- healed where the rows lay rather than moved.
