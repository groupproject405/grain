# The REDS pin's bound raise -- the account, folded off the pin

**Language:** EN - **Stamp:** `20260917.071327` - **Voice:** Kyri - **Style:** Gauge at Meter
**Status:** Landed account - **Room:** Checkable -- every figure here was measured on the day it was written
**Folded from:** [`../REDS.md`](../REDS.md), whose `**Bound:**` header line carried it

*Why it moved.* The ledger's `**Bound:**` line had grown to 904 bytes -- an eighth of the page's
5,002 bytes of prose, beside 61,507 bytes of rows -- and every word of it argued a decision that had
already been taken. A header field declares; an account explains. So the declaration stays on the
pin and the explanation stands here, which is the same move the fold recital made off the same page
on `20260825.132121` for the same reason.

*What it said, kept whole:*

> **Bound:** under `living_pin_max_bytes[construction/REDS.md]` (**65536**, raised `20260915.174500`
> on Keaton's word from 40960). **The elder number modelled 8 open rows; the pin holds 16.** Measured
> at the raise: 39,596 bytes, 1,902 of header, 16 OPEN rows at a mean of 2,355 each, and **1,364
> bytes of headroom against a row that needs 2,355** -- so the ledger could receive no new red at
> all, and five days of fleet findings had nowhere to land. The new number is 24 rows at the
> measured mean plus header, rounded up to the next 4,096: room for three open reds per ship rather
> than two. **The raise unblocks the ledger and leaves the real condition standing:** 16 rows OPEN
> and none foldable means little has CLOSED in a long while, and a bound is the wrong instrument for
> that. A row folds when its instances stand repaired; the cure is closing rows, not carrying them.
> The general bound stays 24,576.

*What two days did to its own forecast.* The raise bought room for 24 rows on `20260915`. On
`20260917.071327` the pin stood at **20 open rows and 65,548 bytes against 65,536** -- over the new
bound already, two days on, with `pin_foldable_rows=0`. The raise's own closing sentence had named
exactly this: *a bound is the wrong instrument for that.* It was right, and it was right twice --
once about the elder number and once about its own.

*What the pin kept.* Its bound, its key, its three-field law, its `## What the pattern says`
reading, and every open row. Nothing about a red moved.

