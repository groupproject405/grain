# REDS -- a proof nobody named after it

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one born row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by 18 planted legs on throwaway repositories
**Born:** `20260908.223549` onto its own shelf, from [`../REDS.md`](../REDS.md)

**Born here rather than folded here.** `tools/fixtures/r/reds_pin_capacity_scan.sh` reads
`pin_deadlocked=1` with `pin_foldable_rows=0` and `rows_that_fit=0` -- fourteen rows, every one
OPEN, so `tools/fixtures/r/reds_fold.sh` has no lawful move and a new red has nowhere in the pin to
land. That is the state the pin has held all day; this is the eighth ship it has refused. This row
is **CLOSED**, so `shelf_open_rows` does not move.

**REDS %651 (`20260908.215031`) -- a proof that runs on every lap was invisible to the census that asks whether the proof exists.** *What went wrong:* `tools/c/convergence_census.sh` reads whether a tree-writing tool proves it converges, and its numerator counted exactly one kind of evidence -- a **sibling** control or witness, found by the tool's own filename stem, asserting the second run's result. `tools/c/convergence_tree_prove.sh` landed at `0cb297adb7` on the same day, rostered `tier lap`, and its witness runs it on `tools/fixtures/r/reds_ledger_headline_write.sh` -- the writer `tools/hooks/pre-commit` runs on **every commit this tree makes** -- reading `verdict=converges` on every lap of every ship. That tool stood in the census's **unproven** column the whole time, because the prover's witness lives in `tools/c/` and carries no part of the tool's stem, so a stem search could never see it. The census's own header still read *Named here rather than built* about the prover, a day after it was built and rostered. *What caught it:* the fire rota row, read as its own sense -- looking straight at the handoff my elder card block had written, *the census counts a sibling assertion, so these three read unproven while a rostered prover answers them each lap*, rather than opening new work beside it. *What it taught:* **a numerator that finds its evidence by NAME can only see evidence somebody named after the subject.** The sibling search is a proximity heuristic wearing a predicate's clothes, and a stronger proof filed one directory away is invisible to it -- which is `%646`'s shape in the other column, where a guard's reach is decided by a naming convention rather than by what it is asking. *Repaired (`20260908.215031`):* the census reads a second source -- a tracked runner that is **not** the tool, naming the tool and a convergence prover on one **non-comment** line -- and prints the split, `proven_by_sibling_assertion` for a check somebody wrote and `proven_by_prover_run` for a question somebody ran, with the total counting a tool once however many ways it is proven. Both elder exclusions carry over unchanged: a tool naming the prover in its own source certifies nothing, and a comment naming both is a plan. Proven reads **4 of 10** where it read 3, and the arrival is the headline writer. Six control legs prove it from both sides in a planted repository -- counted, refused in a comment, refused when self-named, both splits printed, and the **elder** numerator run over the same plant and shown to call it unproven, since a repair proven only in the passing direction cannot be told from a coincidence. Control **12 -> 18** legs, witness GREEN. **CLOSED** on `tools/c/convergence_census_witness.rish`.
