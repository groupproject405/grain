# REDS -- an instrument that prints its finding and gates nothing is read by nobody

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row whose repair is a reader at the round open, proven on metal at a later lap's own open
**Folded:** `20260912.013920` from [`../REDS.md`](../REDS.md)

One row, folded to hold the pin under a bound eight ships share. It teaches that a census which
prints a count and names nothing is a locked door with the hours posted on it: the dead-letter box
had held a finished 747-line guard family for five days while every round open read `verdict=ok`
over it. The repair names each `orphan:work` path beside its stash at the open, bounded at eight
with the remainder counted, and it was proven by a later lap opening its round and reading the
parked work out loud. That lap then found the parked work superseded, which is the sequel the row's
booking clause records: a named path still carries no way to say *judged*, and absence is checked by
path where supersession happens by purpose. Read it beside
[`REDS-fold-recital.md`](REDS-fold-recital.md).

**REDS %720 (`20260911.205727`) -- a lap's finished work sat in the dead-letter box for five days while every pass read `verdict=ok`.** *What went wrong:* `tools/f/fleet_round_open.sh` stashed this seat's previous lap whole -- a 747-line `aether_falloff` family, scan, control, witness, paper and roster seat, all of it complete -- and the next lap opened, read `git stash list`, and built a second census of the same population from scratch before it read the box. `stash@{13}` has held `tools/t/topology_routed_witness.rish` and its two fixtures since `20260906.190715`, five days, unlanded. *What caught it:* `standing_equipment` red on `unrostered: aether_falloff` -- the dead lap's own pass had written a row for that guard into the untracked run card, the round-open then stashed the roster edit that seated it, and the ghost row reddened the roster witness on every later pass. A guard named a file no tree carried, and that was the only trace the lap left in the open. *What it taught:* **an instrument that prints its finding and gates nothing is read by nobody.** `tools/fixtures/s/stash_record_scan.sh` already names these three paths as `orphan:work` under `list`, its header already names the limit -- *a lap whose log landed while its code did not reads `unlanded=0 verdict=ok` over a box that still holds the work* -- and no step at the round open runs it. **OPEN:** the remainder is one lap, a round-open reader that prints `orphans_work` by name before a lap picks its work. *Booked (`20260912.013920`):* `b9823fafc0` landed that reader -- `tools/f/fleet_round_open.sh` calls `tools/fixtures/s/stash_record_scan.sh list` once, the same invocation that already emits the counters, and prints each `orphan:work` path beside its stash, bounded at 8 with the remainder counted. Proven on metal at THIS lap's own open rather than in a pen: five parked work paths named, among them the whole `standing_equipment_yield` triple `stash@{10}` has held since `20260907.042750`, which is how the box was finally read. **The sequel the naming opens:** a named path still carries no way to say *judged*. This lap read that triple and found it superseded by `tools/fixtures/s/standing_equipment_scope_rank.sh`, built meanwhile by another hand and strictly wider, while `tools/fixtures/p/path_absence_scan.sh` answered `verdict=absent` on all three names -- truthfully, and permanently, because **absence is checked by path and supersession happens by purpose.** The checkable proxy is the watch set: two instruments built for one purpose read the same files, and `--kin PATH` on the ranking names them. **BOOKED**
