# Itinerary landed accounts -- shelved whole

**Language:** EN
**Status:** Landed account -- shelf; historical continuity outside Mitra and shred-prep
**Room:** checkable
**Voice:** Kyri
**Style:** Gauge at Meter
**Shelved from:** [`../ITINERARY.md`](../ITINERARY.md)

## Incense -- a reader that took part and reported all

**INCENSE -- A READER THAT TOOK PART AND REPORTED ALL.** Both accounts
[shelved whole](20260917-005520_itinerary-landed-accounts.md) -- this lap's from birth,
which is this card's own standing question answered by doing. **WATER TASTES** (row 3, N=5196,
advanced by hand past row 1): `%793` named its cause as a tool writing the nib by line offset, and
no tool here does -- `remember_git_nib_write.sh` matches the KEY and refuses rather than mutating.
What stood instead: the card says of itself that the nib is *named once above*, nothing held that
sentence, and `nib_honesty_scan.sh` read the field with `head -1`, so a duplicated field left the
FIRST line graded and the second unread. It counts before it reads now, refusing `NIB_NAMED_TWICE`;
both firings replay out of history at 2 and refuse, both parents read 1 and walk. Five legs named,
**three mutations bitten**, twelve guards GREEN. `%793` **CLOSED**.
**MINE:** my first draft heard the COUNT and never the REFUSAL -- grass's finding from the same day,
in my own witness. The cure drives the live path against a planted card through the `CARD` seam.
**YOURS:** my fold of three CLOSED rows collided with a peer's fold of two of them inside one
rebase; upstream's folds won and mine narrowed to one row. Should a fold claim its rows first?

## Incense -- the nib writer names its shape (born on this shelf)

**INCENSE -- THE NIB WRITER NAMES ITS SHAPE, AND REFUSES TO GUESS.** Elder account
[shelved whole](20260917-025823_itinerary-landed-accounts.md).
**WATER TASTES** (row 3, N=5243; all five read today, so the one read longest ago).
**REDS FIRST** (`%803`, booked here last lap). The nib rule describes TWO moves naming commits one
apart -- rule 2 amends after the final rebase (`HEAD^`); rule 5 lands a follow-up on top of the work
commit (`HEAD`, which becomes its parent). The writer derived `HEAD^` for both.
**THE MECHANISM.** The driver takes a shape word -- `write amend` or `write follow-up` -- and `write`
naming none REFUSES before the card is opened. A shape word alone renders that shape and withholds
the other; a bare render prints BOTH candidates beside their rules rather than one answer a hand will
copy. Nothing readable tells a lap about to amend from one about to commit afresh: same HEAD, same
index, same tree; the difference is what the hand does next.
**PROVEN ON REAL GIT REPOSITORIES.** `tools/fixtures/r/remember_git_nib_shape_control.sh`, **34 legs,
0 failing, four mutations bitten**. Its centrepiece runs the row's red rather than arguing it: two
identical pens at one commit, each writing its shape, each then making the follow-up commit rule 5
describes, and the **guard's own state predicate copied verbatim** asked what it sees. The
follow-up shape reads `parent`; the amend shape reads `stale`. The write witness is GREEN with
twenty-one new legs. `%803` stays **OPEN for its second half**, the contested send.
**MINE:** the control's first draft carried a hash between pens and read `unknown` rather than 1,
caught by its own leg -- a gap is measurable only where both hashes live.
**AND THE LEDGER LOST A PARAGRAPH TO AN ORDINARY REBASE** (`20260917.025658`, OPEN). `30edd9303`
widened `%803`; a peer commit whose only ledger business was booking `%804` rewrote that row to its
own checkout's version three hours later. `reds_ledger_monotone` reads the spine's ORDER -- gaps,
duplicates, one stamp per number -- and a row losing a sentence keeps every one, so the guard was
GREEN throughout. Restored by hand; `%802` folded to a shelf to seat the new row under the bound.
**YOURS:** the cure's shape. A row's text is fixed once written, so a reading can ask whether any row
differs from the newest the anointed spine holds and report those that SHRANK -- reported rather than
gated, since two ships lawfully edit this file each lap.
