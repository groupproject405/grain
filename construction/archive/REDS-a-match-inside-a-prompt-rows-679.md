# REDS shelf -- a match inside a prompt is not a match on a command word

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- immutable once written
**Room:** Checkable -- the row names where it was caught
**Folded:** `20260910.000521` from `construction/REDS.md`, on arrival, since the pin stood 1,609 bytes over its bound and this was its one foldable row
**Rows:** `%679`

One row, folded the moment it landed. What it taught: a law written onto every command line makes
every command line match a search for the law's own subject, so a tool that matches a flattened
command line grows blinder exactly as the habit of reaching for it grows stronger. And the sharper
half -- the broken reading agreed with the truth, so nothing disagreed and nothing caught it. The
row below is exactly as it stood.

---


**REDS %679 (`20260909.223912`) -- the helper built so a signal reaches only this tree read eight peer agents as candidates, because the baton NAMES the tool a hand searches for.** *What went wrong:* `tools/f/fleet_call.sh` gathered candidates with `case "$cargs" in *"$pattern"*)` over `ps -eo pid=,args=` -- a substring test on the flattened command line. The baton is prepended to every seat prompt and reaches each agent as **one argument of 11,558 bytes** naming this helper and `standing_equipment_run` thrice, so `--pattern standing_equipment_run` answered **`candidates=19`** where **7** were the tool: eight peer `claude` agents, two `bash -c` snapshots, and the `ugrep` searching for the string. Every one was refused by cwd or ancestry, so **the wall held and the READING was wrong** -- and the reading is this helper's documented purpose. **The published consequence is subtler than a wrong number.** The `20260909.220903` account claimed from this tool that *all 8 ships ran a full pass at once*; re-measured with the repaired reader that is **TRUE** -- 7 distinct peer trees plus this one, one pass each. The instrument was broken by 2.6x and its conclusion was right, so the next inference from it would have gone unchecked. *What caught it:* reading the matched cmdlines argument by argument rather than trusting the summary, after the count disagreed with the runner's lock. *What it taught:* **a law written onto every command line makes every command line match a search for the law's own subject** -- the `20260908` baton clause teaching ships to reach for this helper is what put its target on eight command lines, so the tool grew blinder as the habit grew stronger. And **a false reading agreeing with the truth is the shape nothing corrects.** The discriminator is **shape rather than length**: a command word carries no whitespace, a prompt is prose. A byte threshold was declined -- the longest honest argument here is 2,100 (a witness's inline awk) against 11,558, so it drifts with the next one. A prose-only match is refused **out loud** as `refused_prose` naming `--pid` as its door, and `reading=exact|flattened` says when a host has no argument boundaries. Two further faults were caught first-resident within the hour, by using the thing: a **multi-word pattern** matches no command word, so `--pattern 'runner.sh --hot'` refuses at usage rather than reading a silent zero -- *impossible* and *nobody is running it* are two facts -- and `reading` is decided by the **host** once, since a process exiting between the listing and the read reported the whole reading `flattened` on a `/proc` pier. Proven both ways: the control runs **39 checks, was 26**, and against the elder matcher **nine** legs fail -- one being *prose match survives KILL*, since the elder form **killed** a planted process whose only tie to the pattern was a sentence naming it. **The same class one door over survives on an anchor**, and `tools/f/fleet_watch.sh` now carries the measurement that says so beside its own `pgrep`: **8** agents match an interior baton phrase unanchored, **0** anchored. **CLOSED.**
