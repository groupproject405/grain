# REDS -- a number measured before its own commit

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by 68 planted behaviors on throwaway rooms
**Folded:** `20260908.121934` from [`../REDS.md`](../REDS.md)

**Born here rather than folded here.** The pin stood at `pin_headroom=24` bytes with
`pin_foldable_rows=0` and `pin_deadlocked=1` -- thirteen rows, every one of them OPEN, so
`tools/fixtures/r/reds_fold.sh` had no lawful move and the next red had nowhere to land. That is the
state `tools/fixtures/r/reds_pin_capacity_scan.sh` was written to print rather than to hide, and the
move it records the fleet taking is this one: a row born directly onto a single-row shelf, saying so
in its own header and recorded in [`REDS-fold-recital.md`](REDS-fold-recital.md), so the spine stays
gapless and the trail stays readable. This row is **CLOSED**, so `shelf_open_rows` does not move.

It closes a remainder its own room already named. Read it beside
[`REDS-whose-module-is-this-rows-593.md`](REDS-whose-module-is-this-rows-593.md), whose last clause
reads *a room's line count still has no instrument, which is the figure that drifted*.


**REDS %638 (`20260908.121934`) -- a line count written at a room's front door was measured against the tree as it stood and then shipped inside the very commit that made it stale.** *What went wrong:* `amphora/README.md` read *`purchase_delivery.rye`, `vessel_fetch_wire.rye` and `vessel_fetch_delivery.rye` are **1,160 lines**, a third of what this room owns*. The three carry **1,321** today. The figure is not a slip: it was **exactly true** at `0ee5c8171`, and it was written into `05c87d3d0` -- the commit that also grew `vessel_fetch_delivery.rye` by 109 changed lines. It therefore shipped **77 lines short inside the commit that made it short**, and stood **161** short two days on. *What caught it:* this lap reading the room's own front door under *grade what you touch*, and measuring the three files rather than believing the sentence; `git show <commit>:<path> | wc -l` walked backward until the number came true one commit earlier, which is what turned a drift into a mechanism. *What it taught:* **a hand that measures and then keeps working writes a number that is already false when it lands.** No amount of care reaches this -- the measurement was correct when taken, and the same lap's own remaining edits invalidated it before the commit closed. That is REDS `%360`'s family with the clock moved inside a single lap: the elder cases drifted over days, this one over minutes, and only an instrument can read a number at the moment anybody looks. `%593` named this exact remainder on `20260907.215529` -- *a room's line count still has no instrument, which is the figure that drifted* -- and left it, so the class had already been seen and not yet walled. *Repaired (`20260908.121934`):* `tools/fixtures/am/amphora_roster_scan.sh` grew two readings. `own_lines` and the `detail_lines` listing weigh every authored module on every run (**3,915** across seven, at this stamp), so the door has an instrument to name; and `readme_spelled_lines` **GATES at zero** a digit group carrying the word `line` or `lines` at the front door, case folded, with a word character after `lines` ending the match -- so `own_lines`, `detail_lines`, the reading's own name, and a number spelled as a word all walk free, since a spelled number cannot go stale in silence. Reached by REPAIR rather than by decree: the one offending sentence was replaced the same lap, and the wall has never refused work already done. Control **56 -> 68** behaviors, the refusal shown from the failing side, named by line number, proven under a folded case, and then lifted; witness GREEN. *What it does not reach, named rather than implied:* the gate reads one word, so a door spelling a byte count, a module count, or a percentage drifts exactly the same way and this reading stays quiet -- one word is what fired here, and a wall around every number a door might spell would refuse the tables this page is mostly made of. **CLOSED**
