# REDS -- a guard that reports without demonstrating

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- two folded rows, immutable once written
**Room:** checkable -- two ledger rows, each closed by a witness on metal
**Folded:** `20260909.202547` from [`../REDS.md`](../REDS.md)

Two rows from one evening, and one shape under both: a guard published a reading a reader could
neither check nor act on. One was rostered without ever having demonstrated a refusal of its own,
so its silence carried no information and the ceiling counting exactly that population refused
every ship. The other reported a count and named no member, so three ships surfaced one red and
none of them could reach it. A number is a summary of members, and a summary whose members are
withheld asks a reader to trust the counting rather than read the count. Both were closed the same
way -- by making the instrument show its work: one given a control that plants faults so each
refusal is spoken in the worker's own words, the other made to name what it counted.

**REDS %670 (`20260909.183200`) -- a guard rostered three hours earlier demonstrated no refusal of its own, and the ceiling that counts exactly that population refused every ship's cold pass.** *What went wrong:* `tools/g/glow_rune_alphabet_witness.rish` was rostered at `20260909.155028` asserting only on its worker's result, and `tools/fixtures/s/standing_equipment_redleg_scan.sh`, seated the same afternoon at `20260909.152755`, holds guards-that-demonstrate-no-refusal under a ceiling that only falls. It stood at 53; the new guard made it 54, so `verdict=ceiling_raised` refused the roster on every ship in the fleet. The proof existed -- that lap planted six refusals in a throwaway pen -- and the pen was discarded, so no tracked file carried it and nothing in the tree could read it back. *What caught it:* this ship's cold scoped pass, `192 run, 188 green, 3 red`, whose `standing_equipment_redleg red` line named its own reading. *What it taught:* **a ratchet whose population grows with correct work reds on correct work.** Three guards joined the roster since the ceiling was written; two carried a control fixture and the third did not, and rostering a delegating guard is ordinary correct work rather than a defect. So the repair is a control rather than a raised ceiling: `tools/fixtures/g/glow_rune_alphabet_control.sh` builds a `git worktree` pen, copies in the on-disk worker so a repair is proven in its current form, and plants four faults that each keep the digraph count at thirty -- a deleted pair, an exempt head swapped for a novel one, a spoken head swapped away, and `|^` swapped away -- so each is refused in the worker's own distinct words rather than collapsing into the count check that runs first. Every plant is lifted and read back to `OK`, since a refusal shown only in the failing direction cannot be told from a tool that refuses everything. *Standing, and Keaton's:* whether the ceiling should follow a delegation one hop and count a guard proven by its child's control, which is the same evidence-by-naming-convention lesson `tools/c/convergence_census.sh` has now paid for three times. **CLOSED**: redleg reads 53 of 53 with `ceiling_ok=yes`; the control passes 17 checks and was proven able to fail, a mutated expectation reading `pass=16 fail=1`.

**REDS %671 (`20260909.201318`) -- a ratchet reported a count and named no member, so three ships surfaced one red and none of them could act on it.** *What went wrong:* `tools/fixtures/s/standing_equipment_redleg_scan.sh` published `guards_no_refusal_marker=54` against a ceiling of 53 and printed the members themselves only under an undocumented `list` argument, so a lap meeting the red had to read the scan's own source to learn which guard to repair. Between 15:50 and 19:31 three seats named that count on the card, two of them plainly *not mine*, and the row closed only when the guard's own owner reached it. *What caught it:* this seat's cold open, and a fourth ship about to read it the same way. *What it taught:* **a ratchet holds a number, and the repairable question is which.** The tree already writes that sentence one family over, where the costliest-guard reading names its members for exactly this reason and says so in its header. **CLOSED**: a raised ceiling prints `over_ceiling:` with each guard past the line and its path, bounded by `MAX_REPORT` and taken from the roster's tail where an arrival sits, quiet while the ceiling holds -- both readings planted in `standing_equipment_redleg_control.sh`, 16 -> 18 checks.
