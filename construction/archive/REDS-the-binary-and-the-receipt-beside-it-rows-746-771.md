# REDS -- rows %746 and %771, folded from the living pin

**Folded:** `20260916.183551` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*The built-tool round -- a binary's presence was checked and its freshness was not, and the receipt standing beside a binary spoke for the binary it replaced.*

The two rows are one subject read at two depths. `%746` found that the two binaries every
witness runs through are the two nothing checks for staleness, so a ten-day-old interpreter
made a healthy peer guard read as a hard error; freshness turned out to be a second question
wearing presence's skip. `%771` went one layer in, to the receipt: the build key held the
output's own path, so the one move every ship makes to replace a running binary could never
hit, and the elder receipt stayed beside the new binary speaking for the one it replaced.
Presence, freshness, and then whether the thing beside the binary is telling the truth about
it -- each answered by an instrument the round built.

The rows stand here exactly as they were written. A row leaves the living pin so the pin
stays the length a reader will actually read; the lesson travels forward in the guards the
round built, and the row itself stays one click away.

---

**REDS %746 (`20260915.215302`) -- the two binaries every witness runs through are the two nothing checks for staleness, so a ten-day-old interpreter made a healthy peer guard read as a hard error.** *What went wrong:* this seat's `rishi/bin/rishi` was built `2026-09-05 18:06` and ran every witness of this lap. `out_brief` landed in `rishi/src/main.rye` at `6b2965fe7`, `2026-09-15 21:21:56`, three hours earlier, and `tools/r/remember_git_nib_witness.rish` reads that field on line 79 -- its own GREEN path. So the guard answered `rishi: line 79: NoSuchField` on a card whose nib was honest, and the failure sat on the success branch where nothing had exercised it. *What caught it:* this lap's send, by reading the error rather than the exit code. The card's nib was correct -- `339be29a36` is HEAD's parent -- and the stale assert above it had already passed, which is why the fault surfaced only once the nib was RIGHT. One step from booking a red against a peer's witness for a fault in this ship's own toolchain. *What it taught:* **`tools/fixtures/w/witness_own_build_scan.sh` passes `rishi/bin/rishi` and `rye/bin/rye` free, and its named reason is exactly right about the thing it checks.** Its rule reads *if either is absent nothing runs at all, so their presence is a bootstrap fact rather than a promise any single witness makes* -- a statement about PRESENCE, and true. Freshness is a second question wearing the same skip, and no instrument in this tree asks it: a present binary arbitrarily far behind its own source runs every guard on the ship, and its errors are attributed to whatever it was running. The reading is one `git log` against one mtime, and the repair on this ship was one 2-second rebuild with `RYE_ZIG` pointed at `vendor/zig-toolchain/zig`. *Repaired (`20260915.221356`), and the class has an instrument.* `tools/fixtures/b/built_tool_freshness_scan.sh` declares each built tool as one four-field row -- name, binary, module source directory, repair command -- and compares the binary's modification time against the newest **tracked** source under that directory, one `git ls-files` and one `find -newer` per tool. A stale tool refuses, names the source that outran it, counts the seconds, and prints the command that repairs it. An **absent** binary and a `rishi` older than the `rye` that compiles it are reported and never gated: a fresh clone has neither, and the chain is real. Plain shell on purpose -- a `rishi` too stale to parse the witness cannot silence the reading. **36 legs** on real git repositories in a throwaway pen, every refusal planted and then **lifted**, three mutations bitten -- reading untracked files rather than the index, counting an absent binary as stale, and inverting the comparison. Rostered `built_tool_freshness`, `tier lap`. This ship read `rishi` **877,180 seconds** and `rye` 13,174 behind; both rebuilt, and the new interpreter answers `out_brief` where the old one answers `NoSuchField`. *What it does not reach:* mtime answers *later*, never *different*. *And the fleet's instances were repaired by hand in the same hour* -- a peer rebuilt `rishi` in four peer checkouts by the same rename-over-a-busy-executable move, checking each tree for the source change first, so the remainder is a standing reading rather than a sweep. **BOOKED.**

**REDS %771 (`20260916.080424`) -- the build receipt's key held the output's own path, so the one move every ship makes to replace a running binary could never hit, and left a receipt speaking for the binary it replaced.** *What went wrong:* `rye/src/main.rye` fed every forwarded flag whole into the receipt key, and `-femit-bin=<path>` is a forwarded flag -- so WHERE an output landed decided the key. Measured `20260916.064500` in a throwaway pen: one source emitted to two paths spoke **two keys** (`341e0ca1`, `45540619`) and produced **byte-identical** binaries (`13f512ca` twice). A binary refuses to be written over while it runs, so every ship rebuilds `rishi` by emitting to `rishi/bin/rishi.new` and renaming -- a habit this tree's card names in prose and **no file in it spells**. That move booked a key no later build could match, and it left the elder `rishi.ryekey` standing beside the new binary speaking for the one it replaced. Read over the pier's eight checkouts: **five orphaned, one speaking, two carrying none.** On this ship `rishi.ryekey` line two read `59dd8e88` while the binary hashed `43cd45ce`, and `rishi.new.ryekey` beside it held the true one. *What caught it:* the repair the row folded at `20260916.023136` left for its own next lap -- a freshness guard asking the builder for its receipt rather than the filesystem. The first thing that reading answered was that the receipt beside this ship's `rishi` named a different binary, which is a question mtime cannot ask. *What it taught:* **a path is not an input, and this was the third of the class in one day.** The toolchain's path and the library's left the key that morning on exactly this reasoning -- a path names how an input was found rather than what it compiled -- and the output's path rode in through a different door, as a forwarded flag, where no one was reading for it. A rule applied to a list of named inputs does not reach the general case; the general case is every string in the key, asked one question each. *The repair:* the value of `-femit-bin=` leaves the key and the bare fact `-femit-bin=` stays, so two spellings of one output speak one digest; `ryekey_marker` moves **v6 to v7**, at the cost of one rebuild for each of the **94** receipts standing here. **Narrow on purpose**: a sibling emit flag names an artifact no receipt verifies -- a hit emits nothing at all -- so `-femit-asm=` and its kin keep their values, proven by its own leg. What still holds the served bytes honest is unchanged: every hit re-hashes the file standing beside the receipt against the stamp's second line, so a key two paths share can never serve one path's bytes for the other. *Proven both ways on metal:* `ryekey_control.sh` legs **20 to 22** -- a binary moved with its receipt now HITS, and the same move under a v6 mutant compiled in a pen reads MISS; a too-wide mutant exempting every `-femit-` flag reds the sibling leg. On the real tool: `rishi` rebuilt to `rishi/bin/rishi.new`, both files moved, and a build naming the plain path left the binary untouched at key `f15209cc`. *And the reading landed beside the clock:* `built_tool_freshness_scan.sh` now asks line two of `<binary>.ryekey` against the binary's own SHA-256 and answers `speaks`, `orphan`, `absent`, or `malformed` -- **reported, never gated**, since five ships stand orphaned and none chose it. Control legs **36 to 47**, three more mutations bitten. *What this does not reach:* whether today's sources would speak the standing key. That wants a key-only mode `rye` has yet to carry, and it is the half of `20260916.021537` still owed. **BOOKED.**
