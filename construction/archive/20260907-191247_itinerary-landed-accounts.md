# ITINERARY landed accounts -- folded `20260907.191247`

**Language:** EN
**Status:** Landed accounts, folded from [`../ITINERARY.md`](../ITINERARY.md) -- **checkable room**:
every number below was measured on metal and names the instrument that read it
**Voice:** Kyri
**Room:** checkable

*The live front holds what is OPEN and what waits on Keaton's word; a landed account folds here, the
way a REDS row folds to its shelf. Nothing is edited on the way down.*

---

## GRASS -- a roster line guarded by its subject cannot fail (`20260907.165636`)

Elder [shelved](20260907-165730_itinerary-landed-accounts.md); row `20260907.165636`.
The docs roster named `rye/`, `rishi/`, `aurora/README.md` -- the doors discovery misses -- as
`[ -f "$f" ] && echo`. `phantom` reads the roster OUTPUT, so a line that stops printing when its
page moves never becomes one, and `unrostered` never saw them.
**Both blind**, in the file quoting `%170`. Plain now, 60 paths unchanged; control **16 -> 18**
-- 17 shows the elder spelling go quiet, 18 the repair bite.

---

## GRASS -- the room linked what it shared, bar the one file that mattered (`20260907.191247`)

`mand/tally_copy.rye` was already a symlink to `../tally/copy.rye`, and
`linengrow/capabilities.rye` one to `../caravan/capabilities.rye`. Between them stood
`mand/capabilities.rye`, a real file and a **divergent copy** of a capability policy surface: it
published neither `Refusal` nor `refusal_reason`, kept `name_slice` and `label_slice` private where
caravan's publishes them, and its invariant comments had drifted in nine places.

`tools/co/copy_lag_witness.rish` already counted the drift -- `behind=2`, at a ceiling of 2 -- and
that is the whole reading a drift meter can take. It counts how far a copy trails; it can never ask
whether the copy needs to exist. The room's own hand answered that: two of its three shared modules
were links, and the third was the one that went stale.

The three symbols mand uses -- `CapabilityTable`, `right_read`, `right_write` -- all stand in
caravan's file, so the link costs nothing. `mand/capabilities.rye` is now
`../caravan/capabilities.rye` at mode `120000`. GREEN on metal:
`tools/m/mand_ring1_witness.rish`, `tools/m/mand_ring2_witness.rish`,
`tools/m/mand_ring3_witness.rish`, and `tools/ca/caravan_capabilities_witness.rish`. The scan reads
`behind=1`, `identical=1`, and `behind_ceiling` fell **2 -> 1** with the reason written beside it.

Zig refuses an import that escapes the root file's directory, so a module reached by a bare name is
a directory relationship the language enforces -- which is why a symlink, rather than a second copy,
is this tree's established answer, and why `linengrow/` and `granary/` already use it.

**The door that graded C+.** `mand/README.md` read **C+ 76** at Door: 51 words carrying 4 links,
so reach scored **30** on a cross-reference density of 8 per 100 words against a budget of 1. The
molt was an accretion in place under a cairn -- every elder claim kept word for word, with the why
behind the ring order added, what the rings reward named plainly (a denial that leaves a trace,
since one bare denial covers three separate situations), and a table naming the two symlinks. It
reads **A 92** at 6% negative against the 20% Door ceiling: 320 words, 4 links, reach **100**.
The before-and-after is the clearest example in this tree of the Door cross-reference budget
catching a page of pure navigation.

**And a find left for PHEROMONE**, whose lane the ASCII family is and who landed its Glow meter the
same day. Every ASCII meter in this tree excludes program content by design, and the reason is
sound: converting a fixture byte or a file header changes behavior. The same exclusion swallows
**printed prose a human reads**. Measured `20260907.191247` over authored Rye, outside comments and
outside `\\` string continuations: **1,772 em dashes and 143 middots across 1,925 code lines in 877
files**, every one a form `.claude/rules/ascii-first.md`'s own substitution table names, every one
on a line a person sees on their terminal. A string is data or it is prose, and no meter tells the
two apart.

---

## GRASS -- four reds closed, and three of them were one shape (`20260907.192241`)

The scoped cold open refused with `guards_red=3`. Four reds fell on the lap that followed -- three
of them stand as this lap's own repair, and one was withdrawn to a peer's better answer. Three of
the four are the same defect in three houses: **a guard reading a MENTION as a PROMISE.**

**`index_row_bound`** read `rows_misordered=1` -- today's shelf carried `20260907.155612` above
`20260907.165730`, which is the `%440` class a rebase auto-merge produces. The scan names its own
repair; `tools/fixtures/i/index_shelf_repair.sh` sorted 93 rows and the witness came back GREEN.

**`plant`** asserted `sourcing=13` where the scan read **14**. The fourteenth adopter is
[`../../tools/fixtures/t/topology_partial_control.sh`](../../tools/fixtures/t/topology_partial_control.sh),
born sourcing the plant law at `fb111516c` (`20260907.174056`) -- a lane wrote the control
correctly and left the floor line behind it. I raised the line to fourteen and wrote the lesson
beside it: **a floor line is part of the adoption, not a follow-up to it.**

**That repair is WITHDRAWN, and the reason is better than the repair.** The rebase brought a peer's
answer landed in the same hour: the floor is READ from the scan now (`adoption_floor_held=yes`)
rather than spelled in the witness, so a lane that adopts passes free and only a control that LOST
its import refuses. That removes the class rather than resetting its counter, and it is the
structural form of the sentence I had written as a habit. Theirs published first and is stronger;
mine came out whole on the conflict. **Seventh time in two days that a finding met its peer inside
one hour**, and the first where the peer's shape was the one I had argued for in prose.

**`comment_citation`** refused
[`../../tools/fixtures/c/crushed_index_scan.sh`](../../tools/fixtures/c/crushed_index_scan.sh) for
a path that resolves nowhere. The path sat inside a **quotation** of another page's declaration
line, written relative to that page rather than to this one; from `tools/fixtures/c/` it names
`tools/press/`, which stands nowhere, while `press/` at the root is real. The guard already names
*a backticked span illustrates syntax* among its exclusions, so the repair is its own grammar:
backticks, no link, and the reason written in.

**`dated_spelling`** refused
[`../../tools/fixtures/l/living_prose_roster.sh`](../../tools/fixtures/l/living_prose_roster.sh)
for a dated pattern demanding a sprig. That exclusion means ONE page and identified it by its
sprig, so the repair is the literal path rather than a looser date class -- it excludes exactly
the page it means, and a mutant at a fresh stamp arrives reconsidered rather than silently
inherited. The roster reads 520 paths before and after.

**And the third firing taught the lap its own lesson.** The first repair comment QUOTED the elder
pattern to explain itself, and the guard refused the tree again -- for prose describing the fix.
The elder spelling is now described rather than quoted. A peer booked this same reading at
`20260907.145907`, when a control's paragraph explaining why it does NOT import the plant law
counted as an import. **A grep for a shape counts the sentence that says the opposite**, and it
has now fired in three houses. A lantern that fires twice becomes a loom; this one has fired
three times, and what a loom for it would read is the open question.

Left named rather than taken, each larger than a bounded frame: **`dated_path`** (the lost-
reference ceiling), **`season_leaf_choir`**, and **`two_rooms_doorway`** -- the doorway census the
baton already carries.
