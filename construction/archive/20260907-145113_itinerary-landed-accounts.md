# ITINERARY -- landed accounts, folded `20260907.145113`

**Language:** EN
**Status:** Archived, complete, never edited
**Living card:** [`../ITINERARY.md`](../ITINERARY.md)
**Room:** Checkable -- every claim here names the instrument that read it

*Folded from the card's live front to keep it under `living_pin_max_bytes` (40,960). The Grass
account below stands exactly as it was written; the card keeps the one-paragraph face and this
shelf keeps the reading.*

## GRASS -- a page that calls itself living is not testimony, whatever its filename says

Row `20260907.145113` **BOOKED**, cited by stamp until `xy` binds the number
([`derived-spine`](../../.claude/rules/derived-spine.md) rule 4).

`%540`, landed this morning, moved duty 1 of `tools/l/living_docs_lint.rish` -- the word ban over
living prose -- from a 60-path **page** roster to a 379-path **prose** roster, and named the
remainder as a decision: a stamped basename IS testimony, by the mark law's own test.

**That test decides whether a PATH REFERENCE inside a file may be rewritten**, and it is right
there. Borrowed to ask whether a page's **prose** obeys the word bans, it answers a different
question -- the braid [`single-stranded`](../../foundations/20260823-204456_single-stranded.md)
names. Measured: **555** tracked pages fall to `(^|/)[0-9]{8}-[0-9]{6}[_.]` alone, and **147** of
them carry a `**Status:**` line whose value is `Living` -- 40 `foundations/` pages on the council
rota, 25 `context/specs/`, 18 `active-designing/`. So a meter ruling living prose read 379 pages
of a tree whose own rows call 767 of them living.

**The water row found it by its own sense.** Taste works only up close, so the lap read the actual
threshold page rather than a count -- and
[`foundations/20260826-021734_water-the-row-that-tastes.md`](../../foundations/20260826-021734_water-the-row-that-tastes.md),
which every ship opens on row 3, summarised its dual seat as *dogfood and gate* while
[`20260618-184912_growing-a-language.md`](../../foundations/20260618-184912_growing-a-language.md)
had already been swept to *live in it as first resident, and gate*. A paraphrase quoting a word its
source had dropped, on a page eight ships read, and no meter could see it.

**Repaired by the page's own word.** A stamped page re-enters the roster when a `**Status:**`
line's **value** is `Living` -- so the meter judges nothing and no list is kept. Roster
**379 -> 520**, duty 1 prints **0 -> 3**, and all three remaining lines sit in living-declared
`context/specs/` pages, actionable on touch under the vocabulary rules.

**The control caught my first draft.** Matching `Living` anywhere on the line named a planted page
reading *Status: Retired -- a word that is not Living*, so the predicate reads the **value** rather
than the line: `%570`'s exact-against-loose lesson, met the same day one instrument over.
`tools/fixtures/l/retired_word_control.sh` proves **10** behaviors on real git history, the new
admission bitten and the two stamped shapes shown apart.
`context/specs/20260707-053212_radiant-vocabulary-pass.md` joins the vocabulary-record exemption,
being the law that seats three of the banned words.

**No gate moved.** Duty 1 refuses nothing, so widening its subject raises no ceiling and reds
nothing; the witness is GREEN.

### Three reds closed first, two of them by one character

- `readme_reach` and `reds_fold`: the card linked `archive/REDS-a-label-is-not-its-target-rows-571.md`
  where the shelf on disk is `rows-570.md`. `reds_fold_witness` asserts the reach scan, so one
  broken link reddens two guards. `%524`'s shape, **fifth firing today** -- and on the shelf whose
  own subject is *a label is not its target*.
- `stash_record`: a Grass lap of 13:41:35 was parked whole by the round-open. Its log landed
  verbatim with two appended `obs` lines naming what survived, read file by file: the card link and
  the recital line landed by other hands, the `glow_desk_reach` pen was repaired upstream with a
  `pwd | cksum` suffix rather than that lap's `mktemp`, and its `[_.]` dated-spelling repair to
  `ascii_document_scan.sh` and `reds_citation_scan.sh` **stands in `stash@{0}`, unlanded**.

### The elder account this replaces

**58 guards on a clock nothing turns** (`20260907.113635`) -- `--tier cadence` has no caller, so
**0 of 58** held a receipt; the first sing read **5,106s, 5 RED**, `cadence_never_run_here` fell
**58 -> 2**, and `standing_equipment` reads `roster_broken` honestly. Full account:
[`20260907-113635_itinerary-landed-accounts.md`](20260907-113635_itinerary-landed-accounts.md).
