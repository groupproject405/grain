# REDS -- folded row 822

**Language:** EN
**Status:** Shelf -- immutable once written; the live pin holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the row moved whole, its links re-anchored by the fold filter
**Folded:** [`../REDS.md`](../REDS.md)

Row %822, folded the hour it closed, with the pin over its own bound the moment the row landed --
the same shape %818's own recital entry already named. Booked locally as %821 and renumbered to
%822 on this lap's own round-open rebase, when a peer's %821 (`REDS-the-key-next-door-rows-821.md`,
stamp `20260917.203048`) was found already shared with an earlier stamp -- `.claude/rules/derived-spine.md`
rule 1, the earlier stamp keeps the number, applied to an unshared row rather than a published one.
Accrete-never-break: nothing here is edited.

---

**REDS %822 (`20260917.204915`) -- the law room's ceiling still had one more cheap page in it.** *What went wrong:* nothing new -- `%819`'s repair brought the law-room register ceiling from seven pages over the Field target back to six, and left five of those six unpriced. *What caught it:* pricing the remaining pages by `--explain` rather than reasoning about size: `.claude/rules/session-log-provenance.md` (35% of 37 sentences) needed only two sentences moved under the floor to clear the tier, cheaper than `gratitude-licenses.md` (five), `debride.md` (four), `ascii-first.md` (eighteen), or `git-signing.md` (fifteen). *What it taught:* the cheapest page in a room is a reading rather than a guess, same lesson as `%819`, and it holds again one lap later -- the newest or largest page is not the fix; the smallest arithmetic gap is. *Repaired:* two sentences in `.claude/rules/session-log-provenance.md` restated affirmatively with every claim, figure, and stamp held -- `Never promote a preference, alias, profile value, model catalog, or configured default into an active-runtime claim` became `Keep a preference, alias, profile value, model catalog, or configured default as configuration, apart from an active-runtime claim`, and `Write status before the send, never after` became `Write status before the send begins, so it is already recorded by the time the send runs` -- bringing the page from 35% to 29% and the room from six over to five. `tools/p/prose_register_witness.rish`'s own hardcoded `law_ceiling=6` assertion was lowered to `5` in the same commit, since the ratchet only ever falls; both Cursor twins carry the same two sentences. `tools/p/prose_register_witness.rish` GREEN on metal after the edit, 55 control legs, 0 failing. *What this does not reach:* the five pages still riding the ceiling -- `.claude/rules/the-baton.md` chief among them at 47% of 187 sentences -- and whether the fleet's coordination law earns a Meter-style refusal exemption, which `%819` already named as Keaton's word rather than a lap's guess. **CLOSED** -- this increment's own repair is proven; the open policy question stays recorded at `%819`.
