# REDS -- row %309, folded from the living pin

**Folded:** `20260828.110051` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Two loops died of one handler that named every pull failure the same, and the repair hand then
destroyed the work it came to save -- and recovered all seventeen files from git's own object
store, because every staged byte survives the moment `git add` runs.*

Its cure stands at `tools/f/fleet_round_open.sh`: the self-healing open, proven in a pen across all
four states, wired into every launcher. The row folded within the day, to make room for `%310` --
the silent pair gates its own crashed reporter had found, repaired in Sound's custody the next
morning. A red that dies discovering the next red is the ledger working.

---

**REDS %309 (`20260828.103023`) -- two loops died of one handler, and the repair hand then destroyed what it came to save.** *What went wrong, in three layers.* **The handler:** both 24/7 loop lines read `git pull --rebase xy main || { ...; echo 'PULL DIVERGED: upstream history was rewritten'; break; }` -- one message and one death for every pull failure. Both crashed trees were measured at **zero divergence** (HEAD equaled `xy/main` exactly); the true fault each time was a **dirty index**, a lap that finished writing and never sent, and git's own first line said so. The tail-reading class, third instance in two days. **The mine:** an unattended lap whose send is refused once -- a commit wall, a context end -- exits leaving staged work, and the next round-open pull dies on it. The wall and the loop, each correct alone, compose into a trap. **The hand:** repairing this, the supervising hand staged the orphan lap onto a park branch, had its own commit refused one mechanism word short, and ran `git reset --hard` anyway -- **destroying all seventeen staged files from disk**, since reset --hard removes staged-new files it will not first commit. *What caught it:* the reflex to re-read `git status` after every destructive step; the loss was seen within one command. *What it taught:* **every staged byte survives in the object store the moment `git add` runs.** `git fsck --unreachable` listed 75 dangling blobs; every script here opens with a comment naming its own path, so 14 of 17 files matched on their first line, and the other three by unique content -- recovered whole to `pier/silence-glow-gate-answer-parked`, proven by the shelf row and witness now standing there. And the deeper rule, twice in one incident: **a refusal must be read at its first line, and a destructive step may never follow a refused one unverified.** *Repaired (`20260828.103023`):* `tools/f/fleet_round_open.sh` -- the self-healing open, proven in a pen across all four states: clean opens, a dirty tree is **stashed by stamped name** into the dead-letter box, behind **adopts the anointed order** (reset never merge, `%290`'s word), and true divergence **parks on `pier/diverged-<stamp>` and pushes** before adopting -- no bytes lost, no force, no lie about why. Exit 2 is the one retryable state (network), so a loop line reads `|| { sleep 60; continue; }` and **can no longer die at the open**. Wired into every launcher's loop line, one-round line, and seat prose; `loop_prompt_parse` GREEN after catching one apostrophe. This is the loops' git workflow made **Mycelium-consistent with ourselves**: xy the anointed order, a send a proposal, the ff-push the compare-and-set, a divergence a parked proposal re-derived on the new head. CLOSED on this Mac; the pier repair is one paste of the same medicine.
