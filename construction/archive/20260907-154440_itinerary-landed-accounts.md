# ITINERARY -- landed accounts, folded from the live front

**Folded:** `20260907.154440` -- **Status:** Archived, complete, never edited
**Living card:** [`../ITINERARY.md`](../ITINERARY.md)

*The Incense account below stood on the operator card's live front until its work landed. The card
holds what is OPEN and what waits on a word; a landed account shelves here, one click away. It
moved to make room for the account of the lap that found the wipe class reading a flag rather than
an act.*

---

**INCENSE -- FOUR FIRINGS BY REBASE, AND A WALL THAT READS THE INDEX.** Elder
[shelved](20260907-144041_itinerary-landed-accounts.md). `%524` **CLOSED**, recovered whole
from `stash@{0}` -- a lap the round-open parked at `20260907.135743`, its work finished and its log
unwritten. `readme_reach_scan.sh` gained `--store worktree|index`: the index store reads bodies
through one long-lived `git cat-file --batch` and tests existence against the `git ls-files` path
set plus its directory prefixes. `tools/hooks/pre-commit` gained **rule six**, calling it and
refusing on `verdict=living_link_broken`. **The index rather than a staged set IS the finding** --
all four firings arrived by rebase, so no staged-set trigger could have seen one. **544ms** for
1,884 documents. Pen **8 -> 15**, hook **14 -> 16**, carrying the two readings
where the stores rightly disagree. **Cold open:** 173 guards, **3 red,
one root** -- this card linked `rows-571` where that shelf stands at `rows-570`. **Yours:** six
ships run that file, so a wall that is wrong is a fleet that cannot commit.

---

*Held here on `20260907.154440` so the live card could take this lap's account: COPAL's
ordering measurement, condensed on the pin to a pointer plus every question it carried. Its own
REDS row is folded and its elder account stands on the `104723` shelf; these are the sentences the
card no longer spells.*

**COPAL -- A DOOR AND THE WALL ARE TWO NUMBERS, AND ONLY THE SMALLER IS A PROMISE.**
(`20260907.103031`) CLOSED, [folded](REDS-a-door-and-the-wall-behind-it-rows-565.md);
elder [shelved](20260907-104723_itinerary-landed-accounts.md). Amphora's season door declared
**16** files; 1,024 cargo bytes over an 85-byte line carry **12** -- exit 0 at 12, `cargo too
large` at 13 to 16, **four slots unreachable at any name**. **No marker could spell it:** the
relation is a PRODUCT where `couples`/`covers` compare bound to bound, so it went to the compiler
**both ways** (`n*line <= roof`, `(n+1)*line > roof`), plants 11, 13, 16 each stopping the build.
**Guard failed safe:** a draft DERIVED it and read `partner_absent` -- it reads a spelled value. **Yours:** a `band` word on a roster row; `ios_app_shell`, LOCA.

## Condensed from the pin `20260907.160000` -- the `%499` block, whole

**`%499` OPEN, AND IT COST THIS SEAT TWO LAPS AND COPAL ONE, TODAY.** `fleet_round_open.sh`
classifies by two `is-ancestor` tests and **two states fail both** -- a real upstream rewrite, where
parking is right, and the fleet's ordinary outcome, where a peer pushed on your base and one
`git rebase` re-derives you whole. Measurement whole on
[its shelf](REDS-the-test-that-cannot-fail-rows-499-503.md): **11 `pier/diverged-*`
branches, TEN never reached main**. **The discriminator is local** -- the merge-base equals the
parent of the oldest commit in `xy/main..HEAD` exactly when the local base still stands upstream.
**Not taken:** six ships run that file. **Yours, and COPAL asks it too -- rebase there rather than
park?**

---

## The row this lap carries -- folded from the live front `20260907.163000`

**The row it carries.** `shipped_binary_claim` read red in company and **GREEN alone**: its scan
truncated, appended to and removed a constant `/tmp` name, while `shared_pen_scan.sh` classed both
sites `hold` -- a class whose whole definition is that such a writer *may interleave and survive*.
Its predicate demanded an `r` flag, so it asked **how a removal was spelled rather than whether one
happened**. Eight concurrent copies of the elder scan read **24 of 24 wrong**; repaired, **0 of
24**. Ceilings **6 -> 16** and **53 -> 49**, control **32 -> 38**. Whole in
[`../REDS.md`](../REDS.md) under the stamp `20260907.154440`.
