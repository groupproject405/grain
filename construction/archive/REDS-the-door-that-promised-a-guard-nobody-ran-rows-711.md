# REDS shelf -- row %711, the door that promised a guard nobody ran

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- one BOOKED row, born on its own shelf because the pin has room for no row

Written `20260911`, in the lap that found and repaired the red. `tools/fixtures/r/reds_pin_capacity_scan.sh`
read `pin_headroom=1993` against a `median_row_bytes=2059`, with `pin_foldable_rows=0`,
`rows_that_fit=0` and `pin_deadlocked=1` -- so no lawful fold frees the pin and this row fits
nowhere in it. A BOOKED row may fold by the ledger's own law, so a BOOKED row may be born here: the
move `%699`, `%703`, `%706`, `%707` and `%708` each made before it. The key is the stamp
`20260911.091110`; `%711` is the view, read from the anointed spine with
`sh tools/fixtures/r/reds_spine_derive_scan.sh --next` before it was written.

---

**REDS %711 (`20260911.091110`) -- the vessel room's front door promised a guard the roster seats nowhere, and the instrument built to keep those two in step could only read one of them.** *What went wrong:* `tools/fixtures/am/amphora_roster_scan.sh` holds `readme_unnamed` at zero -- a rostered guard the room's front door never names. It walks the ROSTER and asks the door about each row. Nothing walked the DOOR and asked the roster about each name, and `readme_named` was never counted at all: it printed `guards - readme_unnamed`, a number describing the door derived wholly from the roster. So a name at that door which no roster row seats was invisible to the one reading built to keep the door and the roster together. `amphora/README.md` named `amphora_device_wire` throughout while `construction/standing-equipment.kyri` seated it nowhere, and the scan answered `readme_named=22`, `readme_unnamed=0`, `verdict=ok`. *What caught it:* a fire rota lap reading its own room's guard against the sibling room in the same lane. `tools/fixtures/b/bron_resins_catalog_scan.sh` prints `uncatalogued` beside `orphan_entries` and `unsealed` beside `orphan_seals` -- each list checked against the other -- and the vessel room's scan carried `orphan_rows` for the roster-to-file direction with no door-to-roster twin. Proven in a pen before it was argued: a room whose door names `room_a` and `room_ghost`, with only `room_a` rostered, answers `readme_named=1 readme_unnamed=0 verdict=ok`, exit 0. *What it taught:* **a claim about a door cannot be derived from the roster, and this room has now met that shape three times in three laps.** `%706` widened a roster typed in the instrument, which could not GROW; `%708` widened a group declared in the instrument, which could not SHRINK; this one widens a reading that asked one list about the other and never itself. The tell they share is a number naming a quantity and no name -- `readme_named` said 22 and named nobody, which is `%592`'s reading one room over. *And the invisible name was a second red standing behind the first:* `amphora_device_wire` drives a virtio lab wanting `qemu-system-riscv64 -machine virt`, and the door's own sentence said it *refuses honestly at exit 1 rather than pretending*. That is REDS `%646` exactly -- a hard refusal on an absent OPTIONAL dependency makes a witness unrostable, and an unrostered witness runs nowhere, so the honest refusal reached no reader on any bench, including the benches carrying the emulator that would have run it green. The roster's own `capability qemu_riscv`, seated `20260910.171423` for `mantra_snapshot_wire`, answers it; that probe's comment measures the population as **26 witnesses reaching a wire lab, 0 of 26 rostered**, and this room held one of them. *Repaired:* the scan prints `readme_claims`, counted off the door rather than derived, and `readme_unrostered` naming each claim the roster leaves unseated, under a ceiling that only falls. Candidates come off the guard room's files in both spellings this roster seats -- `amphora_bounds_agree` from a file of its own name and `amphora_pour` from `amphora_pour_witness.rish` -- since a file-basename candidate alone left a hole rather than an undercount: a ghost written the second way was no candidate and so no finding. A path mention credits nothing, so the door's two dozen cited paths stay paths. The blind spot the file rule creates is PRINTED as `readme_claims_fileless`, named off the room's own derived namespace and gated at nothing, because a door name with no file is a real promise of an unwritten witness or a plain noun starting with the room's name, and a hand tells those apart where a pattern cannot; the living door carries one, `amphora_lap3_tree`, a pour fixture. The two populations are disjoint by construction. `amphora_device_wire` took a roster row under `capability qemu_riscv` in the same commit and moved out of the door's *Hand witnesses* section into its standing list, so this pier prints `skipped_capability amphora_device_wire wants=qemu_riscv here=absent` and a bench with the emulator runs the lab whole. *Ratchet rather than gate, measured rather than cautious:* the living door still claims `amphora_lap1` inside the dated sentence `amphora_lap1/2/3`, and the door declares its hand witnesses honestly under the words *run by name, on no clock* -- a wall at zero would refuse that sentence, and a wall that reds on honest prose is a wall somebody turns off. **103 planted behaviors**, up from 77; four mutations each bit their own legs and nothing else. **BOOKED.**

---

*One row, shelved at birth. The pin it could not enter is [`../REDS.md`](../REDS.md).*
