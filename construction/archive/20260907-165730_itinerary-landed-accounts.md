# ITINERARY landed accounts -- folded `20260907.165730`

**Language:** EN
**Status:** Landed accounts, folded from [`../ITINERARY.md`](../ITINERARY.md) -- **checkable room**:
every number below was measured on metal and names the instrument that read it
**Voice:** Kyri
**Room:** checkable

*The live front holds what is OPEN and what waits on Keaton's word; a landed account folds here, the
way a REDS row folds to its shelf. Nothing is edited on the way down.*

---

## GRASS -- a reading of nothing is not a clean tree (`20260907.153705`)

Row `20260907.153705` BOOKED, by stamp. Fed an empty pipe,
[`../../tools/fixtures/l/retired_word_scan.sh`](../../tools/fixtures/l/retired_word_scan.sh)
printed `retired_word_files=0`, `retired_word_hits=0`, exit 0 -- byte for byte what a swept tree
prints -- and its one caller, duty 1 of
[`../../tools/fixtures/l/living_docs_lint_scan.sh`](../../tools/fixtures/l/living_docs_lint_scan.sh),
supplied the zero itself with `${d1_files:-0}`, so a dead roster published
`OK   duty1 retired LEXICON words -- none across 0 living prose pages`.

The scan counts absent roster lines now, prints `retired_word_absent=` beside its two readings, and
exits **2** when no file was read -- naming `roster_empty` on an empty roster and `roster_all_absent`
when every line it held has moved, two words because those send a reader to opposite ends of the
pipeline. A partial absence is reported and never gated. The caller reads that exit code and prints
`ADVISE duty1 unread`.

[`../../tools/fixtures/l/retired_word_control.sh`](../../tools/fixtures/l/retired_word_control.sh)
grew **10 behaviors to 14**: both refusals asserted by name at exit 2, a mixed roster still read at
exit 0 reporting `files=1 absent=1`, and the load-bearing leg -- the refusal block stripped out of a
copy of the scan with `sed`, so the same empty roster walks free and a guard is told apart from a
bypass. That leg also fails when the block it deletes is absent, so a plant that plants nothing says
so. Live reading unchanged: **520 pages, 4 advisories, 0 absent.**

Full account: [`../../session-logs/date/20260907/20260907-155025_a-reading-of-nothing-is-not-a-clean-tree.kyri`](../../session-logs/date/20260907/20260907-155025_a-reading-of-nothing-is-not-a-clean-tree.kyri).
