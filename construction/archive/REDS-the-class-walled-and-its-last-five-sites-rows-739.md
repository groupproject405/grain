# REDS -- row %739, folded from the living pin

**Folded:** `20260915.215554` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A class named by an earlier row is walled at zero, and the five sites that earlier repair left
standing are repaired with it.*

The row stands here exactly as it was written. A closed row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the guard the round built, and
the row itself stays one click away.

---


**REDS %739 (`20260915.191726`) -- the class `%729` named is walled, and the five sites it left standing are repaired.** *What went wrong:* nothing new. `%729` measured 36 unchecked `cd "$pen..."` sites across 26 files, repaired 31 of them in 23 control files at the edge, and left the CLASS open: *`shared_pen` asks whether a pen is SHARED; nothing in this tree asked whether it was ever ENTERED.* *What caught it:* an earth rota lap reading that sentence as a concrete fact at the door rather than an argument, then asking `path_absence_scan.sh` whether a pen-entry reading stood anywhere -- `absent_both=3`. *What it taught:* **contention and containment are two questions, and one guard answered the first.** A pen is contended when eight checkouts share one constant name; a pen is unentered when a `cd` into it can fail and nothing stops the script. The second is the fault that truncated this tree's tracked front door and wrote twelve unsigned commits under a fixture identity. *The reading:* `tools/fixtures/p/pen_entry_scan.sh` collects each file's pen variables -- assigned from `mktemp -d`, or from a string naming one already collected -- and counts every `cd` into one that carries no `||`, no `&&` chain, and no `set -e` in force. **449 runners, 1,140 pen sites, 5 unguarded**, all five in `tools/fixtures/s/stash_record_control.sh`, each a `( cd "$pen/<name>"` opening a subshell whose body writes a scan file and runs `git add -A && git commit`. In the live tree that is `%729` exactly. *Standing:* all five repaired with a named refusal in the same commit, the control re-run at `pass=105 fail=0`, the scan a **WALL at zero**, `pen_entry` rostered at `tier lap` (27s). `unchecked_mktemp=75` is REPORTED and gated by nothing -- an empty pen variable is a different fault with a different cure. **The `set -e` reading is a proxy named as one in the scan's own header:** POSIX suspends it in a condition and on the left of a `&&`, so the meter UNDERCOUNTS and nobody may read its zero as a proof. *What this does not reach:* `%729`'s twelve unsigned commits, which are already pushed -- a history rewrite, custody gate 5, and Keaton's word. *And first residency moved the reading twice in its first hour:* the scan counted `( cd "$pen"; work )` inside its OWN header, and counted the heredoc bodies its control writes as pens. A `#` line is prose and a heredoc body is text a script WRITES rather than shell it RUNS, so both are read past -- which UNDERCOUNTS a generator emitting an unguarded `cd`, and the emitted file is read on its own once tracked. **CLOSED** on `tools/p/pen_entry_witness.rish` GREEN on metal, 45 control legs, four mutations bitten and then lifted.
