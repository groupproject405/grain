# REDS %523, %527, %528 -- the state a reading cannot see

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260907.003622`, oldest-first among the
foldable, to bring the pin back under the 40,960 bytes its own header declares. Two rows stood
BOOKED and one CLOSED; every OPEN row stayed where a reader meets it, and the two rows booked in the
same hour stayed on the pin where peers can still see them.*

**What the three taught together, in one line:** each is a reading that was correct about the state
it imagined and blind to the state the fleet was actually in.

**The three blind spots.** `%523`'s send assumed `HEAD` was its own commit and amended a peer's,
because a conflicted rebase leaves HEAD at the last commit that applied. `%527`'s meter read one of
the two spellings by which a failure is thrown away, and the other was standing in a rostered gate.
`%528`'s lock reading called a pass abandoned only when the holder's parent is `1`, and a detached
subshell -- the way this fleet actually launches a hot pass -- never reaches that state.

**Why they are kin.** Each reading is sound about the world its author was standing in, and each was
carried into a world with one more shape in it: a rebase that stops, a second spelling, a subshell.
None of the three could be found by reading the code, because the code says what it does. They were
found by the fleet arriving in the state nobody had modelled.

**What answers it** is the habit `%463` seats and these keep re-teaching: name the state you did not
read, beside the reading. A guard that publishes its blind spot is one a later lap can widen; a
guard that publishes only its verdict has to be rediscovered from a red.

**REDS %523 (`20260906.230614`) -- `git commit --amend` inside a conflicted rebase rewrote a PEER'S commit and folded my whole lap into it.** *What went wrong:* the send ran its steps as one chained command -- pull, then pin the card, then `git add -A && git commit --amend --no-edit`. The pull hit a conflict and stopped mid-rebase, where **HEAD is the last successfully applied commit, which was a peer's**. The amend therefore rewrote `mantra: a line is named by a pair` to contain my ten files; `rebase --continue` then found my own commit empty and dropped it. The log read as though my lap had never happened while its work sat inside somebody else's commit message. *What caught it:* the push refused as a non-fast-forward, then `git log --oneline` showing a peer's subject at HEAD with my own commit nowhere. *What it taught:* **a chained send has no state between its links, and a rebase is a state.** The round-open law already knows this one room over -- *do not run `git rebase --continue` on a tree that opens mid-rebase* -- and the same sentence is owed to the send: an amend must first ask whether a rebase is standing. *Contained, and this is the whole of the good news:* **nothing was pushed**; `git branch -r --contains HEAD` answered empty, and upstream held the peer's commit at its own hash `bc37657e8` throughout. *Repaired:* parked the mishap on `pier/amend-mishap-20260906-223500`, reset hard to `xy/main`, restored the ten files by name from the park, and committed them as their own commit -- `git diff` between the peer's real commit and the amended one is **exactly** those ten files, which is what proved the peer's work was otherwise untouched. *Standing:* a send checks `.git/rebase-merge` before it amends -- and the FORM matters, learned on the very next send: `ls -d .git/rebase-merge .git/rebase-apply` prints the one that exists and still exits non-zero, so a `&&` after it reads "no rebase" while the rebase stands. The rule spells the test `[ -d ... ] || [ -d ... ]` for that reason. *Erratum:* booked `%520`, renumbered on the merge that found a peer's `%520` at `20260906.195208` -- the earlier stamp keeps the number, and this row kept its own stamp throughout. **BOOKED.**

**REDS %527 (`20260906.222830`) -- the meter built so that a guard which cannot run its instrument must refuse read only one of the two ways a failure is thrown away, and the other spelling was standing in a rostered gate.** *What went wrong:* `tools/fixtures/r/radiant_negation_scan.sh` looked up each rule's baseline row with `awk ... "$BASELINE" 2>/dev/null || echo -`. Both halves are losses and the second is the worse one: `2>/dev/null` discards awk's reason, and `|| echo -` puts back the single token that means *no baseline row for this file, admit it at its measured value*. `|| true` leaves emptiness, which at least looks like nothing; a fallback value reads as an answer. *Proven on metal, twice, before a line was changed:* pointed at a mode-000 baseline the guard printed `enforce_files=51`, `enforce_admitted=51`, `enforce_risen=0`, `verdict=ok`, exit **0** -- green while comparing nothing, with its own stderr already gone. A readable but empty baseline read identically, and that second path is a different mechanism reaching the same green: awk RUNS and answers honestly, because an emptied, truncated, or path-shifted baseline simply holds no row for any file. *What caught it:* classifying my own leftover from `%514`. That row closed two comment meters and left *10 of 244 scans still drop awk's stderr* on the card -- a **grep count**, not a reading. Classified, four of the ten were false positives (the redirect on a different command, a planted control string, a string awk generates), two were prose describing the elder fault, and four were live sites; of those four, exactly **one** could not be innocent. *What it taught, and why it is a wall rather than a repair:* `tools/fixtures/i/instrument_refusal_scan.sh` exists for precisely this class and held it at zero, honestly -- its reading requires the output to land in a **file** and the discard to be `|| true`, and this site is a command substitution ending `|| echo -`. **A meter narrowed to the spelling in front of its author answers about that spelling**, which is the third firing of the shape `%518` and `%519` booked one room over, so it becomes a gate. *Repaired:* the meter reads a second count, `fallback_instrument_passes`, walled at zero under its own ceiling so neither shape's ceiling can free the other; it **joins backslash continuations** before matching, because six of the first seven hits were the `test && echo yes || echo no` ternary and the seventh was that same ternary across three lines, seen only by its tail. Measured after: **1 fault in 764 tracked tool scans**, repaired on this lap. The guard now refuses an unreadable baseline with awk's own reason beside it, refuses a baseline that matched none of the corpus, and prints `enforce_matched` -- because an admitted count alone cannot say whether anything was compared. *One more, small and exact:* the control is itself a tracked tool scan, so its planted shapes entered the corpus under test and reddened the live tree; the elder `|| true` plants had escaped only because a plant's closing quote falls where the pattern wants end-of-line. The plants spell the operator through `$D` now, so the pen holds the bytes and the source spells no discard. Control **11 -> 20**, every new refusal planted and then lifted. **CLOSED.**

**REDS %528 (`20260906.233028`) -- the roster runner tells a lap when a lock's holder has been abandoned, and it cannot see the one way this fleet actually abandons one.** *What went wrong:* `tools/fixtures/s/standing_equipment_run.sh` reads the lock holder's parent with `ps -o ppid=` and calls the pass abandoned only when that ppid is `1` -- the reading `%387` built. A lap that launches its hot pass detached, `( sh runner --hot > out 2>&1; echo EXIT=$? >> out ) &`, forks a subshell to carry that compound command, so the runner's parent is the **subshell** rather than the lap. When the lap ends it is the subshell that reparents to init, while the runner's own ppid goes on naming it, alive. Measured on this tree at `20260906.231137`: `run_lock=in_flight pid=2759749 parent=alive`, where 2759749's parent 2759747 was alive with its own parent `1`, and its process group leader 2739563 did not exist. The orphaning had happened one generation further up than the check can see. *What caught it:* the cold open this card tells every lap to begin with, refused under `run_verdict=run_in_flight` against a pass whose lap had died 26 minutes earlier -- **and refused in silence**, since the repair advice is printed only where `parent=gone`. The instrument withheld the one sentence the reader needed at precisely the moment it was needed. *What it taught:* **a liveness reading fixed at one generation is a reading about a relationship the caller does not control.** `%387` declared its own under-report honestly and named the subreaper as the case -- a host that adopts orphans away from init -- so the limit was understood as exotic and hosted elsewhere. It is neither: the lap's own `&` produces it, on any POSIX host, and it is how six of eight ships end a send. **The honest limit was true and the example was unrepresentative**, which reads as coverage while leaving the common case uncovered. *Repaired (`20260906.233028`):* the runner reads the holder's **process group leader** beside its parent -- `ps -o pgid=`, then `ps -o pid=` on that leader rather than `kill -0`, which answers EPERM for a stranger and would read a live leader as gone -- and prints `run_lock=in_flight pid=N parent=X group_leader=Y lap=Z`. **Neither reading contains the other**, so both are taken and either answers: a direct orphan keeps a live group leader and only the parent reading catches it; a detached pass keeps a live parent and only the group reading does. A process group is the lap's whole spawned family and its leader is whoever started it -- measured here, `sh tools/f/fleet-loop.sh <seat>` for a live pass -- and `&` starts no new group in a non-interactive shell, so the leader's absence is the lap's absence. `lap` is the one word the advice turns on, and the advice now says **which** reading fired, since a hand reading `parent=alive` beside `stop it` deserves to be told why those stand together. **The direction of error is unchanged:** both readings only ever add a reason to say `gone`, both fail silent where `ps` cannot answer, and **nothing is reaped** -- an orphan is a live writer, and one pass ending another's is the cross-hand act `%291` forbids. *Proven:* **13 readings** join the control's lock family, **34** in all. The detached plant is built in three processes under `set -m`, which is the whole of why it is reachable -- job control is off in a non-interactive shell, so `&` starts no new group and a plant would inherit the control's own live-led group, which is how the first draft read `unavailable` on a host that makes the shape perfectly well. The plant is **verified before it is trusted**, and the load-bearing reading asserts the **defect**: `deep_orphan_parent_reads_alive=yes` stands beside `deep_orphan_group_reads_gone=yes`, so a widened check can be told from one that has started saying `gone` about every holder. The group reading's own negative side gets its own plant, since the elder live case holds the lock with the control's pid and that pid's group leader is not this file's to assume. **Both directions on metal against one planted orphan:** the runner answers `parent=alive group_leader=gone lap=gone` and names `kill -TERM`; the same runner stripped of the group reading answers `parent=alive group_leader=unknown lap=alive` and names **nothing** -- the silence that refused this lap's open. *And the elder repair held where it was aimed:* stopping the real orphan with `kill -TERM` ran the runner's own EXIT trap and released the lock unaided, 40 seconds later, exactly as `%387` wrote it. *The witness cannot reach its control leg on this clone* -- `tools/s/standing_equipment_witness.rish` refuses first at its scan leg on `runs_red`, `%387`'s own shape -- so the 13 asserts were checked verbatim against the control's live output, 13 satisfied and 0 unsatisfied, with the five elder lock readings green beside them. *The remainder, and it is the root:* nothing stops a lap from detaching its pass at all, which is what makes an orphan and, in the same stroke, ends the lap at `git add` -- 17 files of finished work went to a stash on this tree in that one gesture. A guard cannot reach it, since it would have to run after the lap has gone. **BOOKED.**
