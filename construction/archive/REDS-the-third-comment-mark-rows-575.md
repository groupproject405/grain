# REDS -- row %575, folded from the living pin

**Folded:** `20260907.144002` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A guard that names the languages it reads covers only the languages somebody thought to name. The
ASCII law held two comment marks for a fortnight while a third language of this tree's own stood
outside it, and the head that said so read as complete because it counted syntaxes rather than
deriving them.*

The row stands here exactly as it was written. A booked row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the instrument the round
built, and the row itself stays one click away.

---


**REDS %575 (`20260907.144002`) -- the ASCII law held two comment marks, and a third language of our own stood outside it for a fortnight.** *What went wrong:* `.claude/rules/ascii-first.md` governs every code comment, and `tools/as/ascii_comment_witness.rish` carried it for `//` in Rye and `#` in Rishi and shell. Its own head read **"ONE LAW, TWO COMMENT SYNTAXES"** and stopped there. Glow spells a comment `::`, and its **451 tracked sources** were read by no ASCII meter at all -- measured `20260907.141019`: **942 non-ASCII characters across 342 files**, in eight forms the rule's own substitution table names seven of. The elder witness's sweep on `20260825` reached 2,163 files across two syntaxes and never touched a `.glow`. *What caught it:* the water rota row sent this lap into `foundations/20260618-184912_growing-a-language.md`, and reading three `glow/gen/s/` data fixtures for `%532` put their heads on screen -- both carrying a middle dot and an em dash. Counting them tree-wide is what turned a glance into a number. *What it taught:* **a guard that names its languages catches only the languages somebody thought to name.** The elder head is the evidence: it says *two syntaxes* rather than *every syntax we author*, so it read as complete while a third sat outside. A meter should derive its population from what the tree holds, exactly as `%532` taught one room over -- and the two rows are the same lesson in different clothing, one about desks and one about languages. *Repaired (`20260907.144002`):* `tools/fixtures/g/glow_comment_ascii_scan.sh` counts non-ASCII characters in `::` comments across every tracked `.glow`, and `tools/as/ascii_comment_witness.rish` now holds **three** readings. **The third meter needs no escape hatch, and the reason is the language's own:** Rye must hold back at a `\\` multiline string and shell at a heredoc body, because in both a literal spans lines and a later line of one can open with the comment mark -- `glow/tokens.rye:239` refuses a newline inside a cord literal outright, so a line opening `::` sits outside every literal. Measured rather than inherited: **all 942 characters sit in comments and none in program content**. **The blind spot is bounded and PRINTED**, where both siblings leave theirs in prose: a trailing `::` after code goes unread, and `trailing_unread` counts it -- **zero** across all 451 sources today, so a reader can tell an empty blind spot from a large one. `tools/fixtures/g/glow_comment_ascii_control.sh` proves **14 behaviors** on planted desk rooms in a pen, the cord and the cord-carrying-a-mark both proven uncounted, the ceiling shown from both sides. *Ratchet, not a gate:* **942**, and it only falls. *Named rather than taken:* the sweep itself, which would convert 921 characters the table names and leave 21 `identical to` marks to a reader's judgment; and this witness rides `tier cadence`, the clock `%568` reports nothing turns, so the reading is exactly as live as that row is. **BOOKED**
