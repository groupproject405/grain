# REDS shelf -- a pointer that carried a total, row %545

**Language:** EN
**Stamp:** `20260907.062544`
**Status:** Shelf -- immutable once written
**Rows:** `%545` -- booked `20260907.062544`, folded off the pin the same lap; the number moved `%540` -> `%542` -> `%545` across three rebases in one send, each bringing peers' rows home ([`derived-spine`](../../.claude/rules/derived-spine.md): the stamp is the key, the number is a view)
**Voice:** Kyri

**What the row taught, in one line:** a canonical page grew three strands and every pointer at it
kept the elder total, because *count, never number* had no meter over living prose. The loom is
[`tools/g/grain_strand_count_witness.rish`](../../tools/g/grain_strand_count_witness.rish), which
derives the count off the page's own structure and holds every disagreeing living sentence at zero.

---

**REDS %545 (`20260907.062544`) -- the grain seated three more strands and ten living sentences kept counting ten.** *What went wrong:* `foundations/20260826-024942_the-grain-and-the-crossing.md` seated ten strands `20260702` and three more `20260802` under *Strands Seated at the Fusion*, "beside the elders" in its own words. Thirty-six days on, ten living sentences across six documents still said ten -- `foundations/README.md`, the day-one rose `docs/COMPASS.md`, `follow-our-compass` in four places, the aether threshold the rota opens with, the singularity history, and a manual tutorial. Every one is a pointer at that page, and two are front doors. *What caught it:* the aether rota read (lap 4145, row 0), whose threshold page asks a lap to listen for the silence where a claim used to be -- then the page's own body tallied against its pointers, by hand and then by `tools/fixtures/g/grain_strand_count_scan.sh`. *What it taught:* **a pointer that carries a total goes wrong on a day nobody is looking.** `.claude/rules/stamp-and-name.md` rule 3 already forbids it -- count, never number -- and nothing had ever applied it to the grain, because no meter read living prose for a carried total. The count is derived from the page now and stated once; the pointers name the strands instead. `tools/g/grain_strand_count_witness.rish` holds disagreements at zero over 849 living documents, and its control grows the pen's page by one strand to prove the total is read rather than spelled. **CLOSED** on that witness GREEN.
