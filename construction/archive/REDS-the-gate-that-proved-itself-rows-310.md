# REDS -- the gate that proved itself against its own copy (row %310)

**Language:** EN
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded:** `20260828.114450` from [`../REDS.md`](../REDS.md), **CLOSED**.

One row, and it names a failure mode worth carrying past the lap that closed it: an oracle derived
from the implementation it checks is that implementation, twice. Three argv plants computed their
expected value from the same expression as the gate body, so every one of them returned zero for
every input and spoke nothing at all -- and eight pair walls could not be seen to refuse. The repair
was subtraction rather than correction: the emitter that could write a tautology is deleted, so the
shape is unwritable rather than merely unused.

The row folds on the lap its kin was written. `%311` carries the other half -- the meter over the
witness surface this row named and parked -- and reads the same sentence one layer up: the framing a
harness prints is not evidence, and a substring test cannot tell the two apart. Both rows describe
one thing from two sides, which is why this one can rest here while that one stays flat and open.

---

**REDS %310 (`20260828.110051`) -- the gate proved itself against its own copy, and the emitter that wrote the tautology is deleted rather than fixed.** *What went wrong:* three sites in `glow/lower_shop_gate.rye` emitted an argv plant whose `main` computed `expect` **from the same format arguments as the gate body** -- the two-face pair site built `if (a op b) then else` twice and compared them, the `.call_one` vane plants confessed it in their own comment (*"body and expect share one expression"*), and `.identity` compared the face to itself. Every such plant returned 0 for every input and **spoke nothing**, so `gate-caravan-caps-pair-bound-u32 9 8` -- a count past its cap -- exited clean and silent, and **eight pair walls plus every vane-decide plant could not be seen to refuse**. Above them, `glow_vane_pair_mirrors_witness.rish` (seated on the cadence roster a day prior) asserted only `.ok` and closed on *"every edge exact"* -- a sentence no run had ever checked -- while the worker's `EXIT:0` tail fed any `contains "0"` assertion for free, Rishi `contains` being a plain substring test. *What caught it:* Silence's crashed lap found it reading the council rota's Aether row -- *a guard that cannot red guards nothing* -- against the newest rung rather than the oldest, split it by lane per the charter, and left the lowering half named for **Sound's language custody**; this is that half, taken on the seat card's own word. *What it taught:* **an oracle derived from the implementation it checks is the implementation, twice.** The fold family had carried the honest shape all along -- *EXIT:0 + spoken answer prove it* -- and the repair is subtraction into it: all three sites now emit the speaking main (`emit_argv_speaking_main`, and a new two-face `emit_argv2_speaking_main`), the truth living with the witness that reads the spoken digit, and **`emit_argv_main` is deleted**, so the tautology is unwritable rather than merely unused. *Repaired (`20260828.110051`), on metal at both edges:* `gardens_lawful 8 -> 1, 9 -> 0`; `caps pair (3,8) -> 1, (9,8) -> 0, (8,8) -> 1`; `mantra floor (3,2) -> 0` matching each gate's own placard; the mirrors witness rewritten to assert the **exact first-line spoken digit per side** -- eight sides, three refusals among them, `contains` refused in favor of trimmed equality precisely because the EXIT tail bought the elder blindness. Regressions GREEN: argv, sample, pair-faces, pair-select, tally-pair, tend-unary, rune-shape, and the TAME gates. **Kin:** the parked `glow_gate_answer` ratchet (Silence's half, awaiting its lap's adoption) counts the remaining `contains`-shaped walls tree-wide. CLOSED.
