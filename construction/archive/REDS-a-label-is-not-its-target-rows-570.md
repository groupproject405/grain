# REDS -- row %570, folded from the living pin

**Folded:** `20260907.121623` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A day shelf names its log twice -- once as the link's label, once as the link's target -- and a
test that reads the basename finds the target's promise and charges it to the label. The loose
question and the exact question are both real, and a predicate copied without its question is how
a guard comes to disagree with itself.*

The row stands here exactly as it was written. A closed row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the instrument the round
repaired, and the row itself stays one click away.

---


**REDS %570 (`20260907.121623`) -- a promise test that reads the basename cannot tell a link's label from its target, so a kept promise reads as a broken one.** *What went wrong:* the lost-set split I landed this lap asked whether a broken reference is a **promise** -- a link a reader clicks -- by grepping the citing file for `](<any dir>/<basename>)`. A day shelf writes its row with the log named **twice**: once in backticks as the link's own **label**, once as the target `<day>/<basename>`. The extractor records those as two references, correctly, because they are two different strings; the target resolves home and the label cannot, since it names no room. The basename-shaped test then found the target's link and marked the **label** a broken promise. *What caught it:* the field, inside the lap, before the reading landed anywhere. `lost_promised_living=1` named exactly one reference tree-wide, so I opened it -- `waymarks/date/README-index-20260725.md` -- and its link is correct. A count of one is small enough to read whole, which is the only reason this was caught rather than published. *What it taught:* **the loose test and the exact test answer two different questions, and both questions are real.** The `declared` pass one screen up is deliberately loose in this same place, because it asks whether a **line** still links what it calls absent -- a question about the sentence, where any spelling withdraws the declaration. This asks whether **this reference** is itself a promise, which only its own text answers. Copying a neighbor's predicate without asking which question it was written for is how a guard comes to disagree with itself. Both spellings were run against the pen before either number was written -- loose reads `3` repairable where exact reads `2`, and on the field `1` against `0` -- and `tools/fixtures/d/dated_path_control.sh` now plants a label-and-target row so the loosening is bitten rather than remembered. **CLOSED** on `tools/d/dated_path_witness.rish` GREEN.
