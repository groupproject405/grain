# REDS -- row %302, folded from the living pin

**Folded:** `20260827.205859` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Ten witnesses could only run on one machine, and 1,391 siblings showed the right way.*

The row's transferable half is a way of reading a population rather than a file. When one shape
holds 99.3% of a set, the remainder is a defect rather than a variant -- ten copies of one hand's
convenience, spread by the same copy-the-neighbour habit that spread the correct form everywhere
else. A meter that counted the two spellings would have said so on the lap each one landed.

It also drew a line a future guard has to respect: a path a program **opens** is a promise, and a
path a program **prints** may be an instruction about another machine entirely. Two absolute paths
stayed for exactly that reason.

---

**REDS %302 (`20260827.201258`) -- ten witnesses could only run on one machine, and the tree holds 1,391 that show the right way.** *What went wrong:* ten tracked witnesses opened with `let zig = "/home/keeper/grain/vendor/zig-toolchain/zig"` -- an **absolute path into one host's home directory** -- where **1,391** of their siblings write the relative `vendor/zig-toolchain/zig`. On any clone that is not the pier, all ten fail at their first build with a missing toolchain, and the failure names a path nobody reading it would recognise as the fault. The ten span six letter rooms: `brix_infuse`, `first_hour`, `interfaces_conformance`, `operations_conformance`, the three `pleac_*` witnesses, `skate_styled_run`, and both `wade1_*` witnesses. *What caught it:* running one of them for an unrelated reason on the macOS clone, and reading the refusal rather than assuming a missing toolchain was this host's problem. The tree's own `tracked_link` witness already carries the sentence this row needs -- *a reference is a promise, and a promise kept only on one machine is not kept* -- and it enforces that for **links** while nothing enforced it for a **path in a string constant**. *What it taught:* **the ratio is the tell.** Ten against 1,391 is not a design decision anyone made; it is ten copies of one hand's convenience, spread by the same copy-the-neighbouring-witness habit that spread the correct form to the other 1,391. When one shape holds 99.3% of a population, the remainder is a defect rather than a variant, and a meter that counted the two spellings would have said so on the lap each one landed. *Repaired (`20260827.201258`):* all ten read the relative path, written through the original inode so the tracked mode survived, and `brix_infuse`, `pleac_lists` and `first_hour` are **GREEN on this macOS clone** where they refused minutes before. `exec_bit` green. **Surfaced, not crossed:** the two remaining absolute paths are correct and stay -- `agent-jail.sh` and `launch-dream-dual-chapter.rish` each print `bash /home/keeper/grain/nixos/rebuild-outer.sh` as an instruction telling Keaton what to type **on the pier**, where that path is the true one. A guard for this class must tell a path a program *opens* from a path a program *prints*. CLOSED.
