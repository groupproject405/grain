# REDS -- row %719, born on its shelf

**Written:** `20260911.192649` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*An ignored declaration is worse than an absent one -- a guard offered a page two comments and its own comparison read only one of them.*

Booked at `%717`, renumbered to `%718` and then to `%719` across two rebases. `xy` published a row
against each number while this one stood unshared -- `20260911.185900` for the first and
`20260911.193818` for the second -- so a published number never moves and this row derives above
both. The key is the stamp
([`../../.claude/rules/derived-spine.md`](../../.claude/rules/derived-spine.md)).

The row is born here rather than on the living pin, as `%699`, `%703`, `%706`, `%707`, `%708`,
`%711`, `%712`, `%713`, `%715`, `%717` and `%718` were: `reds_pin_capacity_scan.sh` read `rows_that_fit=0` with
129 bytes of headroom against a 1,863-byte median row. The pin keeps what a reader will actually
read; the lesson travels forward in the guard the round repaired.

---

**REDS %719 (`20260911.192649`) -- a guard offered a page two declarations and its own comparison read only one of them.** *What went wrong:* `tools/fixtures/t/tutorial_output_scan.sh` lets a page declare an output block two ways, and the two answer different questions -- `<!-- selected: ... -->` says WHICH lines of a longer report the page quotes, `<!-- volatile: ... -->` says the figures MOVE. Its parser consumes both comments into separate variables, so a block may lawfully carry both. The comparison did not: on a containment failure the selected branch counted `drift` without ever testing `vol`. A page quoting two lines of a growing report therefore had no sentence it could write. `docs-geode/demos/README.md:91` is exactly that block, quoting `clutter=43 fascia=57` where the meter reads 44 and 56 today, and the same page teaches the lesson for its own archive count two sections above -- *read the line as the day's reading rather than the tree's*. *What caught it:* the fire rota, row 2 -- the row that sees, reading a red's PARSER rather than its subject, after the refusal named a page whose figures are free by construction. *What it taught:* **an ignored declaration is worse than an absent one.** An absent one leaves a page visibly uncovered; an ignored one is written by the author, read by a reviewer, and discarded by the instrument, so three parties believe the block is held and none of them is. That is the unheard-guard shape moved inside a single scan, where no roster or tier can see it. *Repaired (`20260911.192649`):* an `elif [ -n "$vol" ]` stands ahead of the drift branch, the scan's header states that the two declarations compose, and four legs prove it in the pen -- the refusal planted undeclared, then lifted by the comment alone, with a mutation removing the branch reddening three of them. The control also **tallies its own legs** into a `cases=` line the witness asserts at **49**, because `control=ok` reports only that the run reached its last line, so a leg deleted and a leg that passed read identically; this witness's own header already regretted carrying a count two short for a fortnight. *What the repair did not need:* the page, which a peer had repaired upstream in the same hour by dropping the `selected` comment -- a lawful fix that leaves the guard's own fault standing for the next page to meet. **BOOKED** -- the class is closed here; what remains is whether the tree's other declaration-accepting scans honor what they offer, which is a lap rather than a repair.
