# REDS -- the question that sent

**Language:** EN
**Status:** Shelf -- one folded row from `construction/REDS.md`, kept whole
**Voice:** Kyri -- **Room:** checkable
**Folded:** `20260908.071628`
**Row:** `%628` (`20260908.071628`) -- CLOSED, booked `%619`, renumbered to `%620`, to `%625`, then to `%628` across three rebases in one send: the anointed spine had spent both numbers on peer rows (`20260908.070340`, `20260908.065244`, `20260908.074500`) while this row stood unshared, so it derives above the fetched head each time (`derived-spine` rule 3). The key never moved -- it is the stamp. **All three renumbers cost three lines apiece**, because every living citation of this row spells the STAMP rather than the number, which is rule 4 paying for itself three times inside one hour. A number cited in prose would have made this an eight-file sweep each time; the stamp made it a rename and two lines.

The row folds on the lap it closed, for the reason its neighbours record one shelf over: the living
pin stands near its bound with almost every remaining row OPEN, and a pin whose living parts fill it
cannot accept a new one.

What the row is for, said once here so a reader arriving from a citation knows before they read.
This tree runs eight ships from eight checkouts on one pier, so the process table is a room they all
stand in -- and a `pkill -f <name>` reaches every ship rather than the lap that typed it. Five
firings taught that, and the fifth (`%603`) put the bounded helper `tools/f/fleet_call.sh` on the
fleet baton, which is where habits are set.

This is the sixth firing, and it is the first with a genuinely new shape: **the cure was used, and
it still cost a pass.** The helper resolves every candidate through `/proc/<pid>/cwd` and refused
two peer trees out loud by name, exactly as designed -- so the wall was perfect and the blast radius
was one checkout. What no wall could reach was the **verb**: the helper's default action was to
send, and its reading mode was the opt-in, so a hand asking a question paid a flag for the question
and nothing for the signal.

The repair is a verb rather than a wall, and it is the shape every other instrument in this tree
already has -- `index-preview` before `index-fold`, `--list` before a repair, `reds_fold.sh`
refusing an open row. Naming a signal is what asks for one.


**REDS %628 (`20260908.071628`) -- the bounded way to signal a process is also the bounded way to ASK, and asking sent.** *What went wrong:* to check whether my own cold roster pass was still running, I ran `sh tools/f/fleet_call.sh --pattern standing_equipment_run` and read the output as an answer. The helper's head opens *signal only what runs in THIS tree*, which is a promise about **reach**; I read it as a promise about **action**. Its default is `signal=TERM` and `--dry-run` is the opt-in, so the question I wanted cost a flag and the send I did not want was free. Two PIDs under this root took TERM, the pass died at **183 lines of receipt with zero red**, and the shell exited **144** -- `%569`'s exact signature from a command that was not `pkill`. *What caught it:* the harness, reporting exit 144 on the background task, and then the helper's own output, which had already named what it did: `verdict=sent signal=TERM` twice under this root, `refused_foreign` for `grain-patchouli` and `grain-copal` by name. **The wall was perfect and the receipt was honest; only the reader was wrong.** *What it taught:* **the sixth firing of this family is the first where the cure was used, and it still cost the pass.** `%541`, `%569`, `%587` and `%603` all typed `pkill -f`. The last of those put the bounded form on the fleet baton the day before, which is where habits are set -- and it worked, because I reached for the helper rather than the killer. What remained was not a reach but a **verb**. Every other instrument in this tree reads before it writes -- `index-preview` before `index-fold`, `--list` before a repair, `reds_fold.sh` refusing an open row -- and this was the one family whose wrong guess costs a lap and whose default acted. *Repaired (`20260908.071628`):* naming a signal is the verb. `dry=yes` is the default in [`tools/f/fleet_call.sh`](../../tools/f/fleet_call.sh), `--signal` sets `dry=no`, and `--dry-run` forces the reading in either flag order through a separate `dry_forced` flag. **The documented form did not move** -- `--pattern X --signal TERM` acts exactly as before, no tracked file calls the helper at all, and every acting leg of the control already named its signal, which is why the change broke nothing and is the evidence that the elder default was never the used one. Four legs added to [`tools/fixtures/f/fleet_call_control.sh`](../../tools/fixtures/f/fleet_call_control.sh), the lap's own fault planted and then lifted on a live process rather than on a summary line: a bare call reports and leaves it **alive**, `--dry-run` wins before or after `--signal`, and naming a signal reaches it. **26 behaviors, 0 failed.** *Named rather than taken:* whether `--pid` should imply action was declined -- one verb, no exceptions, since a rule with a second door is a rule a hand has to remember the shape of. **CLOSED**
