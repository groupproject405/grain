# REDS -- the roster that named one instrument, and the tree grew a second

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one row, written straight here, immutable once written
**Room:** checkable -- a ledger row whose repair is a guard, proven on metal and mutation-bitten twice
**Written straight here:** `20260911.104047` -- the living pin read `pin_deadlocked=1`, `pin_foldable_rows=0`, `rows_that_fit=0` at that moment; the rebase below brought a peer's fold in and the deadlock lifted, which is the ABSENCE class reading its own local bytes

One row, born on its shelf as `%699`, `%703` and `%706` were, and renumbered from `%710` to `%711` and then to
`%713` across two rebases, each bringing in a peer's earlier stamp -- the earlier stamp keeps the
number ([`the derived spine`](../../.claude/rules/derived-spine.md)), because
`tools/fixtures/r/reds_pin_capacity_scan.sh` answered that no lawful fold stands here and that no
row of median size fits the headroom. Read it beside
[`REDS-fold-recital.md`](REDS-fold-recital.md), which records the practice, and beside
[`REDS-the-card-said-walled-the-meter-said-door-rows-706.md`](REDS-the-card-said-walled-the-meter-said-door-rows-706.md),
whose own repair is what made this lap read the capacity from the instrument rather than from a
sentence.

---

**REDS %713 (`20260911.104047`) -- a repointer's exclusion roster named one instrument by hand, and the tree grew a second one that quotes elder paths for a living.** *What went wrong:* `tools/fixtures/t/tool_path_repoint_scan.sh` sets `excluded_names="tool_path_*"` under a comment reading *"Only `tool_path_*` cites a `tools/` path that must stay stale"*. `docs_command_path_scan.sh` and `docs_command_path_witness.rish` are a newer meter over the command paths a page prints, and their own explanations quote `tame_style_check.rish` and `kumara_tilak_witness.rish` in the flat `tools/` form the letter fold retired -- elder paths held deliberately, and named here by basename alone so this row is not itself a candidate for the repointer it describes, to teach a reader what a moved reference looks like. Three such references stood. The guard therefore refused with `references_repointed=3`, and its named repair, `tool_path_repoint.rish apply`, would have rewritten each quotation so its own sentence claimed a June log named a path that stood nowhere in June. *What caught it:* the aether rota row, then a hand turning the cadence clock. `tool_path_repoint` is `tier cadence`, and `construction/standing-equipment-runs.kyri` read `cadence_never_run_here=69` in this checkout, so the refusal had never once been heard here. It came out of a `--cadence-slice 10` pass, which turned the ten longest-waiting cadence guards: **9 green, 1 red, 2,393s**. *What it taught:* **a typed roster inside a discovery reading is where a tree grows past its instrument in silence** -- the same shape `%592` repaired one reading over and `amphora_bounds_agree` repaired one room over on `20260911.022245`. Here it went unheard for a second reason on top: the guard that would have said so sits on a clock nobody turns. *Repaired (`20260911.104047`):* `excluded_names="tool_path_* docs_command_path_*"`, with the reason written per entry rather than as a claim about the whole tree; the field reads `references_repointed=0`. And because a wholesale exclusion hides an ordinary reference beside the quoted one -- which the scan's own comment already records happening once, to `dated_path_witness.rish` -- the roster is now PRINTED every run, every excluded file NAMED, and the flat references standing inside them COUNTED: `excluded_files=11`, `excluded_flat_references=20` on the tree this was written against and **22** after the rebase -- a free figure, so run the scan rather than reading it here. *A lantern inside the repair itself:* that count read **1** on its first run and **20** on the next. The scan runs under `set -eu`, so a `grep` matching nothing exits 1 and ends the walk mid-list -- the first excluded file holding no flat path stopped it, and the short number looked exactly like an answer. The pen now orders an empty excluded file ahead of a full one, so the leg reads 2 with the guard and 0 without it. **Two mutations bitten**, each in its own direction: dropping `docs_command_path_*` reds `untouched_second_instrument`, and dropping `|| true` reds `excluded_refs_counted`. Eleven behaviors stand in `tools/fixtures/t/tool_path_repoint_control.sh` under `tools/t/tool_path_repoint_witness.rish`. *What this does not reach:* the other **68** cadence guards this checkout has never run, and the question of whether one machine's eight checkouts each owe that whole clock separately -- measured this lap at **66 of 83** cadence guards holding a receipt somewhere on this pier while each ship's own card reads most of them never run. That is `%568` still standing, and the reading is on `construction/ITINERARY.md` for Keaton's word. **CLOSED.**
