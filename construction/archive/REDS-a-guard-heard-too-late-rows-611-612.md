# REDS %611 and %612 -- a guard is worth what it is heard in time to prevent

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260908.064500`, so the pin keeps its
declared bound and holds what is still open. Both rows are CLOSED, and both are about the distance
between a reading being right and a reading arriving. `%611` records a dialect law that was correct
and forty-three minutes late at another ship, because the family is checked on a roster pass and the
lap that breaks it is never the lap that hears it. `%612` records a guard holding a module head to
its module that could not read a name carrying a digit, and so answered ok over an operation it had
never looked at. Read together: a check that is true but slow and a check that is fast but blind
fail the same reader, and neither shows up as red.*


**REDS %611 (`20260908.040446`) -- the dialect law was right, and 43 minutes late at another ship.** *What went wrong:* `tools/fixtures/s/shell_dialect_scan.sh` holds six GNU-only families, five at a ceiling of ZERO, at `tier lap` inside a roster pass of half an hour -- so the lap that breaks a family is never the lap that hears it. Two GNU-only `sed` in-place plants landed in `tools/am/amphora_mark_wreck_witness.rish` at **01:59** by `0f48f2c85`, first heard at another ship's cold open at **02:42**; two hands then cured them in parallel and one cure withdrew whole. *What caught it:* the elder guard at its clock, and the log of the ship that paid. *What it taught:* **a gated family at a ceiling of zero is a gate that can be asked at the door.** Zero means no lawful site stands anywhere, so every staged hit is a rise and no author is refused for another's line -- which is what makes a whole-blob commit-time reading honest. It is read from the elder, never asserted. *Repaired (`20260908.040446`):* `tools/fixtures/s/shell_dialect_touch_scan.sh` is rule seven of `tools/hooks/pre-commit`. It lifts the six patterns, both read-past paths and all six ceilings out of the elder **by name** rather than respelling a rule two files could disagree about (`%382`), reads staged blobs off the **index**, gates the zero-ceiling five. **Replayed against `0f48f2c85` it answers `staged_gated_sites=2`**, naming file and line -- the refusal a hand would have met at 01:59. `shell_dialect_touch` GREEN over 28 pen behaviors with the hook driven by real commits; the pen's plants sit on the elder's roster, so each assembles its flag. **CLOSED**

**REDS %612 (`20260908.052546`) -- the guard that holds a module head to the module could not read a name with a digit in it, and answered ok over an operation neither of its readings could see.** *What went wrong:* `tools/fixtures/m/mantra_weave_head_scan.sh` reads `mantra/src/weave.rye`'s `//!` head against the module's own `pub fn` declarations in both directions, and matched an operation name as `[a-z_]+` on **both** sides -- the head entry regex and the declaration regex alike. Rye's own convention is `snake_case`, which permits digits. So when `Weave.from_v1` landed with its head line beside it, the scan read `declared=5 listed=5 head_missing=0 head_stale=0 verdict=ok` over a module publishing **six** operations, one of them invisible to the census entirely. **Both blindnesses were symmetric, which is why it read healthy:** a member absent from both sides of a comparison leaves the comparison balanced, so the one reading that could have told a reader something -- the count -- agreed with itself. *What caught it:* adding the operation and reading the scan's own `declared=` line rather than its verdict. `verdict=ok` was true of what the scan could see and false of the module. **The elder scan, run in a pen against this module carrying six operations, still answers `declared=5 listed=5 verdict=ok` on metal** -- the blindness proven from the failing side rather than reasoned about. *What it taught:* **a census whose reader cannot name a member reads exactly like a file with no such member**, which is `%463`'s family and `%500`'s one room over -- and the sharper half is that a two-directional guard is not two guards when one character class feeds both. `head_missing` and `head_stale` were built as mirrors precisely so a fault in one direction could not hide in the other, and a name class shared between them defeated both at once with a single character. This is the third instrument in this lane to be caught answering the cheaper question wearing the expensive one's answer. *Repaired (`20260908.052546`):* both patterns widened to `[a-z][a-z0-9_]*`, with the head entry's optional receiver widened to `[A-Za-z_][A-Za-z0-9_]*\.` for the same reason; the scan now reads `declared=6 listed=6 verdict=ok`. Proven both ways on metal: with the `from_v1` head line deleted in a pen it answers `head_missing=1 verdict=head_disagrees` exit 1, where the elder class could not have seen the line either way. The regression is held rather than remembered -- `tools/fixtures/m/mantra_weave_v1_lift_control.sh` carries a `head_digit` leg aimed at exactly this name shape, beside the elder `head_missing` leg, so a narrowing of the class reds on the lap it arrives; and `tools/m/mantra_weave_v1_lift_witness.rish` asserts `declared=6` by name with the refusal spelling out what the elder class did. **CLOSED**
