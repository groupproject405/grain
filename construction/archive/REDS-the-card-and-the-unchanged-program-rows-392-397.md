# REDS %392, %397 -- the card, and the program that never changed

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260831.090000`, one **CLOSED** and one
**BOOKED** -- two readings of one instrument, taken a day apart by two hands, and both say the same
thing: `tools/fixtures/q/qa_report_card.sh` moved a program's grade while the program stood still.
In the first, a hand's typed `--setting` word decided whether one unchanged module read C, C+ or A,
because the law assigns a program two settings and the card took one. In the second, twenty-eight
lines of a reference table carried no terminal punctuation, so they merged into the sentence beside
them and a head went from C to A on periods alone. A grade is a measurement, and a measurement that
moves when its subject does not is reporting on the instrument. Moved here to bring the pin back
under its bound after a rebase carried two hands' rows in together, which is `%395`'s own class
arriving on the very lap that closed a sibling of it. Link depth moved with the text.*


**REDS %392 (`20260831.031048`) -- a program carries two prose settings by law, the report card reads it at one, and the same unchanged file grades C, C+, or A depending on which word a hand types.** *What went wrong:* `context/GAUGE_STYLE.md` assigns two settings inside one source file -- **Door** at the head of a module, where a reader arrives holding no context, and **Meter** at a bound or an assert, where the comment names the failure the number was chosen against and negation is the subject rather than a fault. `tools/fixtures/q/qa_report_card.sh` takes a single `--setting` for the whole artifact and feeds it to both counted readings, so a program's blended comment corpus is weighed against one ceiling. Measured this lap on `comlink/virtio_net.rye`, one file, one state, three runs: **C 72** at `door`, **C+ 77** at `field`, **A 94** at `meter` -- a 22-point spread and two letter grades decided by the hand rather than by the artifact. The `meter` reading is the sharpest, since `grade_ceiling=0` and `xref_ceiling=0` make Register and Reach report rather than score, so any module carrying comments reads 100 on both counted halves without the instrument weighing a sentence of it. *What caught it:* the carded molt the live card itself named. Rewriting that module's head as Door prose moved Register **65 -> 73** (35% -> 27%, under Field's 30% ceiling) and left Reach pinned at **60**, grade 15 against 11 -- so the repair the reading asked for could not move the reading that asked for it. Grading the two halves apart said why: the new head measures **8.8** against Door's ceiling of 9, and the 501 words of `// invariant:` comments beneath it measure **16.4** against a ceiling the same law leaves uncapped. The head is already right; the file is graded on the average. *What it taught:* **when a law assigns two settings inside one artifact, a single-setting meter reports the hand's choice rather than the artifact.** Kin to `%276`, where this card threw a program's comments away and graded its code, and to `%358`, where its extractor knew `//` and `#` and not Glow's `::` -- the same instrument, the same class, one layer in. The halves are found now and still weighed as one. The QA rule's own test settles the direction: a meter that instructs a repair which would make the artifact worse is the thing to fix, and Field over this file instructs a writer to simplify the invariant comments TAME asks to be exact. *Named, not taken:* a card that reads a module head at Door and bound comments at Meter and reports two readings rather than averaging one. `%386` is the standing warning against the naive form, where a guard built in a hurry flagged 14 and was wrong about 13; the split here needs a rule for the doc comments between the two poles, since a `///` on a public function is neither a module head nor a bound. Shared machinery every body runs, so it wants its own lap and a control proving both poles from both sides. *Repaired (`20260831.065204`):* the card now derives both poles from comment grammar: the module head scores at Door, `invariant:` lines report at Meter, and declaration docs remain a named third count. A caller's legacy `--setting` word cannot change a program's grade. Seventy-nine control behaviors bite when either pole leaks into the other; `virtio_net.rye` reads **A 95** under `door`, `field`, and `meter`, with its 16 head lines, 10 invariant lines, and 13 declaration-doc lines reported separately. *Measured after, on 134 sources sampled every 37th:* the landed card reads **61 below B** where the elder at `577479ade` read **all 134 A+**; a second build read 60. **CLOSED.**

**REDS %397 (`20260831.074836`) -- twenty-eight periods moved a program head from C to A with its content unchanged, because a reference table has no sentences and the card reads one anyway.** *What went wrong:* `tools/fixtures/q/qa_report_card.sh` grades a program's module head at **Door**, and both of its counted readings -- Register and Reach -- are computed over sentences. A fixture scan's head is part prose and part reference table, and this tree writes those tables without terminal punctuation: `#   store_wired      whether store_large still consults that wall`. A block of 28 such lines carries no sentence end, so it merges into whatever sentence abuts it, and the card weighs a 550-word head as if it held a handful of enormous sentences. *What caught it:* a molt frame pushed by this lap's own QA-on-touch reading. `pond_spool_cloth_glow_tend_scan.sh` read **C 72**, so its head was cut from 98 lines to 64 and from 1,019 words to 550 -- and the grade went the WRONG WAY, 15 to 16, with the composite barely moving to 74. Length was not what the reading measured. One single-variable experiment settled it: add a terminal period to each of the 28 reference-table lines, change nothing else, and the same file reads **Register 48 -> 74** (sentences counted 34 -> 46, so the negative share fell), **Reach 30 -> 90** (grade 16 -> 10 against Door's 9), and **composite 74 -> A 91**. Proven both directions on one file, since the without-periods reading is the file this lap started from. *What it taught:* **a readability formula fed a table reports the punctuation of the table, and 17 composite points is more than any sentence in the file is worth.** Kin to `%392` in the same instrument one layer in -- that row found a program graded at whichever setting a hand typed, and its repair landed the split dial this reading runs under; this finds the Door half's own arithmetic dominated by a genre the formula was never meant to read. The instances are wide rather than local: `tools/fixtures/t/tablecloth_glow_tend_scan.sh` measures 102 head lines, 1,059 words, grade 15, **C 73** -- the same shape, in a sibling lane, and every fixture scan in this tree writes its key list the same way. *Repaired here (`20260831.074836`):* this lap's own file carries the periods, and it reads A 91 with its scan still agreeing on metal. A list whose entries end in periods is also a list a reader parses, so the repair is honest writing rather than only a number. *Named, not taken:* a card that recognises a reference block -- a run of lines whose first token is a bare identifier followed by two or more spaces -- and reports it beside the prose rather than inside it, the way it already reports declaration docs as a third count. Shared machinery every body runs, so it wants its own lap and a control proving a table and a paragraph read apart. The sibling scan's C is named for its owner rather than repaired across a lane boundary. **BOOKED.**
