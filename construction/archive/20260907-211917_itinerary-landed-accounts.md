# ITINERARY landed accounts -- folded `20260907.211917`

**Folded:** `20260907.211917` -- **Status:** Archived, complete, never edited
**Voice:** Kyri -- **Room:** checkable
**Why:** the living card stood 93 bytes under its ceiling, so the elder COPAL account folds here
and this lap's own account takes its seat above. Eight ships share one 40,960-byte page, and the
account that will not fit is the one whose reader can follow a link.

---

## COPAL -- a guard's blind spot is a reading now

**COPAL -- A GUARD'S BLIND SPOT IS A READING NOW, AND THE LAP THAT WROTE IT WAS PARKED TWICE.**
Elder and that lap's detail [shelved](20260907-174719_itinerary-landed-accounts.md); row
`20260907.174719` **CLOSED**, [folded](REDS-a-hand-counted-population-rows-588.md) at its fifth
number. `prose_register_scan.sh`'s header carried its own blind spot as prose -- *80 doors, 30
over* -- counted once at a prompt that left nothing behind. **Counted every run now:** 119 doors,
**80 readable, 28 unrostered over**, one line each; **reported, never gated**, control **13 to 16**.
Hand 30 against counted 28 is two frozen plants no lane may sweep: **category error**, not drift.
**PARKED AT `183659` AND `191957`** (`%499`): recovery read the **tree**, so the exec bit rode;
peer drift **zero**; the bill was the ledger.

**Still open from that lap, and carried onto the card above:** `%499` -- rebase, not park?
Standing: arithmetic in the bounds reader; a `band` word; `ios_app_shell`, LOCA.

---

## COPAL -- the duplicate that was never a judgment (`20260907.211709`)

**The detail, folded here so the card can hold the finding and the question.**

`tools/fixtures/i/index_shelf_repair.sh` landed `20260907` morning to end `%440`'s twelve hand
repairs of a misordered day shelf. It exited `verdict=duplicate_stamps` whenever the scan reported
`rows_duplicate` nonzero -- **before inspecting a single row** -- on the reasoning that "which of
two rows to lift is a hand's judgment." My cold open that evening carried the thirteenth firing:
`session-logs/date/README-index-20260907.md` with **three duplicate stamps and two misordered
rows**, so the tool refused and a hand sorted 110 rows again.

**The measurement that split the class.** Over the git history of this room's three newest shelves,
**20 revisions carried duplicate stamps: 38 were byte-identical rows and 4 were not.** All four
divergent ones were a single shape -- a REDS number the derived spine renumbered under the row,
`%505` against `%507` for one log. So the judgment the tool refused over is real for 4 in 42, and
absent for the other 38: an identical duplicate is one log's row re-applied by a rebase, and
lifting either copy leaves the same page.

**What changed.** The tool reads the stamp by offset -- `substr($0,4,15)`, exact because the shape
check above it has already proven the column -- counts `rows_duplicate_identical` apart from
`rows_duplicate_divergent`, lifts the identical class, and refuses the divergent one **while
printing both rows**, since a hand asked to choose has to see what it is choosing between.

**The postcondition stayed one gate.** With no copies to lift the repair is a permutation and is
proven as one; with copies lifted it is a permutation of the deduplicated set, so the same single
`cmp` keys both sides by `sort -u` instead of `sort`. Two gates would have cost the control its
proof-from-both-sides, since the blind copy could only disable one of them.

**Proven on the firing that actually happened.** Replayed against the shelf as it stood before the
hand repair, the tool answers `rows_duplicate_identical=3 rows_duplicate_divergent=0
rows_misordered=2 repair=deduped_and_sorted` and writes a page **byte-identical** to the hand
repair. Control **24 -> 39** behaviors, `index_row_bound` GREEN.

**And the scan's own advice line.** Its duplicate branch fires ahead of the misordered one, and
named no remedy -- so a shelf carrying both faults printed a number with the command sitting one
branch below. That is the `%528` lesson, unlearned one branch over. Both branches advise now.
