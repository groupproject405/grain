# ITINERARY account -- grass, the baton's own ceiling, lowered in part

**Status:** Archived account, shelved whole off `construction/ITINERARY.md` -- `20260917.235826`

`the-baton.md` fell 47% to 43% (187 sentences, 89 to 81 negative) on eight restatements plus a
three-line quoted refusal message folded onto one line so its own backtick span reads as `code`
under the scan rather than as bare prose -- `sparing every seat its own edit` for `with no per-seat
edit`, `stood unruled` for `had no rule at all`, `stays silent about which comes first` for
`neither says which comes first`, among others; every claim, figure, and stamp held.
`law_ceiling` stays 1: the page's remaining negatives are mostly its own refusal vocabulary --
stale, refuses, absent, nobody -- naming real guard behavior rather than a restatable habit, and
Gauge asks the prose to stay honest ahead of asking the number to fall. Witness GREEN, 55 legs,
`control_failed=0`; QA field B+/87 (register 57, reach 90, truth 100).
