# REDS -- row %583, folded from the living pin

**Folded:** `20260907.203412` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A roster line guarded by its own subject's existence cannot fail, and the failure it cannot report
is the one the roster was rebuilt to prevent. Three front doors stood behind `[ -f "$f" ] && echo`,
so a page that moved simply stopped printing and never became a phantom.*

The row stands here exactly as it was written. A booked row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the instrument the round
built, and the row itself stays one click away.

---


**REDS %583 (`20260907.165636`) -- three front doors stood on the meter's roster behind an existence test, the one spelling that can never red when the page moves.** *What went wrong:* `tools/fixtures/l/living_docs_lint_roster.sh` decides which pages the docs meter reads. Its discovery rule asks git for each directory holding a tracked `.rye` source and takes the `README.md` beside it; three doors sit outside that rule because their sources live one level down -- `rye/`, `rishi/` and `aurora/README.md`. `%187` held them by hand for the right reason, quoting `%170`: *a page dropping off a meter is a page whose pass nobody witnessed.* It then wrote them `for f in ...; do [ -f "$f" ] && echo "$f"; done`, which is that drop-off spelled out. The guard against a page that moves is `phantom` in `tools/fixtures/l/living_docs_roster_scan.sh`, and `phantom` reads the roster's **output**, so a line that declines to print when its page is gone can never become one. `unrostered` cannot see these three either. **Both gates blind, by construction.** The other eight hand-named paths, four hammocks and four pins, are named plainly and would red, so one file carried two policies for one class. *What caught it:* the aether rota row, lap 4195 -- the row that listens for what a page says underneath its words. Measured in a pen rather than argued, a roster emitting one real door and holding one absent path behind the test prints `front_doors=1 rostered=1 unrostered=0 phantom=0 verdict=ok`. *What it taught:* **a roster line guarded by its own subject's existence cannot fail**, and the failure it cannot report is the one the roster was rebuilt to prevent. Kin to `20260907.153705` one artifact over, where a scan fed nothing printed a swept tree's bytes. *Repaired:* the three are named unconditionally, like the hammocks and pins beside them. Roster unchanged at **60 paths**, `phantom=0`, `unrostered=0`; a move from here reds on the lap it lands. *Proven both ways:* `living_docs_roster_control.sh` grows **16 behaviors to 18** -- case 17 plants the departing spelling and watches a missing hand-held door pass free at `phantom=0`, case 18 plants the same page named plainly and watches it bite at `phantom=1`. Case 17 carries the load: a repair proven only in the passing direction cannot be told from a rewording. **BOOKED.**
