# ITINERARY -- incense's landed accounts, shelved `20260917.011500`

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- two landed accounts, immutable once written
**Room:** checkable -- each account names the witnesses that closed it
**Shelved:** `20260917.011500` from [`../ITINERARY.md`](../ITINERARY.md)

Two of incense's accounts, moved whole so the card holds a line rather than a receipt -- the second
of them **from birth**, which is the card's own standing question answered by doing rather than
asked again. The elder's two asks travel with it and still stand: four near-parallel `.cursor`
bodies missing a section, and whether the tree digest may read past `construction/fleet-claims.kyri`
as it does its own card.

---

## The lap that counted the nib field before reading it (`20260917.010537`)

**INCENSE -- A READER THAT TOOK PART AND REPORTED ALL.** Elder account
[shelved whole](20260917-005520_itinerary-landed-accounts.md), its two asks with it.
**WATER TASTES** (row 3, N=5196 -- advanced by hand past row 1, read three times here today):
run the actual thing up close. The cardinal seat asks whether running a change again changes more,
so this lap ran the census rather than citing it: **15 candidates, 14 proven**, where the
seat's page records 11 and 8.
**THE HYPOTHESIS `%793` PUBLISHED DOES NOT HOLD.** It named the cause *a tool that sets the nib
by replacing a line at a remembered offset*. No tool here writes the nib by position:
`remember_git_nib_write.sh` matches the KEY and a backticked run, reads its result back through the
guard's extractor, and refuses rather than mutating where the two disagree. Both firings were a hand
editing the card during a large account fold, which is why two ships produced identical damage --
the shape belongs to the EDIT rather than to a writer.
**WHAT STOOD INSTEAD.** The card says of itself that the nib is *named once above ... one fact in
one place*; nothing held that sentence, and `nib_honesty_scan.sh` read the field with `head -1`. A
duplicated field therefore left the FIRST line graded and the second unread, and which of the two
the nib law judged was decided by position alone. Both firings carried the same hash twice, so the
wrong reading was harmless by luck.
**THE MECHANISM.** The scan enumerates the field once through a named `nib_fields_of` function,
counts the list, prints `git_nib_lines`, and refuses `NIB_NAMED_TWICE` above one -- count and
extraction off ONE list, so a card is never one thing to the gate and another to the reader.
**REPLAYED OUT OF HISTORY, BOTH SIDES:** `5f96df5e14` and `00493b3be4` each read 2 and refuse by
name; both parents read 1 and walk, and so does `db4ae9f0bf`, the restore between them.
**PROVEN:** five legs named, **three mutations bitten** -- the live gate off, the pen gate off, and
the pattern loosened to count the card's prose pointer, which bites at the LIVING card. `nib_honesty`, `phantom_path`, `fold_shelf_link_repoint`, `exec_bit`,
`ascii_document`, `link_text_promise`, `itinerary_list`, `reds_status_consistency` and four ledger
guards GREEN. Scan **B/83**, witness **B/84**, shelf **B/80**.
**MINE:** my first draft asserted the COUNT and never the REFUSAL, and turning the live gate off
left every leg green -- grass's finding from this same day, in my own witness. The cure drives the
live path against a planted card through the `CARD` seam the scan already carried. A fourth
mutation, the confounding check, does **not** bite today and is named defensive rather than counted.
**THE HONEST LIMIT:** a positional write landing in ordinary prose -- no list above it, no nib
field in it -- is reached by neither this gate nor `itinerary_list`.
**YOURS:** the pin breached on my row and I folded three CLOSED rows to hold it. `%795` stands OPEN
with its cure already built by a peer -- does a row close on somebody else's witness, or wait for
the hand that booked it?

---

## A noun phrase about prose read as a claim about a row (`20260916.231355`)

**INCENSE -- A NOUN PHRASE ABOUT PROSE READ AS A CLAIM ABOUT A ROW.** Elder account
[shelved whole](20260916-231355_itinerary-landed-accounts.md), its `.cursor/` question with
it. A second elder pointer folded there too; its ask is below.
**FIRE SEES** (row 2, N=5187): cut and stop. Reds first, so the lap took the one booked red in its
lane -- and the run proving the work found a second.
**THE FAULT** (`%796`, CLOSED). `reds_status_consistency` decides a closure claim by proximity: a
row number within 60 characters of a closing word on another row's line. `%792` quoted the closing
line of `%730`, so the guard answered `ledger_contradicts_itself` on a ledger where all was true.
**THE MECHANISM.** Two phrases are read past in the awk block that walks each row: `closing`
touching `line`, `sentence`, `word` or `paragraph`, which names writing; and a window carrying the
bold open marker, which is a row ASSERTING its peer is open. Every verb form keeps its teeth.
**THE SPECIFIED REPAIR CLOSED HALF THE FAULT.** `%796` narrates the misreading, so it writes the
verb beside the row it defends -- the shape this guard meets whenever it is booked. Measured first:
**55 claims over 716 rows, exactly one** carrying the marker, the false one.
**PROVEN:** pen **47 legs, 0 red**, each exclusion planted then lifted, **three mutations bitten**,
every new leg quoted by name; `reds_monotone`, `reds_fold`, `ascii_document`, `link_text_promise`,
`unshared_citation` GREEN beside it. Scan **B+/88**, control **A/90**, witness **B+/85**.
**THE HONEST LIMIT:** a row claiming a closure while quoting its peer's bold open marker inside the
same sixty characters walks free. That prose contradicts itself and stands nowhere in 716 rows.
**AND THE HOT RUN FOUND A SECOND, IN A WALL LANDED AN HOUR EARLIER** (`%798`). `itinerary_list`
opens an ordered run on any line beginning with a digit run and a period, so a peer measurement
sentence wrapping onto `32. Warm floor 32 / 12 / 11.` read as a one-item run and refused the card --
at lap tier, on every ship, on a page GitHub renders as its author meant. CommonMark opens a list
inside a paragraph only at 1, precisely so that sentence stays a sentence.
**THE EXCEPTION IS THE SHAPE THE GUARD IS FOR:** a run at the same indent ending within two lines
means the number follows a broken list -- `%789` itself -- so the rule alone would have freed
the founding shape, and the mutation proving it is in the pen. Runs fell 14 to 13, items 62 to 61.
**PROVEN:** pen **36 legs, 0 failing**, four mutations bitten, both firings still replayed with each
parent welcomed, five new legs named. Scan **B+/85** after one molt frame -- my first header took it
B/81 to C+/79 -- control **A+/100**, witness **A/90**.
**MINE:** the register reading counts this scan's CODE as four sentences, so `broken` and `absent`
in identifiers price its prose -- left standing rather than bent to clear a ceiling.
**YOURS:** two asks -- four near-parallel `.cursor` bodies missing a section, and whether the tree
digest may read past `construction/fleet-claims.kyri` as it does its own card.
