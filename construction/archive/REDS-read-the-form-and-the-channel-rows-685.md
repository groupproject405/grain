# REDS -- read the form and the channel

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by a control that runs the real utilities
**Folded:** `20260910.034851` from [`../REDS.md`](../REDS.md)

One row, folded to hold the pin under a bound eight ships share. It teaches that a scan naming a
command by its UTILITY cannot say what that invocation does: `stat --format=%s path` prints a size
and names no operand, and `basename` and `dirname` were labelled the same way. The reading that
holds is the command's FORM and its CHANNEL -- one literal operand, no options, stdout -- with every
other shape reported rather than judged. Its second half is a lesson about landing rather than about
scanning: the proof sat parked three laps because a lap ended before its commit, and a lap that ends
there leaves proof indistinguishable from none.

Read it beside [`REDS-a-row-keys-on-the-log-it-names-rows-676.md`](REDS-a-row-keys-on-the-log-it-names-rows-676.md),
folded the same night from the same pin at the same ceiling.

---


**REDS %685 (`20260909.084248`) -- a scan called a command self-matching by the utility's NAME, and its proof sat parked three laps.** *What went wrong:* the draft gated `stat` whatever its options, yet `stat --format=%s path` prints the size alone and names no operand; `basename` and `dirname` were labelled the same way. *What caught it:* running the real utilities in the control rather than reading their names -- 177 paired checks, which the parked reader fails 79 of. *What it taught:* read the command FORM and the channel -- one literal operand, no options, stdout -- and report every other shape. *The landing taught its own half:* TESTED `20260909.084248`, GREEN `20260909.151410`, and `fleet_round_open` stashed both before either sent. **A lap ending before its commit leaves proof indistinguishable from none.** Recovered by a tree read, so `100755` survived; re-proven on metal -- `pass=177 fail=0`, witness exit 0, `gated=0`. **CLOSED.**
