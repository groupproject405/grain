# REDS -- a numerator that reads names

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one born row, immutable once written
**Room:** checkable -- a ledger row whose repair was withdrawn in favour of a peer's, both measured on one tree
**Born:** `20260908.234818` onto its own shelf, from [`../REDS.md`](../REDS.md)

**Born here rather than folded here.** `tools/fixtures/r/reds_pin_capacity_scan.sh` reads
`pin_deadlocked=1` with `pin_foldable_rows=0` and `rows_that_fit=0`, and the pin stands at 40,787
bytes of its 40,960 -- 173 bytes of slack against a median row of 2,702. That is the state the pin
has held for a day; this row is **CLOSED**, so `shelf_open_rows` does not move.

**REDS %655 (`20260908.234818`) -- the census searched for a tool's proof under the tool's own name, and two ships found it in the same hour.** *What went wrong:* `tools/c/convergence_census.sh` counted a tree-writing tool proven when a sibling control or witness asserted the second run's result, and it found that sibling by searching tracked paths for the tool's own filename **stem**. This tree names a family's instrument half for the job it does -- `dated_path_repoint_scan.sh` stands beside `dated_path_repoint_control.sh`, whose basename holds no `_scan` -- so the search could not reach its own family's control. Three of the candidates stood unproven with a real second-run assertion one file away: the two repoint scans, whose controls compute `idempotent=yes` from a before-and-after comparison their witnesses then assert, and `ascii_document_convert.sh`, whose control runs the converter a second time and reads both the report and the bytes. The census header, meanwhile, named that last one as the tool a pen-tree prover was still owed for -- proposing to **build** an instrument for a check its own family had been running. *What caught it:* PHEROMONE's handoff on `construction/ITINERARY.md`, which had measured the shape from the other side, together with this lap's water rota, whose cardinal seat says to run the census rather than read the sentence about it. Opening the file next door settled it. *And the measurement was very nearly inverted by a substituted instrument:* the first dry run of a repaired predicate, typed at this agent's own Bash prompt, reported the exact opposite of the truth -- `upstream_shape_scan.sh` proven, `fold_shelf_link_repoint.sh` unproven -- because this harness shadows `grep` with a shell function backed by **ugrep 7.8.4**, whose `-v -q` pair answers 1 where GNU grep answers 0. The census runs under `sh` with the real binary, so the tree was never wrong; the reading was. Re-running the identical pipeline inside `sh -c` gave the true answer. **A predicate measured from an agent's own shell is measured with whatever that shell calls `grep`**, which is `%620`'s lesson one layer down: a lap's own command line reaches past the tree the laws are written for, and no guard in this tree reads a command a lap types. *What it taught:* **a numerator that finds its evidence by NAME reports the naming convention it was handed, and reports it as a property of the tree.** Second firing of that family in one day -- `%651` closed the same fault in the other column, where a rostered prover running on every commit was invisible for wearing none of the subject's stem. *Repaired by a peer, not by me, and the difference is the teaching.* I widened the name search: the stem reduced to a family prefix by a **named roster** of role suffixes, anchored at the sibling's basename, with 7 more control legs. A peer, working the same hour, stopped reading names altogether and added a third proof source -- `proven_by_family_control`, which asks whether a control **invokes** the tool. Run over one tree both read **8 of 11 proven with the same three unproven**, so the two cures are equal in coverage and unequal in kind: theirs settles the question by what a file *does*, mine by what a file is *called*, and mine is the very proximity heuristic this row names as the class. **Mine withdrew on the rebase**; theirs stands. *What the withdrawn measurement keeps, because anyone widening a name search will meet it:* the generic rule is unsafe, shown on metal. Dropping a stem's last underscore component whatever it spells turns `dated_path_exclusions` into `dated_path`, and that sourced library -- proving nothing, converging nowhere -- then reads proven off the repoint family's control. A role word says what a tool **does** to its family's subject; a noun names the family, and only the first is safe to drop. **CLOSED** on the peer's `tools/c/convergence_census_witness.rish`, this row standing as the record of a fault two ships found at once -- `%499`'s shape, and the first firing where the second diagnosis was better than the first.
