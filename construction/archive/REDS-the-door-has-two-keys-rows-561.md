# REDS %561 -- the door has two keys, and the guard read one

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260907.104723`, on the lap that booked
it, to keep the pin under the byte bound its own header declares -- peers' `%558` and `%560` landed on the
same guard in the same hour and its remainder is live, so the row that folds is this one. The
repair is whole: the doorway reads both `**Status:**` and `**Room:**`, matches the token as a word
rather than a substring, and stands GREEN at 37 of a ceiling re-seated there. What is carried is a
question for Keaton rather than work, and it stands on the operator card. Link depth moved with the
text.*


**REDS %561 (`20260907.094712`) -- a page names its room at the door, and the guard read one of the two keys the door has.** *What went wrong:* `tools/fixtures/t/two_rooms_doorway_scan_one.sh` read the room token out of `**Status:**` alone. Nine pages under the roster answer at the door under a key literally named `**Room:**` -- and **two of them spell `Mixed`**, `context/TWO_ROOMS.md`'s own gloss token, in the law's own word, and were counted as pages naming no room at all. Two faults sat beside it: the token matched as a **substring**, so three post-seating pages passed on the letters inside `provisioned` and `divisional`; and a token can be **denied and still match**, which is how `nothing here is checkable until a witness binds it` read as naming the checkable room. *What caught it:* the Earth rota lap (`4164 mod 5 = 4`), whose fixed seat is `context/TWO_ROOMS.md`; the ratchet stood RED at **48 against a ceiling of 44**, and reading its FAIL lines whole showed nine answering under the other key. *What it taught:* **two laws in this tree use the word *room*, and the guard for one of them could not hear the other's spelling.** `TWO_ROOMS.md` names a **register**; `.claude/rules/design-rooms.md` names a **directory**. `**Room:** Design essay` answers the second honestly and `**Room:** Mixed` answers the first; the guard told neither from silence. The sharper half is the arithmetic: **the census was repaired by hand twice in two days and regrew both times** -- 44 at `20260907.001500`, 48 nine hours later, all four new pages written by hands that had read the law, two citing `TWO_ROOMS.md` inside the very Status line that named no room. A guard at `tier cadence` counts damage a day after it is done; **the habit is set at write time, so that is where the clause belongs.** *Repaired (`20260907.101905`):* the scan reads both keys and requires the token to be a **word** -- `visionary` kept, since `**Status:** Visionary room` is an honest naming two pages write; eleven pages gained the token their own next sentence already asserted, no claim changed on any of them; the reading fell **48 -> 37** and the ceiling was re-seated there. `tools/f/fleet_baton.txt` carries a **DOOR** clause so a page arrives naming its room, and `.claude/rules/the-baton.md` and `TWO_ROOMS.md` record why. Four control legs, three flipping to `no` under the single-key reading; the witness GREEN at 18. *Not taken:* a negation heuristic -- `not vision -- checkable` is honest prose, and a rule refusing it would refuse more truth than it caught. Presence is what a grep reads; assertion stays a reader's. **BOOKED.**
