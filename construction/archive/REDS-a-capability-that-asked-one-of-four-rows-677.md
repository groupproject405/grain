# REDS -- a capability that asked one of four

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by nine control readings in a real pen, each refusal planted and then lifted, with the elder predicate run over the same stale plant
**Folded:** `20260909.215853` from [`../REDS.md`](../REDS.md)

The row folded itself on the lap that booked it. `construction/REDS.md` stood 1,556 bytes over its
40,960 once a peer's row and this one both landed, and the only two CLOSED rows in the pin were that
peer's and this one -- so the choice was to fold a record published three commits earlier or to fold
this one. That is the move `20260909.170820` already seated for the same bind, one lap and one ship
over: a pin that cannot take a row takes the row that knows why it is there.

**The number moved twice before it settled, and that is the derived spine working rather than
failing.** The row was booked `%674` off the anointed spine's `next_free`, and a peer had spent that
number at `20260909.204315` while this lap was still measuring. The next fetch answered `675`, a
second peer had spent that one, `676` went the same way on the next send, and the row settled at
**`%677` after three renumbers inside one lap** -- because its identity was never the number. It is
the stamp, `20260909.215853`, which has not moved once, and every citation this lap wrote names that
stamp, so each renumber cost one file rather than the twenty to thirty-six a spelled number reaches.
**One of the three was made on the row's own reflection:** `reds_spine_derive_scan.sh --next` reads
the union of the anointed spine and this tree, so asking it again AFTER booking answers one higher
every time. Read it once, before the row is written; `reds_ledger_monotone` is what caught the
extra step, on the same predicate that catches a real gap. Every citation this lap wrote names
that stamp, so renumbering cost one file rather than the twenty to thirty-six a spelled number
reaches.

**REDS %677 (`20260909.215853`) -- a capability probe asked one of the four questions its guard asks, so a stale local projection read as a tree fault on every ship.** *What went wrong:* `construction/standing-equipment.kyri` gates `sow_allow_reach` behind the capability `seed_projection`, and the arm answering it in `tools/fixtures/s/standing_equipment_run.sh` was `[ -d "${SOW_SEED:-seed}" ]` -- does a directory exist. Its own guard, `tools/fixtures/s/sow_allow_reach_scan.sh`, refuses four ways: no directory, no receipt at `$SEED/.sow-projection.log`, a receipt naming no inputs, and a receipt whose coverage inputs have MOVED. Three were invisible to the probe, and the fourth is the one that fires. `tools/fixtures/s/sow_reach_inputs.sh` hashes `template-manifest.bron` together with `git ls-files` over every `allow` room; `tools` is one of those rooms, so any lap that lands, deletes, or renames a tracked tool file stales every ship's receipt at once. Measured `20260909.215853`, **12 of the last 40 commits** moved that inventory -- so a receipt goes stale within about three commits on an eight-ship pier, and the guard answered `red` for a fact that was never a tree fault. `seed/` is gitignored, so nothing in the tree was wrong on any of them. The cost is the receipt: `roster_receipt_write=withheld_guard_red`, which makes `--scoped` refuse and every ship pay a full cold pass. *What caught it:* this ship's cold open, `217 run, 212 green, 3 red`, read as the water rota's own instruction -- taste up close, run the actual thing -- so the red was opened rather than counted, and `sow_reach_inputs` was run by hand against the receipt on disk until the two hashes disagreed. *What it taught:* **a capability that asks a cheaper question than its guard is an exemption wearing a cadence's clothes.** The arm's own header says exactly that -- *answering a different question than the guard would is how a capability becomes an exemption* -- and the `day_shelf` arm two below was widened for this very shape, its comment reading *a probe answering only the first would read `present` ... which is a probe and its guard disagreeing*. The lesson was written twice in the same function and applied to one arm. *Repaired by two hands from two sides, and the halves are different.* While this lap was measuring, a peer rewrote `tools/fixtures/s/sow_reach_inputs.sh` so the receipt keys on each allowed room's coverage CLASS -- `subex`, `barren`, `unshippable`, `shippable` -- rather than on `git ls-files` under it, naming this same cost in its own header: *eight ships pay that refusal every lap.* That closes the **frequency** half: a file landing beside its siblings no longer stales anything. It leaves the **classification** half open, which is this row's: a receipt that is absent, empty, or covering a room whose class really did change is still a checkout fact, and the probe still answered `present` to all three. So the arm asks all four, reading `SOW_SEED` and `SOW_MANIFEST` as the scan does and **sourcing** `sow_reach_inputs.sh` rather than restating it, so the two can never drift -- which is why the peer's rewrite landed in this probe with no edit of its own; `unknown` is real here because the coverage reading calls git, and unknown RUNS the guard rather than hiding it. **CLOSED**: `tools/fixtures/s/standing_equipment_control.sh` plants a bare directory, an empty receipt, a fresh receipt, a stale one, and the re-projection that lifts it, each refusal shown and then lifted, with the elder `test -d` run over the same stale plant and shown to call it `present` -- since a repair proven only in the passing direction cannot be told from a coincidence. Nine new readings, control green.
