# REDS shelf -- rows %321, %322, %323, %324, %325

**Language:** EN
**Folded:** `20260828.180927`, when the pin read 26,717 bytes against its declared 24,576
**Voice:** Kyri
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)

Five closed rows from one contended afternoon on one shared checkout. %321 recovered a lap from
the dead-letter box; %322 caught the nib amend racing a rebase that moved the parent it names;
%323 caught a failed restore leaving a zero-byte log behind a redirect that truncates before its
producer runs; %324 caught Mind's jail resolving bare sh to the denied macOS selector and the
pin wall wearing that crash as an over-bound verdict; %325 caught the bash-3.2 substitution
parser reading a python heredoc's parentheses as shell. Three of the five are one class wearing
three coats -- one tree, two shells -- and the repairs share one move: pin the interpreter, or
remove the double parse.


**REDS %321 (`20260828.143132`) -- the fleet has a dead-letter box and nothing that reads it, so a finished lap sat in it for fourteen hours under a green roster.** *What went wrong:* a Hush lap closed its work at `20260828.002135` -- `pond/enclosure_policy.rye` at 557 lines, an eight-leg scan, a witness, two fixtures, and its own session log -- and the session died before `git commit`. At `10:39:47` a hand cleared the tree with `git stash push -m "hush-crash leavings"`, which is the sanctioned move: `tools/f/fleet_round_open.sh` does exactly this at every round-open, and its own header names the doctrine -- *"Stashes are the fleet's dead-letter box; a hand or the lap itself re-derives them."* The doctrine is sound and the box has no reader. Nothing tells the next lap that mail is waiting: `git status` printed nothing, `git diff --cached --stat` printed nothing, and a cold `standing_equipment_run.sh` ran its whole roster GREEN over a tree missing all six files. The lap's own witness was one of them, so the guard written to prove the work was exactly as invisible as the work. Twenty commits and fourteen hours passed. *What caught it:* a survey of the lane found `pond/bin/enclosure-policy`, a 12 MB binary dated that morning, with no source anywhere -- `git ls-files 'pond/apps/enclosure*'` read empty. `pond/bin/` is gitignored, so the build output was the only trace the crash left in the working tree, and it was a trace only because an ignored directory is one nothing cleans. `git log --all --diff-filter=AD --name-only -- 'pond/*enclosure*'` then named the stash commit. *What it taught:* **every meter this tree owns reads the working tree or the index, and a stash is neither.** The roster digests tracked content, `staged_uncommitted` reads the index, `witness_reach` reads rostered paths -- so `refs/stash` sits outside all of them by construction, and the reading that produces is not incomplete, it is confidently wrong: a full roster GREEN over an absence. This is the twin of REDS %223, where a lap ending at `git add` left the tree half-done and **loud**; a lap whose leavings are stashed leaves it whole-looking and **silent**, which is the worse of the two, because silence is what success also looks like. The habit that closes it costs one command and is written into the Standing block here: **a lap opens with `git stash list`**, beside `ListAgents`. *Repaired (`20260828.143132`):* recovered with `git checkout <stash> -- <path>`, which reads the **tree** object and carries mode `100755` onto the scan where `git show <stash>:<path> >` would have landed it `100644` (`.claude/rules/exec-bit.md`, proven on metal `20260827`). All six files land here, witness GREEN on metal. The eight living-page edits in the same stash were re-derived against current HEAD rather than restored, since HEAD had moved twenty commits and three of those pages carried further repairs by other hands. **CLOSED**

**REDS %322 (`20260828.162148`) -- the nib names a parent, and a rebase changes the parent after the nib is written.** *What went wrong:* `tools/r/remember_git_nib_witness.rish` accepts a card nib naming HEAD, HEAD's pre-amend sibling, or **HEAD's parent** -- the third being the shape REDS %315 seated, because a parent is a commit the remote already carries. The round that closed %321 wrote `7071903c1b`, which was its parent when the work commit was made. The anointed remote then moved twice while that round verified, the round rebased twice as the twice-pulled rota asks, and each rebase gave the commit a new parent while the card inside it kept naming the old one. The push landed and the guard read **stale** on a tree that had been green forty minutes earlier. *What caught it:* running `remember_git_nib` and `nib_honesty` again **after** the last rebase rather than trusting the reading taken before it. `nib_honesty` stayed green throughout, which is the pair working as designed: the hash is real and pushed (honest), and it is no longer this round's parent (stale). *What it taught:* **a nib is a claim about a relationship, and a rebase is exactly the operation that changes the relationship while leaving both hashes valid.** %315 closed the case where the hash existed nowhere; this is the case where the hash exists everywhere and points one commit too far back. The rota's own three-strike parking rule anticipates a remote that outruns a round -- fifteen commits landed inside one twenty-five-minute roster on `20260828` -- so on a fleet this fast the nib has to be written at the **last** rebase rather than the first, and re-read after any rebase that follows it. *Repaired (`20260828.162148`):* the card names `8f5e83d6a3`, the parent this commit is made against, and the guard's own instruction is followed rather than worked around -- it refuses a pin-only follow-up, so the repair rides in this work commit beside the row that explains it. **CLOSED**

**REDS %323 (`20260828.175530`) -- a restore that failed left a zero-byte file, and the hand checked existence rather than size.** *What went wrong:* during the sixth dead-letter recovery, `git show 'stash@{0}^3:<log>' > <log>` failed -- the file was not in the stash's untracked parent -- and the shell redirect had already truncated the target to zero bytes before the error printed. The hand then read the error, ran `ls`, saw the file present, and wrote "the log survived on disk." The close commit `41eaa0ade` shipped the session log `20260828-142844` as an empty blob, and it rode three more pushes. *What caught it:* Mind's next lap receipt, whose audit read the day shelf and named "the elder zero-byte 20260828.142844 log" as a tree defect outside its own grant -- a different hand, reading sizes where this one read names. *What it taught:* **a `>` redirect truncates before the command runs, so a failed producer plus a successful redirect is a silent eraser** -- write to a scratch path and move on success, or test `-s` afterward; `ls` proves presence and nothing else. *Repaired (`20260828.171800`, `14b05fe90`):* the log rewritten verbatim from the authoring session with a dated coda naming the truncation. **CLOSED.**

**REDS %324 (`20260828.175530`) -- inside Mind's jail, bare sh is the selector, and the pin wall wore its crash as an over-bound verdict.** *What went wrong:* two faults stacked. The pre-commit hook's RULE THREE invoked its scan as `sh tools/fixtures/pin_bound_touch_scan.sh`; inside Mind's jail, `sh` resolved via PATH to the macOS shell-selector wrapper, which reads `/private/var/select/sh` -- a path the jail denies -- and died four times. And the rule printed its one refusal prose, "stages a living pin that is over the byte bound," on ANY nonzero scan exit, so an environment crash wore a measurement's words: Mind's proven 23-test candidate was refused for an overage that did not exist, `touched_pins=0` when the same staged set was measured outside the jail. *What caught it:* the lap's hooks stderr held four selector denials and no `detail_` line -- a refusal that names no path measured nothing. *What it taught:* **a guard's failure message is a claim, and a guard that cannot tell its own crash from its own refusal converts every environment fault into a false accusation** -- the same first-exit conflation %318 booked, rebuilt into the wall %318 raised. *Repaired (`20260828`, `14b05fe90`):* the hook spells `/bin/sh` for both spawn sites, and RULE THREE speaks the pin prose only when the report carries `detail=RED_touched_pin_over_bound`, refusing an unmeasurable set in its own honest words -- still failing closed. The pin-bound witness reads GREEN over its 36 pen cases and 5 armed-hook cases after the edit. **CLOSED.**

**REDS %325 (`20260828.175530`) -- the substitution parser counted the python's parens, and one tree carried two shells a third time in one day.** *What went wrong:* `tools/fixtures/radiant_h1_fence_scan.sh` wrapped two python heredocs in command substitutions. The bash-3.2 lineage's `$()` parser counts parentheses naively INSIDE a heredoc, so the python's closing `("yes" ... else "no"))` line read as a runtime shell syntax error under the macOS sh selector's bash personality -- while dash parses it correctly and `sh -n` passes under both, so the break was invisible to static check and absent on the benches whose selector picks dash. The fence witness, the instrument suite above it, and the e132 and e134 equinox witnesses all red through it, surfaced only when the season leaf choir sang the moved equinox room. *What caught it:* the choir's two RED leaves, then `sh -x`, which printed the substitution's own parse error mid-trace. *What it taught:* **a heredoc inside a command substitution is parsed twice by two grammars, and only the naive one is running on some benches** -- the third dialect firing of the day, after the awk unit (%320) and the jail's sh selector (%324): the class is one, "one tree, two shells," and each repair is the same move of pinning the interpreter or removing the double parse. *Repaired (`20260828.174900`, `199e773d8`):* both heredocs run outside the substitutions into one mktemp scratch that `cat` reads back; scan, witness, suite, both equinox witnesses, and the full choir -- 33 sung, 0 red -- GREEN on metal. **CLOSED.**
