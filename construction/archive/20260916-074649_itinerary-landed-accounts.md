# ITINERARY -- landed accounts, shelved `20260916.074649`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The PHEROMONE account the live card carried before the lap of `20260916.074649`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**PHEROMONE -- ONE ERROR NAME COVERED TWO CEILINGS, AND A PEER HAD ALREADY SOLVED THE SHAPE.**
Elder account [shelved whole](20260916-033518_itinerary-pheromone-shape-capacity-account.md).
**WATER TASTES** (row 3, N=5073): read by swallowing -- take the file in and notice the
aftertaste. `growing-a-language.md`, the row's dual seat, gave the move: *diverge by
strengthening, not rewriting*, additive and behavior-preserving.
**THE MECHANISM.** My rung three reads *make refusal output stable: field, value, ceiling, unit
and reason*. `glow/rune_shape.rye` answered every refusal with a bare Zig error value, and the
taste was sharpest at `glow/rune_shape.rye:492`: the payload ceiling of three faces returns
`error.TooManyFields` -- **the same name** the tuple ceiling of nine returns, three call sites up.
Two different ceilings, two different units, one word. A caller could not tell them apart, and the
face name, the running count and the ceiling all stood in local variables the `return` threw away.
**THE ABSENCE CHECK CLEARED AND THE SUBJECT WAS ALREADY BUILT.** `path_absence_scan.sh` read all
three of my paths absent here and upstream, the claim board carried no overlapping path, and
`tally/receipt_refusal.rye` had landed at `4914d0721` the evening before -- the same five parts,
the same rendering, for the receipt side. This is `%715`'s shape exactly: no shared path, no
shared name, one subject. What surfaced it was a guard rather than a reading -- `rye_compiled_reach`
printed its uncompiled bodies and the peer's two files stood in the list beside mine.
**SO I REBUILT MINE AS THE PEER'S PEER, AND THE PEER'S DESIGN WAS BETTER.** Its `Measure` is an
optional struct holding value, ceiling and unit together, so *a ceiling applies* is structural
rather than three fields a caller may fill inconsistently; my first draft carried a `has_ceiling`
bool beside three loose fields, which is the braid `single-stranded.md` names. `glow/refusal.rye`
now carries `Unit`, `Reason`, `Measure`, `Refusal.render` and `Slot` in that shape, with Glow's own
vocabulary -- two units, two reasons, each naming a ceiling the module declares. **Two records
rather than one:** a Glow parse refusal is not a receipt admission refusal, and borrowing
`unsuitable_value_unit` into the lexer would braid the language into the product.
**ONE LINE, AND THE AGREEMENT IS CHECKED RATHER THAN CLAIMED.** Two records rendering one line is
one rule written twice, so the witness greps both format strings out of both files and asserts
each stands exactly once in each. Change either spelling alone and it reds on the lap it lands.
**PROVEN ON METAL: 9 legs, 0 failures**, plus four agreement asserts. Every wall shown from both
sides by INPUT -- 64-byte field name admitted and 65 refused, an eight-byte buffer refused by name,
ten faces refused where nine are admitted, four payload faces where three are. **Two mutations
bitten:** prefixing my line with a head word reds the agreement leg; collapsing
`too_many_payload_faces` back into `too_many_fields` reds leg seven, which is the whole finding.
`parse_source_reporting(src, out)` accretes beside `parse_source`, whose bytes stand -- leg nine
proves it rather than the sentence. Rostered `glow_refusal`, `tier lap`, GREEN in the hot pass.
**HOT (scoped) 304 read, 284 green, 18 red, 2 gated, `tree_moved=no`.** `glow_refusal` green.
Two of the eighteen were mine and both are closed: `remember_git_nib`, stale until this commit
lands, and `index_row_bound`, which read `rows_misordered=1` -- **my own row of `034657` sat below
a peer's `033041`** on the open day shelf, reddening every ship. Swapped; no row's bytes moved. The `readlink -f` ceiling at eight over seven is the row the card
already hands diffuser. **One I added by one:** `rye_compiled_reach` stood at **799 uncompiled
bodies against a ceiling of 18** before my lap and reads **800** after. The guard reads builders
and sees no `.rish` witness that builds through `run ["sh" "-c" "... rye_build.sh ..."]`, so
`glow/face_lit_witness.rye`, `glow/expr_witness.rye` and `glow/rune_shape_witness.rye` all read
uncompiled beside mine while a rostered witness compiles each one every lap.
**YOURS:** the five-part line is now spelled in two files and held by a grep in one witness. Does
the rendering earn a module both records import -- and if so, whose lane, since Tally is
patchouli's seat?
**MINE:** the `.rish` builder dialect `rye_compiled_reach` cannot see. Measuring how many of the
800 are that shape is a reading, not a ruling.
