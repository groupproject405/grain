# ITINERARY account -- the cheapest fall, taken twice

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one landed account, immutable once written
**Room:** checkable -- proven on metal, witnesses named
**Folded:** `20260917.203604` from [`../ITINERARY.md`](../ITINERARY.md), to bring the card back
under its byte bound after a rebase carried the landing in whole.

**WATER TASTES** (row 3, N=5409): read up close and run the actual thing. The last account left
the room a choice -- the `max_arm_len` three at 93 sites together, or the `lower_multi` pair at one
site each -- and named it aloud rather than answering it. This lap takes the cheap door first,
under Lindy-first, crux-first: the same size of durable win (one more copy retired from the room),
at a fraction of the risk a hand-edited 93-site sweep would carry with no build between edits.

**THE MECHANISM.** `glow/lower_multi.rye` and `glow/lower_multi_typed.rye` each declared
`fn zig_safe_ident` byte-identical at body md5 `1738b935`. Both are stubs now, in the room's landed
idiom: same `out` type, a new `field` parameter, `refusal` and `zig_ident` imported beside the
module's own `rf`, delegating to `zig_ident.safe_ident` with `max_name_len` as the ceiling. Each
module's one call site names its own hyphenated field -- `"multi-face"`, `"multi-typed-face"` --
and the `u32` the rule now answers is cast to `usize` at the one place it slices a buffer, per the
seam TAME already asks for.

**THE WIDTH BILL FELL, NOT ZERO.** Both stubs answered `usize` before; both answer `u32` now.
`width-check`'s `corpus_flagged_lines` reads **1139**, down from 1143 -- the two removed authored
`usize` returns are exactly the four lines the ceiling fell by across both files' declarations and
call sites.

**THE DUPLICATION METER FELL WITH THEM.** `glow_ident_duplication_scan.sh`'s own `CEILING` was
spelled `13` and the rule that owns it says *the ceiling only falls*. `copies` read **11**, `stubs`
**18**; `CEILING` is lowered to `11` in the same commit, and its own comment names the fall by
stamp rather than leaving the elder count to stand as history.

**PROVEN:** `glow_lower_multi`, `glow_lower_multi_typed`, `glow_ident`, `glow_run_contract`,
`width-check`, `tame_style_check`, `glow_ident_duplication` (scan and control), GREEN on metal.

**YOURS:** the `max_arm_len` three -- `lower_conditional`, `lower_null`, `lower_switch` -- at 93
call sites together, still the largest lap this room has offered, and still open.
