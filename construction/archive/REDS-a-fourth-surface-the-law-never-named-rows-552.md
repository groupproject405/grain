# REDS shelf -- a fourth surface the law never named, row %552

**Language:** EN
**Stamp:** `20260907.075107`
**Status:** Shelf -- immutable once written; `checkable`
**Rows:** `%552` -- born straight onto a shelf, because `construction/REDS.md` stood at 40,333 of
its 40,960 bytes with **all ten of its rows OPEN**, so `reds_fold.sh` had no lawful move. That is
the pin deadlock `reds_pin_capacity` was built to report (REDS `%338`), and writing a row straight
to its own shelf is the escape the guard's own note describes.

One row: the ASCII-first law names three subjects, and this tree's own guards speak in a fourth.

---

**REDS %552 (`20260907.075107`) -- the ASCII-first law names three subjects, and the tree's own guards speak in a fourth that no meter had ever read.** *What went wrong:* two meters hold the ASCII-first law over source, and both refuse to count what a program feeds onward. `tools/as/ascii_comment_witness.rish` states the reason in its own GREEN line: converting a string "changes what a program prints", so the exclusion "makes a sweep safe rather than a rewrite of what programs print". That single line answers **two questions with one rule**. A heredoc body, or a string a parser consumes, is fed ONWARD -- converting it changes behavior, and excluding it is exactly right. A `say` line, or an `assert ... else` message, is prose a guard says TO A PERSON, on their terminal and into `session-output/` -- converting it changes register, and register is the one thing the law governs. The exclusion covers both, so the law's instrument was blind at the point where the law applies most plainly. *What caught it:* the fire rota row read close, against a carried card line saying ASCII-first reads comments and never what a tool prints. The number on the card was 5,511 lines across 1,584 files and was itself a crude reading -- it counted every non-comment line, which sweeps in the grep patterns the tree's own guards must contain in order to find what they guard. *What it taught:* **the population is decidable, and the crude reading and the honest one differ by what a guard is allowed to hold.** Classified by leading keyword across 2,387 tracked `.rish` sources, **6,847 of 6,923** non-comment non-ASCII lines are one of exactly two shapes -- **5,260 `say` and 1,587 `assert ... else`** -- and 76 are anything else. Those carry **11,154 characters in 1,516 files**, against the **505** the comment meter counts and holds in the same files under the same law: **twenty-two times the enforced surface**. The sharper reading is a comparison rather than a ratio -- 11,154 is very nearly the **10,468** the comment surface itself stood at on the day its own meter was built, so this is the same tree, one sweep less of attention. And **10,789 of the 11,154 (96.7%)** are the six forms the rule's own substitution table names and spells, with **365** left as notation a reader chooses a word for. *Repaired:* `tools/fixtures/r/rish_spoken_ascii_scan.sh` counts what a Rishi guard says and prints the sweepable subset apart from the judgment tail, since reporting one total would hide which half is work; `tools/fixtures/r/rish_spoken_ascii_control.sh` proves twenty-four keyed readings on real git history, nineteen affirmative and five refusals; `tools/s/spoken_ascii_witness.rish` holds it as a ratchet that only falls, rostered `tier cadence`. **The load-bearing leg strips `if (!spoken) next` from a copy and reads the same pen**, which is what makes the exclusion of grep patterns a mechanism rather than a coincidence of fixtures. *What is NOT taken, and is not a lap's to take:* the sweep itself. The rule's own text names document, comment, and commit message; a spoken line is none of the three, so extending the law's subject list is Keaton's word rather than a meter's. The ceiling stands at the true reading and falls the day that word comes. **BOOKED.**
