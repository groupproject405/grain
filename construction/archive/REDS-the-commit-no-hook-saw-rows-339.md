# REDS -- row %339, folded from the living pin

**Folded:** `20260829.013539` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Two generated front-door pages shipped stale inside a commit that added a witness, because the
round closed through a cherry-pick and git runs `pre-commit` for two paths out of five.*

Booked and repaired inside one lap, born on its own shelf directly as %336 through %338 were, since
the pin stands five bytes under its bound and every elder row still reads OPEN. The lesson it
carries: a wall armed at `git commit` guards `git commit`, and the send discipline that requires a
rebase walks in through a door that wall does not stand in.

**This row was booked as %337 and renumbered TWICE before it landed**, which is the derived-spine
law doing exactly what it was written for (`.claude/rules/derived-spine.md`). Its stamp
`20260829.013539` is EARLIER than both rows it yielded to -- two minutes and thirty-two seconds
before the `20260829.013811` of %337, and four and a half minutes before the `20260829.031804` of
%338 -- and the earlier stamp is not what decides. Each of those rows reached the anointed spine on
`xy` first, so each number was shared and frozen, while this one stayed local and unshared through
three rebases in one send. **The stamp is the key and the number is a view**, allocated by the
remote rather than by whichever tree looked first. Recorded here rather than smoothed over, since a
law is worth more when a reader can see it bite -- and this one bit twice in ninety minutes, on a
pier where three other bodies were landing work the whole time.

**REDS %339 (`20260829.013539`) -- the commit no hook saw: a wall armed at `git commit` guards two paths out of five, and the twice-pulled send walks the other three.** *What went wrong:* commit `12d675d65e` added `tools/p/pond_enclosure_built_witness.rish`, which is exactly the trigger `tools/hooks/pre-commit` rule one exists for -- a witness arriving moves the witness counts on `README.md` and on `docs-geode/libraries/README.md`, so the hook renders both pages and stages them into that same commit. It rendered neither. The round had been re-derived onto a rewritten upstream and its work replayed by `git cherry-pick`, and **git runs `pre-commit` for `git commit` and `git commit --amend` and for no other path.** Both pages shipped stale in a commit whose own witnesses were green, on a clean tree, with nothing in `git status` to say so. *What caught it:* the next lap's cold roster, two rows red side by side -- `geode_libraries red` and `readme_metrics red` -- in a different body's round, one lap and forty minutes after the commit landed. The drift was one integer each: the `rishi` room read 1,723 witnesses where the tree held 1,724. *What it taught:* **a guard is armed at a verb, and a discipline that reaches for a different verb walks past it.** The tree's own twice-pulled send *requires* a rebase whenever the anointed remote moved (`construction/ITINERARY.md`, the sync rota), so the skipping paths are the ordinary close of a contested round rather than an exotic corner. This is the sibling of %188, %220 and %223 -- *a lap ends at the commit, never at `git add`* -- through the opposite door: there the round stopped short of the commit, here the round made a commit the hook never saw. Measured on real repositories in a throwaway pen `20260829`, and written down because nothing in this tree had recorded it: `git commit` fires pre-commit and post-commit; `git commit --amend` fires both; `git cherry-pick` and `git rebase` fire post-commit alone; `git merge --no-ff` fires neither. *Repaired (`20260829.013539`):* both pages regenerated. `tools/hooks/post-commit` is new and asks the question directly rather than probing for a sequencer directory -- **did my sibling run?** -- reading a token `pre-commit` drops on its success path, so a commit path git adds tomorrow answers correctly without anyone editing a list of four names. When the token is absent it records `derived-pages-owed` in the git directory, never in the work tree, since a rebase refuses to continue over unstaged changes and a hook writing into the tree mid-replay could wedge the very rebase it was helping. Rule one of `pre-commit` gains that marker as a second trigger, so the next ordinary commit pays the debt whether or not it moves a witness of its own; the marker is cleared by `post-commit` when a commit actually LANDS, never by the `pre-commit` that merely had its say, or a refusal further down would forget a debt still owed. Rendering in `post-commit` was weighed and declined: the two generators take 3.1s and 9.4s measured on this bench, and a rebase replaying ten commits would spend two minutes answering a question the next commit answers free. `tools/fixtures/g/generated_page_freshness_control.sh` grows from eight proven behaviors to **fourteen**, every one on real git repositories in a pen -- the debt recorded on a cherry-pick and on a rebase, the debt paid by a commit adding no witness at all, rule one still resting when no debt stands, the debt KEPT across a refusal, and `post-commit` resting with no rishi on disk exactly as `pre-commit` does. The control also caught a behavior nobody asked for and worth keeping: `--no-verify` skips `pre-commit` too, so a commit reaching for the flag this tree forbids leaves a mark. `tools/i/install_hooks.rish` asserts the new hook present and executable before arming, since a hook at `100644` is a hook git skips without a word. **CLOSED** on the class it names. *What it does not reach, said plainly:* `git merge --no-ff` creates a commit and runs neither hook, so a page staled by one goes unseen -- this tree's seated send merges `--ff-only`, which creates no commit and stales nothing, so the gap sits off the path the tree walks. A `post-merge` hook was declined rather than forgotten: it fires on every `git pull` fast-forward too, and would mark a debt no local commit incurred. The rostered guards remain the backstop.
