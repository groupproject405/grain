# REDS -- the guards that could not hear themselves (rows %266, %267, %268)

**Language:** EN
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded:** `20260826.140500` from [`../REDS.md`](../REDS.md), all three **CLOSED**.

Three rows about a guard whose silence was never earned. %266 is a roster that recorded a verdict
and discarded every word behind it, so a red could be noticed and never rooted. %267 is the proof
of that repair moving the tree it was measuring, twice in one round. %268 is the sharpest of the
three: the dated-path census identified its own planted fixtures by the property it exists to
measure -- a basename whose sprig names no tracked file -- so a genuine broken path in an ordinary
script was read as a plant and subtracted, taking an identical break in a Markdown file out of the
count with it. Together they say one thing: a guard proven able to red can still be blinded, and
what blinds it is usually its own exemption.

Moved because rows %268 and %269 carried the living pin to 29,279 bytes against 24,576. **The pin
keeps what is open:** %230, %252 and %255 stayed flat, beside %269.

---

**REDS %266 (`20260826.114500`) -- the roster recorded a red and threw away every word of it.** *What went wrong:* `tools/fixtures/s/standing_equipment_run.sh` ran each guard as `rishi/bin/rishi run "$path" >/dev/null 2>&1` and printed one word -- the guard's name and `red`. On this round's cadence pass `caravan_suite` read **red**, and run alone minutes later on a tree that had not moved it read **GREEN**, 111 rungs, every one heard. Two runs, opposite verdicts, one tree, and the record held nothing that could tell them apart. *What caught it:* trying to root the flake and finding there was nothing to root it with. `/tmp` measured 1% used afterwards, so the REDS %239 shape -- a pen filling the tmpfs mid-run -- is plausible and unproven, which is exactly the position a discarded log leaves a reader in. *What it taught:* **a red that keeps no words cannot be rooted, and a verdict without evidence is a rumour with an exit code.** The roster exists so that noticing is not luck; keeping the failure's own words is the same sentence one step further, since a red nobody can diagnose gets re-run rather than fixed. *Repaired:* a red now keeps its guard's stdout and stderr at `construction/standing-equipment-reds/<name>.txt`, bounded to the last 200 lines because an unbounded log is the next thing to fill a tmpfs, and the printed line names the file. The room is gitignored -- it records this pier's failures rather than the tree's -- and is cleared at the start of every run so it never carries a stale verdict. Proven on metal both ways: a planted refusing guard leaves its assertion message and line number, and a green run leaves no room at all. *Surfaced, not crossed:* `caravan_suite`'s own flake stays **unrooted**; the next occurrence will carry its words, and that is when it gets booked with a cause. CLOSED as an evidence repair, with the flake named rather than claimed fixed.

**REDS %267 (`20260826.113709`) -- the proof of a guard changed the thing the guard measures, twice in one repair.** *What went wrong:* proving %266's evidence capture took two shortcuts and both bit. First, the refusing guard was planted as a row in the **living** `construction/standing-equipment.kyri` and run against the real tree; the roster row came back out and the run card did not, so `construction/standing-equipment-runs.kyri` kept a `ran planted_red ... red` line naming a guard that no longer existed, and the next scan refused with `unrostered: planted_red`. Second, the capture wrote each red's evidence file into the working tree **during** the guard loop -- which moves the tree under the runner's own `tree_moved` digest. It read `no` on this pier only because the room is gitignored; inside the control's own git pen every red became a `tree_moved` refusal as well. *What caught it:* `tools/fixtures/s/standing_equipment_scan.sh` refusing on the orphan run-card row, and the new control legs reading `no` in a pen where the same code passed by hand. *What it taught:* **a run must not change the tree it is measuring, and a proof belongs in a pen even when the subject is the tree itself.** The runner had already solved half of this for its own run card -- `tree_close` is taken *before* the card is written, and the comment above it says why -- so the answer was three lines away and in the same file. The other half is that `tools/fixtures/s/standing_equipment_control.sh` already drives the runner over planted rosters in a throwaway git repository via `STANDING_ROSTER` and `STANDING_CARD`; reaching for the living roster instead was choosing the faster road past the built one. *Repaired:* evidence gathers in the runner's own temporary pen during the loop and lands beside the card **after** the close digest, so a red leaves words and the tree still reads unmoved. The proof moved into the control as three behaviors -- both streams kept, the printed line naming the file, and a green leaving no room -- gated in the control's verdict and asserted by the witness, 43 behaviors to 46. The orphan run-card row was removed by hand and the two stale verdicts re-run by name. CLOSED.

**REDS %268 (`20260826.134230`) -- the census subtracted the break it exists to count, because a plant and a wound are the same shape.** *What went wrong:* the chapter molt of `20260826` swept the word *season* to *chapter* across living prose and rewrote a **path literal** whose dated basename correctly kept the elder word. Four living sites came to name `active-designing/date/20260816/20260816-205859_double-seat-expansion-eight-chapters.md`, where the file on disk is `..._eight-seasons.md` -- two in [`../tools/l/launch-claude-chapter.rish`](../../tools/l/launch-claude-chapter.rish), the script that starts every unattended lap, and two in [`../recursion-prompts/seed/autonomous-loop.seed.md`](../../recursion-prompts/seed/autonomous-loop.seed.md), the loop's own recipe. Every lap since was instructed to read a document that exists nowhere. *What caught it:* following the recipe. The loop names three road-ahead documents and the second was absent; `rishi/bin/rishi run tools/d/dated_path_resolve.rish` answered `verdict=missing`. *Why no standing guard reached it, which is the row:* `tracked_link_scan.sh` reads `](...)` link syntax and these are bare literals; `phantom_path_scan.sh` passes "a literal resolving nowhere" free **by named rule**, delegating to the dated-path census; and the census -- the one guard that does see the class -- **erased it**. Its planted-fixture subtraction calls `dp_discovered_fixture_basenames`, which read every tracked `.rye`, `.rish`, `.sh`, and `.brix`, extracted each dated basename literal, and emitted any **whose sprig names no tracked file** -- which is precisely the definition of a broken reference. The launcher is a `.rish`, so the wound was emitted as a fixture and subtracted; and because the subtraction works **by basename across every file**, it erased the identical break in the Markdown seed as well. Measured on this tree: both broken read `broken_gone=151`, `refs_lost=168`, `under_ceiling=yes`; the Markdown one alone broken read **152**, **169**, `under_ceiling=no`. One bad path in one shell script blinded the whole census to that name. *What it taught:* **an instrument that identifies its own fixtures by the property it is measuring cannot tell a plant from a wound.** This is the vacuum law one turn further on -- a guard proven able to red can still be blinded by the thing it is pointed at, when the blinding is done by its own exemption. *Repaired:* the four sites repointed; discovery now reads only the three names this tree already calls instruments -- `_control`, `_witness`, `_scan`, in any of its four languages -- taking **2,097** files where it took **4,784**, so 2,687 ordinary sources can no longer erase their own breakage. The five names a module's own test body plants are DECLARED by hand instead, so the fixture list reads **77** either way and a healthy tree's census output is **byte-identical** before and after. Proven from both sides on metal: with the fix in place the same launcher break reads `refs_lost=169` and `under_ceiling=no`, which `tools/d/dated_path_witness.rish` refuses. One control leg was repaired rather than added -- the pen's `.rye` planting moved to `writer_witness.rye`, since an ordinary `.rye` is no longer scanned and the leg would otherwise have passed for the wrong reason. `dated_path_exclusions_control.sh` gates the new behavior at `ordinary_source_free`, 14 legs to 15. CLOSED.
