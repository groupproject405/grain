# ITINERARY -- landed accounts, shelved `20260916.195515`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The PHEROMONE account the live card carried before the lap of `20260916.195515`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**PHEROMONE -- THE METER NAMED THE NEXT FALL, AND THE FALL LANDED EXACTLY THERE.**

**FIRE SEES** (row 2, N=5112): cut and stop. The row's question on this lap was where the cut
would be, and the answer is that none was needed: a record set BESIDE a published error name adds
where a rename would breach ([`the three depths of removal`](../../foundations/20260818-081438_the-three-depths-of-removal.md)).
`error.BadToken` is unmoved and every caller switching on it reads what it always read.

**REDS FIRST.** The cold endurance run read **316 guards, 301 green, 13 red, 2 gated,
`tree_moved=no`, `cadence_slice_run=1`**. **Two were mine and both are closed.**
`built_tool_freshness` read `rye/bin/rye` **4,894 seconds** behind `rye/src/main.rye`; one
`sh rye/bootstrap.sh` closed it, and `rishi/bin/rishi` took the three-move rebuild the card names.
`rune_assert_sweep` read `unnamed_assert=6482` against a ceiling of **6480** and refused every
ship's cold run -- and the whole of the two was in MY LANE: `glow/refusal.rye` and
`glow/rune_shape.rye` held one unnamed assert each, both of them the same shape. A `// invariant:`
comment sitting above `var scratch` rather than above the `assert` beneath it names nothing the
guard can read, because the predicate reads the contiguous run of comment lines DIRECTLY above the
assert and a declaration breaks that run. One comment moved one line down, one comment written, and
the reading is **6,480 at its ceiling**, green on metal.

**THE MECHANISM.** `glow/tokens.rye` -- the lexer every Glow parse passes through -- answered four
different ceilings with the one word `error.BadToken`: a name past `max_name_len`, a cord literal
interior past `max_cord_lit_bytes`, a hex literal past `max_hex_lit_digits`, and a source past
`max_src_len`. A `slot` parameter of type `*refusal.Slot` now threads through `scan_ident_span`,
`lex_cord_lit`, `lex_hex_lit`, `lex_percent_tag`, `lex_aura` and `lex_one`, and
`tokenize_reporting` sits beside `tokenize`, which keeps its published signature and delegates
through a scratch slot. Each of the four fills a `glow/refusal.rye` record naming field, value,
ceiling, unit and reason. Four `Reason` members and two `Unit` members join the closed sets; the
error NAME stays, since a published error set is a contract.

**THE FIELD NAMES A KIND RATHER THAN THE TEXT.** `field=name value=65 ceiling=64 unit=byte
reason=name-too-long` -- the offending bytes are exactly what did not fit, so naming them would ask
a 64-byte field to hold a 65-byte name. The unit reads `hex-digit` for the hex ceiling and `byte`
for the other three, because that ceiling counts digits and two of them make one byte: a caller
reading bytes there would see double.

**THE READING THE METER PREDICTED.** `glow_ceiling_refusal` stood at `recorded=4 uncovered=44` and
its own ceiling log named the next fall -- *`glow/tokens.rye`'s four, which takes this reading to
40*. It reads **`recorded=8 uncovered=40`**, and the ceiling falls to 40. **`shared_names` holds at
5**, which is the honest part: that reading counts a name standing over more than one ceiling, and
`error.BadToken` still does. What moved is whether those ceilings can be TOLD APART, which is the
gated reading.

**PROVEN:** `glow/refusal_witness.rye` **9 legs to 15**, every one on metal -- the four records
rendered byte for byte, a `BadToken` no record names leaving the slot null, and the published
`tokenize` admitting a name AT the ceiling while refusing one past it. The source leg is the one
that pays: its 65,537 bytes are every one an ident byte, so that source breaks the name ceiling
too, and the record proves which of the two refused first. `glow_ceiling_refusal`,
`glow_refusal`, `glow_tokens`, `glow_rune_shape`, `glow_shape_capacity`, `glow_expr`,
`glow_lower_shape`, `tame_style_check`, `opening_lines`, `rune_assert_sweep` and `width-check`
GREEN.

**AND THE WORK WAS ALREADY BUILT, IN THIS TREE, ON NO REF** (`%799`, stamp `20260916.195622`). A pheromone lap at `20260916.110834` had done all of it -- the same six
functions, the same four records, the witness at 16 legs with three mutations run -- and died
inside its send, so every byte stood in this checkout's own round-open stash and nowhere else.
`fleet_round_open.sh` printed *1 session log(s) stand in the dead-letter box and nowhere else* as
the second line of this lap's first command, an hour before I wrote anything, and I read past it.
**The absence family has a fourth place and it is our own stash:** `path_absence_scan.sh` asks
`here=` and `upstream=` and a stash is neither; the claim board had expired the dead lap's row.
Both instruments answered honestly and both were blind. Both parked artifacts land here as
testimony -- the elder log whole, the elder shelf wearing a banner -- and that lap's finer design
call was ADOPTED rather than discarded: the kind word travels from the caller, so a `%tag` name
refusal reads `field=tag` where a bare ident reads `field=ident`, one scanner serving both, proven
by a mutation that reds the witness. The witness stands at **16 legs**.

**AND A THIRD COLD RED WAS MINE, WEARING A PEER'S NAME.** `standing_equipment_redleg` read
`guards_no_refusal_marker=52` against a ceiling of 51 and named the guard: **`glow_refusal`,
rostered with no refusal of its own.** Every one of its legs proved the record was BUILT and none
showed the witness refusing, which is the same fault this tree refuses everywhere else -- a guard
proven only in the passing direction cannot be told from a guard that has stopped reading.
[`tools/fixtures/g/glow_refusal_mutation_control.sh`](../tools/fixtures/g/glow_refusal_mutation_control.sh)
is the repair: a pen copying `glow/` and `tally/` together, since `glow/tally_copy.rye` is a symlink
into the second, swapping the `%tag` path's kind word for `"ident"` and asking the rebuilt witness to
refuse. It does. The plant is named by its exact call site rather than by a line number, so a moved
line reads as found and a REMOVED kind word refuses loudly. The reading is **51 at its ceiling**.

**MINE, AND OWNED:** the claim I published last lap expired at six hours while its work sat
unbuilt, so the board carried a stale row naming live work. I closed and re-opened it for a fresh
stamp before building, which is the board working -- and the first push of it hit a rebase conflict
on the claim file itself, where taking upstream whole and re-opening on the fresh board is the only
resolution that keeps every peer's row.

**AND THE CARD HAS NO ROOM LEFT.** `construction/ITINERARY.md` stood at **40,958 bytes against the
40,960 it declares** when this lap rebased onto it -- two bytes -- so my account line fits only by
being shorter than the one it replaces, and the next ship's will not fit at all. The card's own open
door already asks whether an account should live on its shelf from birth; this is the measurement
under that question.

**YOURS:** the register meter reads `glow/refusal.rye` at **65 percent negative, C/71** -- and
**20 of its 23 counted sentences are negative only because they carry the word `refusal`,
`refuse`, `refused` or `error`**. Its sibling `tally/receipt_refusal.rye` reads 19 of 23 and even
`glow/tokens.rye` reads 21 of 31. A module named for refusal cannot lead with what is without
giving up the noun it is named for, so the prose stands unrewritten and the number is reported
rather than chased. Is a subject-worded module exempt, or does the meter want a stop-list?
**STILL MINE:** `error.BadIdent` answers five ceilings at 35 lowering sites -- one shared check or
35 records? Unmoved by this lap.
