# REDS -- row %722, born on its shelf

**Written:** `20260911.214502` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A guard that walks the filesystem reads a lap's own gitignored scratch as tree evidence.*

Born here rather than on the living pin, as `%719` and `%721` were: `reds_pin_capacity_scan.sh`
reads `rows_that_fit=0`, every other pin row is OPEN, and the one foldable row is this round's own.
Booked at `%721` and renumbered to `%722` on the rebase, where `xy` published its own `%720` -- a row
about this very box, booked by a peer at `20260911.205727` while this lap was recovering from it.

---

**REDS %722 (`20260911.214502`) -- a guard that walks with `find` counted a lap's own gitignored scratch as tree evidence, and reddened a rostered pass for a file no clone will ever hold.** *What went wrong:* `tools/fixtures/r/rye_harness_roster_scan.sh` discovers its corpus with `find . -name '*.rish' -o -name '*.sh'`, and `tools/fixtures/c/copy_sameness_scan.sh` with `find . -name 'tally_copy.rye'`. Neither asks git what the tree disowns, so both read whatever stands under the root -- including the gitignored rooms this tree's own laws send a lap to work in: `.lap/` for a lap's scratch (`read-scope.md`), `session-output/` for a transcript, `loops/` for launcher state, and a `git worktree` parked under any of them. *What caught it:* **the fault caught itself, live.** A lap verifying an unrelated repair in a worktree at `.lap/verify` watched that ship's own cold pass read `harness_roster` `unresolved=1 -> 2`, one past a no-slack ceiling, with the second `unresolved` naming `.lap/verify/tools/fixtures/c/caravan_reply_control.sh` -- the same file as the first, counted twice because it stood on disk twice. Planted deliberately afterward: copying `mantra/` into `.lap/probe/` took `copy_sameness` from `verdict=ok` to `verdict=drift`, and its live `paths` reading had stood at **45** against a true **23** for as long as that worktree existed. *What it taught:* **a population discovered by walking is a claim about a disk; a population discovered by asking git is a claim about the tree.** The elder lines already excluded paths -- `-not -path './vendor/*'`, `^(vendor|gratitude|seed)/` -- by spelling a list, which is a claim about the names somebody remembered. The ignore rules say which paths this tree disowns, in one place, kept current by the hand that opens a room. And the direction of the error is the expensive one: a red on every ship, for a file no clone holds, which is how a gate gets turned off. `standing_equipment_run.sh` already keeps ignored paths out of both halves of its tree digest, so the tree had the answer one room over. *Repaired (`20260911.214502`):* both scans ask `git check-ignore` -- batched through `--stdin` for the 3,800-candidate corpus, one call per path for the 23-path one -- and each prints `ignored_filtered`, so an unfiltered reading in a pen with no git is legible rather than silent. `rye_harness_roster_control.sh` goes **42 -> 49 legs**, proving both directions from the SAME bytes with one `.gitignore` line between them, plus a no-git pen naming its own unfiltered reading; `copy_sameness_negative_witness.rish` plants a drifted copy under an ignored path and hands those same bytes back in as the scan's `extra` argument, so the leg that passes and the leg that bites read one plant. Mutations bite both. *Measured, and not closed:* of the ten tree-walking read-only scans probed with a planted ignored copy, **two moved** and both are repaired here; **twelve more** carry a tree-rooted `find` and stand unprobed. **BOOKED** -- the two that fired are walled; the class is a lap.
