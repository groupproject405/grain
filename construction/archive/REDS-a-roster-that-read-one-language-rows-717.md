# REDS -- row %717, folded from the living pin

**Folded:** `20260911.205727` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*The port-roster round -- a census that reads one language reports the language rather than the tree, and a README sentence excusing one column taught every reader past another.*

The row stands here exactly as it was written. A closed row leaves the living pin so the
pin stays the length a reader will actually read; the lesson travels forward in the guard
the round built, and the row itself stays one click away.

---

**REDS %717 (`20260911.185900`) -- the port roster read Rye, and fifteen wire labs declare their ports in Rishi.** *What went wrong:* `tools/fixtures/p/port_band_scan.sh` counts every `const <name>_port: u16 = <number>;` in living Rye and held `ports_outside_band` at zero. The wire labs carrying those same capabilities onto virtio declare their ports in **Rishi**, as the default half of an override -- `if port_request_raw == "" then let port_request = "15561"` -- so **29 declarations across 15 labs** stood outside every reading, twenty-three thousand numbers below the seated band. **Five numbers carried two or three claimants**, and **four override names were each read by two labs** -- exactly the four labs whose numbers collide, so a hand reaching for the documented override moved both at once. Each lab was made by copying a sibling and editing the capability name while the port block travelled unread. *What caught it:* the fire rota, row 2 -- reading a mantra wire runner's device lab to ask whether its port sat under the lock, and finding it bound a port in another language. A peer's paper named the shape in its own *does not reach* section a day earlier (`20260911.074907`) and nothing counted it. *What it taught:* **a roster that reads one language reports the language, never the tree.** `comlink/README.md`'s Port Map said *ports repeat by design ... and both rows are right* about the hosted column, and that sentence taught every reader past the device column's repeats; the map was also stale in four rows. *Standing:* the scan reads both rooms, `device_ports_outside_band`, `device_double_claimed` and `device_override_shared` are **walls at zero**, and four mantra labs moved to 15577-15584 with no other lane's file touched. **CLOSED** on `tools/p/port_band_witness.rish`, 80 behaviors, four mutations biting their own legs.
