# REDS -- the guards that ran in no pass (row %645)

**Shelf.** Folded from `construction/REDS.md` on `20260908.184000`, CLOSED on its own repair.

**Two corrections on the rebase (`20260908.185500`).** This shelf opened carrying `%643` as well; a
peer folded that row to its own shelf after it was pushed, and a row stands on one shelf, so it is
dropped here rather than duplicated. And the row below booked `%644`, which a peer had bound at
`20260908.182735` -- nine minutes earlier. The earlier stamp keeps the number and this unshared row
renumbers to `%645`, which is the derived-spine repair working exactly as written.

**What the two taught together, and why they share a shelf.** Each is a lesson about **reach**
rather than about quality. `%643` found a stop sentinel only its own author could set, so a hand
asking a loop to finish and stop was answered by the loop erasing the request. `%644` found ten
careful, specific witnesses guarding the tree's front doors, rostered nowhere, so one of them stood
red for sixteen days while a 190-green pass ran beside it every twenty minutes.

Neither was a badly built thing. Both were built well and could not be reached -- by a hand in the
first case, by a pass in the second. A guard's reach is the roster; a stop's reach is whoever may
set it.

**REDS %645 (`20260908.183635`) -- the ten guards on the front doors run in no pass, and one stood red for sixteen days.** *What went wrong:* the ten `tools/gen/chapter/sunn*_witness.rish` files assert the shape of `README.md`, `CLAUDE.md`, `SOURCE.md`, `CONTRIBUTING.md`, `STEWARDS.md`, `llms.txt` and `keys/README.md`, and appear **zero times** in `construction/standing-equipment.kyri`. Four were RED, and `sunn8` had been red since `7191a938b` on **2026-08-23**: a link sweep removed the front door's `**Voice:**` line naming Kyri and linking her identity page as collateral, so the tree's README stopped naming its own voice and no pass said so for sixteen days, while a 190-green roster ran beside it every twenty minutes. *What caught it:* a SUNN mark sweep on Keaton's word, which touched those faces and ran their witnesses by hand. *What it taught:* **a guard's reach is the roster, never its own quality** -- these ten are careful, specific, and were completely inert. **CLOSED** on the repair: the voice line is restored with `seed_link` reading `front_door_links_outside_seed=0`, the stale assertions naming a private remote and a personal name retire to the one-page-two-repositories decision that removed them, and all ten are rostered at `tier cadence`.
