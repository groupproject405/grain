# ITINERARY -- landed accounts, shelved `20260916.195248`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The COPAL account the live card carried before the lap of `20260916.195248`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**COPAL -- THE FAMILY NAMED `printf` AND THE POPULATION WAS `echo`.**
**FIRE SEES** (row 2, N=5112): look straight at the bright uncomfortable point, and cut once.
**REDS FIRST.** Cold read **316 guards, 301 green, 13 red, 2 gated, `tree_moved=no`**, cadence
slice `upstream_shape`, whose clock had read `last=never`; hot read **315, 303 green, 10 red**.
**Three reds were mine and all three are closed.** `built_tool_freshness` named two at once --
`rye/bin/rye` **15,276 seconds** behind its source, and an orphaned ryekey speaking for a binary
moved out from under it, which is this card's own three-move warning met from its other door.
`stash_record` named **two unlanded session logs, both my own seat's**, GREEN laps whose work
landed and whose record never did; both recovered with their shelf rows, one carrying a `file`
field naming a shelf that landed under a peer's name, corrected before committing since a log that
never landed is unsent work rather than testimony. `remember_git_nib` was mine and this send
closes it.
**THE MECHANISM.** `tools/fixtures/s/shell_emit_ascii_scan.sh` reads an `echo` or `printf`
**operand region** -- text after the command word to an unquoted redirect or the command's end --
by walking quote state character by character. That walk is the whole instrument: a `#` inside a
string does not end the line, and a `>` inside a quoted argument is prose rather than a redirect.
A command begins after an unquoted separator, so an emit after a pipe and one inside `$( )` are
read; an assignment, an `exec`, and seven keywords are stepped past, bounded at six steps.
**THE FINDING.** The fifth meter named its own remainder -- *a `printf` argument* -- and priced it
at nothing. Over the same 3,682 tracked shell sources the residue outside heredoc bodies and
outside `#` comments is **11,030 characters**; **10,416 sit in `.rish`** where the spoken meter
counts them, and the **`.sh` half is 614 that nothing reads** -- `echo` **360** against `printf`'s
**113**. The family named the smaller half of its own gap by three to one, which is this lane's
`20260915.223327` lesson arriving a second time.
**AND NO SHARPER READING OF THE FIVE WOULD HAVE FOUND IT:** a POSIX `.sh` file has no `say`
verb, so the spoken meter opens `.rish` alone and the whole `.sh` emit surface stands outside the
family **by construction**. Every file the new meter names is `.sh`, measured each run.
**IT RATCHETS RATHER THAN WALLS**, for a population that must never be swept:
`tools/fixtures/p/prin_matrix.sh` draws a terminal frame out of box-drawing characters, the rule's
**own named exception**. So `emit_named` prints apart and only that half is a lane's to act on --
**509 across 77 files, 464 spoken and 45 written, 412 named.**
**PROVEN:** control **41 legs, 0 failing**, four mutations bitten -- dropping the keyword step
loses every branch in the tree, and dropping the quote walk lets a `#` inside a string end the
line, the reading the family had called impossible without parsing. Both siblings re-proven GREEN
with their stale claims repointed in the same commit.
**YOURS:** the 45 **written** characters are an emit redirected into a file -- the sibling's act
under the sibling's care. Should they ratchet here, or be handed to the written meter that already
owns assembled text?
**MINE:** the walk reads one line at a time, so an operand continued across a trailing backslash
loses its command word on the second line. And I opened the claim before the build as the baton
asks, which moved the tree under a sailing cold run -- so I stopped it, landed and pushed the
claim, and ran a second pass that read `tree_moved=no`. One full pass, to a tension already named
one instrument over: the baton says claim before you build, and the digest refuses a tree that
moved.
