# REDS %292 -- three renames and an ASCII sweep moved the tree, and four witnesses were never told

*Folded off the living pin on `20260827.165500` by `tools/fixtures/r/reds_fold.sh`, **CLOSED**. The
pin was carrying five rows from two hands at 25,235 bytes of its 24,576 -- the shed lane's `%292`
and `%293` and the Dream lane's `%294` and `%295`, around the still-open `%291` -- so the oldest
closed row moved to make room. `%289` and `%290` folded in the same round, to
[`REDS-reset-never-merge-rows-289-290.md`](REDS-reset-never-merge-rows-289-290.md).*

**Why it reads well beside its neighbours.** This row and the Dream lane's `%295` were found the
same afternoon, on different hosts, and they are the same shape from two directions: something the
tree renamed or reshaped, and a checkable thing downstream that nobody told. Here it is four
chapter witnesses asserting a directory and a spelling that a rename and an ASCII sweep had moved
underneath them. There it is a launcher printing an invocation for a wrapper that had never heard
of its agent. **A reference sweep repoints paths, and an assertion about the shape of the world is
not a path.**

---


**REDS %292 (`20260827.162143`) -- three renames and an ASCII sweep moved the tree, and four unrostered chapter witnesses were never told.** *What went wrong:* the shed's own pre-cut baseline ran the eight chapter witnesses that depend on the prepped fossils, and **four were already RED before a single file moved**. All four trace to one root: the tree renamed and swept around them, and nothing reached them. `oven_seat_witness.rish` and `oven_season_o0_witness.rish` both assert `test -d oven`, and the forge module became `ember/` on `20260808.220423`. `horizon_stack_thin_witness.rish` asserts the living Realidream cites `20260826-024944_lantern-lattice-oven.md`, and the Kiln molt of `20260827.025117` moved that citation to the Kiln page in the same round it was written. `nona_season_n2_witness.rish` greps `context/LEXICON.md` for `three - quad - quint` spelled with **middots**, and ASCII-first (REDS %83) swept every middot to a hyphen. *What caught it:* running the baseline before the cut rather than after -- the shed's own discipline, which asks whether parity holds before an elder is removed. Nothing else would have: none of the four is on `construction/standing-equipment.kyri`, so no lap ran them, and each had been quietly wrong for between one day and nineteen. *What it taught:* **a rename sweeps what a grep can see, and an unrostered witness is what a grep does not reach** -- these four sit in the 295-of-298 unheard chapter class REDS %219 measured, where a stale claim costs nothing until someone believes it. The sharper half is that three of the four assert a **directory** and a **spelling**, which no reference sweep looks for: repointing paths is a solved habit here, and repointing an assertion about the shape of the world is not. *Repaired (`20260827.162143`):* all four read what stands -- `ember/` lives while `oven/` and `anvil/` are retired, the living Kiln page carries the bake section its Oven elder held, the LEXICON's scope list reads in hyphens -- and the four are GREEN on metal beside the four the shed itself molted. The eight now prove the **shed** rather than the molt: an elder is gone, and the prep pin's record names it. **Surfaced, not crossed:** rostering the chapter witnesses is REDS %219's own open door and stays Keaton's, since 298 witnesses on the every-lap tier would cost every lap what this round spent once. CLOSED.
