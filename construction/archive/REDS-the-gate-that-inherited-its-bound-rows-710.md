# REDS -- row %710, folded from the living pin

**Folded:** `20260911.115500` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*The gate that inherited its bound -- a guard whose own argument was wider than the class it walled, and eleven rooms that stayed unread because a scan's name said `tools/`.*

The row stands here exactly as it was written. A booked row leaves the living pin so the pin
stays the length a reader will actually read; the lesson travels forward in the guard the round
built, and the row itself stays one click away.

---


**REDS %710 (`20260911.093000`) -- the gate bounded itself to the class its own measurement found, and eleven other rooms stayed unread.** *What went wrong:* `tools/fixtures/l/law_tool_citation_scan.sh` walls every `tools/` path a `.claude/rules/*.md` page prints at zero, and its own header states the argument as being about a LAW PAGE: *naming a guard is a promise about a file that already exists*. That is as true of a foundation, a spec, a guide or a design essay, and the room prints **128** such paths into eleven other rooms with nothing reading one. **Two were stale:** the pre-fold flat paths of the Radiant self-critique essay (`20260715-163000`) and the TAME SLC audit ledger (`20260717-181715`), both folded to `active-designing/yonder/date/`, cited across `radiant-style.md`, `radiant-wishes-ending.md`, `tame-guidance.md` and the `.cursor` twin. One line of `tame-guidance.md` carried a **repaired** citation beside a **stale** one -- a repoint pass hit the neighbour and missed this one, which is what an unread class looks like from inside. *What caught it:* the aether rota, row 0 -- listen for the page nobody answered. `tools/t/tracked_link_witness.rish` reads Markdown LINKS, and `tools/d/docs_command_path_witness.rish` passes an absent path free on purpose, so a backticked room path was invisible to both. *What it taught:* **a gate inherits the bound of the measurement that discovered it** -- the class found becomes the scope, and the argument reaches further than the gate. *Repaired:* a second reading, `room_cited_untracked`, walled at zero over every non-`tools/` path the room prints, normalized to the root. Its three read-past classes are **mechanical rather than a typed list** and the scan's own header says why each is honest; two readings rather than one merged number, since `cited_bare` and `cited_runners` are measured against `cited_paths`. **67 control legs, fail=0, five mutations bitten**; the leg COUNT is pinned. **BOOKED.**
