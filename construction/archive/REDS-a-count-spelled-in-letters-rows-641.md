# REDS -- a count spelled in letters

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by 77 planted behaviors on throwaway rooms
**Folded:** `20260908.135445` from [`../REDS.md`](../REDS.md)

**Born here rather than folded here.** The pin stands at `pin_headroom=24` bytes with
`pin_foldable_rows=0` and `pin_deadlocked=1` -- thirteen rows, every one OPEN, so
`tools/fixtures/r/reds_fold.sh` has no lawful move and a new red has nowhere in the pin to land.
That is the same state [`REDS-a-number-measured-before-its-own-commit-rows-638.md`](REDS-a-number-measured-before-its-own-commit-rows-638.md)
recorded three hours earlier, and this row is that one's own correction, so the two belong side by
side. This row is **CLOSED**, so `shelf_open_rows` does not move.

**REDS %641 (`20260908.135445`) -- a count spelled in letters at a room's front door drifted under the very gate built to catch a count spelled in digits.** *What went wrong:* `amphora/README.md` read *Those eight build three modules -- `src/main.rye`, `vessel_core.rye`, `vessel_seal.rye`*. Nine of the sixteen rostered guards build all three today, measured by grepping each rostered guard file for the three paths. The sentence was written into `05c87d3d0` when **twelve** guards stood over the room; `amphora_mark_wreck`, seated hours before this reading on `20260908`, is the ninth builder and made the roster sixteen. *What caught it:* the Earth rota row read as its own sense -- take the concrete fact in at the door before arguing with it -- so this lap read the numbers on its own front door and asked the instrument rather than the sentence. *What it taught:* **`%638` closed with a claim that is false, and it was false in the same room on the same day.** That row seated `readme_spelled_lines`, gating a digit group beside the word `line` or `lines`, and its closing clause let a number spelled as a word walk free *"since a spelled number cannot go stale in silence."* It can, and one was, two paragraphs down the same page, while the new gate watched. A gate's exemption is a claim like any other, and this one was reasoned rather than measured. *Repaired (`20260908.135445`):* `tools/fixtures/am/amphora_roster_scan.sh` grew `readme_spelled_words`, a number word from `one` to `fifty` beside `line`, `module`, `guard` or `witness` -- the four nouns this instrument itself measures -- case folded, a word character after the noun ending the match so `eight guardrails` walks free and a letter before the number ending it so `someone-line` does. **REPORTED, never gated**, and the reason is measured rather than cautious: the same door carries `three more modules` in a sentence that enumerates what it counts, so a wall at zero would refuse honest prose, which is a wall somebody turns off. The one drifted sentence was replaced the same lap, naming `covered`, `singly_covered` and `detail_singly` in place of a number. Control **68 -> 77** behaviors, the reading shown from both sides and proven not to gate; witness GREEN. *What it does not reach, named rather than implied:* one room. Measured across **113** living module front doors outside the closed stacks, **36 pages carry 63 such lines** -- so this is a tree-wide shape and this lap walls one door of it. **CLOSED**

