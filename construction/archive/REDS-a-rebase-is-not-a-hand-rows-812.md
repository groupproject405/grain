# REDS %812 -- a rebase is not a hand

*Folded off the living pin [`../REDS.md`](../REDS.md) on the lap the row was booked, because the
pin held nothing else foldable: twenty-one rows stood **OPEN** and `reds_fold.sh` refuses an open
row by contract, so the ledger could receive this red only by shelving it. Born-on-a-shelf is
recorded practice rather than law -- the recital carries its own account -- and its cost is named
where the practice is: a live red stands where a lap reading the pin will not see it.*

*What the row taught, in one line: the ledger's immutability is a habit held by hands, and a rebase
is not a hand. The instrument that came of it,
[`tools/fixtures/r/reds_row_shrink_scan.sh`](../../tools/fixtures/r/reds_row_shrink_scan.sh), found
on its first run that the class had fired **seven times** over the spine's 636 revisions rather
than once, and that **three** records still stand shorter than the longest text they ever held.*

---


**REDS %812 (`20260917.025658`) -- a peer's ordinary rebase reverted a ledger row's widening, and every guard stayed green.** *What went wrong:* `30edd9303` widened `%803` with a paragraph naming the contested send; three hours later `66f3c17a1`, whose only ledger business was booking `%804`, rewrote that line to the version ITS checkout held, and the widening went with it. This is the one file whose first law is that a row is never edited, edited by a commit that never meant to touch that row. *What caught it:* a lap returning to that row read what stood on disk against what it had written, and the two differed. Nothing else would have -- `reds_ledger_monotone` reads the spine's ORDER, and a row losing a sentence keeps gaps, numbers, stamps and file-count intact. *What it taught:* **the ledger's immutability is a habit held by hands, and a rebase is not a hand.** Eight ships write this file every lap, and a rebase resolving one row's text in favour of the side that booked a DIFFERENT row is ordinary git behaviour, silently lossy across every OPEN row at once. *Booked twice over by its own subject:* this row was written as `%805`, which a peer bound to `20260917.023800` in the same hour, and the same lap's fold of `%802` duplicated a peer's fold of that row eighteen minutes earlier -- both withdrawn here, which is the second and third firings inside one lap. **OPEN** -- the cure is a reading rather than a wall, since two ships lawfully edit this file each lap: a row's text is fixed once written, so a scan can ask whether any row differs from the newest the anointed spine holds and report those that SHRANK. Restored by hand out of `30edd9303`. *Booked (`20260917.071213`):* the cure stands built and the instances stand repaired. `tools/fixtures/r/reds_row_shrink_scan.sh` walks the anointed spine's whole history in one `git log -p` process, keys each record by the head it writes about itself -- the number, the marker word and the stamp together -- and reports a record whose WORD count fell while that head held. **It found the class had fired seven times rather than once**, across 636 revisions: `988a0ceaf8`, a commit whose subject named the ITINERARY nib, shortened `%449` by 150 words and `%450` by 132 on `20260906`, eleven days before this row was written, and no eye returned to those rows. **Three records still stand shorter** than the longest text they ever held -- `%228`, `%449`, and `%803` at exactly the 136 words this row describes. *What the building taught that the row could not:* **words, never bytes.** The ascii-first sweeps rewrote em dashes across this ledger, changing a row's bytes and not one word of its text -- 18 byte-shrink events stand against 7 word-shrink events, and all eleven of the difference are sweeps of 1 to 20 bytes that would have buried the one event that mattered. *And a record's key is the head it writes about itself:* keyed by number alone the reading invented twenty losses, keyed by number and stamp it invented seven, because a closure note wears its row's number AND its row's stamp. Gated NOTHING, by this row's own ruling: an open row accreted to BOOKED lawfully sheds its tail, and a wall that reds on honest work is a wall somebody turns off. `tools/r/reds_row_shrink_witness.rish` over 34 pen legs, four mutations bitten. **BOOKED.**
