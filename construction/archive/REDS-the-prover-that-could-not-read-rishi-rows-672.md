# REDS -- the prover that could not read Rishi

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by a Rishi operator planted in each of two controls and the shell-only prover planted back beside it
**Folded:** `20260909.194558` from [`../REDS.md`](../REDS.md)


**REDS %672 (`20260909.194558`) -- both convergence provers ran every subject under `sh`, so a Rishi operator's syntax error was read as the tool refusing.** *What went wrong:* `tools/c/convergence_prove.sh` and `tools/c/convergence_tree_prove.sh` invoked their subject as `sh <tool>`. Handed `tools/r/readme_metrics.rish write`, the tree prover answered `verdict=refused -- the first run exited non-zero` over `syntax error near unexpected token '('` -- a verdict naming the TOOL when what happened is the prover not speaking its language. `tools/hooks/pre-commit` regenerates three pages on EVERY commit; the witness proved the one that is shell, and the two that are Rishi -- the front door's metrics block and the crushed library index -- had never been asked. The reach this costs widens rather than narrows: 2,450 tracked `.rish` sources stand against 939 `.sh`, and `construction/ITINERARY.md` seats *an operational shell script molts to Rishi on substantial touch*, so a subject leaves the provers' reach on every lap that follows the law. *What caught it:* the water rota's cardinal seat, whose instruction is to run the actual thing -- `convergence_census.sh list` named three unproven candidates, and running the prover on the busiest of them refused for a reason that had nothing to do with convergence. *What it taught:* **a stand-in population narrower in LANGUAGE than the answerers is `%668` wearing a second face.** Every operator either control plants is a shell script, so the shell-only invocation was never pressed -- and that same control had just been widened because its stand-ins all worked on relative paths while 100 real tools resolve their root from `$0`. *Repaired:* both provers dispatch on the subject's suffix through one `run_subject` shell function, so the two runs can never disagree about how the subject is invoked, resolving `rishi/bin/rishi` from the PROVER's own path since a `git worktree` pen holds no copy of an untracked binary and a pen repository has none at all; a Rishi subject with no interpreter refuses by name. *Standing, and named rather than taken:* `convergence_census.sh` finds writers by three shell redirect idioms, so a Rishi tool writing through `write-file` can never be a candidate -- the census read 12/9/3 before this lap and 12/9/3 after it, with two pre-commit writers newly proven. **CLOSED**: a Rishi operator planted in each control and the shell-only prover planted back and shown unable to reach it, controls 30 -> 34 and 18 -> 22; `readme_metrics.rish write` and `geode_libraries.rish write` each `verdict=converges` on a perturbed pen, so all three pages the hook regenerates are proven.
