# REDS -- whose module is this

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by a control on real git repositories
**Folded:** `20260907.222553` from [`../REDS.md`](../REDS.md)

One row, folded the hour it was written to hold the pin under a bound eight ships share. It teaches
that an instrument built to publish one blindness can carry another: a census whose whole subject is
that a flat glob cannot see a subdirectory could not see whose module a room held, because
`git ls-files` lists a symlink exactly like a file. Read it beside
[`REDS-a-duplicate-that-was-never-a-judgment-rows-592.md`](REDS-a-duplicate-that-was-never-a-judgment-rows-592.md),
its own lap's other row, and [`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %593 (`20260907.215529`) -- the census publishing which rooms a flat glob lies about counts a peer's linked module as the room's own, so 26 of 44 rooms read a size they did not author.** *What went wrong:* `tools/fixtures/m/module_room_reach_scan.sh` asks `git ls-files` for a room's tracked Rye, and **`git ls-files` lists a symlink exactly like a regular file**. `amphora/`'s three `.rye` links into `tally/` and `comlink/` made the room read 10 modules where it authored 7. Measured: **228 of 1,954** tracked `.rye` are symlinks and **26 of 44** rooms carry one, each count inflated by a peer's work with nothing to tell them apart. *What caught it:* this lane's roster header, spelling `7 tracked Rye modules of its own and 3,603 lines` -- corrected by hand once on `20260906.063124` from a reading of 10 that counted the symlinks, its line count drifted **273** a day later with no instrument reading either number. *What it taught:* **the instrument's reach did not match the room it was pointed at -- this scan's own founding lesson, arriving from a third side.** It was written because a flat glob cannot see a subdirectory (`%441`), beside `room_bound`'s zero for `tools/` and `%175`'s 237 sprigless logs: depth-blind, pattern-blind, now **provenance-blind**, each coming back clean. *And the tree already held the right answer.* `tools/fixtures/g/geode_libraries_scan.sh` counts with `find ... -type f`, which excludes a symlink, so `docs-geode/libraries/README.md` published `amphora/ 7` and `pond/ 156` all along where this census answered 10 and 222 -- two readings of one question, 228 files apart, never read beside each other. *Repaired:* two more independent patterns, `own` and `linked`, counted from their own lists off `git ls-files -s` mode `120000` rather than derived from each other -- so the second arithmetic gate `own + linked == recursive` can actually refuse, which `own = recursive - linked` never could. Both print per room and in total; the roster header cites the command. Control **6 -> 8 phases**, the link planted then lifted, and a COPY with the `own` pattern broken refuses `verdict=inconsistent` at exit 1. GREEN. *Not taken:* a room's **line** count still has no instrument, which is the figure that drifted. **CLOSED**
