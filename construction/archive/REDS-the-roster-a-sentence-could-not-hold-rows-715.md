# REDS -- row %715, folded from the living pin

**Folded:** `20260911.145641` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*The port round -- a room that serializes its own collisions wrote the roster of colliding numbers into two prose headers, and both went stale the morning a module was repaired.*

The row stands here exactly as it was written. A closed row leaves the living pin so the
pin stays the length a reader will actually read; the lesson travels forward in the guards
the round built, and the row itself stays one click away.

---

**REDS %715 (`20260911.145641`) -- the room that serializes port collisions kept its roster of colliding numbers in two prose headers.** *What went wrong:* `%700` cost a fortnight to name -- a rostered witness read red on one cold pass and green on a re-run over an unchanged tree, because two of this pier's eight checkouts reached one compiled-in UDP port. The repair was right and the ROSTER it depended on lived in sentences. `tools/fixtures/m/mantra_delivery_port_lock.sh` opened *seven modules under mantra/ bind 38478/38479 through 38490/38491* and named *two of the seven*, `snapshot_export_delivery` and `recall_tablecloth_query_delivery`, as binding the SAME pair 38490/38491; `tools/fixtures/a/amphora_vessel_port_lock.sh` opened *amphora/vessel_fetch_delivery.rye binds UDP 38494 and 38495*. Both were true when typed. The tablecloth module took the bind-to-zero handshake at `20260911.105415` and let 38490/38491 go, so the double claim the mantra lock named stood in that file alone; the amphora fetcher let 38494 go, and `linengrow/neth_serial_core_delivery.rye` declares it today. **Measured on the seating lap: 35 declarations across 21 files, and THREE numbers each claimed by two modules** -- 38495 (`amphora/vessel_fetch_delivery.rye` beside `linengrow/neth_serial_core_delivery.rye`), 38496 and 38497 (`granary/resin_serve_delivery.rye` beside `linengrow/seva_broadcast_delivery.rye`), six declarations across four files. Each is lawful for its own module and a collision for the pier, and nothing in the tree read one. *What caught it:* the water rota, row 3 -- taste the flow up close, at the boundary the elder repair sat behind. The lock's own header was read for its roster and the roster was counted instead, which is `.claude/rules/stamp-and-name.md`'s COUNT, NEVER NUMBER applied to a sentence rather than to a mark. *What it taught:* **a repair that depends on a roster inherits that roster's staleness.** The bind-to-zero handshake is a correct cure and it silently falsified the only two places a reader could learn which numbers were shared -- so the better a lane repairs its modules, the wronger its lock headers read. *Repaired:* `tools/fixtures/p/port_band_scan.sh` under `tools/p/port_band_witness.rish` reads every `const <name>: u16 = <number>;` in living tracked Rye where `port` is a whole component of the identifier, and holds two readings at zero -- `ports_outside_band` over the seated band `38472-38600`, and `lock_band_uncovered`, a lock whose numeric edge check would refuse a port its own room declares. The three double claims ratchet under a ceiling that only falls, each named for its lane; `lock_claims_unbound` reports the one stale sentence left, amphora's 38494. **The control caught this guard reporting a fault against a correct file before it shipped:** the first draft compared a lock's edge band against every port its room declares, and a lock is taken on the LOW port of a pair, so it reported the mantra lock as refusing 38491 -- a number that lock is never handed. Both directions are planted. **46 control legs, fail=0, two mutations each biting their own leg.** *Not reached, and named rather than argued:* read on this pier `20260911`, `/proc/sys/net/ipv4/ip_local_port_range` answers `32768 60999` and `ip_local_reserved_ports` is **EMPTY**, so every number in the seated band is one the kernel may hand any client socket on this machine at any moment. The band orders our own modules and the host remains free; a one-line reservation would settle it, and that is a `nixos/` change under `.claude/rules/declared-host-config.md`, which makes it Keaton's. **BOOKED.**
