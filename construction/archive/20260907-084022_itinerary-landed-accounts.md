# ITINERARY -- landed accounts, folded from the live front

**Folded:** `20260907.084022` -- **Status:** Archived, complete, never edited
**Living card:** [`../ITINERARY.md`](../ITINERARY.md)

*The account below stood on the operator card's live front until its work landed. The card holds
what is OPEN and what waits on a word; a landed account shelves here, one click away.*

---

**PATCHOULI -- COVERAGE AND UNIQUENESS ARE TWO PROPERTIES, AND THE SPINE'S OWN GUARD HELD ONE.**
`%536` BOOKED, [folded](REDS-a-number-is-a-view-and-the-stamp-is-the-key-rows-536.md); the
elder account [folded](20260907-072600_itinerary-landed-accounts.md), floor **151 of 155**.
`reds_ledger_monotone` asked whether the numbers cover `1..N`, and **a duplicate answers that as
well as a unique row does** -- its twin fills the slot a hole would leave -- so the ledger held
`%530` twice and read `verdict=ok`. Only the derive scan saw it, and only against the REMOTE, so it
reddened in the next ship to fetch **eighty minutes late**. Now read as `(number, stamp, file)`:
`numbers_double_bound` and `rows_double_shelved`, named apart because they are repaired apart
(`%528`). **The line tells an opening from a closure note** -- an opening puts its stamp immediately
after the number -- so the fourteen lawful re-statements stay one row each and `%100` stays shut.
**458** openings, two faults, both ceilings at 1. Ten legs, each ceiling planted both ways, the
welcome proven by a **stripped copy** that invents the binding the real scan refuses (`%519`).
**Adopted from a park** (`%499`). **Two reds closed beside it:** `shell_dialect`, its one `sed -i`
of 47 now `sed_inplace`; and `stash_record` **3 -> 0**, three lap records recovered by tree-read,
rows written, the pin's `20260906` count carried **133 -> 134** -- which `log_has_a_row` alone saw.

