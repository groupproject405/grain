# REDS %650 -- a sample that never landed

*Born onto this shelf rather than folded to it, on `20260908.204213`, **BOOKED** -- the living pin
[`../REDS.md`](../REDS.md) stood at 40,787 bytes of the 40,960 its own header declares, with
**173 bytes of headroom, 14 OPEN rows and none foldable by law**, so `reds_pin_capacity` read
`pin_deadlocked=1` and `rows_that_fit=0`. This is the **seventh** ship the pin has refused today.
The practice of writing a row straight onto its own shelf is the one the fleet already took for
`%335`, `%336` and `%337` for the same reason, and it keeps the spine gapless. Raising the bound
again is Keaton's word.*


**REDS %650 (`20260908.204213`) -- a prover's own sample was eaten by an escaping layer, exited zero, and read as a verdict about the tool.** *What went wrong:* `tools/c/convergence_tree_prove.sh` was written this lap to answer the nine of ten convergence candidates that take a flag rather than a path, and its `--perturb` flag is how a caller hands the pen a tree with work waiting. The witness proving it passed a `sed` expression through Rishi -- `sed -i 's/^\*\*Rows: [0-9]*/**Rows: 1/'` -- and Rishi's own string escaping doubled the backslashes, so the expression that reached the shell matched nothing and **exited zero**. The prover then read the tree, found the first run had changed nothing, and answered `verdict=inert`. That verdict is a true sentence about a tool with no work to do, and nothing was wrong with the tool. *What caught it:* the witness refused, which is the instrument working -- and it refused for the wrong reason, which is the finding. Reading the printed `perturb=` line back showed the doubled backslashes standing in the recital, so the sample was visible in the output the whole time and the verdict beside it disagreed. *What it taught:* **a sample that never landed and a tool with no work are two different facts wearing one appearance, and only one of them is about the tool.** The prover asked the exit status, and a command may exit zero and match nothing; the tree is what has the answer. A sixth verdict, `perturb_inert`, now tells them apart by digesting the pen before the perturbation and after it, and refusing when they are equal. The shape is this tree's own braid -- one appearance over two facts -- met one lap after `dated_path`'s `refs_lost` was found holding testimony and living breakage under one number, and the same lap `stash_record`'s `unlanded` was found unable to tell a parked lap wanting recovery from one already superseded. It is also `%620`'s lesson one artifact over: **a reading you did not watch land is a reading to distrust**, whether the thing that failed to land was a file or an expression. *Repaired (`20260908.204213`):* the verdict is seated in the prover with its reason in the header, two control legs plant a perturbation that exits zero and changes nothing and require `perturb_inert` rather than `inert` (19 legs, `pass=19 fail=0`), and the witness carries the account of its own escaping so the next reader meets it. The witness's perturbation was respelled without backslashes and reads `converges`. *Standing:* three whole-tree operators are answered by running for the first time -- `reds_ledger_headline_write.sh write` **converges** on a perturbed headline, and it runs in `tools/hooks/pre-commit` on every commit this tree makes; `dated_path_repoint_scan.sh apply` **converges** at HEAD with real work to do; `tool_path_repoint_scan.sh apply` **converges** on a tree carrying one stale citation. **BOOKED.**
