# REDS %372, %373, %375 -- three instruments reading a population the tree had moved past

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260830.214500`, each **CLOSED** -- one
shape three times in a day. A rune alphabet grew to thirty while five census mirrors kept reciting
twenty-eight. An atomic install moved one artifact where the build had come to emit two. And a
ledger scan read `gate %5` as a claim about ledger row five, because one sigil serves every number
this tree assigns itself. In each the reading was right when it was written, and the population it
counted widened underneath it. Moved here as REDS %380 carried the living pin past its
24,576-byte bound. Link depth moved with the text.*

---


**REDS %372 (`20260830.193555`) -- the rune alphabet grew to 30 and five census mirrors kept reading 28.** *What went wrong:* seating `?&` and `?|` as alias-sameness twins of `and`/`or` grew the digraph pairs table in `glow/tokens.rye` -- and the census that guards that table stayed pinned at the elder count in five mirrors: `tools/fixtures/g/glow_digraph_table.txt`, its wrong-order twin, the `sample-digraph-table.glow` rows cord, the `glow/digraph_twin_check.rye` assert, and the count pins in `glow_rune_alphabet_worker.sh` and both witnesses. The alphabet witness was RED from the seating commit forward, and nothing ran it. *What caught it:* the unheard-guard sweep -- running every rostered guard Sound's own lanes answer for, from Silence's census of guards no loop invokes. *What it taught:* **a census pinned by count goes stale the lap its population grows; the growth lap sweeps the census mirrors in the same commit** -- the same one-number-many-mirrors class as %341, worn by my own rune seat. *Repaired:* all five mirrors read 30, and alphabet, digraph-twin, nest-parse, and stoa255 witnesses run GREEN. **CLOSED.**

**REDS %373 (`20260830.193555`) -- the atomic install moved one artifact and the build emits two.** *What went wrong:* `tools/g/glow_run_worker.sh` builds every binary at a private `$_dst.building.$$` path and renames it into place -- and `rye build` writes a `.ryekey` sidecar beside every emit. The install's `mv` carried the binary alone and `cleanup_tmp` cleared the binary temps alone, so every successful lane left two `*.building.*.ryekey` orphans in `glow/bin` -- **4,666** stood on disk. The concurrency scan then counted them as guarded-worker sins, and its own guard-1 grep still spelled the elder `flock` after the worker had molted to `lock_acquire` (REDS %279), so the first check it ran was vacuous. *What caught it:* running the unheard guards -- `glow_build_concurrency_witness` read `CONC_LEFTOVER_TEMPS 12` inside the guarded section, which is exactly where a clean worker reads zero. *What it taught:* **an atomic install moves everything the build emits, not the one artifact its author pictured** -- and a scan that pins a mechanism by its spelling goes vacuous the day the mechanism molts under it. *Repaired:* the install renames the sidecar beside the binary, `cleanup_tmp` clears both temp pairs, the scan greps the `lock_acquire` spelling and sweeps the elder twin's debris between its sections, the 4,666 orphans are cleared, and the witness runs GREEN on all six lanes. **CLOSED.**

**REDS %375 (`20260830.193255`) -- a custody gate wears the same sigil as a ledger row, so naming one inside a row refused a ledger that was entirely correct.** *What went wrong:* `tools/fixtures/r/reds_status_consistency_scan.sh` reads every `clos(e|es|ed|ing|ure)` in a row line and scans 60 characters either side for `%N` or `#N`, calling each one a closure claim about that row. `.claude/rules/git-signing.md` seats `%` for **any** number this tree assigns itself -- REDS rows, custody gates, errata, OQ, study numbers -- so a row saying *both guards sit at gate `%5` and gate `%7`, so every full pass closes red* was read as two claims about REDS rows 5 and 7. Neither is in the parsed spine, since the elder rows wear `#`, so the guard answered `phantom_closures=2` and `verdict=ledger_contradicts_itself` over a ledger with nothing wrong in it. *What caught it:* the row it fired on was `%372`, written minutes earlier in this same lap, and stashing that one row and re-running the scan attributed it exactly -- `phantom_closures` 0 with the row out, 2 with it in. *What it taught:* **one sigil over several namespaces asks every reader of it to guess, and a guard cannot guess.** The `%` seating is right and stays; what was missing is that the ledger's own reader had no way to tell a gate from a row, and the ambiguity surfaced only when a row first named a gate whose number the spine happens to lack -- `%360` names *custody gate `%1`* and passed, because row 1 exists. A latent false refusal that waits for a number is still a false refusal. *Repaired (`20260830.193255`):* one `gsub` blanks `gate %N` and `gate #N` in the context string before the tokens are scanned, so a gate is dropped and `closed by %N` keeps every tooth it had -- and that illustration is spelled with a letter because the first draft of this very row wrote a real number into it and the guard bit, which is the placeholder law of `.claude/rules/stamp-and-name.md` arriving one namespace over. Four control cases, two of them proven to bite under a scan stripped of the exemption; the narrowness pair are written down as companions, since a stripped scan is stricter and passes them either way. **CLOSED.**
