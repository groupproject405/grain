# REDS -- a reading that answered about something else, rows 757-759

**Language:** EN
**Stamp:** `20260916.062545`
**Style:** Gauge at Meter
**Voice:** Kyri
**Status:** Shelf -- three rows folded off `construction/REDS.md` so the pin could take the day's next two
**Room:** Checkable -- every row here was caught by a named instrument

Three rows of `20260916`, each one an instrument answering a question next to the one it was
asked. A parent read a dependent's echoed output and called a proven dependent red. A pen meter
counted a removal inside a pen as the removal of the pen, so three leakers read as released. And
the operator card advertised a commit hash read one step too early, which no repository holds.

The pin keeps what is open; these three stand BOOKED or CLOSED, and they rest here whole.


**REDS %757 (`20260916.014107`) -- a capture became bounded, and a parent that read a child's echo went red on a child that had proven everything it was asked to.** *What went wrong:* `tools/d/drey_witness.rish` asserts `session.out contains "GREEN drey-session"` for each of sixteen rungs. Each rung witness runs its module's selftest into a file, cats it, and says the result; the parent's literal was satisfied by that echo passing the selftest's own words through. `57b6b904f` changed all sixteen says from `${shown.out}` to `${shown.out_brief}`, which keeps a bounded tail, and the selftest's GREEN line sits above that tail. So every rung still proved its own claim -- each greps the file directly and asserts on it -- and the parent read sixteen failures. *What caught it:* the hot endurance run, `drey red`, on a lap that had touched nothing in `mikrophone/`. *What it taught:* **a coupling that survives only while a capture is untruncated is a coupling nobody declared.** The parent never asked the child for a token; it read a substring of whatever the child happened to relay, which is a promise the child never made and could not have known it was keeping. The repair publishes the token deliberately -- each rung says `selftest printed GREEN drey-<rung>` on the strength of the grep that just passed -- so the parent reads a line written for it. *The class is larger than the instance:* `866` tracked `.rish` witnesses now interpolate a brief capture in a `say`, measured this stamp, and any parent grepping a child's relayed output for a literal is at the same risk. Whether that class holds other live instances is unmeasured, and it is the door beside this row. *What holds this still:* `tools/d/drey_witness.rish` GREEN on metal, all sixteen rungs named in its closing line. **CLOSED** on that witness -- the instance; the class stands unmeasured and is named here rather than claimed repaired.

**REDS %758 (`20260916.012247`) -- the pen-release meter counts a removal INSIDE a pen as the removal OF a pen, so three of this room's eight leakers read as released.** *What went wrong:* `tools/fixtures/p/pen_release_scan.sh:167` accepts a line as a release when it names `rm -r` and the pen variable, and it matches `${home}` followed by any non-word character -- so `rm -rf ${home}/f/elder.kyri.season`, a file deleted *inside* the pen, reads as the pen being taken down. `amphora_carry`, `amphora_pour_negative` and `amphora_restore` each carry exactly such a line and no root removal at all, and each read `straight` while leaking every run. *A second blind spot pulls the other way:* the predicate requires a `$` sigil, and Rishi's argv spelling `run ["rm" "-rf" home]` names the variable bare -- so `tools/am/amphora_restore_negative_witness.rish:96`, which genuinely sweeps its pen, reads `never_removed`. Twelve tracked Rishi sources write that form; two carry a `mktemp -d` pen today, both in this room. *What caught it:* first residency in the room the meter was built for. The by-hand reading of `tools/am/` found **eight** witnesses with no root removal anywhere; the scan named **six**, one of which was the false positive above. The pier settles it independently -- `%745`'s own directory census names **eight** leaking amphora families, `amphora_cneg` `amphora_carry` `amphora_first_resident` `amphora_grand` `amphora_mark_wreck` `amphora_pour_neg` `amphora_pour` `amphora_restore`, and those are exactly the eight. *What it taught:* **a predicate that reads a prefix reads a family, and a pen's own children wear its name.** The subject is the pen ROOT, so the reading wants the variable at an argument boundary rather than at a character boundary -- `${home}` or `${home}"` and never `${home}/`. The two faults pull opposite ways, which is why the headline figure looked plausible: an undercount of three and an overcount of one net to a number nobody questioned. *What this lap did:* repaired all eight witnesses with a root sweep, so `never_removed` fell to **9** -- 16 at seating, 14 once bakery's `make-pen` took two, 9 with these eight -- and the ceiling fell with it, both sides proven. *What it does not reach:* the predicate itself, which lives in the meter bakery landed three hours before this row, renumbered from `%753` when a peer's earlier stamp took that view and is that lane's to move; and `unreleased_on_refusal`, which rose **336 to 341** as the eight honest success-path sweeps joined it -- the half a failing assert still leaks, and the half `bakery-rishi-make-pen` is claimed against. **BOOKED** -- the eight instances stand repaired and GREEN on metal; the predicate is a peer's one-line fix.

**REDS %759 (`20260916.004032`) -- the operator card advertised a commit that exists in no repository, and the rule writing it reads its hash one step too early.** *What went wrong:* commit `5b8e2ec2f`, a log-only follow-up, wrote `Git nib: 22db6899fe` into `construction/ITINERARY.md`. `git cat-file -t` answers *Not a valid object name*: the card named a hash this tree, the anointed remote, and every clone alike carry nowhere. The work commit that follow-up sat on is `973a4e11ab`. *What caught it:* two rostered guards, on every ship's cold endurance run at once -- `tools/n/nib_honesty_witness.rish` answering `verdict=FLOATING_CLAIM`, and `tools/r/remember_git_nib_witness.rish` refusing by name. *What it taught:* **`remember-git-nib` rule 5 reads its hash before the follow-up commit, rule 2 reads its hash after the final rebase, and a send that rebases between those two moments breaks the first.** Rule 5 spares rule 2's amend on one stated assumption -- *the final rebase is already behind the send* -- and a rebase landing after the nib is read rewrites the very commit the nib names. That a pre-rebase sibling is what it WAS is an INFERENCE, named as one: the object is gone. What is OBSERVED is that the hash resolves nowhere, and that a rebase did rewrite that lap's work commit. *And a second reading rode in:* `nib_honesty` reads every hash the living card spells, so quoting the dead one in this row's card account re-advertised it and the guard refused again -- a guard reading a page for hashes cannot tell a citation from a claim. The hash lives here, where a row is testimony; the card names it in words. *What holds it still:* the two guards, both `tier lap`. *Repaired:* the nib resolves; both GREEN on metal. *What this does not reach:* the rule page. Rule 5's reading wants rule 2's *after the final rebase* clause, and amending a seated send law is Keaton's word. **BOOKED** -- instance repaired, the law's own sentence still open.
