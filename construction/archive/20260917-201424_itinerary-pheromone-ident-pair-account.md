# ITINERARY account -- the ident pair delegates, and a width leg stops reading prose

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one landed account, immutable once written
**Room:** checkable -- proven on metal, witnesses named
**Folded:** `20260917.201424` from [`../ITINERARY.md`](../ITINERARY.md), to bring the card back
under its byte bound after a rebase carried the landing in whole.

**WATER TASTES** (row 3, N=5408): read up close and run the actual thing. *Growing a Language*
asks that a family diverge by strengthening rather than rewriting -- one root, a small supplement
each -- which is this arc in one sentence: one published rule, each caller keeping only its own
budget.
**THE MECHANISM.** `glow/lower_shape.rye` and `glow/lower_named_cast.rye` each declared
`fn zig_safe_ident` byte-identical at body md5 `4484b371`. Both are stubs now in the room's landed
idiom -- same `out` type, same `LowerError` name, a new `field` slice -- delegating to
`zig_ident.safe_ident` with `rm.max_name_len` as the ceiling parameter, `refusal` and `zig_ident`
imported beside the module's others. Their **14 and 15 call sites** each take the hyphenated name
of what they lower (`"tuple-init-face"`, `"cross-tagged-unit-tag"`), so a refusal names which face
refused.
**THE WIDTH BILL WAS ZERO AND THE CLAIM SAID SO BEFORE THE BUILD.** Both already answered `u32`,
so `width_check`'s `corpus_flagged_lines` reads **1143**, exactly where the last lap left it.
Copies **15 to 13**, stubs 14 to 16.
**A WITNESS LEG READ A COMMENT AS A TYPE**, and both modules' witnesses carried it: the authored-
`usize` leg is `grep 'usize' <file> | grep -v '@intCast' | grep -v '@as(usize'`, so the stub's own
*say why* -- the sentence naming the width TAME leaves at the seam -- reddened a guard about types.
Both legs read past a whole-line `//` comment now, which is the truer reading, since a comment
declares nothing. Proven both ways on metal: a planted `const planted_len: usize = 0;` reads 1 and
lifting it reads 0.
**THE THIRTEEN THAT REMAIN ARE READ INTO THE SCAN'S OWN COMMENT** by body md5 with the declared
ceiling normalized out, so a cohort is a cohort whatever budget it names: the `max_arm_len` THREE
at `c370400f` carrying **31 sites each**, still the largest lap; a NEW `max_name_len` PAIR at
`658a5b6f`, `lower_multi` and `lower_multi_typed` at **one site each** -- the cheapest fall this
room has offered; and eight singletons.
**THE COLD RUN ANSWERED SIXTEEN REDS AND I ADVANCED ANYWAY, WHICH I SAY ALOUD.** `tree_moved=no`,
338 green -- the ORDER clause worked, after the runner refused a second pass whose verdict the
round-open pull had already spent. None of the sixteen touches `glow/`. **One was closable and
shared:** `vocabulary_collection` read a retired word in the petrichor account on this very pin,
red on every ship; the clause now says *the collection `declared_ceiling` reads*, and the scan
reads `enforce_hits=0`.
**THE HOT PASS ANSWERED TWO REDS OF MY OWN**, and both were the shelving rather than the build:
the elder account carried its own elder's link at the card's depth, so from inside
`construction/archive/` it pointed at nothing. `tools/f/fold_shelf_link_repoint.sh --apply` made
the one correction the scan had already computed.
**PROVEN:** `glow_lower_shape`, `glow_lower_named_cast`, `glow_ident`, `glow_run_contract`,
`glow_run_desk`, `fold_shelf_link`, `fold_shelf_link_repoint`, `width-check`, `tame_style_check`,
GREEN on metal.
**YOURS:** the `max_arm_len` three are one lap at 93 sites together; the `lower_multi` pair is two.
Which door -- the largest, or the cheapest twice over?
