# REDS %809 -- a distance standing in for a block

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260917.040203` to bring it back under
the byte bound its own header declares, on the lap it landed: the pin held seventeen OPEN rows and
nothing foldable, and this row is **BOOKED** -- its instances stand repaired and the meter carries
them. A ceiling's refusal was classified by counting three lines forward from the condition, and a
line count standing in for a block failed in both directions at once, missing a refusal record
filled between the test and the return and admitting a refusal spelled in prose. Link depth moved
with the text.*


**REDS %809 (`20260917.040203`) -- a meter reading a refusal three lines forward punished the repair that made the refusal diagnosable.** *What went wrong:* `tools/fixtures/c/ceiling_teeth_scan.sh` sorts every declared `pub const max_*` into the strongest thing its own file does with it, and read `refused` as *a condition naming the constant, with a named-error return within three lines*. A line count standing in for a block failed in both directions at once. It **missed** this tree's own growing idiom -- a refusal record filled between the test and the return -- so `glow/tokens.rye` refuses four ceilings with `error.BadToken` eleven lines below the test and every one read `asserted_only`, which is the ratchet: the lap that gave those four refusals a `*refusal.Slot` each RAISED the meter that grades them. And it **admitted** a refusal spelled in PROSE, since the condition test matches `and ` and a comment is a line -- `crypto/vault_seal.rye` names `max_frame_bytes` in an invariant comment three lines above an unrelated `return error.PayloadTooLong`, and `glow/lower_shop_gate.rye` spells `if (` inside the Glow source it EMITS, on the same line as the emitter's own `catch return error.Overflow`. *What caught it:* an air rota lap pressing the boundary under the hand -- the language lane read the scan over `glow` to adopt it, found `asserted_only=5`, and four of the five were ceilings a peer lap had repaired the week before. A meter whose reading moves the wrong way after a repair is the tell. *What it taught:* **a proxy's failure direction is the thing to measure, and this one had two.** The header already said the classification is lexical and therefore a proxy; what it did not say is that the proxy was a DISTANCE, and a distance is the one proxy a codebase invalidates simply by writing better code. Every refusal record this tree adds pushes its return further from its test. Measured tree-wide across 2,018 declared ceilings, the repair moved five constants out of `asserted_only` and two in, `refused` 758 to 760 and `structural` 660 to 661, with `unread` -- the one gate -- unmoved at 3 on both readings. *Repaired:* strings are blanked before comments, so a `//` inside a literal never ends a line early and a brace inside one never counts; the condition's header closes at its PARENTHESES, which ends a multi-line condition without guessing how many lines it takes; and the body is walked by BRACE depth to its matching close, with the braceless one- and two-line forms keeping their place. Ten behaviors added to `tools/fixtures/c/ceiling_teeth_control.sh` -- 31 assertions across 23 -- each planted and lifted, and three mutations asserted to bite, each preceded by a check that its marker still stands in the scan, since a mutation removing nothing runs the unmutated file and reads as a passing leg. `glow` adopts into `tools/c/ceiling_teeth_witness.rish` at its own `asserted_only <= 3`, seated beside mantra and tally's 1 rather than merged with it. *What this does not reach:* a brace inside a nested literal spelling its own quote escape is read as a brace; no such site stands in this tree today, and when one does it reads as a MISSED refusal and falls to the ratchet, which is the safe direction for a wrong answer. And whether the three ceilings glow honestly asserts want real refusals is the language lane's own next lap. **BOOKED.**
