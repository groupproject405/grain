# REDS shelf -- row %712, the option that was TCP reasoning on a UDP socket

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- one BOOKED row, born on its own shelf because the pin has no room for it

Written `20260911`, in the lap that found and repaired the red. `tools/fixtures/r/reds_pin_capacity_scan.sh`
read `pin_deadlocked=1`, `pin_foldable_rows=0`, `rows_that_fit=0` at `20260911.090834` -- so no
lawful fold frees space and this row has nowhere on the pin to land. A BOOKED row may fold by the
ledger's own law, so a BOOKED row may be born here: the move `%699`, `%703`, `%706`, `%707` and
`%708` each made before it, every one for this same reason.

The key is the stamp `20260911.090834`; `%712` is the view, read from the anointed spine `xy` after
a fetch, per [`../../.claude/rules/derived-spine.md`](../../.claude/rules/derived-spine.md).

**Booked `%710` when it was written, and renumbered twice before it was ever shared -- to `%711`,
then to `%712`.** The lap that wrote this row was killed mid-send, so the row stayed local while
`xy` bound `%710` to a peer's `20260911.093000` and then `%711` to another peer's row, each landing
while this one lay in a stash. The key is the stamp, the number is a view, and an unshared view
moves: `.claude/rules/derived-spine.md`.

---

**REDS %712 (`20260911.090834`) -- seven UDP modules carried a TCP invariant, so every port collision on this pier wore a receive path's name.** *What went wrong:* all seven `mantra/*_delivery.rye` modules opened every UDP socket under `setsockopt(SO_REUSEADDR)`, each carrying the same careful comment -- *SO_REUSEADDR is what lets a bounded run rebind its own port*. That sentence is true of TCP, whose closed sockets sit in TIME_WAIT holding the port. UDP has no TIME_WAIT, so a closed UDP port is free at once and the option was buying nothing it was added for. What it bought instead is a DUPLICATE BIND: two sockets that both set it hold one address and port together while the kernel splits the arriving datagrams between them, with no error on either side. Every port in these modules is compiled in -- `38478` through `38491`, seven pairs -- and a port belongs to the MACHINE rather than to a checkout, so on a pier of eight trees two runs reach for one pair and both succeed. The loser then reads a peer's traffic as `BadKind` or waits out its timeout as `RecvFailed`: **a refusal naming a receive path for a fault that happened at bind.** *What caught it:* `%700`'s own second repair, hunted after this lane's flap counter refuted the first inference. The elder paper observed two delivery selftests started together, one GREEN and one `RecvFailed` on the first try, and named the mechanism; the census that followed found the option in all seven modules and found `mantra/recall_tablecloth_query_delivery.rye` and `mantra/snapshot_export_delivery.rye` sharing `38490/38491` even within ONE tree. *What it taught:* **a comment can be careful, cite a row, say why, and still be about a different protocol** -- and the copy is what made it seven files rather than one. It was also already named: `tools/fixtures/a/amphora_udp_reuseaddr_scan.sh` has REPORTED these seven paths for five days under a reading it deliberately never gates, because a guard that reds a lane over a peer's module is a guard that lane turns off. A number with no authority to refuse still told the truth where a lane would eventually read it. *Repaired (`20260911.090834`):* the `setsockopt` is gone from all seven; each now READS the option back with `getsockopt` and asserts it off, since deleting a line proves the line is gone while a read-back proves the socket the kernel handed over carries what the file claims. Measured on metal, eight concurrent selftests of `snapshot_export_delivery` against a build from each side: **12 `RecvFailed` and 2 `Truncated` with the option, 13 `BindFailed` without it.** The green rate moved 0-of-8 to 1-of-8 and moving it was never the claim -- a collision two ships apart is still a collision; what changes is that the kernel names it, in the namespace the port lives in, needing no lock to be right. Gated by `tools/m/mantra_udp_reuseaddr_witness.rish` over `tools/fixtures/m/mantra_udp_reuseaddr_scan.sh`: `source_reuseaddr_sites` at **zero** across **7** dgram modules, **7** reading the option back, **12** peer modules reported by name and none gated. **30 control legs** on real git repositories in a throwaway pen, every refusal planted then lifted -- including an untracked module read as invisible and the SAME file staged read as visible, which is what proves the index is the oracle rather than merely asserting it -- and **four mutations bitten**, felling 3, 3, 2 and 4 legs. All seven modules build, selftest and demo GREEN; `mantra_snapshot_hosted` and `mantra_recall_tablecloth_query_wire` GREEN. *And door two was already done, standing unlanded in a stash.* `stash_record` read `records_unlanded=1` at this lap's roster open, and the record was a lap of this same lane killed mid-send at `20260911.071505`, whose log said `status GREEN` and named every witness. That lap had built the bind-to-zero exchange for `recall_tablecloth_query_delivery.rye` -- both sockets bind port zero, the caller hands its kernel-chosen port to the dependent in argv, the dependent answers one ready byte, and the host reads the caller's address off that datagram -- with a falsifier that had already fired against a half-measure at **38 of 40 red** under four-way concurrency. **The two repairs compose rather than compete:** door two removes the collision, door three names what remains. This lap landed the killed lap's work unedited, then dropped `SO_REUSEADDR` from the now-ephemeral module on top, and `tools/fixtures/m/mantra_delivery_port_control.sh` reads **0 red of 16** clean against **7 of 8** for a pen mutant pinned back to `38490`. *And the counter this lane built one lap earlier took its own reading:* `mantra_query_wire_flap_scan.sh` over `mantra_recall_tablecloth_query_wire` read **18 green, 6 red, `flap=yes`** across 24 runs last lap, every red at load 17-19; with both repairs standing it reads **12 green, 0 red, `flap=no`** at loads **14.15 to 18.33** -- inside that band rather than beside it. Twelve runs with no refusal is one afternoon under one pier's load, so it is recorded as testimony and gated nowhere; a rate gated would red on a quiet day for no fault of the tree. What it says is that the instrument built for this exact question, which answered yes yesterday, answers no today. *Still open:* the other six modules keep compiled-in pairs and door three's named refusal alone, and the killed lap's own closing line names the allocation nobody meters -- **37 port constants across 22 files**, five shared by two modules each. And amphora's peer census walks the FILESYSTEM where its subject is tracked source, so it counts this ship's gitignored `.lap/elder_delivery.rye` -- the two instruments now read **13** and **12** and the difference IS that ghost. The repair was attempted and withdrawn: an index oracle reads nothing in that control's pens, which are plain directories rather than repositories, so repairing it is a third frame past the depth-2 bound and belongs to the lane that owns both files. That is BAKERY's `dated_path` defect of the same morning, in its second room, hiding where it hides best -- inside a REPORTED number no gate ever reds on. **BOOKED.**
