# Saved account -- `20260907.103111`

**Language:** EN
**Status:** Recovered historical record -- checkable
**Style:** Gauge, Meter
**Voice:** Kyri
**Recovered:** `20260909.120503`
**Source stash:** `3a26be5d773d716bb7f4b4e97dc49dc5f701cbef`
**Source blob:** `c37996c6ca214c272a26764ddc5058162c7fa7dc`

This is the saved account shelf, recovered as historical testimony. Its readings
and open questions describe the original lap; the [operator card](../ITINERARY.md)
holds the current work.

---

# Landed accounts -- `20260907.103111`, Bakery

*Folded off [`../ITINERARY.md`](../ITINERARY.md)'s live front, which holds what is OPEN and what
waits on Keaton's word. Cited by stamp rather than by number where the anointed spine has yet to
bind a view (`.claude/rules/derived-spine.md` rule 4).*

## Why this fold happened here

The live front stood at 40,958 bytes of its 40,960 bound before this lap wrote a line, and the
block below is this lap's own elder account -- closed by its own author, and replaced by the
account of the item it names as still open. No peer's block was moved to make the room.

## The elder block this shelf replaces

**BAKERY -- THE MAP NOBODY CHECKED IS CHECKABLE, AND THE TWO ROWS IT FIRST READ WERE WRONG IN THE
DIRECTION THAT SKIPS WORK.** DIFFUSER's item 1, rostered: `tools/fixtures/s/scope_trace.sh` runs a
guard under `strace -f -y -e trace=openat`, reads the resolved path of every successful open under
the root, and matches each observed FILE against its row with **the same `scope_match.sh` the runner
skips by** -- so a gap it names is a skip the runner takes. `tier lap` 4s behind a new **`capability
trace_instrument`**; **31 behaviors**, the load-bearing leg a blinded copy reading `gaps=0` where
the real reads 1. **Both repairs derived, not recalled, reproducing your sample file for file:**
`radiant_negation` 142/89/**53** and `tally_roster` 844/28/**816**, unioned to **142/142** and
**843/844**. **The sharper half is which rooms.** `radiant_negation`'s row named `foundations/` and three
`context/` pages -- its **ADVISORY** tier -- and omitted `.claude/rules/`, its **ENFORCE** roster:
*naming what is reported, skipping what is gated.*

## And the item that block left open, now closed

The same block carried **YOURS, AND NOT TAKEN: THE DATED-PATH CENSUS GATES A CLASS IT WAS NEVER
BUILT FOR** -- `dated_path_witness` red at `refs_lost=173` against a ceiling of 165, with a hand
count reading 139 mentions, 15 bare, 15 links, all 30 promises in dated testimony, and a repairable
count of zero. That reading was right about the conclusion and had the wrong root beneath it. The
lap of `20260907.102741` found the root: **87 of the 175 were names a hand had deliberately
delinked**, marked `*(log never landed)*` on a day shelf, and the census had been counting a
finished repair as open breakage since `20260823`. The account stands in the ledger; what remains
of the elder question -- whether the gate itself should move to `lost_promised_living` -- stands on
the live front, now carrying its own standing measurement rather than a hand count.
