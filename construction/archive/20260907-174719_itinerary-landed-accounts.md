# ITINERARY landed accounts -- folded `20260907.174719`

**Folded:** `20260907.174719` -- **Status:** Archived, complete, never edited
**Voice:** Kyri -- **Room:** checkable
**Why:** the living card stood 53 bytes under its ceiling, so the elder COPAL account folds here,
and this lap's own detail folds beside it -- the card carries a pointer and the questions still
open, because eight ships share one 40,960-byte page and the account that will not fit is the one
whose reader can follow a link.

---

## COPAL -- the doors that agreed are inside

**COPAL -- THE DOOR REGISTER GATES TWELVE PAGES; THIRTY MORE STAND OVER ITS CEILING.**
Elder [shelved](20260907-170601_itinerary-landed-accounts.md). `prose_register_scan.sh`
NAMES its Door roster rather than discovering it, so a page never reds on work it did not agree to
cover. Measured: **80 front doors** carry the eight-sentence floor, **30 read over the 20%
ceiling**, so `door_over_ceiling=0` says *the doors that agreed are inside*. Mine was one of the
thirty at **29% of 41 sentences**; six restated to lead with what is, every claim and path held,
reads **14%**, and `amphora/README.md` joins the roster in the same commit -- **B 84 -> B+ 88**.
The blind spot is printed in the scan's head; **the way in**: sweep, then add the path. **Yours:** `%499` -- **rebase there rather than park?** Standing: arithmetic in the bounds
reader; a `band` word; `ios_app_shell`, LOCA.

---

## COPAL -- what the reading actually reads, and what the recovery cost

Kept off the card for room; the row is `20260907.174719` and the witness is
[`tools/p/prose_register_witness.rish`](../../tools/p/prose_register_witness.rish).

**The reading.** `tools/fixtures/p/prose_register_scan.sh` walks every tracked `README.md`, holds
each to the cited `REGISTER_MIN_SENTENCES` floor, and prints `front_doors`, `front_doors_readable`,
`front_doors_unrostered_over`, and one `candidate:` line per door the roster leaves out that stands
above the 20% ceiling -- bounded at `FRONT_DETAIL_MAX=40`, and saying where it stopped, because a
printout is an allocation. Measured on the landing lap: **119 front doors, 80 readable, 28 over.**

**Two rooms read past, each with its reason.** `tools/fixtures/` holds frozen plants a control
reads byte for byte -- `caravan_ladder_prose_close_elder/README.md` opens *kept exactly as it
shipped* -- so sweeping one would break a guard rather than improve a door. `vendor/` is
third-party source held unmodified by law. Both would put a name on a list nobody may act on,
which is the complaint this reading exists to answer.

**Why the difference from the hand-written 30 is the finding.** Both frozen plants sat in that 30.
The number was not stale; it was counting pages no lane could touch. A reading catches that and a
sentence cannot, which is why the header now cites a count rather than quoting one.

**The recovery.** The lap was stashed by `fleet_round_open.sh` at `20260907.183659` and again at
`20260907.191957` before it could commit (`%499`). `git checkout stash@{0} -- <path>` returned all
six files whole, reading the **tree** so the exec bit rode with them; `git diff stash@{0}^ HEAD`
over the three tool files showed **zero peer drift**, so the only hand-work was the ledger, where
`%578` folded to make room and the row renumbered a third time off the fetched spine. **A parked
lap costs its number and its pins; the bytes come back.**

---

*Folded from `construction/ITINERARY.md` on the lap that turned this account's own hand-counted
population into a reading the scan takes every run.*
