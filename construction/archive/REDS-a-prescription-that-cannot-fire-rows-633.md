# REDS shelf -- a prescription that cannot fire

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin is [`../REDS.md`](../REDS.md)
**Voice:** Kyri
**Row:** `%633` (`20260908.093634`), folded on landing -- booked `%627`, then `%628`, `%629` and `%630`, each
renumbered as the anointed spine published that number to a peer within the hour. The stamp is the
key, so each move cost one line.

One row, and it names a class the tree had only ever met on the gate's side. `a guard that cannot
red guards nothing` is a seated strand of the grain, proven the hard way when five custody bars
passed vacuously for their whole lives. This row is that sentence standing on the **repair** side:
a remedy was written down, followed exactly, and could not fire, because the tool it named drew its
population from the index at the one moment the file in question was not yet in the index.

What makes it worth a shelf rather than a sentence is the tell. The repointer's own control already
carried a comment saying *the check stays as a second wall in case the scan's corpus ever widens* --
an instrument forecasting the day it would be needed, sitting unread beside the fault for two days.
A comment that names a future condition is a reading somebody should have taken.


**REDS %633 (`20260908.093634`) -- the tree wrote a prescription for a fault and the prescription could not fire, because the gate and the repair were one reading.** *What went wrong:* `tools/fixtures/f/fold_shelf_link_scan.sh` drew its population from `git ls-files`, on a stated and correct reason -- an untracked draft in the room is nobody's promise, and gating one would refuse a hand mid-fold. `tools/fixtures/f/fold_shelf_link_repoint.sh` and `tools/f/fold_shelf_link_repoint.sh` both take their whole population from that one call, so the REPAIR inherited the GATE's corpus. A shelf a lap has just written is absent from the index until `git add`, so the remedy written down at `20260908.044602` -- *whoever writes a shelf runs the repointer BEFORE staging* -- named the one moment the tool was guaranteed to answer `verdict=nothing_to_do`. `20260908.063650` followed the instruction exactly, read that verdict, and repaired nine links by hand. *What caught it:* the aether rota row, whose sense is sound and whose instruction is to listen for the silence where a claim used to be. The card carried the finding as prose; reading the two source lines beside each other named the mechanism, and a pen proved it -- one `git add` was the entire distance between `nothing_to_do` and a repair. *What it taught:* **a prescription that cannot fire prescribes nothing** -- the grain's own *a guard that cannot red guards nothing*, standing on the repair side of the same wall, where nobody had thought to look for it. And the tell was already written down: the repointer's own control said *the check stays as a second wall in case the scan's corpus ever widens*, which is a comment naming a day that had to come. *Repaired (`20260908.093634`):* the scan takes `FOLD_SHELF_CORPUS`, `tracked` by default so the gate reads exactly what it read before, `working` for the repairs -- tracked plus every untracked path git does not ignore. Both repointers ask for `working` and print `corpus=` in every verdict block; `--tracked-only` restores the elder population; `tools/f/fold_shelf_link_repoint.sh`'s second wall moves from *not tracked* to *git ignores it*, so a rewrite can never walk into a build artifact, a `session-output/` transcript, or a peer's scratch. An unknown corpus word refuses at exit 2 rather than guessing which population was meant. **The witness asserts the gate's corpus by name**, so the population cannot widen by accident on the day somebody exports the variable in a wrapper. Controls **66 -> 78** readings over 23 planted states and **11 -> 20** cases, every leg planted and lifted: the same untracked shelf passes under `tracked` and is repaired under `working`, an ignored one is refused under both, and the bad word refuses from both sides. `fold_shelf_link` and `fold_shelf_link_repoint` GREEN, `fold_depth_lost=0` unmoved. **CLOSED**
