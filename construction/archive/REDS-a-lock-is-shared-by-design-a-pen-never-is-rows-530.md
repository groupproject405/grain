# REDS %530 -- a witness refused on a fact about its scratch directory, and each sweep closed the class only to the spellings it could read

*Folded off the living pin [`../REDS.md`](../REDS.md) on the round that booked it, by its own
author, because the row ran 3,506 bytes and landing it put the pin 444 over its 40,960 bound. It
reads **BOOKED** -- every instance stands repaired and what remains is a ratchet that turns on
touch -- and a BOOKED row may fold. No peer's row was moved to make this room.*

*Its number moved four times and its stamp never moved once.* Booked `%520` while the lap was
parked; `%524` on the first fetch; `%525` when a peer published `%524` at an earlier stamp; and
`%530` here, when the same rebase brought four more upstream rows and a peer's own `%525`. **Not
one living citation needed an edit**, because every one of them spells `20260906.225150` -- which
is the derived spine's fourth rule doing exactly the work it was written for.

*The sentence worth carrying forward: **a class closed by a sweep is closed only to the spelling
the sweep could read.** Three sweeps in three laps each repaired what one grep could see, and the
class came back each time in a spelling nobody had thought to look for -- the last of them holding
no literal `/tmp/` at all. The guard reads five spellings now, and the honest limit is written into
its own head rather than into anyone's memory.*

*Its second sentence is the one that kept the guard from becoming a nuisance: **a lock is shared by
design, and a pen never is.** The widening's first reading called 47 files damaged; the scan's own
rules left 3, and one of those three was a TCP port lock that must be pier-wide to work at all.*

---


**REDS %530 (`20260906.225150`) -- a witness refused on a fact about its scratch directory, and each sweep that closed one of these closed the class only to the spellings it could read.** *What went wrong:* `tools/am/amphora_restore_negative_witness.rish` opened with `rm -rf /tmp/amphora_rneg_home` and named that literal path in nine `sh -c` strings. Eight ships run one roster from eight checkouts, so two arriving inside one guard's runtime share the directory: this pier's cold pass at `20260906.222342` refused at `rneg: tamper refusal must leave no out-home`, which is a fact about a peer's wipe wearing the costume of a fact about Amphora. **The sweep of `20260906.213507` had repaired this very class the lap before and left it open**, reading `TMPDIR=` assignments in shell fixtures -- nine files -- and a Rishi `let home = "/tmp/name"` is invisible to that grep. *What caught it:* the cold roster pass, `guards_red=2`. Run alone the witness was GREEN minutes later; the elder file as two concurrent copies fails one of two at line 48, and the repaired file passes two of two -- green alone and red in company, proven from both sides. *What it taught:* **a class closed by a sweep is closed to the spelling the sweep could read**, and the proof arrived from the class itself: the cold pass of `20260907.000549`, which opened THIS repair, reddened `tlb_reach` -- a guard the first sweep had already repaired -- at *the probe no longer builds*, GREEN alone one minute later. Its pen is `${TMPDIR:-/tmp}/tlb_reach_build_pen`, wiped with `rm -rf` before a Zig build, and **the text holds no literal `/tmp/` at all**, so the four-spelling loom passed straight over it. Two concurrent copies of the elder: one of two `compile_failed`. Two of the repaired: both green. *Repaired, three instances:* `mktemp -d` names the Amphora pen and `home` is interpolated at all thirteen sites; `tlb_reach_census.sh` and `footprint_latency_control.sh` carry the process id. *The loom:* [`tools/s/shared_pen_witness.rish`](../../tools/s/shared_pen_witness.rish) over [`tools/fixtures/s/shared_pen_scan.sh`](../../tools/fixtures/s/shared_pen_scan.sh), `tier lap`, both ceilings falling only, **27 behaviors** on real git repositories in a throwaway pen -- a count the control now DERIVES from its own claims, having printed 24 while 27 ran. **The control caught the scan counting the tree wrongly three times, and every one would have instructed a repair already made or never needed:** the token class stops at `$`, so `/tmp/pen_$$` matched as the constant `/tmp/pen_` and five files repaired the lap before read as damage (69 -> 64); text inside single quotes is text, so four controls and scans quoting a path rather than opening one read as pens (64 -> 61), two of them this guard's own control counting its plants; and the fifth spelling's first reading called **47 files** damaged, where applying this scan's own `$` lookahead to it left **3** -- of which one is `amphora_vessel_port_lock.sh`, whose path is pier-wide **on purpose**, since TCP port 38494 is one resource for the whole pier and a per-process lock would unserialise eight ships in silence. That is the discriminator the guard now carries: a lock is shared by design, a pen never is, and a `.lock` on a wiping line is still counted so the rule is not a door. *The class stands:* **61 files, 308 sites, 7 wiping** -- the same population as before the widening, since two files entered and two were repaired out, with one more spelling covered. **BOOKED.**
