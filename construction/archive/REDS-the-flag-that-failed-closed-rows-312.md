# REDS -- row %312, folded from the living pin

**Folded:** `20260828.125441` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*The interim flag disabled the only execution path there is, and the warning had said so in four
words.*

Booked and closed inside one hour, on the bench that shipped the flag and on the pier that blocked
on it. The lesson it carries is the third firing of one class in two days: a refusal, or a warning,
says what is wrong in its own first words, and a diagnosis already in hand reads past them.

It folds on the lap after it was written, to make room for `%313` -- the ledger reading itself, and
the fold-blocked rows that reading found.

---


**REDS %312 (`20260828.115623`) -- the interim flag disabled the only execution path there is, and the warning had said so in four words.** *What went wrong:* to unblock Dream before the NixOS rebuild, this hand added `--disable code_mode_host` to the three codex exec lines in `tools/l/launch-dream-dual-chapter.rish`, reasoning that a classic tool path would serve. Codex 0.150 has no classic fallback -- the router routes **every** tool call through the code-mode host -- so the flag did not route around the missing binary; it disabled the one path that exists, and Dream's first post-rebuild lap blocked on `code-mode host is disabled`. The cruelest part: the very first warning quoted while diagnosing said it -- *"Code mode will fail closed"* -- and the sentence was read past while being copied. *What caught it:* Dream's own refusal, pasted by Keaton -- the lap's error named the disable, not the binary. *What it taught:* **the refusal-first-line law reaches warnings too: a warning's second sentence is a contract, not color.** This is the third firing of the read-the-refusal class in two days (gp405's missing-remote tail, the loop handler's one-message-for-three-faults, now a warning's fail-closed clause), and three firings is a loom: the cure each time was reading the message's own words over the diagnosis already in hand. *Repaired (`20260828.115623`):* the three flags retired the same hour in `c83e0ca25`, the card's why-paragraph rewritten to say what the warning said, and Dream ran clean on the rebuilt host -- both binaries in the new store path, a warning-free pong, and the loop live. CLOSED.
