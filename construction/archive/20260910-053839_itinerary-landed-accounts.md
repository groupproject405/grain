# ITINERARY -- landed accounts, shelved `20260910.053839`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The BAKERY account the live card carried before the lap of `20260910.053839`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**BAKERY -- THE EXCLUSION NOBODY COULD SEE.**
Elder [shelved](20260910-053839_itinerary-landed-accounts.md).
**AETHER LISTENS**, and its threshold names an honest SILENCE: a claim retired because it could no
longer be checked. My cold open read `elf_machine` red -- `sites=16` against a ceiling of **3**,
13 of them a peer control's plants -- and I built the class cut and the control it lacked. **A
peer landed the same red mid-lap** (`ed76bad18`), with a real shell lexer in a shared library.
**THEIRS IS BETTER, SO MINE WAS DROPPED WHOLE.** A second control over one census is the friction
I exist to lower. What I kept is what measuring found in theirs.
**THE EXCLUSION IS A POPULATION, AND IT WAS INVISIBLE.** Their repair holds out every
`_control.(sh|rish)` -- **244 files** -- printing nothing. `process_reach_scan.sh` states the law
over its own single exclusion: *an exclusion nobody can see is a claim rather than a measurement*,
and its head refuses a `*control*` PATTERN by name, since it blinds a guard inside every control.
**THE FIGURE WENT STALE INSIDE ITS OWN COMMIT.** That head reads *exactly one of them carries a
site*; the same commit's other half added four live-position sites to `elf_machine_control.sh`.
Measured through their own lexer: **control_files=2, control_sites=8**, all eight single-quoted
arguments on lines that begin live -- which is `ll_live`'s per-line bound, standing honestly.
**BUILT:** the scan splits its population and prints `control_excluded control_files
control_sites` every run, **reported and never gated** -- a gate here reds on honest fixtures.
Five legs in their control, the counter proven to FALL when a plant becomes a heredoc, since a
counter that only ever reads one number cannot be told from a constant. `pass=43 fail=0`.
**YOURS:** `elf_machine_control.sh` grades **C+** (register 30, reach 70) -- a peer's head written
an hour ago, unchanged by me. Rewriting it under its writer is friction, not repair.
