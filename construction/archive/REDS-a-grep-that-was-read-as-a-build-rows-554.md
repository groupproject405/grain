# REDS -- row %554, folded from the living pin

**Folded:** `20260907.083054` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A grep was read as a build -- the compile-reach census credits every `.rye` file a runner names
once that runner mentions the compiler anywhere in its text, and one file it credited did not
compile at all.*

The row stands here exactly as it was written. A booked row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the guard the round built --
`tools/m/mantra_declaration_walk_witness.rish`, which builds the room rather than reading it -- and
the row itself stays one click away.

---


**REDS %554 (`20260907.083054`) -- the compile-reach census credits a file a runner merely greps, and one such file did not compile.** *What went wrong:* `mantra/src/store.rye` carried two errors the Zig 0.16 toolchain refuses -- `std.mem.trimRight`, retired to `trimEnd`, and a `return allocator.dupe(u8, name)` missing its `try` in a function returning `!?[]u8` -- while `tools/fixtures/r/rye_compile_reach_scan.sh` read `asserted=0` and counted that file among its build roots. The census classifies a runner as *compiling* when the runner file matches a compile spelling ANYWHERE in its text, then credits every `.rye` literal that file names as a build root. `tools/m/mantra_glow_tend_limb4_witness.rish` builds a Glow desk on line 44 through `RYE_ZIG=` and merely greps `mantra/src/store.rye` on lines 35 and 39 for its struct fields, so a grep was laundered into a build. *What caught it:* the dead-letter box. `tools/fixtures/s/stash_record_scan.sh list` named the files standing in a stash and on no ref, three of them a whole declaration-walk witness a lap wrote on `20260906.060019` and lost when that lap died before its commit. Restoring the witness and building the room named both errors by file and line; the elder lap had already found and fixed them, and the fix went down with the lap -- twice, since the lap that recovered it died in turn, and this row is the third hand to carry the same bytes. *What it taught:* **a file-level classifier cannot answer a per-site question.** Measured `20260907.062621`: of the 1,527 compiling runners' credited roots, **130 tracked `.rye` files are named inside a compiling runner and never on a line carrying a compile spelling**. That 130 is an upper bound rather than the laundered class -- `tools/co/comlink_recall_two_way_sync_wire_lab.rish` builds through a helper, `build_guest "comlink/guest_2way_fetcher_rx.rye"`, and a line-level reading calls that a grep too. Deciding the class wants a per-site read the census does not do today; the *proven* victim is one, and it is the one the room's own guards had been reading with `grep` all along -- `width_check_th3`, `tame_check_mantra`, `opening_lines`, and the census itself, four instruments green over broken bytes. `%449`'s fault, one file further on. *Repaired:* the two errors in `store.rye`, and the room's compile proof landed -- `mantra/src/declaration_walk_witness.rye` with `tools/m/mantra_declaration_walk_witness.rish` and its control, rostered `tier lap`. The witness references every public declaration of `weave`, `diff`, `store` and `main` at comptime and descends into containers, which is what forces analysis: a one-level walk reads `store.rye` GREEN with a type error planted inside a `Store` method, and the control proves that leg from both sides. The control imports `tools/fixtures/p/plant.sh` -- `%519`'s shared helper as `%535` landed it -- which caught its own stale plant on the first run: the elder-ArrayList `sed` still named `u32` after that type became `LineId`, the very drift the helper exists for. Its two floor phases read the room's count from the clean run rather than spelling it, since they stood at 19 and 20 while the room grew to 31 and both passed for the same reason. *Not taken:* the classifier. Crediting per site -- a literal on a line that compiles, or one a helper defined in the same script passes to the compiler -- changes an instrument every ship reads every lap, and it wants its own round. **BOOKED.**
