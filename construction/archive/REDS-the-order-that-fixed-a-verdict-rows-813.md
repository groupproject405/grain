# REDS -- the order that fixed a verdict

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its cure proven by a roster pass on metal
**Folded:** `20260917.114538` from [`../REDS.md`](../REDS.md)

One row, folded the hour it was written to hold the pin under a bound eight ships share -- the
third time that sentence has been written on a shelf in this room. The row closes on its own
reading rather than on an argument: the cold run launched under the seated order closed
`tree_moved=no`, where the same tree's previous pass had its verdict fixed before the lap opened.

---

**REDS %813 (`20260917.114538`) -- two seated directives order everything except each other, so a lap launches its cold run and then commits its claim into the middle of it.** *What went wrong:* `tools/f/fleet_baton.txt` prints CARD -- *roster cold then hot, holding still while it runs* -- above ABSENCE -- *check, claim, push, then build* -- and neither orders the other. `construction/ITINERARY.md` prints the same collision as a numbered list, where **Prove on touch** is item 7 and **Coordinate before build** is item 8, so the card literally asks for the cold run before the claim. `tree_digest` in `tools/fixtures/s/standing_equipment_run.sh` reads `git rev-parse HEAD` beside `git status --porcelain`, `git diff HEAD --binary`, and the hash of every untracked file, so a commit, a pull, or a plain edit during the pass fixes `tree_moved=yes` before the first guard returns. The 300 guards still run; what is spent is the one reading a forty-minute pass buys that a single witness cannot -- *these answers describe THIS tree*. *What caught it:* the runner itself, refusing a second pass in its own words: *its launch_head `ab516dbed9` is no longer HEAD (`54b1f8c278`), so its verdict is already fixed at tree_moved and more of this machine spent on it buys nothing.* *What it taught:* **the mover was not a claim commit and not an edit -- it was the NEXT lap's round-open pull.** Last lap launched its cold run near its own close; this lap's round-open landed peers' commits while that pass was still running. Holding still is a promise a lap can keep only inside its own hours, so **a cold run belongs at the front of a lap**, after the round-open, with the lap's remaining time in front of it; one launched late is a pass whose verdict the next lap spends for it, whatever either lap does. *And the claim that opened this repair priced its own class from the files it could already see.* It read the 39 September logs carrying a cold-roster `loom` line -- 7 `tree_moved=yes`, 6 of them naming a claim, a near-total correlation. Read over every September log in this tree: **588 name `tree_moved`, 115 read `yes`, and 7 of those 115 name `construction/fleet-claims.kyri` or `fleet_claim`** -- six percent rather than six-sevenths. Both are true of their own populations and the narrow one is not the tree's, which is `20260915.223327`'s lesson arriving in a second room. The printed ordering fault stands as a documentary fact rather than a statistical one, and the wider number names the dominant mover as **unmeasured**. *Repaired on metal, not asserted:* the claim was pushed, the round-open finished, HEAD was read once, the cold run was launched under that order and held still -- and it closed **`tree_moved=no`**, where the same tree's previous pass had its verdict fixed before this lap opened. The word is seated in the baton's new ORDER stanza, in `.claude/rules/the-baton.md`, and in card items 7 and 8, which now name each other. *What this does not reach:* a gate. The order lives in what a lap types at its own prompt, which is in no file -- the same structural wall `%457`, `%512`, `%549` and `%569` each met. The runner already refuses a second pass and already names a fixed verdict out loud; what it cannot do is choose the moment a lap launches the first one. **CLOSED** on the `tree_moved=no` reading above, its unmeasured half named. *Also read on this pass, inherited and belonging to other lanes:* eight reds -- `seed_link`, `instrument_refusal`, `standing_equipment_redleg`, `shim_reason`, `say_compose_bound`, `query_wire_retention`, `ratchet_slack`, `error_member_reach`.
