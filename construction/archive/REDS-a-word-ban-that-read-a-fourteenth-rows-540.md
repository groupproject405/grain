# REDS %540 -- the word ban that read a fourteenth of the tree

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260907.073000` by Copal, to keep the
pin under its byte bound while a rebase brought two new rows down onto it. The row is **BOOKED** --
its instances stand repaired and the remainder is a ratchet -- and a BOOKED row may fold while an
OPEN one stays. **Nothing here is closed, withdrawn, or edited**; the row's stamp is its key and has
not moved.*

*Why a peer's row again: it was the pin's only BOOKED row at the hour, as `%535` had been six
minutes earlier, and the pin stood 1,640 bytes over. Folding costs the ledger nothing and keeps the
bound honest, which is the trade the fold tool exists for.*


**REDS %540 (`20260907.060813`) -- the meter enforcing a tree-wide word ban read a fourteenth of the tree, and its silence had never once been shown to be a reading.** *What went wrong:* duty 1 of `tools/l/living_docs_lint.rish` checks living prose for retired words. It read `$ROSTER` -- the 60-path roster the docs meter weighs for links, status rooms and byte bounds. The tree holds **767 living markdown pages**, so the vocabulary laws governed all of them while the meter enforcing those laws heard **60**. Nothing pointed at the gap, because a roster looks complete to whichever duty inherited it. *What caught it:* measuring the reach before wiring a fifth ban into the list -- the same move that caught `%532` a day earlier, and the same lesson it taught. *What it taught:* **a page roster and a prose roster answer two questions, and duty 1 had been reading the wrong one its whole life.** Which pages carry promises about links and bytes is a PAGE-level question; which prose may say a retired word is a PROSE-level question that governs every living page. One roster serving both is the braid `single-stranded` names. The sharper half arrived on the way: **`corpus` had to LEAVE the list.** `tools/v/vocabulary_collection_witness.rish` already holds it at zero across 391 reader-facing pages, ENFORCED, with a pattern that tells a prose use from an identifier, a path segment and a backticked token. Read over the living prose, duty 1's word-boundary copy named **135 lines** the stronger guard had already ruled lawful -- two ears on one sound, the cruder one loud. *Repaired:* `tools/fixtures/l/living_prose_roster.sh` derives the corpus from `git ls-files` (**379 pages**, testimony and the vocabulary records named out as decisions), and `tools/fixtures/l/retired_word_scan.sh` holds the reading, exempting a fenced block, a backticked mention, and a keeps.txt line. Duty 1 reads **379 pages and prints zero**. *And the silence now means something:* an advisory nobody has seen name a violation is a guard that cannot red. `tools/fixtures/l/retired_word_control.sh` plants **nine shapes** on real git history and proves both directions -- the violation caught, every lawful shape free -- asserted from the witness, which otherwise refuses nothing. **BOOKED.**
