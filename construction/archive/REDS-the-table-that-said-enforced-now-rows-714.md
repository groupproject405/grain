# REDS -- row %714, folded from the living pin

**Folded:** `20260911.185500` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*A lint table headed* **Enforced now** *held twenty rows and four different sentences, and the one row naming no instrument was answered by a second page that named the first -- each end citing the other, so a reader checking either met a citation rather than a hole.*

The row stands here exactly as it was written. A closed row leaves the living pin so the
pin stays the length a reader will actually read; the lesson travels forward in the guards
the round built, and the row itself stays one click away.

---

**REDS %714 (`20260911.112513`) -- a lint table headed *Enforced now* held a rule nothing enforced, and two pages each said the other owned it.** *What went wrong:* `context/TAME_GUIDANCE.md`'s lint table stands under the words **Enforced now**, and its row *One `# Title` per markdown* named no instrument. The one other page carrying that duty, `tools/fixtures/r/radiant_lint_scan.sh` duty 3, prints `deferred (TAME one-# Title / tame-check owns it)` -- and `tools/t/tame-check.rish` spells no heading anywhere. A reader checking either end met a citation rather than a hole. Four further rows named nothing while `tools/t/tame_style_scan_bans.rish` held them all along, so a reader who wanted to run the check had nowhere to start; and *Line length <= 100 columns* is held by nothing at all. *What caught it:* the aether rota, row 0 -- the row that hears, listening for the page nobody answered -- reading the lint table one row at a time instead of as a heading. *What it taught:* **a heading is a claim about every row beneath it**, and twenty rows under *Enforced now* turned out to be four different sentences: eight held by a rostered tool, six naming a tool no lap runs, four held by a tool they never name, and two held by nothing. **BOOKED** (`20260911.121100`) -- the title rule now has `tools/o/one_title_witness.rish`, rostered `tier lap`, reading **390 living pages, 389 carrying exactly one title**, ceiling **1**, 48 pen legs with six mutations bitten; every row that has an instrument now names it, and the line-length row says plainly that nothing holds it yet. The remainder is a booked lap: measure the long-line population and decide whether that row earns a guard or a retirement. Reading: [`../active-designing/20260911-112513_the-table-that-said-enforced-now.md`](../../active-designing/20260911-112513_the-table-that-said-enforced-now.md).
