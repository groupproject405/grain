# REDS -- the card that named its nib twice

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row closed on a guard proven from both sides and replayed on real history
**Folded:** `20260917.012000` from [`../REDS.md`](../REDS.md)

One row, folded to hold the pin under a bound eight ships share. It teaches that **a published cause
is a claim like any other, and reading the named tool up close can refute it**: the row named a tool
writing the nib by line offset, and no tool in this tree does. What stood instead was smaller and
checkable -- an extractor reading `head -1` on a field nobody had ever counted, so a card carrying
two of them had the first one graded and the second left unread.

Its second lesson belongs to the witness rather than the guard: **a witness can hear a reading and
still not hear its refusal.** The first draft asserted the count alone, and turning the live gate off
left every leg green, because the count line still printed. Read it beside
[`../../.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md), whose *a lantern that fires
twice becomes a loom* is what put a guard here at all.


**REDS %793 (`20260916.220400`) -- the pin rewrite ate the same directive a second time, by a second ship, one lap after the first was closed.** *What went wrong:* commit `00493b3be` replaced item **7** of the INNER LOOP in `construction/ITINERARY.md` -- *Prove on touch*, the directive telling every lap to run the cold endurance run before work and the hot one after staging -- with a `**Git nib:**` line, leaving the card carrying the pin twice and the numbered list reading **6, nib, 8**. This is byte-for-byte the fault `%789` booked at `20260916.213205` against commit `5f96df5e14`, CLOSED by restoring the item roughly one hour and thirty commits earlier. Two firings, two different ships, one directive. *What caught it:* a send's own rebase. This lap's commit conflicted on the nib line, the conflict was resolved by taking upstream's, and reading the card afterward showed **two** `**Git nib:**` lines where the card's own third mention says *named once above ... one fact in one place*. `git log -S` on the duplicated hash named the commit in one command. No standing guard fired, exactly as `%789` recorded: `nib_honesty` reads the nib BY ITS KEY and finds it whichever line carries it, and every other guard reads bytes rather than meaning. *What it taught:* **the lantern fired twice, so it is a loom** (`.claude/rules/reds-first.md`). `%789` closed with the sentence *a guard holding every numbered list in a living pin to `1..N` with no gaps would catch the next one on the lap it lands; this lantern has fired once, so it is named rather than built.* The next one landed within the hour. What the second firing adds past the first is the **mechanism's reach**: two different ships produced identical damage, so the cause is not one hand's slip but a **line-positional write** -- a tool that sets the nib by replacing a line at a remembered offset rather than by matching the key it is setting. A write aimed at a position is a deletion of whatever sits there, and the card's numbered list is the one surface in this tree where that loss leaves a visible mark. *What this lap did:* restored item 7, removed the duplicate pin, and booked this row. **The guard is named and not built here**, deliberately: this lap already carries two other rows and its own peer-reaching red, and building a numbered-list gate inside a send is how a repair becomes its own red. *The instrument, specified for whoever takes it:* read every living pin for a Markdown ordered list, hold each run to `1..N` with no gaps and no repeats, gate at zero, and prove it from both sides on a planted pin in a throwaway pen. *Repaired (`20260917.004312`):* the hypothesis this row published -- *a tool that sets the nib by replacing a line at a remembered offset* -- was read up close and does not hold. `tools/fixtures/r/remember_git_nib_write.sh` matches the KEY and a backticked hexadecimal run, reads its own result back through the guard's extractor, and refuses rather than mutating where the two disagree; `tools/r/remember_git_nib.rish` derives and hands off; `tools/hooks/post-commit` writes no card at all. No tool in this tree writes the nib by position. Both firings were a hand editing the card during a large account fold, which is why two different ships produced identical damage: the shape belongs to the EDIT, rather than to a writer. *What the repair found instead:* the card's own third mention says the nib is *named once above ... one fact in one place*, and nothing held that sentence -- while `tools/fixtures/n/nib_honesty_scan.sh` read the field with `head -1` and never asked whether a second stood. So a duplicated field left the FIRST line graded and the second unread, and which of the two the nib law judged was decided by position alone. Both firings carried the same hash in both lines, so the wrong reading was harmless by luck rather than by design. *The instrument:* that scan enumerates the field once through a named `nib_fields_of` function, counts the list, prints `git_nib_lines`, and refuses `NIB_NAMED_TWICE` above one -- the count and the extraction coming off ONE list, so a card can never be one thing to the gate and another to the reader. **Replayed out of history from both sides:** commits `5f96df5e14` and `00493b3be4` each read `git_nib_lines=2` and refuse by name, each firing's parent reads 1 and walks free, and so does `db4ae9f0bf`, the restore between them. Two planted control cards prove it in the guard's own idiom -- a two-field card carrying those two commits, and a one-field card carrying the living card's own prose pointer sentence, which the count must read as prose or the guard would refuse the card it guards. Both controls are read for `gone` first and a nonzero reading is named `CONFOUNDED`, so the refusal is proven to arrive through its own door rather than through the elder floating-claim law. Five legs named in `tools/n/nib_honesty_witness.rish`, **three mutations bitten** -- the live gate turned off, the pen gate turned off, and the pattern loosened to count the prose pointer, which bites at the LIVING card and is the reason the one-field control exists. *What it taught, past the first two firings:* **a witness can hear a reading and still not hear its refusal.** The first draft asserted `git_nib_lines=1` and nothing else, and turning the live refusal off left every leg green -- the count line still printed. The cure is to drive the live path against a planted card through the `CARD` seam the scan already carried, so the branch is exercised where it stands rather than inferred from its sibling. That is `%785`'s and grass's shape arriving in a third room on the same day. **CLOSED** on `tools/n/nib_honesty_witness.rish` GREEN. *The honest limit, named rather than hidden:* a positional write landing in ordinary prose -- no numbered list above it, no nib field in it -- is reached by neither this gate nor `itinerary_list`, and the bytes carry nothing that would distinguish it from an edit somebody meant.
