# REDS %698 -- a guard that reports history

*Folded from the living pin [`../REDS.md`](../REDS.md) on the lap that booked it. The pin stood at
40,957 of the 40,960 its own header declares -- three bytes -- so a row booked and closed inside one
lap folds rather than pushing the pin past its bound, which is the move `%692`, `%681` and `%548`
each made before it. Cited by stamp (`20260910.062027`) in every living surface until the anointed
spine binds the number, under rule 4 of the derived-spine law.*

*The clause worth carrying forward. A guard is an instrument and a PLACEMENT, and the two are proven
separately. `tools/e/empty_document_witness.rish` has been correct since `20260823` -- it refuses any
tracked document holding nothing, and its own header records the pair `%161` booked. What it never
had was a moment: it reads the tree, and a roster pass runs at a ship's cold open, so its answer
arrives after the push. Two session logs shipped at zero bytes in the eighteen days that followed,
and the second one's content is simply gone, because the hand that could have written it had
finished its session before any reading reached the file.*

*What makes this the same shape the derived spine booked at `20260910.020754`, one room over: both
gates were right, both ran at the roster's clock, and both faults are created at a commit. The
repair in both cases moves a reading rather than building an instrument -- the spine's to
`tools/hooks/pre-push`, this one to `tools/hooks/pre-commit` as rule eight.*

*What kept the wall honest: it reads the commit's own staged set alone. An empty document standing
elsewhere in the tree -- and one was standing, which is how this lap found the class -- refuses no
author. A wall that held the whole fleet still for one hand's hole is a wall somebody turns off, so
the property is proven from both sides in `tools/fixtures/g/generated_page_freshness_control.sh`
rather than promised in a sentence.*

---


**REDS %698 (`20260910.062027`) -- the empty-document guard has stood for eighteen days and never once run at the moment a commit makes the hole.** *What went wrong:* `tools/e/empty_document_witness.rish` refuses any tracked `.md`, `.bron`, `.kyri` or `.txt` that holds nothing, and it has been rostered at `tier lap` since `20260823`. It reads the tree, and a roster pass runs at a ship's cold open -- so the reading arrives after the commit has already shipped. Twice since it was seated a session log landed at zero bytes anyway: `20260828.142844`, filled 91 minutes later by a second commit once another ship's cold open caught it, and `20260910.054448`, whose commit body describes two `loom` lines and a search record that exist nowhere, because the writing session had ended before the reading arrived. Five empty documents stand in this tree's whole history, read by walking every add of those four extensions for git's empty blob `e69de29bb2d1`; three are the pair `%161` already booked plus one deliberate fixture, and the other two are these. *What caught it:* the guard itself, at a cold open seventeen minutes after the push -- correct, and too late to matter, which is the finding rather than a failure of the instrument. *What it taught:* a reading placed where it cannot run at the moment of the fault is a reading that reports history. `empty_document_scan.sh` takes a `staged` mode narrowing the same population rule -- one rule, two reaches -- and `tools/hooks/pre-commit` rule eight asks it of the commit's own staged set alone, so a hollow document elsewhere refuses no author and the wall stays on. **CLOSED** by `tools/g/generated_page_freshness_witness.rish` over three new control legs and `tools/e/empty_document_witness.rish` over eight, each refusal planted and then lifted.
