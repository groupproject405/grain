# REDS -- row %536, folded from the living pin

**Folded:** `20260907.081420` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*Coverage and uniqueness are two properties of one spine, and the guard that read the ledger off
disk held only the first -- so a number bound to two stamps reddened in the next ship to fetch
rather than in the tree that made it.*

The row stands here exactly as it was written. A booked row leaves the living pin so the pin stays
the length a reader will actually read; the lesson travels forward in the guard the round built,
and the row itself stays one click away.

---

**REDS %536 (`20260907.014654`) -- one number is bound to two stamps on the anointed spine itself, and the guard that reads the spine off disk calls it whole.** *What went wrong:* `%530` stands twice on `xy/main`. One is a row folded to `construction/archive/REDS-a-lock-is-shared-by-design-a-pen-never-is-rows-530.md` carrying stamp `20260906.225150`; the other stands on the living pin carrying `20260907.000030`. Both are published, so [`derived-spine`](../../.claude/rules/derived-spine.md)'s *a published number never moves* holds for each of them and the two rules meet head on -- the earlier stamp keeps the number, and neither number may move. *What caught it:* `tools/fixtures/r/reds_spine_derive_scan.sh` in a hot pass, `rebindings=1`, naming both stamps and both bindings in one line. It was GREEN at this lap's cold open 80 minutes earlier, so the collision arrived upstream inside one hour. *What it taught:* **two guards read one spine and only one of them can see a number bound twice.** Over the same tree `tools/gen/chapter/reds_ledger_monotone_witness.rish` reads `rows=534 gaps_or_dupes=0 verdict=ok`, because it asks whether the indices cover `1..N` without a hole -- a question a duplicate answers just as well as a unique row does, since the duplicate's twin fills the slot the missing number would have left. Coverage and uniqueness are two properties, and the ledger's own monotone guard holds only the first. The derive scan holds the second, and holds it only against the REMOTE, so a tree whose spine is internally doubled reads whole from inside and doubled from outside. *Not taken, and it is not mine to take:* both rows belong to other ships and both numbers are published, so renumbering either breaks citations a lap cannot count. What a lap CAN do is name it, which is this row. The repair wants a word on which of the two moves, and a `duplicate_numbers` reading in the monotone scan so the next one reds from inside the tree that made it rather than from the next ship to fetch. *Repaired (`20260907.081420`):* `tools/fixtures/r/reds_ledger_monotone_scan.sh` reads the ledger as `(number, stamp, file)` triples off the row openings and prints two readings beside the coverage one -- **`numbers_double_bound`**, one number carrying more than one stamp, and **`rows_double_shelved`**, one `(number, stamp)` standing in two files. They are named apart rather than summed because they are repaired apart: the first wants a word on which incident renumbers, the second wants both pages opened and may be no fault at all, which is what `%512` is -- two hands folded one row without knowing and both pages were kept. A refusal that gives an exact count and no way to act is `%528`'s own fault. **What makes an opening readable is written on the line:** an opening puts its stamp immediately after the number, where a closure note puts a word in between -- so the fourteen numbers that lawfully re-state themselves under a later stamp stay one row each, and `%100`'s gate is not re-opened. Measured over the **458** openings on disk: the two faults above and no others. Ten legs in `tools/gen/chapter/reds_ledger_monotone_witness.rish`, each ceiling planted from both sides, and the load-bearing welcome proven by a stripped copy -- `plant_write` removes the space-then-paren that binds stamp to number, and the copy then reads the closure note as a second row and invents the double-binding the real scan refuses (`%519`). A floor of 400 bindings catches the reading going dark, which is `%97`'s shape. Both ceilings stand at **1**, where the tree stands, so the NEXT doubling reds inside the tree that made it. *And this repair was itself parked:* it stood complete on `pier/diverged-20260907-030013` from `20260907.023122`, adopted here by path, the two files unmoved at their base -- `%499`. *The remainder is a seat:* which of `%530`'s two published rows renumbers stays Keaton's word, and no byte of either need move -- the ledger can record the collision once and every future citation name its stamp. **BOOKED.**
