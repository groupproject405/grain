# REDS -- a claim nobody read back

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- four folded rows, immutable once written
**Room:** checkable -- four ledger rows, each closed by a witness on metal
**Folded:** `20260909.191228` from [`../REDS.md`](../REDS.md)

Four rows from one day, and one shape under all four: a claim stood in the tree with nothing
reading it back. A transcript path derived rather than reported by its owner. A pronunciation roll
listed beside the lexer table rather than derived from it. A prover promising in its header never to
touch the tree under test, while its code walked out of the pen and edited it. A card advertising a
hash that resolves nowhere.

Each was true when written and each went on being trusted after it stopped being true, because in
every case the only thing holding it was prose. What closed all four was the same move in four
rooms: derive the claim from the thing it describes, or have an instrument read it back.


**REDS %666 (`20260909.073110`) -- a refused cold launch named its requested transcript while the live owner ran scoped.** *What caught it:* stdout path and stash lists, 7 vs 8. *What it taught:* a name arrived at by derivation identifies nothing -- only the owner knows its own path, so it writes it into the lock it holds and every refusal reads it back. *Closed `20260909.173620`:* the `--detach` parent passes `STANDING_TRANSCRIPT` to the child, which records it beside `pid` and prints it read back from the file as `run_transcript=`; both refusals print `owner_transcript=`, and a foreground pass keeping none says `none` rather than deriving. Eleven control readings, the elder line planted back and shown naming the requester's path. **CLOSED**.

**REDS %667 (`20260909.155028`) -- the rune roll was listed beside the lexer table, so `?&` and `?|` entered Glow unpronounced and the guard stayed green.** *What caught it:* the aether rota, asking what an unheard guard is worth. *What it taught:* derive the population from the thing you guard. **CLOSED**, 6 legs, [folded](REDS-the-roll-listed-beside-the-table-rows-667.md).

**REDS %668 (`20260909.170804`) -- the prover that promised never to touch the tree under test ran its subject against the live tree.** *What went wrong:* `tools/c/convergence_tree_prove.sh` `cd`-ed into its pen worktree and then invoked the tool by its absolute path in the REAL tree. This tree's tools resolve their own root from `$0` and `cd` to it -- 100 tracked `tools/` scripts carry the idiom -- so the subject walked back out of the pen, worked on the live tree, and left the pen unchanged, which the prover then read as `inert`. Proven on metal: proving `tools/i/itinerary_account_shelf.sh` wrote `construction/archive/20260909-999999_itinerary-landed-accounts.md` into the working tree of the ship that ran it, under a `verdict=inert` claiming nothing had happened. *What caught it:* the water rota's cardinal seat, whose instruction is to run the actual thing rather than read about it; the aftertaste was an untracked file in `git status`. *What it taught:* **a control's stand-ins must speak the contract the real answerers speak.** All five planted operators worked on relative paths in the current directory, and not one resolved its root from `$0`, so 19 checks stood green over a class the pen never held. The repair copies the tool to its own path inside the pen and runs it there, so `$0` resolves to the pen; a tool from outside the repository keeps its own path, having no root of this tree's to resolve. A second reading followed the first: `refused_on_second` called a write-once writer's `shelf_exists` refusal *the sharpest kind of divergence*, when the tree it leaves is the tree its first run left -- which is what accrete-never-break asks of every shelf writer here. The new **`write_once`** verdict tells them apart by asking the tree rather than the exit code, and the control's own `sour.sh`, planted as the sharpest divergence, turns out to be one of these. **CLOSED**: control 19 -> 30 checks with the elder prover's leak planted and lifted, both witnesses GREEN, census `candidates_proven` 8 -> 9 of 12.

**REDS %669 (`20260909.171151`) -- a card quoting a dead hash advertises it.** *What went wrong:* an account spelled the pre-rebase nib `35622f7454` it said no clone holds; `nib_honesty_scan.sh` reads every 10-hex token there as advertised. *What caught it:* the cold pass, `verdict=FLOATING_CLAIM`. *What it taught:* a hash resolving nowhere is never spelled on a card. **BOOKED.**
