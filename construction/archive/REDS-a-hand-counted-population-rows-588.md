# REDS -- a guard that describes its blind spot has documented it, not measured it

**Folded:** `20260907.202027` -- **Status:** Archived, complete, never edited
**Rows:** `%588`
**Living ledger:** [`../REDS.md`](../REDS.md) -- **Recital:** [`REDS-fold-recital.md`](REDS-fold-recital.md)

*One row, and what it taught: a roster-shaped guard wrote the population it declines to watch into
its own header as two hand-counted figures. Counting them on every run instead found the gap was a
category error rather than drift -- two of the pages were frozen fixtures no lane may sweep -- which
is a distinction a reading carries and a sentence cannot.*

---


**REDS %588 (`20260907.174719`) -- a guard's header named its own blind spot with a number typed by hand, and the number was already two off the next morning.** *What went wrong:* `tools/fixtures/p/prose_register_scan.sh` gates thirteen named front doors at 20% negative sentences and discovers none, by a deliberate clause -- so a green `door_over_ceiling=0` says only *the doors that agreed are inside*. On `20260907.170704` that population went into the scan's own header as prose: *across the 80 tracked front doors carrying at least the eight-sentence floor, 30 read above the 20% ceiling*. Both figures were measured once, by hand, at a shell prompt that left nothing behind. Counted by the scan itself this lap: **119 front doors, 80 readable, 28 above the ceiling** -- and the gap is a category error rather than drift. Two of the thirty were `tools/fixtures/caravan_ladder_prose_close_elder/README.md` and its count sibling, frozen plants a control reads byte for byte, whose own first line reads *kept exactly as it shipped*. A lane sweeping one would break the guard rather than improve a door. *What caught it:* writing the reading the header described. The first run named thirty candidates, and two of them were pages no lane may touch. *What it taught:* **a guard that describes its blind spot has documented it rather than measured it** -- and the difference arrives as names rather than as a number. `.claude/rules/session-logs.md` already says count them rather than quoting a number, of a figure inside prose; this is that law one room in, where the prose is a witness's own header and the figure is the size of what it declines to watch. Sharper: a count nobody can act on is what `tools/fixtures/b/bron_resins_catalog_scan.sh` names every resin to avoid, and a hand-counted population cannot tell an actionable name from a frozen one. *Repaired (`20260907.174719`):* the scan reads every tracked `README.md`, holds each to the cited `REGISTER_MIN_SENTENCES` floor, and prints `front_doors`, `front_doors_readable`, `front_doors_unrostered_over` and one `candidate:` line per door the roster leaves out that stands above the ceiling -- bounded at `FRONT_DETAIL_MAX=40`, saying where it stopped. `tools/fixtures/` and `vendor/` are read past by name, each with its reason. The reading is **reported and never gated**, since a page that has agreed to nothing must not refuse the tree. *Proven in a pen:* `tools/fixtures/p/prose_register_control.sh` grows **13 behaviors to 16** -- one over-ceiling page off the roster is named by path and share, the verdict still reads `ok`, and **the same bytes one roster line later refuse** at `door_over_ceiling=1` and `verdict=register_drift`, so the report is told from the gate rather than assumed to differ from it. Disarming the roster-exclusion line reds that third leg, so it is told from a bypass. **CLOSED.**
