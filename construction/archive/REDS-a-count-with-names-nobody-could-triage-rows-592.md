# REDS shelf -- a count with names nobody could triage

**Language:** EN
**Style:** Gauge, Meter setting (see [`../../context/GAUGE_STYLE.md`](../../context/GAUGE_STYLE.md))
**Status:** Shelf -- immutable once written
**Voice:** Kyri
**Folded:** `20260908.041750`
**Rows:** `%592`

One row, folded on the lap that closed it. The dead-letter box printed nineteen orphan paths and
the flag that named them had existed for days -- what a hand actually got was nineteen paths
wanting three different actions with nothing telling them apart, so every ship read past all of
them. Ten were fold shelves, three were the guard's own elder self after a room fold, and six were
genuinely parked work. Only the six were a lap. A count with names is still a count nobody acts on,
if the names cannot be triaged.

**REDS %592 (`20260907.215114`) -- the dead-letter box counts nineteen orphans and names none of them, so a whole unlanded lap sat six hours behind a bare number.** *What went wrong:* `tools/fixtures/s/stash_record_scan.sh` prints `orphans=19` -- the paths the box holds that nothing outside it carries -- and prints no path. A reader who runs it learns a quantity and cannot act on it, so every lap since `%510` widened the scan has read `orphans=19 verdict=ok` and moved on. Behind that number in this tree stands a complete Bakery lap parked at `2026-09-07 15:32:43`: `tools/l/link_touch_witness.rish`, `tools/fixtures/l/link_touch_scan.sh`, `tools/fixtures/l/link_touch_control.sh`, a `tools/hooks/pre-commit` change and its session log `20260907-145907_the-wall-that-refuses-only-what-this-hand-wrote.kyri` -- 608 insertions across 13 files, and `path_absence_scan.sh` answers `absent` for all three guard paths both here and upstream. *What caught it:* opening `git stash list` by hand at the cold open, as the card's own dead-lap directive says to, and then reading `git stash show --name-only` because the count would not say what it counted. The guard was working exactly as documented -- `unlanded=0` because that gate reads the RECORD, and `orphans` is reported rather than gated on purpose, since a count does not travel between checkouts. *What it taught:* **a count with no names is the second half of a guard nobody runs** (`%360`'s sentence, one field over). Every neighbouring meter in this tree already knows it: `exec_bit` names its files *so the repair is one line away*, `two_rooms_doorway` names its pages, and `process_reach` names its sites in the same commit as this row. The repair is one flag on one scan, and it is left as the next lap's own crux rather than taken here, because `stash_record`'s control builds real repositories with real stashes and re-proving it is a keystone of its own. **The parked lap is recoverable and the recipe is known:** its three new guard files take `git checkout stash@{0} -- <path>` (the TREE, so the mode rides), and its edits to `ITINERARY.md`, `REDS.md` and `standing-equipment.kyri` must be re-applied by hand -- seven peers have written into those three files in the six hours since, and a snapshot of a shared file is a rollback. *Repaired (`20260908.024426`):* **the flag the row asked for already existed, and it was not what was missing.** `sh tools/fixtures/s/stash_record_scan.sh list` has named every orphan since `%510`, and `tools/f/fleet_round_open.sh` already printed that exact command beside the count -- so a hand was never more than one line from the paths. What a hand got when they ran it was **nineteen paths wanting three different actions with nothing telling them apart**, and that is why every ship read past all of them. Sorted by hand this morning: **ten fold shelves** under `construction/archive/`, whose rows come off the living pin, so an unlanded shelf loses nothing and checking one out is a rollback rather than a recovery; **three copies of this guard's own elder self** at `tools/fixtures/f/` paths, from before the letter-room fold moved it to `s/`; and **six files of genuinely parked work** -- a whole unlanded `standing_equipment_yield` guard family, `sow_allow_reach_control.sh`, `agent_jail_control.sh`, `mount_namespace_probe.sh`. Only the six are a lap, and thirteen-nineteenths noise is why nobody read any of it. The scan now classifies each orphan `moved:<path>`, `shelf`, or `work`, prints `orphans_moved` / `orphans_shelf` / `orphans_work` beside the count, asserts the three partition it as `orphan_kinds=partition`, and the round open names the split in its one line. **An exact probe was tried first and rejected on a measurement:** looking each orphan's blob up in `git ls-files -s` is an exact answer where a basename is a strong guess, and it matched **zero of the nineteen**, because a file that moves rooms in this tree is edited on the way -- so the basename ships, with the matched path printed so a reader checks the claim in one `diff`. A basename answered by two files makes no claim at all. **The diagnosis was also measured before it shipped, and the first two shapes were wrong.** Asked per orphan -- one `awk` over a 14,000-path index for each of nineteen -- the open went **1.4s to 3.0s**, a doubling every ship pays every lap for a diagnosis; rewriting the probe as a fixed-string `grep` made it **worse at 4.4s**, since three small processes per orphan beat one larger one only in intuition. The walk defers instead, labelling each orphan `PENDING` and classifying every one of them in a **single** `awk` reading the tracked index and the orphan list together, so the process count stops depending on how full the box is: **1.5s**, and the diagnosis costs about 0.15s rather than the open. Twenty-six control legs on real repositories in a pen, every refusal planted and lifted, ordering asserted where `moved` beats `shelf`; **51 elder legs still pass unchanged**, 77 together. *What it taught, one layer under the row's own sentence:* **a count with names is still a count nobody acts on, if the names cannot be triaged.** The remainder -- landing the six -- stays a lap of its own, and the recipe this row records is unchanged. **CLOSED**
