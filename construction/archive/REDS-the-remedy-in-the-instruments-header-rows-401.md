# REDS %401 -- the remedy that lived in the instrument's header

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260831.113844`, one **BOOKED** row,
to make room for a sibling reading of the same instrument family while the pin stood one byte
under its bound. The row records a repair that was written down, proven, and still did not reach
the hands it was for: `tools/r/remember_git_nib_witness.rish` carried the whole finding in its own
header -- mechanism, two measurements, and the fix -- while `.claude/rules/remember-git-nib.md`,
which is what a sending hand actually reads, still instructed the state that breaks. The send that
booked it applied the repair; bringing the rule and its Cursor twin to the witness's wording is a
rule-pair edit under gate %7 and waits for its own lap. Link depth moved with the text.*


**REDS %401 (`20260831.103836`) -- the repair for a standing fleet-wide double-red was written down, proven, and still did not happen, because it lives in the guard's header rather than in the rule the sender follows.** *What went wrong:* [`.claude/rules/remember-git-nib.md`](../../.claude/rules/remember-git-nib.md) instructs a send to amend once *"so **Git nib** names `git rev-parse --short=10 HEAD`"* -- read before the amend, which produces the **sibling** state: the pre-amend commit. The amend replaces that commit and no push carries it, so it is the one accepted state naming an object no other clone holds. With six bodies reading one anointed card, `nib_honesty` and `remember_git_nib` both refuse on every body but the author's, every lap. *What caught it:* this seat's hot pass -- **109 guards, 106 green, 3 red**, `tree_moved=no`, the third being `rule_twin` (gate %7) -- over a card carrying `43c980c06a`, which `git cat-file -t` calls *not a valid object name* here. **Then reading the guard instead of assuming it.** [`tools/r/remember_git_nib_witness.rish`](../../tools/r/remember_git_nib_witness.rish) already carries the whole finding in its own header: the mechanism, the measurement on two earlier cards (`225d4742e2`, then `c5cfc682e4` five commits later), the phrase *a standing double-red on five bodies out of six, every lap*, and the repair -- *"THE REPAIR NEEDS NO SEAT, because the fix was already inside the three states above"*: write the nib as **HEAD's parent**, read AFTER the final rebase, in the one amend the rule already allows. A parent is by definition a commit the round was built on, so it is already pushed and every clone resolves it. *What it taught:* **a remedy documented only in an instrument's own header is a remedy that instrument's users never receive.** Nothing was unknown here and nothing needed a word; the guidance simply sat where a guard is debugged rather than where a send is instructed, and the rule a hand actually follows still names the state that breaks. That is `docs-implementation-sync` read from the far side: a doc and its instrument agreeing is not enough when they disagree about **which** honest state to aim for. *Repaired here (`20260831.103836`):* this send writes the nib as HEAD's parent, read after the final rebase -- inside the seated convention, inside the guard's accepted states, and resolvable fleet-wide. *Named, not taken:* the rule and its Cursor twin still instruct the sibling state, and bringing both to the witness's own wording is a rule-pair edit under gate %7's care, so it wants its own lap rather than a rider on this one. **BOOKED.**
