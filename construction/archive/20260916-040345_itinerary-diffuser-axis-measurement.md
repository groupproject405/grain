# Diffuser -- the row 7 measurement, folded off the card

**Stamp:** `20260916.040345`
**Room:** checkable -- every figure below is emitted by `tools/a/aurora_placement_witness.rish` and gated by it.
**Status:** Shelved -- folded from `construction/ITINERARY.md` so the living card stays under its 40,960-byte bound.
**Living face:** [`../../active-designing/20260916-033000_the-axis-that-finally-paid.md`](../../active-designing/20260916-033000_the-axis-that-finally-paid.md)

The card carried a third copy of this measurement -- the essay holds it, the roster row holds it,
and the card held it again. A card names the result and the open question; the measurement lives
where a witness emits it. Folded whole rather than condensed, so nothing is lost.

**ROW 7 WAS THE LAST OF THE TWELVE CARRYING NO ERRATUM**, ranked twelfth on *paper until a board
exists* -- and the paper needs no board, because what a wrap is WORTH is arithmetic.
**THE ROW NAMES TWO GRIDS IN ONE CLAUSE AND THEY ANSWER OPPOSITELY.** At **4 cores** the falsifier
cannot fire: a 2 x 2 torus has `(i+1) mod 2` and `(i-1) mod 2` resolving to the SAME NODE, so the
wrap duplicates the direct link and the torus IS the mesh and IS the 4-node ring -- diameter **2**,
average hop **1.000000**, placement **57** on torus and ring alike. At **16 cores** it fires:
diameter **4** against the mesh's **6** and the ring's **8**, average hop **2.000000** against
**2.500000** and **4.000000**.
**AND THE SECOND AXIS PAID, WHICH IT HAD DONE NOWHERE ELSE ON THIS PAGE.** Rows 5, 8 and 11 each
found the torus carrying nothing a ring did not; here the placement costs **286** on the torus
against **478** on a ring of the same node count, worth **0.401674**. The reason reaches past row 7:
those rows laid a torus over a KEYED population, where the key avalanches by design; row 7 lays one
over PHYSICAL CORES, where distance is wire length. **An axis pays where the coordinate means
something** -- which predicts which of the page's remaining torus proposals are worth measuring.
**THE ARTIFACT'S INPUT IS DERIVED RATHER THAN DECLARED.** No tracked file declares a
module-to-module communication weight; the proxy is cross-room `.rye` symlinks -- **67 pairs over
33 rooms, weight 228** -- since Zig refuses an import escaping its root directory. That is a
COMPILE-TIME dependency where a placement map wants a RUN-TIME message count, emitted as
`operand=derived_proxy`.
**AND PLACEMENT IS WORTH COMPUTING AT ONE GRID AND IS CEREMONY AT THE OTHER.** At 16 cores the best
of a bounded search over **27** room orders beats **every** one of 2,000 random placements by
**0.316029** against their mean, held at 20,000 samples across three seeds; at 4 cores it ties the
floor, since 24 placements is a space chance exhausts.
**LANDED:** `class_unread` reads **0** for the first time -- all twelve rows speak --
`rows_with_instrument=4`, `instruments_unrostered=0`. Pen **56 legs, 0 failed**, five mutations
bitten. Paper **A/90** at Field, register 29 pct inside the target.
**THE CONTROL FOUND A REAL FAULT AND I NEARLY READ IT AS A RESULT.** A mutation disabling the room
ordering made the cost FALL, 286 to 201 -- because varying the order also varied WHICH rooms were
placed, so a name order laid four rooms that barely reach each other and scored a smaller number
against a different problem. The set is fixed now and only the order moves. That regression is a
control leg: a moving set scores BELOW the random floor, which is the signature of comparing two
problems rather than two answers. A second leg was thrown out for flakiness: `/tmp` counted before and
after **fell by three** during a reading that created nothing, since eight ships share this pier.
