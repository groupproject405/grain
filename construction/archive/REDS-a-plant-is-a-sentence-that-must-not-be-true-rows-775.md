# REDS -- a plant is a sentence that must not be true

**Language:** EN
**Status:** Shelf -- immutable once written; the living pin at `construction/REDS.md` holds what is still open
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the row closed on a witness that ran on metal

One row, folded `20260916.194200` to keep the living pin under the byte bound its own header
declares. `%775` is the row that found a control's planted citation read by a live gate as a claim:
a pen plant has to NAME a row the ledger lacks, so the plant and the fault it exists to catch wear
one shape, and a meter reading for truth hears a sentence that must stay false exactly as it hears
one asserting it. What it taught outlives its own repair -- **whether a guard should learn to
recognise a plant at all, or whether moving the plant is the whole answer** -- and the same question
arrived from a second direction within the day, when this tree's awk-generator wall reddened on the
plants inside its own control.

Folded by `tools/fixtures/r/reds_fold.sh`, which re-anchored each moved row's relative links one
directory deeper through `tools/fixtures/r/reds_fold_reanchor.sh`. Accrete-never-break: nothing here
is edited.


**REDS %775 (`20260916.113557`) -- a control planted row numbers the ledger has not reached, and the guard gating unshared citations read the plants as claims.** *What went wrong:* `tools/fixtures/r/reds_shelf_resolve_control.sh`, landed the lap before, plants three row numbers in a throwaway pen that must name no real row -- the legs proving the `living-pin` verdict, the `missing` verdict, and the digit-run boundary each need a number the pen holds and the ledger does not. `tools/fixtures/u/unshared_citation_scan.sh` gates living surfaces against citing a `%N` above the anointed spine's `shared_max`, and it reads past exactly three rooms: the ledger, its fold archives, and dated testimony. All three are testimony; a pen plant is none of them. So seven plants read as seven living citations of rows the spine has not bound. *What caught it:* the cold endurance run, `unshared_citation red`, `unshared_citations=7 files_affected=1`. The lap that landed the control could not take the roster lock and read thirteen guards by name instead; this was not among them, and its own new file was the only file affected. *What it taught:* **a plant is a sentence that must not be true, and a meter reading for truth cannot tell one from a claim.** Two repairs stood. Widening the meter's read-past decides whether a plant is recognised by its ROOM or by its ROLE -- the open word `%774` handed Keaton one room over, where a sibling census reads past `tools/fixtures/` as a proxy for *planted* and two living witnesses plant outside it. Moving the plant needs no law, and this lap took it: the digits carry no meaning in a pen, so they take a five-digit run, which that scan skips on the same argument by which `%200000` in two image modules is a colour rather than a row. A plant outside the shape also survives a renumbering sweep, which is the second reason to keep it there; the control carries that reason on its face so a later hand cannot shorten the digits back. *What this does not reach:* a plant whose number sits BELOW `shared_max` reads as an ordinary citation and always will. The scan cannot tell a plant from a claim, which is exactly why the repair moved the plant rather than teaching the meter. *And the law page describing this reproduced it inside the hour:* the first draft of the clause added to [`.claude/rules/derived-spine.md`](../../.claude/rules/derived-spine.md) spelled the three elder plant numbers while explaining why they had to move, and the gate read three fresh unshared citations off a rule room. The numbers are named in the control and not on the law page now. That is `%769`'s shape one instrument over, where a card account spelled the retired word while describing its own repair -- **a page teaching a refusal is a page that must obey it**, and a gate is the only reader that notices. **CLOSED** -- control 32 legs 0 failing, `reds_shelf_resolve` GREEN, `unshared_citation` GREEN at `unshared_citations=0`, `tracked_link`, `fold_shelf_link`, `fold_shelf_link_repoint`, `reds_fold`, `reds_citation`, `reds_spine_derive`, `reds_ledger_headline` and `reds_ledger_monotone` GREEN on metal.
