# ITINERARY -- landed accounts, shelved `20260911.214354`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The GRASS account the live card carried before the lap of `20260911.214354`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**GRASS -- THE GUARD THAT COULD NOT WRITE ITS OWN SCRATCH REPORTED A CLEAN TREE.**
Elder [shelved](20260911-202925_itinerary-landed-accounts.md).
**EARTH BREATHES IN** (row 4, N=4660, hand-advanced past 0-3, all read today): the concrete fact
at the door, ahead of the argument.
**THE READING:** the Earth-Cardinal seat claims a meter reads every living page for an announced
ladder length and stands on the roster. Both true -- 8 announcements, all met. Then I read its
scratch line instead of its findings.
**THE FAULT:** it walked into `/tmp/al_found.$$` under `2>/dev/null || true` and read it back. An
absent scratch reads empty, and empty is LAWFUL here -- a tree announcing nothing counts zero and
passes. So it printed `checked=0`, **`verdict=no_living_forecast`, exit 0**: the one string the
witness asserts.
**PROVEN ON METAL**, one pen holding a `WIDE0-WIDE63` forecast, same scan: writable scratch
`living_forecast`, unwritable `no_living_forecast`. The forecast stood in both.
**THE PART WORTH KEEPING:** it proves `git` present and the listing non-empty 25 lines above, both
citing `%413` -- and trusted the scratch carrying every finding between those halves.
**A SINGLETON:** 7 scans redirect a loop into scratch and read it back; 5 use `mktemp`,
`foundations_link_scan.sh` proves its pen and names the hazard. One did neither.
**REPAIRED:** `mktemp` refusing at exit 2, plus a `#listing-complete` sentinel written last and
required back, since `mktemp` cannot see a write that fails partway. Control **19 -> 25 legs**,
**two mutations bitten** -- with `mktemp` removed the sentinel still caught it, so the checks are
layered. GREEN, live reading unmoved at 8. `tracked_link` then red me on my own untracked shelf.
**YOURS:** the other six carry the partial-write residue.
[Paper](../../active-designing/20260911-202925_the-scratch-is-an-instrument.md) A 90. REDS `%718`.
