# REDS -- a control landed proven and unrun

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row closed on a witness that runs the control it names
**Folded:** `20260916.051438` from [`../REDS.md`](../REDS.md)

One CLOSED row, folded to hold the pin under a bound eight ships share. It teaches that a proof can
land complete and be heard by nobody: a sixteen-leg control shipped beside the wrapper it proves,
no witness named it and no roster row reached it, so it passed review, read like coverage, and
reported nothing on the day the wrapper changed. The meter for that class already stood, and what
spoke was its ceiling, three commits after the fault landed. The cure is a witness that runs the
control, asserts each leg by name, and counts the legs off the control rather than reciting a total.


**REDS %750 (`20260915.202715`) -- a control landed proven and unrun, and the meter that counts unrun proofs refused.** *What went wrong:* `tools/fixtures/r/rye_build.sh` and its ten-leg `tools/fixtures/r/rye_build_control.sh` landed together at `ee2261c28` answering `%734` -- two builds over one module directory delete each other's `.zig` shadows mid-compile, so the wrapper takes a lock inside that directory through `lock_acquire`. No witness named the control and no roster row reached it, so nothing ran it: a proof that passes review, reads like coverage, and reports nothing on the day the wrapper changes. *What caught it:* `sh tools/fixtures/u/unheard_guard_scan.sh`, whose `control_unheard` reading rose to 19 against a ceiling of 18 and answered `verdict=over_control_ceiling`. *What it taught:* **the meter for this class already stood, and the ceiling was the thing that spoke -- three commits after the fault landed.** `%357` booked the same shape from the other side. `tools/r/rye_build_witness.rish` runs the control, asserts each leg by name beside `faults=0`, and counts the legs off the control rather than reciting a total. One leg was added with it: the held-lock refusal stood proven only in the affirmative direction, so the control copies the wrapper with its `lock_acquire` call traded for `true` and asserts the refusal STOPS. *And the witness landed a lap later than the control it runs, which is this row's second half:* `10fcae2878` widened the control to sixteen legs -- the cross-room readings, since a source importing across a room writes its shadow beside the IMPORTED file, plus a room bound that refuses rather than truncating -- and those five stood unrun exactly as long as the ten beneath them. `control_unheard` reads 18 at a ceiling of 18. **CLOSED** on `tools/r/rye_build_witness.rish` GREEN, sixteen legs counted off the control, the mutation biting.
