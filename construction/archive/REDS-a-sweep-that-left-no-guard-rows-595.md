# REDS -- a sweep that left no guard, and the fault that regrew in a day

**Folded:** `20260907.225349` -- **Status:** Archived, complete, never edited
**Rows:** `%595`
**Living ledger:** [`../REDS.md`](../REDS.md) -- **Recital:** [`REDS-fold-recital.md`](REDS-fold-recital.md)

*One row, and what it taught: a hand measured a filing fault across two rooms, moved all 34 of it,
repointed 73 living citers, and shipped it green -- and rostered nothing to read the rule
afterward. Five entries had regrown into the wrong room by the next day, every one written by a
hand that had read the law. A census repaired by hand regrows at the rate its tree writes files,
so the second firing is what earns the loom.*

---


**REDS %595 (`20260907.225349`) -- a hand swept 34 misfiled tools in one lap, left no guard, and five had regrown by the next day.** *What went wrong:* `tools/` and `tools/fixtures/` fold by first sprig letter, and the whole value of that fold is that a path is a pure function of a name -- `tools/t/tool_path_resolve.rish` recovers a stale reference by COMPUTING the room rather than consulting a table. On `20260906.182719` a hand measured 26 misfiled entries under `tools/` and 8 more under `tools/fixtures/`, moved all 34, repointed 73 living citers, and shipped it green. Nothing was rostered to read the rule afterward. Measured `20260907.221559`, one day later, **five entries stood in the wrong room again**: `tools/r/unshared_citation_witness.rish` and its scan and control under `r` where the name says `u`, and `tools/fixtures/l/retired_word_scan.sh` with its control under `l` where the name says `r`. Every one was written by a hand that had read the fold law. *What caught it:* a census run by hand this lap, asking the letter question of both rooms -- and then, sharper, the new scan itself, which read the INDEX while a `git stash` round trip had left the index holding the five elder paths and named all five before a `git add -A` cleared them. *What it taught:* **a census repaired by hand and left unguarded regrows at the rate its tree writes files** -- the doorway census's shape one room over (`20260907.192800`), and the second firing that earns a loom. A misfiled entry is never lost, since the resolver's fourth reading finds it by basename across every tracked file; what it loses is the THIRD reading, the pure computation that needs no index and no memory of which files moved, which is the one a stale reference in dated testimony depends on forever. *Landed (`20260907.225349`):* the five moved and every living citer repointed; `tools/fixtures/t/tool_letter_room_scan.sh` reads every tracked entry sitting directly under a one- or two-character room and holds `misfiled` at zero, judging the entry DIRECTLY under the room rather than the leaf basename -- which is what lets `tools/m/mind-bin/git` pass honestly with no exemption list, where the elder census reported it as a mismatch it could never stop reporting. It reads the index rather than disk, so a lap's untracked scratch is invisible and a staged file is judged on the lap it arrives. `tools/t/tool_letter_room_witness.rish` gates it at `tier lap`, because the fault is born with a file and files are born every lap. Twenty-five behaviors in a real git pen, every refusal planted and lifted, the ceiling proven from both sides, and a tree with no letter room answering `no_rooms` rather than a clean zero. **CLOSED**
