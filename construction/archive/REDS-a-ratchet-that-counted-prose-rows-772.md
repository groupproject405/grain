# REDS -- row %772, folded from the living pin

**Folded:** `20260916.114500` -- **Status:** Archived, complete, never edited
**Living pin:** [`../REDS.md`](../REDS.md) -- **Law:** [`.claude/rules/reds-first.md`](../../.claude/rules/reds-first.md)

*One row that counted a sentence as a build.* `tools/fixtures/b/build_target_scan.sh` read every
emit site out of a guard's source line by line, without asking whether the line was code. A guard's
comment often spells the shape of the command it drives, so a placeholder inside a doc comment read
as a call, and the paragraph that explained what a build receipt is raised the ratchet past its
ceiling.

Two readings inside it carry further than the repair. The population held **three** placeholders and
the scan had been wrong about all three for its whole life, one of them inside the very witness that
teaches the escape -- so the instrument was miscounting its own instruction, and only slack in the
ceiling kept anybody from meeting it. And the ceiling did not fall: it stood at 47 while two of its
number were phantoms, a peer's real arrival landed in the same six commits the round rebased onto,
and the repair freed exactly the room that arrival needed. A total that cannot say which of its
members are real is a total a reader cannot act on, which is why `emit_prose` prints beside it.

What the row leaves behind is a sentence about ratchets generally: **a ratchet counting prose will
eventually be raised by a sentence.** A reading that classifies rather than discards is what tells a
line the scan never saw from one it saw and dropped.

**REDS %772 (`20260916.092127`) -- a guard counted the placeholder in its own prose as a build target, and one doc comment broke a ceiling.** *What went wrong:* `tools/fixtures/b/build_target_scan.sh` extracts every emit site with `grep -oE '\-femit-bin=[^" ]+'` over a guard's source, line by line, without asking whether the line is code. A guard's comment often spells the shape of the command it drives, so `rye build -femit-bin=<path>` in a doc comment reads as a call. *What caught it:* the hot endurance run of the lap that WROTE one. A paragraph added to `tools/b/built_tool_freshness_witness.rish` explaining what the receipt is took `emit_fixed` from 47 to **48** against a ceiling of 47, and `verdict=fixed_over_ceiling` refused a round whose only new build was in a throwaway pen. *What it taught:* **a ratchet counting prose will eventually be raised by a sentence.** The population held **three** placeholders, and the reading had been wrong about all three for as long as it has run -- `port_runner_lock`'s `<target>`, this lap's `<path>`, and, best of all, `build_target`'s OWN witness naming `-femit-bin=$d/<name>`, the escape this very scan recommends. So the instrument was miscounting its own instruction, and the only reason nobody met it is that the ceiling had slack. *The repair:* a target carrying `<` or `>` is a placeholder rather than a path, classified `prose` before the variable walk and counted in its own reading, `emit_prose`, rather than dropped -- a reading that silently discards a line cannot be told from one that never saw it. On the tree this landed on, `emit_fixed` reads **49 under the elder rule and 47 under this one**, `emit_pen` 7 to 6. *The ceiling does not fall, and that is the sharper finding:* it stood at 47 while two of its number were phantoms, a peer's real arrival landed inside the same six commits this round rebased onto, and the repair freed exactly the room that arrival needed. So 47 now bounds 47 builds into fixed tree paths where it bounded 45 and two sentences, and nothing in the total could have told a reader which. `emit_prose` prints beside it for that reason. One control leg added, `legs_run` 33 to 34, proven from both sides: the leg reads 1 with the classification and **0** with it removed, and the ceiling refuses one over and welcomes at its seat. **CLOSED.**
