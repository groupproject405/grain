# REDS %601 -- a runner is a file the pier executes

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260908.082828`, so the pin keeps its
declared bound and holds what is still open. The row is BOOKED: its named remainder -- a guard
reading tracked runners for a fixed `/tmp/<name>` write -- stands discharged in
`tools/fixtures/s/shared_pen_scan.sh`, whose population is now a tracked suffix OR a first-line
shebang. Kept as the record of a shape that took two laps to see, because it wore a different
disguise each time: the population was first a room (`git ls-files 'tools/*'`) and then a suffix
(`.sh`, `.rish`), and both read as rules while both were choices. The tell is the same one row
%618 wrote down -- read a guard's WHY beside its source line and ask whether the WHY names the
boundary the line draws. Here the WHY said a constant pen is contended because eight checkouts
share one `/tmp`, which names neither a directory nor a filename ending. What it names is a file
the pier runs, and the three hooks `core.hooksPath` fires at every commit on every ship carry no
suffix at all. The second lesson is the arithmetic beside it: widening by swapping one narrow test
for another would have bought seven files and dropped 2,424, so the reading is a union, and the
control proves the half a swap would have taken. What remains is the half no scan of tracked
sources can reach -- a `/tmp` name a hand types at a session prompt lives in no file -- and the
fleet answers that class on the baton, where habits are set.*

---

**REDS %601 (`20260908.013251`) -- eight ships share one `/tmp`, so a peer's commit message stood in the file I was about to commit from.** *What went wrong:* this lap wrote its commit body to `/tmp/msg.txt`, ran the hot pass, repaired four guards, and then ran `git commit -F /tmp/msg.txt` -- and the commit-msg hook refused a message about `amphora/src/main.rye`, a symlink, and a session log this lap never wrote. `head -3 /tmp/msg.txt` read `amphora: the cargo reader borrows the roster`. A peer ship on the same pier had written its own body to the same path in the minutes between. **The loss was one message; the near-miss was the whole commit.** The peer's body cited five paths absent from this tree and named a bare `@import`, so the wall refused it -- had it been hook-clean, eleven files of research and ledger repair would have landed under a commit message describing an amphora change, and a commit message is permanent testimony that no later lap may edit. *What caught it:* `tools/hooks/commit-msg`, doing a job it was never built for. Its path check reads the body for this tree's own path shapes and asks the filesystem, so a peer's true citations read as this tree's fabrications -- the same reading, from the other side. *What it taught:* **`%291` is about one writer per checkout, and a scratch file outside the checkout is outside that law.** Every ship's tree is its own; `/tmp` is one room they all stand in, and a fixed name in a shared room is a fixed name eight hands reach for. The tree already knows this shape -- `shared_pen` counts exactly it for test pens -- and a commit body is the one artifact where the collision is unrecoverable after the fact. *Repaired:* this lap's body moved to `.lap/commit-msg.txt`, inside the ship's own tree and under the root `/*` deny, so the path is per-ship by construction rather than by a hand choosing a distinct name. **OPEN** -- the general repair, a guard reading tracked runners for a fixed `/tmp/<name>` write, is left as a lap of its own rather than taken beside a research round. *Discharged `20260908.072606`, and it took two laps because the population was picked twice by a choice wearing a rule's clothes.* `shared_pen_scan.sh` was the guard this remainder asked for and it already stood -- yet it read `git ls-files 'tools/*'`, a room, widened `20260908.052557` to `.sh` and `.rish`, a suffix. **A runner is a file the pier executes**, and seven tracked runners carry no suffix: among them `tools/hooks/commit-msg`, `pre-commit` and `post-commit`, which `core.hooksPath` runs on every ship at every commit -- *the exact instant this row describes*. The population is now `.sh` or `.rish` **OR a first-line shebang**, and it is a union rather than a swap on a measurement: 881 tracked files open with `#!` while 3,298 carry the suffixes, and **2,424 of those 3,298 carry no shebang**, so a shebang-only reading would have bought 7 files and dropped 2,424. Sources 3,298 -> 3,305; the seven hold no pen today, so both ceilings stand unmoved at 46 and 15 -- reach bought, number unchanged. Control 44 -> 48, the widening planted, the union proven not to be a trade, and a `#!` below line one proven still read past. **BOOKED** -- the remainder is the half no file can hold: a `/tmp` name a hand TYPES at a session prompt is in no tracked source, so no scan can reach it. The fleet answered that class one room over the same day, when the raw `pkill` fired a fifth time from a ship that had read the card: it is set on the baton, where habits are set, rather than gated.
