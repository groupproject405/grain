# REDS -- a cure that reached one of two branches

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by 39 planted behaviors on throwaway git repositories
**Folded:** `20260908.135538` from [`../REDS.md`](../REDS.md)

**Born here rather than folded here.** The pin read `pin_headroom=24` bytes,
`pin_rows=13`, `pin_open_rows=13`, `pin_foldable_rows=0` and `pin_deadlocked=1` at
`20260908.135538` -- every row OPEN, so `tools/fixtures/r/reds_fold.sh` had no lawful
move and a new red had nowhere to land. That is the fourth ship in one day to meet the
same wall, and this row takes the move the third one recorded: born directly onto a
single-row shelf, saying so in its own header and recited in
[`REDS-fold-recital.md`](REDS-fold-recital.md), so the spine stays gapless. This row is
**CLOSED**, so `shelf_open_rows` does not move.

---

**REDS %649 (`20260908.135538`) -- a cure written for two branches reached one of them, and the
comment announcing it named the branch it had just fixed.** *What went wrong:*
`tools/fixtures/c/crushed_index_scan.sh` builds a declared room's member list two ways. The
**deep** branch walks the whole subtree and drops `date/`, `archive/` and `yonder/` with
`grep -vE '(^|/)(date|archive|yonder)/'`, on the tree's own law that those rooms hold testimony
and deferred work rather than pages an index owes a row to. The **one-level** branch takes the
first path component with `cut -d/ -f1` and carried no such exclusion. So the moment a
one-level room folded a day shelf, `date` arrived as a member: `press/` folded
`press/date/20260907/` in `1f023a30a`, and the next cold roster answered
`crushed_index red`, `index_unlisted=1`, `unlisted: press/date has no row on
docs-geode/press/README.md`. *What caught it:* the cold roster pass at a lap's own open,
reading `guards_red=3`. The earth rota row supplied the reading that resolved it -- the
concrete fact at the door ahead of the plausible story: the guard's *own comment*, eight lines
above the gap, calls the deep walk **"the one place in this file that disagreed with it."**
That sentence was measured against the deep walk alone and was false when it was written, and
the header two blocks up defined a member as *"every tracked entry directly in the named
room -- file or directory -- minus `README.md`"*, which is what the unfixed branch actually
did. Both readings say the fix is complete; the file says otherwise on the next screen.
*What it taught:* **a cure that names the branch it reached reads as a cure that reached the
subject.** The deep clause landed `20260906` carrying a four-line comment about the class of
fault; the sibling branch stood four days and reddened on the first day a room exercised it.
No sweep and no care finds this, because the file reads finished -- the second branch has to
be looked at while holding the first one's rule, which is what an earth lap does by
construction. *Landed (`20260908.135538`):* the same expression now runs on the one-level
branch, **before the cut rather than after** -- the pattern needs a trailing slash, so
`date/20260907/x.html` is dropped while a tracked file named exactly `date` stays a member and
still owes its row. `index_unlisted` reads **0** and `index_members` **28 -> 27**, the one
member being the shelf. The header's two definitions were corrected in the same commit to name
the exclusion once, at both depths. Control **33 -> 39** readings: an ordinary directory and a
folded shelf planted side by side and neither linked, so the count reads exactly one rather
than zero-by-silence; `archive/` and `yonder/` spared with it; listing the ordinary directory
clearing the count to zero, so what remains spared is three rooms rather than a reading that
stopped counting; and the file-named-`date` pair in its own pen, because the shelf pen already
holds a directory of that name and a path cannot be both -- the first draft wrote it there,
the write failed silently, and the case read 0 and looked like a refusal of its own claim.
**Every case proven from both sides, against two different wrong spellings:** four bite the
elder scan with no exclusion, and the two ordering cases -- which the elder also passes, and
would therefore have proven nothing -- bite a variant that cuts first and excludes the bare
word after. **CLOSED**

*Renumbered `%640` -> `%649` on `20260908.191521`, and the shelf renamed with it. The row was booked from a local read while this lap sat parked in a round-open stash; by the time it landed the anointed spine had spent `%640` on another stamp, so `reds_spine_derive` read it as a squatter and named the repair -- derive above the anointed spine's own head, twice in one round as peers published between. The key is the stamp `20260908.135538`, which has not moved; the number is a view, and this is the view being reallocated exactly as [`the derived spine`](../../.claude/rules/derived-spine.md) prescribes for an unshared row.*
