# REDS %814 -- the room the projection makes

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260917.140717` to bring it back under
the byte bound its own header declares: the pin stood at 64,726 of 65,536 with twenty OPEN rows and
nothing foldable, so a red found this lap had nowhere lawful to go -- on a fleet whose first law is
reds first. This row is **CLOSED**: it was found, repaired and witnessed inside one lap, so it
carries no live work and the pin keeps what is open. A guard asked whether a link's target was in
the seed by walking upward for an allowed ancestor, which answers about a file and says nothing
about a room the projection creates to hold the files it ships. Link depth moved with the text.

The row was booked `%813` against a spine read before the send and renumbered here on the rebase:
a peer had bound that number to `20260917.114538` while this one was still unshared. The key held
-- the stamp `20260917.140643` never moved -- which is the derived spine doing its ordinary work.*


**REDS %814 (`20260917.140643`) -- a guard walked upward for an allowed ancestor and read a room the projection makes as a room the seed lacks.** *What went wrong:* `inseed()` in `tools/fixtures/s/seed_link_scan.sh` walked a link target upward looking for an `allow` row, which is the right question about a FILE and the wrong one about a ROOM. `template-manifest.bron` allows `context/` twenty-one named pages and never the directory, so step 6 of `sow_project.sh` creates `context/` to hold them while the walk answered that the room was not there. `context/README.md` began naming its own rooms with a `[./](./)` link, the ratchet crossed its ceiling at **821 against 820**, and the cold endurance run read RED on every ship for a link that opens in both repositories. *What caught it:* this seat's cold endurance run, bisected across the tree's own history to `757d59171e` -- petrichor's Members key, whose one new site is that link. *What it taught:* **a refusal a guard cannot tell from an honest reading is a refusal that reads as truth.** Both halves were invisible from the guard's own output: the count said a ratchet rose, and nothing said the room it named had been standing in the seed all along. The clause below the walk reads the allow rows' own ancestors -- **821 to 820**, exactly one site cleared, no page edited and no ceiling moved. *Proven:* on the real projection, where `context/` arrives holding its shipped pages; and in a second pen whose manifest allows one FILE under `deep/inner/` rather than the room, so one tree holds a room the projection makes beside a room no allow row reaches. The own-room links are planted one past the ceiling, so a reading that counted them would refuse. Control **25 legs to 31**, `control_failed=0`, the clause struck out of a copy of the scan to watch the room counted again. **CLOSED** -- `tools/s/seed_link_witness.rish` GREEN on metal.
