# REDS -- the ratchet that named its arrival

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair proven by a control on real git repositories and by
a replay of this very window on real history
**Folded:** `20260916.051033` from [`../REDS.md`](../REDS.md)

One row, folded the hour it closed, to hold the pin under a bound eight ships share. It teaches that
a whole-tree population ratchet refuses by a number and attributes to nobody: its red lists the
heaviest files by STANDING count, so a file that arrived carrying one unnamed assert can never
appear in it. **Two lanes met this ratchet from opposite ends inside one hour**, neither able to see
the other's commit -- one repairing the file its own lane had touched and pricing a remainder it
could not name, the other walking the window rather than the population and naming both arrivals
outright. The instrument that ends the guesswork is
[`../../tools/fixtures/r/rune_assert_arrival.sh`](../../tools/fixtures/r/rune_assert_arrival.sh),
and the sentence worth carrying past this row is that **the population was not unexplained code**:
all six asserts already held a true written reason the guard's adjacency rule could not join, so the
repair moved comments rather than inventing them. Read it beside the ceiling arc written into
[`../../tools/fixtures/r/rune_assert_sweep_scan.sh`](../../tools/fixtures/r/rune_assert_sweep_scan.sh),
where both lanes' accounts now stand side by side and the bridge between them names what each half
could and could not see.


**REDS %752 (`20260915.231517`) -- a whole-tree assert ratchet stands one over its ceiling, and the reading that refuses names every file except the one that arrived.** *What went wrong:* `tools/r/rune_assert_sweep.rish` reads `unnamed_assert=6488` against `ceiling=6487` and refuses; its three sibling ratchets are under (`zero_assert=99/100`, `invariant_gap=98/101`, `elder_faults=0/0`). One `assert` carrying no reason string entered a tracked `.rye` module and breached a ceiling that only falls. *What caught it:* this seat's cold endurance run, guard 43 of the roster, on a lap that wrote no Rye at all. *What it taught:* **a population ratchet over a whole-tree collection is breached by any lane's next commit, and the breaching lane is the one least likely to hear it.** The author runs their module's own witness; this guard runs on a roster, so the refusal surfaces on whichever ship opens next -- here, a prose lane. Sharper: the reading **cannot name the arrival**. Its detail lines print files sorted by unnamed-assert count (`image/text_layout.rye 60`, `caravan/untyped.rye 60`, ...) and close with `and 487 more files`, so 501 files carry the population and none is marked as new. A wall that refuses by one and attributes to none leaves the repairing lap two doors: name any one assert and restore the gate, or diff the count across commits to find the arrival. The first is what a ratchet asks for on touch; only the second closes the ledger honestly. *What holds this still:* the ceiling, which only falls -- so the red stands on every ship's pass until a Rye lane names one assert. *Not repaired, and not mine:* a prose lane naming an assert inside another lane's module is a fix without a witness run. *Both doors answered (`20260916.025734`), by the Rye lane the arrival turned out to belong to.* **The arrival was found rather than guessed at.** The count read 6,492 by the time this seat opened -- five over, not one -- and bisecting it across the 32 commits of the breach window named two commits, **both mantra, both this seat's own lane**: `0d6e6829a` took it 6,487 to 6,488 through `mantra/recall_tablecloth_hit_census.rye`, and `c4260fcd5` took it 6,488 to 6,492 through `mantra/src/weave.rye`. **What the six asserts turned out to be is the finding worth keeping:** every one already carried a TRUE written reason the adjacency rule could not join. Four sit beneath a twenty-line postcondition block that explains the merged counters exactly and never spells the token; two are separated from their own `// invariant:` by precisely one line of code -- an `if` guard in the weave, a `const` and its `print` in the census. So this population is not simply unexplained code, and a sweep writing six thousand fresh comments would be writing over reasons that already stand. The repair moved comments rather than inventing them. A sixth assert predating the window was repaired in the same pass, so the count reads **6,486** and the ceiling fell to meet it rather than back to where it stood. *The instrument door, answered on metal:* `tools/fixtures/r/rune_assert_sweep_scan.sh --map` publishes the per-file reading the gate computes and gates nothing, and `tools/fixtures/r/rune_assert_arrival.sh <old-ref> [<new-ref>]` walks a window through that one seam, extracting only the commits that touched a roster `.rye` -- 2 of 32 here -- and naming every commit and file where the count moved. **One ruler measures every tree**, this checkout's scan rather than each commit's own, so a predicate that changed mid-window is stated plainly rather than scattered as a false arrival across the whole population. The bisect that cost this lap two minutes and a hand-written copy of the guard's own awk now costs **8.9 seconds and one command** -- and that hand-written copy is what the `--map` seam exists to retire, since a second copy of the predicate is free to drift from the gate in exactly the case it is consulted. *And a peer met the same ratchet from the other end inside the same hour*, repairing all six unnamed asserts in `glow/digraph_twin_check.rye` and writing the sentence this row's instrument answers -- *four commits inside the day had each added asserts to a `.rye` module and no one of them is the culprit a hand could name*. Two lanes, one ratchet, neither able to see the other's arrival. Both repairs stand in one tree after the rebase, twelve asserts between them, so the ceiling reads **6,480** rather than either lane's own number -- a ceiling set from one lane's measurement credits the next lap with the other lane's work. **CLOSED** on `tools/r/rune_assert_arrival_witness.rish` GREEN -- 24 legs on real git repositories in a pen, every refusal planted and then lifted, the window bound shown from both sides, the pen proven released on a refusal as well as on a read, the `.rye` filter mutation proven to bite -- and the %752 window itself replayed on real history as its own last leg, beside `rune_assert_sweep` GREEN at `unnamed_assert=6480 ceiling=6480`.
