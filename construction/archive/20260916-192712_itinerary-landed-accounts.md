# ITINERARY -- landed accounts, shelved `20260916.192712`

**Language:** EN
**Status:** Shelf -- immutable once written; the live card holds what is OPEN
**Style:** Gauge, Meter
**Voice:** Kyri
**Room:** Checkable -- the account moved whole, its links re-anchored by the writer

The INCENSE account the live card carried before the lap of `20260916.192712`, moved here whole so the live
front holds one account per ship. Accrete-never-break: nothing here is edited. Written by
`tools/i/itinerary_account_shelf.sh`, which ran every relative link one directory deeper
through `tools/fixtures/r/reds_fold_reanchor.sh`.

---

**INCENSE -- THE GUARD READ BOTH HALVES OF ONE FILE AND GATED ONLY THE HALF THAT NEVER MOVED.**
**AETHER HEARS** (row 0, N=5121): listen for the silence where a claim used to be. The claim was
printed on every lap of every ship, and the silence was the comparison standing next to it.
**THE RED.** `tools/fixtures/d/declared_model_scan.sh` has read `.claude/settings.json` for BOTH
`model` and `effortLevel` since it was written, printed `declared_model=` and `declared_effort=`,
and passed the effort value to **no comparison at all**. So the one field it measured and never
gated is the one that drifted: `.claude/rules/session-logs.md` said that file configures `max` while
the file read `medium`, its own sibling `.claude/rules/session-log-provenance.md` said `medium`
correctly the whole time, and the seed's section 0 and this family's own USAGE comment said `max`
beside them. **Six living sites quote that file; four of the six were wrong**, counting each law
page's Cursor twin. A meter that prints a number it never compares teaches a reader the number is
watched.
**THE CARE THE REPAIR TOOK.** The effort is legitimately **not one value**. `tools/f/fleet_lap.sh`
execs `--effort max` inside the enclosure while `tools/f/fleet-loop.sh` execs `--effort medium`
bare, which this pier runs, so `GLOW_PROFILE.template.kyri` at `effort max` and
`docs-geode/tutorials/running-the-fleet.md` tabulating both are each right. A single-value gate
would have been false to the tree and would have reddened honest pages. The second roster names
**six pages that state what `.claude/settings.json` ITSELF carries**, gated by presence of the
driver's value -- the model half's own presence-over-absence discipline, so a page teaching the
split stays free to name both efforts in one sentence. The template sits deliberately OFF it, named
as such in the scan's header.
**AND THE CONTROL LEARNED TO ANSWER FOR ITS OWN LEGS.** Every reading prints through one `leg`
function that tallies it, and `control_verdict` reads `leg_failed` when any leg does. That fired
inside the hour: the new roster reddened leg 4, whose pen page sits on both rosters, and the control
still printed `ok`. **PROVEN:** `declared_model` GREEN on metal, control **16 legs to 26, 0
failing**, two mutations each caught at the VERDICT rather than at a named key -- dropping
`effort_over` from the gate, and reading a baked `max` in place of the driver's value.
**THE PIN HAD 934 FREE BYTES** against a row whose measured mean is 2,355, so no lap could book a
red at all. `%773` and `%777` folded whole to their own shelf, both CLOSED, both teaching one
thing: a guard reads the artifact it was built to read, and the promise that goes stale is the one
standing beside it.
**YOURS:** `GLOW_PROFILE.template.kyri` declares `effort max` while the pier it ships with runs
`medium`. That is a template describing a clone rather than the settings file, so it is honestly off
the new roster -- and a fresh clone reads it first. Should the template follow the pier, or does it
declare the effort an unattended run SHOULD take?
**MINE:** the scan reads presence, so a page naming `medium` about the settings file and `medium`
about a launch path passes on either sentence. Reading which claim the value belongs to needs the
sentence rather than the page, and I have not opened that lap.
