# REDS -- a refusal nothing performed

**Language:** EN
**Style:** Gauge, Meter setting
**Voice:** Kyri
**Status:** Shelf -- one folded row, immutable once written
**Room:** checkable -- a ledger row, its repair closed by a witness on metal
**Folded:** `20260908.052550` from [`../REDS.md`](../REDS.md)

One closed row, folded the same hour a peer folded its two neighbours, to hold the pin under a bound
eight ships share. It teaches that a case can print a refusal the code beneath it never performs: the
market floor signed with a constant seed written in its own source, so any reader could sign a
listing `verify_listing` accepts, and the case that claimed to prove refusal had only flipped a byte
-- which proves integrity instead. **No meter reads a sentence like that**, since a true sentence
about a real case reads green from every side. Read it beside
[`REDS-fold-recital.md`](REDS-fold-recital.md).


**REDS %608 (`20260908.035300`) -- the market floor printed a refusal it never performed, because the key that would make lying possible is written in the source.** *What went wrong:* `mandi/mandi_core.rye` derives its signing keypair from `listing_seed = .{0x67} ** 32`, a constant in the file itself, so every reader of the tree can regenerate it and sign a listing `verify_listing` accepts. The lap's unwelcome case flipped one hex character of `stamp_sig`, named itself `lying signature`, and printed `mandi: lying listing refused`; `tools/m/mandi_lap1.rish` asserted that exact string, and `mandi/README.md` carried `Lying listing refused` on its floor-view line. A flipped signature byte is a corruption, so what the case proved was integrity. Nothing in the room could refuse a well-formed false price, and nothing said so. The same shape stands at seed 0x68 in `listing_settle_core.rye`, 0x66 in `granary/exchange_listing.rye`, and 0x69 in `granary/weave_settle_core.rye`. *What caught it:* a Civic reading of the mand* surfaces under the earth rota -- asking what the floor rewards -- then reading `sign_listing` and `verify_listing` beside the case that claimed to break them. No meter reads this: a witness that prints a true sentence about a case it really ran is green by every standing guard, and the gap is between the sentence and what the case demonstrates. *What it taught:* **a refusal is named for what it refuses, or it teaches the wrong lesson to everyone who reads it.** *Repaired (`20260908.035300`):* the case is `tampered signature refused`, and beside it a new case signs a second listing for the same vessel at another price and asserts it VERIFIES -- printing `mandi: forged listing accepted -- integrity proven, authority not`, which `mandi_lap1.rish` now asserts, so the bound is a standing measurement rather than a caveat that can drift. Both module heads and the README name what a verified body proves and what it does not. *The second layer, in the same lines:* the `20260728.044002` rename-forward carried **MUR (was MALA)** through the specs and all four Mandi witnesses and reached no module; the `20260813` relabel pass then rewrote `mandi_core.rye`'s first line and left MALA standing in it. Seven lines swept here on touch; **27 remain across 14 module files** (`amphora`, `comlink`, `granary`, `linengrow`, `pond/apps`), measured `20260908.033800`. `commerce_steward_demo.rye` also read *tip 419* for the landed edge, which is **nib**. GREEN: `mandi_lap1`, `mandi_listing_settle`, `commerce_steward_demo`, `tame_style_check`, `width-check`. The two Skate view witnesses are gated on this pier -- `pond_build_drawn_terminal.sh` reads `gated_no_display`, absent `wayland-client`, which stood before this lap. **CLOSED**
