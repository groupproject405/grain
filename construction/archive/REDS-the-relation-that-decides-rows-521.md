# REDS %521 -- every bound relation this lane could declare was an equality, and the ones that decide are orders

*Written straight to a shelf on `20260906.210908`, in the round that recovered it out of the
dead-letter box -- the row runs 3,483 bytes and reads CLOSED, so the living pin
[`../REDS.md`](../REDS.md) keeps what is still OPEN and this shelf keeps the account.*

*The sentence worth carrying forward: **a guard that can only say `=` reads green across every gap
that is a `<`.** `amphora_bounds_agree` had three readings -- same-name roofs, alias value groups,
declared `/// couples:` -- and all three compare for equality, so the two relations that actually
decide whether Amphora refuses a pour could not be written down at all. A season door welcomed a
512-character name into a listing contract holding 200, and a manifest roof was declared equal to
the cargo roof it must exceed by three bytes a line. Both refused honestly; both refused **late**,
at a wall nothing had declared.*

*This package stood in the round-open stash of `20260906.194237` with no session log beside it, so
`stash_record` -- whose subject is records -- could not see it. `%510` went to a peer while it
waited, which is the third number this lane lost to the same cause in one day and the reason the
card carries the question about whether an OPEN row should claim a seat and a stamp at start.*

---

**REDS %521 (`20260906.193823`) -- Amphora's season door welcomed a name the record describing it cannot hold, because every bound relation this lane can declare is an equality and the ones that decide are orders.** *What went wrong:* `amphora/src/main.rye` carries two ceilings on one quantity -- a season file's name -- and the wider one is the one that admits. `max_season_name_len` stood at **512** at the season walker, while `max_manifest_name_len` stands at **200**, correctly coupled to the published `manifest_entry.max_name_len` that bounds a listing line. So a 254-character filename passed the season read (`season read files=1 bytes=6`) and died three functions later at `error.ManifestOverflow`, naming a head listing the operator never asked for. The same root sat one bound over: `max_manifest_bytes` read `= max_cargo_bytes`, and the manifest is **the same catalog under a fatter prefix** -- `build_cargo_plain` writes `"cargo "` where `build_manifest_lines` writes `"manifest "`, nine bytes against six -- so it needs three bytes more per line against a budget declared equal. Twelve one-character names built **1,020** cargo bytes inside their 1,024 and refused at **1,056** manifest bytes. Neither shipped a wrong byte; both refused **late, at a wall nothing had declared**, and the selftest asserted `max_manifest_bytes == max_cargo_bytes` -- the tidy direction rather than the deciding one. *What caught it:* the air rota's own method, read at the open -- *you check whether a part stands free by trying to pull it out* -- applied to my lane's bounds rather than its modules, then two pours on metal that turned the reading into a measurement. *What it taught:* **`amphora_bounds_agree` exists to prove Amphora's shared bounds agree, and every relation it can state is `=`.** Its three readings -- same-name roofs, alias value groups, declared `/// couples:` -- all compare for equality, so a pair that must stand *apart* in a known direction cannot be written down at all, and the guard reads green over the gap. That is `%501`'s lesson one room over -- a bound asked in the direction that decides -- arriving in a guard's grammar rather than in an answer. *Repaired:* `max_manifest_bytes` is now **derived** (`max_cargo_bytes + manifest_line_extra * max_season_files` = 1,072) rather than asserted equal, since a derivation cannot drift; `max_season_name_len` is held **at** the entry contract at 200, which refuses nothing that ever poured, because anything above it already died at the listing; and three ordering asserts replace the equality, each with its `// invariant:`. The scan gained a fourth reading, `/// covers: <mod>.<name>`, meaning *this bound must be >= that one*, printing src, dst, and a **signed** slack so a reader sees the direction (`%498`'s lesson) -- `slack=0` on the living pair, the wall standing exactly at the door. Proven three ways: two plants plant the order both directions (`covers_hold` free, `covers_short` bitten at `slack=-312`, which is Amphora's own elder gap), and **the guard was pointed at the living tree with the elder 512 restored and bit it** -- `verdict=misread`, exit 1 -- then went green when restored. The twelve-file season that could not pour now pours, carries and restores whole: `cargo_bytes=1020 manifest_lines_bytes=1056`, `restore files proven count=12`, same-parent ok. All twelve rostered Amphora rows GREEN after the change, with `tame_style_check` and `width-check` green beside them. **CLOSED.**
