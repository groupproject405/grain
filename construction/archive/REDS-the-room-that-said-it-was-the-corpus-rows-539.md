# REDS %539 -- a door named one room as the whole corpus, and the population it derived counted machinery

*Folded off the living pin [`../REDS.md`](../REDS.md) on the round that booked it, by its own
author, because the pin stood at 39,260 of its 40,960 bound and this row runs past the slack. It
reads **CLOSED** -- both repairs are proven on metal, each from both sides -- and a closed row
folds. No peer's row was moved to make this room.*

*The sentence worth carrying forward: **a hand-named room is an enumeration with one element, and
one element is the easiest length at which an enumeration passes for a population.** The meter was
built to catch a hand-written list standing in for a corpus, and it took a hand-written directory
for the same thing, in the same file, four hours apart.*

*The second half is smaller and sharper: a population derived by pruning only `.git` counts a build
artifact, so a permission whose one remaining file is a cache leftover reads live rather than dead.
That is a gate satisfied by machinery, which is the quietest way for a gate to stop working.*

---

**REDS %539 (`20260907.045422`) -- a door named one room as the whole corpus, and the tree-wide population it derived counted machinery.** *What went wrong:* `tools/fixtures/g/glow_desk_reach_scan.sh` opened with *"glow/gen/ holds the generated desk corpus for the language"*, and that sentence was false when it was written. Measured `20260907.045422`: the tree carries **451** tracked `.glow` files across **eighteen** rooms, of which **99 stand outside `glow/gen/`** -- 49 under `src/gate/`, 39 under `src/shape/`, 7 under `tools/fixtures/g/`, 2 under `docs-geode/edu/yonder/pleac/ch01/`, one apiece in `linengrow/gen/` and a survey fixture. Nothing printed either number, so the claim could only be believed. **And the meter's own subject is a hand-written enumeration standing in for a population** -- it caught that fault in the desk witness's list of 218 and committed it one line into its own door, with a directory in place of a list. *What caught it:* crossing the worker's permission `case` against the whole tree rather than against the room. The `case` names **94** stems, and **49 of the 95 files they name live outside `glow/gen/`** -- two of them, `src/gate/gate-caravan-caps-pair-bound-u32.glow` and `src/gate/gate-mantra-gen-floor-pair-u32.glow`, named by **no tool in the tree at all**, so the worker's `case` is the only place that knows they may be run. A reading that had to reach outside the room to be correct is a reading whose room was never the corpus. *The second fault, found in the same pass:* the tree-wide stem list read `find . -name '*.glow' -not -path './.git/*'`, which prunes one directory and counts everything else -- including `glow/.cache/repl-line.glow`, a lowered REPL line the build writes. **A permitted stem whose only remaining file is a cache artifact would satisfy `sample_phantom` and read live**, so the gate against a dead permission could be held open by machinery. *What it taught:* **a population is derived or it is asserted, and a directory name is an assertion.** The room was the right scope for `uncovered` -- those other rooms have their own runners, 47 of 49 under `src/gate/` and all 39 under `src/shape/` -- and the door was wrong to call the room the corpus all the same. Scope and claim are two things, and the row that got the scope right published the claim anyway. *Repaired (`20260907.045422`):* one pruned corpus derived **once** and shared by every tree-wide reading, dropping dot directories (`.git` holds objects, `glow/.cache` holds the REPL line), `vendor/`, `seed/` and `node_modules/`. It reads exactly the paths `git ls-files '*.glow'` reads -- **two methods, one answer** -- while keeping the scan git-free, so a pen outside a repository still resolves. Three readings print: `corpus_glow` and `corpus_outside` make the door's claim checkable, and `stem_collision` names stems two files share. All three **report and gate nothing**, because a desk landing in `src/gate/` is ordinary work in another lane and a guard that reds on ordinary work is a guard somebody turns off. The bound already written for the room now covers the tree-wide count too. *Named, not taken:* `stem_collision` reads **2** -- `gate-count-u32` under `glow/gen/g/` and `tools/fixtures/g/`, and `sample-demo-fact-line-lits` under `glow/gen/s/` and `linengrow/gen/`. Two desks sharing a stem share one built binary, since `tools/g/glow_run_worker.sh` writes `glow/bin/<stem>`, and one sample permission, since its `case` matches the stem alone. Which file keeps the name is a language custody ruling. **CLOSED** on `glow_desk_reach_witness.rish` GREEN and **60** control behaviors (was 43), the new ones proving both sides of each: a desk outside the room reported while the room's own population and ratchets stand still, a shared stem counted and then removed, and a permission whose only file is machinery read as **phantom** and lifted the moment a real desk of that stem arrives.
