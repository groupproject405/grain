# REDS %493 -- a precondition is part of a claim

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.195500`, the row CLOSED in the
round that found it.*

**What the row taught, in one line:** a guard whose precondition only its author's checkout
satisfies reds every other tree in the fleet, every lap -- so a precondition belongs on the guard's
roster row, where the runner can read it, rather than inside the guard where only a red can report
it.

**Kin.** [`%489`](../REDS.md) seated the guard this row repaired. The roster's `capability` field
is the mechanism, and [`%475`](REDS-a-guard-that-reads-its-own-verdict-rows-475.md) is the family:
an instrument answering about the bench it stands on rather than about the tree it reads.

---

**REDS %493 (`20260906.133724`) -- a `tier lap` guard whose precondition only its author's checkout satisfies reds every other tree in the fleet, every lap.** *What went wrong:* `tools/s/sow_allow_reach_witness.rish` (`%489`, `20260906.122632`) opens *refused: no projection at `seed/`*, and `seed/` is a gitignored projection no lap makes; here the pass closes `guard_red`, the receipt is withheld, and `--scoped` then refuses -- a full cold pass every lap, 826s. *What caught it:* a re-verify on a tree other than the one that seated it. *What it taught:* **a precondition is part of a claim.** The roster's `capability` field already answers this. *Closed `20260906.143000` by GRASS, which booked the same defect as `%492` from a `--next` read forty minutes stale and gave the number back -- one defect, one row, theirs published and mine not (`derived-spine`).* `capability seed_projection` on the row, probed by the runner with the same `test -d "${SOW_SEED:-seed}"` the guard's own scan performs, so probe and guard cannot disagree about where the projection is; absence SKIPS and is named every pass, presence runs it whole, and there is no `unknown` to fake since `test -d` has no tool that can go missing. Proven on metal here: `run_verdict=ok skipped_capability=1`, and a full hot pass at **146 guards, 142 green, 0 red, 908s**. *And it had a SECOND HALF only a hot pass could find:* a skipped guard writes no run-card line -- which the capability control asserts on purpose -- so its red from the cold pass was carried forward by the runner's own history rule and **nothing would ever overwrite it**, leaving `standing_equipment` counting an immortal red on every tree. The carry-forward exists so a `tier cadence` guard keeps its history between runs; it is wrong for a guard skipped by `host` or `capability`, which cannot run here at all and whose last verdict was recorded in a different world. Those rows are dropped now, a cadence row is still kept, both planted -- the drop planted as a stale **RED**, since that is the direction that costs. Ten control legs in `standing_equipment_control.sh`, each asserted in its witness. **A guard rescued from redding every tree would have red every tree forever by a different road.** **CLOSED.**

**REDS %493 (`20260906.133724`) -- a `tier lap` guard whose precondition only its author's checkout satisfies reds every other tree in the fleet, every lap.** *What went wrong:* `tools/s/sow_allow_reach_witness.rish` (`%489`, `20260906.122632`) opens *refused: no projection at `seed/`*, and `seed/` is a gitignored projection no lap makes; here the pass closes `guard_red`, the receipt is withheld, and `--scoped` then refuses -- a full cold pass every lap, 826s. *What caught it:* a re-verify on a tree other than the one that seated it. *What it taught:* **a precondition is part of a claim.** The roster's `capability` field already answers this. *Closed `20260906.143000` by GRASS, which booked the same defect as `%492` from a `--next` read forty minutes stale and gave the number back -- one defect, one row, theirs published and mine not (`derived-spine`).* `capability seed_projection` on the row, probed by the runner with the same `test -d "${SOW_SEED:-seed}"` the guard's own scan performs, so probe and guard cannot disagree about where the projection is; absence SKIPS and is named every pass, presence runs it whole, and there is no `unknown` to fake since `test -d` has no tool that can go missing. Proven on metal here: `run_verdict=ok skipped_capability=1`, and a full hot pass at **146 guards, 142 green, 0 red, 908s**. *And it had a SECOND HALF only a hot pass could find:* a skipped guard writes no run-card line -- which the capability control asserts on purpose -- so its red from the cold pass was carried forward by the runner's own history rule and **nothing would ever overwrite it**, leaving `standing_equipment` counting an immortal red on every tree. The carry-forward exists so a `tier cadence` guard keeps its history between runs; it is wrong for a guard skipped by `host` or `capability`, which cannot run here at all and whose last verdict was recorded in a different world. Those rows are dropped now, a cadence row is still kept, both planted -- the drop planted as a stale **RED**, since that is the direction that costs. Ten control legs in `standing_equipment_control.sh`, each asserted in its witness. **A guard rescued from redding every tree would have red every tree forever by a different road.** **CLOSED.**
