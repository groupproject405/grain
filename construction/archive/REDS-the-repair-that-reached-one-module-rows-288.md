# REDS %288 -- the repair that reached one module, and nineteen siblings kept the other kernel's numbers

*Written straight onto this shelf on `20260827.004535`, **CLOSED**, the way `%286`-`%287` were written
on `20260826.235445`. The living pin stood at **24,573 bytes of its 24,576** with all six of its
rows OPEN, so it had three bytes of room and `tools/fixtures/reds_fold.sh` refuses to move an open
row. The pin keeps what is open; a row closed on the lap it is booked can be born here.*

**The three bytes are themselves worth reading.** Every row still in the pin -- `%230`, `%252`,
`%255`, `%273`, `%281`, `%282` -- is open, and four of them wait on Keaton's word. A ledger
that cannot record another open red without a gate being answered is a fact about the ledger rather
than about the tree, and it is named here rather than repaired, because the bound is a seated number
(`%205`).

---

**REDS %288 (`20260827.004535`) -- a platform repair reached one caller, and the class it belonged to was never counted.** *What went wrong:* `%282` found `caravan/harvest.rye` reading a child's exit status through `std.os.linux.waitpid`, a raw Linux syscall that answers a constant 9 on a BSD kernel, and moved seven call sites onto `std.c`, whose namespaces already switch on `native_os`. The same round found `mantra/recall_subscribe_poll_delivery.rye` hand-rolling `sockaddr_in` in the Linux layout and moved that ONE module to `std.c.sockaddr.in`, leaving its `BindFailed` at rebind open. The root of that open leg sat one layer under the struct and one layer wider than the module: the same file still spelled its socket OPTION numbers in Linux's dialect, and so did nineteen others. Measured over the 1,891 authored `.rye` sources outside vendor, aurora, gratitude and dated testimony: **20 modules carrying 46 literal socket-option declarations**, and **19 still hand-rolling the Linux-layout `sockaddr_in`** against **1** reaching `std.c.sockaddr.in`. Five distinct option names appear -- `SO_REUSEADDR` and `SOL_SOCKET` in all twenty, `SO_RCVTIMEO` in four, `SO_BROADCAST` and `SO_BINDTODEVICE` once each -- and read against the vendored toolchain's own tables, every one of the five is Linux's number: `std.c` gives a BSD kernel `SOL_SOCKET` 0xffff rather than 1, `SO_REUSEADDR` 0x0004 rather than 2, `SO_RCVTIMEO` 0x1006 rather than 20, `SO_BROADCAST` 0x0020 rather than 6, and no `SO_BINDTODEVICE` at all. So on macOS `setsockopt(fd, 1, 2, ...)` names a level that does not exist, the call fails, its return is discarded with `_ =`, `SO_REUSEADDR` is never set, and a rebind refuses -- which is exactly the symptom `%282` left open. The receive timeout never arrives either, so a recv meant to be bounded waits instead. *What caught it:* reading `%282`'s open leg for its root rather than its symptom, then `git grep` over the authored corpus rather than a recollection of how many modules do this -- the census answered twenty where the row had described one. *What it taught:* **a platform repair that reaches a caller has not reached the class.** `%282`'s own lesson was that a raw syscall names one kernel; its repair then named the same kernel again in a different vocabulary, inside the very file it had just fixed, and nineteen siblings were never opened. That is `%281`'s shape one room over -- *the fix reached one caller rather than the mechanism* -- and it is why the answer here is a meter rather than a sweep: nineteen of the twenty cannot be proven on this pier, where every one of these numbers reads correct whether or not anything was repaired, and a fix closes on a witness rather than on a claim. *Repaired:* `mantra/recall_subscribe_poll_delivery.rye` reaches `c.SOL.SOCKET`, `c.SO.REUSEADDR` and `c.SO.RCVTIMEO`, and both `setsockopt` calls refuse by a named error instead of discarding their return, since a discarded return is how the leg stayed invisible across a lap. `AF_INET` and `SOCK_DGRAM` keep their literals, measured rather than assumed: both read 2 on every host in the table, so dispatching them would buy nothing. The class is counted by [`../../tools/fixtures/socket_dialect_scan.sh`](../../tools/fixtures/socket_dialect_scan.sh) -- option literals at 19 files and 43 lines, Linux-layout `sockaddr_in` at 19 files, both ceilings that only fall, the dispatched count reported beside them because it is the number meant to rise -- proven from both sides on planted git repositories by [`../../tools/fixtures/socket_dialect_control.sh`](../../tools/fixtures/socket_dialect_control.sh) across **18 behaviors**, and gated by [`../../tools/s/socket_dialect_witness.rish`](../../tools/s/socket_dialect_witness.rish), which joins the standing roster as its **75th** guard. The refusal was shown on this tree as well as in the pen: one planted literal took the reading to 20 files and 44 lines and the scan refused by name, and removing it returned the reading to green. *Surfaced, not crossed:* the nineteen remaining modules turn on touch, which books nothing. **`%282` stays OPEN**, and now names its own root: this pier is the Linux one, so the `BindFailed` leg closes on the macOS bench's witness rather than here. **CLOSED.**
