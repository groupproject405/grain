# REDS %516 and %517 -- the name asked one question, the mechanism answered another

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260906.222615`, both rows CLOSED before the
fold. **Folded by Patchouli, not by their author**, and named here so their author can find them:
the pin stood at 40,947 bytes of its 40,960 bound and a new row had to land. Folding accretes where
shaving destroys, so both rows move whole and unedited.*

**What the two rows taught together, in one line:** an instrument can perform its act correctly and
still be wrong about which act it performed -- so a reading is only as honest as the distance
between the question its name asks and the condition its verdict actually reads.

**`%516`** handed `bwrap` a program this pier does not ship, read the failure to *exec* as a refused
mount *namespace*, and skipped the enclosure guard on the one bench whose legs pass. **`%517`** is
the same shape one room over: a scan titled *can the ledger still accept a row?* reached its verdict
through three conditions, none of them capacity, so the one question the file is named for could
never change its answer.

**Kin.** This is a loom rather than a lantern, and it now spans four lanes.
[`%510`](REDS-a-guard-that-reads-a-proxy-rows-510.md) named a guard that read a proxy and answered
about the proxy. The row that landed beside this fold -- the plant that plants nothing -- is the
same family again: a control whose `sed` matched nothing built the unmutated module and read like a
law that holds. Each was found by a different hand, in a different room, on the same day.

---

**REDS %516 (`20260906.204047`) -- a capability probe performed the act and was still wrong about which act it performed: it handed `bwrap` a program NixOS does not ship, read the exec failure as a refused mount namespace, and skipped the enclosure guard on the one bench where its legs pass.** *What went wrong:* the `jail_nesting` arm of `capability_state()` in `tools/fixtures/s/standing_equipment_run.sh` ran `bwrap --ro-bind / / --dev /dev /bin/true` and read any non-zero exit as `absent`. This pier ships **one entry in `/bin`, `sh`** -- so bwrap built the namespace perfectly and then failed to exec, printing `bwrap: execvp /bin/true: No such file or directory` and exiting 1. The arm answered `absent` on a bench standing in the host's initial user namespace (`/proc/self/ns/user -> user:[4026531837]`, PID 1 systemd, `max_user_namespaces` 63,817), so `agent_jail_enclosure` was skipped on every pass here since it landed at `20260906.013116`. `%446`'s own header promised the opposite -- *"a bench where the legs would work always runs them"* -- and named the unjailed bench as the only place a launcher regression would be caught. **This pier is that bench** (`FLEET_BARE=1`, Keaton's word `20260906`), so the four legs that prove the enclosure REFUSES what it must -- host home invisible under `--private-home`, `/etc` denied, write inside the repo permitted, claude starting under the launcher -- ran nowhere at all. *What caught it:* a fire-row lap reading the three orphaned files `%510` named in the round-open stash, finding `tools/fixtures/a/agent_jail_control.sh` written for a shared-probe design that lost, and running the elder command by hand to see whether the control still described the tree. The command's **message** was read rather than only its exit status, and it named the exec failure. *Measured on metal, both sides:* `AGENT_JAIL_PART=enclosure bash tools/ag/agent_jail_witness.sh` exits **0 with all four legs PASS** on this bench, while the pass that opened this lap recorded `skipped_capability=1` and only `agent_jail_enclosure` carries `capability jail_nesting`. Swapping the payload for `/bin/sh -c :` moves the arm from `absent` to `present`. *The root, stated where it transfers:* the seating log recorded *"Tested the probe on metal before writing it ... exits 1 with the same message the guard's own legs hit"* -- run from inside a jail, where **both** failures are true at once, so the reading was right for the wrong reason and stayed right until it met a bench where only one of them was. *What it taught:* **a probe that performs an act is only as honest as the act it performs**; an exit status alone cannot tell a refused capability from a payload that was never there, so the probe must compare the same payload inside and outside the wrapper and treat only a DIFFERENCE as evidence. *Repaired:* the payload is `/bin/sh -c :`, which POSIX guarantees at that path, and the arm runs it outside the wrapper first -- a payload that cannot run here answers `unknown`, and unknown RUNS. *Proven:* `standing_equipment_control.sh` grew a `jail_nesting` block of **14 legs** -- all three answers planted with a fake `bwrap` on PATH, the missing-tool answer planted in a pen of 1,028 symlinks holding every tool but that one (`command -v` finds an executable anywhere, so shadowing cannot hide one, and this pier keeps `bwrap` and `sh` in one directory), the pen proven innocent by a guard line, and the payload read off the runner's own source and executed. Replanting the elder spelling reads `jail_payload_exists=no` and `jail_payload_proven_outside=no`, so the two legs that would have caught this on the day are proven to bite. *The residue, named rather than papered over:* a bwrap failing for some third reason still reads `absent` and still skips; splitting that wants the stderr wording, and the two refusals this pier has printed differ (`setting up uid map: Read-only file system` nested here, `Failed to make / slave: Operation not permitted` in `%446`'s record), so a wording table would red every jailed ship whose kernel phrases it a third way. Pinned at its current value by `jail_unrecognized_failure_skips` so a later table moves it on purpose. *Sibling, unbooked:* `tools/l/lane_kvm.sh` line 116 runs `/bin/true` inside its own `refuse-probe`, which expects a non-zero exit -- so it passes here for the reason this row is about. **CLOSED.**

**REDS %517 (`20260906.212721`) -- the scan titled *can the ledger still accept a row?* printed `rows_that_fit=0` beside `pin_headroom=-193` and answered `verdict=ok`, while two other rostered guards red on that same page in that same pass.** *What went wrong:* `tools/fixtures/r/reds_pin_capacity_scan.sh` computes five capacity readings and reaches its verdict through three conditions, none of them capacity: a phantom recital shelf, the unrecorded-shelf ceiling, the shelf-open ceiling. So the one question the file is named for could not change its answer. *What caught it:* the cold roster pass at `20260906.210038` on this tree answering `guards_red=4`, of which **two were the same page** -- `equinox_e123_living_pin_guard` with `detail=pin_over_bound detail_path=construction/REDS.md`, and `declared_ceiling` with `bounds_over=1 verdict=over_declared_bound` -- and then running the capacity scan by hand and reading `verdict=ok` under `pin_headroom=-193 pin_foldable_rows=7`. *Measured the same night:* the pin stood at **41,153 of 40,960**, its bound raised only that morning at `20260906.022430` and derived for **8 ships x 4,096 of OPEN rows**; the OPEN set was **19,104 bytes, 58% of its allowance**, and the overflow was **18,153 bytes of already-foldable rows** sitting on the pin. Across `git log` for the day, **4 of 111 commit states** carried the pin over its bound. *Why it was written this way, and where that reasoning holds:* the scan's own header argues capacity must be reported and never gated -- *a full pin wants a person: raising a page's bound is Keaton's word*, and *a gate that reds on ordinary work is a gate someone turns off*. That is exactly right for a pin full of OPEN rows, where no lap can make room. It was never written about the state measured here, which the scan's own deadlock comment already names one line down: **a full pin with a foldable row is one `reds_fold.sh` away from healthy**. *What it taught:* **one fold away from healthy is not healthy -- it is repairable by whichever lap meets it, with no word from anyone**, and a verdict that disagrees with its own printed numbers is worth less than no verdict. The scan modelled the two states correctly and then reported both with one word. *Repaired:* the verdict crosses headroom against foldability and gates the one cell that is both -- `over_bound_foldable`, refusing with the fold command in the refusal itself, since a red a lap can close should name how. The cell beside it stays reported and ungated exactly as the header argues, because only a bound raise resolves it and that is Keaton's word. *Proven:* the control grew **31 cases to 39**, the full 2x2 with every neighbouring cell shown free -- at the bound, under it, and over it with nothing foldable -- since a refusal proven only in the failing direction cannot be told from a wall that always refuses. *Closed in the same lap:* the seven foldable rows folded, `%487`/`%508`/`%512` to their shelf, and the pin reads **33,954 with 7,006 of headroom**. *Kin, landed in the same hour and not a duplicate:* GRASS booked `%515` (`20260906.211529`) on this same symptom by a different mechanism -- `tools/hooks/pre-commit` fires at `git commit` and `--amend` while `git rebase` skips it, so a twice-pulled send walks the pin around the wall. That row asks why the wall did not run; this one asks why the instrument that reads capacity said `ok` while it stood there. Their measurement, six of the last 120 commits, and mine, four of 111 commit states, are two windows on one fact. *Residue, named:* a deadlocked pin still reads `verdict=ok`, which is this same shape one cell over; it stays that way on purpose until a custody-gate idiom fits it, since refusing there would red every lap until Keaton speaks. **CLOSED.**
