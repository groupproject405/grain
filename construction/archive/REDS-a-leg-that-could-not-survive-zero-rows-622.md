# REDS %622 -- a control leg that could not survive zero

*Folded from the living pin [`../REDS.md`](../REDS.md) on `20260908.085500`, so the pin keeps its
declared bound and holds what is still open. The row is CLOSED, and it is kept as the record of a
shape any ratchet can grow: a control leg that plants one fault and asserts the scan still passes
is asserting a ceiling above zero, quietly, from the day it is written. It holds for every lap
except the one that finishes the job -- and that is the lap it reddens, so the guard refuses at the
moment its own work succeeds. Read a ratchet's control beside the ceiling it is written against,
and ask what the leg claims when the ceiling reaches zero.*

---


**REDS %622 (`20260908.080144`) -- a ratchet exists to reach zero, and its control carried one leg that could not survive zero, so the guard reddened on the lap that finished the job.** *What went wrong:* `tools/fixtures/c/copy_lag_control.sh` leg 3 planted exactly one lagging copy in a pen and asserted `verdict=ok` on it, under the marker `behind_under_ceiling_free`, on the reasoning that a ratchet reports before it refuses. That assertion held only while `behind_ceiling` stood at 1 or more. This lap linked the last lagging copy in the tree -- `caravan/parse_int.rye` became a symlink to `../tally/parse_int.rye`, the eighteenth of its room's marks to do so -- and lowered the ceiling `1 -> 0`, which turned one planted lag from under the ceiling into over it. The scan correctly refused, the leg correctly read `no`, and `tools/co/copy_lag_witness.rish` failed at line 71 on the lap that closed the ratchet it guards. *What caught it:* the witness itself, on its first run after the ceiling fell. Nothing earlier could have: the leg was true every day since the guard was seated on `20260906.070240`, and it was true for the same reason it was about to be false -- the ceiling had never been zero. *What it taught:* **a leg that plants a fixed count and asserts a verdict is asking two questions, and only one of them is its own.** *Is a lag counted, named and priced* belongs to leg 3; *what verdict does that count earn* belongs to the ceiling, and the two part exactly at zero -- the same seam the ASCII comment control met the same day, where a pen planting three characters to give the counting readings a subject was the same pen asked whether it sat under a ceiling of nothing. **The right pattern already stood one leg away in the same file:** leg 8 asks the scan for its own ceiling, plants AT it and one PAST it, and proves both sides. Measured this lap: of **194 controls in the tree, exactly 3** read a ceiling out of scan output, and the other two -- `width_check_control.sh` and `socket_dialect_control.sh` -- already plant at the derived ceiling and one past it. So the class is one file, and the tree's own habit is otherwise correct. *Repaired (`20260908.080144`):* the ceiling read moved to the top of the control where every leg can reach it, and leg 3 now asserts the verdict its ceiling implies -- `verdict=ok` at a ceiling of 1 or more, `verdict=behind_over_ceiling` at zero -- under the marker `behind_verdict_follows_ceiling`. Proven three ways on metal, planted and lifted: at the live ceiling of 0 the leg takes the refusal branch and says yes; at a ceiling temporarily raised to 1 it takes the ok branch and says yes; with the branch deliberately inverted it says **no**, so the marker is not one that can only agree. `copy_lag_witness` GREEN, 31 behaviors, the ceiling still shown from both sides. **CLOSED**
