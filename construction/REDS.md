# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/r/reds_spine_derive_scan.sh --next`. **Three status words** (door B, `20260829`, Keaton's word): **OPEN** is a live defect and stays on the pin; **BOOKED** means the instances stand repaired and the remainder is a ratchet, a seat, or a booked lap, and such a row may fold; **CLOSED** closed on a witness. The last bold marker decides, read one way by `reds_fold.sh` and both status scans.
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**REDS %410 (`20260904.204812`) -- a per-page exception no guard had ever been asked to honor, because the one page carrying it was small enough not to need it.** *What went wrong:* `tools/fixtures/l/living_pin_max_bytes.sh` takes an optional page path and answers **that page's** bound, and its own header says why -- *a page may carry its own bound where the general one would refuse it for doing its job.* `tools/fixtures/e/equinox_e123_living_pin_guard_scan.sh` called it **bare**, once, at the top of the file, and then measured **every** pin on its roster against that single answer. So the scan borrowed the number and left the reading behind, which is the precise half-of-the-law shape `%396` booked one duty over. *What caught it:* seating a second exception. `living_pin_max_bytes[construction/ITINERARY.md] = 32768` was granted on Keaton's word and the fixture answered **32768** for that page immediately -- while this scan, on the same tree in the same minute, refused the card at `detail_max=24576`. Two meters reading one law to opposite verdicts, and both were sure. *What it taught:* **an exception nothing exercises is an exception nobody has tested.** The per-page reading was seated `20260824.190000` and stood eleven days with exactly one page named -- `session-logs/README.md` at 57,344 -- and that page measures **7,817 bytes**, comfortably under the general 24,576. So every guard that ignored the per-page argument still passed it, for the wrong reason, and the fault could not surface until a second page took an exception it actually needed. The general lesson is sharper than the fix: **a conditional that only one input reaches has been tested by nothing, however green the tree reads.** Kin to `%408` in the same session, where a branch conditional on a file only the guarded process could create never ran at all -- one is a branch never taken, the other a branch taken always. *Repaired (`20260904.204812`):* the scan asks per page inside its own loop (`pin_max_bytes "$path"`), prints `pin_max=` beside every `pin_ok=` so a reader sees which number each pin was weighed against, and keeps the bare reading only for the single summary line it always printed. Proven from both sides on metal: the card at 25,107 passes against its own 32,768, and the same card planted at **32,769** refuses naming `detail_max=32768` -- so the raise is a real ceiling rather than a removed one -- while `prove-red` still bites and the other nine pins still read 24,576. **CLOSED** on `tools/equinox/witness/equinox_e123_living_pin_guard_witness.rish`, GREEN on metal.

**REDS %409 (`20260904.184844`) -- the fleet's only real datum, written six times, and two of the six had already drifted.** *What went wrong:* the binding **seat -> tree -> engine** is the whole of what the fleet knows about itself, and it stood spelled in six independent places across two executables with nothing comparing them: `tools/l/fleet-loop.sh` carried the seat allow-list, the `want_tree` case, the `FLEET_DRY` engine case and the `run_lap` engine case; `tools/f/fleet_rearm.sh` carried nine `report_seat` rows and a relaunch case. Two had already parted. The loop admitted **six** seat names where the re-arm reported **nine**, so `sound`, `mind` and `mystery` were advertised by one instrument and refused by the other. And the elder-name remap seated on Keaton's word that same day -- `furrow -> pheromone`, `harvest -> petrichor` -- lived in the loop's own case arm and **never reached the re-arm at all**, so one file had healed a rename the other had not heard of. *What caught it:* measuring the fleet rather than remembering it, on a lap opened to debug something else. `sh tools/f/fleet_rearm.sh` walks nine seats and finds **three** on this pier, all Earth; the other six are absent directories, because the aether fleet's trees lived on a Mac and on the Seattle pier, and Seattle was cancelled `20260903`. Reading the two files side by side to write that number down is what showed the counts disagreeing. *What it taught:* **a rule written six times is a rule six files may quietly come to disagree about** -- the mechanism-sentence law's own celebrated commit, one room over, where a note name written forty-seven times became one published function. The sharper half is which two drifted: not the four inside one file, which a hand edits together, but the two **across** the file boundary. A copy you can see is a copy you maintain; a copy in another file is a copy you forget. And the drift was invisible because each instrument reads perfectly **alone** -- the same shape the derived-spine law booked six times before the ledger key was seated. *Repaired (`20260904.184844`):* `construction/fleet-roster.kyri` holds the table once, in the standing roster's own record shape -- `seat` opening a record, then `tree`, `engine`, `lane`, `status`, `elder`, `seated`. `tools/fixtures/f/fleet_roster_scan.sh` is the one parse, answering `--seats`, `--live`, `--tree`, `--engine`, `--lane`, `--status`, and `--resolve`; a question about a seat the table does not hold **exits 2 and prints nothing**, so a caller that forgets to check cannot mistake an empty answer for a real one. All six spellings are lookups now. `status parked` is what lets the six aether seats stay readable without pretending they run -- an absent directory is a filesystem accident where a parked row is a declaration -- and `engine field` is a word rather than a blank, so the interactive bench refuses an unattended lap **by name** instead of falling into a claude branch that would start one. *Found while proving it:* the reader printed its answer **twice**, because awk's `exit` jumps to `END` and the `END` flushed the same record again -- invisible to a caller reading one line and a fault to one reading all of them. The record is cleared before the exit now, and a leg asserts the answer is exactly one line. **CLOSED** on `tools/f/fleet_roster_witness.rish` over `tools/fixtures/f/fleet_roster_control.sh`, seventeen behaviors GREEN on metal, every refusal shown from both sides -- including the one that matters most, that a seat name surviving in a case arm of the loop is a **seventh** copy being born.

**REDS %408 (`20260904.174748`) -- the mount that could only happen once it had already happened, so every jailed Claude met the onboarding it was meant to remember past.** *What went wrong:* `tools/ag/agent-jail.sh` mounts the host's `$HOME/.claude.json` into the jail **only when `loops/claude/dot-claude.json` already exists** -- and under `--private-home` that file is written by exactly one process, the jailed Claude, into a tmpfs the jail exit discards. The condition could therefore never come true on a fresh pier, and the failing direction is silent: a bind-mount whose source is absent is skipped, not refused. Claude Code keeps `hasCompletedOnboarding`, the chosen theme, and per-project trust in `~/.claude.json` -- a FILE beside `~/.claude/`, not inside it -- so the mount that DID land (`loops/claude` onto `~/.claude`, carrying `settings.json` with `{"theme":"dark"}`) delivered the preference and not the record that the question had been asked. Onboarding ran on every jailed launch, and its first screen is the theme picker, which previews a light scheme on the operator's dark terminal: *"some text is completely invisible."* *What caught it:* a maintainer reporting the display fault against **NixOS** and asking for a rebuild, and measuring the two candidate explanations instead of taking them. Inside the jail against outside, on metal `20260904`: `ai-jail` drops `TERMINFO_DIRS` and `LOCALE_ARCHIVE` from the environment, which reads exactly like the cause -- and both are harmless here, `infocmp tmux-256color` resolving through ncurses' compiled-in path and `printf 'e-acute + a CJK glyph' | wc -m` reading **2** inside and out. `TERM=tmux-256color` and `COLORTERM=truecolor` both survive, so the color depth is whole. What did not survive was one file: `[ -f ~/.claude.json ]` answers **ABSENT** in the jail and PRESENT on the host, 42 KB carrying `hasCompletedOnboarding: True`. The NixOS configuration is not at fault and no rebuild was needed. *What it taught:* **a conditional whose condition only the guarded process can satisfy is a branch that never runs.** The elder shape is visible one file over and reads as a courtesy: `tools/e/enclosure.conf.example` says *"Optional: copy host ~/.claude.json to $CLAUDE_STATE/dot-claude.json for quieter Claude starts"*, and `nixos-guide/20260808-041912_3-cli-agents-in-ai-jail.md` tabulates the same mount as `(optional seed)`. Both are accurate descriptions of a workaround, written by a hand that had hit this and stepped around it -- so the fault was diagnosed twice, in two rooms, and filed as a habit rather than booked as a red. That is the sharper half: **a workaround written into the documentation is a red somebody already found.** The three sibling mounts in the same block take the opposite shape and are correct -- `CLAUDE_STATE`, `CURSOR_AGENT_STATE`, `CODEX_STATE` are each `mkdir -p`'d and then mounted unconditionally, and the codex one carries a comment explaining precisely why (*"hence a repo-local durable dir, so login is done once per pier rather than once per lap"*). The rule was written, argued, and applied to three of the four things it governs. *Repaired (`20260904.174748`):* the file is **seeded** rather than the mount skipped -- `printf '{}\n'` when absent, then mounted unconditionally, matching its three siblings. Empty JSON on purpose and never a fabricated `hasCompletedOnboarding` key: guessing another program's config schema is a claim no witness in this tree can hold, and Claude writes its own record on the first jailed run, which then persists. Pond's record already declared this mount at `pond/enclosure_policy.kyri:133`, so `pond_policy_launcher`'s `unspelled_maps` reading is untouched -- the launcher had been *under*-building what the policy described, which is the reported `unbuilt_maps` direction rather than the enforced one. *Found beside it, same file, repaired in the same lap:* `tools/ag/agent_jail_witness.sh` was RED on this pier at its first leg and unrostered, so nothing said so. It greps the dry-run plan for `.claude-state` and `.cursor-agent-state`, the two directories the `20260827` one-room adoption moved to `loops/`; it `source`s `tools/e/enclosure.conf`, which this pier does not have, under `set -u`; and it smoke-tests `cursor-agent --version` on a pier whose Cursor CLI trial ended `20260904`. Three stale pins in one guard -- REDS `%360`'s unheard-guard class exactly, where *a stale pin declares itself by turning the witness red, which is only an alarm if something is listening.* Repaired to read `loops/`, to source the conf only when present, to skip an absent optional agent, and carrying a new leg that greps the dry-run plan for the `.claude.json` bind -- the leg that would have caught this on the day the adoption landed. **CLOSED** on `tools/ag/agent_jail_witness.rish`, GREEN on metal.

**Rows: 410 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 390** -- counted from the ledger and its archives on `20260904.204833` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 410 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

**REDS %360 (`20260830.100059`) -- two of every three guards in this tree are heard by nothing, and half of the ones I ran are red.** *What went wrong:* the standing roster names 115 paths and reaches 603 of this tree's 1,756 tracked witnesses; the other **1,153** are run by nothing on a lap nobody is watching. Forty-one of them are themselves **choirs** that would sing families of their own; one of the 1,153 is `tools/s/standing_equipment_witness.rish`, the guard that proves this very roster honest, which no roster pass has ever run. **Six** more are guards this tree's own living rules instruct a lap to run -- `tools/co/commit_message_guard_witness.rish`, `tools/m/mechanism_sentence_witness.rish`, `tools/s/sel4_userlevel_license_witness.rish` (which `gratitude-licenses.md` calls *"a standing check, not a one-time read"*), `tools/w/waymark_derive_witness.rish`, `tools/w/waymark_registry_witness.rish`, and `tools/cr/crypto_vendored_parity_suite.rish`. Running the 46 sharpest of them on metal `20260830`: **21 red, 25 green** -- a red rate of 46% in the half no pass reaches. The sharpest instance is operational and sits at a custody gate: `tools/hooks/commit-msg` exempts the public seed's root commit by matching a subject exactly equal to `crashed-meteor`, while `publish-seed.sh` on this bench still writes `Grain OS -- initial public seed`, and `.claude/rules/git-signing.md` writes a third spelling (`grain-ww -- the crashed meteor`) beside `mechanism-sentence.md`'s fourth (`grain-ww -- `). Fed the publisher's actual message, the wall answers **REFUSED -- body words: 201, mechanism words found: 1, wanted: 3**, so the next seed publish fails at its own step 4/6. The guard that proves that exemption is `mechanism_sentence_witness`, which is unrostered, so the breakage has stood since the `20260828` rename with nothing to say it. *What caught it:* the census this row books, built because REDS %357 measured the same gap from the other side -- three witnesses of one family red at HEAD for days, none of them rostered, while a 96-guard cold pass read 95 green. *What it taught:* **a roster is a claim about coverage, and this one was never measured.** The roster's own header has carried both halves of the law since it was written -- a guard that cannot red guards nothing, AND a guard that is never run guards nothing either -- and only the first half had a meter. The sharper lesson is what the unheard reds are MADE of: `want_rows_33`, `want_metric_rev_i9`, `CONC_GUARDS_PRESENT 2` -- expected values pinned to a tree that moved. That is REDS %341/%357's borrowed-number class living one room over, in a scan's own constant rather than in a Rishi assert, where `borrowed_number` cannot see it; and the reason nobody noticed is that a stale pin declares itself by turning the witness red, which is only an alarm if something is listening. *Repaired in part (`20260830.100059`):* the meter exists now -- `tools/u/unheard_guard_witness.rish` over `tools/fixtures/u/unheard_guard_scan.sh` closes the set the roster reaches and holds the remainder under a ceiling that only falls, with the choir reading gated beside it and 27 behaviours proven on real repositories in a throwaway pen. The 21 red guards live in lanes this seat does not hold -- Glow, STOA, SUNN, the equinox choirs -- so each is named here and left to its owner rather than repaired across a lane boundary, and the seed-subject spelling is a naming decision at custody gate %1 rather than a fix an agent takes. *Repaired further (`20260830.154940`):* the five rule-named guards of that set now stand on the roster at `tier lap`, each proven GREEN on metal first -- **18s together** against a 2,425s pass. `unheard` 1,153 -> **1,144**, choirs 41 -> **40**, both ceilings falling with them. The sixth is refused by measurement rather than by lane: `mechanism_sentence_witness` reds at `WALL_EXEMPTS_SEED_ROOT` because the wall exempts subject `crashed-meteor` alone while `publish-seed.sh` **and the wall's own fixture** both write `Grain OS -- initial public seed` -- three sites agree, the wall alone moved, and the witness would have caught it the day it was rostered. The spelling stays custody gate %1. Still **OPEN**. *Repaired further (`20260830.202400`):* the sixth stands rostered. Keaton's word on `20260830.190407` settled the subject at `crashed-meteor` -- the wall, the field's publisher, and both rules speak it, and the real publish shipped on it -- so what remained was not a naming decision but one unswept tracked file: `tools/fixtures/m/mechanism_wall_scan.sh` still planted the retired `Grain OS -- initial public seed` as the exempt subject and `caravan: initial public seed` as the borrow attempt, so the fixture the witness trusts was refused by the wall it tests and `WALL_EXEMPTS_SEED_ROOT` read 0. Two lines carry the settled spelling; fed `caravan: crashed-meteor` the wall still refuses, since the exemption matches the whole subject rather than a suffix. GREEN on metal before the roster row was written, 1s alone. `unheard` 1,144 -> 1,143, `witness_reach` unreached 1,093 -> 1,092 and families 218 -> 217 as the row landed -- and every one of those falls was cancelled before the send by two witnesses that landed upstream unrostered in the same window, so the ceilings stand where they were published rather than recording a fall the tree no longer shows. All six guards our own rules demand now run every lap. *Repaired further (`20260831.014630`):* the two lane-safe choirs stand rostered at `tier lap`, each GREEN on metal before its row was written -- `acme_dx` 3s, `drey` 5s, **8s together** against a cold pass of 63 minutes. Two rows carry **28** guards into hearing, since a rostered choir is heard and so is everything it sings: `heard` 660 -> 688, `unheard` 1,144 -> **1,116**, `unheard_choirs` 40 -> **37**, the third choir being `operations_conformance`, which `acme_dx` sings. Both ceilings fall in the scan and in the witness's published pin, which is two copies on purpose -- a fall costs a deliberate hand in both files -- and `unheard_guard` is GREEN with its 27 pen cases. **The remainder is unchanged and is not this seat's:** 21 red guards in the Glow, STOA, SUNN and equinox lanes, each named above and left to its owner, and the pier clones' stale untracked `publish-seed.sh` copies at Keaton's hand. Still **OPEN**.

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **The pin keeps what is open**, so a row still reading OPEN stays flat however old it is.*
