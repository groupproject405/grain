# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/r/reds_spine_derive_scan.sh --next`. **Three status words** (door B, `20260829`, Keaton's word): **OPEN** is a live defect and stays on the pin; **BOOKED** means the instances stand repaired and the remainder is a ratchet, a seat, or a booked lap, and such a row may fold; **CLOSED** closed on a witness. The last bold marker decides, read one way by `reds_fold.sh` and both status scans.
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**REDS %416 (`20260905.072118`) -- a ratchet passes on a low number, so a broken instrument and a healthy tree print the same green.** *What went wrong:* `tools/fixtures/u/unheard_guard_scan.sh` ran both of its awk passes as `... > FILE 2>/dev/null || true`, discarding awk's words and its exit. The two halves fail differently and only one would ever have been noticed. The first feeds a transitive closure, so an empty result stops the walk early, `unheard` reads **high**, and the ceiling reds -- loud, and wrong in the safe direction. The second feeds `choirs`, and **a ratchet passes on a low number**: an empty result reads `unheard_choirs=0` against a ceiling of 37, which is the greenest number on the page. A guard whose instrument had died would have reported the healthiest tree it ever measured. *What caught it:* Keaton's word to take the refusal reflex across every scan, after `%413` found the same shape twice in one repair. *What it taught, and it is the half worth carrying:* **an empty answer from a broken instrument is byte-identical to an empty answer from a clean tree, and every ratchet in this tree reads the second one as good news.** The direction matters: a gate at zero fails safe when its instrument dies, and a ceiling fails **unsafe**. This tree holds far more ceilings than gates. *And the measurement corrected the repair before it was written.* The broad signature -- any instrument with `|| true` -- matched **20 sites across 9 files, and exactly 2 were faults**. `grep` exits 1 when it finds nothing, so `grep p f > out || true` is right rather than a swallow; an awk carrying `END { exit !found }` is a **predicate** whose non-zero IS its answer. A meter with 90% false positives is a meter somebody turns off, so the reading is narrow by construction: an **output-producing** pass, redirected to a file, whose failure is discarded, not led by grep. *Repaired (`20260905.072118`):* both passes check their status and refuse with `instrument=failed`, `detail=name_pass_refused` or `detail=choir_pass_refused`, and awk's own last five lines. Proven by planting a syntactically broken program into the generated instrument and watching the refusal replace the count. *And made checkable:* `tools/fixtures/i/instrument_refusal_scan.sh` reads 676 tracked scans and gates the narrow shape at **zero from birth** rather than ratcheting down, since the known faults were repaired on the same lap. Its own first run found a **third** site, `counsel_census_scan.sh`, which turned out to be a **documented deliberate toleration** -- one dangling symlink among six thousand paths fails the `cat`, and the fifty-six megabytes that DID read are the census. That is a decision rather than a swallow, and **the difference between them is whether anyone wrote it down**, so a `# instrument-tolerated: <why>` comment exempts the next line and nothing else. Eleven behaviors proven in a throwaway git repository, every refusal shown from both sides and every welcome asserted as hard -- including that the meter **obeys the law it enforces** and refuses `instrument=failed` when it cannot reach the tree it was pointed at, which its first draft did not. **CLOSED** on `tools/i/instrument_refusal_witness.rish`, rostered at `tier lap` the lap it was written.

**REDS %415 (`20260905.014249`) -- `claude login` writes to the host's home, and `--private-home` is exactly the thing that replaces it.** *What went wrong:* a jailed lap answered `Not logged in - Please run /login` on two trees, and typing `claude login` at the pier shell did not help, however many times it was run. The login writes to the **host's** `$HOME/.claude/.credentials.json`; the jail runs `--private-home`, whose whole purpose is to replace `$HOME` with a tmpfs, and only `loops/claude` is mounted in. So a login typed at the pier reaches **no tree at all, whichever directory it was typed in** -- `cd` changes the working directory and the credential follows `$HOME`. Measured: the host carried a 509-byte credential, `grain-incense/loops/claude` carried the same 509 bytes and worked, and `grain-pheromone` and `grain-petrichor` carried **none**. *What caught it:* Keaton running the launch, reading `Not logged in`, logging in, running it again, getting the same line, and saying so. The tree could not have caught it: incense had a credential from an earlier in-jail login and every guard that touches the launcher reads mounts and flags rather than whether the thing behind a mount is present. *What it taught:* **a sandbox that replaces a directory replaces everything anyone ever put there**, and an instruction that works outside it can fail inside it while looking identical from the keyboard. The sharper half is that the answer was already written in the same file: `agent-jail.sh` has seeded cursor's browser login from the host into the state room since it was written, with a comment saying private-home would otherwise drop it. **The same seam, the same fix, and only one of the two agents ever got it.** Kin to `%408`, where a mount for claude's own config was conditional on a file only the jailed claude could write -- twice now, the claude seam has been the one left half-done. *Repaired (`20260905.014249`):* the launcher seeds `loops/claude/.credentials.json` from the host when the tree has none, **and refreshes it when the host's is strictly newer**. The refresh clause is what cursor's line does not need and this one does: a token refreshes, the jailed agent writes its own back into the tree's room, so the tree's copy is newer after a working lap and is never clobbered -- while a fresh `claude login` on the pier makes the host's newer and every tree picks it up on its next launch. Whichever hand moved last wins, which is the only rule that stays true without a hand remembering it. **One login per pier rather than one per tree**, which matters at six ships and six expiry clocks. Proven in all three states with mode `0600` preserved: seeded when absent, left alone when the tree's is newer, refreshed when the host's is. *What this is not:* no credential leaves the machine, none is written anywhere tracked (`loops/` is gitignored and a closed stack), and nothing here creates, holds or moves a custody key -- it carries the maintainer's own already-granted session between two directories of his own pier, which the cursor line beside it has done all along. **CLOSED.**

**REDS %414 (`20260905.011715`) -- the sandbox and the agent both own `--verbose`, and the loop counted eight refusals as eight laps.** *What went wrong:* Claude Code refuses `--output-format stream-json` without `--verbose` -- *When using --print, --output-format=stream-json requires --verbose* -- and **ai-jail owns `-v, --verbose` itself**. Its guard fires on a known flag appearing after the command **even when `--` was passed**, which `tools/ag/agent-jail.sh` had been passing all along; the refusal reads *put sandbox flags before the command or use --*, advising the thing already done. So every jailed lap died instantly on `x flag --verbose after command would be passed to the child`. *And the loop had no way to tell that apart from a lap that ran and failed.* It counted each refusal, slept twenty seconds, and opened another -- eight of them in under a minute apiece across the pheromone and petrichor trees, against an eighteen-hour deadline of doing nothing at all. Keaton launched both ships and watched them spin. *What caught it:* Keaton running the two commands this seat had handed him and pasting what came back. Nothing in the tree could have caught it: the smoke test that passed hours earlier was `claude --effort max -p '...'` with no `--verbose`, and `loop_prompt_parse` proves the printed command PARSES rather than that the sandbox accepts it. *What it taught:* **a wrapper inherits its wrapper's namespace.** Every flag the agent needs must survive a second argv reader that has its own opinions about the same words, and `--` is a promise only when the program keeps it. The general form is worth more than the instance: any flag shared between a sandbox and the thing it sandboxes is a collision waiting for the lap that first needs it, and the collision surfaces at launch rather than at review. *Repaired (`20260905.011715`):* **the flags move inside.** `tools/l/fleet_lap.sh` runs within the enclosure, reads the baton and the seat stanza from the files directly, and spells every flag claude needs where only claude reads them; `agent-jail.sh` gains a `lap` command whose jail argv is `sh .../fleet_lap.sh <seat>` with **not one flag in it**. That also retires the giant `-p` argument riding through two shells, so the apostrophe that stranded Silence's loop on `20260829` has nowhere left to bite. *And the loop learned to stop:* **three consecutive laps dying in under ten seconds each break the loop** with a message naming the fault as an invocation failure rather than a lap that failed, and offering the by-hand command and `FLEET_BARE=1`. Proven in a throwaway pen against a launcher stubbed to exit 9. Verified live: a jailed lap now reaches Claude Code with `permissionMode: bypassPermissions` and streams. **CLOSED.**

**Rows: 416 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 396** -- counted from the ledger and its archives on `20260905.072219` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 416 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

**REDS %360 (`20260830.100059`) -- two of every three guards in this tree are heard by nothing, and half of the ones I ran are red.** *What went wrong:* the standing roster names 115 paths and reaches 603 of this tree's 1,756 tracked witnesses; the other **1,153** are run by nothing on a lap nobody is watching. Forty-one of them are themselves **choirs** that would sing families of their own; one of the 1,153 is `tools/s/standing_equipment_witness.rish`, the guard that proves this very roster honest, which no roster pass has ever run. **Six** more are guards this tree's own living rules instruct a lap to run -- `tools/co/commit_message_guard_witness.rish`, `tools/m/mechanism_sentence_witness.rish`, `tools/s/sel4_userlevel_license_witness.rish` (which `gratitude-licenses.md` calls *"a standing check, not a one-time read"*), `tools/w/waymark_derive_witness.rish`, `tools/w/waymark_registry_witness.rish`, and `tools/cr/crypto_vendored_parity_suite.rish`. Running the 46 sharpest of them on metal `20260830`: **21 red, 25 green** -- a red rate of 46% in the half no pass reaches. The sharpest instance is operational and sits at a custody gate: `tools/hooks/commit-msg` exempts the public seed's root commit by matching a subject exactly equal to `crashed-meteor`, while `publish-seed.sh` on this bench still writes `Grain OS -- initial public seed`, and `.claude/rules/git-signing.md` writes a third spelling (`grain-ww -- the crashed meteor`) beside `mechanism-sentence.md`'s fourth (`grain-ww -- `). Fed the publisher's actual message, the wall answers **REFUSED -- body words: 201, mechanism words found: 1, wanted: 3**, so the next seed publish fails at its own step 4/6. The guard that proves that exemption is `mechanism_sentence_witness`, which is unrostered, so the breakage has stood since the `20260828` rename with nothing to say it. *What caught it:* the census this row books, built because REDS %357 measured the same gap from the other side -- three witnesses of one family red at HEAD for days, none of them rostered, while a 96-guard cold pass read 95 green. *What it taught:* **a roster is a claim about coverage, and this one was never measured.** The roster's own header has carried both halves of the law since it was written -- a guard that cannot red guards nothing, AND a guard that is never run guards nothing either -- and only the first half had a meter. The sharper lesson is what the unheard reds are MADE of: `want_rows_33`, `want_metric_rev_i9`, `CONC_GUARDS_PRESENT 2` -- expected values pinned to a tree that moved. That is REDS %341/%357's borrowed-number class living one room over, in a scan's own constant rather than in a Rishi assert, where `borrowed_number` cannot see it; and the reason nobody noticed is that a stale pin declares itself by turning the witness red, which is only an alarm if something is listening. *Repaired in part (`20260830.100059`):* the meter exists now -- `tools/u/unheard_guard_witness.rish` over `tools/fixtures/u/unheard_guard_scan.sh` closes the set the roster reaches and holds the remainder under a ceiling that only falls, with the choir reading gated beside it and 27 behaviours proven on real repositories in a throwaway pen. The 21 red guards live in lanes this seat does not hold -- Glow, STOA, SUNN, the equinox choirs -- so each is named here and left to its owner rather than repaired across a lane boundary, and the seed-subject spelling is a naming decision at custody gate %1 rather than a fix an agent takes. *Repaired further (`20260830.154940`):* the five rule-named guards of that set now stand on the roster at `tier lap`, each proven GREEN on metal first -- **18s together** against a 2,425s pass. `unheard` 1,153 -> **1,144**, choirs 41 -> **40**, both ceilings falling with them. The sixth is refused by measurement rather than by lane: `mechanism_sentence_witness` reds at `WALL_EXEMPTS_SEED_ROOT` because the wall exempts subject `crashed-meteor` alone while `publish-seed.sh` **and the wall's own fixture** both write `Grain OS -- initial public seed` -- three sites agree, the wall alone moved, and the witness would have caught it the day it was rostered. The spelling stays custody gate %1. Still **OPEN**. *Repaired further (`20260830.202400`):* the sixth stands rostered. Keaton's word on `20260830.190407` settled the subject at `crashed-meteor` -- the wall, the field's publisher, and both rules speak it, and the real publish shipped on it -- so what remained was not a naming decision but one unswept tracked file: `tools/fixtures/m/mechanism_wall_scan.sh` still planted the retired `Grain OS -- initial public seed` as the exempt subject and `caravan: initial public seed` as the borrow attempt, so the fixture the witness trusts was refused by the wall it tests and `WALL_EXEMPTS_SEED_ROOT` read 0. Two lines carry the settled spelling; fed `caravan: crashed-meteor` the wall still refuses, since the exemption matches the whole subject rather than a suffix. GREEN on metal before the roster row was written, 1s alone. `unheard` 1,144 -> 1,143, `witness_reach` unreached 1,093 -> 1,092 and families 218 -> 217 as the row landed -- and every one of those falls was cancelled before the send by two witnesses that landed upstream unrostered in the same window, so the ceilings stand where they were published rather than recording a fall the tree no longer shows. All six guards our own rules demand now run every lap. *Repaired further (`20260831.014630`):* the two lane-safe choirs stand rostered at `tier lap`, each GREEN on metal before its row was written -- `acme_dx` 3s, `drey` 5s, **8s together** against a cold pass of 63 minutes. Two rows carry **28** guards into hearing, since a rostered choir is heard and so is everything it sings: `heard` 660 -> 688, `unheard` 1,144 -> **1,116**, `unheard_choirs` 40 -> **37**, the third choir being `operations_conformance`, which `acme_dx` sings. Both ceilings fall in the scan and in the witness's published pin, which is two copies on purpose -- a fall costs a deliberate hand in both files -- and `unheard_guard` is GREEN with its 27 pen cases. **The remainder is unchanged and is not this seat's:** 21 red guards in the Glow, STOA, SUNN and equinox lanes, each named above and left to its owner, and the pier clones' stale untracked `publish-seed.sh` copies at Keaton's hand. Still **OPEN**.

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **The pin keeps what is open**, so a row still reading OPEN stays flat however old it is.*
