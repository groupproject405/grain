# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/r/reds_spine_derive_scan.sh --next`. **Three status words** (door B, `20260829`, Keaton's word): **OPEN** is a live defect and stays on the pin; **BOOKED** means the instances stand repaired and the remainder is a ratchet, a seat, or a booked lap, and such a row may fold; **CLOSED** closed on a witness. The last bold marker decides, read one way by `reds_fold.sh` and both status scans.
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**REDS %413 (`20260905.001835`) -- three more guards forking per item, and a repair that twice taught a guard to report a clean tree it never read.** *What went wrong:* the same shape `%412` found in two guards stood in three more, and each was reading something cheap expensively. `tools/fixtures/t/tracked_link_scan.sh` forked an `awk` and a `grep` **per link**, plus three more per file -- about **47,700 processes** across 4,289 documents and ~17,400 links, at 72s. `tools/fixtures/p/phantom_path_scan.sh` forked an `awk` per source file and then a `grep` per literal -- 2,859 files and **8,793 literals**, at 58s. `tools/fixtures/d/declared_model_scan.sh` walked all **14,709 tracked files** forking a `basename` and one or two `grep`s each -- roughly 35,000 processes to find the 1,127 files that name a model id at all, at 75s. *What caught it:* the previous lap's own recommendation, taken literally. Three guards were named on a hunch that they shared `%412`'s shape; all three did. *What it taught, and this is the half that cost a repair rather than a reading:* **a guard that cannot run its instrument must refuse, never report a clean tree.** The first rewrite of `tracked_link` resolved its new awk helper from `$(pwd)` and sent awk's complaint to `/dev/null`. The control runs that scan by absolute path after `cd`-ing into a throwaway repository, so the helper was not there, awk failed, the candidate list came back empty, and the scan printed **zero unresolved links and passed**. Four control legs caught it. Then the same two mistakes were found sitting in the `living_card_ascii` repair landed an hour earlier under `%412` -- `$(pwd)` and an unchecked pipeline -- where no control pens the scan, so nothing would have caught it until someone did. **An empty answer from a failed instrument is byte-identical to an empty answer from a clean corpus, and the second is the reading everyone wants to hear.** *Repaired (`20260905.001835`):* `tools/fixtures/t/tracked_link_duty2.awk` resolves and tests every link in one process, emitting only the residue that misses the tracked set -- 636 of 17,400 -- for the shell's `-e` test, where `[` is a builtin and costs nothing. `phantom_path` reads every source in one awk and filters 8,793 literals against the known set and the symlink map in a second, leaving a residue for `-e` and `git check-ignore`. `declared_model` asks `git grep -l` once for the 1,127 files naming any model id and loops only over those. Every one now finds its helper from `$(dirname "$0")` and **refuses loudly when its pass fails**, proven by hiding the helper and watching `instrument=failed` where a green had stood. **Measured: 72s -> 5s, 58s -> 6s, 75s -> 5s**, each output byte-identical to its elder and every control leg unchanged -- `phantom_path`'s nine cases including three refusals, `tracked_link`'s twelve. *And two standing guards bit this repair on the lap it entered, which is the roster working rather than a second fault:* `shell_dialect` refused `xargs -0 -a FILE` -- the argument-file flag is a GNU extension and this fleet spans a Mac. The tree had **already written that trap down**, in `tools/fixtures/s/shell_portable.sh`'s own header and in a comment inside `geode_libraries_scan.sh` that says it was *paid for twice*; a third payment was avoided only because a guard was listening. Piped instead. `vocabulary_collection` refused a bare `corpus` in the operator card's reader prose, a seated retirement this lap's own summary walked straight into. Swept to **collection** in the card and in both new instrument headers. **CLOSED** on the three witnesses, GREEN on metal.

**REDS %412 (`20260904.225911`) -- a third of the roster's clock went to forking, not to reading, and the two worst offenders were checking cheap things expensively.** *What went wrong:* a full pass measured **1,510 seconds** across 111 guards with a **median of 2s against a mean of 13** -- and a mean six times its own median is never a rate, it is a handful of guards carrying everything. **Five held 46%.** Two of those were slow by accident rather than by nature. `tools/fixtures/l/living_card_ascii_scan.sh` validated UTF-8 by calling a shell function once per file over **14,709 tracked text files**, and that function forked `mktemp`, `iconv` and `rm` each time -- roughly **44,000 processes** for a reading the bytes make trivial, at **137s**. `tools/r/reds_row_present_witness.rish` proved the ledger's spine complete by asking `reds_row_present.sh` for row 1, then row 2, to the end -- **411 invocations, each re-reading the whole spine** of the pin plus forty archives, at **177s**. *What caught it:* Keaton asking whether six ships could share one pier, which turned a vague sense that the roster was slow into a question with an answer. The run card had recorded per-guard seconds since `%388` and nobody had sorted it. The first move was nearly the wrong one: the obvious repair is to move slow guards to `tier cadence`, and a **137-second ASCII check over nine small files is not slow, it is broken** -- reading the script rather than reaching for the tier is what found the fork loop. *What it taught:* **before you make an expensive thing rare, find out whether it is expensive on purpose.** Two of the three biggest costs on this roster were accidental and one was inherent, and only measurement tells them apart -- a cadence tier applied to the first two would have hidden 277 seconds of pure waste behind a slower clock, and left the waste. The sharper half is where the waste hid: neither guard reads anything expensive. One validates bytes, the other greps a ledger. **The corpus was never the cost; the loop around it was**, and a per-item subprocess is invisible in a script that reads correctly. *Repaired (`20260904.225911`):* `tools/fixtures/l/utf8_valid.awk` validates the whole corpus in **one process** under `LC_ALL=C` so `substr` counts bytes, refusing a lead byte with too few continuations, a bare continuation, and an overlong or out-of-range lead; the list is **NUL-delimited**, because `git ls-files` hands back paths with spaces in them and a whitespace-split list drops them in silence. **137s -> 25s**, proven both ways in a five-file pen -- clean and valid-multibyte pass, orphan and bare-continuation and overlong refuse -- and again on the real tree by planting an orphan lead byte and watching the guard name its path. `reds_row_present.sh` grows an `--all` mode answering from **the same sed expressions over the same spine file set**, so the witness keeps that script as its authority rather than growing a second reader beside it, and calls it once: **177s -> 12s**. The single-row path a scan actually calls stays proven on first, middle, last and past-the-end, because a completeness leg running only `--all` would leave that contract tested by nothing -- `%410`'s own shape, one room over. *And one moved rather than mended:* `sow` at **230s** is expensive on purpose -- it projects the whole public seed and scrubs every name from it. It sits at `tier cadence` now, honest because the act it guards is **custody gate %1**, a manual publish that runs this witness in its own flow, so no lap can take the thing it protects. A tier is a cadence rather than an exemption. **Measured together: 1,510s -> 1,003s routine, 1,233s on the cadence lap.** **CLOSED** on `tools/l/living_card_ascii_witness.rish` and `tools/r/reds_row_present_witness.rish`, both GREEN on metal.

**REDS %411 (`20260904.215903`) -- four behaviors every ship performed every lap, and not one of them was a rule.** *What went wrong:* the fleet's shared opening lived in six seat prompts, each restating it in its own words, and the `.claude/rules/` roster -- the thing a fresh clone reads to learn how this tree works -- carried **no rule at all** for four of the eleven things that opening asks. Measured by grepping the 49 rules for each claim: **one writer per checkout** appeared once, in passing, inside `derived-spine.md`, which is a rule about ledger numbers; **claim-as-override** appeared **zero** times; the **council rota** appeared **zero** times; **GATES-ONLY** appeared **zero** times. Each is load-bearing. One writer per checkout is REDS `%291`, a wound that bit four times in four spellings in a single day. Claim-as-override is what stops an idle ship. The rota is a reading habit that has caught a sleeping doorway guard and a silent page in two commanded laps. GATES-ONLY is how an unattended loop stops at a custody gate instead of circling it all night. *What caught it:* Keaton asking, in plain words, whether the rules match what the launch loops load. The honest answer was no, and it took one grep per claim to say so -- the question was better than the tree's own instruments, none of which compare a seat prompt against the rule roster. *What it taught:* **a behavior carried only in a prompt is a behavior a clone cannot learn.** The rules roster is the tree's answer to *how does this place work*, and four of its load-bearing answers lived in a launcher argument and a design essay instead -- readable by an agent already running with that prompt, invisible to one reading the rules cold. The sharper half is why it stayed invisible: every ship behaved correctly the whole time, because every ship was handed the behavior at launch. **A gap that nothing misbehaves over is a gap no meter finds.** Kin to `%410` in the same session, where an exception no input reached was tested by nothing; here it is a rule no reader missed because no reader was ever without it. *Repaired (`20260904.215903`):* `tools/l/fleet_baton.txt` holds the shared opening once and `tools/l/fleet-loop.sh` prepends it, so a seat prompt is now its **lane stanza** alone -- REDS `%409`'s repair applied one room over, to prompts instead of to a seat table. `.claude/rules/the-baton.md` maps every baton section to the rule that governs it and **seats the four that had none**, each written out rather than pointed at. Six control legs hold the shape: a stanza restating the baton, a looping seat with no stanza, and an invocation reaching the agent without the baton are counted at zero, and a `field` seat is exempted by its own engine word rather than by a name. *Found while proving it:* two of those legs passed for the wrong reason on their first draft, because the marker string they grepped for is **line-wrapped inside the baton** and so appears in neither file. A leg that greps for a phrase across a line break tests nothing and reports green; both now key on a line-anchored marker, and one compares line numbers rather than substring order. **CLOSED** on `tools/f/fleet_roster_witness.rish`, 37 behaviors GREEN on metal.

**Rows: 413 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 393** -- counted from the ledger and its archives on `20260905.001837` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 413 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

**REDS %360 (`20260830.100059`) -- two of every three guards in this tree are heard by nothing, and half of the ones I ran are red.** *What went wrong:* the standing roster names 115 paths and reaches 603 of this tree's 1,756 tracked witnesses; the other **1,153** are run by nothing on a lap nobody is watching. Forty-one of them are themselves **choirs** that would sing families of their own; one of the 1,153 is `tools/s/standing_equipment_witness.rish`, the guard that proves this very roster honest, which no roster pass has ever run. **Six** more are guards this tree's own living rules instruct a lap to run -- `tools/co/commit_message_guard_witness.rish`, `tools/m/mechanism_sentence_witness.rish`, `tools/s/sel4_userlevel_license_witness.rish` (which `gratitude-licenses.md` calls *"a standing check, not a one-time read"*), `tools/w/waymark_derive_witness.rish`, `tools/w/waymark_registry_witness.rish`, and `tools/cr/crypto_vendored_parity_suite.rish`. Running the 46 sharpest of them on metal `20260830`: **21 red, 25 green** -- a red rate of 46% in the half no pass reaches. The sharpest instance is operational and sits at a custody gate: `tools/hooks/commit-msg` exempts the public seed's root commit by matching a subject exactly equal to `crashed-meteor`, while `publish-seed.sh` on this bench still writes `Grain OS -- initial public seed`, and `.claude/rules/git-signing.md` writes a third spelling (`grain-ww -- the crashed meteor`) beside `mechanism-sentence.md`'s fourth (`grain-ww -- `). Fed the publisher's actual message, the wall answers **REFUSED -- body words: 201, mechanism words found: 1, wanted: 3**, so the next seed publish fails at its own step 4/6. The guard that proves that exemption is `mechanism_sentence_witness`, which is unrostered, so the breakage has stood since the `20260828` rename with nothing to say it. *What caught it:* the census this row books, built because REDS %357 measured the same gap from the other side -- three witnesses of one family red at HEAD for days, none of them rostered, while a 96-guard cold pass read 95 green. *What it taught:* **a roster is a claim about coverage, and this one was never measured.** The roster's own header has carried both halves of the law since it was written -- a guard that cannot red guards nothing, AND a guard that is never run guards nothing either -- and only the first half had a meter. The sharper lesson is what the unheard reds are MADE of: `want_rows_33`, `want_metric_rev_i9`, `CONC_GUARDS_PRESENT 2` -- expected values pinned to a tree that moved. That is REDS %341/%357's borrowed-number class living one room over, in a scan's own constant rather than in a Rishi assert, where `borrowed_number` cannot see it; and the reason nobody noticed is that a stale pin declares itself by turning the witness red, which is only an alarm if something is listening. *Repaired in part (`20260830.100059`):* the meter exists now -- `tools/u/unheard_guard_witness.rish` over `tools/fixtures/u/unheard_guard_scan.sh` closes the set the roster reaches and holds the remainder under a ceiling that only falls, with the choir reading gated beside it and 27 behaviours proven on real repositories in a throwaway pen. The 21 red guards live in lanes this seat does not hold -- Glow, STOA, SUNN, the equinox choirs -- so each is named here and left to its owner rather than repaired across a lane boundary, and the seed-subject spelling is a naming decision at custody gate %1 rather than a fix an agent takes. *Repaired further (`20260830.154940`):* the five rule-named guards of that set now stand on the roster at `tier lap`, each proven GREEN on metal first -- **18s together** against a 2,425s pass. `unheard` 1,153 -> **1,144**, choirs 41 -> **40**, both ceilings falling with them. The sixth is refused by measurement rather than by lane: `mechanism_sentence_witness` reds at `WALL_EXEMPTS_SEED_ROOT` because the wall exempts subject `crashed-meteor` alone while `publish-seed.sh` **and the wall's own fixture** both write `Grain OS -- initial public seed` -- three sites agree, the wall alone moved, and the witness would have caught it the day it was rostered. The spelling stays custody gate %1. Still **OPEN**. *Repaired further (`20260830.202400`):* the sixth stands rostered. Keaton's word on `20260830.190407` settled the subject at `crashed-meteor` -- the wall, the field's publisher, and both rules speak it, and the real publish shipped on it -- so what remained was not a naming decision but one unswept tracked file: `tools/fixtures/m/mechanism_wall_scan.sh` still planted the retired `Grain OS -- initial public seed` as the exempt subject and `caravan: initial public seed` as the borrow attempt, so the fixture the witness trusts was refused by the wall it tests and `WALL_EXEMPTS_SEED_ROOT` read 0. Two lines carry the settled spelling; fed `caravan: crashed-meteor` the wall still refuses, since the exemption matches the whole subject rather than a suffix. GREEN on metal before the roster row was written, 1s alone. `unheard` 1,144 -> 1,143, `witness_reach` unreached 1,093 -> 1,092 and families 218 -> 217 as the row landed -- and every one of those falls was cancelled before the send by two witnesses that landed upstream unrostered in the same window, so the ceilings stand where they were published rather than recording a fall the tree no longer shows. All six guards our own rules demand now run every lap. *Repaired further (`20260831.014630`):* the two lane-safe choirs stand rostered at `tier lap`, each GREEN on metal before its row was written -- `acme_dx` 3s, `drey` 5s, **8s together** against a cold pass of 63 minutes. Two rows carry **28** guards into hearing, since a rostered choir is heard and so is everything it sings: `heard` 660 -> 688, `unheard` 1,144 -> **1,116**, `unheard_choirs` 40 -> **37**, the third choir being `operations_conformance`, which `acme_dx` sings. Both ceilings fall in the scan and in the witness's published pin, which is two copies on purpose -- a fall costs a deliberate hand in both files -- and `unheard_guard` is GREEN with its 27 pen cases. **The remainder is unchanged and is not this seat's:** 21 red guards in the Glow, STOA, SUNN and equinox lanes, each named above and left to its owner, and the pier clones' stale untracked `publish-seed.sh` copies at Keaton's hand. Still **OPEN**.

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **The pin keeps what is open**, so a row still reading OPEN stays flat however old it is.*
