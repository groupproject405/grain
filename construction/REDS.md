# REDS -- the ledger of what we got wrong

**Language:** EN
**Stamp:** living ledger (born `20260729.222000`) - refreshed `20260801.162056` (self-work arc - rows 58-60 accreted)
**Style:** Gauge (see `../context/GAUGE_STYLE.md`)
**Voice:** Kyri
**Status:** Living pin -- one row per red, oldest first
**Key:** each row's identity is its one-clock **stamp**; the `%N` is a view allocated by the anointed remote `xy` (`.claude/rules/derived-spine.md`, seated `20260827.181605`). A published number never moves. Book the next with `sh tools/fixtures/r/reds_spine_derive_scan.sh --next`. **Three status words** (door B, `20260829`, Keaton's word): **OPEN** is a live defect and stays on the pin; **BOOKED** means the instances stand repaired and the remainder is a ratchet, a seat, or a booked lap, and such a row may fold; **CLOSED** closed on a witness. The last bold marker decides, read one way by `reds_fold.sh` and both status scans.
**Bound:** under `living_pin_max_bytes` (24576)
**Room:** Checkable -- every row names where it was caught

*A red owned in conversation is a memory. A red recorded here is a proof. This ledger exists because a check at Voice v9 found six reds written into the tree and twenty owned out loud -- so fourteen lessons were living only in a chat window that closes.*

---

## Why this file, and what belongs in it

Own-reds-immediately has been a house law for a long time, and it worked: reds were named the moment they were found, out loud, without excuse. What the law never said is **where**. So the ones that happened to land inside a witness header or a spec erratum survived, and the ones that happened in a sentence did not.

Three fields per row, because a red with fewer teaches nothing:

- **What went wrong** -- stated plainly, no softening.
- **What caught it** -- a compiler, a stopwatch, a guard, a second look. Never "I noticed."
- **What it taught** -- the transferable rule, which is the only part that outlives the incident.

A red enters this ledger when it is found. Rows are never edited or removed; a later correction accretes as a new row pointing back.

---

## Voice Chapter - opened `20260729`

| # | What went wrong | What caught it | What it taught |
|---|---|---|---|

**REDS %380 (`20260830.200027`) -- the guard that proves a pass measured one tree reads a status letter, and a status letter stands still while the bytes move.** *What went wrong:* `tree_digest` in `tools/fixtures/s/standing_equipment_run.sh` hashed `git rev-parse HEAD` plus `git status --porcelain`, once before the first guard and once after the last. Porcelain prints a status letter and a path and nothing else, so a file already carrying `M` reads `M path` however often its bytes change -- and so do `??`, `MM`, and a staged `M ` re-staged. All four were proven blind on metal in a throwaway repository: the digest returns character-identical while the tree has moved entirely. The sharpest is the staged one, because `--hot` is exactly the pass a round runs over its own staged paths, and re-staging an edit is the ordinary motion of a round -- so a forty-minute hot close could print `tree_moved=no` over content it never read. *What caught it:* the instrument rather than the reasoning. On `20260830.154940` a hot pass was killed, `construction/ITINERARY.md` was edited, the pass was restarted, and both passes printed `tree_at_open=94b56275df59`. *What it taught:* **a digest over `git status` is a digest of the tree's SHAPE, and the shape is the half that changes least.** The reason it stood is worth more than the red: %221 proved this guard from both sides at seating, and both used a file that APPEARED. The proof was correct; the case it left untested was the more common one. *Repaired (`20260830.200027`):* the content rides beside the shape -- `git diff HEAD --binary` for every tracked difference from HEAD, staged and unstaged in one reading, the index's own blob hashes standing in before a first commit, and untracked files through `git hash-object --stdin-paths` in ONE process rather than a `sha256sum` per file, which a pen at 2,003 files measured at 45ms against 10,393ms. Ignored paths stay outside both readings. Measured here at 15,165 tracked files: 261ms per call against the elder 148ms, twice a pass. A receipt written by the elder digest cannot match this one, so each pier reads `roster_receipt=miss` once and re-chains at its next full green close -- a miss runs every guard, which is the safe direction. `tools/fixtures/s/standing_equipment_control.sh` adds eleven readings on real git repositories in a pen: one showing porcelain standing still across the very edit, six refusing, four free. Run against the ELDER runner the same control answers `no` to all six refusals and `yes` to all four welcomes, so every new assert is load-bearing and none refuses ordinary work. *And the sharper reading found a second fault on its first run:* the runner appended its own hit-ledger row BETWEEN the two digests, making the runner one of the things its own `tree_moved` reading measures. The field gitignores that shelf so the growth was invisible; the control's pen does not, and four cases answered `tree_moved=yes` the moment the digest read bytes. The run card and the evidence room already keep the discipline and say why beside themselves; the ledger predated it. Its write waits for the close now. **CLOSED.**


**REDS %379 (`20260830.212616`) -- the fascia meter read a missing instrument as a clean tree.** *What went wrong:* every signal in `tools/fixtures/f/fascia_metric_v0.sh` is an `rg` count piped through `wc -l`, and a substitution whose command is absent yields an empty stream that wc reads as ZERO -- so a host without `rg` grades every signal clean and prints GREEN over a tree it never read. Mystery's jailed lap ran on exactly that host, watched the meter offer a green it had not measured, and wrote the defect into its custody note rather than staging a false candidate. *What caught it:* Mystery's supervised lap -- and the first live run of the new fleet re-arm printer (`tools/f/fleet_rearm.sh`), which surfaced the note from `supervisor.err` minutes later. *What it taught:* **an absent instrument refuses by name; it never defaults to a clean reading** -- REDS %335's vacuous-guard class arriving through the environment instead of the type system. *Repaired (`20260830.212616`):* a `command -v rg` guard at the meter's head refuses exit 3 with a named reason, and the upstream `readme_metrics_scan.sh` already turns any fascia refusal into `fascia=unknown` / `verdict=unmeasured`. Proven both ways on metal: with `rg` the grade reads the unchanged 68; under a PATH without it, the named refusal. **CLOSED.**

**REDS %378 (`20260830.173703`) -- I added a ledger row without advancing the ledger's derived headline.** *What went wrong:* the first staged package carried the row now numbered `%377` while its headline still claimed 363 rows, 343 additions, and a complete spine through 363. *What caught it:* the hot roster's `reds_ledger_monotone` and `reds_ledger_headline` guards, with the staged tree held still. *What it taught:* **a new ledger row and every headline value derived from the ledger travel in the same edit before the hot pass.** *Repaired (`20260830.173703`):* after the final fetch renumbered the two unshared rows above upstream's published head, the headline reads the measured 378 rows, 358 additions, and a complete spine through 378; the two headline guards and the spine derivation pass on the staged package. **CLOSED.**

**REDS %377 (`20260830.165037`) -- the fold meter charged a constant the destination harness already owned.** *What went wrong:* `ladder_reach_visibility_scan.sh` counted every private declaration reached in the source rungs, so `answered_by` read as 68 openings: 34 private `hand_path` functions and 34 copies of `const Dir = std.Io.Dir;`. The destination `ladder_checks.rye` already declares that exact constant, which means the moved body resolves it there and opens no rung constant. *What caught it:* the crux-first seam read before the fold, comparing the priced source declarations with the destination harness from both sides. *What it taught:* **price a move at the destination seam, and require an exact declaration match before calling a dependency already owned.** *Repaired (`20260830.165037`):* the shell script names exact destination-owned constants as `REACH_OWNED` and leaves them out of the widening price. One planted harness with the same declaration passes at zero; changing only its value prices both rung constants again. The direct witness reads `answered_by` at 34 function openings, zero constant openings, and 330 lines returned. **CLOSED.**


**REDS %376 (`20260830.203324`) -- a path-limited commit and a staging pre-commit hook each behave correctly and together leave the index describing a tree that no longer exists.** *What went wrong:* `tools/hooks/pre-commit` regenerates `README.md`'s metrics block and `docs-geode/libraries/README.md` when a round adds a witness, and stages each with `git add "$page"` into the real index. This seat commits path-limited -- `git commit -S -m ... -- <paths>` -- and Git builds a partial commit from its own temporary index. The commit is correct: `HEAD:README.md` carried the fresh **1750** and the worktree matched it byte for byte, proven by SHA-256 on both pages. The **index** kept the pre-hook blob at **1749**, so `git diff --cached --stat` read dirty immediately after a clean commit, and the seated amend guard of `%255`/`%331` -- which refuses to amend unless the staged set is empty -- refused the Git-nib amend it exists to protect. A lap that reads that refusal at face value concludes a peer landed work between its two calls, which is exactly the fault the guard was drawn for and exactly what had not happened. *What caught it:* the guard itself, printing `index not empty or HEAD moved -- amend refused` over a tree whose `git status` showed only `MM` on the two pages the hook had just rewritten -- two readings that cannot both be innocent. *Proven on metal both ways* in throwaway repositories rather than reasoned about: one pre-commit hook that rewrites and stages a file, committed **with** a pathspec, leaves HEAD `gen 2`, index `gen 1`, worktree `gen 2` and a dirty `--cached`; the identical hook committed **without** a pathspec leaves all three at `gen 2` and the index clean. The pathspec is the whole difference. *What it taught:* **two correct mechanisms compose into a wrong reading when one of them writes to a store the other has already forked.** It is `%374`'s own class one room over -- there a counter carried two meanings for a mechanism that never asked which; here an index carries two histories for a guard that never asked which commit made it. The guard reads a real store and reports honestly about it; the store simply stopped describing the commit. *Named, not taken:* three repairs are open and each crosses a shared surface -- the hook could refresh the index after staging, the amend guard could compare against HEAD's tree rather than the index, or the seat could commit whole-index after staging precisely. The first two are read by every body in the fleet and the third is the seat prompt's own wording, so the choice is Keaton's rather than a lap's. This round worked around it by re-syncing the two paths with `git restore --staged` after proving HEAD and worktree identical, then amending without a pathspec, and says so rather than leaving a silent divergence. *Read again `20260830.204814`:* the third named move was taken as one round's own practice rather than as a change to any shared surface -- the paths were staged precisely, `git status --porcelain` showed nothing else staged, and `git commit -S -F` ran with **no pathspec**, so the pre-commit hook's two `git add` calls landed in the same index Git built the commit from. The two regenerated pages rode in that commit rather than being left behind it, the index read empty immediately after, and the Git-nib amend passed the `%255`/`%331` guard unaided. That is one round's evidence that the seat-side move works and costs nothing. *Sealed `20260830.223044`* on Keaton's word (approve-your-recommendations, this seat recommending the third door as the only repair crossing no shared mechanism): the practice is worded on all six seat surfaces -- stage exactly your set, prove the index holds nothing else, commit whole-index so the staging hook's pages ride the same commit -- and this seal's own commit is its first resident. **CLOSED.**

**REDS %374 (`20260830.192524`) -- the receipt is written past the red exit, so a tree whose reds are parked at a custody gate can never earn the cheaper pass, and the refusal told it to run the roster again.** *What went wrong:* `tools/fixtures/s/standing_equipment_run.sh` writes `construction/standing-equipment-receipt.kyri` below the `if [ "$red" -ne 0 ]` branch that already exited 1, so a full pass carrying any red leaves no receipt at all -- and `--scoped` reads that receipt for its basis and refuses without one. On this clone both of the roster's red guards sit at custody gates the living card names, `pond_enclosure_door` at gate %5 and `rule_twin` at gate %7, so every full pass closes `guard_red`, no receipt has ever been written here, and the fusion build's cheaper pass is unreachable by construction rather than by delay. The refusal read `run the full roster once` to a hand who had run it twice that morning, at **2h27m** a pass -- the run card's open stamp `20260830.080600` against its own mtime. The same shape stands one layer up: `tools/s/standing_equipment_witness.rish`, the guard that proves the roster honest, refuses at its scan leg for `runs_red=2` on those same two gated rows, so the instrument that measures the roster is unrunnable on exactly the trees carrying a gate. *What caught it:* this lap's own cold open, which printed `roster_receipt=none` while `construction/standing-equipment-runs.kyri` held a complete 96-guard pass from that morning; both readings cannot be innocent at once. *What it taught:* **one counter carrying two meanings decides for a mechanism that never asked which one it meant.** `red` counts *this guard broke* and *this guard is parked at a gate the maintainer named* identically, and the receipt gate reads their sum -- REDS %338's own class, where OPEN carried both *live defect* and *instances repaired, remainder booked* until the ledger deadlocked on it. *Repaired (`20260830.192524`):* additive readings, no gate moved -- a full pass closing red prints `roster_receipt_write=withheld_guard_red`, and a `--scoped` without a basis prints `scoped_basis_blocked=<guards>` read from the run card, eight names then a count, with a refusal that names them rather than prescribing a pass that cannot help. Six cases in the throwaway pen, three proven to bite under a runner stripped of both lines; the fourth is written down as a companion rather than counted as a refusal it is not. **Whether a receipt may chain from a close whose only reds are custody-gated and card-named is Keaton's word**, since it loosens what a receipt promises -- until then the diagnosis is honest and the cost stands. Still **OPEN**.
**Rows: 380 - in the tree before the ledger: 6 - recovered by opening it: 14 - added under the reds-first law: 360** -- counted from the ledger and its archives on `20260830.214924` by `tools/gen/chapter/reds_ledger_monotone_witness.rish`, which reads the spine off disk rather than from a list, so the tally moves with a measurement rather than with a memory, since a tally repeated from memory drifts (REDS %93). Only the first number is measured; the other three are the opening census of `20260729` -- 6 already written into the tree and 14 recovered out of chat windows, 20 together -- and the remainder, which is every row added since. That remainder read 84 for a long while and had stopped moving as rows accreted, so the three no longer summed to the total. It is derived now (rows minus the opening 20) rather than recited, which is REDS %110's own lesson applied to the ledger's own headline. The whole fold recital -- which rows folded, on what stamp, and onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md); it had grown to 5,999 bytes inside this headline, longer than the rest of this page's prose and parsed by nothing. Every number from 1 to 380 is used; the elder rows wear `#` and the living ones wear `%` (`.claude/rules/git-signing.md`).

**REDS %360 (`20260830.100059`) -- two of every three guards in this tree are heard by nothing, and half of the ones I ran are red.** *What went wrong:* the standing roster names 115 paths and reaches 603 of this tree's 1,756 tracked witnesses; the other **1,153** are run by nothing on a lap nobody is watching. Forty-one of them are themselves **choirs** that would sing families of their own; one of the 1,153 is `tools/s/standing_equipment_witness.rish`, the guard that proves this very roster honest, which no roster pass has ever run. **Six** more are guards this tree's own living rules instruct a lap to run -- `tools/co/commit_message_guard_witness.rish`, `tools/m/mechanism_sentence_witness.rish`, `tools/s/sel4_userlevel_license_witness.rish` (which `gratitude-licenses.md` calls *"a standing check, not a one-time read"*), `tools/w/waymark_derive_witness.rish`, `tools/w/waymark_registry_witness.rish`, and `tools/cr/crypto_vendored_parity_suite.rish`. Running the 46 sharpest of them on metal `20260830`: **21 red, 25 green** -- a red rate of 46% in the half no pass reaches. The sharpest instance is operational and sits at a custody gate: `tools/hooks/commit-msg` exempts the public seed's root commit by matching a subject exactly equal to `crashed-meteor`, while `publish-seed.sh` on this bench still writes `Grain OS -- initial public seed`, and `.claude/rules/git-signing.md` writes a third spelling (`grain-ww -- the crashed meteor`) beside `mechanism-sentence.md`'s fourth (`grain-ww -- `). Fed the publisher's actual message, the wall answers **REFUSED -- body words: 201, mechanism words found: 1, wanted: 3**, so the next seed publish fails at its own step 4/6. The guard that proves that exemption is `mechanism_sentence_witness`, which is unrostered, so the breakage has stood since the `20260828` rename with nothing to say it. *What caught it:* the census this row books, built because REDS %357 measured the same gap from the other side -- three witnesses of one family red at HEAD for days, none of them rostered, while a 96-guard cold pass read 95 green. *What it taught:* **a roster is a claim about coverage, and this one was never measured.** The roster's own header has carried both halves of the law since it was written -- a guard that cannot red guards nothing, AND a guard that is never run guards nothing either -- and only the first half had a meter. The sharper lesson is what the unheard reds are MADE of: `want_rows_33`, `want_metric_rev_i9`, `CONC_GUARDS_PRESENT 2` -- expected values pinned to a tree that moved. That is REDS %341/%357's borrowed-number class living one room over, in a scan's own constant rather than in a Rishi assert, where `borrowed_number` cannot see it; and the reason nobody noticed is that a stale pin declares itself by turning the witness red, which is only an alarm if something is listening. *Repaired in part (`20260830.100059`):* the meter exists now -- `tools/u/unheard_guard_witness.rish` over `tools/fixtures/u/unheard_guard_scan.sh` closes the set the roster reaches and holds the remainder under a ceiling that only falls, with the choir reading gated beside it and 27 behaviours proven on real repositories in a throwaway pen. The 21 red guards live in lanes this seat does not hold -- Glow, STOA, SUNN, the equinox choirs -- so each is named here and left to its owner rather than repaired across a lane boundary, and the seed-subject spelling is a naming decision at custody gate %1 rather than a fix an agent takes. *Repaired further (`20260830.154940`):* the five rule-named guards of that set now stand on the roster at `tier lap`, each proven GREEN on metal first -- **18s together** against a 2,425s pass. `unheard` 1,153 -> **1,144**, choirs 41 -> **40**, both ceilings falling with them. The sixth is refused by measurement rather than by lane: `mechanism_sentence_witness` reds at `WALL_EXEMPTS_SEED_ROOT` because the wall exempts subject `crashed-meteor` alone while `publish-seed.sh` **and the wall's own fixture** both write `Grain OS -- initial public seed` -- three sites agree, the wall alone moved, and the witness would have caught it the day it was rostered. The spelling stays custody gate %1. Still **OPEN**. *Repaired further (`20260830.202400`):* the sixth stands rostered. Keaton's word on `20260830.190407` settled the subject at `crashed-meteor` -- the wall, the field's publisher, and both rules speak it, and the real publish shipped on it -- so what remained was not a naming decision but one unswept tracked file: `tools/fixtures/m/mechanism_wall_scan.sh` still planted the retired `Grain OS -- initial public seed` as the exempt subject and `caravan: initial public seed` as the borrow attempt, so the fixture the witness trusts was refused by the wall it tests and `WALL_EXEMPTS_SEED_ROOT` read 0. Two lines carry the settled spelling; fed `caravan: crashed-meteor` the wall still refuses, since the exemption matches the whole subject rather than a suffix. GREEN on metal before the roster row was written, 1s alone. `unheard` 1,144 -> 1,143, `witness_reach` unreached 1,093 -> 1,092 and families 218 -> 217 as the row landed -- and every one of those falls was cancelled before the send by two witnesses that landed upstream unrostered in the same window, so the ceilings stand where they were published rather than recording a fall the tree no longer shows. All six guards our own rules demand now run every lap. **The remainder is unchanged and is not this seat's:** 21 red guards in the Glow, STOA, SUNN and equinox lanes, each named above and left to its owner, and the pier clones' stale untracked `publish-seed.sh` copies at Keaton's hand. Still **OPEN**.

---

## What the pattern says

Read down the *what caught it* column and almost nothing was caught by thinking harder. A compiler, a stopwatch, a file-type check, a guard's first run, seven witnesses at once, a pack catching its own author. **The machine caught nineteen of twenty.**

That is not a complaint about judgment; it is the argument for the whole apparatus. Witnesses exist because the author is the last person able to see their own gap, and this column is twenty lines of evidence for that claim rather than one more assertion of it.

Read down the *what it taught* column and four rules recur: **measure before claiming**, **scope before shipping**, **fixture rather than remember**, and **narrow the objection**. Those four have paid for themselves repeatedly in a single sitting.

---

*May every red find this page on the day it happens. May the lesson outlive the incident. And may the count of what we got wrong be as witnessed as the count of what we got right.*

*The `autoproject96` -> `groupproject405` standfast folded to [`archive/REDS-the-organization-rename-standfast.md`](archive/REDS-the-organization-rename-standfast.md) on `20260823.204456` as row %175 carried the living pin past its bound. **It still stands** -- the living rename is done, and the deep debride of 11 commit-message occurrences awaits Keaton's word.*

*Elder rows fold onto shelves under [`archive/`](archive/) as the pin nears its 24,576-byte bound, and the whole recital -- which rows, on what stamp, onto which shelf -- reads at [`archive/REDS-fold-recital.md`](archive/REDS-fold-recital.md). One note stands here rather than one per fold, because a note per fold grows this pin by exactly what folding shrinks -- %232's own lesson, turned on the note itself. **The pin keeps what is open**, so a row still reading OPEN stays flat however old it is.*
