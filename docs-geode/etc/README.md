# Etc -- the room for a genre that does not exist yet

**Where this sits:** home is [`../../README.md`](../../README.md) - a first hour in your hands is
[`../tutorials/the-first-hour.md`](../tutorials/the-first-hour.md) - the whole
path from nothing to a signed, sandboxed home is [`../../SOURCE.md`](../../SOURCE.md)
**Language:** EN - **Style:** Gauge, Door setting (see `../../context/GAUGE_STYLE.md`)
**Written:** `20260821.190149` - **Last refreshed:** `20260911.025732` -- the neighbor list becomes a declared key a guard reads, on the lap it was found a room short - **Status:** Living -- **checkable room**: the entry criterion, the promotion rule, and the neighbor list are all readable off this shelf - **Kind:** named entry criterion
**Neighbors:** the rooms standing beside this one under [`../`](../) -- `api`, `blog`, `demos`, `edu`, `lessons`, `libraries`, `press`, `sangha`, `study`, `templates`, `tutorials`, `wiki`

---

Every shelf of shipped documentation eventually meets a page that fits outside all of its shelves. This room takes that page, once, so *where does this live?* always has an answer.

**What would enter:** a shipped surface that is genuinely finished, and whose genre falls outside every room named in the `**Neighbors:**` key above.

**What happens next when something does:** a second page of the same kind earns that kind **its own room**, and both pages move there. So this room is a waiting area, held for one page at a time. A permanent `etc/` would name a filing system that has given up.

**Why it waits:** the genres named above have covered everything shipped so far. That is a good sign about the genres rather than about this room.

**The list is a key rather than a sentence, and here is why.** This page named eleven neighbors, by count and by name, while twelve stood: [`../lessons/`](../lessons/README.md) landed `20260910` in commit `e40799aba`, and the one room whose whole job is to notice a new genre was the room that missed one. The promotion rule above had fired and this page slept through it. So the neighbors moved into the `**Neighbors:**` key, where [`../../tools/r/room_enumeration_witness.rish`](../../tools/r/room_enumeration_witness.rish) reads them against the rooms on disk each lap -- the same move the shelf front door made when its prose said ten rooms over a table of twelve, one floor up.

The next genre to arrive will find this shelf ready, and the lap it arrives on will hear about it.

---

*May this room keep waiting, and may it stand ready the day something arrives.*
