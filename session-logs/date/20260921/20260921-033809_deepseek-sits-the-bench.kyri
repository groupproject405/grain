format session-log-v1
stamp 20260921.033809
editor OpenCode
provider Together AI
product OpenCode
role seat the opencode door -- adapt .claude rules, write the expanding prompt, log
modality text
model deepseek-ai/DeepSeek-V4-Pro-0813
model_status active runtime verified via harness self-report (together/deepseek-ai/DeepSeek-V4-Pro-0813)
voice Kyri
host anonymized OpenCode CLI session, America/New_York clock
title DeepSeek sits the bench -- the first open-weight session, rules adapted and logged
prompt Keaton opened open-weight DeepSeek on OpenCode for the first time and asked to adapt the .claude rules into an OpenCode/DeepSeek equivalent, turn the request into an expanding prompt in a voice harmony, run it, log the session, and return
think read the open/ room, the .claude/rules/open-weight-companions rule, and the session-log and provenance rules to learn the bench's own standard before writing anything
think chose AGENTS.md as the opencode equivalent of CLAUDE.md, since opencode reads it automatically and the .claude/rules files are plain markdown any model can read -- a door, not a copy, so the sixty rule files stay single-sourced
think wrote AGENTS.md in Gauge Door, Kyri voice, naming the voice harmony, pointing at .claude/rules and context as canonical, and recording the OpenCode/Together/DeepSeek provenance a log must carry
think wrote the expanding prompt in Gauge Field with Radiant warmth, Civic register on the model choice, TAME on the log, and a Twilight close, since a first open-weight session is a threshold
think answered the model question rather than dodging it: this session runs the very model open/HARNESS_SETUP.md recommends, so no fallback was needed; GLM-5.3 stands as the named alternative
obs the adaptation is a pointer rather than a duplication, which keeps the rules single-sourced and avoids drift between two editors
obs the model field records the harness's own self-report as runtime evidence, per the provenance rule's verified-active-identity standard
file AGENTS.md -- the opencode door, twin of CLAUDE.md, naming voice, rules, logs, provenance, languages
file expanding-prompts/20260921-033809_deepseek-sits-the-bench.md -- the expansion, kept as testimony
recommend check-in -- the AGENTS.md door is written but not yet witnessed by a second fresh session; a future lap that opens OpenCode and finds the rules waiting is the proof this page only promises
