format session-log-v1
stamp 20260920.192100
editor Claude Code
provider Anthropic
product Claude Code
role writing -- Together AI as open/'s default demo, a full register pass across the room
modality text
model unverified
model_status active runtime unverified; no get_session introspection run this lap
voice Kyri
host anonymized Claude Code CLI session, America/New_York clock
title PROVIDER_SETUP.md rebuilt around Together AI as the default demo, other providers noted as similar-but-different, and a full Bhakta/Radiant/Twilight register pass closed the round
prompt the operator asked to update every open/ writing so Together AI (the just-recommended starting path) becomes the default demoed example throughout, noting that other providers are similar but a little different, and asked for another Bhakta/Radiant/Twilight register pass across all the writings before marking the task complete
think verified Together AI's actual API shape before rewriting anything rather than assuming it matched OpenRouter's -- fetched Together's own quickstart and confirmed the base URL (https://api.together.ai/v1), the Bearer auth header, the TOGETHER_API_KEY environment variable, and the vendor/ModelName slug format (deepseek-ai/DeepSeek-V4-Pro-0813, zai-org/GLM-5.3), none of which were assumed to simply mirror OpenRouter's own shape
think tested the llm CLI's Together plugin on this pier before recommending it, the same discipline the prior round applied to llm-openrouter -- tried both candidate plugin names, llm-together and llm-togetherai, through nix-shell -p '(python313Packages.llm.withPlugins {...})', and both built without error yet returned an empty plugins list either way, meaning neither is genuinely packaged under that attribute on this pier today
think reported that finding honestly rather than either recommending an untested plugin or silently dropping the CLI section -- named the two plugin names, named that they were tried, named that neither loaded, and pointed to the plain curl path as the reliable one, since Together's own documentation leads with curl regardless of host
think restructured open/PROVIDER_SETUP.md's Part 1 from "what a router is" to "one worked example, and why this page picked it," since Together AI is a direct provider rather than a router and the elder framing no longer fit the page's own new center of gravity
think rewrote Parts 2 and 3 entirely around Together AI's real signup flow, key storage, and curl verification, replacing every OpenRouter-specific instruction with Together AI's own -- kept the security section (key stays outside the repository, rotate after passing through an uncertain channel, the widened scrub guard) unchanged in substance since none of that reasoning is provider-specific
think folded OpenRouter's own walkthrough into a new Part 4, "similar, but a little different," rather than deleting it -- a table naming the base URL, environment variable, and model-slug shape for Together AI, OpenRouter, and both Hugging Face products side by side, plus OpenRouter's own curl call kept verbatim for direct comparison, so a reader who specifically wants OpenRouter is not sent elsewhere to find it
think re-ran the whole-tree register scan and each open/ file's own explain pass after the rewrite, per the discipline this same room learned the hard way in an earlier round -- every file read verdict=ok, and the one flagged law page (session-log-provenance.md) is the same pre-existing, already-accounted-for exception from before this round began
think caught a real error before it shipped: PROVIDER_SETUP.md's own header stamp was typed as 20260920.200500 without first reading the live clock, and the actual clock read 20260920.192026 at the moment of writing -- a stamp eight minutes in the FUTURE, which the one-clock law forbids fabricating under any circumstance. Read the real clock and corrected the header in the same pass, before this log or any commit could carry the wrong number forward
think checked HARNESS_RATIONALE.md, COMPANION_MODELS_GUIDE.md, and TWILIGHT_NOTE.md for any OpenRouter-specific framing that would now read inconsistently against a Together-AI-demoed setup page, and found none -- all three already spoke about models and harnesses in general terms rather than naming a specific provider, so no rewrite was owed there
obs kept every provider's own reward in Part 4's table honest rather than reordering it to flatter the newly-demoed provider -- Together AI is named for what it actually rewards (US-default, per-token, breadth), not elevated to "best" above the others, since the room's own Civic-register discipline asks that a table state what each thing optimizes for rather than declare a winner
obs the caught stamp error is itself worth naming in this log rather than quietly fixing and moving on, since the one-clock law's whole point is that a fabricated stamp is indistinguishable from a real one once it ships, and the only defense is catching it before that happens
file open/PROVIDER_SETUP.md -- rewritten: Together AI is now the walked-through demo across Parts 1-3, OpenRouter and both Hugging Face products moved into a new Part 4 comparison table and kept curl-callable, stamp error caught and corrected before commit
file open/README.md -- edited: one table-row description updated to name the new default demo
recommend check in (either) -- confirm Together AI is the provider worth an actual first fleet-loop integration, now that the demo, the policy, and the comparison all point the same direction; nothing else in this round needs a decision
