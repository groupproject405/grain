# gen -- Generators

**Status:** Living -- room open; the desks remain at `glow/gen/` until the breach speaks
**Where this sits:** home is [`../../README.md`](../../README.md) - a first hour in your hands is
[`../../docs-geode/tutorials/the-first-hour.md`](../../docs-geode/tutorials/the-first-hour.md) - the whole
path from nothing to a signed, sandboxed home is [`../../SOURCE.md`](../../SOURCE.md)

The `.glow` desks stand today at [`../../glow/gen/`](../../glow/gen/). They arrive here in one
declared motion -- relocate and re-point, keeping every desk's own bytes -- once the O2 instrument
stands green-ready and Keaton declares O3. While that word is awaited this README is the room's
one resident, and new work still writes the path `glow/gen/`.

**Ask the room how large it is**, and read the answer it gives:

```
rishi/bin/rishi run tools/g/gen_home_witness.rish
```

That is the O2 instrument itself. It proves the letter fold -- zero flat desks, every desk in the
room its own first letter names, `hoon-study/` kept whole -- and then prints the desk total beside
the count of tool files speaking `glow/gen/`. Both totals are measured on the run rather than
pinned here, since a count carried in prose drifts the first lap that edits one of the two (REDS
%93). This page said `Three hundred eighteen` desks and `eighty-four` witnesses until
`20260908.145510`, when the witness answered **352** and **29**. It had been answering honestly all
along, on no clock, where a reader would meet it only by typing its name. The working repository's standing roster runs this check every lap.
