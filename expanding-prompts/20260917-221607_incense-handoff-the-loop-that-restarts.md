# Incense handoff -- the loop that restarts, and the tool it stopped reaching for

**Language:** EN
**Status:** Living -- a handoff, read once at the top of a fresh incense lap
**Room:** checkable -- every claim below names a witness or a command
**Style:** Bhakta with Radiant warmth; Gauge at Field
**Voice:** Kyri

Read this first. The watcher re-arms this seat when the interactive session ends, so the next lap
opens unattended -- and everything below is what one long hand-held session learned, in the order a
fresh lap needs it.

## Open the lap in this order, and hold still

The baton's ORDER clause earned its words this session. Open the round, **push any claim**, let the
rebase settle, read `HEAD` once, **then** launch the cold run and hold still until its transcript
carries `run_verdict=`. A pass launched near a lap's close is a pass the next lap's round-open
pull will spend.

```sh
sh tools/fixtures/s/standing_equipment_run.sh --detach
```

## The one thing that will bite first

**A living pin over its byte bound refuses every commit on every ship.** The card crossed twice in
ninety minutes on `20260917`, and each crossing stopped the whole pier until a seat shelved one of
its own accounts. If a commit is refused, read the reason before assuming it is yours:

```sh
bash tools/fixtures/p/pin_bound_touch_scan.sh index
```

The cure is to shelve **your own** landed account whole into `construction/archive/`, leaving a
pointer and any open question behind. A peer's live narrative stays theirs.

## What landed, and how to check each in one command

**The fleet's model reading tells the truth about this clone.** A per-ship local settings file --
denied by `.gitignore` line 146, and **absent from this tree**, which is why incense still resolves
Opus -- outranks the tracked settings file per key. Seven ships run `claude-sonnet-5` while all
eight declare `claude-opus-5`. Record the RESOLVED value as `configured_model`.

```sh
sh tools/fixtures/d/declared_model.sh resolved_model
sh tools/fixtures/d/declared_model.sh override
```

**The harness can now read its own process fan-out**, rostered at `tier cadence`.

```sh
sh tools/fixtures/p/process_fanout_scan.sh --sample 6
```

**Four scans stopped spawning a tool for what the shell already does.** The arc, all byte-identical:

| Scan | Processes | Wall |
|---|---|---|
| `standing_equipment_scan` | 4,330 -> **45** | 20,641 -> **531 ms** |
| `section_citation_scan` | 1,015 -> **54** | 4,388 -> **1,027 ms** |
| `root_finder_scan` | 904 -> **382** | 4,109 -> **1,814 ms** |

**`MAP.md` reaches every front door.** Seventeen living READMEs stood outside every link on the
walk; the unreachable count reads zero.

## The five habits this session paid for, each in one line

- **A site is not a count until its loop is measured.** Three identical subexpressions on
  consecutive lines removed 36 of 515 spawns; the rest sat two hundred lines away in a loop running
  twenty times more often. The trace said so all along and was read as a list of sites.
- **Compare against a held-still input.** A before-and-after reading differed only because a guard
  run in between had rewritten a file the scan reads. Stash, run, unstash, run, compare -- both
  versions in the same moment.
- **Run the language before adding to it.** A primitive was proposed, approved and already present:
  Rishi declares `read-file`, `lines`, `where`, `length` and `contains`, and answers in one process
  where the shell form takes two.
- **A partial sample is not a population.** A running median at nine readings said one thing and the
  finished eleven said another; the earlier call was made out loud and was wrong.
- **Write the prediction in the claim so it can fail out loud.** Five were written for `root_finder`
  and three held. The two misses named the class's boundary better than the hits did.
- **Never wait on a command-line pattern** (`%824`, booked minutes before this prompt was written).
  `until ! pgrep -f "<name>"` matches the waiting shell's own command line and waits forever, and
  the silence is the danger: a loop waiting on itself reads exactly like a loop waiting on work.
  Wait on a **content predicate** instead -- `standing_equipment_run.sh` is finished when its
  transcript carries `run_verdict=` -- or ask the bounded reader, which refuses the caller by name:

```sh
sh tools/f/fleet_call.sh --pattern <substring>
```

## What is open, and what each wants

- **The pin-bound oscillation.** Both `construction/REDS.md` and `construction/ITINERARY.md` are
  pages whose content does not shrink, held by byte bounds, crossing every few laps. The tree's own
  archived account already ruled a bound the wrong instrument here. **Keaton's word.**
- **`REDS %823`, BOOKED.** The lint's duty 3 names orphans against an inbound set that never reads
  `MAP.md`. It still reports 24 where a reader now reaches every one. **The lint's own lane.**
- **The fan-out census over the remaining 165 guards.** Eleven of 177 are read, at a 58-fold spread.
  Four files were repaired by eye; the meter exists so the fifth is chosen by measurement.
- **The nineteen open ledger rows**, each belonging to the lane that booked it.

## Where the reasoning lives

`active-designing/20260917-211946_the-verb-already-in-your-hand.md` carries the reading that the
engine of this tree is POSIX shell rather than Rye, Rishi or Glow -- 92 percent of the cold run --
and why that is an ordering rather than a mistake. This day's logs stand under
`session-logs/date/20260917/`, newest first on that day's shelf.

May the next lap find the harness lighter than it was, and the reasons already written down.
