# Fifteen asks, sorted for the fleet -- rough draft, HELD

**Language:** EN - **Style:** Gauge, Field setting - **Voice:** Kyri
**Stamp:** `20260908.114418` - **Status:** ROUGH DRAFT, **held on Keaton's own word**
**Room:** mixed -- the measurements are checkable, the sequencing is a proposal
**Asked by:** Keaton, `20260908`, fifteen items in one sitting
**Blocked until:** the HTML file and the other agent's context arrive. *"Before running it, wait for
me to give you the html file and context from the other agent."* Nothing below is started.

## What this page is

Fifteen asks arrived together. This sorts them, measures the ones a number can settle, names which
are rulings and which are migrations, and marks what can run in parallel. **It starts none of them.**

## The first finding: five of the fifteen are one question

Items **1, 6, 10, 11**, and the naming half of **9**, are the same shape -- *one word carrying two
jobs* -- which is the braid this tree has found in a dozen rooms this week. Treating them as one
discipline rather than five migrations is the single largest saving available here.

| Ask | The collision | Files carrying the word |
|---|---|---|
| **1** | `mand` / `mandi` / `mandate` -- three spellings, unclear whether one thing | 240 / 168 / 140 |
| **6** | `compass` -- the return habit *and* a Comlink topology | **781** |
| **10** | `topology` vs `constellation` -- synonyms or distinct? | 249 / 562, plus `constel` 351 |
| **11** | `equinox` -- a calendar ring used as a plan subdivision | **1,470 files, 5,370 mentions** |

**The rule these need already exists and none of them cites it.** `stamp-and-name` settles the
equinox case outright: *a calendar ring is a capacity, never a label*, seated `20260829` after the
same misuse. Item 11 is therefore **not an open question** -- it is that law applied at scale, and
what it needs is a word on the replacement term rather than a debate about whether to move.

**The cost is why this is one discipline and not five renames.** 5,370 mentions of `equinox` and 781
of `compass` cannot be swept: dated testimony keeps every word it wrote, so each is a *living-surface
sweep* with a resolver, exactly as the `date/` fold was. A rename without that shape breaks
references, and references are promises.

## Sorted -- what each ask actually is

**RULINGS (cheap, unblock others, no migration).** These want a word, not a lap.

- **1** -- are `mand`/`mandi`/`mandate` one thing? Until this is answered, 5 and 11 cannot be.
- **5** -- does `mand` ring 2/3 fall under `stamp-and-name`? Follows directly from 1.
- **10** -- are topologies and constellations synonyms? Decides 6's replacement term.
- **11** -- `equinox` -> `section`. Law already decided the principle; the word wants approval.

**LAWS AND VOICE (writing, no code).**

- **4** -- a third register beside Radiant and Twilight. **Recommend `Bhakta`** over `Classroom`:
  the tree names its registers for what they *are*, not what they're for, and Classroom names a
  room. Bhakta carries the devotional lens you describe and sits beside Radiant and Twilight as a
  peer. Its brief: no assumed CS background, first principles, uplifting, and every description
  rooted back to biocyclic vegan ecology and the path with heart.
- **3** -- Simple Lovable Complete synthesized into a rota foundation, replacing a seat.
  **Precedent exists**: Water's third seat changed `20260908` by measurement, and the method is
  written down. Same method applies.
- **7** -- TAME Guidance molt.
- **13** -- Rishi as *the Grain build system that is incidentally a bounded shell*, with fusion as
  its genius. This is a reframing across the Rishi docs, not a code change.

**ROOMS (structural, needs a survey first).**

- **2** -- a `docs-spec-designing` room for reference-grade API and OS manual docs; `docs-geode`
  keeps its other rooms; possibly unify `manual/`. Measured today: `docs-geode` **43** tracked,
  `manual/` **33**, `docs/` **16**. That is already booked to petrichor
  (`20260907.074407`), whose lane docs-geode is, and this ask *widens* that booking rather than
  opening a second.

**BUILDS (real work, each a chapter).**

- **9** -- **Sunflower**, task management in TAME style over Dexter, Brushstroke, Mantra, Tablecloth,
  Kumara. Name is free to check; the shape wants its own design page before any code.
- **12** -- Caravan and Microkit reframed through bounded radial/polar/toroidal compute, sheaves and
  topos, unified with Comlink hops and colocation. **This is research, not a build**, and belongs
  beside item 8.
- **14** -- a bounded topos as the recursive product of a bounded degree of freedom and a bounded
  cyclic pattern; *itinerary x rota/wheel*. Same family as 12.
- **8** -- mine `gratitude/grain-lineage/` for toroidal/aetheric insights to reframe foundations.
  **Feeds 12 and 14** -- so it runs first of the three.

**MIGRATIONS (large, word-gated).**

- **15** -- retire Android/GrapheneOS work for the Caravan/Microkit/seL4 mission. Measured:
  **143 files** carry `grapheneos`, **141** carry `android`. This is a molt-breach plus a mitra
  shed-prep at real scale, and the HAWM waymark's whole ladder is in that population. It wants a
  checkpoint before it starts and a shred-prep list rather than a cut.

**BLOCKED.**

- **14 (the SLC half)** -- convert the other agent's HTML to Brushstroke for Skate on localhost,
  over Comlink/Mantra/Tablecloth, with Tablecloth possibly widened to serve as a web server over
  Comlink or a TAME redesign of the Nostr protocol; then a yonder design for a Brushstroke DM
  component as a Skate/Pond application over Caravan rather than Android. **Held for the file.**

## How it parallelizes across the fleet

The rulings are serial and first, because four other asks depend on them. Everything else fans out
by lane rather than by topic, which is how the roster already draws them.

| Wave | Runs | Lane |
|---|---|---|
| **1, serial** | asks 1, 5, 10, 11 -- the naming rulings | incense (law) |
| **2, parallel** | 4 and 3 (voice and rota seat) - 7 (TAME) - 13 (Rishi framing) | incense - grass - pheromone |
| **2, parallel** | 2, widening the existing docs booking | petrichor |
| **2, parallel** | 8, then 12 and 14 behind it | diffuser with bakery |
| **3, gated** | 15, the Android retirement | copal (shed-prep) on a checkpoint |
| **held** | 14's SLC half | incense, on the file's arrival |

## What I would not do, and why

**Not one migration lap for the five naming asks.** Each rename is a living-surface sweep with a
resolver and a dated-testimony exemption; doing them together means one mistake breaks five sets of
references at once. One at a time, `equinox` last because it is 5,370 mentions.

**Not `Classroom` for the voice.** It names a room rather than a register, and this tree's other two
registers name what they are.

**Not a second docs booking.** Ask 2 widens petrichor's existing one; opening a parallel booking
would put two hands in one room, which is the wound `%291` names.

## The single question that would unblock the most

**Are `mand`, `mandi` and `mandate` one thing?** Asks 1, 5 and part of 11 wait on it, and it is a
question only Keaton can answer, because the answer is what those words were *meant* to mean.
