# CION resumes -- the rung-mark molt campaign for a high-volume seat

**Stamp:** `20260829.221841` -- **Language:** EN -- **Style:** New Gauge, Field -- **Voice:** Kyri
**Status:** SEATED `20260829.222718` on Keaton's word -- Mystery carries the campaign (seat card
`recursion-prompts/versions/20260829-222718_codex-mystery-macos-loop-cion.md`, lane wall widened
to the campaign's code rooms); the conversion spelling below is seated with it. Tier D's cuts
still wait for a circled word per cut, as the tier itself says.
**Chapter:** CION (VOLS - LOWE - OFFY - GRAD). VOLS ran and is instrumented; the first LOWE sweep
landed; the GRAD seal stands GREEN. This page is the remainder the spec already names: "the wider
LOWE relabel across the rest of the living collection and any OFFY cut."
**Canon:** `context/specs/20260810-222755_chronological-semantic-labeling-and-the-cion-meta-season.md` -
`.claude/rules/stamp-and-name.md` - `.claude/rules/molt.md` - `.claude/rules/debride.md` -
`.claude/rules/checkpoint.md`
**Meter:** `tools/w/waymark_rung_drift_witness.rish` -- measured `20260829`: `rung_marks_living=17246`
under `rung_ceiling=17257`, ceiling falls only. Seal: `tools/gen/chapter/grad_seal_witness.rish`.
**Seat:** a high-volume loop model on a campaign lane (the Mystery harness shape --
`tools/l/chatgpt-mind.rish` supervisor, pinned lane scanner, park-on-block). The interactive
custody bench rules and spot-grades; it does not grind.

## The finding this expands

Measured `20260829` (workflow census, stratified 40-hit sample): living STOA-digit marks number
2,076 across 572 files; ALES-digit 7,196 across 510 files (lotus/ alone 4,685). Every sampled hit
is a citation of a landed rung -- zero forward counters anywhere living, and the only mentions of
a future number are the five lines that seat the retirement law itself. The wall already stands:
the rung-drift witness counts all 32 seated marks and reds on any rise, so a new ascending mark
cannot enter without a refusal on the lap it arrives.

The worry this answers is precedent: seventeen thousand living marks teach the elder habit to
every reader even though no guard lets the habit act. Keeping the wall and shrinking the count is
the molt-breach-debride path -- rearchitect the living surfaces while dated testimony keeps every
word it wrote.

Not a standfast, by seated law: a standfast stops the line for something wrong
(`foundations/20260816-214652_standfast-the-stopped-line.md`), and quality-assurance.md refuses
spending red vocabulary on uniformity ("Calling a C+ document a red would spend the word that
stops the line on prose"). This is the LOWE ratchet escalated to a booked campaign on Keaton's
word -- a chapter's remainder, not an emergency.

## What one lap does

One bounded batch (one file, or one small room), one signed local path-limited commit, witnesses
GREEN, session log born on its day's shelf with its index row, then the next lap. The supervisor
signs and sends; the loop itself sends nothing. Any ambiguity parks; any custody question stops.

## The map before the molt

Tier C needs a rung-to-stamp map before a single citation moves: derive each rung's one-clock
stamp from `git log` plus the two ledgers that already narrate them (`docs/STOA.md`,
`lotus/LADDER.md`), written once into a tracked fixture and proven by spot-checks against commit
dates. A citation then converts as `STOA332` -> `the fold speaks (STOA 20260721.HHMMSS)` --
waymark kept as a name, stamp carrying the order. SEATED `20260829.222718` with the campaign:
the spelling is `<plain name> (<WAYMARK> <stamp>)`, exactly as illustrated.

## The tiers, sized from the census

- **Tier A -- 18 stoa-named living filenames** (`linengrow/stoa71_glow_import_seam.rye`,
  17 `tools/s/stoaNNN_*_witness.rish`): path molts -- rename to stamp-and-name, repoint every
  inbound reference in the same commit, module witness GREEN after the move. Small and fully
  mechanical; the campaign's proving ground.
- **Tier B -- the 30 `active-designing/20260814-fill-alesN` briefs**: stampless basenames with
  867 citing files. Fold to `active-designing/date/20260814/` (the day is the basename's first
  eight characters, so the move stays invertible); living citers repoint, dated citers resolve.
- **Tier C -- content citations, rung -> stamp-and-name, per file on touch**: the ~9,200 living
  hits, worked one file per lap from the census's own worklist (top: `glow/lower_shop_gate.rye`
  124, `context/TAME_GUIDANCE.md` 99, `lotus/LADDER.md` 294). Interlocking grep-pins (a witness
  asserting a doc still names STOA130) repair in the same commit or the lap parks. The
  rung-drift ceiling falls with every landed lap.
- **Tier D -- OFFY, the word-gated cut**: dead tissue only (a rung record no living thing cites),
  checkpoint first, each cut named to Keaton before it runs. May never come; a well-prepped
  fossil costs a directory entry.

## The wall

Additions and modifications inside the batch's named room only; renames confined to Tier A/B laps
with the repoint in the same commit; no deletions outside a circled Tier D word; dated basenames
(`[0-9]{8}-[0-9]{6}[_.]`) untouched everywhere -- testimony keeps every mark it wrote, including
every commit body. The loop cannot widen its own wall.

## Close conditions

The rung-drift ceiling has fallen to the campaign's target (Tier A+B alone: roughly 400 fewer;
full Tier C: the count approaches the testimony-citation floor), `grad_seal_witness` GREEN,
`dated_path_witness` GREEN after every fold, and the census re-run showing zero living filenames
carrying a rung mark.

## What would show this wrong

If rung stamps cannot be derived honestly for the elder ladders (a rung with no recoverable
commit), those citations keep their numbers as census marks -- the same ruling REDS %N earned in
`active-designing/20260823-173634_the-census-number-keeps-its-place.md` -- and the campaign
narrows to Tiers A, B, and D. If the 867-citer repoint in Tier B proves heavier than the fold is
worth, Tier B parks and says so. A campaign that cannot fall back is a campaign that forces, and
forcing is the one thing a molt never does.
