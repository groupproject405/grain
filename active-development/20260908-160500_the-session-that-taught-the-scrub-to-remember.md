# The session that taught the scrub to remember -- a progress write

**Language:** EN - **Style:** [Gauge](../context/GAUGE_STYLE.md), Field - **Voice:** Kyri
**Stamp:** `20260908.160500` - **Status:** Landed - **Room:** checkable
**Store:** written for Tablecloth (`brushstroke/tablecloth.rye`, `store_artifact` / `address_hex`),
which addresses an artifact by the SHA-256 of its bytes. This page is that artifact; its address is
computable from the file with `sha256sum`, so the record and its name stay one thing.

## What this session produced, measured

**Guards and readers built, each with a control and a witness on metal:**

| Instrument | What it reads | Proven by |
|---|---|---|
| `rota_declared_scan.sh` | the three answers a log gives about its rota row | 11 planted cases |
| `status_declared_scan.sh` | four buckets of send-status honesty | 11 planted cases |
| `fold_shelf_link_repoint.sh` | applies the repair its sibling scan computes | 12 planted cases |
| `loom_trend.sh` | **3,426 measurements across 1,577 logs**, read for the first time | 13 planted cases |
| `convergence_prove.sh` | a writer run twice: converges, diverges, or **inert** | 12 planted cases |

**Speed won by measurement, never by guessing:**

- `width_check_scan.sh` -- a six-stage grep pipeline became one awk pass. **36.4s to 8.4s**, zero
  disagreements across 1,940 files, and the guard over it fell **75s to 17s**.
- `declared_ceiling_scan.sh` -- `basename` became `${f##*/}`. **52.8s to 39.4s**, byte-identical output.

**Registers seated:** `Bhakta` as the third voice, the **register lattice** giving Radiant a zero
floor and Twilight its poetry, and **redact technique** as siloing pointed the other way -- our
particular made general, so the public seed teaches without exposing.

**The front door rewritten:** `README.md` in Bhakta with Gauge and Twilight, negation **14% to 6%**,
composite 93 to 95, its last negations standing inside a Fuller quotation and a foundation's title
that keep their own words.

## The finding this session ends on

**A publish rescrubs 8,187 files through a 251-rule manifest every time, and the tree already knows
which files changed.** Two publishes twenty minutes apart, differing by one rewritten README, each
paid the full cost and each exceeded the ten-minute foreground bound.

The scrub is a pure function of a file's bytes and its manifest verdict, so its answer is cacheable
by content -- which is precisely what Tablecloth holds. Booked as **REDS `%642`**, designed at
`expanding-prompts/20260908-155715_the-scrub-that-remembers.md`, and standing on one load-bearing
clause: **the sow witness still reads the whole projection before any push.** What caches is the
per-file scrub; the proof stays whole, because a cache that skipped the proof would publish on faith.

## What stands open

- **`%642`** -- the incremental scrub, awaiting Keaton's word and the two measurements the design
  names: what share of a publish the scrub actually is, and how many files change between publishes.
- **The fifteen asks** -- sorted, measured, and rewritten at register 100 in
  `expanding-prompts/20260908-160500_fifteen-asks-sorted-for-the-fleet.md`. The single cheapest
  unblock stays one sentence: are `mand`, `mandi`, and `mandate` one thing?
