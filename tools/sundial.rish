# tools/sundial.rish — accrete shim → tools/gen/chapter/sundial.rish (Generator Chapter s4)
if length args == 0 then let r = run ["rishi/bin/rishi" "run" "tools/gen/chapter/sundial.rish"]
if length args != 0 then let r = run ["rishi/bin/rishi" "run" "tools/gen/chapter/sundial.rish" args[0]]
if r.out != "" then say r.out
exit r.code
