# Floating-nib control -- a card that advertises a hash resolving nowhere.

This file exists to prove the nib-honesty guard's RED path on metal rather than
in prose. It is never the living card and is never read as one. Every token below
resolves nowhere in any object store, so each clause of the recognition rule is
shown from the side that refuses and from the side that walks free.

**Git nib:** `deadbeef00` (a hash no object store here can resolve -- the exact
shape of a floating claim: it looks like a landed edge and names nothing.)

- **POSITION outranks SHAPE (`20260101.000000`, checkpoint `0123456789`).** Ten
  decimal digits standing after a key. A guard reading shape alone would let this
  walk, and one short hash in 99 on this history is exactly this shape, so the
  key decides and the guard refuses.
- **SHAPE gates where no key stands.** Proven in a pen at `abcdef0123` -- bare
  prose, no key before it, and one letter is enough to make it a hash claim.
- **RESOLUTION reads what shape cannot.** A generator planted from
  `seed * 1103515245 % 2147483648` carries two ten-digit runs that answer to no
  key, carry no letter, and resolve nowhere. Both are numbers, so both are read
  and neither is refused -- the whole of REDS %782.

So a working guard refuses exactly three and reads two free. A guard catching
fewer has lost a clause; one catching more has taken a number for a promise.
