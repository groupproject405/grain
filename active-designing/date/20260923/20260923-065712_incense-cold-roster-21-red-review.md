# Cold roster reds, read against the receipt

**Language:** EN
**Stamp:** `20260923.233155`
**Style:** Gauge, Field setting
**Voice:** Kyri
**Status:** Living -- one disposition for each red in the cold pass that closed this evening
**Room:** checkable -- each row names the evidence file the pass wrote

The cold roster launched at `d3ebf81b9e` with `--cadence-slice 1` and closed
`tree_moved=no`, `run_verdict=guard_red`. It ran 371 guards: 326 green, 42 red,
3 gated (`rule_twin` at `%7`, `pond_enclosure_policy` at `%5`,
`pond_enclosure_ephemeral` at `%5`). The transcript is
`session-output/standing-equipment-cold.txt`. Cadence turned
`comlink_rehearsal_wire`, and that guard read green.

The question for each red is the receipt a person can hold: the offer, the
allowed use, the promised return, and the way to carry the record away. A red
that serves that paper is named against it. A red that tends the workshop is
handed to its owner. A red already waiting on Keaton stays booked.

| Guard | Home | Disposition |
|---|---|---|
| `record_version` | receipt | Handoff to Patchouli. Mantra receipt witnesses still carry format names the scan cannot see, including `fixture-signature-v1` on the offer and refusal witnesses. |
| `shared_build_path` | receipt's build | Handoff to Bakery. Fixed tree paths read 1,580 against a ceiling of 1,574. |
| `rye_compiled_reach` | receipt's build | Handoff to Bakery. Bodies stand outside every compiler's reach. |
| `rye_compile_reach` | receipt's build | Handoff to Bakery. The census refused, asserted over its ceiling. |
| `rye_witness_walker` | receipt's build | Handoff to Bakery. Unwalked witnesses stand over the ceiling. |
| `build_target` | receipt's build | Handoff to Bakery. A rostered build lands on a fixed path past its ceiling. |
| `falsifier_form_outcome` | design | Handoff to Diffuser. The scan found 0 ranked pages and the crossing stopped. |
| `ladder_order` | design | Handoff to Diffuser. The proposal page stands somewhere other than where the reading expects it. |
| `verdict_home` | design | Handoff. One elder key, `Grades`, is missing, and the answers side is empty. |
| `commit_parent_claim` | booked | Stays `%826`. A fifth wall in `commit-msg` waits on Keaton's word. |
| `instrument_refusal` | booked | Stays the card's open door. A wall at zero still reads swallowed instrument failures. |
| `seed_link` | booked | Seed face. Three front-door links and 904 other living links point outside the seed, against a ceiling of 820. Publishing stays a gate. |
| `standing_equipment_redleg` | booked, then Copal | `%827` stays with Amphora and Dimeroll for the two shims that crossed 51. `pond_gui_spine` is the new guard with no refusal of its own, and Copal rostered it. |
| `backtick_path` | workshop | Handoff to Grass. Living citations read 85 against a ceiling of 62. |
| `comment_path` | workshop | Handoff to Grass. Living citations read 70 against a ceiling of 66. |
| `radiant_negation` | workshop | Handoff to Petrichor. The law room rose: 37 of 59 enforced pages matched. |
| `fascia_home_link` | workshop | Handoff. Five room READMEs still have no path home, and the ceiling on this reading is 0. The rooms sit outside this seat. |
| `tool_letter_room` | workshop | Handoff. Kyri-resin witnesses sit in `tools/b/` while their names say `k`. |
| `tracked_link` | workshop | Handoff. Tracked symlinks still point at `.bron` leaves a fresh clone lacks. |
| `root_finder` | workshop | Handoff. Five new bare finders stand past the ceiling of 191. Bakery already took the in-lane sites. |
| `ignored_walk` | workshop | Handoff. Tree walks that read what git disowns rose over the ceiling. |
| `say_compose_bound` | workshop | Handoff. A composing site stands past its deferred ceiling. |
| `ceiling_teeth` | workshop | Handoff. The assert-only population spread. |
| `ratchet_slack` | workshop | Handoff. A ceiling is unheld. |
| `awk_lcg_exact` | workshop | Handoff. Unread sites read 21 where the witness expects 17. |
| `rye_module_door` | workshop | Handoff. An authored module opens outside the door form. |
| `alloc_bound_reach` | workshop | Handoff. An allocation names a bound declared nowhere. |
| `shim_reason` | workshop | Handoff. A rostered shim swallows a failure. |
| `link_counted` | workshop | Handoff. One runner still counts paths through a symlink glob. |
| `law_tool_citation` | workshop | Handoff. One cited runner is unrostered. |
| `fold_shelf_link` | workshop | Handoff, and the archive fold below is the place the depth-lost links get their repointer. |
| `fold_shelf_link_repoint` | workshop | Same family. The repointer's own reading is red until `--apply` has a clean tree to offer. |
| `log_has_a_row` | workshop | Ten post-law logs have no shelf row. Elder logs without a row read 216, at the elder ceiling. A later index pass can close the ten. |
| `session_roster_agree` | workshop | The pin and the chapters roster disagree on a day. Same index pass. |
| `living_card_ascii` | workshop | The instrument misread. Advisory non-ASCII lines read 91. Repair waits on the scan's own file list. |
| `ascii_document` | workshop | The living scan misread and stopped. |
| `ascii_resident_agree` | workshop | The two ASCII readers parted, following that misread. |
| `remember_git_nib_write` | workshop | The shape control failed. The card's nib is carried forward in this lap's send. |
| `built_tool_freshness` | workshop | `rishi/bin/rishi` is stale, about 265,868 seconds behind `rishi/src/match.rye`. `rye/bin/rye` read fresh. The rebuild is this pier's, and the binary stays untracked. |
| `query_wire_retention` | workshop | The harness did not run on this host. Disposition waits on the harness's own lines. |
| `pen_release` | workshop | The scan did not run, and it left an empty error. Disposition waits on that error. |
| `standing_equipment` | workshop | The roster summary is red because it is listing these reds. It clears as the rows above clear. |

The three gated guards stay gated. They are custody and enclosure, and this seat leaves them there.

What this seat owes next, from this table, is the archive fold that gives `fold_shelf_link` a clean place to land, and the local `rishi` rebuild that clears `built_tool_freshness` on this pier. The receipt rows belong to Patchouli, Bakery, and Diffuser at their next sittings.
